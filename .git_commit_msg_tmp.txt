修复灵感页堆叠图偏移及多图不显示问题（V8.2）

1. 回滚视口 Box 的 wrapContentWidth/Height(unbounded=true)
   - 修复 V8.1 引入的堆叠图整体向右偏移 bug
   - 视口恢复 fillMaxWidth 固定尺寸，保证 onSizeChanged 报告视口宽准确

2. 行 Box 从 requiredSize 改为自定义 layout{} 强制固定尺寸
   - 绕开 Compose requiredSize 被父级 maxW 约束 coerceIn 压缩的问题
   - 多图（cardCount>4，含角标 11 张）场景不再整行空白
   - 通过 constraints.copy(min=max=请求尺寸) 传入测量 + layout() 强制返回请求宽高

3. import 整理
   - 移除未使用：requiredSize、wrapContentWidth、wrapContentHeight
   - 补充缺失：androidx.compose.ui.layout.layout
