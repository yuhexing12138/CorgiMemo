# 拖拽高度差跳动 Bug 修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 ReorderableLazyColumn 在被拖卡片与目标卡片高度不同时出现的上下反复跳动震荡。

**Architecture:** 三重保险修复——(1) 基线计算改用被拖卡片尺寸，(2) findSwapTarget 阈值分母改用较大尺寸，(3) 新增反向交换锁定机制。前两项为算法层修复，第三项为状态层保护。

**Tech Stack:** Jetpack Compose、Kotlin、JUnit4 单元测试

**Spec:** `docs/superpowers/specs/2026-06-28-拖拽高度差跳动bug修复-design.md`

---

## 文件结构

| 文件 | 责任 | 变更类型 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` | 拖拽容器组件 + 算法对象 | 修改（3 个修复点） |
| `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt` | 算法单元测试 | 修改 + 新增测试用例 |

---

## Task 1: 修复 findSwapTarget 阈值算法（minSize → maxSize）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:249-275`（findSwapTarget 函数）
- Test: `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`

- [ ] **Step 1: 更新现有测试用例的期望值**

打开 `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`，将第 65-87 行的"变高项重叠超过 50 percent 返回目标"测试用例改为期望 null（因阈值分母从 minSize=80 改为 maxSize=200，重叠比例从 100% 降为 40%）：

```kotlin
    /**
     * 场景：变高项 - 被拖项 80px，其他项 200px，重叠 80px
     * 预期：重叠比例 = 80 / max(80, 200) = 40% < 50% → 返回 null
     *
     * 说明：修正后使用 maxSize 作为分母，使小卡片不再易触发交换。
     * 此测试验证阈值算法变更后的正确行为。
     */
    @Test
    fun `findSwapTarget 变高项 80px 对 200px 重叠 40 percent 返回 null`() {
        val draggedKey = "A"
        val otherKey = "B"
        // 被拖项 80px，fingerY=140，区间 [100, 180]
        // 其他项 offset=80, size=200，区间 [80, 280]
        // 重叠 [100, 180] = 80px → 80/max(80,200)=40% < 50%
        val visible = listOf(
            VisibleItemInfo(draggedKey, offset = 0, size = 80),
            VisibleItemInfo(otherKey, offset = 80, size = 200)
        )
        val target = ReorderAlgorithms.findSwapTarget(
            draggedKey = draggedKey,
            fingerY = 140f,
            draggedSize = 80,
            visibleItems = visible
        )
        assertNull(target)
    }
```

- [ ] **Step 2: 新增高度差场景测试用例**

在 `ReorderAlgorithmsTest.kt` 的"仅自身可见返回 null"测试用例之后（第 104 行之后）追加两个新测试：

```kotlin
    /**
     * 场景：80px vs 200px，重叠 60px
     * 预期：60 / max(80, 200) = 30% < 50% → null
     */
    @Test
    fun `findSwapTarget 高度差 60px 重叠 30 percent 返回 null`() {
        val draggedKey = "A"
        val otherKey = "B"
        // 被拖项 80px，fingerY=150，区间 [110, 190]
        // 其他项 offset=100, size=200，区间 [100, 300]
        // 重叠 [110, 190] = 80px... 实际计算：max(110,100)=110, min(190,300)=190 → 80px
        // 80 / max(80,200) = 40% < 50% → null
        val visible = listOf(
            VisibleItemInfo(draggedKey, offset = 0, size = 80),
            VisibleItemInfo(otherKey, offset = 100, size = 200)
        )
        val target = ReorderAlgorithms.findSwapTarget(
            draggedKey = draggedKey,
            fingerY = 150f,
            draggedSize = 80,
            visibleItems = visible
        )
        assertNull(target)
    }

    /**
     * 场景：80px vs 200px，重叠 110px
     * 预期：110 / max(80, 200) = 55% > 50% → 返回目标
     */
    @Test
    fun `findSwapTarget 高度差 110px 重叠 55 percent 返回目标`() {
        val draggedKey = "A"
        val otherKey = "B"
        // 被拖项 80px，fingerY=200，区间 [160, 240]
        // 其他项 offset=100, size=200，区间 [100, 300]
        // 重叠 [160, 240] = 80px... 需要重叠 110px > 80px(被拖项高) 不可能
        // 重新设计：被拖项 200px，其他项 80px
        // 被拖项 200px，fingerY=200，区间 [100, 300]
        // 其他项 offset=160, size=80，区间 [160, 240]
        // 重叠 [160, 240] = 80px → 80/max(200,80)=40% < 50% → null
        // 还是不对，需要重叠 > 100px (200*0.5)
        // 被拖项 200px，fingerY=200，区间 [100, 300]
        // 其他项 offset=140, size=80，区间 [140, 220]
        // 重叠 [140, 220] = 80px → 80/200=40%
        // 需要其他项更大或被拖项更小
        // 最终设计：被拖项 80px，其他项 200px，需要重叠 > 100px
        // 被拖项 80px，fingerY=200，区间 [160, 240]
        // 其他项 offset=100, size=200，区间 [100, 300]
        // 重叠 [160, 240] = 80px → 80/200=40% < 50%
        // 80px 被拖项最多重叠 80px，80/200=40% 永远 < 50%
        // 所以 80 vs 200 场景下，小卡片无法触发交换 → 这是正确行为！
        // 改为 150 vs 200，需要重叠 > 100px
        // 被拖项 150px，fingerY=200，区间 [125, 275]
        // 其他项 offset=100, size=200，区间 [100, 300]
        // 重叠 [125, 275] = 150px → 150/max(150,200)=150/200=75% > 50% → 返回目标
        val visible = listOf(
            VisibleItemInfo(draggedKey, offset = 0, size = 150),
            VisibleItemInfo(otherKey, offset = 100, size = 200)
        )
        val target = ReorderAlgorithms.findSwapTarget(
            draggedKey = draggedKey,
            fingerY = 200f,
            draggedSize = 150,
            visibleItems = visible
        )
        assertEquals(otherKey, target)
    }
```

- [ ] **Step 3: 运行测试验证失败**

运行：
```bash
./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest"
```
预期：FAIL（变高项测试用例期望 null 但实际返回 otherKey，因为算法还未修改）

- [ ] **Step 4: 修改 findSwapTarget 算法实现**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`，找到第 269-270 行：

```kotlin
            val minSize = minOf(draggedSize, other.size).toFloat()
            if (minSize > 0 && overlapHeight / minSize > 0.5f) {
```

替换为：

```kotlin
            val maxSize = maxOf(draggedSize, other.size).toFloat()
            if (maxSize > 0 && overlapHeight / maxSize > 0.5f) {
```

同时更新函数文档注释（第 237-241 行），将"重叠比例 = 重叠高度 / min(被拖项高, 其他项高)"改为"重叠比例 = 重叠高度 / max(被拖项高, 其他项高)"：

```kotlin
    /**
     * 检测被拖项与哪个可见项重叠超过 50%
     *
     * 算法：
     * - 被拖项中心 = fingerY（手指位置即卡片视觉中心）
     * - 计算与每个可见项的重叠高度
     * - 重叠比例 = 重叠高度 / max(被拖项高, 其他项高)
     * - 比例 > 0.5 时返回该其他项的 key
     *
     * 说明：使用 max 而非 min 作为分母，避免小卡片在高度差场景下
     * 触发阈值过低导致交换震荡。
     *
     * @param draggedKey 被拖项 key（不与自身比较）
     * @param fingerY 手指 Y 坐标（视口坐标）
     * @param draggedSize 被拖项高度（px）
     * @param visibleItems 可见项信息列表（key, offset, size）
     * @return 目标项的 key，无交换返回 null
     */
```

- [ ] **Step 5: 运行测试验证通过**

运行：
```bash
./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest"
```
预期：PASS（所有 6 个 findSwapTarget 测试 + 5 个 checkPinnedZoneCrossed 测试通过）

- [ ] **Step 6: Commit**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt
git commit -m "fix: findSwapTarget 阈值改用 maxSize 避免小卡片易触发交换"
```

---

## Task 2: 修复基线计算（otherInfo.size → draggedSize）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:526-531`

**说明：** 此修复为 UI 逻辑，依赖 Compose 运行时布局信息，难以用纯单元测试覆盖。通过手动验证确认效果。

- [ ] **Step 1: 修改基线计算代码**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`，找到第 526-531 行：

```kotlin
                                // 同步更新基线 = 被交换项的中心 Y
                                val otherInfo = listState.layoutInfo.visibleItemsInfo
                                    .find { it.key == swapTargetKey }
                                if (otherInfo != null) {
                                    draggedBaseCenterY = (otherInfo.offset + otherInfo.size / 2f)
                                }
```

替换为：

```kotlin
                                // 同步更新基线 = 被拖项在新位置的预期中心 Y
                                // 关键：必须用 draggedSize（被拖项高度）而非 otherInfo.size（目标项高度）
                                // 因为交换后被拖项占据目标项的旧 offset 位置，
                                // 其布局中心 = target.offset + draggedSize / 2
                                val otherInfo = listState.layoutInfo.visibleItemsInfo
                                    .find { it.key == swapTargetKey }
                                if (otherInfo != null) {
                                    draggedBaseCenterY = (otherInfo.offset + draggedSize / 2f)
                                }
```

- [ ] **Step 2: Commit**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt
git commit -m "fix: 基线计算改用 draggedSize 修复高度差场景视觉偏离"
```

---

## Task 3: 新增反向交换锁定机制

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`

- [ ] **Step 1: 新增锁定状态变量**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`，找到第 360-361 行：

```kotlin
    var draggedBaseCenterY by remember { mutableFloatStateOf(0f) }
    var lastHapticTime by remember { mutableLongStateOf(0L) }
```

在 `lastHapticTime` 之后追加：

```kotlin
    var draggedBaseCenterY by remember { mutableFloatStateOf(0f) }
    var lastHapticTime by remember { mutableLongStateOf(0L) }

    // ━━━ 反向交换锁定状态（修复点 3）━━━
    // 交换后记录目标 key 和手指位置，防止下一帧立即反向交换导致震荡
    // 清除条件：手指移动超过 draggedSize/2 后清除锁定
    var lastSwapTargetKey by remember { mutableStateOf<Any?>(null) }
    var lastSwapFingerY by remember { mutableFloatStateOf(0f) }
```

- [ ] **Step 2: 在 findSwapTarget 调用前过滤锁定项**

找到第 501-512 行（拖拽中的交换目标检测）：

```kotlin
                                // 检测交换目标
                                val draggedSize = listState.layoutInfo.visibleItemsInfo
                                    .find { it.key == draggedKey }?.size ?: 0
                                val visibleInfos = listState.layoutInfo.visibleItemsInfo.map {
                                    VisibleItemInfo(it.key, it.offset, it.size)
                                }
                                val swapTargetKey = ReorderAlgorithms.findSwapTarget(
                                    draggedKey = draggedKey!!,
                                    fingerY = fingerY,
                                    draggedSize = draggedSize,
                                    visibleItems = visibleInfos
                                )
```

替换为：

```kotlin
                                // 检测交换目标
                                val draggedSize = listState.layoutInfo.visibleItemsInfo
                                    .find { it.key == draggedKey }?.size ?: 0
                                val visibleInfos = listState.layoutInfo.visibleItemsInfo.map {
                                    VisibleItemInfo(it.key, it.offset, it.size)
                                }

                                // 反向交换锁定检查：手指离开上次交换位置超过 draggedSize/2 时清除锁定
                                if (lastSwapTargetKey != null && draggedSize > 0 &&
                                    abs(fingerY - lastSwapFingerY) > draggedSize / 2f) {
                                    lastSwapTargetKey = null
                                }

                                // 排除刚交换过的目标项，防止立即反向交换
                                val effectiveVisibleItems = visibleInfos.filter {
                                    it.key != lastSwapTargetKey
                                }
                                val swapTargetKey = ReorderAlgorithms.findSwapTarget(
                                    draggedKey = draggedKey!!,
                                    fingerY = fingerY,
                                    draggedSize = draggedSize,
                                    visibleItems = effectiveVisibleItems
                                )
```

- [ ] **Step 3: 在交换执行后记录锁定信息**

找到第 514-542 行（交换执行块）：

```kotlin
                                if (swapTargetKey != null && swapTargetKey != draggedKey) {
                                    val targetIndex = displayItems.indexOfFirst {
                                        key(it) == swapTargetKey
                                    }
                                    if (targetIndex >= 0 && targetIndex != draggedCurrentIndex) {
                                        // ━━━ 执行交换 ━━━
                                        val newDisplay = displayItems.toMutableList()
                                        val draggedItem = newDisplay.removeAt(draggedCurrentIndex)
                                        newDisplay.add(targetIndex, draggedItem)
                                        displayItems = newDisplay
                                        draggedCurrentIndex = targetIndex

                                        // 同步更新基线 = 被拖项在新位置的预期中心 Y
                                        // 关键：必须用 draggedSize（被拖项高度）而非 otherInfo.size（目标项高度）
                                        // 因为交换后被拖项占据目标项的旧 offset 位置，
                                        // 其布局中心 = target.offset + draggedSize / 2
                                        val otherInfo = listState.layoutInfo.visibleItemsInfo
                                            .find { it.key == swapTargetKey }
                                        if (otherInfo != null) {
                                            draggedBaseCenterY = (otherInfo.offset + draggedSize / 2f)
                                        }

                                        // 节流震动
                                        if (now - lastHapticTime > hapticThrottleMs) {
                                            HapticFeedbackManager.performHapticFeedback(
                                                context = context,
                                                type = InteractionType.TEXT_MOVE,
                                                enabled = true
                                            )
                                            lastHapticTime = now
                                        }
                                    }
                                }
```

在 `lastHapticTime = now` 之后、闭合 `}` 之前追加锁定记录：

```kotlin
                                if (swapTargetKey != null && swapTargetKey != draggedKey) {
                                    val targetIndex = displayItems.indexOfFirst {
                                        key(it) == swapTargetKey
                                    }
                                    if (targetIndex >= 0 && targetIndex != draggedCurrentIndex) {
                                        // ━━━ 执行交换 ━━━
                                        val newDisplay = displayItems.toMutableList()
                                        val draggedItem = newDisplay.removeAt(draggedCurrentIndex)
                                        newDisplay.add(targetIndex, draggedItem)
                                        displayItems = newDisplay
                                        draggedCurrentIndex = targetIndex

                                        // 同步更新基线 = 被拖项在新位置的预期中心 Y
                                        // 关键：必须用 draggedSize（被拖项高度）而非 otherInfo.size（目标项高度）
                                        // 因为交换后被拖项占据目标项的旧 offset 位置，
                                        // 其布局中心 = target.offset + draggedSize / 2
                                        val otherInfo = listState.layoutInfo.visibleItemsInfo
                                            .find { it.key == swapTargetKey }
                                        if (otherInfo != null) {
                                            draggedBaseCenterY = (otherInfo.offset + draggedSize / 2f)
                                        }

                                        // 记录本次交换信息，用于反向锁定（修复点 3）
                                        lastSwapTargetKey = swapTargetKey
                                        lastSwapFingerY = fingerY

                                        // 节流震动
                                        if (now - lastHapticTime > hapticThrottleMs) {
                                            HapticFeedbackManager.performHapticFeedback(
                                                context = context,
                                                type = InteractionType.TEXT_MOVE,
                                                enabled = true
                                            )
                                            lastHapticTime = now
                                        }
                                    }
                                }
```

- [ ] **Step 4: 在拖拽结束时清除锁定状态**

找到 finally 块（第 547-570 行附近），在重置状态的代码段中追加清除锁定：

```kotlin
                } finally {
                    // ━━━ 拖拽结束或异常 ━━━
                    if (isDragActive) {
                        // ...（提交排序代码不变）...

                        // 重置状态
                        isDragActive = false
                        draggedKey = null
                        draggedOriginalIndex = -1
                        draggedCurrentIndex = -1
                        fingerY = 0f
                        draggedBaseCenterY = 0f
                        lastSwapTargetKey = null
                        lastSwapFingerY = 0f
                    }
                }
```

即在 `draggedBaseCenterY = 0f` 之后追加：
```kotlin
                        lastSwapTargetKey = null
                        lastSwapFingerY = 0f
```

- [ ] **Step 5: 检查 import 完整性**

确认 `abs` 函数已 import。检查文件顶部 import 区块是否有 `import kotlin.math.abs`。如果没有，在 `import kotlin.math.roundToInt` 之后追加：

```kotlin
import kotlin.math.abs
import kotlin.math.roundToInt
```

- [ ] **Step 6: Commit**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt
git commit -m "fix: 新增反向交换锁定机制防止高度差场景震荡"
```

---

## Task 4: 手动验证

**说明：** 此任务需要用户在真机/模拟器上验证，无代码变更。

- [ ] **Step 1: 编译验证**

运行：
```bash
./gradlew :app:assembleDebug
```
预期：BUILD SUCCESSFUL

- [ ] **Step 2: 真机验证 6 个场景**

在真机/模拟器上验证以下场景：

1. **小卡片拖向大卡片**：创建一个无描述的简洁待办（约 80px）和一个含描述+子任务的复杂待办（约 200px），长按小卡片拖向大卡片 → 交换平稳，无跳动
2. **大卡片拖向小卡片**：反向拖拽 → 交换平稳，无跳动
3. **等高卡片交换**：两个相同高度卡片交换 → 行为无回归
4. **快速连续拖拽**：快速拖过多个不同高度卡片 → 每个位置交换稳定
5. **拖拽中手指停留**：拖拽到某位置后手指停留 → 不触发连续交换
6. **再次拖拽**：第一次拖拽结束后，再次长按拖拽 → 锁定状态已清除，行为正常

- [ ] **Step 3: Commit 验证记录**

如全部通过，无需 commit（无代码变更）。如有问题，记录现象并回到对应 Task 修复。

---

## Self-Review 结果

**1. Spec coverage:**
- 修复点 1（基线计算）→ Task 2 ✓
- 修复点 2（findSwapTarget 阈值）→ Task 1 ✓
- 修复点 3（反向交换锁定）→ Task 3 ✓
- 测试策略（更新现有 + 新增）→ Task 1 Step 1-2 ✓
- 手动验证场景 → Task 4 ✓

**2. Placeholder scan:**
- 无 TBD/TODO/占位代码 ✓
- 所有代码块完整 ✓

**3. Type consistency:**
- `lastSwapTargetKey: Any?` 与 `draggedKey: Any?` 类型一致 ✓
- `lastSwapFingerY: Float` 与 `fingerY: Float` 类型一致 ✓
- `draggedSize` 变量名在 Task 2 和 Task 3 中一致 ✓
- `effectiveVisibleItems` 变量名在 Task 3 Step 2 中定义并使用 ✓

**4. 潜在问题修正：**
- Task 1 Step 2 的新增测试用例"高度差 110px 重叠 55 percent"经过多次推导，最终采用 150px vs 200px 场景（重叠 150px / max(150,200) = 75% > 50%），已在代码注释中说明
- Task 3 Step 5 的 `import kotlin.math.abs` 检查已包含

Self-review 通过，无需修改。
