# 已完成区二次拖拽跳跃修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `onMove` 允许已完成项跨 CompletedDivider 被 onMove 移到 pending 区，导致 ViewModel 误判跨区将 status 改为 0 的位置跳跃 bug；同时保留双向跨区拖拽能力。

**Architecture:** 在 `ReorderableLazyColumn` 的 `onMove` 中阻止跨 CompletedDivider 移动并记录 `attemptedCrossDivider` 标志；在 `onDragStopped` 中通过"attemptedCrossDivider + 被拖项在 divider 紧邻位置"双重检查触发跨区，调用 `onReorder` 时把 `toIndex` 设为 divider 另一侧紧邻位置。`onDragStarted` 中重置标志。

**Tech Stack:** Kotlin、Jetpack Compose 1.9.2、JUnit 4、Gradle

---

## File Structure

| 文件 | 职责 | 修改类型 |
|------|------|----------|
| `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` | 拖拽状态、onMove、onDragStarted、onDragStopped | 新增状态变量 + 修改 3 个回调 |
| `app/src/test/java/com/corgimemo/app/viewmodel/HomeViewModelReorderTest.kt` | ViewModel 层测试 | 已新增 2 个测试（已完成） |

---

## Task 1: 新增 `attemptedCrossDivider` 状态变量

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:258`

- [ ] **Step 1: 在拖拽状态区新增 `attemptedCrossDivider`**

将 [ReorderableLazyColumn.kt:253-258](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt#L253-258) 中：

```kotlin
    // ━━━ 拖拽状态（从 15+ 精简到 5 个）━━━
    var displayItems by remember { mutableStateOf(items) }
    var isDragActive by remember { mutableStateOf(false) }
    var draggedOriginalIndex by remember { mutableIntStateOf(-1) }
    var draggedOriginalIsPinned by remember { mutableStateOf(false) }
    var crossedPinnedZone by remember { mutableStateOf(false) }
```

替换为：

```kotlin
    // ━━━ 拖拽状态（从 15+ 精简到 5 个）━━━
    var displayItems by remember { mutableStateOf(items) }
    var isDragActive by remember { mutableStateOf(false) }
    var draggedOriginalIndex by remember { mutableIntStateOf(-1) }
    var draggedOriginalIsPinned by remember { mutableStateOf(false) }
    var crossedPinnedZone by remember { mutableStateOf(false) }
    /** 是否尝试跨 CompletedDivider（onMove 阻止跨 divider 时记录，onDragStopped 据此触发跨区） */
    var attemptedCrossDivider by remember { mutableStateOf(false) }
```

- [ ] **Step 2: 验证编译通过**

Run: `.\gradlew.bat :app:compileDebugKotlin`

Expected: BUILD SUCCESSFUL

---

## Task 2: 修改 onMove 阻止跨 divider 移动

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:335-360`

- [ ] **Step 1: 在 onMove 中新增跨 divider 检查**

将 [ReorderableLazyColumn.kt:335-360](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt#L335-360) 中的 `onMove`：

```kotlin
        onMove = { from, to ->
            // ① 校验 from/to 是否指向不可拖拽项（CompletedDivider）
            val fromItem = displayItems.getOrNull(from.index)
            val toItem = displayItems.getOrNull(to.index)
            if (fromItem == null || toItem == null) return@rememberReorderableLazyListState
            if (!isDraggable(fromItem) || !isDraggable(toItem)) {
                return@rememberReorderableLazyListState
            }

            // ② 重排 displayItems
            val newDisplay = displayItems.toMutableList()
            val draggedItem = newDisplay.removeAt(from.index)
            newDisplay.add(to.index, draggedItem)
            displayItems = newDisplay

            // ③ 触觉反馈（节流）
            val now = System.currentTimeMillis()
            if (now - lastHapticTime > hapticThrottleMs) {
                HapticFeedbackManager.performHapticFeedback(
                    context = context,
                    type = InteractionType.TEXT_MOVE,
                    enabled = true
                )
                lastHapticTime = now
            }
        }
```

替换为：

```kotlin
        onMove = { from, to ->
            // ① 校验 from/to 是否指向不可拖拽项（CompletedDivider）
            val fromItem = displayItems.getOrNull(from.index)
            val toItem = displayItems.getOrNull(to.index)
            if (fromItem == null || toItem == null) return@rememberReorderableLazyListState
            if (!isDraggable(fromItem) || !isDraggable(toItem)) {
                return@rememberReorderableLazyListState
            }

            // ② [新增] 阻止跨 CompletedDivider 移动，但记录尝试意图
            //    原因：库的 findTargetItem 会跳过 divider，把 divider 后面的项作为 targetItem，
            //    导致 onMove 跨过 divider，引发 ViewModel 误判跨区将 status 改为 0。
            //    修复：onMove 阻止跨 divider，保留被拖项在原区域；
            //         onDragStopped 中通过 attemptedCrossDivider + 紧邻位置检查触发跨区。
            if (dividerIndex >= 0) {
                val minIdx = minOf(from.index, to.index)
                val maxIdx = maxOf(from.index, to.index)
                if (minIdx < dividerIndex && maxIdx > dividerIndex) {
                    attemptedCrossDivider = true
                    return@rememberReorderableLazyListState
                }
            }

            // ③ 重排 displayItems
            val newDisplay = displayItems.toMutableList()
            val draggedItem = newDisplay.removeAt(from.index)
            newDisplay.add(to.index, draggedItem)
            displayItems = newDisplay

            // ④ 触觉反馈（节流）
            val now = System.currentTimeMillis()
            if (now - lastHapticTime > hapticThrottleMs) {
                HapticFeedbackManager.performHapticFeedback(
                    context = context,
                    type = InteractionType.TEXT_MOVE,
                    enabled = true
                )
                lastHapticTime = now
            }
        }
```

- [ ] **Step 2: 验证编译通过**

Run: `.\gradlew.bat :app:compileDebugKotlin`

Expected: BUILD SUCCESSFUL

---

## Task 3: 修改 onDragStarted 重置 attemptedCrossDivider

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:718-736`

- [ ] **Step 1: 在 onDragStarted 中重置 attemptedCrossDivider**

将 [ReorderableLazyColumn.kt:724-736](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt#L724-736) 中的 `onDragStarted`：

```kotlin
                                    onDragStarted = {
                                        // 注意：draggedOriginalIndex 与 draggedOriginalIsPinned 必须用 displayItems 计算，
                                        // 与 onDragStopped 中 draggedCurrentIndex 的参照系保持一致。
                                        // 原因：onDragStarted lambda 可能捕获旧的 item（因 longPressDraggableHandle
                                        // 的 pointerInput 未因 items 变化而重启），导致 isPinned(item) 返回旧值，
                                        // 进而引发 "已置顶项二次拖拽时被误判为跨区" 的位置跳跃。
                                        isDragActive = true
                                        val draggedIdx = displayItems.indexOfFirst {
                                            key(it) == key(item)
                                        }
                                        draggedOriginalIndex = draggedIdx
                                        draggedOriginalIsPinned = if (draggedIdx >= 0) isPinned(displayItems[draggedIdx]) else false
                                        crossedPinnedZone = false
                                        HapticFeedbackManager.performHapticFeedback(
                                            context = context,
                                            type = InteractionType.TEXT_MOVE,
                                            enabled = true
                                        )
                                    },
```

替换为：

```kotlin
                                    onDragStarted = {
                                        // 注意：draggedOriginalIndex 与 draggedOriginalIsPinned 必须用 displayItems 计算，
                                        // 与 onDragStopped 中 draggedCurrentIndex 的参照系保持一致。
                                        // 原因：onDragStarted lambda 可能捕获旧的 item（因 longPressDraggableHandle
                                        // 的 pointerInput 未因 items 变化而重启），导致 isPinned(item) 返回旧值，
                                        // 进而引发 "已置顶项二次拖拽时被误判为跨区" 的位置跳跃。
                                        isDragActive = true
                                        val draggedIdx = displayItems.indexOfFirst {
                                            key(it) == key(item)
                                        }
                                        draggedOriginalIndex = draggedIdx
                                        draggedOriginalIsPinned = if (draggedIdx >= 0) isPinned(displayItems[draggedIdx]) else false
                                        crossedPinnedZone = false
                                        attemptedCrossDivider = false
                                        HapticFeedbackManager.performHapticFeedback(
                                            context = context,
                                            type = InteractionType.TEXT_MOVE,
                                            enabled = true
                                        )
                                    },
```

- [ ] **Step 2: 验证编译通过**

Run: `.\gradlew.bat :app:compileDebugKotlin`

Expected: BUILD SUCCESSFUL

---

## Task 4: 修改 onDragStopped 触发跨区

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:737-767`

- [ ] **Step 1: 在 onDragStopped 中新增跨 divider 触发分支**

将 [ReorderableLazyColumn.kt:737-767](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt#L737-767) 中的 `onDragStopped`：

```kotlin
                                    onDragStopped = {
                                        val draggedCurrentIndex = displayItems.indexOfFirst {
                                            key(it) == key(item)
                                        }
                                        if (draggedOriginalIndex >= 0 &&
                                            draggedOriginalIndex != draggedCurrentIndex &&
                                            draggedCurrentIndex >= 0
                                        ) {
                                            crossedPinnedZone = ReorderAlgorithms.checkPinnedZoneCrossed(
                                                displayItems = displayItems,
                                                isPinned = isPinned,
                                                dividerKind = dividerKind,
                                                draggedOriginalIsPinned = draggedOriginalIsPinned,
                                                draggedCurrentIndex = draggedCurrentIndex
                                            )
                                            onReorder(
                                                draggedOriginalIndex,
                                                draggedCurrentIndex,
                                                dividerIndex,
                                                crossedPinnedZone
                                            )
                                            HapticFeedbackManager.performHapticFeedback(
                                                context = context,
                                                type = InteractionType.CONFIRM,
                                                enabled = true
                                            )
                                        }
                                        isDragActive = false
                                        draggedOriginalIndex = -1
                                        crossedPinnedZone = false
                                    }
```

替换为：

```kotlin
                                    onDragStopped = {
                                        val draggedCurrentIndex = displayItems.indexOfFirst {
                                            key(it) == key(item)
                                        }

                                        // [新增] 跨 CompletedDivider 检测：
                                        // 如果 onMove 期间尝试跨 divider（被阻止），
                                        // 且被拖项当前在 divider 紧邻位置，触发跨区。
                                        var crossDividerTriggered = false
                                        if (attemptedCrossDivider && dividerIndex >= 0 &&
                                            draggedOriginalIndex >= 0 && draggedCurrentIndex >= 0
                                        ) {
                                            val originalInPending = draggedOriginalIndex < dividerIndex
                                            // 双重检查：尝试跨 divider + 被拖项在 divider 紧邻位置
                                            val isAdjacentToDivider =
                                                draggedCurrentIndex == dividerIndex - 1 ||
                                                draggedCurrentIndex == dividerIndex + 1
                                            if (isAdjacentToDivider) {
                                                // 跨区目标位置：divider 另一侧紧邻位置
                                                val effectiveToIndex = if (originalInPending) {
                                                    dividerIndex + 1
                                                } else {
                                                    dividerIndex - 1
                                                }

                                                crossedPinnedZone = ReorderAlgorithms.checkPinnedZoneCrossed(
                                                    displayItems = displayItems,
                                                    isPinned = isPinned,
                                                    dividerKind = dividerKind,
                                                    draggedOriginalIsPinned = draggedOriginalIsPinned,
                                                    draggedCurrentIndex = effectiveToIndex
                                                )
                                                onReorder(
                                                    draggedOriginalIndex,
                                                    effectiveToIndex,
                                                    dividerIndex,
                                                    crossedPinnedZone
                                                )
                                                HapticFeedbackManager.performHapticFeedback(
                                                    context = context,
                                                    type = InteractionType.CONFIRM,
                                                    enabled = true
                                                )
                                                crossDividerTriggered = true
                                            }
                                            // 如果 attemptedCrossDivider=true 但不在紧邻位置，回退到原逻辑
                                        }

                                        // 原逻辑：同区域内拖拽（仅在未触发跨区时执行）
                                        if (!crossDividerTriggered &&
                                            draggedOriginalIndex >= 0 &&
                                            draggedOriginalIndex != draggedCurrentIndex &&
                                            draggedCurrentIndex >= 0
                                        ) {
                                            crossedPinnedZone = ReorderAlgorithms.checkPinnedZoneCrossed(
                                                displayItems = displayItems,
                                                isPinned = isPinned,
                                                dividerKind = dividerKind,
                                                draggedOriginalIsPinned = draggedOriginalIsPinned,
                                                draggedCurrentIndex = draggedCurrentIndex
                                            )
                                            onReorder(
                                                draggedOriginalIndex,
                                                draggedCurrentIndex,
                                                dividerIndex,
                                                crossedPinnedZone
                                            )
                                            HapticFeedbackManager.performHapticFeedback(
                                                context = context,
                                                type = InteractionType.CONFIRM,
                                                enabled = true
                                            )
                                        }
                                        isDragActive = false
                                        draggedOriginalIndex = -1
                                        crossedPinnedZone = false
                                        attemptedCrossDivider = false
                                    }
```

- [ ] **Step 2: 验证编译通过**

Run: `.\gradlew.bat :app:compileDebugKotlin`

Expected: BUILD SUCCESSFUL

---

## Task 5: 回归测试 - 算法层 + ViewModel 层

**Files:**
- Test: `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`
- Test: `app/src/test/java/com/corgimemo/app/viewmodel/HomeViewModelReorderTest.kt`

- [ ] **Step 1: 运行算法层全部测试**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest"`

Expected: 20 tests PASSED（无回归）

- [ ] **Step 2: 运行 ViewModel 层全部测试**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.viewmodel.HomeViewModelReorderTest"`

Expected: 全部 PASSED（含新增 2 个测试）

---

## Task 6: 提交修复

**Files:**
- All modified files

- [ ] **Step 1: 检查 git status**

Run: `git status`

Expected: 显示 `ReorderableLazyColumn.kt` 修改

- [ ] **Step 2: 添加修改的文件**

Run: `git add app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`

- [ ] **Step 3: 提交修复（使用临时文件传递 commit message）**

创建临时文件 `COMMIT_MSG_TEMP.txt`，内容：

```
fix(reorder): 修复已完成区二次拖拽跳跃回待完成区 bug

修改 ReorderableLazyColumn 的 onMove 和 onDragStopped：

1. onMove：阻止跨 CompletedDivider 移动，记录 attemptedCrossDivider 标志
   原因：库的 findTargetItem 会跳过 divider，把 divider 后面的项作为
   targetItem，导致 onMove 跨过 divider，引发 ViewModel 误判跨区将
   status 改为 0，被拖项跳跃回待完成区。

2. onDragStopped：基于 attemptedCrossDivider + 紧邻位置双重检查触发跨区
   - 检查 attemptedCrossDivider=true（onMove 期间尝试过跨 divider）
   - 检查被拖项当前在 divider 紧邻位置（dividerIndex ± 1）
   - 满足时把 toIndex 设为 divider 另一侧紧邻位置，触发跨区

3. onDragStarted：重置 attemptedCrossDivider=false

修复后：
- 同区域内拖拽不再误跨区（onMove 阻止跨 divider）
- 双向跨区可用（pending→completed 和 completed→pending）
- 误触发缓解（拖回原区域不触发跨区）

新增 2 个 ViewModel 层测试验证 ViewModel 层逻辑正确。
```

然后运行：

Run: `git commit -F COMMIT_MSG_TEMP.txt; Remove-Item COMMIT_MSG_TEMP.txt`

Expected: 提交成功

---

## Self-Review

### 1. Spec coverage

| Spec 章节 | 对应 Task |
|----------|-----------|
| §3.3.1 新增 attemptedCrossDivider 状态变量 | Task 1 |
| §3.3.2 onMove 修改（跨 divider 检查） | Task 2 |
| §3.3.3 onDragStarted 重置（隐含在 §7.1） | Task 3 |
| §3.3.3 onDragStopped 修改（跨区触发） | Task 4 |
| §5.3 算法层回归测试 | Task 5 Step 1 |
| §5.3 ViewModel 层回归测试 | Task 5 Step 2 |
| §8 验收标准 1-3 | Task 5 |
| §8 验收标准 4-6（手动验证） | 不在 plan 内（用户手动） |

### 2. Placeholder scan

- 无 "TBD"、"TODO"、"implement later"
- 所有代码块均完整（含完整 onMove、onDragStarted、onDragStopped 实现）
- 所有命令均给出具体执行内容和期望输出

### 3. Type consistency

- `attemptedCrossDivider` 在 Task 1、Task 2、Task 3、Task 4 中均使用相同名称
- `dividerIndex` 参数名与原代码一致
- `onReorder`、`checkPinnedZoneCrossed` 签名未改变
- `crossDividerTriggered` 局部变量名在 Task 4 中一致使用

### 4. 备注

- Task 1-4 是顺序依赖：状态变量 → onMove → onDragStarted → onDragStopped
- 每个 Task 都有编译验证步骤，确保修改不破坏编译
- Task 5 是回归测试，确保不引入回归
- Task 6 提交信息使用中文（遵循项目规则 `.trae/rules/git提交.md`）
- 手动验证（§8.4-8.6）由用户在真机或模拟器上执行，不在 plan 内
- Commit message 通过临时文件传递，避免 PowerShell 转义问题
