# 日期区骨架屏适配最新 UI 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重写 `SpecialDateSkeleton.kt` 使其与日期页最新 UI（3 段分组 + 置顶卡 + 3 列卡片）完全对齐，消除骨架屏到真实内容的视觉跳变。

**Architecture:** 在单一文件 `SpecialDateSkeleton.kt` 内集中实现新版骨架屏：复用 `Skeleton.kt` 通用组件（`SkeletonSectionHeader` / `SkeletonCircle` / `SkeletonText`），新增 `DateItemSkeleton` 与 `PinnedDateItemSkeleton` 两个私有 Composable 分别模拟 `SpecialDateCard` 与 `PinnedDateCard` 的 3 列布局；分组颜色硬编码（与 `DateSectionHeader.kt:39-43` 保持同步），不引入新颜色对象。

**Tech Stack:** Jetpack Compose（BOM 2026.04.01 + Compose 1.9.2）、Material3、LazyColumn

**Spec 参考:** `docs/superpowers/specs/2026-07-14-日期区骨架屏适配最新UI-design.md`

---

## 文件结构

本次仅修改 **1 个文件**：

| 文件 | 职责 |
|------|------|
| `app/src/main/java/com/corgimemo/app/ui/screens/date/SpecialDateSkeleton.kt` | 日期页骨架屏；签名 `fun SpecialDateSkeleton(modifier: Modifier = Modifier)` 不变；内部完全重写 |

**不修改** `Skeleton.kt`、`SpecialDateCard.kt`、`PinnedDateCard.kt`、`DateSectionHeader.kt`、`SpecialDateScreen.kt`（签名/调用方未变）。

---

## Task 1: 重写 SpecialDateSkeleton.kt

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/SpecialDateSkeleton.kt`（完全替换）

### 子任务分解

按"自下而上"顺序：先实现两个私有 Composable，再重写主函数 `SpecialDateSkeleton`。

---

### Task 1.1: 实现 `PinnedDateItemSkeleton` 私有 Composable

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/SpecialDateSkeleton.kt`（先添加此 Composable，其他 Composable 后续追加）

- [ ] **Step 1: 在文件顶部添加所需 imports**

在 `package` 声明后追加（与现有 imports 合并去重）：

```kotlin
import androidx.compose.foundation.layout.Arrangement
import androidx.compose.foundation.layout.Box
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Row
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.layout.size
import androidx.compose.foundation.layout.widthIn
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Card
import androidx.compose.material3.CardDefaults
import androidx.compose.material3.MaterialTheme
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import com.corgimemo.app.ui.components.SkeletonCircle
import com.corgimemo.app.ui.components.SkeletonText
```

保留现有的 `SkeletonSearchBar` import，删除不再使用的 `SkeletonButton` import（若仍不需要）。

- [ ] **Step 2: 添加 `PinnedDateItemSkeleton` 私有 Composable**

在主函数 `SpecialDateSkeleton` 之后追加（与原 `DateItemSkeleton` 同级）：

```kotlin
/**
 * 置顶日期卡片骨架（新增于 2026-07-14）
 *
 * 模拟 [com.corgimemo.app.ui.screens.date.components.PinnedDateCard] 的三区布局：
 * - 左区(widthIn min=70dp)：标题 + 大数字占位
 * - 中区(weight=1f)：单行倒数占位
 * - 右区：56dp 圆形头像占位
 *
 * 容器用 Card(surface 色 + 2dp elevation) 而非 SkeletonCard，
 * 原因：与真实 PinnedDateCard 的容器视觉完全一致，避免骨架→真实内容的颜色跳变。
 */
@Composable
private fun PinnedDateItemSkeleton() {
    Card(
        shape = RoundedCornerShape(20.dp),
        elevation = CardDefaults.cardElevation(defaultElevation = 2.dp),
        colors = CardDefaults.cardColors(containerColor = MaterialTheme.colorScheme.surface),
        modifier = Modifier.fillMaxWidth()
    ) {
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(16.dp),
            verticalAlignment = Alignment.CenterVertically
        ) {
            // 左区：标题 + 大数字（min 70dp，与真实卡一致）
            Column(modifier = Modifier.widthIn(min = 70.dp)) {
                // 标题（16sp Medium 占位）
                SkeletonText(
                    width = 0.4f,
                    height = 16.dp
                )
                // 大数字（40sp Bold 的视觉占比 → 32dp 高度）
                SkeletonText(
                    width = 0.5f,
                    height = 32.dp,
                    modifier = Modifier.padding(top = 4.dp)
                )
            }

            // 中区：单行倒数（14sp Medium 占位）
            Box(
                modifier = Modifier
                    .weight(1f)
                    .padding(horizontal = 8.dp),
                contentAlignment = Alignment.Center
            ) {
                SkeletonText(
                    width = 0.6f,
                    height = 14.dp
                )
            }

            // 右区：56dp 圆形头像（与真实卡一致）
            SkeletonCircle(size = 56.dp)
        }
    }
}
```

---

### Task 1.2: 重写 `DateItemSkeleton` 私有 Composable

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/SpecialDateSkeleton.kt`（替换原 `DateItemSkeleton`）

- [ ] **Step 1: 定位并删除原 `DateItemSkeleton`**

删除文件内现有的 `DateItemSkeleton` 函数（原为 `private fun DateItemSkeleton(color: androidx.compose.ui.graphics.Color)`），同时删除它依赖的 `color` 参数（新版不再使用颜色参数）。

- [ ] **Step 2: 添加新版 `DateItemSkeleton`**

在 `PinnedDateItemSkeleton` 之后追加：

```kotlin
/**
 * 普通日期卡片骨架（重构于 2026-07-14）
 *
 * 模拟 [com.corgimemo.app.ui.screens.date.components.SpecialDateCard] 的 3 列布局：
 * - 左：48dp 圆形图片区
 * - 中(weight=1f)：标题(16dp) + 时间信息(14dp)
 * - 右：大数字(22dp) + 单位(11dp) 两行
 *
 * 容器用 Card(surface 色 + 2dp elevation) 而非 SkeletonCard，
 * 原因：与真实 SpecialDateCard 的容器视觉完全一致。
 */
@Composable
private fun DateItemSkeleton() {
    Card(
        shape = RoundedCornerShape(20.dp),
        elevation = CardDefaults.cardElevation(defaultElevation = 2.dp),
        colors = CardDefaults.cardColors(containerColor = MaterialTheme.colorScheme.surface),
        modifier = Modifier.fillMaxWidth()
    ) {
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(12.dp),
            verticalAlignment = Alignment.CenterVertically
        ) {
            // 左：48dp 圆形图（与真实 SpecialDateCard 一致）
            SkeletonCircle(size = 48.dp)

            // 中间间隔
            androidx.compose.foundation.layout.Spacer(
                modifier = Modifier.size(12.dp)
            )

            // 中：标题 + 时间
            Column(
                modifier = Modifier.weight(1f),
                verticalArrangement = Arrangement.spacedBy(4.dp)
            ) {
                // 标题（16sp Medium 占位）
                SkeletonText(width = 0.5f, height = 16.dp)
                // 时间信息（14sp Medium 占位）
                SkeletonText(width = 0.4f, height = 14.dp)
            }

            // 中右间隔
            androidx.compose.foundation.layout.Spacer(
                modifier = Modifier.size(8.dp)
            )

            // 右：大数字 + 单位两行
            Column(
                horizontalAlignment = Alignment.End,
                verticalArrangement = Arrangement.spacedBy(2.dp)
            ) {
                // 大数字（22sp Bold 占位）
                SkeletonText(
                    width = 0.18f,
                    height = 22.dp
                )
                // 单位（11sp 占位）
                SkeletonText(
                    width = 0.12f,
                    height = 11.dp
                )
            }
        }
    }
}
```

- [ ] **Step 3: 清理 Spacer 的完全限定名**

为可读性，将 `androidx.compose.foundation.layout.Spacer` 的完全限定名替换为顶部 import。更新 imports 区域：

```kotlin
import androidx.compose.foundation.layout.Spacer
```

并把 `DateItemSkeleton` 内的两处 `androidx.compose.foundation.layout.Spacer(...)` 简化为 `Spacer(...)`。

---

### Task 1.3: 重写主函数 `SpecialDateSkeleton`

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/SpecialDateSkeleton.kt`（替换主函数）

- [ ] **Step 1: 添加 `SkeletonSectionHeader` 与 `LazyColumn.items` 的 import**

在 imports 区域追加：

```kotlin
import androidx.compose.foundation.lazy.items
import androidx.compose.ui.graphics.Color
import com.corgimemo.app.ui.components.SkeletonSectionHeader
```

- [ ] **Step 2: 替换主函数**

完全替换现有的 `fun SpecialDateSkeleton(modifier: Modifier = Modifier) { ... }`，新版本：

```kotlin
/**
 * 特殊日期页面专属骨架屏组件（重构于 2026-07-14）
 *
 * 模拟日期页面的完整布局结构，与最新 UI 完全对齐：
 * - 搜索栏
 * - 置顶日期卡（1 张，与 PinnedDateCard 对应）
 * - 三段分组（倒计时 2 张 / 正计时 1 张 / 已归档 2 张）
 *   - 倒计时：主色 #FF9A5C（与 DateSectionHeader.kt:39 同步）
 *   - 正计时：柔和绿 #7EC8A0（与 DateSectionHeader.kt:40 同步）
 *   - 已归档：提示灰 #999999（与 DateSectionHeader.kt:42 同步）
 *
 * 切换到真实内容时无视觉跳变（容器色、圆角、内边距均与真实卡一致）。
 *
 * @param modifier 修饰符
 */
@Composable
fun SpecialDateSkeleton(
    modifier: Modifier = Modifier
) {
    Column(
        modifier = modifier.fillMaxSize()
    ) {
        // 1. 搜索栏骨架
        SkeletonSearchBar(
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 20.dp, vertical = 8.dp)
        )

        Spacer(modifier = Modifier.size(8.dp))

        // 2. 内容区：置顶卡 + 3 段分组
        LazyColumn(
            modifier = Modifier
                .fillMaxSize()
                .padding(horizontal = 20.dp),
            verticalArrangement = Arrangement.spacedBy(12.dp)
        ) {
            // 2.1 置顶日期卡
            item(key = "pinned_skeleton") {
                PinnedDateItemSkeleton()
            }

            // 2.2 倒计时分组
            item(key = "header_countdown") {
                SkeletonSectionHeader(
                    label = "倒计时",
                    color = Color(0xFFFF9A5C)
                )
            }
            items(count = 2, key = { "countdown_skeleton_$it" }) {
                DateItemSkeleton()
            }

            // 2.3 正计时分组
            item(key = "header_countup") {
                SkeletonSectionHeader(
                    label = "正计时",
                    color = Color(0xFF7EC8A0)
                )
            }
            items(count = 1, key = { "countup_skeleton_$it" }) {
                DateItemSkeleton()
            }

            // 2.4 已归档分组
            item(key = "header_expired") {
                SkeletonSectionHeader(
                    label = "已归档",
                    color = Color(0xFF999999)
                )
            }
            items(count = 2, key = { "expired_skeleton_$it" }) {
                DateItemSkeleton()
            }
        }
    }
}
```

- [ ] **Step 3: 删除不再使用的旧 `DateGroupTitleSkeleton`**

删除文件内的 `private fun DateGroupTitleSkeleton(title: String)`（已由 `SkeletonSectionHeader` 替代）。

---

### Task 1.4: 验证文件完整性

- [ ] **Step 1: 校对文件结构**

文件应包含（按顺序）：
1. `package` 声明
2. imports 区域
3. `fun SpecialDateSkeleton(modifier: Modifier = Modifier)` — public
4. `private fun PinnedDateItemSkeleton()` — 新增
5. `private fun DateItemSkeleton()` — 重写
6. **无** `DateGroupTitleSkeleton`（已删除）

- [ ] **Step 2: 校对 imports**

确认以下 imports 存在（其余可删除）：

```kotlin
import androidx.compose.foundation.layout.Arrangement
import androidx.compose.foundation.layout.Box
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Row
import androidx.compose.foundation.layout.Spacer
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.layout.size
import androidx.compose.foundation.layout.widthIn
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.foundation.lazy.items
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Card
import androidx.compose.material3.CardDefaults
import androidx.compose.material3.MaterialTheme
import androidx.compose.runtime.Composable
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.corgimemo.app.ui.components.SkeletonCircle
import com.corgimemo.app.ui.components.SkeletonSearchBar
import com.corgimemo.app.ui.components.SkeletonSectionHeader
import com.corgimemo.app.ui.components.SkeletonText
```

确认以下 imports **不存在**（已删除）：
- `androidx.compose.foundation.layout.height`
- `androidx.compose.foundation.layout.width`
- `com.corgimemo.app.ui.components.SkeletonButton`

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/date/SpecialDateSkeleton.kt
git commit -m "refactor(date-skeleton): 适配最新 UI（3 段分组+置顶卡+3 列卡片）"
```

---

## Task 2: 手动视觉验证

**Files:** 无（验证任务）

- [ ] **Step 1: 编译项目（仅用户执行）**

> ⚠️ **根据项目规则，编译由用户手动执行。** 请用户在本地执行：
> ```
> ./gradlew assembleDebug
> ```
> 预期：编译通过，无报错。

- [ ] **Step 2: 验证骨架屏首次显示**

启动 App → 进入日期页：
- [ ] 骨架屏在 ~200ms 内显示
- [ ] 包含：搜索框 + 1 张置顶卡 + 3 段分区头（带对应颜色）+ 5 张普通卡
- [ ] 倒计时分区头文字为橙色
- [ ] 正计时分区头文字为绿色
- [ ] 已归档分区头文字为灰色

- [ ] **Step 3: 验证骨架屏与真实 UI 对齐**

等待数据加载完成（骨架屏消失）：
- [ ] 切换前后无明显位置跳变（高度差 ≤ 4dp）
- [ ] 切换前后无颜色跳变（卡片背景一致）
- [ ] 切换前后无圆角跳变（均 20dp）

- [ ] **Step 4: 验证主题切换**

切换浅色/深色主题：
- [ ] 骨架屏颜色正常跟随主题
- [ ] 卡片背景仍为 surface 色（非灰色块）

- [ ] **Step 5: 验证回归**

- [ ] 灵感页骨架屏未受影响
- [ ] 待办页骨架屏未受影响
- [ ] 日期页正常功能（搜索、折叠、卡片点击）正常

---

## 风险与回退

| 风险 | 回退方案 |
|------|---------|
| 编译报错 | `git revert <本次提交>` 后重做 |
| 视觉差异仍存在 | 调整 `DateItemSkeleton` / `PinnedDateItemSkeleton` 的 height/width 参数 |
| `SkeletonSectionHeader` 调用方式不匹配 | 参考 `Skeleton.kt:247-281` 校对参数名（`label` / `color` / `modifier`） |
