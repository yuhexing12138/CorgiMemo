# 提醒弹窗芯片布局与默认值修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复提醒弹窗中芯片垂直重叠、移除"未设置"占位、统一标签颜色和行间距、待办卡片移除截止时间显示。

**Architecture:** 仅修改两个文件：`ReminderPickerBottomSheet.kt`（弹窗组件）和 `TodoListItem.kt`（待办卡片）。弹窗侧移除 `isReminderSet`/`isDueDateSet` 状态，改为始终显示具体日期时间；`DateTimeRow` 组件重构布局修复重叠；待办卡片删除截止时间显示段。

**Tech Stack:** Jetpack Compose, Material3, Kotlin

---

## File Structure

| 文件 | 改动 |
|------|------|
| `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt` | 移除状态变量、重构 DateTimeRow 布局、统一行间距、调整初始化逻辑和确定按钮逻辑 |
| `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt` | 删除截止时间显示段（第 472-483 行） |

---

### Task 1: 待办卡片移除截止时间显示

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt:472-483`

- [ ] **Step 1: 删除截止时间显示段**

在 `TodoListItem.kt` 中，删除第 472-483 行的截止时间显示代码块：

```kotlin
                        /** 截止时间（dueDate）显示 */
                        if (todo.dueDate != null) {
                            val isOverdue = todo.status != 1 && todo.dueDate!! < System.currentTimeMillis()
                            Column(modifier = Modifier.padding(top = 4.dp)) {
                                Text(
                                    text = "\u23F0 ${formatDateTime(todo.dueDate!!)}${if (isOverdue) " (已过期)" else ""}",
                                    fontSize = 12.sp,
                                    color = if (isOverdue) Color(0xFFDC2626) else MaterialTheme.colorScheme.onSurfaceVariant,
                                    fontWeight = if (isOverdue) androidx.compose.ui.text.font.FontWeight.SemiBold else androidx.compose.ui.text.font.FontWeight.Normal
                                )
                            }
                        }
```

删除后，第 470 行 `}` 之后直接接第 485 行的 `// 进度条` 注释。

- [ ] **Step 2: 检查 formatDateTime 是否仍被使用**

在 `TodoListItem.kt` 中搜索 `formatDateTime`，确认删除截止时间显示后是否还有其他调用。如果没有，删除该函数定义和相关 import。

- [ ] **Step 3: 编译验证**

Run: `./gradlew assembleDebug`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt
git commit -m "fix: 移除待办卡片截止时间显示"
```

---

### Task 2: 移除 isReminderSet/isDueDateSet 状态并调整初始化逻辑

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt:100-154`

- [ ] **Step 1: 移除 isReminderSet 状态变量**

删除第 102 行：
```kotlin
    var isReminderSet by remember { mutableStateOf(initialDateMillis != null) }
```

- [ ] **Step 2: 调整截止日期初始化逻辑**

将第 141-154 行的截止日期状态块替换为：

```kotlin
    // ===== 截止日期状态 =====
    // 截止时间默认与提醒时间一致（日期 + 时分完全相同）
    val initDueZoned = if (initialDueDateMillis != null) {
        java.time.Instant.ofEpochMilli(initialDueDateMillis)
            .atZone(java.time.ZoneId.systemDefault())
    } else {
        // 未传入截止日期时，使用提醒时间的时区日期（与 selectedDate/selectedHour/Minute 一致）
        initLocalDate.atTime(initialHour, initialMinute)
            .atZone(java.time.ZoneId.systemDefault())
    }
    var dueDate by remember { mutableStateOf(initDueZoned.toLocalDate()) }
    var dueHour by remember { mutableIntStateOf(initDueZoned.hour) }
    var dueMinute by remember { mutableIntStateOf(initDueZoned.minute) }
```

注意：这会移除 `isDueDateSet` 状态变量和 `initDueLocalDate` 局部变量。

- [ ] **Step 3: 调整标题动态化逻辑**

将第 178 行替换为：

```kotlin
        val displayTitle = if (initialDateMillis == null && initialDueDateMillis != null) "设置截止日期" else title
```

- [ ] **Step 4: 编译验证（预期有错误）**

Run: `./gradlew assembleDebug`
Expected: 编译错误，因为 `isReminderSet` 和 `isDueDateSet` 仍在 DateTimeRow 调用和确定按钮中被引用。这些将在 Task 3 和 Task 4 中修复。

---

### Task 3: 重构 DateTimeRow 调用（移除"未设置"、移除清除按钮）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt:190-246`

- [ ] **Step 1: 替换提醒时间行调用**

将第 190-215 行替换为：

```kotlin
        // ===== 提醒时间行 =====
        DateTimeRow(
            label = rowLabel,
            isActive = editTarget == EditTarget.REMINDER,
            dateText = "${selectedDate.year}/${String.format("%02d", selectedDate.monthValue)}/${String.format("%02d", selectedDate.dayOfMonth)}",
            timeText = "${String.format("%02d", selectedHour)}:${String.format("%02d", selectedMinute)}",
            isCalendarActive = viewMode == "calendar",
            isTimeActive = viewMode == "time",
            onDateClick = {
                editTarget = EditTarget.REMINDER
                viewMode = "calendar"
            },
            onTimeClick = {
                editTarget = EditTarget.REMINDER
                viewMode = "time"
            },
            onClear = null
        )
```

- [ ] **Step 2: 替换截止日期行调用**

将第 217-246 行替换为：

```kotlin
        // ===== 截止日期行 =====
        DateTimeRow(
            label = "截止日期",
            isActive = editTarget == EditTarget.DUE_DATE,
            dateText = "${dueDate.year}/${String.format("%02d", dueDate.monthValue)}/${String.format("%02d", dueDate.dayOfMonth)}",
            timeText = "${String.format("%02d", dueHour)}:${String.format("%02d", dueMinute)}",
            isCalendarActive = viewMode == "calendar",
            isTimeActive = viewMode == "time",
            onDateClick = {
                editTarget = EditTarget.DUE_DATE
                viewMode = "calendar"
            },
            onTimeClick = {
                editTarget = EditTarget.DUE_DATE
                viewMode = "time"
            },
            onClear = null,
            highlightEnabled = isDueDateAutoFixed
        )
```

- [ ] **Step 3: 编译验证（预期有错误）**

Run: `./gradlew assembleDebug`
Expected: 编译错误，因为 `DateTimeRow` 签名仍包含 `isDateSelected` 参数，且确定按钮仍引用 `isReminderSet`/`isDueDateSet`。这些将在 Task 4 和 Task 5 中修复。

---

### Task 4: 重构 DateTimeRow 组件（移除 isDateSelected、固定标签宽度、修复重叠）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt:535-639`

- [ ] **Step 1: 替换 DateTimeRow 组件定义**

将第 535-639 行（从 `// ==================== 日期时间行组件 ====================` 到 `DateTimeRow` 函数结束）替换为：

```kotlin
// ==================== 日期时间行组件 ====================

/**
 * 提醒/截止日期通用行组件
 *
 * 包含标签、日期芯片、时间芯片、清除按钮，用于 ReminderPickerBottomSheet 中的
 * 提醒时间行和截止日期行，减少重复代码。
 *
 * @param label 行标签文字（如"提醒时间"、"截止日期"）
 * @param isActive 是否为当前编辑目标（高亮标签颜色）
 * @param dateText 日期芯片显示文字
 * @param timeText 时间芯片显示文字，null 时隐藏时间芯片
 * @param isCalendarActive 日历视图是否为当前激活视图
 * @param isTimeActive 时间视图是否为当前激活视图
 * @param onDateClick 日期芯片点击回调
 * @param onTimeClick 时间芯片点击回调
 * @param onClear 清除按钮点击回调，null 时不显示清除按钮
 * @param highlightEnabled 是否高亮显示（自动修正后短暂高亮提示）
 */
@Composable
private fun DateTimeRow(
    label: String,
    isActive: Boolean,
    dateText: String,
    timeText: String?,
    isCalendarActive: Boolean,
    isTimeActive: Boolean,
    onDateClick: () -> Unit,
    onTimeClick: () -> Unit,
    onClear: (() -> Unit)?,
    highlightEnabled: Boolean = false
) {
    // 高亮背景色：自动修正后短暂使用橙色淡背景
    val rowBackground = if (highlightEnabled) Color(0xFFFFF3E8) else Color.Transparent
    Row(
        modifier = Modifier
            .fillMaxWidth()
            .clip(RoundedCornerShape(8.dp))
            .background(rowBackground)
            .padding(vertical = if (highlightEnabled) 4.dp else 0.dp),
        verticalAlignment = Alignment.CenterVertically
    ) {
        // 标签固定宽度，避免压缩右侧芯片区域
        Text(
            text = label,
            fontSize = 15.sp,
            color = if (isActive) MaterialTheme.colorScheme.primary else Color(0xFF2D2D2D),
            fontWeight = FontWeight.Medium,
            modifier = Modifier.width(64.dp)
        )

        // 右侧芯片区域整体 weight(1f) 右对齐
        Row(
            modifier = Modifier.weight(1f),
            horizontalArrangement = Arrangement.spacedBy(8.dp, Alignment.End),
            verticalAlignment = Alignment.CenterVertically
        ) {
            // 日期芯片
            Row(
                modifier = Modifier
                    .clip(RoundedCornerShape(8.dp))
                    .background(if (isActive && isCalendarActive) MaterialTheme.colorScheme.primary else Color(0xFFFFE4CC))
                    .clickable(onClick = onDateClick)
                    .padding(horizontal = 12.dp, vertical = 6.dp),
                verticalAlignment = Alignment.CenterVertically
            ) {
                Text(
                    text = dateText,
                    fontSize = 13.sp,
                    color = if (isActive && isCalendarActive) Color.White else Color(0xFF2D2D2D),
                    fontWeight = if (isActive && isCalendarActive) FontWeight.Medium else FontWeight.Normal,
                    maxLines = 1,
                    softWrap = false
                )
            }

            // 时间芯片（仅在 timeText 非 null 时显示）
            if (timeText != null) {
                Row(
                    modifier = Modifier
                        .clip(RoundedCornerShape(8.dp))
                        .background(if (isActive && isTimeActive) MaterialTheme.colorScheme.primary else Color(0xFFFFE4CC))
                        .clickable(onClick = onTimeClick)
                        .padding(horizontal = 12.dp, vertical = 6.dp),
                    verticalAlignment = Alignment.CenterVertically
                ) {
                    Text(
                        text = timeText,
                        fontSize = 13.sp,
                        color = if (isActive && isTimeActive) Color.White else Color(0xFF2D2D2D),
                        fontWeight = if (isActive && isTimeActive) FontWeight.Medium else FontWeight.Normal,
                        maxLines = 1,
                        softWrap = false
                    )
                }
            }

            // 清除按钮（仅在 onClear 非 null 时显示）
            if (onClear != null) {
                Icon(
                    imageVector = Icons.Default.Close,
                    contentDescription = "清除",
                    modifier = Modifier
                        .size(18.dp)
                        .clip(RoundedCornerShape(9.dp))
                        .clickable(onClick = onClear)
                        .padding(2.dp),
                    tint = Color(0xFF999999)
                )
            }
        }
    }
}
```

**关键改动**：
- 移除 `isDateSelected` 参数
- 标签 `Modifier.width(64.dp)` 固定宽度
- 右侧芯片区域 `Modifier.weight(1f)` + `Arrangement.spacedBy(8.dp, Alignment.End)`
- 芯片 Text 添加 `maxLines = 1` + `softWrap = false`
- 标签未激活颜色从 `#999999` 改为 `#2D2D2D`
- 芯片文字颜色从 `#666666` 改为 `#2D2D2D`

- [ ] **Step 2: 编译验证（预期有错误）**

Run: `./gradlew assembleDebug`
Expected: 编译错误，因为确定按钮仍引用 `isReminderSet`/`isDueDateSet`。将在 Task 5 中修复。

---

### Task 5: 调整确定按钮逻辑（移除 null 校验）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt:467-509`

- [ ] **Step 1: 替换确定按钮的 clickable 逻辑**

将第 467-509 行的 `.clickable { ... }` 内容替换为：

```kotlin
                    .clickable {
                        // 计算提醒时间戳（始终有值，因为默认为今天当前时间）
                        val reminderMillis = selectedDate.atTime(selectedHour, selectedMinute)
                            .atZone(java.time.ZoneId.systemDefault())
                            .toInstant().toEpochMilli()

                        // 计算截止日期时间戳（始终有值，默认等于提醒时间）
                        val dueDateMillis = dueDate.atTime(dueHour, dueMinute)
                            .atZone(java.time.ZoneId.systemDefault())
                            .toInstant().toEpochMilli()

                        // 截止时间自动修正：早于提醒时间时自动调整为提醒时间
                        // 修正后不立即提交，切换到截止日期行高亮让用户确认
                        if (dueDateMillis < reminderMillis) {
                            val adjustedDateTime = java.time.Instant.ofEpochMilli(reminderMillis)
                                .atZone(java.time.ZoneId.systemDefault())
                            dueDate = adjustedDateTime.toLocalDate()
                            dueHour = adjustedDateTime.hour
                            dueMinute = adjustedDateTime.minute
                            editTarget = EditTarget.DUE_DATE
                            isDueDateAutoFixed = true
                            Toast.makeText(context, "截止时间已自动调整为提醒时间，请确认", Toast.LENGTH_SHORT).show()
                            return@clickable
                        }

                        onConfirm(
                            reminderMillis,
                            selectedHour,
                            selectedMinute,
                            repeatType,
                            calendarEnabled && showAdvancedOptions,
                            dueDateMillis
                        )
                    },
```

- [ ] **Step 2: 编译验证**

Run: `./gradlew assembleDebug`
Expected: BUILD SUCCESSFUL

---

### Task 6: 统一行间距为 16dp

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt:246-252`

- [ ] **Step 1: 在截止日期行后添加 Spacer，删除重复 Spacer**

当前结构（Task 3 修改后行号可能变化，以代码内容为准）：

```kotlin
        // ===== 截止日期行 =====
        DateTimeRow(
            ...
            highlightEnabled = isDueDateAutoFixed
        )

        Spacer(modifier = Modifier.height(16.dp))    // ← 删除此行

        // ===== 重复提醒行（仅 showAdvancedOptions=true 时显示） =====
        if (showAdvancedOptions) {
        Spacer(modifier = Modifier.height(16.dp))    // ← 删除此行
```

修改为：

```kotlin
        // ===== 截止日期行 =====
        DateTimeRow(
            ...
            highlightEnabled = isDueDateAutoFixed
        )

        Spacer(modifier = Modifier.height(16.dp))

        // ===== 重复提醒行（仅 showAdvancedOptions=true 时显示） =====
        if (showAdvancedOptions) {
        // ===== 重复提醒行（胶囊按钮 + ↕箭头，展开时显示背景） =====
```

具体操作：
1. 截止日期行 `DateTimeRow(...)` 之后保留一个 `Spacer(modifier = Modifier.height(16.dp))`
2. 删除 `if (showAdvancedOptions) {` 之后紧接的 `Spacer(modifier = Modifier.height(16.dp))`
3. 在提醒时间行 `DateTimeRow(...)` 与截止日期行 `DateTimeRow(...)` 之间新增 `Spacer(modifier = Modifier.height(16.dp))`

最终四行间距结构：
```
DateTimeRow(提醒时间)
Spacer(16dp)          ← 新增
DateTimeRow(截止日期)
Spacer(16dp)          ← 保留
[重复提醒 Row]         ← if (showAdvancedOptions) 内
Spacer(16dp)
[农历 Row]
Spacer(16dp)
[分割线]
Spacer(16dp)
```

- [ ] **Step 2: 编译验证**

Run: `./gradlew assembleDebug`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt
git commit -m "fix: 提醒弹窗芯片布局与默认值修复

- 移除未设置占位，始终显示具体日期时间
- 截止时间默认与提醒时间一致
- 标签颜色改为黑色#2D2D2D
- 修复芯片垂直重叠（固定标签宽度+weight右对齐+maxLines）
- 统四行间距为16dp
- 移除isReminderSet/isDueDateSet状态"
```

---

### Task 7: 全量编译与验证

**Files:**
- All modified files

- [ ] **Step 1: 全量编译**

Run: `./gradlew assembleDebug`
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 手动验证清单**

启动应用，逐项验证：

- [ ] 打开提醒弹窗，提醒时间和截止时间都显示今天日期 + 当前时间
- [ ] 编辑旧待办时，提醒时间和截止时间显示待办原有值
- [ ] 标签颜色为黑色，激活时为橙色主题色
- [ ] 两行芯片在垂直方向不重叠，对齐良好
- [ ] 提醒时间行、截止日期行、重复提醒行、农历行间距均为 16dp
- [ ] 待办卡片不显示截止时间
- [ ] 截止时间早于提醒时间时自动修正并高亮
- [ ] 确定按钮提交正确的提醒时间和截止时间

- [ ] **Step 3: 最终 Commit（如有修复）**

```bash
git add -A
git commit -m "fix: 编译验证修复"
```

---

## Self-Review

**Spec coverage:**
- ✅ 标签颜色改为黑色 `#2D2D2D` → Task 4 Step 1
- ✅ 移除"未设置"占位 → Task 3 Step 1-2 + Task 2 Step 1
- ✅ 截止时间默认与提醒时间一致 → Task 2 Step 2
- ✅ 芯片垂直重叠修复 → Task 4 Step 1（固定标签宽度 + weight + maxLines）
- ✅ 行间距统一 16dp → Task 6 Step 1
- ✅ 待办卡片移除截止时间 → Task 1 Step 1
- ✅ 确定按钮逻辑调整 → Task 5 Step 1
- ✅ 标题动态化调整 → Task 2 Step 3

**Placeholder scan:** 无占位符，所有步骤包含完整代码。

**Type consistency:**
- `DateTimeRow` 签名移除 `isDateSelected` 参数，Task 3 调用方同步移除 ✅
- `isReminderSet`/`isDueDateSet` 在 Task 2 移除，Task 3 和 Task 5 同步移除引用 ✅
- `initDueZoned` 在 Task 2 定义，后续无其他引用 ✅
