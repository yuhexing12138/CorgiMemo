# 待办卡片多容器分类独立设置修复 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复待办卡片编辑页多容器场景下分类状态共享问题，实现每个容器的分类独立存储、互不影响、即时保存显示。

**Architecture:** 将 `TodoEditViewModel` 中的单值 `_categoryId: MutableStateFlow<Long>` 改造为按 groupId 隔离的 `_groupCategoryIds: MutableStateFlow<Map<Int, Long>>`，与已有的 `_groupReminders`/`_groupPriorities` 等分组状态模式完全一致。UI 层（TodoEditScreen、CheckboxEditText）同步改造为按 groupId 读写分类状态。

**Tech Stack:** Kotlin、Jetpack Compose、StateFlow、Map<Int, Long> 分组状态模式

**关联设计文档:** `docs/superpowers/specs/2026-06-28-待办卡片多容器分类独立设置修复-design.md`

---

## 文件结构

| 文件 | 责任 | 改动类型 |
|------|------|---------|
| `app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt` | 容器渲染组件，按 groupId 渲染 TodoGroupContainer | 函数签名改造 + 2 处调用点 |
| `app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt` | 编辑页 UI，状态收集与弹窗控制 | 状态变量 + 状态收集 + 传参 + 弹窗 |
| `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt` | 状态管理与持久化 | 字段替换 + 方法替换 + 3 处调用点 + 注释 |

**改动顺序**：CheckboxEditText → TodoEditScreen → TodoEditViewModel（自底向上，确保调用链一致）。三个文件改动有强依赖关系，无法独立编译，统一在 Task 4 编译验证。

---

## Task 1: CheckboxEditText 函数签名与调用点改造

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt`

**目标**：把 `CheckboxEditText` 公共函数的分类相关参数从单值改为按 groupId 的 Map，并在两处调用 `TodoGroupContainer` 的位置按 groupId 取值。`TodoGroupContainer` 私有函数签名保持不变。

- [ ] **Step 1: 修改函数签名（L111-118）**

打开 `app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt`，定位到 L111-118 的分类相关参数。

改前：

```kotlin
/** 各分组的分类 ID（null=未设置）；用于底部"分类"按钮显示 */
categoryId: Long? = null,
/** 各分组的分类名称（用于底部"分类"按钮显示） */
categoryName: String? = null,
/** "分类"按钮点击回调（无分组概念，整个容器共用） */
onCategoryClick: (() -> Unit)? = null,
/** "分类"×按钮点击回调：清除已设置的分类 */
onCategoryClear: (() -> Unit)? = null,
```

改后：

```kotlin
/** 各分组的分类 ID 映射（key=groupId, value=categoryId, 0L=未分类） */
groupCategoryIds: Map<Int, Long> = emptyMap(),
/** 各分组的分类名称映射（key=groupId, value=categoryName, null=未设置） */
groupCategoryNames: Map<Int, String?> = emptyMap(),
/** "分类"按钮点击回调（参数=groupId） */
onCategoryClick: ((Int) -> Unit)? = null,
/** "分类"×按钮点击回调：清除已设置的分类（参数=groupId） */
onCategoryClear: ((Int) -> Unit)? = null,
```

- [ ] **Step 2: 修改空状态占位容器传参（L240-243）**

定位到 L240-243（空状态 `groups.isEmpty()` 分支内的 `TodoGroupContainer` 调用）。

改前：

```kotlin
categoryId = categoryId,
categoryName = categoryName,
onCategoryClick = { onCategoryClick?.invoke() },
onCategoryClear = { onCategoryClear?.invoke() },
```

改后：

```kotlin
categoryId = groupCategoryIds[0] ?: 0L,
categoryName = groupCategoryNames[0],
onCategoryClick = { onCategoryClick?.invoke(0) },
onCategoryClear = { onCategoryClear?.invoke(0) },
```

- [ ] **Step 3: 修改多容器循环传参（L305-308）**

定位到 L305-308（`for ((groupId, groupLines) in groups)` 循环内的 `TodoGroupContainer` 调用）。

改前：

```kotlin
categoryId = categoryId,
categoryName = categoryName,
onCategoryClick = { onCategoryClick?.invoke() },
onCategoryClear = { onCategoryClear?.invoke() },
```

改后：

```kotlin
categoryId = groupCategoryIds[groupId] ?: 0L,
categoryName = groupCategoryNames[groupId],
onCategoryClick = { onCategoryClick?.invoke(groupId) },
onCategoryClear = { onCategoryClear?.invoke(groupId) },
```

- [ ] **Step 4: 检查 import 语句**

确认 `CheckboxEditText.kt` 顶部 import 无需变更（`Map`/`Int`/`Long` 均为 Kotlin 内置类型，无需额外 import）。如 IDE 报告未使用 import（如 `mutableStateOf` 等仍被其他代码使用），保持不变。

- [ ] **Step 5: 不要在此任务编译验证**

由于 TodoEditScreen 仍在调用旧参数名（`categoryId =`、`categoryName =`），此时编译会失败。编译验证统一在 Task 4 进行。

---

## Task 2: TodoEditScreen 状态与弹窗改造

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt`

**目标**：将 `showCategoryDialog: Boolean` 替换为 `pendingCategoryGroupId: Int?`，状态收集改为 `groupCategoryIds` + 派生的 `groupCategoryNames`，传参与弹窗按 groupId 处理。

- [ ] **Step 1: 替换弹窗触发状态变量（L162）**

定位到 L162。

改前：

```kotlin
/** 分类选择弹窗状态 */
var showCategoryDialog by remember { mutableStateOf(false) }
```

改后：

```kotlin
/**
 * 分类选择弹窗目标 groupId
 *
 * null = 弹窗关闭；非 null = 弹窗打开且为该 groupId 选分类
 * 替代旧 showCategoryDialog: Boolean，携带目标分组上下文
 */
var pendingCategoryGroupId by remember { mutableStateOf<Int?>(null) }
```

- [ ] **Step 2: 改造状态收集（L165-167）**

定位到 L165-167。

改前：

```kotlin
val categories by viewModel.categories.collectAsState()
val categoryId by viewModel.categoryId.collectAsState()
val currentCategory = categories.find { it.id == categoryId }
```

改后：

```kotlin
val categories by viewModel.categories.collectAsState()
val groupCategoryIds by viewModel.groupCategoryIds.collectAsState()

/**
 * 派生：各 groupId 对应的分类名称映射
 * key = groupId, value = categoryName（null = 未设置/未分类）
 */
val groupCategoryNames = remember(groupCategoryIds, categories) {
    groupCategoryIds.mapValues { (_, catId) ->
        if (catId == 0L) null
        else categories.find { it.id == catId }?.name
    }
}
```

- [ ] **Step 3: 改造传参给 CheckboxEditText（L1018-1021）**

定位到 L1018-1021。

改前：

```kotlin
categoryId = categoryId,
categoryName = currentCategory?.name,
onCategoryClick = { showCategoryDialog = true },
onCategoryClear = { viewModel.clearCategory() },
```

改后：

```kotlin
groupCategoryIds = groupCategoryIds,
groupCategoryNames = groupCategoryNames,
onCategoryClick = { groupId -> pendingCategoryGroupId = groupId },
onCategoryClear = { groupId -> viewModel.clearGroupCategory(groupId) },
```

- [ ] **Step 4: 改造分类弹窗（L1694-1713）**

定位到 L1694-1713（`if (showCategoryDialog) { ... }` 块）。

改前：

```kotlin
if (showCategoryDialog) {
    CategorySelectorDialog(
        categories = categories,
        currentCategoryId = categoryId,
        onDismiss = { showCategoryDialog = false },
        onCategorySelected = { id, name ->
            if (id > 0L) {
                viewModel.setCategoryId(id)
            } else {
                viewModel.createCustomCategory(name) { newId ->
                    viewModel.setCategoryId(newId)
                }
            }
            showCategoryDialog = false
        }
    )
}
```

改后：

```kotlin
pendingCategoryGroupId?.let { targetGroupId ->
    CategorySelectorDialog(
        categories = categories,
        currentCategoryId = groupCategoryIds[targetGroupId] ?: 0L,
        onDismiss = { pendingCategoryGroupId = null },
        onCategorySelected = { id, name ->
            if (id > 0L) {
                viewModel.setGroupCategory(targetGroupId, id)
            } else {
                // 自定义分类：异步创建后写入对应 groupId
                viewModel.createCustomCategory(name) { newId ->
                    viewModel.setGroupCategory(targetGroupId, newId)
                }
            }
            pendingCategoryGroupId = null
        }
    )
}
```

- [ ] **Step 5: 检查 import 语句**

确认 `TodoEditScreen.kt` 顶部 import：
- `androidx.compose.runtime.remember` 已存在（被其他代码使用）
- `androidx.compose.runtime.mutableStateOf` 已存在
- `androidx.compose.runtime.collectAsState` 已存在
- 无需新增 import

- [ ] **Step 6: 不要在此任务编译验证**

由于 TodoEditViewModel 仍提供旧 `categoryId`/`setCategoryId`/`clearCategory` 而未提供新 `groupCategoryIds`/`setGroupCategory`/`clearGroupCategory`，此时编译会失败。编译验证统一在 Task 4 进行。

---

## Task 3: TodoEditViewModel 状态与方法改造

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt`

**目标**：将 `_categoryId: MutableStateFlow<Long>` 替换为 `_groupCategoryIds: MutableStateFlow<Map<Int, Long>>`，新增 `setGroupCategory`/`clearGroupCategory` 方法，删除旧 `setCategoryId`/`clearCategory`，修改 `loadTodo`/`performSave`/`buildTodoItemForGroup` 三处调用点，更新 `createCustomCategory` 注释。

- [ ] **Step 1: 替换状态字段（L78-79）**

定位到 L78-79。

改前：

```kotlin
private val _categoryId = MutableStateFlow(0L)
val categoryId: StateFlow<Long> = _categoryId.asStateFlow()
```

改后：

```kotlin
/**
 * 各分组的分类 ID 状态
 *
 * key = groupId (Int), value = categoryId (Long, 0L = 未分类)
 * 每个编辑容器拥有独立的分类，保存时写入对应 TodoItem.categoryId
 *
 * 设计与 _groupReminders / _groupPriorities / _groupRepeatTypes 完全一致，
 * 保证多容器场景下分类状态互不影响。
 */
private val _groupCategoryIds = MutableStateFlow<Map<Int, Long>>(emptyMap())

/** 暴露分组分类 ID 供 UI 层收集 */
val groupCategoryIds: StateFlow<Map<Int, Long>> = _groupCategoryIds.asStateFlow()
```

- [ ] **Step 2: 替换方法（L390-402）**

定位到 L390-402。

改前：

```kotlin
fun setCategoryId(categoryId: Long) {
    _categoryId.value = categoryId
}

/**
 * 清除当前分类
 *
 * 复刻"设置提醒"×按钮的交互：用户清除后回到未分类状态
 * 调用方传入 -1L 作为"未设置"的标记，避免修改 _categoryId 的类型为 Long?
 */
fun clearCategory() {
    _categoryId.value = 0L
}
```

改后：

```kotlin
/**
 * 设置指定分组的分类 ID
 *
 * @param groupId 分组 ID
 * @param categoryId 分类 ID（0L = 未分类）
 */
fun setGroupCategory(groupId: Int, categoryId: Long) {
    _groupCategoryIds.value = _groupCategoryIds.value + (groupId to categoryId)
}

/**
 * 清除指定分组的分类（置为 0L 未分类）
 *
 * @param groupId 分组 ID
 */
fun clearGroupCategory(groupId: Int) {
    _groupCategoryIds.value = _groupCategoryIds.value + (groupId to 0L)
}
```

> 注：`clearGroupCategory` 选择 `+ (groupId to 0L)` 而非移除条目，与 `setGroupCategory` 行为对称，且保证 map 中有明确"未分类"记录，避免与"从未设置过"产生歧义。

- [ ] **Step 3: 修改 loadTodo 初始化（L607）**

定位到 L607。

改前：

```kotlin
_categoryId.value = todo.categoryId
```

改后：

```kotlin
// 单容器场景：groupId=0 持有该 todo 的分类
_groupCategoryIds.value = mapOf(0 to todo.categoryId)
```

- [ ] **Step 4: 修改 performSave 第一处（L764，existingTodo != null 的 update 分支）**

定位到 L764（`val todo = existingTodo!!.copy(...)` 内的 `categoryId = _categoryId.value,`）。

改前：

```kotlin
categoryId = _categoryId.value,
```

改后：

```kotlin
categoryId = _groupCategoryIds.value[0] ?: 0L,
```

- [ ] **Step 5: 修改 performSave 第二处（L792，existingTodo == null 的 insert 分支）**

定位到 L792（`val todo = TodoItem(...)` 内的 `categoryId = _categoryId.value,`）。

改前：

```kotlin
categoryId = _categoryId.value,
```

改后：

```kotlin
categoryId = _groupCategoryIds.value[0] ?: 0L,
```

- [ ] **Step 6: 修改 buildTodoItemForGroup（L866）**

定位到 L866（`return TodoItem(...)` 内的 `categoryId = _categoryId.value,`）。

改前：

```kotlin
categoryId = _categoryId.value,
```

改后：

```kotlin
categoryId = _groupCategoryIds.value[targetGroupId] ?: 0L,
```

- [ ] **Step 7: 更新 createCustomCategory 注释（L1217 附近）**

定位到 L1217 附近 `createCustomCategory` 函数的注释中提及"调用方应立即调用 setCategoryId"的部分。

改前（注释片段）：

```kotlin
 * 调用方应立即调用 [setCategoryId] 切换当前 todo 到新分类。
```

改后：

```kotlin
 * 调用方应立即调用 [setGroupCategory] 切换对应分组到新分类。
```

> 注：实际行号与措辞可能略有差异，请在文件中搜索 `setCategoryId` 关键字确认是否还有其他引用。如搜索结果为空（除上述已修改的位置外），则改造完整。

- [ ] **Step 8: 全文搜索验证无遗漏**

在 `TodoEditViewModel.kt` 中搜索以下关键字，确认无残留：
- `_categoryId`：应为 0 处
- `setCategoryId`：应为 0 处
- `clearCategory`：应为 0 处
- `val categoryId`：应为 0 处

如有残留，按上述同模式改造。

- [ ] **Step 9: 检查 import 语句**

确认 `TodoEditViewModel.kt` 顶部 import：
- `kotlinx.coroutines.flow.MutableStateFlow` 已存在
- `kotlinx.coroutines.flow.StateFlow` 已存在
- `kotlinx.coroutines.flow.asStateFlow` 已存在
- 无需新增 import（`Map<Int, Long>` 为 Kotlin 内置类型）

---

## Task 4: 编译验证

**Files:**
- 无文件改动，仅编译验证

**目标**：编译项目确认三个文件改动一致、无遗漏、无类型不匹配。

- [ ] **Step 1: 询问用户是否进行编译验证**

按工作区规则 `c:\Users\Lenovo\Desktop\CorgiMemo\.trae\rules\编译验证.md`，编译前必须询问用户。

向用户提问："是否进行编译验证？"

- [ ] **Step 2: 执行编译验证（如用户同意）**

Run: `./gradlew assembleDebug`（或在 Windows 下 `gradlew.bat assembleDebug`）

Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 处理编译错误（如有）**

如编译失败，根据错误信息定位：
- 类型不匹配：检查 `groupCategoryIds`/`groupCategoryNames` 的类型传递
- 未解析引用：检查 `setGroupCategory`/`clearGroupCategory`/`groupCategoryIds` 拼写
- 参数缺失：检查 `CheckboxEditText` 调用方是否传齐 `groupCategoryIds`/`groupCategoryNames`

修复后重新编译，直到 BUILD SUCCESSFUL。

---

## Task 5: 手动验证场景

**Files:**
- 无文件改动，仅运行时验证

**目标**：在设备/模拟器上验证多容器分类独立设置功能符合预期。

- [ ] **Step 1: 启动应用到待办编辑页**

在模拟器或真机上启动 CorgiMemo，进入待办编辑页（新建 todo）。

- [ ] **Step 2: 验证场景 1 — 创建多容器**

在待办编辑页输入框输入 "/"，创建容器 A、B。
预期：A、B 都显示"分类"占位按钮。

- [ ] **Step 3: 验证场景 2 — 在 A 选择分类**

在容器 A 点击"分类"按钮，选择"工作"。
预期：A 显示"工作"，B 仍显示"分类"占位（不变）。

- [ ] **Step 4: 验证场景 3 — 在 B 选择不同分类**

在容器 B 点击"分类"按钮，选择"学习"。
预期：A 显示"工作"，B 显示"学习"（互不影响）。

- [ ] **Step 5: 验证场景 4 — 在 A 清除分类**

在容器 A 点击分类按钮右侧的"×"。
预期：A 回到"分类"占位，B 仍显示"学习"（不变）。

- [ ] **Step 6: 验证场景 5 — 保存后重新打开**

保存所有容器，返回首页。重新打开各 todo。
预期：每个 todo 的 categoryId 各自正确持久化（A=0L，B=学习ID）。

- [ ] **Step 7: 验证场景 6 — 编辑已有 todo**

打开一个已有 todo（existingTodo 场景），其 categoryId 应正确加载到容器显示。
输入 "/" 创建第二个容器，新容器默认显示"分类"占位（0L）。
预期：原有 todo 分类保持，新容器未分类。

- [ ] **Step 8: 验证场景 7 — 自定义分类**

在容器中点击"分类" → "自定义" → 输入新分类名 → 确认。
预期：仅当前容器显示该自定义分类，其他容器不变。

如任一场景不符合预期，回到对应 Task 修复后重新验证。

---

## Task 6: Git 提交

**Files:**
- 无文件改动，仅 git 操作

**目标**：按工作区规则询问用户是否提交，如同意则提交本次改动。

- [ ] **Step 1: 询问用户是否进行 git 提交**

按工作区规则 `c:\Users\Lenovo\Desktop\CorgiMemo\.trae\rules\git提交.md`，任务完成后询问用户。

向用户提问："是否进行 git 提交？"

- [ ] **Step 2: 执行 git 提交（如用户同意）**

Run:

```bash
git status
git diff --stat
git log -n 5 --oneline
```

分析改动后，添加三个改动文件并提交：

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt
git commit -m "$(cat <<'EOF'
修复: 待办卡片多容器分类独立设置

将 TodoEditViewModel 中的单值 _categoryId 改造为按 groupId 隔离的
_groupCategoryIds: Map<Int, Long>，与已有的 _groupReminders/_groupPriorities
等分组状态模式完全一致。

- ViewModel: 新增 setGroupCategory/clearGroupCategory，删除旧 setCategoryId/clearCategory
- TodoEditScreen: 用 pendingCategoryGroupId:Int? 替代 showCategoryDialog:Boolean
- CheckboxEditText: 函数签名改为接收 groupCategoryIds/groupCategoryNames 映射

修复前在第一个容器设置分类后，所有后续容器分类被同步更改的问题。
现在每个容器的分类独立存储、互不影响、即时保存显示。

零数据库 Migration，零数据模型变更，100% 兼容旧数据。
EOF
)"
git status
```

Expected: 提交成功，工作区干净。

---

## 自检

**1. Spec 覆盖检查**：
- 设计 4.1.1（字段替换）→ Task 3 Step 1 ✓
- 设计 4.1.2（方法替换）→ Task 3 Step 2 ✓
- 设计 4.1.3（loadTodo）→ Task 3 Step 3 ✓
- 设计 4.1.4（performSave 两处）→ Task 3 Step 4-5 ✓
- 设计 4.1.5（buildTodoItemForGroup）→ Task 3 Step 6 ✓
- 设计 4.1.6（注释更新）→ Task 3 Step 7 ✓
- 设计 4.2.1（弹窗状态）→ Task 2 Step 1 ✓
- 设计 4.2.2（状态收集）→ Task 2 Step 2 ✓
- 设计 4.2.3（传参）→ Task 2 Step 3 ✓
- 设计 4.2.4（弹窗）→ Task 2 Step 4 ✓
- 设计 4.3.1（签名）→ Task 1 Step 1 ✓
- 设计 4.3.2（空状态占位）→ Task 1 Step 2 ✓
- 设计 4.3.3（多容器循环）→ Task 1 Step 3 ✓
- 设计 4.3.4（TodoGroupContainer 不变）→ 计划中明确不动 ✓
- 设计 5.x（持久化/兼容性）→ 由代码改动自然保证，无独立任务 ✓
- 设计 6.x（边界/错误）→ 由 `?: 0L` 兜底自然保证，无独立任务 ✓
- 设计 7.x（测试）→ Task 5 手动验证场景 ✓

**2. 占位符扫描**：无 TBD/TODO/未完整代码块。所有 Step 含完整代码。

**3. 类型一致性**：
- `_groupCategoryIds: MutableStateFlow<Map<Int, Long>>` — Task 3 Step 1 定义，Task 2 Step 2 / Task 3 Step 3-6 使用，类型一致 ✓
- `groupCategoryIds: StateFlow<Map<Int, Long>>` — Task 3 Step 1 暴露，Task 2 Step 2 collectAsState，类型一致 ✓
- `setGroupCategory(groupId: Int, categoryId: Long)` — Task 3 Step 2 定义，Task 2 Step 4 调用，签名一致 ✓
- `clearGroupCategory(groupId: Int)` — Task 3 Step 2 定义，Task 2 Step 3 调用，签名一致 ✓
- `pendingCategoryGroupId: Int?` — Task 2 Step 1 定义，Task 2 Step 3-4 使用，类型一致 ✓
- `groupCategoryNames: Map<Int, String?>` — Task 2 Step 2 派生，Task 2 Step 3 传参，Task 1 Step 1 接收，类型一致 ✓
- `onCategoryClick: ((Int) -> Unit)?` — Task 1 Step 1 定义，Task 2 Step 3 传参，类型一致 ✓
- `onCategoryClear: ((Int) -> Unit)?` — Task 1 Step 1 定义，Task 2 Step 3 传参，类型一致 ✓

**4. 工作区规则遵守**：
- `编译验证.md`：Task 4 Step 1 询问用户 ✓
- `git提交.md`：Task 6 Step 1 询问用户 ✓
- `import语句检查.md`：Task 1 Step 4 / Task 2 Step 5 / Task 3 Step 9 ✓
- `api不可用处理规则.md`：本次改动不涉及新 API，Compose BOM 兼容 ✓
- `entity与migration同步检查.md`：本次改动不涉及 Entity/Migration ✓
- `ui设计规范.md`：本次改动不涉及视觉样式 ✓
- `调用AskUserQuestion工具询问.md`：Task 4 / Task 6 使用 ✓
- `优化建议.md`：将在最终总结中提供 ✓

---

## 执行交接

计划已完成并保存至 `docs/superpowers/plans/2026-06-28-待办卡片多容器分类独立设置修复.md`。两种执行方式：

**1. Subagent-Driven（推荐）** — 每个 Task 派发独立 subagent，Task 间审查，快速迭代

**2. Inline Execution** — 在当前会话中按 executing-plans 批量执行，带 checkpoint 审查

请选择执行方式。
