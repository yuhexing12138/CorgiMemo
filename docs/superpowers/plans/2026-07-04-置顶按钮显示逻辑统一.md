# 置顶按钮显示逻辑统一 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将置顶按钮显示阈值从 `pinnedCount >= 4` 改为 `pinnedCount >= 1`，同步统一 PendingDivider 位置逻辑，消除 Case A/B 分支。

**Architecture:** 修改 4 处代码：HomeViewModel.kt 的 `pendingCount` 简化为单一逻辑；HomeScreen.kt 的 `displayItems` 构建改为单分支；HomeScreen.kt 的 `midPendingDividerIndex` 阈值调整；HomeViewModel.kt 的 `reorderOnDisplayList` 注释更新。新增 3 个 ViewModel 层回归测试覆盖 pinnedCount=0/1/3 场景。

**Tech Stack:** Kotlin、Jetpack Compose、JUnit 4、MockK、kotlinx-coroutines-test

---

## File Structure

| 文件 | 责任 | 操作 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt` | `pendingCount` 定义 + `reorderOnDisplayList` 注释 | 修改 L200-L216, L816-L820 |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | `displayItems` 构建 + `midPendingDividerIndex` | 修改 L744-L779, L807-L810 |
| `app/src/test/java/com/corgimemo/app/viewmodel/HomeViewModelReorderTest.kt` | ViewModel 测试 | 新增 3 个测试方法 |

---

### Task 1: 简化 HomeViewModel.kt 的 pendingCount

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt:200-216`

- [ ] **Step 1: 修改 pendingCount 定义**

定位到 `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt` 第 200-216 行的 `pendingCount` 定义。

将以下旧代码：

```kotlin
    /**
     * V2.11: 待完成待办总数(动态计算)
     *
     * 位置语义根据置顶数量动态调整:
     * - 置顶 ≤ 3:待完成按钮在最前,代表所有待完成(含置顶)
     * - 置顶 ≥ 4:待完成按钮在置顶区后,仅代表非置顶
     */
    val pendingCount: StateFlow<Int> = combine(_todos, pinnedCount) { todos, pinnedN ->
        val nonPinned = todos.count { !it.isPinned && it.status == 0 }
        if (pinnedN <= 3) {
            // Case B:按钮在最前,代表所有待完成(含置顶)
            todos.count { it.status == 0 }
        } else {
            // Case A:按钮在置顶区后,仅代表非置顶
            nonPinned
        }
    }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), 0)
```

替换为：

```kotlin
    /**
     * 待完成待办总数（仅非置顶）
     *
     * 新逻辑：pinnedCount >= 1 时显示 PinnedDivider，PendingDivider 始终只代表非置顶待完成。
     * - pinnedCount >= 1：PendingDivider 在置顶区后，count = 非置顶待完成数
     * - pinnedCount == 0：无 PinnedDivider，PendingDivider 代表所有待完成（此时都是非置顶）
     *
     * 两种情况 count 都是 "非置顶待完成数"，逻辑统一。
     */
    val pendingCount: StateFlow<Int> = _todos.map { todos ->
        todos.count { !it.isPinned && it.status == 0 }
    }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), 0)
```

- [ ] **Step 2: 检查 import 语句完整性**

按 workspace rule 检查 `HomeViewModel.kt` 的 import 语句。本次修改移除了 `combine(_todos, pinnedCount)` 调用，但 `combine` 仍可能在文件其他地方使用，无需移除 import。新增 `_todos.map` 已有 `map` import。无需新增 import。

- [ ] **Step 3: 不提交，进入下一个 Task**

---

### Task 2: 修改 HomeScreen.kt 的 displayItems 构建

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt:744-779`

- [ ] **Step 1: 修改 displayItems 构建逻辑**

定位到 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 第 744-779 行的 `buildList { ... }` 块。

将以下旧代码：

```kotlin
                        val displayItems = remember(
                            filteredPending, filteredCompleted,
                            showPinned, showPending, showCompleted,
                            pinnedCount, pendingCount, completedCount,
                            hideCompletedItems
                        ) {
                            buildList {
                                if (pinnedCount >= 4) {
                                    // ===== Case A:置顶 ≥ 4 =====
                                    // 1. 置顶区(顶部)
                                    add(DisplayItem.PinnedDivider(
                                        count = pinnedCount,
                                        isExpanded = showPinned
                                    ))
                                    if (showPinned) {
                                        filteredPending.filter { it.isPinned }
                                            .forEach { add(DisplayItem.Todo(it)) }
                                    }
                                    // 2. 待完成区(置顶区之后)
                                    add(DisplayItem.PendingDivider(
                                        count = pendingCount,
                                        isExpanded = showPending
                                    ))
                                    if (showPending) {
                                        filteredPending.filter { !it.isPinned }
                                            .forEach { add(DisplayItem.Todo(it)) }
                                    }
                                } else {
                                    // ===== Case B:置顶 < 4 =====
                                    // 待完成按钮在最前(代表所有待完成)
                                    add(DisplayItem.PendingDivider(
                                        count = pendingCount,
                                        isExpanded = showPending
                                    ))
                                    if (showPending) {
                                        // 置顶先,非置顶后
                                        filteredPending.filter { it.isPinned }
                                            .forEach { add(DisplayItem.Todo(it)) }
                                        filteredPending.filter { !it.isPinned }
                                            .forEach { add(DisplayItem.Todo(it)) }
                                    }
                                }
                                // 3. 已完成区(原有逻辑)
                                if (!hideCompletedItems && completedCount > 0) {
                                    add(DisplayItem.CompletedDivider(
                                        count = completedCount,
                                        isExpanded = showCompleted
                                    ))
                                    if (showCompleted) {
                                        filteredCompleted.forEach { add(DisplayItem.Todo(it)) }
                                    }
                                }
                            }
                        }
```

替换为：

```kotlin
                        val displayItems = remember(
                            filteredPending, filteredCompleted,
                            showPinned, showPending, showCompleted,
                            pinnedCount, pendingCount, completedCount,
                            hideCompletedItems
                        ) {
                            buildList {
                                // 置顶区（仅当有置顶待办时显示）
                                if (pinnedCount >= 1) {
                                    add(DisplayItem.PinnedDivider(
                                        count = pinnedCount,
                                        isExpanded = showPinned
                                    ))
                                    if (showPinned) {
                                        filteredPending.filter { it.isPinned }
                                            .forEach { add(DisplayItem.Todo(it)) }
                                    }
                                }
                                // 待完成区（始终显示，代表非置顶待完成）
                                add(DisplayItem.PendingDivider(
                                    count = pendingCount,
                                    isExpanded = showPending
                                ))
                                if (showPending) {
                                    filteredPending.filter { !it.isPinned }
                                        .forEach { add(DisplayItem.Todo(it)) }
                                }
                                // 已完成区（原有逻辑不变）
                                if (!hideCompletedItems && completedCount > 0) {
                                    add(DisplayItem.CompletedDivider(
                                        count = completedCount,
                                        isExpanded = showCompleted
                                    ))
                                    if (showCompleted) {
                                        filteredCompleted.forEach { add(DisplayItem.Todo(it)) }
                                    }
                                }
                            }
                        }
```

- [ ] **Step 2: 检查 import 语句完整性**

按 workspace rule 检查 `HomeScreen.kt` 的 import 语句。本次修改未引入新类型或新函数，仅修改 buildList 内部逻辑，无需新增 import。

- [ ] **Step 3: 不提交，进入下一个 Task**

---

### Task 3: 修改 HomeScreen.kt 的 midPendingDividerIndex

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt:807-810`

- [ ] **Step 1: 修改 midPendingDividerIndex 阈值**

定位到 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 第 807-810 行的 `midPendingDividerIndex` 定义。

将以下旧代码：

```kotlin
                            val midPendingDividerIndex =
                                if (pinnedCount >= 4)
                                    displayItems.indexOfFirst { it is DisplayItem.PendingDivider }
                                else -1
```

替换为：

```kotlin
                            val midPendingDividerIndex =
                                if (pinnedCount >= 1)
                                    displayItems.indexOfFirst { it is DisplayItem.PendingDivider }
                                else -1
```

- [ ] **Step 2: 不提交，进入下一个 Task**

---

### Task 4: 更新 reorderOnDisplayList 注释

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt:816-820`

- [ ] **Step 1: 更新注释中的 Case A/B 描述**

定位到 `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt` 第 816-820 行的注释块。

将以下旧注释：

```kotlin
            // [Bug 修复] 不再假设 dividerIndex = pendingList.size。
            // 已完成分隔按钮在 displayItems 中的真实索引由 HomeScreen 传入。
            // Case A (置顶 ≥ 4): dividerIndex = pendingList.size + 2 (PinnedDivider + PendingDivider + 待办项)
            // Case B (置顶 < 4): dividerIndex = pendingList.size + 1 (PendingDivider + 待办项)
            // dividerIndex = -1: 无已完成区
```

替换为：

```kotlin
            // [Bug 修复] 不再假设 dividerIndex = pendingList.size。
            // 已完成分隔按钮在 displayItems 中的真实索引由 HomeScreen 传入。
            // pinnedCount >= 1: dividerIndex = pendingList.size + 2 (PinnedDivider + PendingDivider + 待办项)
            // pinnedCount == 0: dividerIndex = pendingList.size + 1 (PendingDivider + 待办项)
            // dividerIndex = -1: 无已完成区
```

- [ ] **Step 2: 不提交，进入下一个 Task**

---

### Task 5: 编译验证

- [ ] **Step 1: 编译验证**

Run:
```bash
./gradlew :app:assembleDebug --console=plain
```

Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 不提交，进入下一个 Task**

---

### Task 6: 新增 ViewModel 层回归测试

**Files:**
- Modify: `app/src/test/java/com/corgimemo/app/viewmodel/HomeViewModelReorderTest.kt`

**说明：** 新增 3 个测试覆盖 pinnedCount=0/1/3 场景，验证新阈值下跨区拖拽和同区拖拽行为正确。

- [ ] **Step 1: 在 HomeViewModelReorderTest.kt 末尾、`// ==================== 测试辅助方法 ====================` 注释行之前追加 3 个测试方法**

```kotlin
    /**
     * 场景：pinnedCount=1 时跨区拖拽（N 拖到 P 上方）
     *
     * 验证新阈值 pinnedCount >= 1 下，PinnedDivider 显示，
     * 非置顶项拖到置顶区时 isPinned 正确翻转。
     *
     * displayItems: [PinnedDivider(0), P1(1), PendingDivider(2), N1(3), N2(4)]
     * 拖 N1(3) 到 P1(1) 上方 → toIndex=1, crossedPinnedZone=true
     * 预期：N1.isPinned 翻转为 true
     */
    @Test
    fun `pinnedCount 为 1 时跨区拖拽应翻转 isPinned`() = runTest(testDispatcher) {
        val todos = listOf(
            testTodo(1, isPinned = true, sortOrder = 0),
            testTodo(2, isPinned = false, sortOrder = 1),
            testTodo(3, isPinned = false, sortOrder = 2)
        )
        setupTodos(todos)

        // displayItems: [PinnedDivider(0), P1(1), PendingDivider(2), N1(3), N2(4)]
        // 拖 N1(3) 到 P1(1) 上方 → fromIndex=3, toIndex=1
        viewModel.reorderOnDisplayList(
            fromIndex = 3,
            toIndex = 1,
            dividerIndex = -1,
            crossedPinnedZone = true,
            pendingStartIndex = 1,
            midPendingDividerIndex = 2
        )

        // 验证：N1(id=2) isPinned 翻转为 true
        coVerify(exactly = 1) { mockTodoRepository.updateTodo(match { it.id == 2L && it.isPinned }) }
    }

    /**
     * 场景：pinnedCount=3 时跨区拖拽（N 拖到 P 上方）
     *
     * 验证阈值不再是 4，pinnedCount=3 也能正常跨区拖拽。
     *
     * displayItems: [PinnedDivider(0), P1(1), P2(2), P3(3), PendingDivider(4), N1(5), N2(6)]
     * 拖 N1(5) 到 P1(1) 上方 → toIndex=1, crossedPinnedZone=true
     * 预期：N1.isPinned 翻转为 true
     */
    @Test
    fun `pinnedCount 为 3 时跨区拖拽应翻转 isPinned`() = runTest(testDispatcher) {
        val todos = listOf(
            testTodo(1, isPinned = true, sortOrder = 0),
            testTodo(2, isPinned = true, sortOrder = 1),
            testTodo(3, isPinned = true, sortOrder = 2),
            testTodo(4, isPinned = false, sortOrder = 3),
            testTodo(5, isPinned = false, sortOrder = 4)
        )
        setupTodos(todos)

        // displayItems: [PinnedDivider(0), P1(1), P2(2), P3(3), PendingDivider(4), N1(5), N2(6)]
        // 拖 N1(5) 到 P1(1) 上方 → fromIndex=5, toIndex=1
        viewModel.reorderOnDisplayList(
            fromIndex = 5,
            toIndex = 1,
            dividerIndex = -1,
            crossedPinnedZone = true,
            pendingStartIndex = 1,
            midPendingDividerIndex = 4
        )

        // 验证：N1(id=4) isPinned 翻转为 true
        coVerify(exactly = 1) { mockTodoRepository.updateTodo(match { it.id == 4L && it.isPinned }) }
    }

    /**
     * 场景：pinnedCount=0 时同区拖拽不翻转 isPinned
     *
     * 验证无置顶时，待完成区内拖拽不会误翻转 isPinned。
     *
     * displayItems: [PendingDivider(0), N1(1), N2(2), N3(3)]
     * 拖 N1(1) 到 N2(2) 上方 → toIndex=2, crossedPinnedZone=false
     * 预期：不调用 updateTodo 翻转 isPinned
     */
    @Test
    fun `pinnedCount 为 0 时同区拖拽不应翻转 isPinned`() = runTest(testDispatcher) {
        val todos = listOf(
            testTodo(1, isPinned = false, sortOrder = 0),
            testTodo(2, isPinned = false, sortOrder = 1),
            testTodo(3, isPinned = false, sortOrder = 2)
        )
        setupTodos(todos)

        // displayItems: [PendingDivider(0), N1(1), N2(2), N3(3)]
        // 拖 N1(1) 到 N2(2) 上方 → fromIndex=1, toIndex=2
        viewModel.reorderOnDisplayList(
            fromIndex = 1,
            toIndex = 2,
            dividerIndex = -1,
            crossedPinnedZone = false,
            pendingStartIndex = 1,
            midPendingDividerIndex = -1
        )

        // 验证：不调用 updateTodo（isPinned 未变，status 未变）
        coVerify(exactly = 0) { mockTodoRepository.updateTodo(any()) }
        // 验证：调用 updateTodos 重排 sortOrder
        coVerify(atLeast = 1) { mockTodoRepository.updateTodos(any()) }
    }

```

- [ ] **Step 2: 检查 import 语句完整性**

按 workspace rule 检查 `HomeViewModelReorderTest.kt` 的 import 语句。新增测试使用了 `match`、`coVerify`、`any`，这些已在文件顶部 import（`io.mockk.match`、`io.mockk.coVerify`、`io.mockk.any`）。无需新增 import。

- [ ] **Step 3: 运行新增测试验证通过**

Run:
```bash
./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.viewmodel.HomeViewModelReorderTest.pinnedCount 为 1 时跨区拖拽应翻转 isPinned" --tests "com.corgimemo.app.viewmodel.HomeViewModelReorderTest.pinnedCount 为 3 时跨区拖拽应翻转 isPinned" --tests "com.corgimemo.app.viewmodel.HomeViewModelReorderTest.pinnedCount 为 0 时同区拖拽不应翻转 isPinned" --console=plain
```

Expected: 3 个测试 PASSED

- [ ] **Step 4: 不提交，进入下一个 Task**

---

### Task 7: 全量测试 + 真机验证

- [ ] **Step 1: 运行全量单元测试**

Run:
```bash
./gradlew :app:testDebugUnitTest --console=plain
```

Expected: BUILD SUCCESSFUL，所有测试通过（含原有 27 个 + 新增 3 个）

- [ ] **Step 2: 真机手动验证**

在真机上执行以下场景：

**场景 1：pinnedCount=0**
1. 创建 5 个非置顶待办
2. 验证：只显示 PendingDivider(count=5)，无 PinnedDivider

**场景 2：pinnedCount=1**
1. 将 1 个待办设为置顶
2. 验证：显示 PinnedDivider(count=1) + PendingDivider(count=4)

**场景 3：pinnedCount=3**
1. 将 3 个待办设为置顶
2. 验证：显示 PinnedDivider(count=3) + PendingDivider(count=2)

**场景 4：pinnedCount=5**
1. 将 5 个待办设为置顶
2. 验证：显示 PinnedDivider(count=5) + PendingDivider(count=0)

**场景 5：跨区拖拽（pinnedCount=2）**
1. 2 个置顶 + 3 个非置顶
2. 长按非置顶项，拖到置顶区释放
3. 验证：被拖项变为置顶，落入置顶区

**场景 6：同区拖拽（pinnedCount=2）**
1. 2 个置顶 + 3 个非置顶
2. 长按非置顶项，在待完成区内移动释放
3. 验证：被拖项保持非置顶，位置交换

- [ ] **Step 3: 不提交，等待用户确认**

---

### Task 8: 询问用户是否进行 git 提交

按 workspace rule（`c:\Users\EDY\Desktop\CorgiMemo\.trae\rules\git提交.md`），任务完成后必须询问用户是否进行 git 提交。

- [ ] **Step 1: 询问用户是否进行 git 提交**

向用户呈现本次变更摘要：

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt` | 修改 | pendingCount 简化为单一逻辑 + reorderOnDisplayList 注释更新 |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 修改 | displayItems 构建单分支 + midPendingDividerIndex 阈值调整 |
| `app/src/test/java/com/corgimemo/app/viewmodel/HomeViewModelReorderTest.kt` | 新增测试 | 3 个 pinnedCount=0/1/3 场景回归测试 |
| `docs/superpowers/specs/2026-07-04-置顶按钮显示逻辑统一-design.md` | 新增 | 设计文档 |
| `docs/superpowers/plans/2026-07-04-置顶按钮显示逻辑统一.md` | 新增 | 实施计划 |

若用户选择提交：
- 智能生成中文提交信息
- 执行 `git add <specific files>` + `git commit -m "..."`

若用户选择不提交：跳过提交步骤。

---

## Self-Review

### Spec 覆盖检查

| Spec 要求 | 对应 Task |
|---|---|
| pendingCount 简化为 `_todos.map { ... }` | Task 1 Step 1 |
| displayItems 构建使用 `if (pinnedCount >= 1)` 单分支 | Task 2 Step 1 |
| midPendingDividerIndex 使用 `if (pinnedCount >= 1)` 阈值 | Task 3 Step 1 |
| reorderOnDisplayList 注释更新 | Task 4 Step 1 |
| `./gradlew assembleDebug` 编译通过 | Task 5 Step 1 |
| 新增 3 个 ViewModel 层测试 | Task 6 Step 1 |
| `./gradlew test` 全部测试通过 | Task 7 Step 1 |
| 真机验证：pinnedCount=0/1/3/5 四个场景 | Task 7 Step 2 场景 1-4 |
| 真机验证：跨区拖拽在 pinnedCount=1/3 时正常工作 | Task 7 Step 2 场景 5-6 |
| import 语句检查 | Task 1 Step 2, Task 2 Step 2, Task 6 Step 2 |
| git 提交询问 | Task 8 |

### Placeholder 扫描

无 TBD/TODO/placeholder。所有代码块完整。✓

### 类型一致性

- `pendingCount`：`StateFlow<Int>`（与原类型一致）✓
- `midPendingDividerIndex`：`Int`（与原类型一致）✓
- `testTodo` 辅助方法：已有，无需新增 ✓
- `setupTodos` 辅助方法：已有，无需新增 ✓
- `match`、`coVerify`、`any`：已 import ✓

### 后续可优化方向（来自 spec 第 8 节）

1. 抽取 DisplayItemBuilder：将 displayItems 构建逻辑抽取为独立类
2. 动画过渡：PinnedDivider 出现可添加入场动画
3. 空状态处理：PendingDivider count=0 时可考虑隐藏或显示空状态提示
