# 完成待办鼓励语改 Snackbar — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把"完成待办"时的全屏居中 `CelebrationOverlay` 改为屏幕底部 Snackbar 提示，保留 `GlowOverlay` 边缘光晕，单条与批量完成共用 Snackbar 通道。

**Architecture:** 全部改动集中在 `HomeScreen.kt` 渲染层。删除包裹 `CelebrationOverlay` 的 `AnimatedVisibility` 块；新增 `LaunchedEffect(celebrationState)` 监听 `_celebrationState.isShowing` 变化并通过 `snackbarHostState.showSnackbar` 弹出分级鼓励语。`GlowOverlay` 的 `AnimatedVisibility(visible = isShowing)` 保持不变。ViewModel 零改动（`isShowing` 状态机复用即可）。

**Tech Stack:** Jetpack Compose (BOM 2026.04.01 + 1.9.2) / Material 3 / Coroutines StateFlow / SnackbarHostState

**Spec:** [2026-07-14-完成待办鼓励语改Snackbar-design.md](../specs/2026-07-14-完成待办鼓励语改Snackbar-design.md)

---

## 文件结构

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 删除 `CelebrationOverlay` 渲染；新增 `LaunchedEffect(celebrationState)` 显示 Snackbar；保留 `GlowOverlay` |

> ViewModel 零改动。

---

## Task 1: 删除 CelebrationOverlay 全屏渲染

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

### 步骤

- [ ] **Step 1.1: 删除 CelebrationOverlay 的 AnimatedVisibility 块**

在 [HomeScreen.kt#L1027-L1040](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt#L1027-L1040) 找到如下代码块并**整段删除**：

```kotlin
// 对齐组件容器（包裹需要使用 align 的组件）
Box(modifier = Modifier.fillMaxSize()) {
    // 庆祝覆盖层
    AnimatedVisibility(
        visible = celebrationState.isShowing,
        enter = fadeIn() + slideInVertically { it / 2 },
        exit = fadeOut() + slideOutVertically { -it / 2 },
        modifier = Modifier.align(Alignment.Center)
    ) {
        CelebrationOverlay(
            level = celebrationState.level,
            message = celebrationState.message
        )
    }
```

> **注意**：只删除到内层 `AnimatedVisibility` 块结束（含其尾部 `}`），保留外层 `Box(modifier = Modifier.fillMaxSize()) { ... }` 以及其后的 `GlowOverlay` 块。

删除后，Box 容器内的开头应只保留 `GlowOverlay` 的 `AnimatedVisibility` 块，整体结构变为：

```kotlin
// 对齐组件容器（包裹需要使用 align 的组件）
Box(modifier = Modifier.fillMaxSize()) {
    // CelebrationOverlay 已移除：分级鼓励语改由 Snackbar 显示
    // （见下方 LaunchedEffect(celebrationState)）

    // 覆盖层效果
    AnimatedVisibility(
        visible = celebrationState.isShowing,
        enter = fadeIn(),
        exit = fadeOut()
    ) {
        GlowOverlay(level = celebrationState.level)
    }
}
```

- [ ] **Step 1.2: 验证 CelebrationOverlay 函数体仍保留**

`CelebrationOverlay` 与 `GlowOverlay` 均为文件内的 top-level `@Composable fun`（[HomeScreen.kt#L2256](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt#L2256) 与 [HomeScreen.kt#L2358](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt#L2358)），本任务**只删除调用，不删除函数定义**。`CelebrationOverlay` 函数保留可避免误删；若后续确认无需复用，可单独提交清理。

---

## Task 2: 新增 celebrationState 监听 → 显示 Snackbar

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

### 步骤

- [ ] **Step 2.1: 在 LaunchedEffect(pendingCompleteTodo) 之后插入新 LaunchedEffect**

定位 [HomeScreen.kt#L377-L388](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt#L377-L388) 的 `LaunchedEffect(pendingCompleteTodo)` 块，**在该块结束后的下一个空行后**，插入以下代码：

```kotlin
/**
 * 监听 CelebrationState 变化，通过 Snackbar 显示分级鼓励语
 *
 * 触发时机：
 * - 单条完成：handleTaskCompleted 设置 _celebrationState.isShowing = true
 * - 批量完成：triggerBatchCelebration 设置 _celebrationState.isShowing = true
 *
 * 显示策略：
 * - 等待升级弹窗（showLevelUp）/ 成就弹窗（showAchievementUnlock）关闭后再显示
 * - 避免与全屏 Dialog 抢占屏幕
 * - isShowing: false → true 时启动一次 Snackbar；不持续监听
 *
 * 关联文件：HomeViewModel.kt 中 _celebrationState 与 getEncouragementMessage
 */
LaunchedEffect(
    celebrationState.isShowing,
    celebrationState.message,
    showLevelUp,
    showAchievementUnlock
) {
    // 只在 isShowing 由 false 变 true 时触发
    if (!celebrationState.isShowing) {
        return@LaunchedEffect
    }
    // 等待弹窗关闭（与 pendingBatchCompleteCount 策略一致）
    if (showLevelUp != null || showAchievementUnlock != null) {
        return@LaunchedEffect
    }

    val emoji = when (celebrationState.level) {
        CelebrationLevel.LOW -> "😊"
        CelebrationLevel.MEDIUM -> "⭐"
        CelebrationLevel.HIGH -> "🎉"
        CelebrationLevel.SUPER -> "🏆"
    }
    val message = "$emoji ${celebrationState.message}"

    snackbarHostState.showSnackbar(
        message = message,
        duration = SnackbarDuration.Short
    )
}
```

- [ ] **Step 2.2: 确认必要 import 已存在**

下方 import 在文件中已存在（无需新增）：
- `androidx.compose.material3.SnackbarDuration`（line 30 区域）
- `androidx.compose.runtime.LaunchedEffect`（文件顶部）
- `com.corgimemo.app.viewmodel.CelebrationLevel`（随 `celebrationState` 一起引入）

若工程编译报"unresolved reference"，按 IDE 提示补全 import。

- [ ] **Step 2.3: 编译检查（询问用户）**

询问用户是否进行编译验证。若用户同意运行：

```powershell
.\gradlew.bat :app:compileDebugKotlin
```

预期：BUILD SUCCESSFUL，无新增 warning。

> **若用户拒绝编译**，跳到 Task 3。

---

## Task 3: 提交

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

### 步骤

- [ ] **Step 3.1: 询问用户是否 git 提交**

按项目规则 `git提交.md`，完成任务后必须询问用户是否进行 git 提交。

- [ ] **Step 3.2: 提交（仅当用户同意时执行）**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt
git commit -m "feat: 完成待办鼓励语改 Snackbar 提示，保留边缘光晕"
```

> 提交信息必须用中文（按 `git提交.md` 规则）。

---

## 验证清单

按 [设计文档"验证清单"](../specs/2026-07-14-完成待办鼓励语改Snackbar-design.md#验证清单) 逐项验证：

- [ ] 单条完成显示两个顺序 Snackbar：先"✅ 'XXX' 已完成"（带撤销），再分级鼓励语
- [ ] 批量完成显示两个顺序 Snackbar：先"✅ 已完成 N 项"，再分级鼓励语
- [ ] MEDIUM/HIGH/SUPER 级别边缘光晕保留 2-4s
- [ ] LOW 级别无光晕
- [ ] 升级弹窗/成就弹窗期间不显示鼓励语 Snackbar，关闭后正常显示
- [ ] 撤销按钮仍然有效
- [ ] 柯基姿态变化（`_currentPose`）与之前一致
- [ ] 编译通过，无 Lint 警告
- [ ] 无 `isShowing` 误删（应保留，ViewModel 仍依赖其状态机）

---

## 不在本次改动范围

- ViewModel 中 `CelebrationState` 结构调整
- ViewModel 中 `getEncouragementMessage` / `getCelebrationDuration` 调整
- `CelebrationOverlay` 函数体删除（保留以备复用）
- Snackbar 合并"已完成 + 鼓励语"为单条（后续可优化 #1）
- Snackbar 文案 i18n 化（后续可优化 #4）
- 批量完成时 `isShowing` 自动重置（后续可优化 #2）
