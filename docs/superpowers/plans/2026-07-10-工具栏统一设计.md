# 工具栏统一设计 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 统一灵感编辑页底部上下两层工具栏的高度(56dp)、背景色(暖米色)、按钮尺寸(IconButton 40dp + Icon 22dp)和图标(Material Icons)

**Architecture:** 修改两个文件：RichTextFormatToolbar.kt(重构 FormatToolButton→FormatIconButton，移除白色背景)和 InspirationEditBottomBar.kt(垂直内边距 10dp→8dp)。上层格式工具栏通过移除自身背景继承父 Surface 的暖米色。

**Tech Stack:** Jetpack Compose, Material 3, Material Icons (filled), Compose BOM 2026.04.01

**Spec:** `docs/superpowers/specs/2026-07-10-工具栏统一设计-design.md`

---

## File Structure

| 文件 | 路径 | 责任 | 改动类型 |
|------|------|------|------|
| RichTextFormatToolbar.kt | `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/RichTextFormatToolbar.kt` | 富文本格式工具栏 UI | 重构按钮组件 + 移除背景 + 调整分隔符 |
| InspirationEditBottomBar.kt | `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationEditBottomBar.kt` | 底部栏容器 | 垂直内边距调整 |

---

### Task 1: 重构 RichTextFormatToolbar.kt — 替换按钮组件和图标

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/RichTextFormatToolbar.kt`

- [ ] **Step 1: 更新 import 语句**

在文件顶部，移除不再需要的 import，添加新的 Material Icon import。

**移除以下 import（共 4 行）：**
```kotlin
import androidx.compose.foundation.clickable
import androidx.compose.foundation.layout.Box
import androidx.compose.material3.Text
import androidx.compose.ui.unit.sp
```

**在 `import androidx.compose.material3.MaterialTheme` 之后添加：**
```kotlin
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.Code
import androidx.compose.material.icons.filled.FormatAlignCenter
import androidx.compose.material.icons.filled.FormatAlignLeft
import androidx.compose.material.icons.filled.FormatAlignRight
import androidx.compose.material.icons.filled.FormatBold
import androidx.compose.material.icons.filled.FormatItalic
import androidx.compose.material.icons.filled.FormatListBulleted
import androidx.compose.material.icons.filled.FormatListNumbered
import androidx.compose.material.icons.filled.FormatStrikethrough
import androidx.compose.material.icons.filled.FormatUnderlined
import androidx.compose.material.icons.filled.Link
import androidx.compose.material3.Icon
import androidx.compose.material3.IconButton
```

**在 `import androidx.compose.ui.graphics.Color` 之后添加：**
```kotlin
import androidx.compose.ui.graphics.vector.ImageVector
```

**保留以下 import（虽然原 FormatToolButton 用到，但 isActive 检查仍需要）：**
- `FontWeight` — 用于 `state.currentSpanStyle.fontWeight == FontWeight.Bold`
- `FontStyle` — 用于 `state.currentSpanStyle.fontStyle == FontStyle.Italic`
- `TextDecoration` — 用于 `textDecoration?.contains(TextDecoration.Underline/LineThrough)`
- `background` — 用于 FormatIconButton 激活态背景
- `clip` — 用于 FormatIconButton 圆角
- `RoundedCornerShape` — 用于 clip 参数
- `size` — 用于 IconButton 和 Icon 尺寸

- [ ] **Step 2: 移除 Row 的白色背景**

将 `RichTextFormatToolbar` 函数中 Row 的 modifier 里移除 `.background(MaterialTheme.colorScheme.surface)`。

**改动前（第 74-79 行）：**
```kotlin
    Row(
        modifier = modifier
            .fillMaxWidth()
            .background(MaterialTheme.colorScheme.surface)
            .horizontalScroll(rememberScrollState())
            .padding(horizontal = 12.dp, vertical = 8.dp),
```

**改动后：**
```kotlin
    Row(
        modifier = modifier
            .fillMaxWidth()
            .horizontalScroll(rememberScrollState())
            .padding(horizontal = 12.dp, vertical = 8.dp),
```

- [ ] **Step 3: 替换 11 个按钮调用为 FormatIconButton + Material Icon**

将所有 `FormatToolButton(text=..., isActive=..., onClick=..., contentDescription=...)` 调用替换为 `FormatIconButton(imageVector=..., isActive=..., onClick=..., contentDescription=...)`。

**第一组（基础样式，第 84-112 行）替换为：**
```kotlin
        FormatButtonGroup {
            FormatIconButton(
                imageVector = Icons.Default.FormatBold,
                isActive = state.currentSpanStyle.fontWeight == FontWeight.Bold,
                onClick = onToggleBold,
                contentDescription = "加粗"
            )
            FormatIconButton(
                imageVector = Icons.Default.FormatItalic,
                isActive = state.currentSpanStyle.fontStyle == FontStyle.Italic,
                onClick = onToggleItalic,
                contentDescription = "斜体"
            )
            FormatIconButton(
                imageVector = Icons.Default.FormatUnderlined,
                isActive = state.currentSpanStyle.textDecoration?.contains(TextDecoration.Underline) == true,
                onClick = onToggleUnderline,
                contentDescription = "下划线"
            )
            FormatIconButton(
                imageVector = Icons.Default.FormatStrikethrough,
                isActive = state.currentSpanStyle.textDecoration?.contains(TextDecoration.LineThrough) == true,
                onClick = onToggleStrikethrough,
                contentDescription = "删除线"
            )
        }
```

**第二组（列表，第 117-130 行）替换为：**
```kotlin
        FormatButtonGroup {
            FormatIconButton(
                imageVector = Icons.Default.FormatListBulleted,
                isActive = state.isUnorderedList,
                onClick = onInsertUnorderedList,
                contentDescription = "无序列表"
            )
            FormatIconButton(
                imageVector = Icons.Default.FormatListNumbered,
                isActive = state.isOrderedList,
                onClick = onInsertOrderedList,
                contentDescription = "有序列表"
            )
        }
```

**第三组（对齐，第 135-157 行）替换为：**
```kotlin
        FormatButtonGroup {
            FormatIconButton(
                imageVector = Icons.Default.FormatAlignLeft,
                isActive = false,
                onClick = onAlignLeft,
                contentDescription = "左对齐"
            )
            FormatIconButton(
                imageVector = Icons.Default.FormatAlignCenter,
                isActive = false,
                onClick = onAlignCenter,
                contentDescription = "居中对齐"
            )
            FormatIconButton(
                imageVector = Icons.Default.FormatAlignRight,
                isActive = false,
                onClick = onAlignRight,
                contentDescription = "右对齐"
            )
        }
```

**第四组（高级，第 162-176 行）替换为：**
```kotlin
        FormatButtonGroup {
            FormatIconButton(
                imageVector = Icons.Default.Link,
                isActive = state.isLink,
                onClick = onInsertLink,
                contentDescription = "插入链接"
            )
            FormatIconButton(
                imageVector = Icons.Default.Code,
                isActive = state.isCodeSpan,
                onClick = onToggleCodeSpan,
                contentDescription = "代码块"
            )
        }
```

- [ ] **Step 4: 调整分隔符高度**

将 `ToolbarDivider` 中的 `height = 24.dp` 改为 `height = 28.dp`。

**改动前（第 200-207 行）：**
```kotlin
@Composable
private fun ToolbarDivider() {
    HorizontalDivider(
        modifier = Modifier
            .size(width = 1.dp, height = 24.dp)
            .padding(vertical = 4.dp),
        color = MaterialTheme.colorScheme.outline.copy(alpha = 0.3f)
    )
}
```

**改动后：**
```kotlin
@Composable
private fun ToolbarDivider() {
    HorizontalDivider(
        modifier = Modifier
            .size(width = 1.dp, height = 28.dp)
            .padding(vertical = 4.dp),
        color = MaterialTheme.colorScheme.outline.copy(alpha = 0.3f)
    )
}
```

- [ ] **Step 5: 替换 FormatToolButton 函数为 FormatIconButton**

删除整个 `FormatToolButton` 函数（第 221-268 行），替换为 `FormatIconButton`。

**删除旧函数，替换为：**
```kotlin
/**
 * 单个格式化图标按钮
 *
 * 统一使用 IconButton 40dp + Icon 22dp，与下层 BottomBarButton 一致。
 * 激活态：浅暖橙背景 + 主题 primary 图标色。
 *
 * @param imageVector Material Icon 图标
 * @param isActive 是否激活
 * @param onClick 点击回调
 * @param contentDescription 无障碍描述
 */
@Composable
private fun FormatIconButton(
    imageVector: ImageVector,
    isActive: Boolean,
    onClick: () -> Unit,
    contentDescription: String
) {
    val backgroundColor = if (isActive) {
        Color(0xFFFFE0C0) // 浅暖橙色背景（激活态）
    } else {
        Color.Transparent
    }

    val tint = if (isActive) {
        Color(0xFFFF9A5C) // 主题 primary（激活态，与下层 ⋮ 按钮一致）
    } else {
        MaterialTheme.colorScheme.onSurfaceVariant
    }

    IconButton(
        onClick = onClick,
        modifier = Modifier
            .size(40.dp)
            .clip(RoundedCornerShape(8.dp))
            .background(backgroundColor)
    ) {
        Icon(
            imageVector = imageVector,
            contentDescription = contentDescription,
            tint = tint,
            modifier = Modifier.size(22.dp)
        )
    }
}
```

- [ ] **Step 6: Commit**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/RichTextFormatToolbar.kt
git commit -m "refactor: 格式工具栏统一为 IconButton+Material Icon"
```

---

### Task 2: 调整 InspirationEditBottomBar.kt 垂直内边距

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationEditBottomBar.kt:126`

- [ ] **Step 1: 修改垂直内边距 10dp → 8dp**

**改动前（第 123-128 行）：**
```kotlin
            Row(
                modifier = Modifier
                    .fillMaxWidth()
                    .padding(horizontal = 16.dp, vertical = 10.dp),
                horizontalArrangement = Arrangement.SpaceEvenly,
                verticalAlignment = Alignment.CenterVertically
            ) {
```

**改动后：**
```kotlin
            Row(
                modifier = Modifier
                    .fillMaxWidth()
                    .padding(horizontal = 16.dp, vertical = 8.dp),
                horizontalArrangement = Arrangement.SpaceEvenly,
                verticalAlignment = Alignment.CenterVertically
            ) {
```

- [ ] **Step 2: Commit**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationEditBottomBar.kt
git commit -m "style: 底部栏垂直内边距 10dp→8dp 统一高度"
```

---

### Task 3: Import 检查与编译验证

**Files:**
- Check: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/RichTextFormatToolbar.kt`
- Check: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationEditBottomBar.kt`

- [ ] **Step 1: 检查 RichTextFormatToolbar.kt 的 import 完整性**

确认以下 import 均已存在（Task 1 Step 1 已添加）：
- `Icons` ✓
- `FormatBold`, `FormatItalic`, `FormatUnderlined`, `FormatStrikethrough` ✓
- `FormatListBulleted`, `FormatListNumbered` ✓
- `FormatAlignLeft`, `FormatAlignCenter`, `FormatAlignRight` ✓
- `Link`, `Code` ✓
- `Icon`, `IconButton` ✓
- `ImageVector` ✓

确认以下 import 已移除（不再需要）：
- `clickable` ✓ (IconButton 自带 onClick)
- `Box` ✓ (IconButton 替代)
- `Text` ✓ (Icon 替代)
- `sp` ✓ (无字号设置)

确认以下 import 保留（isActive 检查仍需要）：
- `FontWeight` — `state.currentSpanStyle.fontWeight == FontWeight.Bold`
- `FontStyle` — `state.currentSpanStyle.fontStyle == FontStyle.Italic`
- `TextDecoration` — `TextDecoration.Underline` / `TextDecoration.LineThrough`
- `background` — FormatIconButton 激活态背景
- `clip` — FormatIconButton 圆角
- `RoundedCornerShape` — clip 参数

- [ ] **Step 2: 询问用户是否进行编译验证**

> ⚠️ 按工作区规则，编译验证前必须询问用户。

向用户确认后，执行编译命令：
```bash
./gradlew :app:assembleDebug
```

预期：BUILD SUCCESSFUL

- [ ] **Step 3: 视觉验证（可选，需用户确认）**

在设备/模拟器上运行应用，进入灵感编辑页，点击底部"⋮"按钮展开格式工具栏，验证：
1. 上下两层工具栏高度一致 (56dp)
2. 上下两层背景色一致 (暖米色)
3. 上下两层按钮大小一致 (IconButton 40dp + Icon 22dp)
4. 上层所有按钮显示为 Material Icon (无文字)
5. 点击加粗/斜体等按钮，激活态颜色为 #FF9A5C

---

## Self-Review

**Spec coverage:**
- ✓ 高度统一 56dp → Task 1 (IconButton 40dp + padding 8dp) + Task 2 (padding 10dp→8dp)
- ✓ 背景色统一 → Task 1 Step 2 (移除 Row.background)
- ✓ 按钮组件统一 → Task 1 Step 3+5 (FormatToolButton→FormatIconButton)
- ✓ 图标映射 → Task 1 Step 3 (11 个 Material Icon)
- ✓ 激活状态统一 → Task 1 Step 5 (#FF9A5C + #FFE0C0)
- ✓ 分隔符高度 → Task 1 Step 4 (24dp→28dp)
- ✓ Import 检查 → Task 3

**Placeholder scan:** 无 TBD/TODO，所有代码块完整。✓

**Type consistency:** `FormatIconButton` 签名在 Step 5 定义，在 Step 3 调用，参数一致。✓
