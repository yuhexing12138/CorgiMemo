# 堆叠图展开/收起功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `SwipeableImageStack.kt` 内部通过 `isExpanded` 状态驱动堆叠/展开两种布局切换，实现展开按钮、收起按钮、横向滑动、拖拽互斥等功能，对外保持现有 API 兼容。

**Architecture:** 复用现有 5 个 `Animatable`（positionX/Y/scaleAnim/rotationAnim/zIndexAnim）+ `LaunchedEffect` 自然过渡，不重建卡片实例；同一 `Box` 容器内通过 `isExpanded` 切换目标值、容器宽度、`horizontalScroll` 挂载与 `pointerInput` 挂载。

**Tech Stack:** Kotlin + Jetpack Compose（Material 3）+ Compose BOM 2026.04.01（Compose 1.9.2）

**设计文档：** [docs/superpowers/specs/2026-08-25-堆叠图展开收起功能-design.md](file:///c:/Users/EDY/Desktop/CorgiMemo/docs/superpowers/specs/2026-08-25-堆叠图展开收起功能-design.md)

**项目规则约束：**
- 编译验证前**必须询问用户**（规则：`编译验证.md`）
- AI 不擅自跑 `./gradlew` / `./gradlew assembleDebug`
- 编辑后必须检查 import 缺失（规则：`import语句检查.md`）
- Compose 长效 lambda 用最新状态（规则：`lambda 捕获陷阱防御.md`）
- 任务末尾询问 git 提交（规则：`git提交.md`）

---

## 文件结构

| 文件 | 责任 | 改动类型 |
|------|------|---------|
| `app/src/main/java/com/corgimemo/app/ui/components/SwipeableImageStack.kt` | 堆叠图片组件 | 修改 |
| `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/TimelineInspirationItem.kt` | 调用方 | 无需改动（现有调用已足够） |

### SwipeableImageStack.kt 改动定位（行号基于当前文件）

| 改动区域 | 当前位置 | 改动内容 |
|---------|---------|---------|
| 标准函数签名 | L201-L219 | 新增 `cardGap` 与 `onExpandStateChange` 参数 |
| 重载函数签名（customContent） | L154-L200 | 透传新参数到标准函数 |
| 内部状态 | L235-L273 附近 | 新增 `isExpanded` 和 `expandedScrollState` |
| 容器尺寸计算 | L368-L425 | 根据 `isExpanded` 切换 `boxWidth` |
| 外层 Box modifier | L427-L432 | 条件挂载 `horizontalScroll` |
| 目标值计算 | L485-L512 | 根据 `isExpanded` 切换扇形/横排目标 |
| LaunchedEffect 触发条件 | L528-L544 | 5 个 LaunchedEffect 增加 `isExpanded` 到 key |
| pointerInput 挂载条件 | L632-L633 附近 | 增加 `!isExpanded` 条件 |
| countBadge 显隐 | L879 | 增加 `!isExpanded` 条件 |
| expandButton | L925-L980 | 增加 `!isExpanded` 条件 + onClick 切换 |
| 新增 collapseButton | （尾部追加） | 与 expandButton 样式一致，位于顶卡左侧 |

---

## Task 1: API 扩展 — 新增 `cardGap` 与 `onExpandStateChange` 参数

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/SwipeableImageStack.kt` (L154-L219)

**目的：** 对外暴露 `cardGap`（展开态卡片间距）和 `onExpandStateChange`（状态变化回调）两个参数，为后续展开/收起逻辑铺路。本任务**不涉及**业务逻辑，仅扩展签名。

- [ ] **Step 1.1: 修改标准函数签名（L201-L219），追加 `cardGap` 和 `onExpandStateChange` 参数**

在 `SwipeableImageStack` 标准函数（无 customContent 的版本）参数列表末尾追加：

```kotlin
@Composable
fun SwipeableImageStack(
    imageUris: List<String> = emptyList(),
    modifier: Modifier = Modifier,
    cardWidth: Dp = 300.dp,
    cardHeight: Dp = 400.dp,
    cardRadius: Float = 2f,
    swipeThreshold: Dp = 10.dp,
    maxElasticDistance: Dp = 0.dp,
    tiltAngleStart: Float = 0f,
    xOffset: Dp = 0.dp,
    visibleDepth: Int = 4,
    swipeDirection: SwipeDirection = SwipeDirection.Horizontal,
    countBadge: Boolean = false,
    showExpandButton: Boolean = false,
    // ↓↓↓ 本次新增 ↓↓↓
    cardGap: Dp = 8.dp,                                  // 展开态卡片间距（堆叠态不使用）
    onExpandStateChange: ((Boolean) -> Unit)? = null,    // 展开/收起状态变化回调
    // ↑↑↑ 本次新增 ↑↑↑
    onCardSwiped: ((originalIndex: Int) -> Unit)? = null,
    onCardClick: ((originalIndex: Int) -> Unit)? = null,
    customContent: (@Composable BoxScope.(stackIndex: Int) -> Unit)? = null
) {
    // ... 函数体暂不改动
}
```

- [ ] **Step 1.2: 修改重载函数签名（L154-L200），透传新参数**

在带 `customContent` 重载版的 `SwipeableImageStack` 函数参数列表与函数体透传 `cardGap` 和 `onExpandStateChange`：

```kotlin
@Composable
fun SwipeableImageStack(
    imageUris: List<String> = emptyList(),
    modifier: Modifier = Modifier,
    cardWidth: Dp = 300.dp,
    cardHeight: Dp = 400.dp,
    cardRadius: Float = 2f,
    swipeThreshold: Dp = 10.dp,
    maxElasticDistance: Dp = 0.dp,
    tiltAngleStart: Float = 0f,
    xOffset: Dp = 0.dp,
    visibleDepth: Int = 4,
    swipeDirection: SwipeDirection = SwipeDirection.Horizontal,
    countBadge: Boolean = false,
    showExpandButton: Boolean = false,
    cardGap: Dp = 8.dp,                                  // 透传
    onExpandStateChange: ((Boolean) -> Unit)? = null,    // 透传
    onCardSwiped: ((originalIndex: Int) -> Unit)? = null,
    onCardClick: ((originalIndex: Int) -> Unit)? = null,
    customContent: (@Composable BoxScope.(stackIndex: Int) -> Unit)? = null
) {
    SwipeableImageStack(
        imageUris = imageUris,
        modifier = modifier,
        cardWidth = cardWidth,
        cardHeight = cardHeight,
        cardRadius = cardRadius,
        swipeThreshold = swipeThreshold,
        maxElasticDistance = maxElasticDistance,
        tiltAngleStart = tiltAngleStart,
        xOffset = xOffset,
        visibleDepth = visibleDepth,
        swipeDirection = swipeDirection,
        countBadge = countBadge,
        showExpandButton = showExpandButton,
        cardGap = cardGap,                                // 透传
        onExpandStateChange = onExpandStateChange,       // 透传
        onCardSwiped = onCardSwiped,
        onCardClick = onCardClick,
        customContent = customContent
    )
}
```

- [ ] **Step 1.3: 检查 import 完整性**

确认文件顶部 import 已包含 `Dp` 与 `mutableStateOf`（通常已存在），无需新增。

- [ ] **Step 1.4: 询问用户是否编译验证**

按项目规则不擅自编译。向用户询问：

> 是否运行 `./gradlew assembleDebug` 编译验证 Task 1？

若用户同意，由用户执行编译；若用户拒绝，跳过进入下一任务。

- [ ] **Step 1.5: 询问用户是否 git 提交**

提交信息建议：`feat(swipeable-stack): 新增 cardGap 与 onExpandStateChange API 参数`

---

## Task 2: 内部状态 + 模式切换基础设施

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/SwipeableImageStack.kt` (L235-L273, L368-L432, L485-L544)

**目的：** 引入 `isExpanded` 状态和 `expandedScrollState`，改造目标值计算、容器宽度计算、LaunchedEffect 触发条件与外层 Box modifier，让 `isExpanded` 真正驱动布局切换。本任务完成后，状态切换还**没有 UI 入口**（按钮 onClick 留到 Task 4/5），但可以通过临时 Preview 修改 `isExpanded` 初值验证动画。

- [ ] **Step 2.1: 新增内部状态 `isExpanded` 与 `expandedScrollState`**

在 `SwipeableImageStack` 标准函数体内，紧接 `isPressed` 与 `shouldReturnToCenter` 定义之后（约 L273 之后）追加：

```kotlin
// ============ 展开态状态（设计文档 §3.3）============
// isExpanded：组件内部管理的"堆叠/展开"模式开关
// - false：堆叠态（现有扇形布局）
// - true：展开态（横向排列 + horizontalScroll）
// 由展开按钮和收起按钮点击切换，同时通过 onExpandStateChange 通知调用方
var isExpanded by remember { mutableStateOf(false) }

// expandedScrollState：展开态的横向滚动状态
// - 展开时挂载到外层 Box 的 horizontalScroll
// - 收起时通过 animateScrollTo(0) 归零
val expandedScrollState = rememberScrollState()
```

并在文件顶部 import 区域追加（若未导入）：

```kotlin
import androidx.compose.foundation.horizontalScroll
import androidx.compose.foundation.rememberScrollState
```

- [ ] **Step 2.2: 修改目标值计算（L485-L512），根据 `isExpanded` 切换扇形/横排目标**

找到 `targetX/targetY/targetScale/targetRotation/targetZIndex` 计算块，整体替换为：

```kotlin
// =================== 目标值计算（堆叠 / 展开两种模式）===================
// 设计文档 §4.3：
// - 堆叠态：扇形（ei 决定 rotation/Y/scale，zIndex 由 stackIndex 决定）
// - 展开态：横排（rotation=0, y=0, scale=1, x=stackIndex×(cardW+cardGap), zIndex=1）
// 用 stackIndex（而非 originalIndex）作为展开态 X 偏移依据，
// 让顶卡（stackIndex=0）天然落在最左侧（设计文档 §2.3 决策）
val M = minOf(visibleDepth, cardCount).coerceIn(1, 4)
val ei = minOf(stackIndex, M - 1)
val denom = max(M - 1, 1)
val effectiveTiltAngle = -(M - 1) * 15f

val targetX = if (isExpanded) {
    // 展开态：等距排列，顶卡=0 在最左侧
    stackIndex.toFloat() * (cardWidth.value + cardGap.value)
} else {
    if (M > 1) ei.toFloat() / denom * xOffset.value else 0f
}
val targetY = if (isExpanded) 0f else -(ei * 8f)
val targetScale = if (isExpanded) 1f else 1f - ei * 0.05f
val targetRotation = if (isExpanded) {
    0f
} else {
    if (M > 1) {
        tiltAngleStart + (ei.toFloat() / denom) * (effectiveTiltAngle - tiltAngleStart)
    } else {
        tiltAngleStart
    }
}
val targetZIndex = if (isExpanded) 1f else (order.size - stackIndex).toFloat()
```

- [ ] **Step 2.3: 修改 LaunchedEffect 触发条件（L528-L544），增加 `isExpanded` 到 key**

将 5 个 `LaunchedEffect(stackIndex, order.size, ...)` 的 key 增加 `isExpanded`，让 `isExpanded` 变化时所有卡片自动 `animateTo` 新目标：

```kotlin
LaunchedEffect(stackIndex, order.size, isExpanded) {
    positionX.animateTo(targetX, TRANSITION_SPRING)
}
LaunchedEffect(stackIndex, order.size, isExpanded) {
    positionY.animateTo(targetY, TRANSITION_SPRING)
}
LaunchedEffect(stackIndex, order.size, isExpanded) {
    scaleAnim.animateTo(targetScale, TRANSITION_SPRING)
}
LaunchedEffect(stackIndex, order.size, isExpanded) {
    rotationAnim.animateTo(targetRotation, TRANSITION_SPRING)
}
LaunchedEffect(stackIndex, order.size, isExpanded, isPressed.value, isTopCard) {
    val target = if (isPressed.value && isTopCard) 1000f else targetZIndex
    zIndexAnim.animateTo(target, ZINDEX_TWEEN)
}
```

- [ ] **Step 2.4: 修改容器尺寸计算（L368-L425），根据 `isExpanded` 切换 `boxWidth`**

在 `boxWidth` 计算之前（约 L423 之前）追加展开态宽度分支：

```kotlin
// ============ 展开态容器宽度（设计文档 §4.4）============
// 展开态：cardCount × (cardW + cardGap) - cardGap + collapseBtnWidth + 8dp 间距
// - collapseBtnWidth 与 expandEstWidth 保持一致（76dp 估算，实际宽度 onSizeChanged 读取后修正）
// - 设计文档 §3.5：collapseBtnWidth ≈ 文本宽度 + padding + icon ≈ 76dp
val collapseBtnEstWidth = 76f
val collapseBtnGap = 8f  // 收起按钮与顶卡左边缘的间距，与 expandMarginToCardNoBadge 视觉一致
val expandedBoxWidth = cardCount * (cardWVal + cardGap.value) - cardGap.value +
    collapseBtnEstWidth + collapseBtnGap
```

然后将 `boxWidth` 计算改为（注意展开态不依赖堆叠包围盒相关变量）：

```kotlin
val boxWidth = if (isExpanded) {
    expandedBoxWidth
} else {
    maxOf(cardWVal + xOffset.value, bboxWidth, badgeRequiredRight, cardBoxStartX + bboxWidth)
}
val boxHeight = if (isExpanded) {
    cardHVal
} else {
    maxOf(cardHVal + stackVPVal, bboxHeight)
}
```

- [ ] **Step 2.5: 修改外层 Box modifier（L427-L432），条件挂载 `horizontalScroll`**

```kotlin
Box(
    modifier = modifier
        .size(boxWidth.dp, boxHeight.dp)
        .then(if (isExpanded) Modifier.horizontalScroll(expandedScrollState) else Modifier)
)
```

- [ ] **Step 2.6: 修改内部堆叠卡片容器定位（L440-L453），展开态跳过 `cardBoxStartX/Y` 偏移**

展开态下不需要旋转包围盒平移，让卡片直接从 Box 左上角对齐。将堆叠卡片容器的 `graphicsLayer` 改为：

```kotlin
Box(
    modifier = Modifier
        .align(Alignment.TopStart)
        .size(
            width = if (isExpanded) boxWidth.dp else bboxWidth.dp,
            height = if (isExpanded) boxHeight.dp else bboxHeight.dp
        )
        .graphicsLayer {
            this.cameraDistance = 1000f / density.density
            // 展开态：translationX/Y = 0（不依赖旋转包围盒平移）
            // 堆叠态：原有平移逻辑
            translationX = if (isExpanded) 0f else cardBoxStartX
            translationY = if (isExpanded) 0f else cardBoxStartY
        },
    contentAlignment = if (isExpanded) Alignment.TopStart else Alignment.Center
) {
    // ... 卡片渲染逻辑
}
```

- [ ] **Step 2.7: 检查 import 完整性**

确认已 import：

```kotlin
import androidx.compose.foundation.horizontalScroll
import androidx.compose.foundation.rememberScrollState
```

- [ ] **Step 2.8: 临时验证（可选）**

任选一个 Preview（如 `SwipeableImageStackPreview5`），临时把 `isExpanded` 初值改为 `true`（仅在 Preview 函数体内，通过 `MutableState` 注入或在组件内部临时硬编码）。

> 注：本步骤为开发期临时验证，提交前必须还原 `isExpanded = false` 初值。

- [ ] **Step 2.9: 询问用户是否编译验证**

向用户询问：

> 是否运行 `./gradlew assembleDebug` 编译验证 Task 2？

- [ ] **Step 2.10: 询问用户是否 git 提交**

提交信息建议：`feat(swipeable-stack): 新增 isExpanded 状态驱动布局模式切换`

---

## Task 3: 拖拽与翻牌互斥

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/SwipeableImageStack.kt` (L632-L855 附近)

**目的：** 展开状态下禁用拖拽翻牌（设计文档 §3.4、§4.6、§7 边界情况），避免横向滑动与翻牌手势冲突。

- [ ] **Step 3.1: 修改 `pointerInput` 挂载条件（L632-L634 附近）**

找到顶卡的 `Modifier.pointerInput(slot.stableId, swipeDirection)` 块，将其外层 `if (isTopCard)` 改为 `if (isTopCard && !isExpanded)`：

```kotlin
.then(
    if (isTopCard && !isExpanded) {
        Modifier.pointerInput(slot.stableId, swipeDirection) {
            // ... 现有拖拽逻辑不变
        }
    } else {
        Modifier  // 展开态或非顶卡：不挂载拖拽
    }
)
```

- [ ] **Step 3.2: 修改 `onCardClick` 行为（保留回调，但展开态可选择性禁用）**

设计文档 §7：展开态下点击图片仍保留 `onCardClick` 回调（顶卡点击进入全屏 Pager 预览）。本步骤**无需改动**，仅记录设计决策。

> 注：若后续产品决定展开态下点击图片应进入"当前可见卡片"而非"顶卡"的预览，需在调用方 `TimelineInspirationItem` 调整 `onCardClick` 实现。本次不涉及。

- [ ] **Step 3.3: 检查 import 完整性**

本任务无新增 import。

- [ ] **Step 3.4: 询问用户是否编译验证**

向用户询问编译验证。

- [ ] **Step 3.5: 询问用户是否 git 提交**

提交信息建议：`feat(swipeable-stack): 展开态禁用拖拽翻牌手势`

---

## Task 4: expandButton 接入 onClick + countBadge 显隐切换

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/SwipeableImageStack.kt` (L879, L925-L980)

**目的：** 让 `expandButton` 点击触发 `isExpanded = true`；展开态下 `countBadge` 和 `expandButton` 都隐藏。

- [ ] **Step 4.1: 修改 `showCountBadge` 计算（L373），增加 `!isExpanded` 条件**

```kotlin
val showCountBadge = countBadge && cardCount > visibleDepth && !isExpanded
```

- [ ] **Step 4.2: 修改 `showExpand` 计算（L374），增加 `!isExpanded` 条件**

```kotlin
val showExpand = showExpandButton && cardCount >= 2 && !isExpanded
```

> 注：`badgeEstWidth` / `expandEstWidth` 等下游变量在 `isExpanded = true` 时都为 0f，自动让 `boxWidth` 走展开态分支，无需额外改动。

- [ ] **Step 4.3: 修改 `expandButton` 的 `Row` 增加 `clickable`（L956-L964 附近）**

```kotlin
Row(
    modifier = Modifier
        .background(
            color = Color(0xFFF2F3F5).copy(alpha = 0.55f),
            shape = RoundedCornerShape(11.dp)
        )
        .padding(horizontal = 5.dp, vertical = 2.dp)
        .onSizeChanged { buttonSize = it }
        .clickable {
            // 点击展开按钮 → 切换到展开态
            // 设计文档 §5 数据流：isExpanded = true 触发 LaunchedEffect 重排
            isExpanded = true
            onExpandStateChange?.invoke(true)
        },
    verticalAlignment = Alignment.CenterVertically
) {
    // ... 现有 Text + Icon 不变
}
```

需在文件顶部 import：

```kotlin
import androidx.compose.foundation.clickable
```

- [ ] **Step 4.4: 检查 import 完整性**

确认已 import `clickable`。

- [ ] **Step 4.5: 询问用户是否编译验证**

向用户询问编译验证。

- [ ] **Step 4.6: 询问用户是否 git 提交**

提交信息建议：`feat(swipeable-stack): expandButton 接入 onClick 并实现展开态 UI 切换`

---

## Task 5: 新增收起按钮（collapseButton）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/SwipeableImageStack.kt`（外层 Box 内尾部追加，约 L980 之前 `}` 之前）

**目的：** 展开态下在第一张图片（顶卡）左侧显示"收起"按钮，样式与 `expandButton` 完全一致；点击触发 `isExpanded = false` + 滚动归零。

- [ ] **Step 5.1: 在 `expandButton` 渲染块之后、外层 Box 闭合 `}` 之前（约 L980）追加 `collapseButton` 渲染块**

```kotlin
// ============ 收起按钮（collapseButton）============
// 设计文档 §3.5：展开态下显示在顶卡左边缘左侧，垂直居中于顶卡
// - 样式与 expandButton 完全一致（灰底半透明胶囊 + 11sp Medium + ChevronRight 图标）
// - 文本：显示"收起"
// - 显示条件：isExpanded = true（堆叠态不显示）
// - 实现：与 expandButton 相同的 onSizeChanged + graphicsLayer.translationY 精确居中方案
if (isExpanded) {
    // 顶卡左边缘（外层 Box 坐标）：展开态下 cardBoxStartX=0，顶卡中心 = cardWVal/2
    val topCardLeftX_expanded = 0f  // 展开态卡片容器从 (0,0) 开始，顶卡左边缘 = 0
    val topCardTop_expanded = 0f
    val topCardBottom_expanded = cardHVal
    val topCardCenterY_expanded = (topCardTop_expanded + topCardBottom_expanded) / 2f

    // 收起按钮 start：顶卡左边缘 - 按钮宽度 - 8dp 间距
    // 按钮宽度通过 onSizeChanged 实时读取（与 expandButton 一致）
    // 这里用估算值 76dp 计算初始位置，onSizeChanged 后用 graphicsLayer.translationX 修正
    val collapseBtnEstWidth = 76f
    val collapseBtnGap = 8f
    val buttonStartX = topCardLeftX_expanded - collapseBtnEstWidth - collapseBtnGap

    var collapseButtonSize by remember { mutableStateOf(IntSize.Zero) }
    val density = LocalDensity.current
    val collapseBtnActualHalfOffsetDp = with(density) {
        if (collapseButtonSize == IntSize.Zero) 0.dp
        else (collapseButtonSize.height / 2).toDp()
    }

    Box(
        modifier = Modifier
            .align(Alignment.TopStart)
            .padding(start = buttonStartX.dp, top = topCardCenterY_expanded.dp)
            .graphicsLayer {
                translationY = -collapseBtnActualHalfOffsetDp.toPx()
            }
    ) {
        Row(
            modifier = Modifier
                .background(
                    color = Color(0xFFF2F3F5).copy(alpha = 0.55f),
                    shape = RoundedCornerShape(11.dp)
                )
                .padding(horizontal = 5.dp, vertical = 2.dp)
                .onSizeChanged { collapseButtonSize = it }
                .clickable {
                    // 点击收起按钮 → 切换回堆叠态 + 滚动归零
                    // 设计文档 §5 数据流：scrollState 归零 + isExpanded = false
                    scope.launch {
                        expandedScrollState.animateScrollTo(0)
                        isExpanded = false
                        onExpandStateChange?.invoke(false)
                    }
                },
            verticalAlignment = Alignment.CenterVertically
        ) {
            Text(
                text = "收起",
                color = Color(0xFF4F5660),
                fontSize = 11.sp,
                fontWeight = FontWeight.Medium
            )
            Icon(
                imageVector = Icons.Default.ChevronRight,
                contentDescription = "收起图片堆叠",
                tint = Color(0xFF4F5660),
                modifier = Modifier.size(16.dp)
            )
        }
    }
}
```

- [ ] **Step 5.2: 检查 import 完整性**

确认已 import（通常已存在）：

```kotlin
import androidx.compose.foundation.clickable
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.getValue
import androidx.compose.runtime.setValue
import androidx.compose.ui.unit.IntSize
import androidx.compose.ui.layout.onSizeChanged
import androidx.compose.ui.platform.LocalDensity
import androidx.compose.ui.graphics.graphicsLayer
import androidx.compose.foundation.layout.Row
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.background
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Text
import androidx.compose.material3.Icon
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.ChevronRight
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.foundation.layout.Box
import androidx.compose.ui.Alignment
import kotlinx.coroutines.delay
import kotlinx.coroutines.launch
```

> 注：`scope` 已在 L263 定义为 `rememberCoroutineScope()`，可直接复用。

- [ ] **Step 5.3: 询问用户是否编译验证**

向用户询问编译验证。

- [ ] **Step 4: 询问用户是否 git 提交**

提交信息建议：`feat(swipeable-stack): 新增收起按钮 + 展开态滚动归零逻辑`

---

## Task 6: 临时验证 Preview + 收尾

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/SwipeableImageStack.kt` (Preview 区段，L1141-L1430)

**目的：** 在任一 Preview 中临时验证展开/收起按钮的视觉效果（不持久化到 git），并完成全功能集成验证。

- [ ] **Step 6.1: 临时修改 `SwipeableImageStackPreview5`（L1163）传入 `showExpandButton = true` 与 `countBadge = true`**

```kotlin
@Composable
private fun SwipeableImageStackPreview5() {
    SwipeableImageStack(
        imageUris = (1..5).map { "file:///android_asset/sample$it.jpg" },
        cardWidth = 120.dp,
        cardHeight = 120.dp,
        visibleDepth = 4,
        countBadge = true,           // 临时启用
        showExpandButton = true,     // 临时启用
        cardGap = 8.dp,              // 临时显式传入
        // onExpandStateChange 留空
    )
}
```

> 注：仅作开发期 Preview 验证，提交前还原为原值或保持不变（用户决定）。

- [ ] **Step 6.2: 询问用户是否运行 `./gradlew assembleDebug` 完整编译验证**

向用户询问：

> 是否运行 `./gradlew assembleDebug` 编译验证全部任务（Task 1-6）？

- [ ] **Step 6.3: 询问用户是否在浏览器查看原型对比**

向用户询问：

> 是否需要打开 `docs/prototypes/expand-collapse-stack.html` 在浏览器中对比最终视觉效果？

- [ ] **Step 6.4: 询问用户是否 git 提交全部任务**

提交信息建议：`feat(swipeable-stack): 完成展开/收起功能集成（设计文档方案 A）`

---

## Self-Review（已完成）

### Spec 覆盖

| Spec 需求 | 任务 |
|----------|------|
| R1 扇形→横排过渡 | Task 2（目标值 + LaunchedEffect） |
| R2 全部图片横排 | Task 2（targetX = stackIndex × (cardW+gap)） |
| R3 第一张保持在原位 | Task 2（顶卡 stackIndex=0 → targetX=0） |
| R4 展开态禁用拖拽 | Task 3 |
| R5 收起按钮 | Task 5 |
| R6 展开态不显示角标 | Task 4（showCountBadge += !isExpanded） |
| R7 展开按钮 ≥2 张显示 | Task 4（showExpand 保留 cardCount >= 2 条件） |

### 占位符扫描

- 无 TBD/TODO/implement later
- 每个步骤都给出了完整代码

### 类型一致性

- `cardGap: Dp` 在 Task 1 定义，Task 2 使用 `.value` 取 Float，一致
- `onExpandStateChange: ((Boolean) -> Unit)?` 在 Task 1 定义，Task 4/5 调用 `.invoke(true/false)`，一致
- `isExpanded` 在 Task 2 定义为 `Boolean`，Task 3/4/5 都按 `Boolean` 使用，一致
- `expandedScrollState` 在 Task 2 定义为 `ScrollState`，Task 5 调用 `animateScrollTo(0)`，一致

### 项目规则对齐

- 编译前必询问：每个 Task 末尾都有"询问用户是否编译验证"步骤
- import 检查：每个涉及代码改动的 Task 都有 import 检查步骤
- lambda 捕获：`isExpanded` 通过 `by remember` 委托，Compose 自动重组时读取最新值，避免快照混用
- git 提交：每个 Task 末尾都有"询问用户是否 git 提交"步骤

---

## 执行选项

**Plan complete and saved to `docs/superpowers/plans/2026-08-25-堆叠图展开收起功能.md`. Two execution options:**

**1. Subagent-Driven（推荐）** - 每个 Task 派发新 subagent，任务间 review，迭代快

**2. Inline Execution** - 在当前会话顺序执行 Task，带 checkpoint review

**Which approach?**
