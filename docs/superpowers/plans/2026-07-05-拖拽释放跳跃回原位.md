# 拖拽释放后卡片跳跃回原位修复 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 HomeScreen.onReorder 中 displayItems 闭包变量未在拖拽中更新导致取错被拖项 + relativeIndex 计算错误的根因，消除"拖拽释放后卡片跳跃回原位"现象。

**Architecture:** 将"被拖项查找 + relativeIndex 计算"职责从 HomeScreen 上移到 ZonedReorderableLazyColumn 组件，组件基于内部 displayItems（已被 onMove 更新）计算，通过 onReorder 签名变更传递 (dragResult, draggedItem, targetZoneRelativeIndex) 三元组。HomeScreen 简化为纯转发。

**Tech Stack:** Kotlin、Jetpack Compose、Calvin-LL/Reorderable 库、JUnit 4、MockK

---

## File Structure

| 文件 | 类型 | 责任 |
|------|------|------|
| `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt` | 修改 | onReorder 签名变更；新增 `computeRelativeIndexInZone` 内部纯函数；onDragStopped 调用计算逻辑 |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 修改 | onReorder 简化为纯转发 |
| `app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt` | 新增 | 4 个 `computeRelativeIndexInZone` 单元测试 |

---

## Task 1: 新增 computeRelativeIndexInZone 纯函数

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt`（在文件末尾，`ZonedReorderableLazyColumn` 函数之后添加 top-level internal 函数）
- Test: `app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt`

- [ ] **Step 1: 创建测试文件，写入 4 个失败测试**

创建 `app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt`：

```kotlin
package com.corgimemo.app.ui.components

import com.corgimemo.app.data.model.TodoItem
import com.corgimemo.app.ui.screens.home.DisplayItem
import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * ZonedReorderableLazyColumn 组件内部纯函数单元测试
 *
 * 覆盖 computeRelativeIndexInZone 的 4 种场景：
 * - 同 zone 内拖拽
 * - 跨 zone 拖拽（PENDING → PINNED_PENDING）
 * - 拖到 zone 开头
 * - 跨 zone 拖到已完成区
 */
class ZonedReorderableLazyColumnTest {

    /** 构造 TodoItem 辅助函数 */
    private fun buildItem(
        id: Long,
        isPinned: Boolean = false,
        status: Int = 0
    ): TodoItem {
        return TodoItem(
            id = id,
            title = "测试项$id",
            categoryId = 1L,
            priority = 1,
            status = status,
            repeatType = 0,
            createdAt = 0L,
            updatedAt = 0L,
            isPinned = isPinned
        )
    }

    /**
     * 场景：同 zone 内拖拽（PENDING 区内）
     *
     * 列表：[PendingDivider, A, B, C]（A/B/C 均为 PENDING）
     * 被拖项当前在 idx=2（B 的位置）
     * targetZone = PENDING
     *
     * 预期：relativeIndex = 1（前面只有 A 一个同 zone 项）
     */
    @Test
    fun `同 zone 内拖拽计算 relativeIndex`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PendingDivider(count = 3, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1)),
            DisplayItem.Todo(buildItem(id = 2)),
            DisplayItem.Todo(buildItem(id = 3))
        )
        val result = computeRelativeIndexInZone(
            displayItems = displayItems,
            draggedCurrentIndex = 2,
            targetZone = TodoZone.PENDING
        )
        assertEquals(1, result)
    }

    /**
     * 场景：跨 zone 拖拽（被拖项从 COMPLETED 跨到 PENDING）
     *
     * 列表：[PinnedDivider, A_pinned, PendingDivider, B, C, draggedItem]
     *   - A_pinned 是 PINNED_PENDING（isPinned=true, status=0）
     *   - B/C 是 PENDING
     *   - draggedItem 是被拖项，已在 idx=5（原 COMPLETED 跨到 PENDING 区末尾）
     * targetZone = PENDING
     *
     * 预期：relativeIndex = 2（前面有 B、C 两个 PENDING 项；A_pinned 不算）
     */
    @Test
    fun `跨 zone 拖拽计算 relativeIndex`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PinnedDivider(count = 1, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1, isPinned = true)),
            DisplayItem.PendingDivider(count = 2, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 2)),
            DisplayItem.Todo(buildItem(id = 3)),
            DisplayItem.Todo(buildItem(id = 4, status = 1))  // 被拖项（status=1 但 currentZone=PENDING）
        )
        val result = computeRelativeIndexInZone(
            displayItems = displayItems,
            draggedCurrentIndex = 5,
            targetZone = TodoZone.PENDING
        )
        assertEquals(2, result)
    }

    /**
     * 场景：拖到 zone 开头
     *
     * 列表：[PendingDivider, draggedItem, A, B]
     * 被拖项当前在 idx=1（divider 后第一项）
     * targetZone = PENDING
     *
     * 预期：relativeIndex = 0（前面没有同 zone 项）
     */
    @Test
    fun `拖到 zone 开头 relativeIndex 为 0`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PendingDivider(count = 3, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1)),
            DisplayItem.Todo(buildItem(id = 2)),
            DisplayItem.Todo(buildItem(id = 3))
        )
        val result = computeRelativeIndexInZone(
            displayItems = displayItems,
            draggedCurrentIndex = 1,
            targetZone = TodoZone.PENDING
        )
        assertEquals(0, result)
    }

    /**
     * 场景：跨 zone 拖到已完成区
     *
     * 列表：[PinnedDivider, A_pinned, PendingDivider, B, CompletedDivider, X, draggedItem]
     *   - A_pinned 是 PINNED_PENDING
     *   - B 是 PENDING
     *   - X 是 COMPLETED（isPinned=false, status=1）
     *   - draggedItem 被拖到 idx=6（已完成区末尾）
     * targetZone = COMPLETED
     *
     * 预期：relativeIndex = 1（前面只有 X 一个 COMPLETED 项）
     */
    @Test
    fun `跨 zone 拖到已完成区 relativeIndex`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PinnedDivider(count = 1, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1, isPinned = true)),
            DisplayItem.PendingDivider(count = 1, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 2)),
            DisplayItem.CompletedDivider(count = 2, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 3, status = 1)),
            DisplayItem.Todo(buildItem(id = 4))  // 被拖项（status=0 但 currentZone=COMPLETED）
        )
        val result = computeRelativeIndexInZone(
            displayItems = displayItems,
            draggedCurrentIndex = 6,
            targetZone = TodoZone.COMPLETED
        )
        assertEquals(1, result)
    }
}
```

- [ ] **Step 2: 运行测试验证失败（函数未定义）**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ZonedReorderableLazyColumnTest" --no-daemon`

Expected: FAIL with "Unresolved reference: computeRelativeIndexInZone"

- [ ] **Step 3: 在 ZonedReorderableLazyColumn.kt 末尾添加 computeRelativeIndexInZone 纯函数**

在 `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt` 文件末尾（`ZonedReorderableLazyColumn` 函数之后，文件最后一行 `}` 之后）添加：

```kotlin
/**
 * 计算被拖项在目标 zone 内的相对索引
 *
 * 仅统计 displayItems[0..draggedCurrentIndex-1] 中与 targetZone 相同的 Todo 项数量，
 * 跳过 divider 与其他 zone 的 Todo。
 *
 * 内部可见（internal），便于单元测试。
 *
 * @param displayItems 组件内部 displayItems（已被 onMove 更新）
 * @param draggedCurrentIndex 被拖项在 displayItems 中的当前位置
 * @param targetZone 目标 zone（来自 [ZoneDragResult.currentZone]）
 * @return 目标 zone 内的相对索引（0..size）
 */
internal fun computeRelativeIndexInZone(
    displayItems: List<DisplayItem>,
    draggedCurrentIndex: Int,
    targetZone: TodoZone
): Int {
    var relativeIndex = 0
    for (i in 0 until draggedCurrentIndex) {
        val item = displayItems[i]
        if (item is DisplayItem.Todo && item.item.zone() == targetZone) {
            relativeIndex++
        }
    }
    return relativeIndex
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ZonedReorderableLazyColumnTest" --no-daemon`

Expected: PASS（4 个测试全部通过）

- [ ] **Step 5: 提交**

```powershell
git add app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt
$msg = @"
feat(reorder): 新增 computeRelativeIndexInZone 纯函数与单元测试

将 relativeIndex 计算逻辑提取为内部可见的纯函数，便于后续在
onDragStopped 中复用，并支持单元测试覆盖 4 种场景：
- 同 zone 内拖拽
- 跨 zone 拖拽
- 拖到 zone 开头
- 跨 zone 拖到已完成区
"@
git commit -m $msg
```

---

## Task 2: 修改 onReorder 签名与 onDragStopped 实现

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt:55`（onReorder 签名）
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt:158-182`（onDragStopped lambda）

- [ ] **Step 1: 修改 onReorder 签名**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt`，定位第 55 行：

修改前：
```kotlin
    onReorder: (dragResult: ZoneDragResult, fromIndex: Int, toIndex: Int) -> Unit,
```

修改后：
```kotlin
    onReorder: (
        dragResult: ZoneDragResult,
        draggedItem: DisplayItem.Todo,
        targetZoneRelativeIndex: Int
    ) -> Unit,
```

- [ ] **Step 2: 修改 onDragStopped lambda 中的 onReorder 调用**

定位 `onDragStopped = {` lambda（约第 158-182 行）。

修改前（第 158-178 行）：
```kotlin
                                onDragStopped = {
                                    val draggedCurrentIndex = displayItems.indexOfFirst {
                                        key(it) == key(item)
                                    }
                                    if (draggedOriginalIndex >= 0 &&
                                        draggedOriginalIndex != draggedCurrentIndex &&
                                        draggedCurrentIndex >= 0
                                    ) {
                                        // 状态机输出最终 zone 状态
                                        val dragResult = dragZoneState.endDrag()
                                        onReorder(
                                            dragResult,
                                            draggedOriginalIndex,
                                            draggedCurrentIndex
                                        )
                                        HapticFeedbackManager.performHapticFeedback(
                                            context = context,
                                            type = InteractionType.CONFIRM,
                                            enabled = true
                                        )
                                    }
                                    isDragActive = false
                                    draggedOriginalIndex = -1
                                    dragZoneState.reset()
                                }
```

修改后：
```kotlin
                                onDragStopped = {
                                    val draggedCurrentIndex = displayItems.indexOfFirst {
                                        key(it) == key(item)
                                    }
                                    if (draggedOriginalIndex >= 0 &&
                                        draggedOriginalIndex != draggedCurrentIndex &&
                                        draggedCurrentIndex >= 0
                                    ) {
                                        // 状态机输出最终 zone 状态
                                        val dragResult = dragZoneState.endDrag()

                                        // 基于内部 displayItems（已被 onMove 更新）取被拖项
                                        val draggedTodoItem = displayItems[draggedCurrentIndex]
                                            as? DisplayItem.Todo
                                            ?: return@longPressDraggableHandle

                                        // 基于 dragResult.currentZone 计算目标 zone 内相对索引
                                        val relativeIndex = computeRelativeIndexInZone(
                                            displayItems = displayItems,
                                            draggedCurrentIndex = draggedCurrentIndex,
                                            targetZone = dragResult.currentZone
                                        )

                                        onReorder(dragResult, draggedTodoItem, relativeIndex)

                                        HapticFeedbackManager.performHapticFeedback(
                                            context = context,
                                            type = InteractionType.CONFIRM,
                                            enabled = true
                                        )
                                    }
                                    isDragActive = false
                                    draggedOriginalIndex = -1
                                    dragZoneState.reset()
                                }
```

- [ ] **Step 3: 更新 onReorder 参数的 KDoc 注释**

定位 `ZonedReorderableLazyColumn` 函数顶部的 KDoc（约第 23-49 行），更新 `@param onReorder` 描述。

修改前：
```kotlin
 * @param onReorder 排序提交回调 (dragResult, fromIndex, toIndex)
 *                  - dragResult: 状态机输出的最终 zone 状态
 *                  - fromIndex: 被拖项在 displayItems 中的原始索引
 *                  - toIndex:   被拖项在 displayItems 中的最终索引
```

修改后：
```kotlin
 * @param onReorder 排序提交回调 (dragResult, draggedItem, targetZoneRelativeIndex)
 *                  - dragResult: 状态机输出的最终 zone 状态
 *                  - draggedItem: 被拖项（基于组件内部 displayItems 取得，类型安全）
 *                  - targetZoneRelativeIndex: 目标 zone 内的相对索引（仅统计同 zone Todo）
```

- [ ] **Step 4: 验证 import 语句完整**

`DisplayItem` 已在文件顶部 `import com.corgimemo.app.ui.screens.home.DisplayItem`，无需新增。
`TodoZone` 已在 `TodoZone.kt` 同包，无需 import。
`computeRelativeIndexInZone` 是同文件同包的 top-level 函数，无需 import。

- [ ] **Step 5: 编译验证**

Run: `.\gradlew.bat :app:compileDebugKotlin --no-daemon`

Expected: BUILD SUCCESSFUL

- [ ] **Step 6: 提交**

```powershell
git add app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt
$msg = @"
refactor(reorder): onReorder 签名变更，组件计算被拖项与 relativeIndex

将 onReorder 签名从 (dragResult, fromIndex, toIndex) 改为
(dragResult, draggedItem, targetZoneRelativeIndex)。

onDragStopped 中基于内部 displayItems（已被 onMove 更新）：
- 取被拖项 DisplayItem.Todo（as? 安全转换）
- 调用 computeRelativeIndexInZone 计算 relativeIndex

修复 HomeScreen.onReorder 中 displayItems 闭包变量未更新的根因。
"@
git commit -m $msg
```

---

## Task 3: 修改 HomeScreen.onReorder 为纯转发

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt:811-832`

- [ ] **Step 1: 修改 onReorder lambda 为纯转发**

打开 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`，定位第 811 行的 `onReorder = { dragResult, fromIndex, toIndex ->`。

修改前（第 811-833 行）：
```kotlin
                                onReorder = { dragResult, fromIndex, toIndex ->
                                    // 找到被拖项（基于最终位置）
                                    val draggedTodoItem = displayItems.getOrNull(toIndex) as? DisplayItem.Todo
                                    val draggedTodo = draggedTodoItem?.item
                                        ?: return@ZonedReorderableLazyColumn

                                    // 计算目标 zone 内的相对索引（仅统计同 zone 的 Todo 项）
                                    val targetZone = dragResult.currentZone
                                    var relativeIndex = 0
                                    for (i in 0 until toIndex) {
                                        val item = displayItems[i]
                                        if (item is DisplayItem.Todo && item.item.zone() == targetZone) {
                                            relativeIndex++
                                        }
                                    }

                                    viewModel.reorderOnDragResult(
                                        draggedItemId = draggedTodo.id,
                                        draggedTodo = draggedTodo,
                                        dragResult = dragResult,
                                        targetZoneRelativeIndex = relativeIndex
                                    )
                                },
```

修改后：
```kotlin
                                onReorder = { dragResult, draggedTodoItem, targetZoneRelativeIndex ->
                                    // 纯转发：组件已基于内部 displayItems 计算好被拖项和相对索引
                                    viewModel.reorderOnDragResult(
                                        draggedItemId = draggedTodoItem.item.id,
                                        draggedTodo = draggedTodoItem.item,
                                        dragResult = dragResult,
                                        targetZoneRelativeIndex = targetZoneRelativeIndex
                                    )
                                },
```

- [ ] **Step 2: 验证 import 语句完整**

`DisplayItem` 是同包 top-level sealed interface（`HomeScreen.kt` 自身定义），无需 import。
`TodoZone` 不再在 HomeScreen 中直接引用，已通过组件传递。

- [ ] **Step 3: 编译验证**

Run: `.\gradlew.bat :app:compileDebugKotlin --no-daemon`

Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 提交**

```powershell
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt
$msg = @"
refactor(reorder): HomeScreen.onReorder 简化为纯转发

删除 displayItems 取项 + relativeIndex 计算逻辑（已上移到组件），
onReorder 仅承担转发职责，从 14 行简化为 7 行。

修复"任意待办卡片拖拽释放后跳跃回原位"的视觉跳跃现象。
"@
git commit -m $msg
```

---

## Task 4: 回归测试与手动验证

**Files:**
- 无新增/修改文件

- [ ] **Step 1: 运行所有单元测试**

Run: `.\gradlew.bat :app:testDebugUnitTest --no-daemon`

Expected: BUILD SUCCESSFUL
- ReminderTimeFormatterTest: 13/13 通过
- MigrationTest: 3/3 通过
- HomeViewModelReorderTest: 3/3 通过
- DragZoneStateMachineTest: 19/19 通过
- HomeViewModelPinnedButtonTest: 3/3 通过
- TodoZoneTest: 4/4 通过
- ZonedReorderableLazyColumnTest: 4/4 通过（Task 1 新增）
- 合计：49/49 通过

- [ ] **Step 2: 验证 Debug APK 编译**

Run: `.\gradlew.bat :app:assembleDebug --no-daemon`

Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 手动验证场景 1 - PENDING 区内同区拖拽**

1. 启动 app
2. 创建 3 条待办（A, B, C），都为非置顶待完成
3. 长按 A，拖到 B 和 C 之间释放
4. 验证：A 落在 B 和 C 之间，**不跳跃回原位**

- [ ] **Step 4: 手动验证场景 2 - PENDING → PINNED_PENDING 跨区拖拽**

1. 创建 1 条置顶待完成（X）+ 2 条非置顶待完成（A, B）
2. 长按 A，拖到置顶区（X 之前或之后）释放
3. 验证：A 跨入置顶区，位置正确

- [ ] **Step 5: 手动验证场景 3 - PENDING → COMPLETED 跨区拖拽**

1. 创建 2 条非置顶待完成（A, B）+ 1 条非置顶已完成（X）
2. 长按 A，拖到已完成区释放
3. 验证：A 跨入已完成区，触发庆祝动画，位置正确

- [ ] **Step 6: 手动验证场景 4 - COMPLETED → PENDING 跨区拖拽**

1. 创建 1 条非置顶待完成（A）+ 2 条非置顶已完成（X, Y）
2. 长按 X，拖到待完成区释放
3. 验证：X 跨回待完成区，位置正确

- [ ] **Step 7: 手动验证场景 5 - COMPLETED 区内同区拖拽**

1. 创建 3 条非置顶已完成（X, Y, Z）
2. 长按 X，拖到 Y 和 Z 之间释放
3. 验证：X 落在 Y 和 Z 之间，**不跳跃回原位**

- [ ] **Step 8: 最终提交（如有手动验证发现的修复）**

若手动验证发现新问题，按 spec 中的风险章节排查并修复后提交。
若全部通过，无需额外提交。

---

## Self-Review

### 1. Spec coverage

| Spec 章节 | 对应 Task |
|----------|-----------|
| §3 设计方案（方案 A） | Task 2 + Task 3 |
| §4 组件与接口（onReorder 签名变更） | Task 2 |
| §4 组件与接口（computeRelativeIndexInZone） | Task 1 |
| §5 组件实现变更（onDragStopped） | Task 2 |
| §6 调用方变更（HomeScreen.onReorder） | Task 3 |
| §7 测试策略（4 个单元测试） | Task 1 |
| §7 测试策略（5 个手动验证场景） | Task 4 |
| §9 实施范围与验收标准 | Task 4 |

✅ 无遗漏。

### 2. Placeholder scan

- 无 TBD / TODO / "implement later"
- 所有代码步骤均含完整代码
- 所有命令均含 PowerShell 兼容语法
- 无"Similar to Task N"引用

✅ 无 placeholder。

### 3. Type consistency

- `onReorder` 签名：`(dragResult: ZoneDragResult, draggedItem: DisplayItem.Todo, targetZoneRelativeIndex: Int) -> Unit` —— Task 2 定义，Task 3 使用，一致
- `computeRelativeIndexInZone` 签名：`(displayItems: List<DisplayItem>, draggedCurrentIndex: Int, targetZone: TodoZone) -> Int` —— Task 1 定义，Task 2 调用，一致
- `DisplayItem.Todo` —— spec、Task 1/2/3 均使用 `item.item` 取 `TodoItem`，一致
- `HomeViewModel.reorderOnDragResult` 签名未变，Task 3 调用方式与 spec 一致

✅ 类型一致。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-05-拖拽释放跳跃回原位.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?
