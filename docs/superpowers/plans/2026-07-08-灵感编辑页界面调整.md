# 灵感编辑页界面调整实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 对灵感编辑页进行精确 UI 修改：统一返回箭头样式、移除铃铛通知按钮及时间设置功能、删除优先级按钮、将标签添加按钮迁移至分类按钮右侧、标题占位淡色化。

**Architecture:** 方案 A（状态上提）：将 TagInputField 的 `isAdding` 状态上提到 InspirationEditScreen 托管，新标签按钮通过该状态触发输入框显示。所有修改仅限灵感编辑页（InspirationEditScreen.kt + TagInputField.kt），不动 ViewModel/Model 层。

**Tech Stack:** Jetpack Compose (BOM 2026.04.01 + Compose 1.9.2)、Material3、Kotlin

**Spec:** `docs/superpowers/specs/2026-07-08-灵感编辑页界面调整-design.md`

---

## 文件结构

| 文件 | 责任 | 修改类型 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/TagInputField.kt` | 标签展示与输入组件 | 修改：状态上提，移除内部添加按钮 |
| `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt` | 灵感编辑页主屏幕 | 修改：顶栏/标题区/交互区改造，删除时间功能 |

---

## Task 1: TagInputField 状态上提改造

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/TagInputField.kt`

**目标：** 将内部 `isAdding` 状态上提为参数，移除"添加"按钮，使外部（InspirationEditScreen）能控制输入框显示。

- [ ] **Step 1: 修改函数签名，新增 isAdding 与 onAddingChange 参数**

在 `TagInputField.kt` 中，将函数签名（约 L53-L58）从：

```kotlin
@Composable
fun TagInputField(
    tags: List<String>,
    onTagsChange: (List<String>) -> Unit,
    modifier: Modifier = Modifier
) {
```

改为：

```kotlin
@Composable
fun TagInputField(
    tags: List<String>,
    onTagsChange: (List<String>) -> Unit,
    modifier: Modifier = Modifier,
    /** 是否显示标签输入框（状态由父组件托管） */
    isAdding: Boolean = false,
    /** 输入框显示状态变更回调 */
    onAddingChange: (Boolean) -> Unit = {}
) {
```

- [ ] **Step 2: 移除内部 isAdding 状态变量**

删除约 L59-L62 的内部状态声明：

```kotlin
    /** 控制是否显示输入框 */
    var isAdding by remember { mutableStateOf(false) }
```

（保留 `var newTagText by remember { mutableStateOf("") }` 不变）

- [ ] **Step 3: 移除"添加"按钮整块**

删除约 L112-L141 的添加按钮 Box（从 `/** 添加按钮 */` 注释到对应 Box 闭合 `}`），即以下整块：

```kotlin
            /**
             * 添加按钮
             * 点击后切换到输入模式或显示输入框
             */
            Box(
                modifier = Modifier
                    .clip(RoundedCornerShape(12.dp))
                    .background(UiColors.Primary.copy(alpha = 0.1f))
                    .clickable { isAdding = true }
                    .padding(horizontal = 12.dp, vertical = 6.dp),
                contentAlignment = Alignment.Center
            ) {
                Row(
                    verticalAlignment = Alignment.CenterVertically,
                    horizontalArrangement = Arrangement.spacedBy(4.dp)
                ) {
                    Icon(
                        imageVector = Icons.Default.Add,
                        contentDescription = "添加标签",
                        tint = UiColors.Primary,
                        modifier = Modifier.size(16.dp)
                    )
                    Text(
                        text = "添加",
                        fontSize = 14.sp,
                        color = UiColors.Primary
                    )
                }
            }
```

注意：删除后，标签列表行（L79-L111）末尾会留下 `Spacer(modifier = Modifier.width(8.dp))`（L110），需一并删除该 Spacer，避免多余间距。删除后 Row 内仅保留 LazyRow（或无标签时的占位 Spacer）。

- [ ] **Step 4: 更新"确定"按钮重置逻辑**

在输入框区域的"确定"按钮（约 L173-L197）中，将 `isAdding = false` 改为 `onAddingChange(false)`。

将：

```kotlin
                Box(
                    modifier = Modifier
                        .clip(RoundedCornerShape(8.dp))
                        .background(UiColors.Primary)
                        .clickable {
                            /** 验证输入不为空且不重复 */
                            if (newTagText.isNotBlank() && newTagText !in tags) {
                                val updatedTags = tags.toMutableList()
                                updatedTags.add(newTagText.trim())
                                onTagsChange(updatedTags)
                            }
                            /** 重置状态 */
                            newTagText = ""
                            isAdding = false
                        }
```

改为：

```kotlin
                Box(
                    modifier = Modifier
                        .clip(RoundedCornerShape(8.dp))
                        .background(UiColors.Primary)
                        .clickable {
                            /** 验证输入不为空且不重复 */
                            if (newTagText.isNotBlank() && newTagText !in tags) {
                                val updatedTags = tags.toMutableList()
                                updatedTags.add(newTagText.trim())
                                onTagsChange(updatedTags)
                            }
                            /** 重置状态 */
                            newTagText = ""
                            onAddingChange(false)
                        }
```

- [ ] **Step 5: 清理 TagInputField 中不再使用的 import**

删除以下仅被"添加"按钮使用的 import：
- `import androidx.compose.material.icons.Icons`
- `import androidx.compose.material.icons.filled.Add`

注意：`Icons.Default.Close` 仍在 TagChip 中使用，所以 `Icons` import **保留**。检查后仅删除 `import androidx.compose.material.icons.filled.Add`。

同时检查 `clickable` 是否仍被使用（TagChip 的删除按钮 L240 仍用 `.clickable`），**保留** `import androidx.compose.foundation.clickable`。

- [ ] **Step 6: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/TagInputField.kt
git commit -m "重构: TagInputField 状态上提，移除内部添加按钮"
```

---

## Task 2: 顶部工具栏改造——返回箭头统一 + 删除铃铛弹窗

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt:603-744`

**目标：** 返回箭头改为与 TodoEditScreen 一致（onSurface + 24dp）；删除铃铛按钮及下拉弹窗。

- [ ] **Step 1: 统一返回箭头样式**

在 `InspirationEditScreen.kt` 约 L603-L613，将返回箭头 Icon 的 `tint` 和 `size` 改为与 TodoEditScreen 一致。

将：

```kotlin
                /** 返回按钮 */
                IconButton(
                    onClick = { navController.popBackStack() },
                    modifier = Modifier.size(40.dp)
                ) {
                    Icon(
                        imageVector = Icons.AutoMirrored.Filled.ArrowBack,
                        contentDescription = "返回",
                        tint = Color(0xFFFF9A5C),
                        modifier = Modifier.size(20.dp)
                    )
                }
```

改为：

```kotlin
                /** 返回按钮：颜色与尺寸与 TodoEditScreen / EnhancedTopBar 统一 */
                IconButton(
                    onClick = { navController.popBackStack() },
                    modifier = Modifier.size(40.dp)
                ) {
                    Icon(
                        imageVector = Icons.AutoMirrored.Filled.ArrowBack,
                        contentDescription = "返回",
                        tint = MaterialTheme.colorScheme.onSurface,
                        modifier = Modifier.size(24.dp)
                    )
                }
```

- [ ] **Step 2: 删除铃铛按钮及下拉弹窗整块**

删除约 L659-L744 的"通知 + 锁定"Row 中铃铛相关代码。具体地，将"通知 + 锁定（紧凑组）"Row 内容从：

```kotlin
                /** 通知 + 锁定（紧凑组） */
                Row(verticalAlignment = Alignment.CenterVertically) {
                /** 通知/时间设置（下拉菜单：开始日期 | 截止时间 | 提醒时间） */
                var showTimeMenu by remember { mutableStateOf(false) }
                Box {
                    IconButton(
                        onClick = { showTimeMenu = true },
                        modifier = Modifier.size(36.dp)
                    ) {
                        Icon(
                            imageVector = Icons.Default.Notifications,
                            contentDescription = "时间设置",
                            tint = MaterialTheme.colorScheme.onSurfaceVariant,
                            modifier = Modifier.size(18.dp)
                        )
                    }
                    androidx.compose.material3.DropdownMenu(
                        ...（整个 DropdownMenu 及其所有菜单项，直到 Box 闭合）
                    }
                }
                    IconButton(
                        onClick = { isLocked = !isLocked },
                        ...
                    ) { ... }
                }
```

改为（仅保留锁定按钮）：

```kotlin
                /** 锁定按钮 */
                IconButton(
                    onClick = { isLocked = !isLocked },
                    modifier = Modifier.size(36.dp)
                ) {
                    Icon(
                        imageVector = if (isLocked) Icons.Default.Lock else Icons.Default.LockOpen,
                        contentDescription = if (isLocked) "解锁" else "锁定",
                        tint = if (isLocked) Color(0xFFFF9A5C) else MaterialTheme.colorScheme.onSurfaceVariant,
                        modifier = Modifier.size(18.dp)
                    )
                }
```

即：删除整个外层 `Row(verticalAlignment = Alignment.CenterVertically) { ... }` 包裹，删除 `showTimeMenu` 状态、`Box` + `IconButton`(Notifications) + `DropdownMenu` 整块，仅保留锁定 `IconButton` 并去掉外层 Row 包裹（锁定按钮直接放在 Spacer 后）。

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt
git commit -m "重构: 灵感编辑页返回箭头统一样式，删除铃铛通知按钮及下拉弹窗"
```

---

## Task 3: 标题区与交互区改造——标题淡色 + 删优先级 + 新增标签按钮

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt:836-925`

**目标：** 标题占位淡色化；删除优先级按钮；新增标签添加按钮；托管 showTagInput 状态。

- [ ] **Step 1: 新增 showTagInput 托管状态**

在 InspirationEditScreen 函数体内，与其他 `var showXxx by remember` 状态声明处（约 L448 附近，`showMentionPopup` 声明附近），新增：

```kotlin
    /** 标签输入框显示状态（由顶部标签按钮触发，状态上提自 TagInputField） */
    var showTagInput by remember { mutableStateOf(false) }
```

- [ ] **Step 2: 标题占位淡色化**

将标题 OutlinedTextField 的 placeholder（约 L836-L843）从：

```kotlin
                placeholder = {
                    Text(
                        "标题",
                        style = MaterialTheme.typography.headlineMedium.copy(
                            color = MaterialTheme.colorScheme.onSurfaceVariant
                        )
                    )
                },
```

改为：

```kotlin
                placeholder = {
                    Text(
                        "标题",
                        style = MaterialTheme.typography.headlineMedium.copy(
                            color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.5f)
                        )
                    )
                },
```

- [ ] **Step 3: 删除优先级按钮，替换为标签添加按钮**

将"分类 + 优先级行"中优先级部分（约 L888-L902）：

```kotlin
                Spacer(modifier = Modifier.width(10.dp))

                /** 优先级内联选择 */
                listOf(Pair(0, "低"), Pair(1, "中"), Pair(2, "高")).forEach { (value, label) ->
                    Text(
                        text = label,
                        modifier = Modifier
                            .clickable(enabled = !isLocked) { viewModel.setPriority(value) }
                            .padding(horizontal = 5.dp, vertical = 2.dp),
                        color = if (priority == value) Color(0xFFFF9A5C) else MaterialTheme.colorScheme.onSurfaceVariant,
                        fontWeight = if (priority == value) FontWeight.Bold else FontWeight.Normal,
                        fontSize = 12.sp
                    )
                    if (value < 2) Spacer(modifier = Modifier.width(2.dp))
                }
```

替换为标签添加按钮（样式与分类按钮完全一致）：

```kotlin
                Spacer(modifier = Modifier.width(10.dp))

                /** 标签添加按钮：样式与分类按钮一致，点击触发标签输入框 */
                Surface(
                    shape = RoundedCornerShape(14.dp),
                    color = MaterialTheme.colorScheme.surfaceVariant,
                    enabled = !isLocked,
                    onClick = { showTagInput = true }
                ) {
                    Row(
                        modifier = Modifier.padding(horizontal = 10.dp, vertical = 4.dp),
                        verticalAlignment = Alignment.CenterVertically
                    ) {
                        Icon(
                            imageVector = Icons.Default.Add,
                            contentDescription = "添加标签",
                            modifier = Modifier.size(16.dp)
                        )
                        Spacer(modifier = Modifier.width(4.dp))
                        Text(
                            text = "标签",
                            style = MaterialTheme.typography.bodySmall
                        )
                    }
                }
```

- [ ] **Step 4: 更新 TagInputField 调用，传入托管状态**

将 TagInputField 调用（约 L921-L925）从：

```kotlin
            TagInputField(
                tags = tags,
                onTagsChange = { newTags -> viewModel.updateTags(newTags) },
                modifier = Modifier.fillMaxWidth()
            )
```

改为：

```kotlin
            TagInputField(
                tags = tags,
                onTagsChange = { newTags -> viewModel.updateTags(newTags) },
                modifier = Modifier.fillMaxWidth(),
                isAdding = showTagInput,
                onAddingChange = { showTagInput = it }
            )
```

- [ ] **Step 5: 新增 Add 图标 import**

在 import 区域（约 L32-L44 的 `androidx.compose.material.icons...` 处），新增：

```kotlin
import androidx.compose.material.icons.filled.Add
```

- [ ] **Step 6: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt
git commit -m "feat: 灵感编辑页标题淡色化，删除优先级按钮，新增标签添加按钮"
```

---

## Task 4: 删除时间/提醒/重复功能的状态与对话框

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt`

**目标：** 删除铃铛弹窗已移除后遗留的时间设置状态、日期/时间选择器对话框、派生显示逻辑。

- [ ] **Step 1: 删除时间相关 collectAsState 状态**

删除约 L156-L172 的以下状态声明：

```kotlin
    val startDate by viewModel.startDate.collectAsState()
    val dueDate by viewModel.dueDate.collectAsState()
    val estimatedDurationMinutes by viewModel.estimatedDurationMinutes.collectAsState()
    val repeatType by viewModel.repeatType.collectAsState()
```

以及提醒时间相关：

```kotlin
    // 提醒时间相关状态
    val reminderTime by viewModel.reminderTime.collectAsState()
    val recommendedReminderTime by viewModel.recommendedReminderTime.collectAsState()
    val showReminderRecommendation by viewModel.showReminderRecommendation.collectAsState()
```

注意：保留 `val priority by viewModel.priority.collectAsState()`（L148，仅删 UI 不删状态会导致未使用警告，但 spec 要求保留状态；若编译警告需后续处理）。

- [ ] **Step 2: 删除日期/时间选择器状态变量**

删除约 L419-L444 的以下状态：

```kotlin
    var showDatePicker by remember { mutableStateOf(false) }
    val datePickerState = rememberDatePickerState()
    var selectedDateMillis by remember { mutableStateOf<Long?>(null) }
    var showStartTimePicker by remember { mutableStateOf(false) }
    val startTimePickerState = rememberTimePickerState(
        initialHour = 9,
        initialMinute = 0,
        is24Hour = true
    )
    var showTimePicker by remember { mutableStateOf(false) }
    val timePickerState = rememberTimePickerState(
        initialHour = 9,
        initialMinute = 0,
        is24Hour = true
    )

    /** 截止时间选择器相关状态 */
    var showDueDatePicker by remember { mutableStateOf(false) }
    val dueDatePickerState = rememberDatePickerState()
    var selectedDueDateMillis by remember { mutableStateOf<Long?>(null) }
    var showDueTimePicker by remember { mutableStateOf(false) }
    val dueTimePickerState = rememberTimePickerState(
        initialHour = 18,
        initialMinute = 0,
        is24Hour = true
    )
```

- [ ] **Step 3: 删除自动计算时长的 LaunchedEffect**

删除约 L514-L520：

```kotlin
    /** 自动计算预计时长（当开始日期和截止时间都设置后） */
    LaunchedEffect(startDate, dueDate) {
        if (startDate != null && dueDate != null && dueDate!! > startDate!!) {
            val totalMinutes = (dueDate!! - startDate!!) / 1000 / 60
            viewModel.setEstimatedDurationMinutes(totalMinutes.toInt())
        }
    }
```

- [ ] **Step 4: 删除预计时长显示与推荐提醒时间显示**

在内容 Column 内（约 L1415-L1469），删除以下两块 AnimatedVisibility：

预计时长显示：

```kotlin
            /** 自动计算时长显示（根据开始日期和截止时间自动计算） */
            val autoDuration = remember(startDate, dueDate) { ... }

            AnimatedVisibility(visible = autoDuration != null) { ... }
```

推荐提醒时间显示：

```kotlin
            // 推荐提醒时间标签
            AnimatedVisibility(visible = showReminderRecommendation) { ... }
```

注意精确定位：这两块位于"需要分类"警告 Surface（L1382-L1413）之后，主内容 Column 闭合（L1471）之前。删除时注意保持 Column 结构的括号平衡。

- [ ] **Step 5: 删除日期/时间选择器对话框**

删除约 L1531-L1680 的所有日期/时间对话框：
- `if (showDatePicker) { DatePickerDialog(...) }`（L1531-L1556）
- `if (showStartTimePicker) { AlertDialog(...) }`（L1558-L1588）
- `if (showTimePicker) { AlertDialog(...) }`（L1590-L1619）
- `if (showDueDatePicker) { DatePickerDialog(...) }`（L1621-L1647）
- `if (showDueTimePicker) { AlertDialog(...) }`（L1650-L1680）

- [ ] **Step 6: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt
git commit -m "重构: 删除灵感编辑页时间/提醒/重复功能的状态与对话框"
```

---

## Task 5: import 清理与编译验证

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt`

**目标：** 清理因功能删除而不再使用的 import，询问用户进行编译验证。

- [ ] **Step 1: 检查并删除不再使用的 import**

在 `InspirationEditScreen.kt` import 区域，检查并删除以下仅被已删代码使用的 import：

- `import androidx.compose.material.icons.filled.Notifications`（铃铛图标）
- `import androidx.compose.material.icons.filled.DateRange`（日期菜单项图标）
- `import androidx.compose.material.icons.filled.Schedule`（截止时间菜单项图标）
- `import androidx.compose.material3.DatePicker`
- `import androidx.compose.material3.DatePickerDialog`
- `import androidx.compose.material3.rememberDatePickerState`
- `import androidx.compose.material3.TimePicker`
- `import androidx.compose.material3.rememberTimePickerState`
- `import com.corgimemo.app.data.repository.RepeatTaskManager`
- `import java.text.SimpleDateFormat`
- `import java.util.Calendar`
- `import java.util.Date`

**注意**：删除每个 import 前需用 Grep 确认该符号在文件内无其他使用点。例如 `SimpleDateFormat` 可能仅在被删的推荐提醒时间显示中使用；`Calendar` 可能在保留的代码中无使用。逐一验证后再删。

保留确认：
- `import androidx.compose.material3.AlertDialog`——仍在位置提醒弹窗、子任务弹窗、语音权限弹窗中使用，**保留**
- `import androidx.compose.material.icons.filled.Delete`——在 EditToolbar 中使用，**保留**
- `import java.util.Locale`——可能在其他地方使用，需 Grep 确认

- [ ] **Step 2: 检查 priority 状态未使用警告**

`val priority by viewModel.priority.collectAsState()`（L148）删除优先级按钮后不再被引用。由于 spec 要求保留 ViewModel 状态不主动设置默认值，但屏幕级 `priority` 变量已无使用点。

处理方式：删除 `val priority by viewModel.priority.collectAsState()` 这一行屏幕级变量声明（ViewModel 中的 priority Flow 不受影响，仍保留默认值）。这样既符合"仅删 UI"又不产生未使用警告。

- [ ] **Step 3: 检查 import 完整性**

运行 Grep 检查以下新增/修改所需 import 是否齐全：
- `import androidx.compose.material.icons.filled.Add`（Task 3 Step 5 已加）
- `import androidx.compose.material.icons.Icons`（已存在）

- [ ] **Step 4: 询问用户是否进行编译验证**

按工作区规则，编译验证前必须询问用户。向用户确认后执行：

```bash
./gradlew :app:compileDebugKotlin
```

或 Windows：

```bash
gradlew.bat :app:compileDebugKotlin
```

预期：BUILD SUCCESSFUL，无未解析引用、无未使用变量错误。

- [ ] **Step 5: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt
git commit -m "chore: 清理灵感编辑页不再使用的 import 与 priority 屏幕级变量"
```

---

## Self-Review 自审结果

**1. Spec 覆盖检查：**
- 返回箭头统一 → Task 2 Step 1 ✓
- 删除铃铛按钮及弹窗 → Task 2 Step 2 ✓
- 标题占位淡色 → Task 3 Step 2 ✓
- 删除优先级按钮 → Task 3 Step 3 ✓
- 新增标签添加按钮（与分类按钮一致）→ Task 3 Step 3 ✓
- 移除原有标签添加按钮 → Task 1 Step 3 ✓
- TagInputField 状态上提 → Task 1 ✓
- 删除时间/提醒/重复功能 → Task 4 ✓
- import 清理 → Task 5 ✓
- 保留地理围栏 → 未列入删除，默认保留 ✓

**2. 占位符扫描：** 无 TBD/TODO，所有步骤含具体代码。✓

**3. 类型一致性：** `isAdding: Boolean` 与 `onAddingChange: (Boolean) -> Unit` 在 Task 1 定义、Task 3 使用，签名一致。`showTagInput` 在 Task 3 定义并传入，命名一致。✓
