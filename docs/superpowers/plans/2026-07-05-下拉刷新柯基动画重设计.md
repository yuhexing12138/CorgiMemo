# 下拉刷新柯基动画重设计 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除现有"柯基从右侧跑出"下拉刷新逻辑，改为自定义 NestedScrollConnection + 居中奔跑柯基 + 阻尼曲线的全新下拉刷新体验。

**Architecture:** 自定义 `PullRefreshState` 4 值状态机（IDLE/PULLING/RELEASING/REFRESHING）+ 纯函数 `computeDampedOffset` / `computeNextPullRefreshState`（与 Compose runtime 解耦，可单元测试）+ `PullRefreshStateHolder` 持有 `Animatable` 与 `NestedScrollConnection` + 新 `CorgiPullRefreshIndicator` 渲染空白区与居中奔跑柯基。HomeScreen 用 `Box + nestedScroll + graphicsLayer.translationY` 替换 Material3 `PullToRefreshBox`。

**Tech Stack:** Jetpack Compose（BOM 2026.04.01）、NestedScrollConnection、Animatable、FrameAnimation、AnimationResourceManager、JUnit4。

---

## 文件结构

| 文件 | 责任 | 操作 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshState.kt` | 4 值状态机枚举 | 新建 |
| `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshUtils.kt` | 阻尼曲线 + 状态机转换纯函数 | 新建 |
| `app/src/test/java/com/corgimemo/app/ui/components/PullRefreshUtilsTest.kt` | 纯函数单元测试（10 用例） | 新建 |
| `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshStateHolder.kt` | 状态持有者 + NestedScrollConnection + remember 工厂 | 新建 |
| `app/src/main/java/com/corgimemo/app/ui/components/CorgiPullToRefreshIndicator.kt` | 空白区 + 居中奔跑柯基 + 提示文字（重写） | 修改（整体替换） |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 替换 PullToRefreshBox 为自定义 Box + nestedScroll | 修改 |

---

## Task 1: 创建 PullRefreshState 枚举 + computeDampedOffset 纯函数 + 测试

**Files:**
- Create: `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshState.kt`
- Create: `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshUtils.kt`
- Test: `app/src/test/java/com/corgimemo/app/ui/components/PullRefreshUtilsTest.kt`

- [ ] **Step 1: 创建 PullRefreshState 枚举**

Create `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshState.kt`:

```kotlin
package com.corgimemo.app.ui.components

/**
 * 下拉刷新状态机
 *
 * 4 值枚举，描述下拉刷新全生命周期：
 * - IDLE：空闲，无下拉
 * - PULLING：用户正在下拉
 * - RELEASING：释放后回弹动画中（未达阈值）
 * - REFRESHING：刷新中（达阈值释放后）
 *
 * 状态转换规则（详见 PullRefreshUtils.computeNextPullRefreshState）：
 * IDLE → PULLING → (释放) → REFRESHING（达阈值）或 RELEASING（未达阈值）
 * RELEASING → IDLE（回弹完成）
 * REFRESHING → IDLE（isRefreshing=false）
 */
enum class PullRefreshState {
    IDLE,
    PULLING,
    RELEASING,
    REFRESHING
}
```

- [ ] **Step 2: 创建 PullRefreshUtils.kt（含 computeDampedOffset）**

Create `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshUtils.kt`:

```kotlin
package com.corgimemo.app.ui.components

/**
 * 下拉刷新核心纯函数
 *
 * 与 Compose runtime 解耦，便于单元测试。
 * 所有函数均为纯函数：相同输入 → 相同输出，无副作用。
 */

/**
 * 阻尼曲线：随下拉量增大，阻尼系数递减
 *
 * 阻尼公式：dampingFactor = 1 - (rawOffset / maxPullHeight)²
 *
 * 特性：
 * - rawOffset = 0：阻尼系数 1.0（无阻尼，跟随手指）
 * - rawOffset = maxPullHeight / 2：阻尼系数 0.75（轻微阻尼）
 * - rawOffset = maxPullHeight：阻尼系数 0.0（完全锁死，无法继续下拉）
 *
 * 边界处理：
 * - rawOffset <= 0：返回 0（无下拉）
 * - rawOffset >= maxPullHeight：返回 maxPullHeight（封顶）
 * - maxPullHeight <= 0：返回 0（防护）
 *
 * @param rawOffset 原始下拉量（单位与 maxPullHeight 一致，px 或 dp）
 * @param maxPullHeight 最大下拉高度
 * @return 阻尼后的实际偏移
 */
fun computeDampedOffset(rawOffset: Float, maxPullHeight: Float): Float {
    if (maxPullHeight <= 0f) return 0f
    if (rawOffset <= 0f) return 0f
    if (rawOffset >= maxPullHeight) return maxPullHeight
    val ratio = rawOffset / maxPullHeight
    val dampingFactor = 1f - ratio * ratio
    return rawOffset * dampingFactor
}
```

- [ ] **Step 3: 创建测试文件（5 个阻尼曲线用例）**

Create `app/src/test/java/com/corgimemo/app/ui/components/PullRefreshUtilsTest.kt`:

```kotlin
package com.corgimemo.app.ui.components

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * PullRefreshUtils 纯函数单元测试
 *
 * 覆盖：
 * - computeDampedOffset 阻尼曲线（5 用例）
 * - computeNextPullRefreshState 状态机转换（5 用例）
 */
class PullRefreshUtilsTest {

    // ===================== computeDampedOffset 阻尼曲线测试 =====================

    @Test
    fun `阻尼曲线_rawOffset为0时返回0`() {
        assertEquals(0f, computeDampedOffset(0f, 100f))
    }

    @Test
    fun `阻尼曲线_中段下拉返回阻尼后的值`() {
        // rawOffset=50, max=100
        // ratio = 0.5, dampingFactor = 1 - 0.25 = 0.75
        // result = 50 * 0.75 = 37.5
        assertEquals(37.5f, computeDampedOffset(50f, 100f), 0.001f)
    }

    @Test
    fun `阻尼曲线_达最大值时返回最大值`() {
        assertEquals(100f, computeDampedOffset(100f, 100f))
    }

    @Test
    fun `阻尼曲线_超过最大值时仍封顶`() {
        assertEquals(100f, computeDampedOffset(200f, 100f))
    }

    @Test
    fun `阻尼曲线_负值归零`() {
        assertEquals(0f, computeDampedOffset(-10f, 100f))
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run:
```bash
./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.PullRefreshUtilsTest"
```
Expected: 5 tests PASS

- [ ] **Step 5: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/PullRefreshState.kt app/src/main/java/com/corgimemo/app/ui/components/PullRefreshUtils.kt app/src/test/java/com/corgimemo/app/ui/components/PullRefreshUtilsTest.kt
git commit -m "feat(pull-refresh): 新增 PullRefreshState 枚举与 computeDampedOffset 纯函数及单元测试"
```

---

## Task 2: 添加 computeNextPullRefreshState 纯函数 + 测试

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshUtils.kt`
- Test: `app/src/test/java/com/corgimemo/app/ui/components/PullRefreshUtilsTest.kt`

- [ ] **Step 1: 在 PullRefreshUtils.kt 末尾追加 computeNextPullRefreshState 函数**

在 `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshUtils.kt` 末尾追加：

```kotlin
/**
 * 状态机转换：根据当前状态与触发条件计算下一个状态
 *
 * 转换规则：
 * - IDLE + pullOffset > 0 → PULLING
 * - IDLE + pullOffset == 0 → IDLE（保持）
 * - PULLING + isReleased + pullOffset >= threshold → REFRESHING
 * - PULLING + isReleased + pullOffset < threshold → RELEASING
 * - PULLING + !isReleased → PULLING（保持）
 * - RELEASING + pullOffset <= 0 → IDLE（回弹完成）
 * - RELEASING + pullOffset > 0 → RELEASING（保持，动画中）
 * - REFRESHING + !isRefreshing → IDLE（刷新完成）
 * - REFRESHING + isRefreshing → REFRESHING（锁定，忽略下拉与释放）
 *
 * @param current 当前状态
 * @param pullOffset 当前下拉偏移（已阻尼，单位与 threshold 一致）
 * @param refreshThreshold 刷新阈值
 * @param isReleased 用户是否释放手指（onPreFling 触发时为 true）
 * @param isRefreshing ViewModel 的 isRefreshing 状态
 * @return 下一个状态
 */
fun computeNextPullRefreshState(
    current: PullRefreshState,
    pullOffset: Float,
    refreshThreshold: Float,
    isReleased: Boolean,
    isRefreshing: Boolean
): PullRefreshState {
    return when (current) {
        PullRefreshState.IDLE -> {
            if (pullOffset > 0f) PullRefreshState.PULLING else PullRefreshState.IDLE
        }
        PullRefreshState.PULLING -> {
            when {
                isReleased && pullOffset >= refreshThreshold -> PullRefreshState.REFRESHING
                isReleased && pullOffset < refreshThreshold -> PullRefreshState.RELEASING
                else -> PullRefreshState.PULLING
            }
        }
        PullRefreshState.RELEASING -> {
            if (pullOffset <= 0f) PullRefreshState.IDLE else PullRefreshState.RELEASING
        }
        PullRefreshState.REFRESHING -> {
            if (!isRefreshing) PullRefreshState.IDLE else PullRefreshState.REFRESHING
        }
    }
}
```

- [ ] **Step 2: 在 PullRefreshUtilsTest.kt 的类末尾追加状态机测试**

在 `app/src/test/java/com/corgimemo/app/ui/components/PullRefreshUtilsTest.kt` 的 `PullRefreshUtilsTest` 类内末尾追加：

```kotlin
    // ===================== computeNextPullRefreshState 状态机测试 =====================

    @Test
    fun `状态机_IDLE且无下拉时保持IDLE`() {
        assertEquals(
            PullRefreshState.IDLE,
            computeNextPullRefreshState(
                current = PullRefreshState.IDLE,
                pullOffset = 0f,
                refreshThreshold = 60f,
                isReleased = false,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_IDLE有下拉时进入PULLING`() {
        assertEquals(
            PullRefreshState.PULLING,
            computeNextPullRefreshState(
                current = PullRefreshState.IDLE,
                pullOffset = 10f,
                refreshThreshold = 60f,
                isReleased = false,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_PULLING达阈值释放进入REFRESHING`() {
        assertEquals(
            PullRefreshState.REFRESHING,
            computeNextPullRefreshState(
                current = PullRefreshState.PULLING,
                pullOffset = 70f,
                refreshThreshold = 60f,
                isReleased = true,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_PULLING未达阈值释放进入RELEASING`() {
        assertEquals(
            PullRefreshState.RELEASING,
            computeNextPullRefreshState(
                current = PullRefreshState.PULLING,
                pullOffset = 30f,
                refreshThreshold = 60f,
                isReleased = true,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_RELEASING回弹完成进入IDLE`() {
        assertEquals(
            PullRefreshState.IDLE,
            computeNextPullRefreshState(
                current = PullRefreshState.RELEASING,
                pullOffset = 0f,
                refreshThreshold = 60f,
                isReleased = false,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_RELEASING动画中保持RELEASING`() {
        assertEquals(
            PullRefreshState.RELEASING,
            computeNextPullRefreshState(
                current = PullRefreshState.RELEASING,
                pullOffset = 15f,
                refreshThreshold = 60f,
                isReleased = false,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_REFRESHING刷新完成进入IDLE`() {
        assertEquals(
            PullRefreshState.IDLE,
            computeNextPullRefreshState(
                current = PullRefreshState.REFRESHING,
                pullOffset = 60f,
                refreshThreshold = 60f,
                isReleased = false,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_REFRESHING中保持REFRESHING_忽略下拉`() {
        assertEquals(
            PullRefreshState.REFRESHING,
            computeNextPullRefreshState(
                current = PullRefreshState.REFRESHING,
                pullOffset = 80f,
                refreshThreshold = 60f,
                isReleased = true,  // 即使释放也忽略
                isRefreshing = true
            )
        )
    }

    @Test
    fun `状态机_PULLING未释放保持PULLING`() {
        assertEquals(
            PullRefreshState.PULLING,
            computeNextPullRefreshState(
                current = PullRefreshState.PULLING,
                pullOffset = 50f,
                refreshThreshold = 60f,
                isReleased = false,
                isRefreshing = false
            )
        )
    }

    @Test
    fun `状态机_PULLING恰等于阈值释放进入REFRESHING`() {
        assertEquals(
            PullRefreshState.REFRESHING,
            computeNextPullRefreshState(
                current = PullRefreshState.PULLING,
                pullOffset = 60f,
                refreshThreshold = 60f,
                isReleased = true,
                isRefreshing = false
            )
        )
    }
```

- [ ] **Step 3: 运行测试验证通过**

Run:
```bash
./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.PullRefreshUtilsTest"
```
Expected: 15 tests PASS（5 阻尼 + 10 状态机）

- [ ] **Step 4: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/PullRefreshUtils.kt app/src/test/java/com/corgimemo/app/ui/components/PullRefreshUtilsTest.kt
git commit -m "feat(pull-refresh): 新增 computeNextPullRefreshState 状态机纯函数及 10 个单元测试"
```

---

## Task 3: 创建 PullRefreshStateHolder + rememberPullRefreshStateHolder

**Files:**
- Create: `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshStateHolder.kt`

- [ ] **Step 1: 创建 PullRefreshStateHolder.kt**

Create `app/src/main/java/com/corgimemo/app/ui/components/PullRefreshStateHolder.kt`:

```kotlin
package com.corgimemo.app.ui.components

import androidx.compose.animation.Animatable
import androidx.compose.animation.core.tween
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.rememberCoroutineScope
import androidx.compose.runtime.setValue
import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.input.nestedscroll.NestedScrollConnection
import androidx.compose.ui.input.nestedscroll.NestedScrollSource
import androidx.compose.ui.platform.LocalDensity
import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.Velocity
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.launch
import kotlin.math.min

/**
 * 下拉刷新状态持有者
 *
 * 持有：
 * - state: PullRefreshState（当前状态）
 * - pullOffset: Float（当前下拉偏移，单位 px，已阻尼）
 * - nestedScrollConnection: NestedScrollConnection（注入外层 Box）
 * - onRefreshComplete: () -> Unit（刷新完成回调，由 LaunchedEffect 调用）
 *
 * 不持有 isRefreshing，由 ViewModel 提供，外层通过 LaunchedEffect 监听并调用 onRefreshComplete。
 *
 * 坐标系约定（Compose NestedScrollConnection）：
 * - available.y < 0：用户下拉（列表在顶部，无法继续向下滚动）
 * - available.y > 0：用户上拉（列表在底部，无法继续向上滚动）
 *
 * @param maxPullHeightPx 最大下拉高度（px）
 * @param refreshThresholdPx 刷新阈值（px）
 * @param onRefresh 触发刷新回调
 * @param pullOffsetAnimatable 下拉偏移 Animatable（由 remember 持有）
 * @param scope 协程作用域
 */
class PullRefreshStateHolder(
    val maxPullHeightPx: Float,
    val refreshThresholdPx: Float,
    private val onRefresh: () -> Unit,
    private val pullOffsetAnimatable: Animatable<Float, *>,
    private val scope: CoroutineScope
) {
    /** 当前状态 */
    var state: PullRefreshState by mutableStateOf(PullRefreshState.IDLE)
        private set

    /** 当前下拉偏移（px，已阻尼） */
    val pullOffset: Float
        get() = pullOffsetAnimatable.value

    /**
     * 嵌套滚动连接
     *
     * 实现策略：
     * - onPostScroll：列表在顶部无法继续向下滚动时，剩余的 available.y < 0 转为 pullOffset
     * - onPreFling：用户释放时，根据 pullOffset 判断触发刷新还是回弹
     *
     * 在 REFRESHING 状态下锁定，不响应任何手势。
     */
    val nestedScrollConnection: NestedScrollConnection = object : NestedScrollConnection {
        /**
         * 列表滚动后处理
         *
         * available.y < 0：用户下拉，列表无法继续滚动 → 增加 pullOffset
         * available.y > 0：用户上拉，且 pullOffset > 0 → 减少 pullOffset
         */
        override fun onPostScroll(
            consumed: Offset,
            available: Offset,
            source: NestedScrollSource
        ): Offset {
            if (state == PullRefreshState.REFRESHING) return Offset.Zero

            // 用户下拉（available.y < 0）
            if (available.y < 0f) {
                val rawDelta = -available.y  // 转为正数
                val newRawOffset = pullOffsetAnimatable.value + rawDelta
                val newDampedOffset = computeDampedOffset(newRawOffset, maxPullHeightPx)
                val actualConsumed = newDampedOffset - pullOffsetAnimatable.value
                pullOffsetAnimatable.snapTo(newDampedOffset)
                updateState(isReleased = false)
                return Offset(0f, -actualConsumed)  // 返回消费的量（与 available 同方向，负值）
            }

            // 用户上拉（available.y > 0），且 pullOffset > 0，回收 pullOffset
            if (available.y > 0f && pullOffsetAnimatable.value > 0f) {
                val consumed = min(available.y, pullOffsetAnimatable.value)
                pullOffsetAnimatable.snapTo(pullOffsetAnimatable.value - consumed)
                updateState(isReleased = false)
                return Offset(0f, consumed)
            }

            return Offset.Zero
        }

        /**
         * 用户释放（fling 开始前）
         *
         * 根据 pullOffset 判断：
         * - pullOffset >= refreshThreshold → REFRESHING，触发 onRefresh，保持 pullOffset 在阈值
         * - pullOffset < refreshThreshold → RELEASING，回弹到 0
         */
        override suspend fun onPreFling(available: Velocity): Velocity {
            if (state != PullRefreshState.PULLING) return Velocity.Zero

            if (pullOffsetAnimatable.value >= refreshThresholdPx) {
                // 达阈值，触发刷新
                state = PullRefreshState.REFRESHING
                onRefresh()
                // 保持 pullOffset 在阈值位置
                pullOffsetAnimatable.animateTo(refreshThresholdPx, tween(durationMillis = 300))
            } else {
                // 未达阈值，回弹
                state = PullRefreshState.RELEASING
                pullOffsetAnimatable.animateTo(0f, tween(durationMillis = 300))
                state = PullRefreshState.IDLE
            }
            return Velocity.Zero  // 消费所有速度，阻止列表 fling
        }
    }

    /**
     * 刷新完成回调
     *
     * 由外层 LaunchedEffect(isRefreshing) 在 isRefreshing=false 时调用。
     * 启动协程将 pullOffset 动画到 0，并将状态置为 IDLE。
     */
    fun onRefreshComplete() {
        if (state != PullRefreshState.REFRESHING) return
        scope.launch {
            pullOffsetAnimatable.animateTo(0f, tween(durationMillis = 300))
            state = PullRefreshState.IDLE
        }
    }

    /**
     * 更新状态（下拉过程中实时调用）
     *
     * @param isReleased 用户是否释放
     */
    private fun updateState(isReleased: Boolean) {
        state = computeNextPullRefreshState(
            current = state,
            pullOffset = pullOffsetAnimatable.value,
            refreshThreshold = refreshThresholdPx,
            isReleased = isReleased,
            // 下拉过程中 isRefreshing 不影响（仅 REFRESHING 状态关心，由 onRefreshComplete 处理）
            isRefreshing = state == PullRefreshState.REFRESHING
        )
    }
}

/**
 * 记住 PullRefreshStateHolder
 *
 * @param maxPullHeight 最大下拉高度（默认 100dp）
 * @param refreshThreshold 刷新阈值（默认 60dp）
 * @param onRefresh 触发刷新回调
 */
@Composable
fun rememberPullRefreshStateHolder(
    maxPullHeight: Dp = 100.dp,
    refreshThreshold: Dp = 60.dp,
    onRefresh: () -> Unit
): PullRefreshStateHolder {
    val density = LocalDensity.current
    val scope = rememberCoroutineScope()
    val maxPullHeightPx = with(density) { maxPullHeight.toPx() }
    val refreshThresholdPx = with(density) { refreshThreshold.toPx() }
    val pullOffsetAnimatable = remember { Animatable(0f) }

    return remember(maxPullHeightPx, refreshThresholdPx) {
        PullRefreshStateHolder(
            maxPullHeightPx = maxPullHeightPx,
            refreshThresholdPx = refreshThresholdPx,
            onRefresh = onRefresh,
            pullOffsetAnimatable = pullOffsetAnimatable,
            scope = scope
        )
    }
}
```

- [ ] **Step 2: 编译验证**

Run:
```bash
./gradlew :app:compileDebugKotlin
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/PullRefreshStateHolder.kt
git commit -m "feat(pull-refresh): 新增 PullRefreshStateHolder 与 rememberPullRefreshStateHolder"
```

---

## Task 4: 重写 CorgiPullToRefreshIndicator

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/CorgiPullToRefreshIndicator.kt`（整体替换）

- [ ] **Step 1: 整体替换 CorgiPullToRefreshIndicator.kt**

用以下内容**完整覆盖** `app/src/main/java/com/corgimemo/app/ui/components/CorgiPullToRefreshIndicator.kt`：

```kotlin
package com.corgimemo.app.ui.components

import androidx.compose.animation.core.Animatable
import androidx.compose.foundation.layout.Box
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.size
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.remember
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.platform.LocalDensity
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp
import com.corgimemo.app.animation.AnimationResourceManager
import com.corgimemo.app.animation.AnimationType
import com.corgimemo.app.animation.FrameAnimation

/**
 * 柯基下拉刷新指示器（新版）
 *
 * 与旧版的差异：
 * - 删除"从右侧跑入"逻辑
 * - 改为空白区 + 水平居中奔跑柯基 + 提示文字
 * - 柯基动画所有阶段都使用 AnimationType.RUN（不切换为 WAG）
 * - 柯基大小随下拉进度从 48dp 渐变至 64dp
 *
 * 渲染层次：
 * 1. 空白区（高度 = pullOffset，铺满宽度）
 * 2. 居中奔跑柯基（FrameAnimation）
 * 3. 提示文字（下拉刷新 / 释放刷新 / 柯基努力加载中~）
 *
 * @param pullOffset 当前下拉偏移（px，已阻尼）
 * @param state 当前下拉刷新状态
 * @param maxPullHeightPx 最大下拉高度（px，用于计算柯基大小渐变）
 * @param modifier 修饰符
 */
@Composable
fun CorgiPullRefreshIndicator(
    pullOffset: Float,
    state: PullRefreshState,
    maxPullHeightPx: Float,
    modifier: Modifier = Modifier
) {
    val density = LocalDensity.current
    val pullOffsetDp = with(density) { pullOffset.toDp() }

    // 柯基显示条件：有下拉量 或 刷新中
    val showCorgi = pullOffset > 0.01f || state == PullRefreshState.REFRESHING

    // 下拉进度（0~1），用于柯基大小渐变
    val pullProgress = if (maxPullHeightPx > 0f) {
        (pullOffset / maxPullHeightPx).coerceIn(0f, 1f)
    } else {
        0f
    }

    // 柯基大小：48dp → 64dp 渐变
    val corgiSizeDp = 48.dp + (64.dp - 48.dp) * pullProgress

    // 提示文字
    val tipText = when {
        state == PullRefreshState.REFRESHING -> "柯基努力加载中~"
        state == PullRefreshState.PULLING && pullOffset >= with(density) { 60.dp.toPx() } -> "释放刷新"
        state == PullRefreshState.PULLING -> "下拉刷新"
        else -> null
    }

    if (!showCorgi) return

    Box(
        modifier = modifier
            .fillMaxWidth()
            .height(pullOffsetDp),
        contentAlignment = Alignment.Center
    ) {
        // 居中奔跑柯基
        val frames = remember(AnimationType.RUN) {
            AnimationResourceManager.getAnimationFrames(AnimationType.RUN)
        }

        if (frames.isNotEmpty()) {
            FrameAnimation(
                frames = frames,
                fps = 8,
                isLooping = true,
                isPlaying = true,
                modifier = Modifier.size(corgiSizeDp)
            )
        }

        // 提示文字（位于柯基下方）
        if (tipText != null) {
            Text(
                text = tipText,
                fontSize = 12.sp,
                color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.6f),
                modifier = Modifier.align(Alignment.BottomCenter)
            )
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run:
```bash
./gradlew :app:compileDebugKotlin
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/CorgiPullToRefreshIndicator.kt
git commit -m "feat(pull-refresh): 重写 CorgiPullRefreshIndicator 为空白区+居中奔跑柯基+提示文字"
```

---

## Task 5: 修改 HomeScreen 替换 PullToRefreshBox

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

- [ ] **Step 1: 删除旧 import（3 行）**

在 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 中删除以下 3 行 import：

```kotlin
import com.corgimemo.app.ui.components.CorgiPullToRefreshIndicator  // L126
import androidx.compose.material3.pulltorefresh.PullToRefreshBox    // L134
import androidx.compose.material3.pulltorefresh.rememberPullToRefreshState  // L135
```

- [ ] **Step 2: 添加新 import**

在 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 的 import 区域（紧挨 `import com.corgimemo.app.ui.components.*` 之后）添加：

```kotlin
import com.corgimemo.app.ui.components.CorgiPullRefreshIndicator
import com.corgimemo.app.ui.components.PullRefreshState
import com.corgimemo.app.ui.components.rememberPullRefreshStateHolder
import androidx.compose.ui.input.nestedscroll.nestedScroll
```

- [ ] **Step 3: 替换 L714-715 的 isRefreshing + pullToRefreshState**

将 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 第 714-715 行：

```kotlin
                        val isRefreshing by viewModel.isRefreshing.collectAsState()
                        val pullToRefreshState = rememberPullToRefreshState()
```

替换为：

```kotlin
                        val isRefreshing by viewModel.isRefreshing.collectAsState()
                        val pullRefreshState = rememberPullRefreshStateHolder(
                            maxPullHeight = 100.dp,
                            refreshThreshold = 60.dp,
                            onRefresh = { viewModel.onRefresh() }
                        )

                        // 刷新完成时回弹 pullOffset
                        LaunchedEffect(isRefreshing) {
                            if (!isRefreshing) pullRefreshState.onRefreshComplete()
                        }
```

- [ ] **Step 4: 替换 L785-797 的 PullToRefreshBox 块**

将 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 第 785-797 行的 `PullToRefreshBox { ... }` 块：

```kotlin
                        PullToRefreshBox(
                            isRefreshing = isRefreshing,
                            onRefresh = { viewModel.onRefresh() },
                            state = pullToRefreshState,
                            modifier = Modifier.fillMaxSize(),
                            indicator = {
                                CorgiPullToRefreshIndicator(
                                    isRefreshing = isRefreshing,
                                    pullProgress = pullToRefreshState.distanceFraction.coerceIn(0f, 1f),
                                    modifier = Modifier.align(Alignment.TopCenter)
                                )
                            }
                        ) {
                            ZonedReorderableLazyColumn(
                                items = displayItems,
                                // ... 其余参数与 content lambda 体保持原样（L798-... 末尾）
                            )
                        }
```

替换为以下结构（**关键：原 `ZonedReorderableLazyColumn(...)` 调用从 L798 开始到 `PullToRefreshBox` 闭合 `}` 的整段代码原封不动地嵌入内层 `Box` 中，不修改任何参数与 content lambda 体**）：

```kotlin
                        // 外层 Box：承载 nestedScrollConnection 与柯基指示器
                        Box(
                            modifier = Modifier
                                .fillMaxSize()
                                .nestedScroll(pullRefreshState.nestedScrollConnection)
                        ) {
                            // 空白区 + 居中奔跑柯基（铺满宽度，高度=pullOffset）
                            CorgiPullRefreshIndicator(
                                pullOffset = pullRefreshState.pullOffset,
                                state = pullRefreshState.state,
                                maxPullHeightPx = pullRefreshState.maxPullHeightPx,
                                modifier = Modifier.fillMaxWidth()
                            )

                            // 内层 Box：列表 + 搜索框整体下移 pullOffset
                            Box(
                                modifier = Modifier
                                    .fillMaxSize()
                                    .graphicsLayer { translationY = pullRefreshState.pullOffset }
                            ) {
                                // ====== 以下为原 ZonedReorderableLazyColumn 调用，原样保留 ======
                                ZonedReorderableLazyColumn(
                                    items = displayItems,
                                    listState = lazyListState,
                                    isDragEnabled = !isBatchMode && swipeExpandedTodoId == null
                                        && searchQuery.isBlank() && selectedCategoryId == null,
                                    key = { item ->
                                        when (item) {
                                            is DisplayItem.Todo -> item.item.id
                                            is DisplayItem.PinnedDivider -> "pinned_divider_${item.count}"
                                            is DisplayItem.PendingDivider -> "pending_divider_${item.count}"
                                            is DisplayItem.CompletedDivider -> "completed_divider"
                                        }
                                    },
                                    onReorder = { dragResult, draggedTodoItem, targetZoneRelativeIndex ->
                                        viewModel.reorderOnDragResult(
                                            draggedItemId = draggedTodoItem.item.id,
                                            draggedTodo = draggedTodoItem.item,
                                            dragResult = dragResult,
                                            targetZoneRelativeIndex = targetZoneRelativeIndex
                                        )
                                    },
                                    modifier = Modifier
                                        .fillMaxSize()
                                        .padding(horizontal = 8.dp)
                                        .graphicsLayer {
                                            translationY = -(1f - searchRevealProgress.value) * searchBarFullHeightPx * 0.12f
                                        }
                                ) { index, displayItem, isDragging, dragActive ->
                                    // ... 原 content lambda 体（从 L827 到 ZonedReorderableLazyColumn 闭合）原样保留
                                    //     包括 PinnedSectionHeader / PendingSectionHeader / CompletedSectionHeader / TodoListItem 渲染
                                }
                                // ====== 原 ZonedReorderableLazyColumn 调用结束 ======
                            }
                        }
```

**操作要点**：
1. 删除 `PullToRefreshBox(...)` 的外层包裹与 `indicator = { ... }` 参数
2. 将原 `ZonedReorderableLazyColumn(...)` 调用整体平移到内层 `Box` 中（参数与 content lambda 体不修改一字）
3. 在 `ZonedReorderableLazyColumn` 同级（外层 Box 内）添加 `CorgiPullRefreshIndicator` 渲染
4. 在内层 `Box` 的 `Modifier` 上添加 `.graphicsLayer { translationY = pullRefreshState.pullOffset }`

- [ ] **Step 5: 检查 import 完整性**

确认以下 import 已存在或已添加：

```kotlin
import androidx.compose.foundation.layout.Box          // 已存在
import androidx.compose.ui.graphics.graphicsLayer      // 已存在（L93）
import androidx.compose.runtime.LaunchedEffect         // 已存在（L76）
import com.corgimemo.app.ui.components.CorgiPullRefreshIndicator  // 新增
import com.corgimemo.app.ui.components.PullRefreshState           // 新增
import com.corgimemo.app.ui.components.rememberPullRefreshStateHolder  // 新增
import androidx.compose.ui.input.nestedscroll.nestedScroll         // 新增
```

- [ ] **Step 6: 编译验证**

Run:
```bash
./gradlew :app:compileDebugKotlin
```
Expected: BUILD SUCCESSFUL

如有 import 缺失，按编译错误提示逐个添加。

- [ ] **Step 7: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt
git commit -m "refactor(home): HomeScreen 替换 PullToRefreshBox 为自定义 Box + nestedScroll + 柯基指示器"
```

---

## Task 6: 编译 Debug APK + 运行所有单元测试

**Files:**
- 无文件修改

- [ ] **Step 1: 编译 Debug APK**

Run:
```bash
./gradlew :app:assembleDebug
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 运行所有单元测试**

Run:
```bash
./gradlew :app:testDebugUnitTest
```
Expected: ALL TESTS PASS（57+ 已有用例 + 15 新增 PullRefreshUtilsTest 用例）

- [ ] **Step 3: 检查测试报告**

如有失败，定位并修复。重点关注：
- `PullRefreshUtilsTest`：15 个用例应全部通过
- `ReorderAlgorithmsTest`：20 个用例应全部通过
- `HomeViewModelReorderTest`：拖拽逻辑应不受影响
- `DragZoneStateMachineTest`：拖拽状态机应不受影响
- `inferZoneFromDisplayItems`：5 个用例应全部通过
- `TodoZoneTest`：zone 枚举应不受影响
- `ZonedReorderableLazyColumnTest`：拖拽列表应不受影响

- [ ] **Step 4: 提交（如有修复）**

如无修复则跳过此步骤。如有修复：

```bash
git add -A
git commit -m "test(pull-refresh): 修复测试并验证所有用例通过"
```

---

## 验收清单

完成所有任务后，逐项验证：

- [ ] 删除"柯基从右侧跑出"逻辑（旧 `CorgiPullToRefreshIndicator` 完全替换）
- [ ] 下拉时搜索框与卡片区之间出现空白区，阻尼递增
- [ ] 空白区水平居中处显示奔跑柯基动画
- [ ] 柯基大小随下拉量从 48dp 渐变至 64dp
- [ ] 达阈值（60dp）后释放触发刷新
- [ ] 未达阈值释放回弹，不触发刷新
- [ ] 刷新中状态锁定，再次下拉不响应
- [ ] 刷新完成后柯基消失，列表复位
- [ ] 现有 57+ 单元测试全部通过
- [ ] 新增 15 个 PullRefreshUtilsTest 用例通过
- [ ] Debug APK 编译成功

---

## 后续手动验证（真机 · 8 项）

| # | 场景 | 操作 | 期望表现 |
|---|---|---|---|
| 1 | 未达阈值释放 | 轻下拉 30dp 后释放 | 空白回弹消失，不触发刷新，柯基消失 |
| 2 | 达阈值释放 | 下拉至 60dp 后释放 | 触发刷新，柯基奔跑，提示"柯基努力加载中~" |
| 3 | 拉至最大 | 下拉至 100dp 后继续下拉 | 阻尼明显增大，无法继续下拉，柯基 64dp |
| 4 | 刷新完成 | 场景 2 后等待刷新完成 | 柯基消失，列表复位到顶部 |
| 5 | 刷新中再下拉 | 场景 2 刷新中再次下拉 | 不响应（状态锁定在 REFRESHING） |
| 6 | 搜索框位置 | 任意下拉 | 搜索框随列表下移，与卡片区同步 |
| 7 | 柯基居中 | 任意下拉 | 柯基始终在空白区水平居中 |
| 8 | 柯基大小渐变 | 下拉 0→100dp | 柯基从 48dp 渐变至 64dp |

---

## 风险与回滚

### 风险点

1. **NestedScrollConnection 方向**：如真机测试发现"下拉变上拉"或"无响应"，检查 `onPostScroll` 中 `available.y` 的符号
2. **onPreFling 不触发**：如慢速下拉释放无响应，可能需要改用 `onPostFling` 或添加 `pointerInput` 检测手势抬起
3. **translationY 与拖拽冲突**：拖拽进行中应禁用 `nestedScrollConnection`（可在 `isDragActive` 时返回 `Offset.Zero`）

### 回滚方案

如出现严重问题，回滚到 commit `d979c16`（spec 提交点）之前：

```bash
git revert <commit-hash>
```

或直接恢复 `HomeScreen.kt` 和 `CorgiPullToRefreshIndicator.kt` 到修改前状态。
