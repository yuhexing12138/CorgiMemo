# 日期卡片尺寸重设 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 严格按照参考图对两种日期详情卡片的尺寸和内部元素比例进行重设，添加新装饰元素，并调整状态栏图标颜色行为。

**Architecture:** 使用 `BoxWithConstraints` 测量卡片高度，所有内部元素按卡片总高百分比分配；卡片整体使用 `aspectRatio` 设置固定宽高比；虚线使用 `Canvas` + `PathEffect.dashPathEffect` 实现；顶部波浪复用 `WavyBottomShape` 的思路创建 `WavyTopShape`。

**Tech Stack:** Jetpack Compose, Room (已有), Kotlin, `BoxWithConstraints`, `PathEffect.dashPathEffect`

---

## 任务分解总览

| 任务 | 描述 | 改动文件 |
|------|------|---------|
| Task 1 | 状态栏图标固定为深色 | `SpecialDateCardStyleScreen.kt` |
| Task 2 | MiniCalendar 添加虚线 | `MiniCalendar.kt` |
| Task 3 | 创建顶部波浪 Shape | `WavyTopShape.kt` (新文件) |
| Task 4 | 橙色撕页卡片比例化改造 + 底部点点装饰 | `OrangeTearOffCard.kt` |
| Task 5 | 日历撕页卡片比例化改造 + 顶部波浪 + 月历下方虚线 | `CalendarTearOffCard.kt` |

---

### Task 1: 状态栏图标固定为深色

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/SpecialDateCardStyleScreen.kt:149-156`

**目标：** 移除状态栏图标随背景色自动变色的逻辑，固定为深色（黑色）。

- [ ] **Step 1: 修改 SideEffect 中的状态栏逻辑**

将：
```kotlin
val view = LocalView.current
if (!view.isInEditMode) {
    SideEffect {
        val window = (view.context as Activity).window
        WindowCompat.getInsetsController(window, view)
            .isAppearanceLightStatusBars = screenBackgroundColor.luminance() > 0.5f
    }
}
```

替换为：
```kotlin
val view = LocalView.current
if (!view.isInEditMode) {
    SideEffect {
        val window = (view.context as Activity).window
        WindowCompat.getInsetsController(window, view)
            .isAppearanceLightStatusBars = true
    }
}
```

- [ ] **Step 2: 移除无用 import**

检查 `luminance()` 相关 import 是否还在使用，如果不再使用则移除：
- `import androidx.compose.ui.graphics.luminance`
- `import androidx.compose.ui.graphics.toArgb`（如未使用）

---

### Task 2: MiniCalendar 添加虚线

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/components/cardstyle/MiniCalendar.kt`

**目标：** 在星期表头下方和每行日期之间添加虚线分割线。

- [ ] **Step 1: 添加必要的 import**

在文件顶部添加：
```kotlin
import androidx.compose.foundation.Canvas
import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.graphics.PathEffect
import androidx.compose.ui.graphics.drawscope.Stroke
```

- [ ] **Step 2: 添加虚线参数**

修改 `MiniCalendar` 函数签名，新增两个参数：
```kotlin
@Composable
fun MiniCalendar(
    year: Int,
    month: Int,
    targetDay: Int,
    modifier: Modifier = Modifier,
    textSize: TextUnit = 14.sp,
    targetRingColor: Color = Color(0xFFFF8A80),
    targetTextColor: Color = Color.Unspecified,
    showDividers: Boolean = true,
    dividerColor: Color = Color(0xFFCCCCCC)
) {
```

- [ ] **Step 3: 添加表头下方虚线**

在 `Spacer(Modifier.height(4.dp))` 之后、`repeat(5)` 之前，添加表头虚线（仅当 `showDividers` 为 true 时）：

```kotlin
        Spacer(Modifier.height(4.dp))
        // 表头下方虚线
        if (showDividers) {
            Canvas(modifier = Modifier.fillMaxWidth().height(1.dp)) {
                drawLine(
                    color = dividerColor,
                    start = Offset(0f, 0f),
                    end = Offset(size.width, 0f),
                    strokeWidth = 1f,
                    pathEffect = PathEffect.dashPathEffect(floatArrayOf(4.dp.toPx(), 4.dp.toPx()), 0f)
                )
            }
            Spacer(Modifier.height(4.dp))
        }
```

- [ ] **Step 4: 添加每行日期之间的虚线**

在 `repeat(5) { weekIndex ->` 内部，每一行日期渲染完成后（行末），添加行之间的虚线（除了最后一行）：

在每行的 `Row` 关闭之后，添加：
```kotlin
            if (showDividers && weekIndex < 4) {
                Spacer(Modifier.height(2.dp))
                Canvas(modifier = Modifier.fillMaxWidth().height(1.dp)) {
                    drawLine(
                        color = dividerColor,
                        start = Offset(0f, 0f),
                        end = Offset(size.width, 0f),
                        strokeWidth = 1f,
                        pathEffect = PathEffect.dashPathEffect(floatArrayOf(4.dp.toPx(), 4.dp.toPx()), 0f)
                    )
                }
                Spacer(Modifier.height(2.dp))
            }
```

注意：需要调整行间距以适应虚线。原行间距由 Row 自身高度决定，现在需要在 Row 之间插入虚线。

---

### Task 3: 创建顶部波浪 Shape

**Files:**
- Create: `app/src/main/java/com/corgimemo/app/ui/screens/date/components/cardstyle/WavyTopShape.kt`

**目标：** 创建 `WavyTopShape`，与 `WavyBottomShape` 对称，仅修改顶部边缘为波浪形。

- [ ] **Step 1: 创建 WavyTopShape.kt 文件**

```kotlin
package com.corgimemo.app.ui.screens.date.components.cardstyle

import androidx.compose.ui.geometry.Size
import androidx.compose.ui.graphics.Outline
import androidx.compose.ui.graphics.Path
import androidx.compose.ui.graphics.Shape
import androidx.compose.ui.unit.Density
import androidx.compose.ui.unit.LayoutDirection

/**
 * 撕页效果顶部波浪 Shape
 *
 * 仅修改顶部边缘为波浪形，底部/左侧/右侧为直线。
 * 配合 Card 的 clip 使用，实现"撕日历"纸张顶部效果。
 *
 * @param waveHeightPx 波浪高度(像素, 默认 8f)
 * @param waveCount 波浪数量(默认 12)
 */
class WavyTopShape(
    private val waveHeightPx: Float = 8f,
    private val waveCount: Int = 12
) : Shape {
    override fun createOutline(
        size: Size,
        layoutDirection: LayoutDirection,
        density: Density
    ): Outline {
        val path = Path().apply {
            // 起点:左下
            moveTo(0f, size.height)
            // 底部直线:左下到右下
            lineTo(size.width, size.height)
            // 右侧直线:右下到右上
            lineTo(size.width, 0f)
            // 顶部波浪:从右上到左上
            val waveWidth = size.width / waveCount
            for (i in 0 until waveCount) {
                val endX = size.width - (i + 1) * waveWidth
                val startX = size.width - i * waveWidth
                val midX = (startX + endX) / 2
                quadraticTo(
                    midX, waveHeightPx,
                    endX, 0f
                )
            }
            // 左侧直线:左上到左下
            lineTo(0f, size.height)
            close()
        }
        return Outline.Generic(path)
    }
}
```

---

### Task 4: 橙色撕页卡片比例化改造 + 底部点点装饰

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/components/cardstyle/OrangeTearOffCard.kt`

**目标：**
1. 添加 `aspectRatio(4f/5f)` 固定宽高比
2. 使用 `BoxWithConstraints` 按比例分配所有元素尺寸
3. 添加底部点点装饰
4. 完整版使用比例，缩略图版保持原固定值

- [ ] **Step 1: 添加 import**

添加：
```kotlin
import androidx.compose.foundation.layout.BoxWithConstraints
import androidx.compose.foundation.layout.size
import androidx.compose.ui.unit.Dp
import kotlin.math.roundToInt
```

- [ ] **Step 2: 修改函数体，使用 BoxWithConstraints 包裹**

将原有的 `Box(modifier = modifier...)` 改为：
- 最外层加 `aspectRatio(4f / 5f)`（仅完整版，缩略图版不加）
- 内部用 `BoxWithConstraints` 获取 maxHeight

完整结构：
```kotlin
@Composable
fun OrangeTearOffCard(
    title: String,
    targetDateMillis: Long,
    modifier: Modifier = Modifier,
    isThumbnail: Boolean = false,
    cardColor: DateCardColor = DateCardColor.DEFAULT,
    onShareClick: (() -> Unit)? = null,
    onCalendarClick: (() -> Unit)? = null,
    onCardClick: (() -> Unit)? = null
) {
    val displayTitle = title.ifBlank { "未命名" }

    // 颜色源
    val topColor = topBarColor(cardColor)
    val bgColor = backgroundColor(DateCardColor.DEFAULT, DateCardStyle.OrangeTearOff)
    val numberColor = bigNumberColor(cardColor, DateCardStyle.OrangeTearOff)
    val titleColor = topBarColor(cardColor)

    // 完整版:aspectRatio(4:5) + BoxWithConstraints 比例尺寸
    // 缩略图版:保持原固定尺寸
    if (isThumbnail) {
        OrangeTearOffCardThumbnail(
            displayTitle = displayTitle,
            targetDateMillis = targetDateMillis,
            modifier = modifier,
            cardColor = cardColor,
            bgColor = bgColor,
            topColor = topColor,
            numberColor = numberColor,
            titleColor = titleColor,
            onCardClick = onCardClick
        )
    } else {
        Box(
            modifier = modifier
                .aspectRatio(4f / 5f)
        ) {
            OrangeTearOffCardFull(
                displayTitle = displayTitle,
                targetDateMillis = targetDateMillis,
                cardColor = cardColor,
                bgColor = bgColor,
                topColor = topColor,
                numberColor = numberColor,
                titleColor = titleColor,
                onShareClick = onShareClick,
                onCalendarClick = onCalendarClick,
                onCardClick = onCardClick
            )
        }
    }
}
```

- [ ] **Step 3: 抽取完整版函数 OrangeTearOffCardFull**

使用 `BoxWithConstraints` 按比例计算所有尺寸：

```kotlin
@Composable
private fun OrangeTearOffCardFull(
    displayTitle: String,
    targetDateMillis: Long,
    cardColor: DateCardColor,
    bgColor: Color,
    topColor: Color,
    numberColor: Color,
    titleColor: Color,
    onShareClick: (() -> Unit)?,
    onCalendarClick: (() -> Unit)?,
    onCardClick: (() -> Unit)?
) {
    BoxWithConstraints {
        val cardHeight = maxHeight
        val topSectionHeight = cardHeight * 0.20f
        val waveHeightPx = (cardHeight * 0.03f).value.roundToInt().toFloat()
        val holeSize = topSectionHeight * 0.27f
        val holeHorizontalPadding = maxWidth * 0.10f
        val distanceFontSize = (cardHeight * 0.04f).value.sp
        val numberFontSize = (cardHeight * 0.30f).value.sp
        val titleFontSize = (cardHeight * 0.05f).value.sp
        val shadowElevation = 4.dp
        val cornerRadius = 20.dp
        val iconSize = cardHeight * 0.05f
        val dotSize = cardHeight * 0.015f

        Box(
            modifier = Modifier
                .fillMaxSize()
                .shadow(shadowElevation, RoundedCornerShape(cornerRadius))
                .clip(WavyBottomShape(waveHeightPx = waveHeightPx))
                .background(bgColor)
                .then(if (onCardClick != null) Modifier.clickable { onCardClick() } else Modifier)
        ) {
            Column(modifier = Modifier.fillMaxSize()) {
                // 顶部撕页条
                Box(
                    modifier = Modifier
                        .fillMaxWidth()
                        .height(topSectionHeight)
                        .background(topColor)
                ) {
                    Box(
                        modifier = Modifier
                            .align(Alignment.CenterStart)
                            .padding(start = holeHorizontalPadding)
                            .size(holeSize)
                            .background(Color.White, CircleShape)
                    )
                    Box(
                        modifier = Modifier
                            .align(Alignment.CenterEnd)
                            .padding(end = holeHorizontalPadding)
                            .size(holeSize)
                            .background(Color.White, CircleShape)
                    )
                }
                // 主体内容
                Column(
                    modifier = Modifier
                        .fillMaxSize()
                        .padding(horizontal = maxWidth * 0.08f, vertical = cardHeight * 0.04f),
                    horizontalAlignment = Alignment.CenterHorizontally,
                    verticalArrangement = Arrangement.SpaceBetween
                ) {
                    Spacer(Modifier.height(cardHeight * 0.02f))
                    // 距离文字
                    Text(
                        text = formatDistanceTextWithWeekday(targetDateMillis),
                        fontSize = distanceFontSize,
                        color = Color(0xFF999999)
                    )
                    // 大数字
                    Text(
                        text = daysUntil(targetDateMillis).toString(),
                        fontSize = numberFontSize,
                        fontWeight = FontWeight.Bold,
                        color = numberColor
                    )
                    // 标题
                    Text(
                        text = displayTitle,
                        fontSize = titleFontSize,
                        fontWeight = FontWeight.Medium,
                        color = titleColor,
                        textAlign = TextAlign.Center,
                        maxLines = 1
                    )
                    // 底部点点装饰 (左侧)
                    Row(
                        modifier = Modifier.fillMaxWidth(),
                        horizontalArrangement = Arrangement.Start
                    ) {
                        repeat(6) { index ->
                            Box(
                                modifier = Modifier
                                    .size(dotSize)
                                    .background(
                                        if (index < 3) Color(0xFFCCCCCC) else Color(0xFFEEEEEE),
                                        CircleShape
                                    )
                            )
                            if (index < 5) {
                                Spacer(Modifier.size(dotSize * 0.8f))
                            }
                        }
                    }
                }
            }
            // 右下角操作图标
            Row(
                modifier = Modifier
                    .align(Alignment.BottomEnd)
                    .padding(end = maxWidth * 0.04f, bottom = cardHeight * 0.025f),
                horizontalArrangement = Arrangement.spacedBy(4.dp)
            ) {
                Icon(
                    imageVector = Icons.Filled.Share,
                    contentDescription = "分享",
                    modifier = Modifier
                        .size(iconSize)
                        .clickable { onShareClick?.invoke() },
                    tint = Color(0xFF999999)
                )
                Icon(
                    imageVector = Icons.Filled.CalendarMonth,
                    contentDescription = "添加到日历",
                    modifier = Modifier
                        .size(iconSize)
                        .clickable { onCalendarClick?.invoke() },
                    tint = Color(0xFF999999)
                )
            }
        }
    }
}
```

- [ ] **Step 4: 抽取缩略图版函数 OrangeTearOffCardThumbnail**

保持原有固定值逻辑，确保缩略图显示正常。代码与原有实现类似，但封装为独立函数。

---

### Task 5: 日历撕页卡片比例化改造 + 顶部波浪 + 月历下方虚线

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/date/components/cardstyle/CalendarTearOffCard.kt`

**目标：**
1. 添加 `aspectRatio(1f/2f)` 固定宽高比
2. 使用 `BoxWithConstraints` 按比例分配所有元素尺寸
3. 添加顶部波浪 `WavyTopShape`
4. 添加月历下方主分割虚线
5. 完整版使用比例，缩略图版保持原固定值

- [ ] **Step 1: 添加 import**

添加：
```kotlin
import androidx.compose.foundation.Canvas
import androidx.compose.foundation.layout.BoxWithConstraints
import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.graphics.PathEffect
import kotlin.math.roundToInt
```

- [ ] **Step 2: 主函数结构改造**

与橙色撕页类似，分为完整版和缩略图版：
- 完整版：`aspectRatio(1f / 2f)` + `BoxWithConstraints` + 顶部波浪 + 底部波浪
- 缩略图版：保持原固定尺寸

- [ ] **Step 3: 完整版函数 CalendarTearOffCardFull**

关键比例参数：
- 顶部波浪：3%
- 顶部标题区：7%
- 月历区：50%
- 标题区：7%
- 大数字区：20%
- 距离文字区：7%
- 底部波浪：3%
- 间距缓冲：3%

顶部使用 `WavyTopShape` clip，内容整体向下偏移一个波浪高度（`padding(top = waveHeightDp)`）以避免顶部内容被波浪裁剪。

月历下方主分割虚线：月历区域结束后，添加一条 0.5dp 高的虚线。

- [ ] **Step 4: 缩略图版函数 CalendarTearOffCardThumbnail**

保持原有固定值逻辑，封装为独立函数。

---

## 自测清单

- [ ] 橙色撕页卡片宽高比 4:5，内部元素比例正确
- [ ] 日历撕页卡片宽高比 1:2，内部元素比例正确
- [ ] 日历撕页顶部有波浪效果
- [ ] MiniCalendar 表头下方和行之间有虚线
- [ ] 日历撕页月历下方有主分割虚线
- [ ] 橙色撕页底部左侧有点点装饰
- [ ] 状态栏图标始终为深色
- [ ] 缩略图版显示正常，尺寸比例与之前一致
- [ ] 颜色功能（cardColor）在比例化后仍然正常工作
- [ ] import 无缺失，无编译错误
