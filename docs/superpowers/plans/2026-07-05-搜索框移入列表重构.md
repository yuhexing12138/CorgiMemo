# 搜索框移入列表重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将搜索框从 LazyColumn 外的折叠头部重构为 LazyColumn 首项，下拉刷新柯基作为 spacer item 保持原位（搜索框与卡片之间），彻底消除布局反馈循环与 isScrolling 切换跳跃。

**Architecture:** 给 `ZonedReorderableLazyColumn` 新增 `headerContent: LazyListScope.() -> Unit` + `headerItemCount: Int` 参数；HomeScreen 将 SearchBar 与 PullRefreshSpacer 注入 headerContent；删除 `scrollDrivenProgress`/`animatableProgress`/`currentProgress`/`computeScrollDrivenProgress` 等中转状态与 `translationY` 外层偏移。

**Tech Stack:** Kotlin、Jetpack Compose（foundation.lazy、runtime、animation.core、ui.platform）、JUnit 4

**相关 Spec:** [2026-07-05-搜索框移入列表重构-design.md](file:///c:/Users/EDY/Desktop/CorgiMemo/docs/superpowers/specs/2026-07-05-搜索框移入列表重构-design.md)

---

## File Structure

| 文件 | 责任 | 操作 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt` | 可拖拽排序 LazyColumn | 新增 `headerContent` + `headerItemCount` 参数；`onMove` 索引偏移 |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 待办页 | 删除折叠头部架构；新增 `PullRefreshSpacer` 组件；注入 `headerContent` |
| `app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt` | `computeScrollDrivenProgress` 单元测试 | 删除整个文件 |

---

### Task 1: ZonedReorderableLazyColumn 新增 headerContent + headerItemCount 参数

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt`

- [ ] **Step 1: 修改函数签名，新增 headerContent + headerItemCount 参数**

定位到 `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt` 第 50-63 行的 `ZonedReorderableLazyColumn` 函数签名。

将以下旧代码：

```kotlin
@Composable
fun ZonedReorderableLazyColumn(
    items: List<DisplayItem>,
    isDragEnabled: Boolean,
    key: (DisplayItem) -> Any,
    onReorder: (
        dragResult: ZoneDragResult,
        draggedItem: DisplayItem.Todo,
        targetZoneRelativeIndex: Int
    ) -> Unit,
    listState: LazyListState = rememberLazyListState(),
    modifier: Modifier = Modifier,
    content: @Composable (index: Int, item: DisplayItem, isDragging: Boolean, isDragActive: Boolean) -> Unit
) {
```

替换为：

```kotlin
@Composable
fun ZonedReorderableLazyColumn(
    items: List<DisplayItem>,
    isDragEnabled: Boolean,
    key: (DisplayItem) -> Any,
    onReorder: (
        dragResult: ZoneDragResult,
        draggedItem: DisplayItem.Todo,
        targetZoneRelativeIndex: Int
    ) -> Unit,
    listState: LazyListState = rememberLazyListState(),
    modifier: Modifier = Modifier,
    /** 列表前置项内容（搜索框、下拉刷新 spacer 等），不参与拖拽排序 */
    headerContent: LazyListScope.() -> Unit = {},
    /** 前置项数量（用于 onMove 索引偏移，全局索引 → displayItems 索引） */
    headerItemCount: Int = 0,
    content: @Composable (index: Int, item: DisplayItem, isDragging: Boolean, isDragActive: Boolean) -> Unit
) {
```

- [ ] **Step 2: 在 LazyColumn 渲染中调用 headerContent**

定位到第 124-128 行的 `LazyColumn` 渲染块。

将以下旧代码：

```kotlin
    // ━━━ 渲染 ━━━
    LazyColumn(state = listState, modifier = modifier) {
        itemsIndexed(
            items = displayItems,
            key = { _, it -> key(it) }
        ) { index, item ->
```

替换为：

```kotlin
    // ━━━ 渲染 ━━━
    LazyColumn(state = listState, modifier = modifier) {
        // 前置项（搜索框、下拉刷新 spacer 等），不参与拖拽排序
        headerContent()
        itemsIndexed(
            items = displayItems,
            key = { _, it -> key(it) }
        ) { index, item ->
```

- [ ] **Step 3: 修改 onMove 索引偏移**

定位到第 88-100 行的 `onMove` lambda。

将以下旧代码：

```kotlin
        onMove = { from, to ->
            val fromItem = displayItems.getOrNull(from.index)
            // ① 仅校验 from 是 Todo（divider 不可拖拽，由 Modifier.draggable(enabled=false) 保证）
            //    to 可以是 Todo 或 divider（divider 已加入 reorderableKeys，可作为目标）
            if (fromItem !is DisplayItem.Todo) {
                return@rememberReorderableLazyListState
            }

            // ② 重排 displayItems（A 跨过 divider / Todo，divider 不动）
            val newDisplay = displayItems.toMutableList()
            newDisplay.removeAt(from.index)
            newDisplay.add(to.index, fromItem)
            displayItems = newDisplay
```

替换为：

```kotlin
        onMove = { from, to ->
            // 全局索引（含 header）→ displayItems 索引（不含 header）
            val fromIdx = from.index - headerItemCount
            val toIdx = to.index - headerItemCount
            val fromItem = displayItems.getOrNull(fromIdx)
            // ① 仅校验 from 是 Todo（divider 不可拖拽，由 Modifier.draggable(enabled=false) 保证）
            //    to 可以是 Todo 或 divider（divider 已加入 reorderableKeys，可作为目标）
            if (fromItem !is DisplayItem.Todo) {
                return@rememberReorderableLazyListState
            }

            // ② 重排 displayItems（A 跨过 divider / Todo，divider 不动）
            val newDisplay = displayItems.toMutableList()
            newDisplay.removeAt(fromIdx)
            newDisplay.add(toIdx, fromItem)
            displayItems = newDisplay
```

- [ ] **Step 4: 修改 onMove 中 newDraggedIdx 计算**

定位到第 102-106 行的 `newDraggedIdx` 计算。

将以下旧代码：

```kotlin
            // ③ 基于 newDisplay 推断 currentZone（含 divider，准确识别 zone 边界）
            val newDraggedIdx = newDisplay.indexOfFirst {
                (it as? DisplayItem.Todo)?.item?.id == fromItem.item.id
            }
```

替换为：

```kotlin
            // ③ 基于 newDisplay 推断 currentZone（含 divider，准确识别 zone 边界）
            //    newDisplay 不含 header，newDraggedIdx 已是 displayItems 内索引
            val newDraggedIdx = newDisplay.indexOfFirst {
                (it as? DisplayItem.Todo)?.item?.id == fromItem.item.id
            }
```

（注：此步仅更新注释，代码不变。`indexOfFirst` 基于 item.id 匹配，与 header 偏移无关。）

- [ ] **Step 5: 检查 import 语句完整性**

`LazyListScope` 是 `androidx.compose.foundation.lazy.LazyListScope`，已被 `LazyColumn` 隐式使用，无需新增 import。确认文件顶部 import 无需改动。

- [ ] **Step 6: 编译验证**

Run:
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; .\gradlew.bat :app:compileDebugKotlin --console=plain
```

Expected: BUILD SUCCESSFUL（向后兼容，默认参数不影响现有调用）

- [ ] **Step 7: 不提交，进入下一个 Task**

---

### Task 2: HomeScreen 新增 PullRefreshSpacer 私有组件

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`（在 `computeScrollDrivenProgress` 函数之前插入）

- [ ] **Step 1: 在 HomeScreen.kt 中新增 PullRefreshSpacer 组件**

定位到 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 第 3157 行附近（`computeScrollDrivenProgress` 函数的注释上方）。

在第 3156 行（`}` 闭合 `shareTodoAsImage` 函数后）与第 3157 行（`/**` 注释开始）之间插入以下代码：

```kotlin

/**
 * 下拉刷新空白区 + 柯基动画
 *
 * 作为 LazyColumn 的前置 item，高度随 pullOffset 增长，
 * 替代原外层 Box 的 translationY 偏移方案。
 *
 * @param pullOffset 当前下拉偏移量（px）
 * @param state 下拉刷新状态
 * @param maxPullHeightPx 最大下拉高度（px）
 * @param refreshThresholdPx 刷新触发阈值（px）
 * @param modifier Modifier
 */
@Composable
private fun PullRefreshSpacer(
    pullOffset: Float,
    state: PullRefreshState,
    maxPullHeightPx: Float,
    refreshThresholdPx: Float,
    modifier: Modifier = Modifier
) {
    // 高度 = pullOffset（px → dp），未下拉时高度为 0 不可见
    val heightDp = with(LocalDensity.current) { pullOffset.toDp() }
    Box(
        modifier = modifier
            .height(heightDp)
            .clipToBounds()
    ) {
        CorgiPullRefreshIndicator(
            pullOffset = pullOffset,
            state = state,
            maxPullHeightPx = maxPullHeightPx,
            refreshThresholdPx = refreshThresholdPx,
            modifier = Modifier.fillMaxWidth()
        )
    }
}
```

- [ ] **Step 2: 检查 import 语句完整性**

确认以下 import 已存在（文件顶部）：
- `androidx.compose.ui.platform.LocalDensity` — 已存在（L144）
- `com.corgimemo.app.ui.components.CorgiPullRefreshIndicator` — 已存在（L126）
- `com.corgimemo.app.ui.components.PullRefreshState` — 已存在（L127）

新增以下 import（若不存在）：
- `androidx.compose.foundation.layout.height`
- `androidx.compose.ui.draw.clipToBounds`

使用 Grep 确认：
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; Select-String -Path "app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt" -Pattern "import androidx.compose.foundation.layout.height|import androidx.compose.ui.draw.clipToBounds"
```

若缺失，在文件顶部 import 区添加：
```kotlin
import androidx.compose.foundation.layout.height
import androidx.compose.ui.draw.clipToBounds
```

- [ ] **Step 3: 编译验证**

Run:
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; .\gradlew.bat :app:compileDebugKotlin --console=plain
```

Expected: BUILD SUCCESSFUL（新增私有函数，不影响现有代码）

- [ ] **Step 4: 不提交，进入下一个 Task**

---

### Task 3: HomeScreen 重构 — 删除旧折叠头部 + 注入 headerContent

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`（多处编辑）

**说明：** 本 Task 是核心重构，包含多个编辑步骤。删除旧折叠头部状态与 SearchBar 外层 Box，将 SearchBar 与 PullRefreshSpacer 注入 `ZonedReorderableLazyColumn` 的 `headerContent`。

- [ ] **Step 1: 删除 isScrolling 与 searchBarFullHeightPx 状态**

定位到第 532-536 行。

将以下旧代码：

```kotlin
                    /** 订阅 LazyListState 滚动状态（含 fling + settle），Compose 官方统一管理，跨设备一致 */
                    val isScrolling = lazyListState.isScrollInProgress

                    /** 搜索框完整高度（含padding），作为滚动→进度映射的基准 */
                    val searchBarFullHeightPx = with(LocalDensity.current) { 64.dp.toPx() }
```

替换为空（直接删除这两段注释和代码）。

- [ ] **Step 2: 删除 scrollDrivenProgress derivedStateOf**

定位到第 538-556 行。

将以下旧代码：

```kotlin
                    /**
                     * 阶段 1：滚动中同步跟手（无协程延迟）
                     *
                     * 直接基于 LazyListState 官方 API 派生 progress，1:1 跟随滚动 delta。
                     * - firstVisibleItemIndex == 0：progress = 1 - offset / searchBarHeight
                     * - firstVisibleItemIndex > 0：强制 0f（第一项已完全滚出）
                     *
                     * 替换原 Animatable + LaunchedEffect + snapTo 异步链路，
                     * 消除每帧 effect 重启 + 协程调度引入的 1-3 帧延迟（跳跃感根因）。
                     */
                    val scrollDrivenProgress by remember {
                        derivedStateOf {
                            computeScrollDrivenProgress(
                                firstVisibleItemIndex = lazyListState.firstVisibleItemIndex,
                                firstVisibleItemScrollOffset = lazyListState.firstVisibleItemScrollOffset,
                                searchBarHeightPx = searchBarFullHeightPx
                            )
                        }
                    }
```

替换为空（直接删除）。

- [ ] **Step 3: 删除 animatableProgress、currentProgress、LaunchedEffect(isScrolling)、isAtTop、LaunchedEffect(searchQuery)**

定位到第 558-645 行。

将以下旧代码（从 `/**\n * 阶段 2：滚动停止时的吸附动画` 到 `LaunchedEffect(searchQuery) { ... }` 的闭合 `}`）全部删除：

```kotlin
                    /**
                     * 阶段 2：滚动停止时的吸附动画
                     *
                     * 仅在 isScrollInProgress == false 时由 currentProgress 读取。
                     * 滚动中此 Animatable 不参与计算，被 scrollDrivenProgress 覆盖。
                     */
                    val animatableProgress = remember { Animatable(1f) }

                    /**
                     * 当前生效的 progress（二选一）
                     *
                     * - 滚动中：scrollDrivenProgress（同步跟手）
                     * - 滚动停止：animatableProgress.value（吸附动画结果）
                     *
                     * layout/graphicsLayer 应读取本字段，不要直接读 Animatable.value。
                     */
                    val currentProgress = if (isScrolling) scrollDrivenProgress else animatableProgress.value

                    /**
                     * 滚动停止时：同步对齐起点 → animateTo 吸附端点
                     *
                     * - snapTo(scrollDrivenProgress) 同步对齐，消除阶段切换时的数值跳跃
                     * - animateTo(0f 或 1f) 用 tween 动画吸附到端点
                     *
                     * 阈值：< 0.5 吸附到 0f（隐藏），>= 0.5 吸附到 1f（显示）
                     * 仅在 searchQuery 为空时执行（搜索词非空时强制保持显示）
                     */
                    LaunchedEffect(isScrolling) {
                        if (!isScrolling && searchQuery.isBlank()) {
                            val start = scrollDrivenProgress
                            if (start > 0.05f && start < 0.95f) {
                                val target = if (start < 0.5f) 0f else 1f
                                val duration = if (target == 0f) 200 else 250
                                animatableProgress.snapTo(start)
                                animatableProgress.animateTo(
                                    targetValue = target,
                                    animationSpec = tween(duration, easing = FastOutSlowInEasing)
                                )
                            }
                        }
                    }

                    val isAtTop by remember {
                        derivedStateOf {
                            lazyListState.firstVisibleItemIndex == 0 &&
                                lazyListState.firstVisibleItemScrollOffset == 0
                        }
                    }

                    /**
                     * 搜索词变化时驱动搜索框：
                     * - 输入搜索词（blank → 非 blank）→ 立即显示（250ms 展开）
                     * - 清空搜索词（非 blank → blank）→ 基于当前 progress 主动 snap 到端点
                     *
                     * 原实现使用 isAtTop 判断"清空时是否隐藏"，但 isAtTop 与搜索框实际
                     * 状态（progress）可能不一致（如滚动到中部时 progress=0.3 但 isAtTop=false）。
                     * 改为基于 progress 判定，逻辑统一且与滚动停止检测一致。
                     */
                    /**
                     * 搜索词变化时驱动搜索框：
                     * - 输入搜索词（blank → 非 blank）→ 立即显示（250ms 展开）
                     * - 清空搜索词（非 blank → blank）→ 基于当前 progress 主动 snap 到端点
                     *
                     * 注意：animatableProgress.snapTo 用于同步对齐起点，避免从旧值动画到目标值产生跳跃
                     */
                    LaunchedEffect(searchQuery) {
                        if (searchQuery.isNotBlank()) {
                            // 输入搜索词：立即显示
                            val start = if (isScrolling) scrollDrivenProgress else animatableProgress.value
                            animatableProgress.snapTo(start)
                            animatableProgress.animateTo(
                                targetValue = 1f,
                                animationSpec = tween(250, easing = FastOutSlowInEasing)
                            )
                        } else {
                            // 清空搜索词：基于当前 progress 主动 snap 到端点
                            val current = if (isScrolling) scrollDrivenProgress else animatableProgress.value
                            if (current > 0.05f && current < 0.95f) {
                                val target = if (current < 0.5f) 0f else 1f
                                val duration = if (target == 0f) 200 else 250
                                animatableProgress.snapTo(current)
                                animatableProgress.animateTo(
                                    targetValue = target,
                                    animationSpec = tween(duration, easing = FastOutSlowInEasing)
                                )
                            }
                        }
                    }
```

替换为空（直接删除）。

- [ ] **Step 4: 删除搜索框外层 Box（含 layout/clipToBounds/graphicsLayer 修饰符）**

定位到第 647-680 行。

将以下旧代码：

```kotlin
                    /**
                     * 搜索框容器：
                     * - 上边缘固定，底部边缘根据 currentProgress 收缩/展开
                     * - 透明度与进度同步变化，实现淡入淡出
                     * - clipToBounds 裁剪超出部分
                     */
                    Box(
                        modifier = Modifier
                            .fillMaxWidth()
                            .layout { measurable, constraints ->
                                val placeable = measurable.measure(constraints)
                                val targetHeight = (placeable.height * currentProgress).roundToInt()
                                layout(placeable.width, targetHeight) {
                                    // place(0, 0) 保证上边缘固定
                                    placeable.place(0, 0)
                                }
                            }
                            .clipToBounds()
                            .graphicsLayer { alpha = currentProgress }
                    ) {
                        SearchBar(
                            query = searchQuery,
                            onQueryChange = { newQuery ->
                                viewModel.updateSearchQuery(newQuery)
                            },
                            onClear = {
                                viewModel.clearSearch()
                            },
                            modifier = Modifier
                                .fillMaxWidth()
                                .padding(horizontal = 20.dp)
                                .padding(bottom = dimensionResource(com.corgimemo.app.R.dimen.ui_search_bar_bottom_margin))
                        )
                    }
```

替换为空（直接删除，SearchBar 将在 Step 8 注入 headerContent）。

- [ ] **Step 5: 删除外层 Box 中的 CorgiPullRefreshIndicator overlay 与内层 Box（含 translationY）**

定位到第 802-823 行。

将以下旧代码：

```kotlin
                        // 外层 Box：承载 nestedScrollConnection 与柯基指示器
                        Box(
                            modifier = Modifier
                                .fillMaxSize()
                                .nestedScroll(pullRefreshState.nestedScrollConnection)
                        ) {
                            // 空白区 + 居中奔跑柯基（铺满宽度，高度=pullOffset）
                            CorgiPullRefreshIndicator(
                                pullOffset = pullRefreshState.pullOffset,
                                state = pullRefreshState.state,
                                maxPullHeightPx = pullRefreshState.maxPullHeightPx,
                                refreshThresholdPx = pullRefreshState.refreshThresholdPx,
                                modifier = Modifier.fillMaxWidth()
                            )

                            // 内层 Box：列表 + 搜索框整体下移 pullOffset
                            Box(
                                modifier = Modifier
                                    .fillMaxSize()
                                    .graphicsLayer { translationY = pullRefreshState.pullOffset }
                            ) {
                            ZonedReorderableLazyColumn(
```

替换为：

```kotlin
                        // 外层 Box：仅承载 nestedScrollConnection
                        // 柯基动画已移入 LazyColumn 的 headerContent（PullRefreshSpacer）
                        Box(
                            modifier = Modifier
                                .fillMaxSize()
                                .nestedScroll(pullRefreshState.nestedScrollConnection)
                        ) {
                            ZonedReorderableLazyColumn(
```

- [ ] **Step 6: 删除 ZonedReorderableLazyColumn 调用末尾的多余闭合括号**

由于 Step 5 删除了内层 Box，需要同步删除其闭合 `}`。

定位到第 968 行附近（原 `} // 闭合内层 Box（列表 + 搜索框整体下移 pullOffset）`）。

将以下旧代码：

```kotlin
                            }
                            } // 闭合内层 Box（列表 + 搜索框整体下移 pullOffset）
                        }
```

替换为：

```kotlin
                            }
                        }
```

（注：具体缩进与行号可能因前序编辑而变化，请以"闭合内层 Box"注释为定位锚点，删除该注释行及其对应的 `}`）

- [ ] **Step 7: 删除 ZonedReorderableLazyColumn modifier 中的级联偏移 graphicsLayer**

定位到第 845-851 行（`modifier = Modifier` 参数）。

将以下旧代码：

```kotlin
                                modifier = Modifier
                                    .fillMaxSize()
                                    .padding(horizontal = 8.dp)
                                    /** 级联效果：列表跟随搜索框显隐产生视差偏移 */
                                    .graphicsLayer {
                                        translationY = -(1f - currentProgress) * searchBarFullHeightPx * 0.12f
                                    }
```

替换为：

```kotlin
                                modifier = Modifier
                                    .fillMaxSize()
                                    .padding(horizontal = 8.dp)
```

- [ ] **Step 8: 在 ZonedReorderableLazyColumn 调用中注入 headerContent + headerItemCount**

定位到 `ZonedReorderableLazyColumn(` 调用处（Step 5 替换后的位置）。

在 `onReorder = { ... }` 参数之后、`modifier = Modifier` 参数之前，插入 `headerContent` 与 `headerItemCount` 参数。

将以下旧代码（onReorder 参数末尾 + modifier 参数开头）：

```kotlin
                                onReorder = { dragResult, draggedTodoItem, targetZoneRelativeIndex ->
                                    // 纯转发：组件已基于内部 displayItems 计算好被拖项和相对索引
                                    viewModel.reorderOnDragResult(
                                        draggedItemId = draggedTodoItem.item.id,
                                        draggedTodo = draggedTodoItem.item,
                                        dragResult = dragResult,
                                        targetZoneRelativeIndex = targetZoneRelativeIndex
                                    )
                                },
                                modifier = Modifier
                                    .fillMaxSize()
                                    .padding(horizontal = 8.dp)
```

替换为：

```kotlin
                                onReorder = { dragResult, draggedTodoItem, targetZoneRelativeIndex ->
                                    // 纯转发：组件已基于内部 displayItems 计算好被拖项和相对索引
                                    viewModel.reorderOnDragResult(
                                        draggedItemId = draggedTodoItem.item.id,
                                        draggedTodo = draggedTodoItem.item,
                                        dragResult = dragResult,
                                        targetZoneRelativeIndex = targetZoneRelativeIndex
                                    )
                                },
                                headerContent = {
                                    // 第 0 项：搜索框（随滚动自然出屏）
                                    item {
                                        SearchBar(
                                            query = searchQuery,
                                            onQueryChange = { newQuery ->
                                                viewModel.updateSearchQuery(newQuery)
                                            },
                                            onClear = {
                                                viewModel.clearSearch()
                                            },
                                            modifier = Modifier
                                                .fillMaxWidth()
                                                .padding(horizontal = 20.dp)
                                                .padding(bottom = dimensionResource(com.corgimemo.app.R.dimen.ui_search_bar_bottom_margin))
                                        )
                                    }
                                    // 第 1 项：下拉刷新 spacer（高度 = pullOffset，含柯基动画）
                                    item {
                                        PullRefreshSpacer(
                                            pullOffset = pullRefreshState.pullOffset,
                                            state = pullRefreshState.state,
                                            maxPullHeightPx = pullRefreshState.maxPullHeightPx,
                                            refreshThresholdPx = pullRefreshState.refreshThresholdPx,
                                            modifier = Modifier.fillMaxWidth()
                                        )
                                    }
                                },
                                headerItemCount = 2,
                                modifier = Modifier
                                    .fillMaxSize()
                                    .padding(horizontal = 8.dp)
```

- [ ] **Step 9: 删除 computeScrollDrivenProgress 顶层函数**

定位到第 3157-3180 行（`computeScrollDrivenProgress` 函数及其注释）。

将以下旧代码（含上方注释）全部删除：

```kotlin
/**
 * 计算滚动驱动的搜索框显隐进度
 *
 * 规则:
 * - firstVisibleItemIndex == 0: progress = 1 - scrollOffset / searchBarHeight,clamp 到 [0, 1]
 * - firstVisibleItemIndex > 0: 强制返回 0f(第一项已完全滚出)
 *
 * @param firstVisibleItemIndex LazyListState.firstVisibleItemIndex
 * @param firstVisibleItemScrollOffset LazyListState.firstVisibleItemScrollOffset
 * @param searchBarHeightPx 搜索框完整高度(px)
 * @return 搜索框显隐进度 [0f, 1f],1f=完全显示,0f=完全隐藏
 */
internal fun computeScrollDrivenProgress(
    firstVisibleItemIndex: Int,
    firstVisibleItemScrollOffset: Int,
    searchBarHeightPx: Float
): Float {
    if (searchBarHeightPx <= 0f) return 1f
    return if (firstVisibleItemIndex == 0) {
        (1f - firstVisibleItemScrollOffset / searchBarHeightPx).coerceIn(0f, 1f)
    } else {
        0f
    }
}
```

替换为空（直接删除）。

- [ ] **Step 10: 检查 import 语句完整性**

删除以下不再使用的 import（若文件其他地方未使用）：

使用 Grep 检查每个 import 在文件中的使用次数：
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; Select-String -Path "app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt" -Pattern "Animatable|FastOutSlowInEasing|derivedStateOf|graphicsLayer|roundToInt" | Measure-Object
```

逐个检查：
- `androidx.compose.animation.core.Animatable` — 若无其他使用，删除
- `androidx.compose.animation.core.FastOutSlowInEasing` — 若无其他使用，删除
- `androidx.compose.animation.core.tween` — 若无其他使用，删除
- `androidx.compose.runtime.derivedStateOf` — 若无其他使用，删除
- `androidx.compose.ui.graphics.graphicsLayer` — 若无其他使用，删除
- `kotlin.math.roundToInt` — 若无其他使用，删除

**注意**：`tween` 可能在 `PullRefreshStateHolder.kt` 中使用，但那是另一个文件。在 HomeScreen.kt 中检查。若不确定，保留 import 不会报错，仅删除明确未使用的。

- [ ] **Step 11: 编译验证**

Run:
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; .\gradlew.bat :app:compileDebugKotlin --console=plain
```

Expected: BUILD SUCCESSFUL

若出现 `unresolved reference` 错误，检查是否遗漏删除了引用旧状态的代码（如 `currentProgress`、`scrollDrivenProgress`、`animatableProgress`、`isScrolling`、`searchBarFullHeightPx`）。

- [ ] **Step 12: 不提交，进入下一个 Task**

---

### Task 4: 删除 SearchRevealProgressTest.kt

**Files:**
- Delete: `app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt`

- [ ] **Step 1: 删除测试文件**

被测函数 `computeScrollDrivenProgress` 已在 Task 3 Step 9 删除，测试文件需同步删除。

使用 DeleteFile 工具删除：
```
app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt
```

- [ ] **Step 2: 编译验证**

Run:
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; .\gradlew.bat :app:compileDebugUnitTestKotlin --console=plain
```

Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 不提交，进入下一个 Task**

---

### Task 5: 全量测试 + Debug APK 编译

- [ ] **Step 1: 运行全量单元测试**

Run:
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; .\gradlew.bat :app:testDebugUnitTest --console=plain
```

Expected: BUILD SUCCESSFUL，所有测试通过（`HomeViewModelReorderTest` 等不受影响）

- [ ] **Step 2: 编译 Debug APK**

Run:
```bash
cd c:/Users/EDY/Desktop/CorgiMemo; .\gradlew.bat :app:assembleDebug --console=plain
```

Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 不提交，进入下一个 Task**

---

### Task 6: 真机手动验证

- [ ] **Step 1: 真机验证 10 个场景**

安装 Debug APK 后执行以下场景：

| # | 场景 | 预期 |
|---|------|------|
| 1 | 慢速上滑 | SearchBar 平滑滚出屏幕，无跳跃 |
| 2 | 快速上滑 fling | SearchBar 随 fling 滚出，停止后无回弹跳跃 |
| 3 | 慢速下滑 | SearchBar 平滑滚入屏幕，无跳跃 |
| 4 | 快速下滑 fling | SearchBar 随 fling 滚入，停止后无跳跃 |
| 5 | 顶部下拉刷新 | 柯基出现在 SearchBar 与卡片之间，高度随 pullOffset 增长 |
| 6 | 搜索框滚出后下拉 | 不触发刷新，列表正常滚动 |
| 7 | 搜索词输入后滚动 | SearchBar 滚出，过滤结果保持；滚回顶部可清空搜索词 |
| 8 | 拖拽排序 | SearchBar/Spacer 不被拖动，待办卡片正常排序 |
| 9 | 置顶按钮存在时 | PinnedDivider 作为 displayItems 首项，SearchBar 仍为 LazyColumn 第 0 项 |
| 10 | SolarTermCard 显示时 | 布局正常，SearchBar 仍可滚动 |

- [ ] **Step 2: 不提交，等待用户确认**

---

### Task 7: 询问用户是否进行 git 提交

按 workspace rule（`c:\Users\EDY\Desktop\CorgiMemo\.trae\rules\git提交.md`），任务完成后必须询问用户是否进行 git 提交。

- [ ] **Step 1: 询问用户是否进行 git 提交**

向用户呈现本次变更摘要：

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt` | 修改 | 新增 `headerContent` + `headerItemCount` 参数；`onMove` 索引偏移 |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 修改 | 删除折叠头部架构；新增 `PullRefreshSpacer` 组件；注入 `headerContent` |
| `app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt` | 删除 | `computeScrollDrivenProgress` 已删除 |

若用户选择提交：
- 智能生成中文提交信息
- 执行 `git add <specific files>` + `git commit -m "..."`

若用户选择不提交：跳过提交步骤。

---

## Self-Review

### Spec 覆盖检查

| Spec 要求 | 对应 Task |
|---|---|
| `ZonedReorderableLazyColumn` 新增 `headerContent` + `headerItemCount` 参数 | Task 1 Step 1 |
| `LazyColumn` 渲染中调用 `headerContent()` | Task 1 Step 2 |
| `onMove` 索引偏移 `headerItemCount` | Task 1 Step 3-4 |
| 新增 `PullRefreshSpacer` 组件 | Task 2 Step 1 |
| 删除 `isScrolling`/`searchBarFullHeightPx` | Task 3 Step 1 |
| 删除 `scrollDrivenProgress` | Task 3 Step 2 |
| 删除 `animatableProgress`/`currentProgress`/`LaunchedEffect`/`isAtTop` | Task 3 Step 3 |
| 删除搜索框外层 Box | Task 3 Step 4 |
| 删除 `CorgiPullRefreshIndicator` overlay + 内层 Box `translationY` | Task 3 Step 5-6 |
| 删除级联偏移 `graphicsLayer` | Task 3 Step 7 |
| 注入 `headerContent`（SearchBar + PullRefreshSpacer） | Task 3 Step 8 |
| 删除 `computeScrollDrivenProgress` 函数 | Task 3 Step 9 |
| import 清理 | Task 3 Step 10 |
| 删除 `SearchRevealProgressTest.kt` | Task 4 Step 1 |
| `./gradlew assembleDebug` 编译通过 | Task 5 Step 2 |
| `./gradlew test` 全部测试通过 | Task 5 Step 1 |
| 真机验证 10 个场景 | Task 6 Step 1 |
| git 提交询问 | Task 7 |

### Placeholder 扫描

无 TBD/TODO/placeholder。所有代码块完整。✓

### 类型一致性

- `headerContent: LazyListScope.() -> Unit`（Task 1 定义 → Task 3 使用）✓
- `headerItemCount: Int`（Task 1 定义 → Task 3 传入 `2`）✓
- `PullRefreshSpacer` 参数：`pullOffset: Float`、`state: PullRefreshState`、`maxPullHeightPx: Float`、`refreshThresholdPx: Float`（Task 2 定义 → Task 3 使用）✓
- `onMove` 中 `fromIdx`/`toIdx` 均为 `Int`（displayItems 索引）✓

### 后续可优化方向（来自 spec 第 8 节）

1. **搜索框滚出后快速回到顶部**：可添加"点击状态栏回到顶部"或"搜索按钮悬浮"入口
2. **stickyHeader 替代方案**：若后续需要搜索框始终可见，可改用 `stickyHeader` 而非 `item`
3. **PullRefreshSpacer 抽离为独立文件**：若其他页面也需下拉刷新，可将 `PullRefreshSpacer` 移到 `components/` 目录
