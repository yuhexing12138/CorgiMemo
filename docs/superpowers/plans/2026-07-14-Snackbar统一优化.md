# Snackbar 统一优化与灵感页撤销提示 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 AppSnackbarHost 中添加与底部导航栏的 16dp 间隙并降低高度；为灵感页删除操作添加带撤销按钮的 Snackbar 提示。

**Architecture:** 在 [AppSnackbarHost.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/AppSnackbarHost.kt) 容器上统一添加 `padding(bottom = 16.dp)` 和缩小的 vertical padding；在 [InspirationViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt) 仿照 [HomeViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt) 添加 `_pendingDeletedInspiration` 和 `_pendingBatchDeletedInspirations` StateFlow + 5s 倒计时 Job + undo 方法；在 [InspirationScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt) 新增 snackbarHostState 参数和 LaunchedEffect 监听。

**Tech Stack:** Jetpack Compose / Material 3 / Hilt / Coroutines / StateFlow

**关联文档**：[Snackbar统一优化 design](file:///c:/Users/EDY/Desktop/CorgiMemo/docs/superpowers/specs/2026-07-14-Snackbar统一优化-design.md)

---

## 文件结构

| 文件 | 角色 | 改动 |
|------|------|------|
| [AppSnackbarHost.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/AppSnackbarHost.kt) | 全项目统一的 Snackbar 容器 | 添加 16dp bottom padding + 缩小 vertical padding |
| [InspirationViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt) | 灵感页 ViewModel | 新增 2 个 StateFlow + 4 个方法 + 改造 2 个现有方法 |
| [InspirationScreen.kt](file:///c:/Users/EDY/Desktop\CorgiMemo\app\src\main\java\com\corgimemo\app\ui\screens\inspiration\InspirationScreen.kt) | 灵感页 Composable | 新增 snackbarHostState 参数 + 2 个 LaunchedEffect |
| [MainScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt) | 顶层 Scaffold | 传递 snackbarHostState 给 InspirationScreen |

---

### Task 1: AppSnackbarHost 添加 16dp 底部间隙

**Files:**
- Modify: [AppSnackbarHost.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/AppSnackbarHost.kt):57-60

- [ ] **Step 1: 修改外层 Box 的 modifier，添加 16dp bottom padding**

将第 57-60 行的：
```kotlin
Box(
    modifier = modifier.fillMaxWidth(),
    contentAlignment = Alignment.Center
) {
```

替换为：
```kotlin
Box(
    modifier = modifier
        .fillMaxWidth()
        // 需求 5：与底部导航栏留 16dp 间隙（安卓标准）
        .padding(bottom = 16.dp),
    contentAlignment = Alignment.Center
) {
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/AppSnackbarHost.kt
git commit -m "feat(snackbar): 添加与底部导航栏的 16dp 间隙"
```

---

### Task 2: AppSnackbarHost 缩小垂直内边距（降低高度）

**Files:**
- Modify: [AppSnackbarHost.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/AppSnackbarHost.kt):72-77

- [ ] **Step 1: 修改 Row 的 modifier，将 vertical padding 从 8/6 改为 4/2**

将第 72-77 行的：
```kotlin
Row(
    modifier = Modifier.padding(
        horizontal = 16.dp,
        // 带按钮时垂直内边距稍大，让按钮文字与图标垂直居中更协调
        vertical = if (hasAction) 6.dp else 8.dp
    ),
    verticalAlignment = Alignment.CenterVertically,
    horizontalArrangement = Arrangement.Start
) {
```

替换为：
```kotlin
Row(
    modifier = Modifier.padding(
        horizontal = 16.dp,
        // 需求 6：减小垂直内边距，降低 Snackbar 整体高度
        vertical = if (hasAction) 2.dp else 4.dp
    ),
    verticalAlignment = Alignment.CenterVertically,
    horizontalArrangement = Arrangement.Start
) {
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/AppSnackbarHost.kt
git commit -m "feat(snackbar): 减小垂直内边距降低 Snackbar 高度"
```

---

### Task 3: InspirationViewModel 添加撤销状态 StateFlow

**Files:**
- Modify: [InspirationViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt)

- [ ] **Step 1: 在 InspirationViewModel 顶部 imports 后，找到适当位置添加新的 StateFlow 声明**

先查看文件，定位到 `private val _selectedInspirationIds` 附近（或者 ViewModel 字段声明区）。

在 `_selectedInspirationIds` 声明之后（或相近的私有字段区）添加：

```kotlin
// ========== 撤销删除相关 ==========

/** 待删除灵感的临时存储（用于显示 Snackbar 和撤销）*/
private val _pendingDeletedInspiration = MutableStateFlow<Inspiration?>(null)
val pendingDeletedInspiration: StateFlow<Inspiration?> = _pendingDeletedInspiration.asStateFlow()

/** 待批量删除的灵感列表临时存储（用于显示 Snackbar 和撤销）*/
private val _pendingBatchDeletedInspirations = MutableStateFlow<List<Inspiration>?>(null)
val pendingBatchDeletedInspirations: StateFlow<List<Inspiration>?> = _pendingBatchDeletedInspirations.asStateFlow()

/** 删除倒计时任务（可取消）*/
private var deleteInspirationTimerJob: Job? = null

/** 删除倒计时时长（5秒）*/
private val UNDO_DELETE_INSPIRATION_DELAY_MS = 5000L
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt
git commit -m "feat(inspiration): 添加删除撤销的 StateFlow 状态"
```

---

### Task 4: InspirationViewModel 改造 deleteInspiration 方法

**Files:**
- Modify: [InspirationViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt):477-487

- [ ] **Step 1: 修改 deleteInspiration 方法，添加撤销状态保存和倒计时**

将第 477-487 行（`fun deleteInspiration(id: Long)`）的现有实现替换为：

```kotlin
/**
 * 删除灵感（软删除 + 设置撤销状态）
 *
 * 流程：
 * 1. 查询灵感实体
 * 2. 移入回收站（软删除）
 * 3. 从灵感表删除
 * 4. 设置 _pendingDeletedInspiration 状态
 * 5. 启动 5s 倒计时，超时后清除状态（灵感已在回收站）
 *
 * 撤销：用户点 Snackbar "撤销" → undoDeleteInspiration() 从回收站移回灵感表
 */
fun deleteInspiration(id: Long) {
    viewModelScope.launch {
        runCatching {
            val inspiration = inspirationRepository.getInspirationById(id)
            if (inspiration != null) {
                // 1. 移入回收站
                deletedInspirationRepository.insertDeletedInspiration(inspiration)
                // 2. 从灵感表删除
                inspirationRepository.deleteById(id)
                // 3. 设置撤销状态
                _pendingDeletedInspiration.value = inspiration
                // 4. 取消之前的倒计时任务（如果有）
                deleteInspirationTimerJob?.cancel()
                // 5. 启动新的倒计时
                deleteInspirationTimerJob = launch {
                    delay(UNDO_DELETE_INSPIRATION_DELAY_MS)
                    _pendingDeletedInspiration.value = null
                }
            }
        }
    }
}
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt
git commit -m "feat(inspiration): deleteInspiration 改造为支持撤销"
```

---

### Task 5: InspirationViewModel 改造 batchDeleteInspirations 方法

**Files:**
- Modify: [InspirationViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt):562-585

- [ ] **Step 1: 修改 batchDeleteInspirations 方法，添加撤销状态保存和倒计时**

将 `batchDeleteInspirations()` 函数（第 562-585 行）替换为：

```kotlin
/**
 * 批量删除选中的灵感（软删除 + 设置撤销状态）
 *
 * 流程：
 * 1. 遍历选中 ID，逐个软删除到回收站
 * 2. 设置 _pendingBatchDeletedInspirations 状态
 * 3. 退出批量模式
 * 4. 启动 5s 倒计时
 *
 * 撤销：用户点 Snackbar "全部撤销" → undoBatchDeleteInspiration() 全部从回收站移回
 */
fun batchDeleteInspirations() {
    val selectedIds = _selectedInspirationIds.value
    if (selectedIds.isEmpty()) return
    viewModelScope.launch {
        val deletedList = mutableListOf<Inspiration>()
        selectedIds.forEach { id ->
            val inspiration = inspirationRepository.getInspirationById(id)
            if (inspiration != null) {
                deletedInspirationRepository.insertDeletedInspiration(inspiration)
                inspirationRepository.deleteById(id)
                deletedList.add(inspiration)
            }
        }
        // 退出批量模式
        exitBatchMode()
        // 设置批量撤销状态
        _pendingBatchDeletedInspirations.value = deletedList
        // 取消之前的倒计时任务（如果有）
        deleteInspirationTimerJob?.cancel()
        // 启动新的倒计时
        deleteInspirationTimerJob = launch {
            delay(UNDO_DELETE_INSPIRATION_DELAY_MS)
            _pendingBatchDeletedInspirations.value = null
        }
    }
}
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt
git commit -m "feat(inspiration): batchDeleteInspirations 改造为支持撤销"
```

---

### Task 6: InspirationViewModel 添加 undo 和 clear 方法

**Files:**
- Modify: [InspirationViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt)

- [ ] **Step 1: 在文件末尾添加 4 个新方法**

在 `batchDeleteInspirations` 函数定义之后，添加：

```kotlin
/**
 * 撤销单个灵感删除（从回收站移回灵感表）
 *
 * 调用时机：用户点击 Snackbar "撤销" 按钮
 * 关键：必须先从回收站永久删除，再重新插入灵感表（id 保持一致）
 */
fun undoDeleteInspiration() {
    viewModelScope.launch {
        val inspiration = _pendingDeletedInspiration.value ?: return@launch
        // 1. 取消倒计时任务
        deleteInspirationTimerJob?.cancel()
        deleteInspirationTimerJob = null
        // 2. 从回收站永久删除
        deletedInspirationRepository.permanentlyDelete(inspiration.id)
        // 3. 重新插入灵感表
        inspirationRepository.insert(inspiration)
        // 4. 清除状态
        _pendingDeletedInspiration.value = null
    }
}

/**
 * 撤销批量灵感删除
 *
 * 调用时机：用户点击 Snackbar "全部撤销" 按钮
 */
fun undoBatchDeleteInspiration() {
    viewModelScope.launch {
        val list = _pendingBatchDeletedInspirations.value ?: return@launch
        // 1. 取消倒计时任务
        deleteInspirationTimerJob?.cancel()
        deleteInspirationTimerJob = null
        // 2. 逐个从回收站移回灵感表
        list.forEach { inspiration ->
            deletedInspirationRepository.permanentlyDelete(inspiration.id)
            inspirationRepository.insert(inspiration)
        }
        // 3. 清除状态
        _pendingBatchDeletedInspirations.value = null
    }
}

/**
 * 清除单个待撤销状态
 *
 * 调用时机：Snackbar 自动消失（用户未点撤销按钮）
 */
fun clearPendingDeletedInspiration() {
    deleteInspirationTimerJob?.cancel()
    deleteInspirationTimerJob = null
    _pendingDeletedInspiration.value = null
}

/**
 * 清除批量待撤销状态
 *
 * 调用时机：Snackbar 自动消失（用户未点撤销按钮）
 */
fun clearPendingBatchDeletedInspiration() {
    deleteInspirationTimerJob?.cancel()
    deleteInspirationTimerJob = null
    _pendingBatchDeletedInspirations.value = null
}
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt
git commit -m "feat(inspiration): 添加 undo/clear 删除撤销方法"
```

---

### Task 7: InspirationScreen 添加 snackbarHostState 参数

**Files:**
- Modify: [InspirationScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt):80-92

- [ ] **Step 1: 修改 InspirationScreen 函数签名，添加 snackbarHostState 参数**

将第 80-85 行的：
```kotlin
fun InspirationScreen(
    navController: NavController,
    onFabClick: () -> Unit = {},
    viewModel: InspirationViewModel = hiltViewModel()
) {
```

替换为：
```kotlin
fun InspirationScreen(
    navController: NavController,
    onFabClick: () -> Unit = {},
    viewModel: InspirationViewModel = hiltViewModel(),
    /**
     * 共享的 Snackbar 状态（由 MainScreen 顶层 Scaffold 创建并传入）
     *
     * 与 HomeScreen / SpecialDateScreen 一致，避免在每个页面单独创建 SnackbarHostState
     */
    snackbarHostState: SnackbarHostState? = null
) {
```

- [ ] **Step 2: 添加必要的 imports**

检查文件顶部 imports（已有 SnackbarHostState 和 SnackbarDuration）。如果没有，在 imports 区域添加：

```kotlin
import androidx.compose.material3.SnackbarHostState
import androidx.compose.material3.SnackbarDuration
import androidx.compose.material3.SnackbarResult
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt
git commit -m "feat(inspiration): InspirationScreen 添加 snackbarHostState 参数"
```

---

### Task 8: InspirationScreen 添加 LaunchedEffect 监听删除事件

**Files:**
- Modify: [InspirationScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt):86-92

- [ ] **Step 1: 在 state 收集区添加新 state 收集**

在第 86-91 行附近的 state 收集区域（在 `val hideDetails` 之后），添加：

```kotlin
/** 灵感删除撤销状态（用于触发 Snackbar）*/
val pendingDeletedInspiration by viewModel.pendingDeletedInspiration.collectAsState()
val pendingBatchDeletedInspirations by viewModel.pendingBatchDeletedInspirations.collectAsState()
```

- [ ] **Step 2: 在 InspirationScreen 函数体内（与现有 LaunchedEffect 同级），添加 2 个 LaunchedEffect**

找到 InspirationScreen 函数体内部的 LaunchedEffect 位置（参考 pullRefreshState 相关的 LaunchedEffect），在它们后面添加：

```kotlin
/**
 * 监听单个灵感删除事件，显示 Snackbar（带撤销按钮）
 *
 * 关键：snackbarHostState 为 null 时（无主 Scaffold）直接 return，不显示提示
 * 行为：用户点撤销 → undoDeleteInspiration 移回灵感表；否则 5s 后 Snackbar 自动消失
 */
LaunchedEffect(pendingDeletedInspiration) {
    pendingDeletedInspiration?.let {
        val host = snackbarHostState ?: return@let
        val result = host.showSnackbar(
            message = "已删除 1 条灵感",
            actionLabel = "撤销",
            duration = SnackbarDuration.Long
        )
        if (result == SnackbarResult.ActionPerformed) {
            viewModel.undoDeleteInspiration()
        } else {
            viewModel.clearPendingDeletedInspiration()
        }
    }
}

/**
 * 监听批量删除灵感事件，显示 Snackbar（带全部撤销按钮）
 */
LaunchedEffect(pendingBatchDeletedInspirations) {
    pendingBatchDeletedInspirations?.let { list ->
        if (list.isNotEmpty()) {
            val host = snackbarHostState ?: return@let
            val result = host.showSnackbar(
                message = "已删除 ${list.size} 条灵感",
                actionLabel = "全部撤销",
                duration = SnackbarDuration.Long
            )
            if (result == SnackbarResult.ActionPerformed) {
                viewModel.undoBatchDeleteInspiration()
            } else {
                viewModel.clearPendingBatchDeletedInspiration()
            }
        }
    }
}
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt
git commit -m "feat(inspiration): InspirationScreen 添加 Snackbar 监听"
```

---

### Task 9: MainScreen 传递 snackbarHostState 给 InspirationScreen

**Files:**
- Modify: [MainScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt):855-865

- [ ] **Step 1: 找到 InspirationScreen 调用点并添加参数**

定位到 `TabItem.INSPIRE -> InspirationScreen(...)` 调用位置（第 855 行附近），将：

```kotlin
TabItem.INSPIRE -> InspirationScreen(
    navController = navController,
    onFabClick = onFabClick,
    viewModel = inspirationViewModel
)
```

替换为：
```kotlin
TabItem.INSPIRE -> InspirationScreen(
    navController = navController,
    onFabClick = onFabClick,
    viewModel = inspirationViewModel,
    snackbarHostState = snackbarHostState
)
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt
git commit -m "feat(inspiration): MainScreen 传递 snackbarHostState"
```

---

### Task 10: 验证与手动测试

**Files:**
- No file changes

- [ ] **Step 1: 验证 import 完整性**

运行 import 检查（手动搜索）：
- `InspirationViewModel.kt`：应已 import `Inspiration`, `kotlinx.coroutines.Job`, `kotlinx.coroutines.delay`
- `InspirationScreen.kt`：应已 import `SnackbarHostState`, `SnackbarDuration`, `SnackbarResult`

如果缺失，添加。

- [ ] **Step 2: 手动测试清单**

按照设计文档第 8 节的"测试要点"逐项验证：

| # | 场景 | 期望 |
|---|------|------|
| 1 | 灵感页长按 → 删除 → 二次确认 → 确认 | 显示 "已删除 1 条灵感" + 撤销 |
| 2 | 点撤销 | 灵感从回收站移回灵感表，恢复显示 |
| 3 | 不点撤销，等 5s | Snackbar 自动消失，灵感保留在回收站（30 天后自动清理） |
| 4 | 灵感页批量模式 → 选中 N 条 → 删除 → 二次确认 → 确认 | 显示 "已删除 N 条灵感" + 全部撤销 |
| 5 | 点全部撤销 | 全部恢复 |
| 6 | 任意页面触发 Snackbar | 与底部导航栏有 16dp 间隙 |
| 7 | 任意 Snackbar | 整体高度约 36dp，更紧凑 |
| 8 | 短文本无按钮（如"权限不足"） | 居中显示，宽度跟随内容 |
| 9 | 短文本带按钮（如"已删除 1 个待办 撤销"） | 文字靠左，按钮靠右 |
| 10 | 长文本（如"☑️ 已删除 5 个待办"） | 末尾省略号截断，不换行，最大 560dp |
| 11 | 灵感页图片全屏预览 → 下载 | Snackbar 离底约 96dp（80+16），避开下载按钮 |

- [ ] **Step 3: 提交验证报告（如有问题则修复）**

如果发现任何问题，提交修复：
```bash
git add .
git commit -m "fix(snackbar): 修复测试中发现的问题"
```

如果没有问题，无需提交。

---

## 自审检查

✅ **Spec 覆盖检查**：
- 需求 4（灵感页 Snackbar）：Task 3-8 实现 ✅
- 需求 5（Snackbar 间隙）：Task 1 实现 ✅
- 需求 6（减小高度）：Task 2 实现 ✅

✅ **占位符扫描**：无 TBD/TODO

✅ **类型一致性**：
- `_pendingDeletedInspiration: MutableStateFlow<Inspiration?>` - 一致使用
- `_pendingBatchDeletedInspirations: MutableStateFlow<List<Inspiration>?>` - 一致使用
- `deleteInspirationTimerJob: Job?` - 一致使用
- `UNDO_DELETE_INSPIRATION_DELAY_MS = 5000L` - 与 HomeViewModel 一致

✅ **API 引用正确性**：
- `deletedInspirationRepository.permanentlyDelete(id)` - 已确认存在 ✅
- `inspirationRepository.insert(inspiration)` - 已确认存在 ✅
- `inspirationRepository.deleteById(id)` - 已确认存在 ✅
- `inspirationRepository.getInspirationById(id)` - 已确认存在 ✅
- `deletedInspirationRepository.insertDeletedInspiration(inspiration)` - 已确认存在 ✅
