# 灵感长按标签与改日期功能 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 接线灵感长按弹窗的「标签」和「改日期」两个按钮，使其分别打开标签管理弹窗和日期时间滚轮选择器，修改后自动同步到时间轴。

**Architecture:** 数据层在 `InspirationViewModel` 新增 `savedTags` 流和 `updateTags` 方法；组件层将 `ReminderPickerBottomSheet` 参数化以支持隐藏「重复提醒」和「农历」行；接线层在 `InspirationScreen` 渲染 `TagPickerSheet` 和参数化后的 `ReminderPickerBottomSheet`。依赖 DAO 层 `ORDER BY isPinned DESC, createdAt DESC` 实现日期修改后的自动重排。

**Tech Stack:** Jetpack Compose (BOM 2026.04.01 + 1.9.2) / Room / Hilt / StateFlow / JUnit4

**关联设计文档:** `docs/superpowers/specs/2026-07-09-灵感长按标签与改日期功能-design.md`

---

## 文件结构

| 文件 | 职责 | 改动类型 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt` | 灵感数据管理：新增 `savedTags` 流与 `updateTags` 方法 | 修改 |
| `app/src/test/java/com/corgimemo/app/viewmodel/InspirationSavedTagsTest.kt` | `aggregateSavedTags` 纯函数单元测试 | 新建 |
| `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt` | 日期时间滚轮选择器：参数化支持隐藏高级选项 | 修改 |
| `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt` | 灵感列表页：接线两个弹窗 | 修改 |

---

## Task 1: InspirationViewModel 新增 savedTags 流与 updateTags 方法

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt`
- Create: `app/src/test/java/com/corgimemo/app/viewmodel/InspirationSavedTagsTest.kt`

- [ ] **Step 1: 写失败测试 — aggregateSavedTags 纯函数**

创建测试文件 `app/src/test/java/com/corgimemo/app/viewmodel/InspirationSavedTagsTest.kt`：

```kotlin
package com.corgimemo.app.viewmodel

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * InspirationViewModel.aggregateSavedTags 单元测试
 *
 * 验证历史标签聚合逻辑：
 * - 将多个灵感的标签列表扁平化
 * - 去重
 * - 按字典序排序
 *
 * 用于 TagPickerSheet 的「历史标签快速添加」区域。
 */
class InspirationSavedTagsTest {

    @Test
    fun `空列表返回空`() {
        val result = InspirationViewModel.aggregateSavedTags(emptyList())
        assertEquals(emptyList<String>(), result)
    }

    @Test
    fun `单个灵感返回其标签列表`() {
        val result = InspirationViewModel.aggregateSavedTags(
            listOf(listOf("工作", "想法"))
        )
        assertEquals(listOf("工作", "想法"), result)
    }

    @Test
    fun `多个灵感扁平化所有标签`() {
        val result = InspirationViewModel.aggregateSavedTags(
            listOf(
                listOf("工作", "想法"),
                listOf("生活", "灵感"),
                emptyList()
            )
        )
        assertEquals(listOf("工作", "想法", "生活", "灵感"), result)
    }

    @Test
    fun `重复标签去重`() {
        val result = InspirationViewModel.aggregateSavedTags(
            listOf(
                listOf("工作", "想法"),
                listOf("工作", "生活"),
                listOf("想法", "灵感")
            )
        )
        assertEquals(listOf("工作", "想法", "生活", "灵感"), result)
    }

    @Test
    fun `标签按字典序排序`() {
        val result = InspirationViewModel.aggregateSavedTags(
            listOf(
                listOf("zebra", "apple"),
                listOf("mango", "banana")
            )
        )
        assertEquals(listOf("apple", "banana", "mango", "zebra"), result)
    }

    @Test
    fun `中文标签按Unicode序排序`() {
        val result = InspirationViewModel.aggregateSavedTags(
            listOf(
                listOf("工作", "生活"),
                listOf("想法", "灵感")
            )
        )
        // Unicode 序：工作(0x5DE5) < 想法(0x60F3) < 生活(0x751F) < 灵感(0x7075)
        // 实际 Unicode：工=0x5DE5, 想=0x60F3, 生=0x751F, 灵=0x7075
        // 排序：工(5DE5) < 想(60F3) < 灵(7075) < 生(751F)
        assertEquals(listOf("工作", "想法", "灵感", "生活"), result)
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.viewmodel.InspirationSavedTagsTest"`
Expected: 编译失败，`aggregateSavedTags` 未定义

- [ ] **Step 3: 实现 aggregateSavedTags 纯函数 + savedTags 流 + updateTags 方法**

修改 `app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt`：

**3.1 在 companion object 内新增 aggregateSavedTags 静态方法**

定位到 [InspirationViewModel.kt:84](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt#L84) `companion object {` 末尾（`isSameDay` 函数之后，`}` 闭合之前），添加：

```kotlin
        /**
         * 聚合多个灵感的标签为去重排序列表
         *
         * 用于 savedTags 流：从所有灵感中收集已使用的标签，
         * 去重后按字典序排序，供 TagPickerSheet 的「历史标签快速添加」区域使用。
         *
         * @param allTagsLists 每个灵感已解码的标签列表
         * @return 去重并按字典序排序的标签列表
         */
        fun aggregateSavedTags(allTagsLists: List<List<String>>): List<String> {
            return allTagsLists
                .flatten()
                .distinct()
                .sorted()
        }
```

**3.2 新增 savedTags StateFlow**

定位到 [InspirationViewModel.kt:139](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt#L139) `displayInspirations` 流定义之后，添加：

```kotlin
    /** 历史标签列表（从所有灵感聚合去重排序，用于 TagPickerSheet 快速选择） */
    val savedTags: StateFlow<List<String>> =
        _inspirations.map { list ->
            aggregateSavedTags(list.map { decodeTags(it.tags) })
        }.stateIn(
            scope = viewModelScope,
            started = SharingStarted.WhileSubscribed(5000),
            initialValue = emptyList()
        )
```

**3.3 新增 updateTags 方法**

定位到 [InspirationViewModel.kt:370](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt#L370) `updateInspirationDateTime` 方法之后，添加：

```kotlin
    /**
     * 更新指定灵感的标签
     *
     * 修改后的 tags 通过 encodeTags 编码为 JSON 字符串后持久化。
     * 数据库更新后，_inspirations 流自动触发，UI 自动刷新。
     *
     * @param id 灵感ID
     * @param newTags 新标签列表
     */
    fun updateTags(id: Long, newTags: List<String>) {
        viewModelScope.launch {
            val currentList = _inspirations.value
            val inspiration = currentList.find { it.id == id } ?: return@launch
            inspirationRepository.update(
                inspiration.copy(
                    tags = encodeTags(newTags),
                    updatedAt = System.currentTimeMillis()
                )
            )
        }
    }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.viewmodel.InspirationSavedTagsTest"`
Expected: 6 个测试全部 PASS

- [ ] **Step 5: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt app/src/test/java/com/corgimemo/app/viewmodel/InspirationSavedTagsTest.kt
git commit -m "feat(inspiration): InspirationViewModel 新增 savedTags 流与 updateTags 方法" -m "- 新增 aggregateSavedTags 纯函数（companion object）" -m "- 新增 savedTags StateFlow（聚合所有灵感标签去重排序）" -m "- 新增 updateTags 方法（更新指定灵感标签）" -m "- 添加 6 个单元测试覆盖聚合逻辑"
```

---

## Task 2: ReminderPickerBottomSheet 参数化改造

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt`

**回归保护说明:**
现有调用方（待办编辑页）不传新参数 → 使用默认值 → 行为完全不变。本任务无自动化测试，依赖编译期保证 + 手动回归验证。

- [ ] **Step 1: 修改函数签名新增 3 个参数**

定位到 [ReminderPickerBottomSheet.kt:69-78](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt#L69-L78)，将函数签名替换为：

```kotlin
@Composable
fun ReminderPickerBottomSheet(
    initialDateMillis: Long? = null,
    initialHour: Int = 13,
    initialMinute: Int = 35,
    initialRepeatType: Int = 0,
    initialCalendarEnabled: Boolean = false,
    showAdvancedOptions: Boolean = true,
    title: String = "设置提醒时间",
    rowLabel: String = "提醒时间",
    onDismiss: () -> Unit,
    onConfirm: (dateMillis: Long?, hour: Int, minute: Int, repeatType: Int, calendarEnabled: Boolean) -> Unit
) {
```

- [ ] **Step 2: 替换标题硬编码为 title 参数**

定位到 [ReminderPickerBottomSheet.kt:130](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt#L130)：

```kotlin
        // 标题
        Text(
            text = "设置提醒时间",
```

替换为：

```kotlin
        // 标题
        Text(
            text = title,
```

- [ ] **Step 3: 替换行标签硬编码为 rowLabel 参数**

定位到 [ReminderPickerBottomSheet.kt:146](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt#L146)：

```kotlin
            Text(
                text = "提醒时间",
                fontSize = 15.sp,
                color = Color(0xFF2D2D2D),
                fontWeight = FontWeight.Medium
            )
            Spacer(modifier = Modifier.weight(1f))

            // 日期芯片
```

替换为：

```kotlin
            Text(
                text = rowLabel,
                fontSize = 15.sp,
                color = Color(0xFF2D2D2D),
                fontWeight = FontWeight.Medium
            )
            Spacer(modifier = Modifier.weight(1f))

            // 日期芯片
```

- [ ] **Step 4: 用 showAdvancedOptions 条件包裹「重复提醒」行**

定位到 [ReminderPickerBottomSheet.kt:190-271](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt#L190-L271)。

**原代码（L190-L271）:**

```kotlin
        Spacer(modifier = Modifier.height(16.dp))

        // ===== 重复提醒行（胶囊按钮 + ↕箭头，展开时显示背景） =====
        var expanded by remember { mutableStateOf(false) }
        Box(
            modifier = Modifier.fillMaxWidth()
        ) {
            Row(
                // ...（重复提醒行内容）
            ) {
                // ...
            }

            // ===== 下拉选择菜单 =====
            androidx.compose.material3.DropdownMenu(
                // ...
            ) {
                repeatOptions.forEach { (type, name) ->
                    // ...
                }
            }
        }

        Spacer(modifier = Modifier.height(16.dp))
```

**替换为:**

```kotlin
        Spacer(modifier = Modifier.height(16.dp))

        // ===== 重复提醒行（仅 showAdvancedOptions=true 时显示） =====
        if (showAdvancedOptions) {
            var expanded by remember { mutableStateOf(false) }
            Box(
                modifier = Modifier.fillMaxWidth()
            ) {
                Row(
                    modifier = Modifier.fillMaxWidth(),
                    verticalAlignment = Alignment.CenterVertically
                ) {
                    Text(
                        text = "重复提醒",
                        fontSize = 15.sp,
                        color = Color(0xFF2D2D2D),
                        fontWeight = FontWeight.Medium
                    )
                    Spacer(modifier = Modifier.weight(1f))
                    // 胶囊形按钮：展开时显示浅灰圆角背景，收起时透明
                    Row(
                        modifier = Modifier
                            .clip(RoundedCornerShape(16.dp))
                            .background(if (expanded) Color(0xFFF0F0F0) else Color.Transparent)
                            .clickable { expanded = true }
                            .padding(horizontal = 12.dp, vertical = 4.dp),
                        verticalAlignment = Alignment.CenterVertically
                    ) {
                        Text(
                            text = repeatOptions.firstOrNull { it.first == repeatType }?.second ?: "不重复",
                            fontSize = 14.sp,
                            color = if (expanded) MaterialTheme.colorScheme.primary else Color(0xFF999999)
                        )
                        Spacer(modifier = Modifier.width(4.dp))
                        Text(
                            text = "\u2195", // ↕
                            fontSize = 14.sp,
                            color = if (expanded) MaterialTheme.colorScheme.primary else Color(0xFFCCCCCC)
                        )
                    }
                }

                androidx.compose.material3.DropdownMenu(
                    expanded = expanded,
                    onDismissRequest = { expanded = false },
                    offset = androidx.compose.ui.unit.DpOffset(x = (-60).dp, y = 4.dp),
                    containerColor = Color.White
                ) {
                    repeatOptions.forEach { (type, name) ->
                        androidx.compose.material3.DropdownMenuItem(
                            onClick = {
                                repeatType = type
                                expanded = false
                            },
                            text = {
                                Row(
                                    modifier = Modifier.fillMaxWidth(),
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Text(
                                        text = name,
                                        fontSize = 15.sp,
                                        color = if (repeatType == type) MaterialTheme.colorScheme.primary else Color(0xFF2D2D2D)
                                    )
                                    if (repeatType == type) {
                                        Icon(
                                            imageVector = Icons.Default.Check,
                                            contentDescription = null,
                                            tint = MaterialTheme.colorScheme.primary,
                                            modifier = Modifier.size(18.dp)
                                        )
                                    }
                                }
                            }
                        )
                    }
                }
            }

            Spacer(modifier = Modifier.height(16.dp))
        }
```

- [ ] **Step 5: 用 showAdvancedOptions 条件包裹「农历」行**

定位到 [ReminderPickerBottomSheet.kt:273-298](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt#L273-L298)。

**原代码（L273-L298）:**

```kotlin
        // ===== 农历行 =====
        Row(
            modifier = Modifier.fillMaxWidth(),
            verticalAlignment = Alignment.CenterVertically
        ) {
            Text(
                text = "农历",
                fontSize = 15.sp,
                color = Color(0xFF2D2D2D),
                fontWeight = FontWeight.Medium
            )
            Spacer(modifier = Modifier.weight(1f))
            Switch(
                checked = calendarEnabled,
                onCheckedChange = { calendarEnabled = it },
                colors = SwitchDefaults.colors(
                    checkedThumbColor = Color(0xFFFFFFFF),
                    checkedTrackColor = MaterialTheme.colorScheme.primary,
                    uncheckedThumbColor = Color(0xFFFFFFFF),
                    uncheckedTrackColor = Color(0xFFE0E0E0)
                ),
                modifier = Modifier.height(28.dp)
            )
        }

        Spacer(modifier = Modifier.height(16.dp))
```

**替换为:**

```kotlin
        // ===== 农历行（仅 showAdvancedOptions=true 时显示） =====
        if (showAdvancedOptions) {
            Row(
                modifier = Modifier.fillMaxWidth(),
                verticalAlignment = Alignment.CenterVertically
            ) {
                Text(
                    text = "农历",
                    fontSize = 15.sp,
                    color = Color(0xFF2D2D2D),
                    fontWeight = FontWeight.Medium
                )
                Spacer(modifier = Modifier.weight(1f))
                Switch(
                    checked = calendarEnabled,
                    onCheckedChange = { calendarEnabled = it },
                    colors = SwitchDefaults.colors(
                        checkedThumbColor = Color(0xFFFFFFFF),
                        checkedTrackColor = MaterialTheme.colorScheme.primary,
                        uncheckedThumbColor = Color(0xFFFFFFFF),
                        uncheckedTrackColor = Color(0xFFE0E0E0)
                    ),
                    modifier = Modifier.height(28.dp)
                )
            }

            Spacer(modifier = Modifier.height(16.dp))
        }
```

- [ ] **Step 6: 强制非高级模式下 calendarEnabled=false**

定位到 [ReminderPickerBottomSheet.kt:323](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt#L323) `DateWheelPickerView` 调用处：

```kotlin
            "calendar" -> DateWheelPickerView(
                selectedDate = selectedDate,
                onDateChange = { newDate ->
                    selectedDate = newDate
                    navMonth = YearMonth.from(newDate)
                },
                isExpanded = isCalendarExpanded,
                onToggleExpand = { isCalendarExpanded = !isCalendarExpanded },
                calendarEnabled = calendarEnabled,  // ✅ 传递农历开关状态
                modifier = Modifier.weight(1f)
            )
```

替换 `calendarEnabled = calendarEnabled,` 为：

```kotlin
                calendarEnabled = calendarEnabled && showAdvancedOptions,  // 非高级模式强制不显示农历
```

- [ ] **Step 7: 询问用户编译验证**

按工作区规则（编译验证规则最高优先级），询问用户是否进行编译验证。

- [ ] **Step 8: 手动回归验证 — 待办编辑页**

在模拟器中：
1. 打开待办编辑页
2. 点击「时间提醒」打开 ReminderPickerBottomSheet
3. 验证：
   - 标题显示「设置提醒时间」✓
   - 行标签显示「提醒时间」✓
   - 「重复提醒」行正常显示，可展开下拉选择 ✓
   - 「农历」行正常显示，可切换开关 ✓
   - 日历视图中切换农历开关，农历日期显示/隐藏正常 ✓

- [ ] **Step 9: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/ReminderPickerBottomSheet.kt
git commit -m "refactor(reminder-picker): 参数化 ReminderPickerBottomSheet 支持隐藏高级选项" -m "- 新增 showAdvancedOptions 参数（默认 true，兼容现有调用）" -m "- 新增 title 和 rowLabel 参数（默认值兼容现有调用）" -m "- showAdvancedOptions=false 时隐藏重复提醒和农历行" -m "- 非高级模式强制 calendarEnabled=false，日历视图不显示农历"
```

---

## Task 3: InspirationScreen 接线 TagPickerSheet 与 ReminderPickerBottomSheet

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt`

**回归保护说明:**
现有置顶/删除逻辑不变。本任务为 UI 接线，无自动化测试，依赖手动验证。

- [ ] **Step 1: 新增 showTagPicker 状态与 import**

定位到 [InspirationScreen.kt:1-43](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt#L1-L43) import 区域，在 `import com.corgimemo.app.ui.screens.inspiration.components.InspirationLongPressSheet` 之后添加：

```kotlin
import com.corgimemo.app.ui.screens.inspiration.components.TagPickerSheet
import com.corgimemo.app.ui.components.ReminderPickerBottomSheet
import androidx.compose.material3.rememberModalBottomSheetState
import java.util.Calendar
```

定位到 [InspirationScreen.kt:78](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt#L78)：

```kotlin
    var showDateTimePicker by remember { mutableStateOf(false) }
```

在其后添加：

```kotlin
    var showDateTimePicker by remember { mutableStateOf(false) }
    var showTagPicker by remember { mutableStateOf(false) }
```

- [ ] **Step 2: 改造 onTagClick 与 onDateClick 回调**

定位到 [InspirationScreen.kt:189-197](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt#L189-L197)：

**原代码:**

```kotlin
            onTagClick = {
                // 标签管理功能后续实现
                showLongPressSheet = false
                longPressedInspiration = null
            },
            onDateClick = {
                showDateTimePicker = true
                showLongPressSheet = false
            },
```

**替换为:**

```kotlin
            onTagClick = {
                // 关闭长按面板，打开标签管理弹窗（保留 longPressedInspiration 供 TagPickerSheet 使用）
                showLongPressSheet = false
                showTagPicker = true
            },
            onDateClick = {
                // 关闭长按面板，打开日期时间选择器（保留 longPressedInspiration 供 ReminderPickerBottomSheet 使用）
                showLongPressSheet = false
                showDateTimePicker = true
            },
```

- [ ] **Step 3: 渲染 TagPickerSheet**

定位到 [InspirationScreen.kt:207](file:///c:/Users/Lenovo/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt#L207) 长按面板 `if (showLongPressSheet && longPressedInspiration != null) { ... }` 闭合之后，添加：

```kotlin
    // 标签管理弹窗
    if (showTagPicker && longPressedInspiration != null) {
        val tagSheetState = rememberModalBottomSheetState(skipPartiallyExpanded = true)
        val savedTags by viewModel.savedTags.collectAsState()
        TagPickerSheet(
            sheetState = tagSheetState,
            tags = viewModel.decodeTags(longPressedInspiration!!.tags),
            savedTags = savedTags,
            onTagsChange = { newTags ->
                viewModel.updateTags(longPressedInspiration!!.id, newTags)
            },
            onDismiss = {
                showTagPicker = false
                longPressedInspiration = null
            }
        )
    }
```

- [ ] **Step 4: 渲染 ReminderPickerBottomSheet**

紧接 TagPickerSheet 渲染逻辑之后，添加：

```kotlin
    // 日期时间修改弹窗
    if (showDateTimePicker && longPressedInspiration != null) {
        val inspiration = longPressedInspiration!!
        val calendar = remember(inspiration.id) {
            Calendar.getInstance().apply { timeInMillis = inspiration.createdAt }
        }
        ReminderPickerBottomSheet(
            initialDateMillis = inspiration.createdAt,
            initialHour = calendar.get(Calendar.HOUR_OF_DAY),
            initialMinute = calendar.get(Calendar.MINUTE),
            showAdvancedOptions = false,        // 隐藏重复提醒和农历
            title = "修改日期时间",
            rowLabel = "日期时间",
            onDismiss = {
                showDateTimePicker = false
                longPressedInspiration = null
            },
            onConfirm = { dateMillis, _, _, _, _ ->
                if (dateMillis != null) {
                    viewModel.updateInspirationDateTime(inspiration.id, dateMillis)
                }
                showDateTimePicker = false
                longPressedInspiration = null
            }
        )
    }
```

- [ ] **Step 5: 询问用户编译验证**

按工作区规则（编译验证规则最高优先级），询问用户是否进行编译验证。

- [ ] **Step 6: 手动验证 — 标签管理弹窗**

在模拟器中：
1. 打开灵感页
2. 长按任意灵感 → 弹出长按面板
3. 点击「标签」→ 长按面板关闭，弹出标签管理弹窗
4. 验证：
   - 弹窗标题显示「标签管理」✓
   - 已有标签正确显示（与灵感卡片上的标签一致）✓
   - 历史标签区域显示所有灵感使用过的标签去重列表 ✓
   - 输入框输入新标签 + 点击添加按钮 → 标签出现在列表中 ✓
   - 长按已有标签 → 弹出删除确认对话框 ✓
   - 点击历史标签 → 快速添加到当前列表 ✓
   - 点击「取消」→ 弹窗关闭，灵感卡片标签不变 ✓
   - 重新打开标签管理弹窗 → 之前的修改未保存 ✓
   - 修改后点击「确认」→ 弹窗关闭，灵感卡片标签更新 ✓

- [ ] **Step 7: 手动验证 — 日期时间修改**

在模拟器中：
1. 打开灵感页，记录某条灵感的当前位置和日期
3. 长按该灵感 → 弹出长按面板
4. 点击「改日期」→ 长按面板关闭，弹出日期时间选择器
5. 验证：
   - 标题显示「修改日期时间」✓
   - 行标签显示「日期时间」✓
   - 「重复提醒」行不显示 ✓
   - 「农历」行不显示 ✓
   - 日历视图中不显示农历日期 ✓
   - 日期芯片初始值为灵感的当前日期 ✓
   - 时间芯片初始值为灵感的当前时间 ✓
   - 修改日期为更早日期 → 点击「确定」→ 灵感在时间轴上向下移动到对应位置 ✓
   - 修改日期为更晚日期 → 点击「确定」→ 灵感在时间轴上向上移动到对应位置 ✓
   - 修改时间（同日期）→ 灵感位置不变（同日内排序按时间倒序）✓
   - 点击「取消」→ 灵感位置不变 ✓
6. 验证置顶灵感改日期：
   - 长置顶灵感 → 改日期 → 确认 → 灵感保持置顶状态，仅在置顶区域内重排 ✓

- [ ] **Step 8: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt
git commit -m "feat(inspiration): 接线灵感长按标签与改日期弹窗" -m "- 新增 showTagPicker 状态，onTagClick 触发显示 TagPickerSheet" -m "- onDateClick 触发显示 ReminderPickerBottomSheet（showAdvancedOptions=false）" -m "- TagPickerSheet 传入当前灵感标签与 savedTags，确认后调用 updateTags" -m "- ReminderPickerBottomSheet 传入灵感当前日期时间，确认后调用 updateInspirationDateTime" -m "- 日期修改后依赖 DAO 层 ORDER BY 自动重排时间轴位置"
```

---

## Self-Review

### 1. Spec 覆盖检查

| Spec 需求 | 对应任务 | 状态 |
|---|---|---|
| InspirationViewModel 新增 savedTags 流 | Task 1 Step 3.2 | ✓ |
| InspirationViewModel 新增 updateTags 方法 | Task 1 Step 3.3 | ✓ |
| ReminderPickerBottomSheet 新增 showAdvancedOptions 参数 | Task 2 Step 1 | ✓ |
| ReminderPickerBottomSheet 新增 title 参数 | Task 2 Step 1 | ✓ |
| ReminderPickerBottomSheet 新增 rowLabel 参数 | Task 2 Step 1 | ✓ |
| 标题硬编码替换为 title | Task 2 Step 2 | ✓ |
| 行标签硬编码替换为 rowLabel | Task 2 Step 3 | ✓ |
| 重复提醒行条件包裹 | Task 2 Step 4 | ✓ |
| 农历行条件包裹 | Task 2 Step 5 | ✓ |
| 非高级模式强制 calendarEnabled=false | Task 2 Step 6 | ✓ |
| InspirationScreen 新增 showTagPicker 状态 | Task 3 Step 1 | ✓ |
| onTagClick 接线 TagPickerSheet | Task 3 Step 2, 3 | ✓ |
| onDateClick 接线 ReminderPickerBottomSheet | Task 3 Step 2, 4 | ✓ |
| 标签修改数据流自动刷新 | Task 1 (updateTags) + 数据库 Flow | ✓ |
| 日期修改后时间轴自动重排 | Task 3 (updateInspirationDateTime) + DAO ORDER BY | ✓ |
| 现有待办编辑页回归保护 | Task 2 默认参数 + Step 8 手动验证 | ✓ |

### 2. 占位符扫描

无 TBD/TODO/implement later 等占位符。所有代码块完整。

### 3. 类型一致性

- `aggregateSavedTags(allTagsLists: List<List<String>>): List<String>` — Task 1 定义，Task 1 测试使用 ✓
- `savedTags: StateFlow<List<String>>` — Task 1 定义，Task 3 使用 ✓
- `updateTags(id: Long, newTags: List<String>)` — Task 1 定义，Task 3 使用 ✓
- `showAdvancedOptions: Boolean` — Task 2 定义，Task 3 传 `false` ✓
- `title: String` — Task 2 定义，Task 3 传 `"修改日期时间"` ✓
- `rowLabel: String` — Task 2 定义，Task 3 传 `"日期时间"` ✓

---

## 执行交接

计划已保存至 `docs/superpowers/plans/2026-07-09-灵感长按标签与改日期功能.md`。两种执行方式：

**1. Subagent-Driven（推荐）** — 每个 Task 分派独立 subagent，任务间审查，快速迭代

**2. Inline Execution** — 在当前会话内执行，批量执行 + 检查点审查
