# divider 类型抽取 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 抽取 `DividerKind` 枚举，将 `isDivider: (T) -> Boolean` 升级为 `dividerKind: (T) -> DividerKind?`，同时优化 `checkPinnedZoneCrossed` 算法为"基于 divider 位置直接判定区域"的混合算法。

**Architecture:** 新增 `DividerKind` 枚举；修改 `checkPinnedZoneCrossed` 为混合算法（有 divider 时基于类型判定，无 divider 时回退找邻居）；同步修改 `ReorderableLazyColumn` 组件签名和 HomeScreen 调用方；适配 15 个现有测试 + 新增 2 个精细化测试。

**Tech Stack:** Kotlin、Jetpack Compose、JUnit 4

---

## File Structure

| 文件 | 责任 | 操作 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` | `DividerKind` 枚举 + `checkPinnedZoneCrossed` 算法 + 组件签名 | 修改 L100-L210 |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 调用方传入 `dividerKind` | 修改 L804-L806 |
| `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt` | 算法纯函数测试 | 适配 15 个 + 新增 2 个 |

---

### Task 1: 新增 DividerKind 枚举

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`

- [ ] **Step 1: 在 ReorderableLazyColumn.kt 顶部（package 之后、import 之前或 import 之后）新增 DividerKind 枚举**

在 `package com.corgimemo.app.ui.components` 行之后、第一个 import 之前，新增：

```kotlin
/**
 * 分隔按钮类型枚举
 *
 * 用于区分 displayItems 中三种不同的区域分隔按钮，
 * 让算法能基于 divider 类型直接判定区域，而非间接"找邻居"。
 *
 * - [PINNED]: 置顶区与待完成区之间的分隔按钮（PinnedDivider）
 * - [PENDING]: 待完成区与已完成区之间的分隔按钮（PendingDivider）
 * - [COMPLETED]: 已完成区之后的分隔按钮（CompletedDivider）
 */
enum class DividerKind {
    PINNED,
    PENDING,
    COMPLETED
}
```

- [ ] **Step 2: 不提交，进入下一个 Task**

---

### Task 2: 修改 checkPinnedZoneCrossed 算法为混合算法

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:100-146`

**说明：** 采用混合算法：有 divider 时基于 divider 类型直接判定区域（更精确）；无 divider 时回退到"找邻居 + 比较 isPinned"（保持向后兼容）。

- [ ] **Step 1: 修改 checkPinnedZoneCrossed 算法**

定位到 `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` 第 100-146 行的 `checkPinnedZoneCrossed` 函数。

将以下旧代码：

```kotlin
/**
 * 检测拖拽是否跨越了置顶区边界
 *
 * 判定逻辑：
 * - 置顶区 = 列表顶部连续的 isPinned=true 项
 * - 跨越 = 被拖项原始 isPinned 与当前位置邻居的 isPinned 不同
 *
 * 边界处理：
 * - 分隔按钮（PinnedDivider/PendingDivider/CompletedDivider）的 isPinned=false，
 *   算法不能直接用作邻居（会误判），需跳过
 * - 被拖项紧贴 divider 放置时，divider 是区域边界，不能跨越
 *
 * @param displayItems 全局显示列表（含分隔按钮和待办项）
 * @param isPinned 查询项是否置顶
 * @param isDivider 查询项是否分隔按钮（需跳过的项）
 * @param draggedOriginalIsPinned 被拖项原始 isPinned
 * @param draggedCurrentIndex 被拖项当前在 displayItems 中的索引
 * @return true 表示跨越了置顶区边界（需翻转 isPinned）
 */
fun <T> checkPinnedZoneCrossed(
    displayItems: List<T>,
    isPinned: (T) -> Boolean,
    isDivider: (T) -> Boolean,
    draggedOriginalIsPinned: Boolean,
    draggedCurrentIndex: Int
): Boolean {
    if (draggedCurrentIndex < 0 || draggedCurrentIndex >= displayItems.size) return false

    // 向前找邻居：divider 是区域边界，遇到立即停止（不能跨越边界找邻居）
    var neighborIdx = -1
    val prevIdx = draggedCurrentIndex - 1
    if (prevIdx >= 0 && !isDivider(displayItems[prevIdx])) {
        neighborIdx = prevIdx
    }
    // 前面被 divider 阻断或到列表头 → 向后找
    if (neighborIdx < 0) {
        val nextIdx = draggedCurrentIndex + 1
        if (nextIdx < displayItems.size && !isDivider(displayItems[nextIdx])) {
            neighborIdx = nextIdx
        }
    }
    // 两边都被 divider 阻断或到列表边界 → 无法判定，不跨区
    if (neighborIdx < 0) return false

    return draggedOriginalIsPinned != isPinned(displayItems[neighborIdx])
}
```

替换为：

```kotlin
/**
 * 检测拖拽是否跨越了置顶区边界
 *
 * 混合算法：
 * 1. 优先基于 divider 类型判定：扫描被拖项前面最近的 divider，
 *    根据 divider 类型直接确定当前所在区域（PINNED/PENDING/COMPLETED）
 * 2. 若无 divider（如 pinnedCount=0 的纯待办场景），回退到"找邻居 + 比较 isPinned"
 *
 * 优势：
 * - 有 divider 时：基于 divider 类型直接判定，语义更清晰
 * - 无 divider 时：回退到找邻居，保持向后兼容
 *
 * @param displayItems 全局显示列表（含分隔按钮和待办项）
 * @param isPinned 查询项是否置顶
 * @param dividerKind 查询项的分隔按钮类型，返回 null 表示非 divider
 * @param draggedOriginalIsPinned 被拖项原始 isPinned
 * @param draggedCurrentIndex 被拖项当前在 displayItems 中的索引
 * @return true 表示跨越了置顶区边界（需翻转 isPinned）
 */
fun <T> checkPinnedZoneCrossed(
    displayItems: List<T>,
    isPinned: (T) -> Boolean,
    dividerKind: (T) -> DividerKind?,
    draggedOriginalIsPinned: Boolean,
    draggedCurrentIndex: Int
): Boolean {
    if (draggedCurrentIndex < 0 || draggedCurrentIndex >= displayItems.size) return false

    // 1. 扫描被拖项前面最近的 divider，确定其当前所在区域
    var currentZone: DividerKind? = null
    for (i in draggedCurrentIndex - 1 downTo 0) {
        val kind = dividerKind(displayItems[i])
        if (kind != null) {
            currentZone = kind
            break
        }
    }

    // 2. 若找到 divider，基于 divider 类型直接判定
    if (currentZone != null) {
        val originalZone = if (draggedOriginalIsPinned) DividerKind.PINNED else DividerKind.PENDING
        return currentZone != originalZone
    }

    // 3. 无 divider → 回退到"找邻居 + 比较 isPinned"（保持向后兼容）
    var neighborIdx = -1
    val prevIdx = draggedCurrentIndex - 1
    if (prevIdx >= 0 && dividerKind(displayItems[prevIdx]) == null) {
        neighborIdx = prevIdx
    }
    if (neighborIdx < 0) {
        val nextIdx = draggedCurrentIndex + 1
        if (nextIdx < displayItems.size && dividerKind(displayItems[nextIdx]) == null) {
            neighborIdx = nextIdx
        }
    }
    if (neighborIdx < 0) return false

    return draggedOriginalIsPinned != isPinned(displayItems[neighborIdx])
}
```

- [ ] **Step 2: 不提交，进入下一个 Task**

---

### Task 3: 修改 ReorderableLazyColumn 组件签名

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:199-210`

- [ ] **Step 1: 修改组件签名中的 isDivider 参数为 dividerKind**

定位到 `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` 中 `ReorderableLazyColumn` 函数签名。

将以下旧代码中的 `isDivider` 参数：

```kotlin
@Composable
fun <T> ReorderableLazyColumn(
    items: List<T>,
    isDragEnabled: Boolean,
    isDraggable: (T) -> Boolean = { true },
    isPinned: (T) -> Boolean,
    isDivider: (T) -> Boolean,
    key: (T) -> Any,
    onReorder: (fromIndex: Int, toIndex: Int, dividerIndex: Int, crossedPinnedZone: Boolean) -> Unit,
    dividerIndex: Int = -1,
    isBatchMode: Boolean = false,
    selectedIds: Set<Any> = emptySet(),
    onMergeReorder: (selectedIds: Set<Any>, toIndex: Int, dividerIndex: Int, crossedPinnedZone: Boolean) -> Unit = { _, _, _, _ -> },
    listState: LazyListState = rememberLazyListState(),
    modifier: Modifier = Modifier,
    content: @Composable (index: Int, item: T, isDragging: Boolean, isDragActive: Boolean) -> Unit
)
```

替换为：

```kotlin
@Composable
fun <T> ReorderableLazyColumn(
    items: List<T>,
    isDragEnabled: Boolean,
    isDraggable: (T) -> Boolean = { true },
    isPinned: (T) -> Boolean,
    dividerKind: (T) -> DividerKind?,
    key: (T) -> Any,
    onReorder: (fromIndex: Int, toIndex: Int, dividerIndex: Int, crossedPinnedZone: Boolean) -> Unit,
    dividerIndex: Int = -1,
    isBatchMode: Boolean = false,
    selectedIds: Set<Any> = emptySet(),
    onMergeReorder: (selectedIds: Set<Any>, toIndex: Int, dividerIndex: Int, crossedPinnedZone: Boolean) -> Unit = { _, _, _, _ -> },
    listState: LazyListState = rememberLazyListState(),
    modifier: Modifier = Modifier,
    content: @Composable (index: Int, item: T, isDragging: Boolean, isDragActive: Boolean) -> Unit
)
```

- [ ] **Step 2: 修改组件内部所有使用 isDivider 的地方**

在 `ReorderableLazyColumn` 函数体中，将所有 `isDivider` 引用替换为 `dividerKind`：

1. 调用 `checkPinnedZoneCrossed` 的地方（单拖路径）：
   - `isDivider = isDivider` → `dividerKind = dividerKind`

2. 调用 `checkPinnedZoneCrossed` 的地方（合并拖路径）：
   - `isDivider = isDivider` → `dividerKind = dividerKind`

3. 其他 `isDivider(it)` 调用：
   - `isDivider(it)` → `dividerKind(it) != null`

使用全局替换（注意上下文）：
- `isDivider = isDivider` → `dividerKind = dividerKind`
- `isDivider(it)` → `dividerKind(it) != null`
- `isDivider = ` → `dividerKind = `

- [ ] **Step 3: 检查 import 语句完整性**

按 workspace rule 检查 `ReorderableLazyColumn.kt` 的 import 语句。本次修改未引入新类型或新函数（DividerKind 已在同文件顶部定义），无需新增 import。

- [ ] **Step 4: 不提交，进入下一个 Task**

---

### Task 4: 修改 HomeScreen 调用方

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt:804-806`

- [ ] **Step 1: 修改 isDivider 传入为 dividerKind**

定位到 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` 第 804-806 行。

将以下旧代码：

```kotlin
                                isDivider = { it is DisplayItem.PinnedDivider ||
                                              it is DisplayItem.PendingDivider ||
                                              it is DisplayItem.CompletedDivider },
```

替换为：

```kotlin
                                dividerKind = { when (it) {
                                    is DisplayItem.PinnedDivider -> DividerKind.PINNED
                                    is DisplayItem.PendingDivider -> DividerKind.PENDING
                                    is DisplayItem.CompletedDivider -> DividerKind.COMPLETED
                                    else -> null
                                }},
```

- [ ] **Step 2: 检查 import 语句完整性**

按 workspace rule 检查 `HomeScreen.kt` 的 import 语句。新增使用了 `DividerKind`，需要确认是否已 import。

若 `HomeScreen.kt` 顶部未 import `DividerKind`，新增：

```kotlin
import com.corgimemo.app.ui.components.DividerKind
```

- [ ] **Step 3: 不提交，进入下一个 Task**

---

### Task 5: 编译验证

- [ ] **Step 1: 编译验证**

Run:
```bash
./gradlew :app:assembleDebug --console=plain
```

Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 不提交，进入下一个 Task**

---

### Task 6: 适配算法层测试

**Files:**
- Modify: `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`

**说明：** 适配 15 个现有测试到新签名，新增 2 个基于 divider 类型的精细化测试。

- [ ] **Step 1: 修改 TestItem 数据类和 checkCrossed 辅助方法**

定位到 `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt` 第 14-28 行。

将以下旧代码：

```kotlin
    /** 测试用数据项：isPinned 表示是否置顶，isDivider 表示是否分隔按钮 */
    private data class TestItem(val isPinned: Boolean, val isDivider: Boolean = false)

    /** 便捷封装：传入 List<TestItem> 调用算法 */
    private fun checkCrossed(
        items: List<TestItem>,
        draggedOriginalIsPinned: Boolean,
        draggedCurrentIndex: Int
    ): Boolean = ReorderAlgorithms.checkPinnedZoneCrossed(
        displayItems = items,
        isPinned = { it.isPinned },
        isDivider = { it.isDivider },
        draggedOriginalIsPinned = draggedOriginalIsPinned,
        draggedCurrentIndex = draggedCurrentIndex
    )
```

替换为：

```kotlin
    /** 测试用数据项：isPinned 表示是否置顶，dividerKind 表示分隔按钮类型（null = 非分隔按钮） */
    private data class TestItem(val isPinned: Boolean, val dividerKind: DividerKind? = null)

    /** 便捷封装：传入 List<TestItem> 调用算法 */
    private fun checkCrossed(
        items: List<TestItem>,
        draggedOriginalIsPinned: Boolean,
        draggedCurrentIndex: Int
    ): Boolean = ReorderAlgorithms.checkPinnedZoneCrossed(
        displayItems = items,
        isPinned = { it.isPinned },
        dividerKind = { it.dividerKind },
        draggedOriginalIsPinned = draggedOriginalIsPinned,
        draggedCurrentIndex = draggedCurrentIndex
    )
```

- [ ] **Step 2: 新增 import DividerKind**

在 `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt` 顶部 import 区域新增：

```kotlin
import com.corgimemo.app.ui.components.DividerKind
```

（注意：DividerKind 已在 `com.corgimemo.app.ui.components` 包下，测试文件也在同包，可能无需 import。若编译报错未找到 DividerKind，则添加此 import。）

- [ ] **Step 3: 适配 15 个测试中的 isDivider = true 为具体 dividerKind**

使用全局替换，将所有测试中的 `isDivider = true` 替换为 `dividerKind = DividerKind.PENDING`。

具体替换规则：
- `TestItem(isPinned = false, isDivider = true)` → `TestItem(isPinned = false, dividerKind = DividerKind.PENDING)`
- `TestItem(isPinned = false, isDivider = true)` → `TestItem(isPinned = false, dividerKind = DividerKind.PINNED)`（若是 PinnedDivider 场景）

根据每个测试的注释语义选择合适的 DividerKind：

| 测试 | 旧代码 | 新代码 | 说明 |
|---|---|---|---|
| `置顶项拖到置顶区内应跳过 PinnedDivider 不跨区` | `isDivider = true` | `dividerKind = DividerKind.PINNED` | PinnedDivider |
| `非置顶项拖到置顶区内应跳过 PinnedDivider 跨区` | `isDivider = true` | `dividerKind = DividerKind.PINNED` | PinnedDivider |
| `置顶项拖到置顶区末尾应取前方真实邻居` | `isDivider = true` | `dividerKind = DividerKind.PENDING` | PendingDivider |
| `全 divider 列表应返回 false` | `isDivider = true` (两处) | `dividerKind = DividerKind.PINNED` (两处) | 两个 PinnedDivider |
| `divider 后第一项不应跨区` | `isDivider = true` (两处) | `dividerKind = DividerKind.PINNED` + `dividerKind = DividerKind.PENDING` | PinnedDivider + PendingDivider |
| `divider 前第一项不应跨区` | `isDivider = true` (两处) | `dividerKind = DividerKind.PINNED` + `dividerKind = DividerKind.PENDING` | PinnedDivider + PendingDivider |

逐个修改每个测试的 TestItem 构造。

- [ ] **Step 4: 在文件末尾（`}` 之前）新增 2 个基于 divider 类型的精细化测试**

```kotlin
    /**
     * 场景：PinnedDivider 后的项判定为 PINNED 区
     *
     * 验证新算法基于 divider 类型直接判定区域。
     * - displayItems: [PinnedDivider, N1(被拖到此处), P1]
     * - 前面最近 divider 是 PinnedDivider → currentZone=PINNED
     * - draggedOriginalIsPinned=false → originalZone=PENDING
     * - 期望 crossed=true（PENDING → PINNED 跨区）
     */
    @Test
    fun `PinnedDivider 后的项判定为 PINNED 区`() {
        val items = listOf(
            TestItem(isPinned = false, dividerKind = DividerKind.PINNED),  // PinnedDivider
            TestItem(isPinned = false),                                      // N1（被拖到此处）
            TestItem(isPinned = true)                                         // P1
        )
        val result = checkCrossed(items, draggedOriginalIsPinned = false, draggedCurrentIndex = 1)
        assertEquals(true, result)
    }

    /**
     * 场景：PendingDivider 后的项判定为 PENDING 区
     *
     * 验证新算法基于 divider 类型直接判定区域。
     * - displayItems: [PinnedDivider, P1, PendingDivider, N1(被拖到此处), N2]
     * - 前面最近 divider 是 PendingDivider → currentZone=PENDING
     * - draggedOriginalIsPinned=true → originalZone=PINNED
     * - 期望 crossed=true（PINNED → PENDING 跨区）
     */
    @Test
    fun `PendingDivider 后的项判定为 PENDING 区`() {
        val items = listOf(
            TestItem(isPinned = false, dividerKind = DividerKind.PINNED),   // PinnedDivider
            TestItem(isPinned = true),                                        // P1
            TestItem(isPinned = false, dividerKind = DividerKind.PENDING),  // PendingDivider
            TestItem(isPinned = false),                                      // N1（被拖到此处）
            TestItem(isPinned = false)                                         // N2
        )
        val result = checkCrossed(items, draggedOriginalIsPinned = true, draggedCurrentIndex = 3)
        assertEquals(true, result)
    }
```

- [ ] **Step 5: 不提交，进入下一个 Task**

---

### Task 7: 运行测试验证

- [ ] **Step 1: 运行算法层测试**

Run:
```bash
./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest" --console=plain
```

Expected: 全部 17 个测试通过（15 原有 + 2 新增）

- [ ] **Step 2: 运行全量单元测试**

Run:
```bash
./gradlew :app:testDebugUnitTest --console=plain
```

Expected: BUILD SUCCESSFUL，所有测试通过

- [ ] **Step 3: 不提交，进入下一个 Task**

---

### Task 8: 真机验证

- [ ] **Step 1: 真机手动验证 4 个跨区拖拽场景**

在真机上执行以下步骤：

**场景 1：N6 拖到 PendingDivider 后**
1. 创建 10 条待办，1-4 设为置顶
2. 长按 6，拖到 PendingDivider 和 5 之间释放
3. 验证：6 落在 5 前，仍为非置顶 ✓

**场景 2：P4 拖到 P1 上方**
1. 长按 4，拖到 1 上方释放
2. 验证：4 落在 1 前，仍为置顶 ✓

**场景 3：N5 拖到 P1/P2 之间**
1. 长按 5，拖到 1/2 之间释放
2. 验证：5 变为置顶，落入置顶区 ✓

**场景 4：P4 拖到 N5/N6 之间**
1. 长按 4，拖到 5/6 之间释放
2. 验证：4 变为非置顶，落入待完成区 ✓

- [ ] **Step 2: 不提交，等待用户确认**

---

### Task 9: 询问用户是否进行 git 提交

按 workspace rule（`c:\Users\EDY\Desktop\CorgiMemo\.trae\rules\git提交.md`），任务完成后必须询问用户是否进行 git 提交。

- [ ] **Step 1: 询问用户是否进行 git 提交**

向用户呈现本次变更摘要：

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` | 修改 | 新增 DividerKind 枚举 + 算法改为混合算法 + 组件签名变更 |
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 修改 | 调用方传入 dividerKind 返回具体类型 |
| `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt` | 修改 | 适配 15 个测试 + 新增 2 个精细化测试 |
| `docs/superpowers/specs/2026-07-04-divider类型抽取-design.md` | 新增 | 设计文档 |
| `docs/superpowers/plans/2026-07-04-divider类型抽取.md` | 新增 | 实施计划 |

若用户选择提交：
- 智能生成中文提交信息
- 执行 `git add <specific files>` + `git commit -m "..."`

若用户选择不提交：跳过提交步骤。

---

## Self-Review

### Spec 覆盖检查

| Spec 要求 | 对应 Task |
|---|---|
| 新增 DividerKind 枚举 | Task 1 Step 1 |
| checkPinnedZoneCrossed 算法改为混合算法 | Task 2 Step 1 |
| ReorderableLazyColumn 组件签名变更 | Task 3 Step 1-2 |
| HomeScreen 调用方变更 | Task 4 Step 1 |
| import 语句检查 | Task 3 Step 3, Task 4 Step 2, Task 6 Step 2 |
| 算法层 15 个测试适配 | Task 6 Step 1-3 |
| 新增 2 个精细化测试 | Task 6 Step 4 |
| `./gradlew assembleDebug` 编译通过 | Task 5 Step 1 |
| `./gradlew test` 全部测试通过 | Task 7 Step 1-2 |
| 真机验证 4 个场景 | Task 8 Step 1 |
| git 提交询问 | Task 9 |

### Placeholder 扫描

无 TBD/TODO/placeholder。所有代码块完整。✓

### 类型一致性

- `DividerKind`：枚举（PINNED, PENDING, COMPLETED）✓
- `dividerKind: (T) -> DividerKind?`：返回 null 或枚举值 ✓
- `TestItem.dividerKind: DividerKind?`：null = 非 divider ✓
- `currentZone != originalZone`：DividerKind 比较 ✓

### 算法正确性验证

混合算法的关键设计：有 divider 时基于类型判定，无 divider 时回退找邻居。

| 测试场景 | 有 divider？ | 算法路径 | 预期 |
|---|---|---|---|
| 测试 1-5（无 divider） | 否 | 回退找邻居 | 与旧算法一致 ✓ |
| 测试 6-8（有 divider） | 是 | 基于 divider 类型 | 正确判定 ✓ |
| 测试 9-13（无 divider） | 否 | 回退找邻居 | 与旧算法一致 ✓ |
| 测试 14-15（有 divider） | 是 | 基于 divider 类型 | 正确判定 ✓ |
| 新增测试 1-2（有 divider） | 是 | 基于 divider 类型 | 正确判定 ✓ |

### 后续可优化方向（来自 spec 第 8 节）

1. 进一步泛型化：将 DividerKind 改为泛型 T : DividerKind
2. 算法性能优化：缓存 divider 位置索引
3. 区域判定 API：抽取 determineZone 为独立公开函数
4. Divider 类型与 Zone 类型统一：拆分为 DividerType 和 Zone 两个枚举
