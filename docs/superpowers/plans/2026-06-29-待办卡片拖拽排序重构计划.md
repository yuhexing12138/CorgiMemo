# 待办卡片拖拽排序重构 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 `sh.calvin.reorderable:reorderable:3.1.0` 库彻底重写待办卡片拖拽排序，解决"长按首项向下拖失控下滑"与"与首项交换卡片上移飞出"两个异常，保留对外 API 与视觉风格。

**Architecture:** 移除自研 `awaitEachGesture` 状态机（15+ 状态变量、三套坐标系混用），改用库的 `rememberReorderableLazyListState` + `ReorderableItem` + `Modifier.longPressDraggableHandle`。库内部统一基于 `LazyListItemInfo.offset`（视口坐标）定位，内置基于手柄中心距视口边缘距离的自动滚动，内置释放回弹 spring 动画，内置首可见项位置保护。对外 API 与视觉反馈（虚线占位框 + 浮起卡片）保持不变，HomeScreen / InspirationEditScreen 零改动。

**Tech Stack:** Kotlin 2.2.10 / Compose BOM 2026.04.01 / Compose 1.9.2 / sh.calvin.reorderable:reorderable:3.1.0

**关联设计文档:** `docs/superpowers/specs/2026-06-29-待办卡片拖拽排序重构设计.md`

**本地库源码:** `Reorderable/`（Calvin-LL/Reorderable，commit `2eb3b59a`）

---

## 文件结构

| 文件 | 责任 | 改动 |
|---|---|---|
| `gradle/libs.versions.toml` | 版本目录 | 新增 reorderable 版本与库声明 |
| `app/build.gradle.kts` | 应用依赖 | 新增 `implementation(libs.reorderable)` |
| `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` | 拖拽核心 | 重写 ReorderableLazyColumn 与 ReorderableColumn；精简 ReorderAlgorithms |
| `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt` | 算法测试 | 移除废弃函数测试，保留 checkPinnedZoneCrossed 测试 |

HomeScreen.kt、InspirationEditScreen.kt 零改动（API 签名不变）。

---

## Task 1: 添加 reorderable 库依赖

**Files:**
- Modify: `gradle/libs.versions.toml`
- Modify: `app/build.gradle.kts`

- [ ] **Step 1: 在 libs.versions.toml 添加 reorderable 版本**

打开 `gradle/libs.versions.toml`，在 `[versions]` 段末尾（第 20 行 `kotlinxSerializationJson = "1.6.2"` 之后）添加一行：

```toml
reorderable = "3.1.0"
```

- [ ] **Step 2: 在 libs.versions.toml 添加 reorderable 库声明**

在同一文件的 `[libraries]` 段中（第 63 行 `androidx-compose-ui-test-manifest` 之后）添加：

```toml
reorderable = { module = "sh.calvin.reorderable:reorderable", version.ref = "reorderable" }
```

- [ ] **Step 3: 在 app/build.gradle.kts 添加依赖**

打开 `app/build.gradle.kts`，在 `dependencies` 块中（第 127 行 `implementation("cn.6tail:tyme4kt:1.4.5")` 之后）添加：

```kotlin
    /** 拖拽排序库 Calvin-LL/Reorderable */
    implementation(libs.reorderable)
```

- [ ] **Step 4: 验证依赖解析**

运行命令验证依赖能正确解析：

```powershell
.\gradlew.bat :app:dependencies --configuration releaseRuntimeClasspath | Select-String "reorderable"
```

预期输出包含 `sh.calvin.reorderable:reorderable:3.1.0`。

- [ ] **Step 5: 询问是否 git 提交**

向用户询问是否进行 git 提交（按项目 git 规则）。若提交，提交信息：
```
chore: 添加 Calvin-LL/Reorderable 3.1.0 拖拽排序库依赖
```

---

## Task 2: 精简 ReorderAlgorithms 与测试

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`（仅 ReorderAlgorithms object 与 VisibleItemInfo）
- Modify: `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`

- [ ] **Step 1: 精简 ReorderAlgorithms object**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`，找到 `object ReorderAlgorithms`（约第 235 行），将整个 object 替换为仅保留 `checkPinnedZoneCrossed`：

```kotlin
/**
 * 拖拽排序算法纯函数集合
 *
 * 重构后仅保留置顶区跨越检测，其余算法由 Calvin-LL/Reorderable 库内部处理。
 */
object ReorderAlgorithms {

    /**
     * 检测拖拽是否跨越置顶区分界线
     *
     * 规则：
     * - 置顶区 = 列表顶部连续的 isPinned=true 项
     * - 跨越 = 被拖项原始 isPinned 与当前位置邻居的 isPinned 不同
     *
     * @param displayItems 当前显示列表（被拖项已移除后）的 isPinned 序列
     * @param draggedOriginalIsPinned 被拖项原始 isPinned
     * @param draggedCurrentIndex 被拖项当前列表位置（插入位置）
     * @return true=已跨越分界线
     */
    fun checkPinnedZoneCrossed(
        displayItems: List<Boolean>,
        draggedOriginalIsPinned: Boolean,
        draggedCurrentIndex: Int
    ): Boolean {
        if (draggedCurrentIndex < 0 || draggedCurrentIndex >= displayItems.size) return false
        val neighborIsPinned = when {
            draggedCurrentIndex > 0 -> displayItems[draggedCurrentIndex - 1]
            draggedCurrentIndex < displayItems.size - 1 -> displayItems[draggedCurrentIndex + 1]
            else -> draggedOriginalIsPinned
        }
        return draggedOriginalIsPinned != neighborIsPinned
    }
}
```

同时删除 `VisibleItemInfo` data class（约第 379-383 行）：

```kotlin
// 删除整个 VisibleItemInfo data class
```

- [ ] **Step 2: 精简 ReorderAlgorithmsTest**

打开 `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`，将整个文件替换为仅保留 `checkPinnedZoneCrossed` 的 5 个测试：

```kotlin
package com.corgimemo.app.ui.components

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * 拖拽排序算法纯函数单元测试
 *
 * 重构后仅覆盖 checkPinnedZoneCrossed（置顶区跨越检测）。
 * 其余算法已由 Calvin-LL/Reorderable 库内部实现，无需测试。
 */
class ReorderAlgorithmsTest {

    // ==================== checkPinnedZoneCrossed 测试 ====================

    /**
     * 场景：原置顶，拖到非置顶区
     * 预期：返回 true
     */
    @Test
    fun `checkPinnedZoneCrossed 置顶到非置顶返回 true`() {
        val displayItems = listOf(false, false)
        val result = ReorderAlgorithms.checkPinnedZoneCrossed(
            displayItems = displayItems,
            draggedOriginalIsPinned = true,
            draggedCurrentIndex = 0
        )
        assertEquals(true, result)
    }

    /**
     * 场景：原非置顶，拖到置顶区
     * 预期：返回 true
     */
    @Test
    fun `checkPinnedZoneCrossed 非置顶到置顶返回 true`() {
        val displayItems = listOf(true, true)
        val result = ReorderAlgorithms.checkPinnedZoneCrossed(
            displayItems = displayItems,
            draggedOriginalIsPinned = false,
            draggedCurrentIndex = 0
        )
        assertEquals(true, result)
    }

    /**
     * 场景：置顶区内移动
     * 预期：返回 false（未跨越分界线）
     */
    @Test
    fun `checkPinnedZoneCrossed 置顶区内移动返回 false`() {
        val displayItems = listOf(true, true)
        val result = ReorderAlgorithms.checkPinnedZoneCrossed(
            displayItems = displayItems,
            draggedOriginalIsPinned = true,
            draggedCurrentIndex = 1
        )
        assertEquals(false, result)
    }

    /**
     * 场景：非置顶区内移动
     * 预期：返回 false
     */
    @Test
    fun `checkPinnedZoneCrossed 非置顶区内移动返回 false`() {
        val displayItems = listOf(false, false)
        val result = ReorderAlgorithms.checkPinnedZoneCrossed(
            displayItems = displayItems,
            draggedOriginalIsPinned = false,
            draggedCurrentIndex = 1
        )
        assertEquals(false, result)
    }

    /**
     * 场景：index 越界
     * 预期：返回 false（兜底）
     */
    @Test
    fun `checkPinnedZoneCrossed index 越界返回 false`() {
        val displayItems = listOf(true)
        val result = ReorderAlgorithms.checkPinnedZoneCrossed(
            displayItems = displayItems,
            draggedOriginalIsPinned = true,
            draggedCurrentIndex = 5
        )
        assertEquals(false, result)
    }
}
```

- [ ] **Step 3: 运行测试验证**

```powershell
.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest"
```

预期：5 个测试全部通过。

- [ ] **Step 4: 询问是否 git 提交**

向用户询问是否进行 git 提交。若提交，提交信息：
```
refactor: 精简 ReorderAlgorithms，仅保留置顶区跨越检测
```

---

## Task 3: 重写 ReorderableLazyColumn（核心任务）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`（重写 ReorderableLazyColumn 与 ReorderableColumn 函数）

**注意**：本 Task 不动 `ReorderAlgorithms` object（Task 2 已处理）。

- [ ] **Step 1: 替换文件头部 import 区**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`，将第 1-55 行的 import 区替换为：

```kotlin
package com.corgimemo.app.ui.components

import androidx.compose.foundation.Canvas
import androidx.compose.foundation.layout.Box
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.foundation.lazy.LazyListState
import androidx.compose.foundation.lazy.itemsIndexed
import androidx.compose.foundation.lazy.rememberLazyListState
import androidx.compose.runtime.Composable
import androidx.compose.runtime.LaunchedEffect
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableIntStateOf
import androidx.compose.runtime.mutableLongStateOf
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier
import androidx.compose.ui.geometry.CornerRadius
import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.geometry.Size
import androidx.compose.ui.graphics.PathEffect
import androidx.compose.ui.graphics.drawscope.Stroke
import androidx.compose.ui.graphics.graphicsLayer
import androidx.compose.ui.platform.LocalContext
import androidx.compose.ui.unit.dp
import com.corgimemo.app.animation.HapticFeedbackManager
import com.corgimemo.app.animation.InteractionType
import sh.calvin.reorderable.ReorderableItem
import sh.calvin.reorderable.rememberReorderableLazyListState
```

- [ ] **Step 2: 重写 ReorderableLazyColumn 函数**

将现有的 `ReorderableLazyColumn` 函数（约第 413-913 行，从 `@Composable fun <T> ReorderableLazyColumn(` 到文件末尾的 `}`）整体替换为：

```kotlin
/**
 * 可拖拽排序 LazyColumn 容器组件（基于 Calvin-LL/Reorderable 库）
 *
 * **职责**：
 * - 长按进入拖拽模式（库内置 detectDragGesturesAfterLongPress）
 * - 拖拽中卡片半透明跟随手指，其他项 animateItem() 让位
 * - 库内置：边缘自动滚动（基于手柄中心距视口边缘）、释放回弹 spring 动画、首可见项保护
 * - 置顶区跨越自动检测
 * - 手指抬起提交排序，调用 onReorder
 *
 * **手势分层**：
 * - L1 库 longPressDraggableHandle：长按 + 拖拽
 * - L2 SwipeableTodoBox：拖拽激活时通过 isDragActive 禁用左滑
 * - L3 TodoListItem：检测 isDragActive=true 时让位
 *
 * @param items 列表数据
 * @param isDragEnabled 是否允许拖拽（批量模式/左滑展开时设为 false）
 * @param isDraggable 判断项是否可拖拽（用于分隔按钮等不可拖拽项），默认全部可拖拽
 * @param isPinned 获取项是否置顶的函数（用于跨越检测）
 * @param key 项的唯一标识
 * @param onReorder 排序提交回调 (fromIndex, toIndex, crossedPinnedZone)
 * @param listState LazyListState 实例，外部传入可读取滚动状态；默认内部创建
 * @param modifier Modifier
 * @param content 列表项 Composable，参数为 (index, item, isDragging, isDragActive)
 */
@Composable
fun <T> ReorderableLazyColumn(
    items: List<T>,
    isDragEnabled: Boolean,
    isDraggable: (T) -> Boolean = { true },
    isPinned: (T) -> Boolean,
    key: (T) -> Any,
    onReorder: (fromIndex: Int, toIndex: Int, crossedPinnedZone: Boolean) -> Unit,
    listState: LazyListState = rememberLazyListState(),
    modifier: Modifier = Modifier,
    content: @Composable (index: Int, item: T, isDragging: Boolean, isDragActive: Boolean) -> Unit
) {
    val context = LocalContext.current

    // ━━━ 拖拽状态（从 15+ 精简到 5 个）━━━
    var displayItems by remember { mutableStateOf(items) }
    var isDragActive by remember { mutableStateOf(false) }
    var draggedOriginalIndex by remember { mutableIntStateOf(-1) }
    var draggedOriginalIsPinned by remember { mutableStateOf(false) }
    var crossedPinnedZone by remember { mutableStateOf(false) }

    /** 触觉反馈节流时间戳 */
    var lastHapticTime by remember { mutableLongStateOf(0L) }
    val hapticThrottleMs = 200L

    // ━━━ 同步外部 items 变更 ━━━
    LaunchedEffect(items) {
        if (!isDragActive) {
            displayItems = items
        } else {
            // 拖拽中 items 被外部变更（如同步）→ 取消拖拽
            displayItems = items
            isDragActive = false
            draggedOriginalIndex = -1
        }
    }

    // ━━━ 主题色（虚线占位框）━━━
    val primaryColor = androidx.compose.material3.MaterialTheme.colorScheme.primary

    // ━━━ 创建库的 ReorderableLazyListState ━━━
    val reorderableState = rememberReorderableLazyListState(
        lazyListState = listState,
        onMove = { from, to ->
            // ① 校验 from/to 是否指向不可拖拽项（CompletedDivider）
            val fromItem = displayItems.getOrNull(from.index)
            val toItem = displayItems.getOrNull(to.index)
            if (fromItem == null || toItem == null) return@rememberReorderableLazyListState
            if (!isDraggable(fromItem) || !isDraggable(toItem)) {
                return@rememberReorderableLazyListState
            }

            // ② 重排 displayItems
            val newDisplay = displayItems.toMutableList()
            val draggedItem = newDisplay.removeAt(from.index)
            newDisplay.add(to.index, draggedItem)
            displayItems = newDisplay

            // ③ 触觉反馈（节流）
            val now = System.currentTimeMillis()
            if (now - lastHapticTime > hapticThrottleMs) {
                HapticFeedbackManager.performHapticFeedback(
                    context = context,
                    type = InteractionType.TEXT_MOVE,
                    enabled = true
                )
                lastHapticTime = now
            }
        }
    )

    LazyColumn(
        state = listState,
        modifier = modifier
    ) {
        itemsIndexed(
            items = displayItems,
            key = { _, item -> key(item) }
        ) { index, item ->
            ReorderableItem(
                state = reorderableState,
                key = key(item),
                enabled = isDragEnabled && isDraggable(item)  // 控制可重排序性
            ) { isDragging ->
                // longPressDraggableHandle 只需注册一次，库内部通过 isDragging 切换视觉
                Box(
                    modifier = Modifier
                        .longPressDraggableHandle(
                            enabled = isDragEnabled && isDraggable(item),
                            onDragStarted = {
                                isDragActive = true
                                draggedOriginalIndex = items.indexOfFirst { key(it) == key(item) }
                                draggedOriginalIsPinned = isPinned(item)
                                crossedPinnedZone = false
                                HapticFeedbackManager.performHapticFeedback(
                                    context = context,
                                    type = InteractionType.TEXT_MOVE,
                                    enabled = true
                                )
                            },
                            onDragStopped = {
                                // 提交排序 + 计算置顶区跨越（与原实现一致，松手时一次性计算）
                                val draggedCurrentIndex = displayItems.indexOfFirst {
                                    key(it) == key(item)
                                }
                                if (draggedOriginalIndex >= 0 &&
                                    draggedOriginalIndex != draggedCurrentIndex &&
                                    draggedCurrentIndex >= 0) {
                                    val displayPinned = displayItems.map { isPinned(it) }
                                    crossedPinnedZone = ReorderAlgorithms.checkPinnedZoneCrossed(
                                        displayItems = displayPinned,
                                        draggedOriginalIsPinned = draggedOriginalIsPinned,
                                        draggedCurrentIndex = draggedCurrentIndex
                                    )
                                    onReorder(
                                        draggedOriginalIndex,
                                        draggedCurrentIndex,
                                        crossedPinnedZone
                                    )
                                    HapticFeedbackManager.performHapticFeedback(
                                        context = context,
                                        type = InteractionType.CONFIRM,
                                        enabled = true
                                    )
                                }
                                isDragActive = false
                                draggedOriginalIndex = -1
                                crossedPinnedZone = false
                            }
                        )
                ) {
                    if (isDragging) {
                        // ━━━ 被拖项：虚线边框装饰 + 浮起卡片 ━━━
                        // 注意：库的 ReorderableItem 内部已通过 graphicsLayer.translationY
                        // 让整个 item 跟随手指，无需手动 offset
                        Box {
                            // 1. 虚线边框（跟随手指，作为被拖项的视觉标记）
                            Canvas(modifier = Modifier.matchParentSize()) {
                                drawRoundRect(
                                    color = primaryColor.copy(alpha = 0.4f),
                                    topLeft = Offset(1.dp.toPx(), 1.dp.toPx()),
                                    size = Size(
                                        this.size.width - 2.dp.toPx(),
                                        this.size.height - 2.dp.toPx()
                                    ),
                                    style = Stroke(
                                        width = 2.dp.toPx(),
                                        pathEffect = PathEffect.dashPathEffect(
                                            floatArrayOf(12f, 8f), 0f
                                        )
                                    ),
                                    cornerRadius = CornerRadius(16.dp.toPx())
                                )
                            }
                            // 2. 卡片内容（浮起效果：阴影 + 缩放 + 半透明）
                            Box(
                                modifier = Modifier.graphicsLayer {
                                    alpha = 0.9f
                                    scaleX = 1.05f
                                    scaleY = 1.05f
                                    shadowElevation = 8.dp.toPx()
                                }
                            ) {
                                content(index, item, true, true)
                            }
                        }
                    } else {
                        // ━━━ 普通项：animateItem 让位动画（库默认 spring）━━━
                        content(index, item, false, isDragActive)
                    }
                }
            }
        }
    }
}
```

**关键说明**：
- `longPressDraggableHandle` 的 `enabled` 参数直接使用（库源码 `draggable.kt:49-52` 确认支持）
- `longPressDraggableHandle` 只需注册一次，库内部通过 `isDragging` 状态切换视觉（被拖项的 `translationY` 由库内部 `ReorderableItem` 的 `offsetModifier` 处理）
- `crossedPinnedZone` 在 `onDragStopped` 中一次性计算（与原实现一致），而非 `onMove` 中实时计算
- 释放回弹动画由库内置（`previousDraggingItemOffset`），无需自研
- `onMove` 中 `from.index`/`to.index` 是 `LazyListItemInfo.index`（visibleItemsInfo 中的索引），与 `displayItems` 索引一致（因为 `itemsIndexed` 的 index 就是 displayItems 的索引）
- **视觉差异说明**：库的 `ReorderableItem` 内部通过 `graphicsLayer.translationY` 移动整个 item，因此虚线框会跟随手指移动（作为被拖项的边框装饰），而非原实现的"原位置占位"。这是引入库后的视觉变化，原型中会展示效果供确认

- [ ] **Step 3: 重写 ReorderableColumn 函数**

将现有的 `ReorderableColumn` 函数（约第 68-227 行，从 `/** 简化版可拖拽列表面向非 LazyColumn 场景` 到对应的闭合 `}`）替换为基于库的实现：

```kotlin
/**
 * 简化版可拖拽列表面向非 LazyColumn 场景（基于 Calvin-LL/Reorderable 库）
 *
 * 对于少量固定数量的列表项（如设置页面），
 * 使用库的 ReorderableColumn（基于 Column）实现拖拽排序。
 *
 * @param items 列表数据
 * @param onReorder 重排回调
 * @param modifier Modifier
 * @param content 列表项 Composable
 */
@Composable
fun <T> ReorderableColumn(
    items: List<T>,
    onReorder: (fromIndex: Int, toIndex: Int) -> Unit,
    modifier: Modifier = Modifier,
    content: @Composable (index: Int, item: T, isDragging: Boolean) -> Unit
) {
    val context = LocalContext.current

    sh.calvin.reorderable.ReorderableColumn(
        list = items,
        onSettle = { fromIndex, toIndex ->
            if (fromIndex != toIndex) {
                HapticFeedbackManager.performHapticFeedback(
                    context = context,
                    type = InteractionType.CONFIRM,
                    enabled = true
                )
                onReorder(fromIndex, toIndex)
            }
        },
        modifier = modifier,
    ) { index, item, isDragging ->
        ReorderableItem {
            content(index, item, isDragging)
        }
    }
}
```

**关键说明**：
- 库的 `ReorderableColumn` 参数名是 `list`，回调是 `onSettle`，与项目签名不同，此处做适配
- 库的 content 是 `ReorderableColumnScope.(index, item, isDragging)`，内部调用 `ReorderableItem { ... }` 注册拖拽手柄
- 项目 content 签名 `(index, item, isDragging)` 保持不变，InspirationEditScreen 零改动
- `ReorderableItem` 内部默认使用 `longPressDraggableHandle`（整卡可拖），如需手柄可在 content 内自行指定

- [ ] **Step 4: 检查 import 语句完整性**

确认以下 import 均已添加（Step 1 已包含），无缺失：

- `sh.calvin.reorderable.ReorderableItem`（LazyColumn 场景）
- `sh.calvin.reorderable.rememberReorderableLazyListState`
- `androidx.compose.material3.MaterialTheme`（Step 1 用了完整路径 `androidx.compose.material3.MaterialTheme.colorScheme.primary`，确认是否需要 import 或保持完整路径）

若 Step 1 的 import 区中未显式 import `MaterialTheme`，则代码中 `androidx.compose.material3.MaterialTheme.colorScheme.primary` 使用完整路径是合法的，无需修改。

- [ ] **Step 5: 编译验证**

```powershell
.\gradlew.bat :app:compileDebugKotlin
```

预期：编译成功，无错误。若报错，检查 import 缺失或 API 签名不匹配。

- [ ] **Step 6: 询问是否 git 提交**

向用户询问是否进行 git 提交。若提交，提交信息：
```
refactor: 重写 ReorderableLazyColumn 与 ReorderableColumn 基于 Calvin-LL/Reorderable 库
```

---

## Task 4: 验证 ReorderableColumn 与 InspirationEditScreen

**Files:**
- Read-only: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationEditScreen.kt`
- Read-only: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

- [ ] **Step 1: 确认 InspirationEditScreen 调用签名兼容**

读取 `InspirationEditScreen.kt`，确认其调用 `ReorderableColumn` 的签名与重构后一致：

```kotlin
ReorderableColumn(
    items = ...,
    onReorder = { fromIndex, toIndex -> ... },
    modifier = ...,
    content = { index, item, isDragging -> ... }
)
```

预期：签名完全匹配，零改动。

- [ ] **Step 2: 确认 HomeScreen 调用签名兼容**

读取 `HomeScreen.kt`，确认其调用 `ReorderableLazyColumn` 的签名与重构后一致：

```kotlin
ReorderableLazyColumn(
    items = displayItems,
    isDragEnabled = ...,
    isDraggable = { it is DisplayItem.Todo },
    isPinned = ...,
    key = ...,
    onReorder = { fromIndex, toIndex, crossed -> viewModel.reorderOnDisplayList(fromIndex, toIndex, crossed) },
    listState = ...,
    modifier = ...,
    content = { index, item, isDragging, isDragActive -> ... }
)
```

预期：签名完全匹配，零改动。

- [ ] **Step 3: 全量编译验证**

```powershell
.\gradlew.bat :app:compileDebugKotlin
```

预期：编译成功，无错误。

- [ ] **Step 4: 询问是否 git 提交**

向用户询问是否进行 git 提交。若提交，提交信息：
```
test: 验证 ReorderableColumn 与 InspirationEditScreen/HomeScreen 签名兼容
```

---

## Task 5: 全量回归测试

**Files:**
- Run: 单元测试 + 编译 + 手工验收

- [ ] **Step 1: 运行单元测试**

```powershell
.\gradlew.bat :app:testDebugUnitTest
```

预期：所有测试通过，包括 `ReorderAlgorithmsTest` 的 5 个 `checkPinnedZoneCrossed` 测试。

- [ ] **Step 2: 运行 lint 检查**

```powershell
.\gradlew.bat :app:lintDebug
```

预期：无致命错误（warning 可接受）。

- [ ] **Step 3: 构建 Debug APK**

```powershell
.\gradlew.bat :app:assembleDebug
```

预期：构建成功。

- [ ] **Step 4: 手工验收测试清单**

安装 APK 到设备/模拟器，逐项验证：

| 场景 | 操作 | 预期 |
|---|---|---|
| 问题1不复现 | 长按首项向下拖动 | 列表不失控滚动，卡片跟随手指 |
| 问题2不复现 | 长按第3项与首项交换 | 被拖卡片归位到首项位置，不上移飞出 |
| 底部边缘滚动 | 长按拖动到列表底部 | 平滑自动向下滚动，速度随距离递增 |
| 顶部边缘滚动 | 长按拖动到列表顶部 | 平滑自动向上滚动 |
| 置顶区跨越 | 拖拽跨越置顶区分界 | isPinned 自动切换，onReorder 传 crossedPinnedZone=true |
| 批量模式禁用 | 批量模式时长按 | 不触发拖拽 |
| 左滑展开禁用 | 左滑展开后长按 | 不触发拖拽 |
| CompletedDivider | 长按分隔按钮 | 不可拖拽，也不被挤动 |
| 释放回弹 | 松手 | 平滑弹簧归位动画（库内置） |
| InspirationEditScreen | 灵感编辑页拖拽排序 | ReorderableColumn 正常工作 |

- [ ] **Step 5: 询问是否 git 提交**

向用户询问是否进行 git 提交。若提交，提交信息：
```
test: 全量回归测试通过，拖拽排序重构完成
```

---

## Task 6: 实现多选合并拖拽

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt`（新增合并拖拽包装层）

**背景**：Calvin-LL/Reorderable 库仅支持单项拖拽，不原生支持多选合并拖拽。本 Task 在库之上自定义包装，实现多选模式下长按已选中项触发合并拖拽。参考设计文档第十三章与 HTML 原型 `docs/superpowers/prototypes/拖拽排序重构原型.html`。

**依赖**：Task 3（核心重写完成后才能在 `ReorderableLazyColumn` 之上包装合并拖拽）

- [ ] **Step 1: 扩展合并拖拽状态**

在 `ReorderableLazyColumn` 内部状态基础上（设计文档第五章），新增合并拖拽相关状态：

```kotlin
var isBatchMode by remember { mutableStateOf(false) }
var selectedIds by remember { mutableStateOf(setOf<Any>()) }
var isMergeDragging by remember { mutableStateOf(false) }
var mergeSelectedItems by remember { mutableStateOf(listOf<T>()) }
var mergeFingerY by remember { mutableStateOf(0f) }
```

| 状态 | 用途 | 对应原型变量 |
|---|---|---|
| `isBatchMode` | 多选模式开关 | `isBatchMode` |
| `selectedIds` | 已选中项 key 集合 | `selectedItems` |
| `isMergeDragging` | 合并拖拽进行中标志 | `isMergeDragging` |
| `mergeSelectedItems` | 合并拖拽中收集的选中项列表（按列表顺序） | `mergeSelectedItems` |
| `mergeFingerY` | 合并卡片跟随的手指 Y 坐标 | `draggedItem` fixed top |

- [ ] **Step 2: 实现多选模式管理函数**

新增三个函数，对应原型 `enterBatchMode` / `exitBatchMode` / `toggleSelection`（设计文档 13.2 交互流程前三行）：

```kotlin
/** 进入多选模式（非多选模式下长按 500ms + 抬手未移动触发） */
private fun enterBatchMode(itemId: Any) {
    isBatchMode = true
    selectedIds = setOf(itemId)
}

/** 退出多选模式（点击顶部栏关闭按钮） */
private fun exitBatchMode() {
    isBatchMode = false
    selectedIds = emptySet()
}

/** 切换选中状态（多选模式下点击卡片或勾选框） */
private fun toggleSelection(itemId: Any) {
    selectedIds = if (itemId in selectedIds) selectedIds - itemId
                  else selectedIds + itemId
}
```

- [ ] **Step 3: 实现手势分层**

合并拖拽期间禁用库的 `longPressDraggableHandle`，改用自定义 `pointerInput`（设计文档 13.3 手势分层）：

```kotlin
// 库的手柄启用条件：非多选模式，或多选模式下仅选中 1 项（回退普通拖拽）
val handleEnabled = isDragEnabled && isDraggable(item) &&
    (!isBatchMode || selectedIds.size <= 1)

val handleModifier = Modifier.longPressDraggableHandle(
    enabled = handleEnabled,
    onDragStarted = { /* 记录 draggedOriginalIndex; isDragActive = true */ },
    onDragStopped = { /* 提交 onReorder; isDragActive = false */ }
)

// 多选模式下额外绑定自定义 pointerInput 检测合并拖拽
val mergeDragModifier = if (isBatchMode && key(item) in selectedIds && selectedIds.size > 1) {
    Modifier.pointerInput(key(item)) {
        detectDragGesturesAfterLongPress(
            onDragStart = { startMergeDrag(item) },
            onDrag = { _, dragAmount -> mergeFingerY += dragAmount.y },
            onDragEnd = { endMergeDrag() },
            onDragCancel = { cancelMergeDrag() }
        )
    }
} else Modifier
```

- [ ] **Step 4: 实现 startMergeDrag**

对应原型 `startMergeDrag`（行 1667-1707）。收集选中项、插入占位框、折叠隐藏、创建合并卡片：

```kotlin
/** 开始合并拖拽：收集选中项 + 折叠隐藏 + 创建浮层合并卡片 */
private fun startMergeDrag(anchor: T) {
    isMergeDragging = true
    isDragActive = true
    // 1. 收集已选中项（按 displayItems 顺序，保留原相对顺序）
    mergeSelectedItems = displayItems.filter { key(it) in selectedIds }
    // 2. 触觉反馈（长按触发）
    hapticFeedback.performHapticFeedback(HapticFeedbackType.LongPress)
}
```

UI 层根据 `isMergeDragging` 渲染（对应原型步骤 3-5）：

1. **选中项折叠隐藏**：`Modifier.height(0.dp).alpha(0f)`，对应原型 `merge-hidden` CSS 类（max-height:0, opacity:0）
2. **anchor 位置渲染占位框**：单项高度虚线边框，对应原型 `placeholder`（仅代表合并组插入点，非组合总高度）
3. **浮层合并卡片**：`Box(Modifier.offset { IntOffset(0, mergeFingerY) })` 显示"选中 N 项"，对应原型 `merged-card`（脱离 LazyList，加入外层 Box 浮层）

- [ ] **Step 5: 实现拖拽中占位框定位 + 边界限制**

对应原型 `updatePlaceholderPosition`（行 1453-1506），含占位框边界限制（行 1481-1489，设计文档 13.5）。拖拽中需处理三件事（设计文档 13.3 Step 2）：

1. **合并卡片跟随手指**：由 Step 3 的 `mergeDragModifier` 中 `onDrag = { _, dragAmount -> mergeFingerY += dragAmount.y }` 处理
2. **占位框定位 + 边界限制**：复用 `updatePlaceholderPosition` 算法
3. **边缘自动滚动**：复用库的 scroller（合并拖拽期间手动驱动 `lazyListState.scrollBy`）

```kotlin
/** 拖拽中更新占位框位置（含置顶区边界限制） */
private fun updateMergePlaceholderPosition() {
    // 1. 收集所有可见项中心 Y（排除折叠隐藏项与占位框）
    // 2. 找到第一个中心 Y > mergeFingerY 的项作为插入点 insertBeforeIdx
    // 3. 占位框边界限制：任何待办的占位框都不能插入到"置顶"标签上方
    //    val pinnedLabelIdx = comparePoints.indexOfFirst { 它是"置顶" section-label }
    //    if (pinnedLabelIdx >= 0 && insertBeforeIdx in 0..pinnedLabelIdx) {
    //        insertBeforeIdx = pinnedLabelIdx + 1
    //    }
    // 4. 非置顶项仍可拖入置顶区（拖入后由 updateItemPinnedState 自动切换为置顶项）
}
```

**占位框边界限制算法**（设计文档 13.5）：

1. 收集所有可比较子元素（todo-item、section-label、completed-divider）
2. 找到第一个中心 Y > 被拖项中心 Y 的元素作为插入点
3. 边界限制：若插入点在"置顶"标签之前或就是它，强制限制到"置顶"标签之后
4. 非置顶项仍可拖入置顶区（拖入后由 `updateItemPinnedState` 自动切换为置顶项）

- [ ] **Step 6: 实现 endMergeDrag（方案 C：transform 补偿同步释放）**

对应原型 `endDrag` 合并分支（行 1545-1608）。释放时原项展开会引发布局跳动，采用 FLIP 变体消除：

```kotlin
/** 结束合并拖拽：cross-fade + transform 补偿同步释放 */
private fun endMergeDrag() {
    val N = mergeSelectedItems.size
    val itemHeightPx = ... // 单项完整高度（含 margin，从占位框量取）
    // 原项展开后多出的高度：(N-1) × 单项完整高度
    val offset = (N - 1) * itemHeightPx

    scope.launch {
        // 1. 合并卡片弹簧动画到占位框位置 + 淡出（0.35s）
        // 2. 立即将原项插入到占位框位置 + 取消折叠隐藏（alpha 锁定为 0）
        // 3. 移除占位框
        // 4. 给后续项加 graphicsLayer { translationY = -offset } 抵消原项展开位移
        // 5. 用 Animatable 替代原型的双 rAF，同步启动：
        val alphaAnim = Animatable(0f)
        val offsetAnim = Animatable(-offset)
        launch { alphaAnim.animateTo(1f, spring(dampingRatio = 0.7f)) }  // 原项淡入 0.2s
        launch { offsetAnim.animateTo(0f, spring()) }                     // 后续项下移 0.3s
        // 6. cross-fade：合并卡片淡出 ↔ 原项淡入同步启动
    }
}
```

**Compose 与原型对应**（设计文档 13.6）：

| HTML 原型 | Compose 实现 |
|---|---|
| `merge-hidden`（max-height:0, opacity:0, !important） | `Modifier.height(0.dp).alpha(0f)` 或 `AnimatedVisibility` |
| 合并卡片 `position:fixed` 跟随手指 | `Box(Modifier.offset { IntOffset(0, mergeFingerY) })` 浮层 |
| `updatePlaceholderPosition` 比较中心 Y | 自研位置计算（LazyList visibleItemsInfo） |
| `translateY(-offset)` transform 补偿 | `Modifier.graphicsLayer { translationY = -offset }` |
| 双 rAF 清除 inline opacity | `Animatable.animateTo`（无 `!important` 冲突） |
| cross-fade | `AnimatedVisibility` / `Crossfade` |
| `suppressNextClick` 捕获阶段拦截 | 无需（Compose 无 click 冒泡问题） |

- [ ] **Step 7: 实现 cleanupMergeDrag**

对应原型 `cleanupMergeDrag`（行 1717-1743）：

```kotlin
/** 清理合并拖拽（弹簧动画结束后调用） */
private fun cleanupMergeDrag() {
    // 1. 移除合并卡片（占位框已在 endMergeDrag 中提前移除）
    // 2. 按新位置刷新完成/置顶状态与分区计数
    mergeSelectedItems.forEach { item ->
        // updateItemZoneState(item); updateItemPinnedState(item)
    }
    updateZoneCounts()
    // 3. 重置合并拖拽状态（保留多选模式，仍处于批量态）
    isMergeDragging = false
    isDragActive = false
    mergeSelectedItems = emptyList()
}
```

- [ ] **Step 8: 实现单选回退**

`selectedIds.size == 1` 时，长按已选中项回退为普通单项拖拽（避免"选中 1 项"合并卡片突兀，设计文档 13.4 关键技术点）。Step 3 的 `handleEnabled` 已处理此逻辑：

```kotlin
// 多选模式下仅选中 1 项时，库的 longPressDraggableHandle 仍启用
val handleEnabled = isDragEnabled && isDraggable(item) &&
    (!isBatchMode || selectedIds.size <= 1)
```

- [ ] **Step 9: 编译验证 + 询问 git 提交**

```powershell
.\gradlew.bat :app:compileDebugKotlin
```

预期：编译成功，无错误。

向用户询问是否进行 git 提交。若提交，提交信息：
```
feat: 实现多选合并拖拽（库之上自定义包装）
```

---

## 实施依赖关系

```
Task 1（依赖）──→ Task 2（独立，可并行）──→ Task 3（核心，依赖 Task 1+2）
                                              ↓
                                         Task 4（验证，依赖 Task 3）
                                              ↓
                                         Task 5（回归，依赖 Task 4）
                                              ↓
                                         Task 6（增量，依赖 Task 3：合并拖拽包装）
```

## 测试策略总结

| 层级 | 范围 | 工具 |
|---|---|---|
| 单元测试 | `checkPinnedZoneCrossed` 纯函数 | JUnit4 |
| 集成测试 | 编译验证 + 签名兼容 | `compileDebugKotlin` |
| 性能测试 | 长列表拖拽流畅度（手工） | 设备实测 60fps |
| 验收测试 | 10 个手工场景 | 设备实测 |

## 风险检查清单

- [ ] `longPressDraggableHandle(enabled=...)` 参数确认可用（源码 `draggable.kt:49-52`）
- [ ] `onMove` 的 `from.index`/`to.index` 与 `displayItems` 索引一致（`itemsIndexed` 保证）
- [ ] `ReorderableItem.enabled` 与 `longPressDraggableHandle.enabled` 双重设置（语义：可重排序 + 可拖动）
- [ ] 释放回弹动画由库内置（`previousDraggingItemOffset`），不自研
- [ ] `ReorderableColumn` 适配层正确传递 `onSettle` → `onReorder`
