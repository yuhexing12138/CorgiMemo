# 待办编辑页撤销/恢复功能 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将待办编辑页（TodoEditScreen）的撤销/恢复功能从现有未激活的双栈架构重构为统一全量快照栈，并接入 UI 层使用户可操作。

**Architecture:** 移除 ViewModel 中现有的纯文本栈 + 扩展栈双栈架构，替换为 `EditSnapshot` 全量快照 + `UndoRedoStack` 单栈管理器。每个 setter 方法在变更前推入快照，文本编辑使用防抖机制。UI 层通过已预留的 `canUndo/canRedo/onUndoClick/onRedoClick` 参数激活撤销/恢复按钮。

**Tech Stack:** Kotlin, Jetpack Compose, StateFlow, Hilt ViewModel

---

## 文件结构

| 操作 | 文件路径 | 职责 |
|------|---------|------|
| 修改 | `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt` | 核心：添加 EditSnapshot + UndoRedoStack，移除双栈，改造所有 setter |
| 修改 | `app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt` | UI：收集 canUndo/canRedo，传入 CheckboxEditText 参数，监听恢复事件 |
| 修改 | `app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt` | 传递 canUndo/canRedo/onUndoClick/onRedoClick 到 TodoGroupContainer |
| 不变 | `app/src/main/java/com/corgimemo/app/ui/components/EditToolbar.kt` | 不修改（不在底部工具栏加撤销/恢复按钮） |

---

### Task 1: 添加 EditSnapshot 数据类和 UndoRedoStack 栈管理器

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt`

- [ ] **Step 1: 在 TodoEditViewModel 文件顶部（class 定义之前）添加 EditSnapshot 数据类和 UndoRedoStack 类**

在 `TodoEditViewModel` 类定义之前（`EditOperation` 密封类附近，行 213 之前），添加以下代码：

```kotlin
/**
 * 编辑状态全量快照，用于撤销/恢复功能
 * 记录编辑页某一时刻的所有可编辑字段状态（排除背景色）
 */
data class EditSnapshot(
    // 文本内容
    val title: String,
    val content: String,
    val contentFormat: String,
    val subTasks: List<SubTask>,
    val contentBlocks: List<ContentBlock>,

    // 单值元数据
    val startDate: Long?,
    val dueDate: Long?,

    // 位置
    val geofenceLat: Double,
    val geofenceLng: Double,
    val geofenceRadius: Float,
    val geofenceType: Int,
    val geofenceEnabled: Boolean,
    val geofenceAddress: String,

    // 附件
    val imagePaths: List<String>,
    val voiceNotePath: String?,
    val voiceDuration: Int?,
    val lineAttachmentsSnapshot: String?,

    // 多分组字段
    val groupCategoryIds: Map<Int, Long>,
    val groupPriorities: Map<Int, Int>,
    val groupReminders: Map<Int, Long?>,
    val groupRepeatTypes: Map<Int, Int>,
    val groupRelations: Map<Int, List<CardRelation>>,
)

/**
 * 撤销/恢复栈管理器
 * 维护两个 ArrayDeque，深度限制 MAX_SNAPSHOT_DEPTH = 50
 */
class UndoRedoStack(
    private val maxDepth: Int = MAX_SNAPSHOT_DEPTH
) {
    private val undoStack = ArrayDeque<EditSnapshot>(maxDepth)
    private val redoStack = ArrayDeque<EditSnapshot>(maxDepth)

    val canUndo: Boolean get() = undoStack.isNotEmpty()
    val canRedo: Boolean get() = redoStack.isNotEmpty()

    /** 推入快照到撤销栈，清空恢复栈 */
    fun push(snapshot: EditSnapshot) {
        if (undoStack.size >= maxDepth) {
            undoStack.removeFirst()
        }
        undoStack.addLast(snapshot)
        redoStack.clear()
    }

    /** 撤销：当前状态入恢复栈，弹出撤销栈顶 */
    fun undo(current: EditSnapshot): EditSnapshot? {
        if (undoStack.isEmpty()) return null
        redoStack.addLast(current)
        return undoStack.removeLast()
    }

    /** 恢复：当前状态入撤销栈，弹出恢复栈顶 */
    fun redo(current: EditSnapshot): EditSnapshot? {
        if (redoStack.isEmpty()) return null
        undoStack.addLast(current)
        return redoStack.removeLast()
    }

    /** 清空所有栈 */
    fun clear() {
        undoStack.clear()
        redoStack.clear()
    }

    companion object {
        const val MAX_SNAPSHOT_DEPTH = 50
    }
}
```

- [ ] **Step 2: 提交**

```
git add app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt
git commit -m "feat: 添加 EditSnapshot 数据类和 UndoRedoStack 栈管理器"
```

---

### Task 2: 移除现有的双栈架构代码

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt`

- [ ] **Step 1: 移除 EditOperation 密封类（行 213-222）**

删除以下代码：

```kotlin
sealed class EditOperation {
    data class TextChange(val before: AnnotatedString) : EditOperation()
    data class BlockDeleted(val blocks: List<ContentBlock>, val index: Int) : EditOperation()
    data class BlockInserted(val index: Int) : EditOperation()
    data class BlocksReordered(val oldOrder: List<ContentBlock>) : EditOperation()
}
```

- [ ] **Step 2: 移除扩展双栈声明（行 225-228）**

删除以下代码：

```kotlin
private val _undoStackExtended = ArrayDeque<EditOperation>()
private val _redoStackExtended = ArrayDeque<EditOperation>()
```

- [ ] **Step 3: 移除批量合并机制变量（行 230-255）**

删除以下变量和常量：
- `MERGE_WINDOW_MS = 500L`
- `lastOperationTime`
- `pendingMergeOp`
- `EXTENDED_STACK_MAX_SIZE_BYTES`
- 以及相关的大小计算逻辑

- [ ] **Step 4: 移除纯文本双栈声明（行 381-390）**

删除以下代码：

```kotlin
private val _undoStack = ArrayDeque<AnnotatedString>()
private val _redoStack = ArrayDeque<AnnotatedString>()
private val MAX_UNDO_DEPTH = 50
```

- [ ] **Step 5: 移除所有 push 操作方法**

删除以下方法：
- `commitPendingMerge()`
- `trimExtendedStackIfNeeded()`
- `pushBlockDeletedOperation()`
- `pushBlockInsertedOperation()`
- `pushBlocksReorderedOperation()`
- `pushSnapshot(currentText)` （纯文本栈推送，行 2046-2077）

- [ ] **Step 6: 移除所有旧的 undo/redo 方法**

删除以下方法：
- `undoExtended(): Any?`
- `redoExtended(): Any?`
- `undo(): AnnotatedString?` （纯文本栈撤销）
- `pushToRedo(currentText)`
- `redo(): AnnotatedString?` （纯文本栈重做）
- `undoToHistoryStep(steps)`
- `redoToHistoryStep(steps)`
- `getUndoHistoryDescriptions()`
- `getRedoHistoryDescriptions()`

- [ ] **Step 7: 移除 DataStore 持久化相关代码**

删除以下方法：
- `persistUndoRedoStacksAsync()`
- `restoreUndoStacks(todoId)`
- `serializeEditOperations()`
- `deserializeEditOperations()`
- `serializeContentBlock()`
- `deserializeContentBlock()`
- `AnnotatedStringSerializer` 对象

同时删除 `loadTodo()` 中的 `restoreUndoStacks(todoId)` 调用（行 731 附近）。

删除 `saveGroup()` 中的 `corgiPreferences.clearUndoRedoStacks()` 调用（行 956 附近）。

- [ ] **Step 8: 提交**

```
git add app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt
git commit -m "refactor: 移除现有双栈撤销/恢复架构代码"
```

---

### Task 3: 在 ViewModel 中接入新的 UndoRedoStack

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt`

- [ ] **Step 1: 添加 UndoRedoStack 实例和防循环标志**

在 ViewModel 类体内（原来 `_undoStack` / `_redoStack` 的位置，行 381 附近），添加：

```kotlin
/** 统一撤销/恢复栈管理器 */
private val undoRedoStack = UndoRedoStack()

/** 防循环标志：恢复快照时为 true，阻止 LaunchedEffect 推入新快照 */
private val _isRestoring = MutableStateFlow(false)
val isRestoring: StateFlow<Boolean> = _isRestoring.asStateFlow()

/** 恢复事件：通知 UI 层重建 todoLines */
private val _restoreEvent = MutableStateFlow<EditSnapshot?>(null)
val restoreEvent: StateFlow<EditSnapshot?> = _restoreEvent.asStateFlow()
```

- [ ] **Step 2: 添加 currentSnapshot 属性**

```kotlin
/** 从当前 StateFlow 值构建全量快照 */
private val currentSnapshot: EditSnapshot
    get() = EditSnapshot(
        title = _title.value,
        content = _content.value,
        contentFormat = _contentFormat.value,
        subTasks = _subTasks.value,
        contentBlocks = _currentContentBlocks.value,
        startDate = _startDate.value,
        dueDate = _dueDate.value,
        geofenceLat = _geofenceLat.value,
        geofenceLng = _geofenceLng.value,
        geofenceRadius = _geofenceRadius.value,
        geofenceType = _geofenceType.value,
        geofenceEnabled = _geofenceEnabled.value,
        geofenceAddress = _geofenceAddress.value,
        imagePaths = _imagePaths.value,
        voiceNotePath = _voiceNotePath.value,
        voiceDuration = _voiceDuration.value,
        lineAttachmentsSnapshot = _lineAttachmentsSnapshot.value,
        groupCategoryIds = _groupCategoryIds.value,
        groupPriorities = _groupPriorities.value,
        groupReminders = _groupReminders.value,
        groupRepeatTypes = _groupRepeatTypes.value,
        groupRelations = _groupRelations.value,
    )
```

- [ ] **Step 3: 添加 pushSnapshot 和 pushSnapshotDebounced 方法**

```kotlin
private var debounceJob: Job? = null
private var isDebouncing = false

/** 带防抖的快照推入（用于文本编辑） */
fun pushSnapshotDebounced() {
    if (isDebouncing) return  // 冷却期内，不推入
    if (_isRestoring.value) return  // 恢复中，不推入
    isDebouncing = true
    pushSnapshot()  // 立即推入当前快照
    debounceJob?.cancel()
    debounceJob = viewModelScope.launch {
        delay(500)
        isDebouncing = false  // 冷却期结束
    }
}

/** 即时推入当前快照（用于元数据变更） */
fun pushSnapshot() {
    if (_isRestoring.value) return  // 恢复中，不推入
    undoRedoStack.push(currentSnapshot)
    _canUndo.value = undoRedoStack.canUndo
    _canRedo.value = undoRedoStack.canRedo
}
```

- [ ] **Step 4: 添加 restoreFromSnapshot 方法**

```kotlin
/** 从快照恢复编辑状态，将所有字段值写回 StateFlow */
private fun restoreFromSnapshot(snapshot: EditSnapshot) {
    _title.value = snapshot.title
    _content.value = snapshot.content
    _contentFormat.value = snapshot.contentFormat
    _subTasks.value = snapshot.subTasks
    _currentContentBlocks.value = snapshot.contentBlocks
    _startDate.value = snapshot.startDate
    _dueDate.value = snapshot.dueDate
    _geofenceLat.value = snapshot.geofenceLat
    _geofenceLng.value = snapshot.geofenceLng
    _geofenceRadius.value = snapshot.geofenceRadius
    _geofenceType.value = snapshot.geofenceType
    _geofenceEnabled.value = snapshot.geofenceEnabled
    _geofenceAddress.value = snapshot.geofenceAddress
    _imagePaths.value = snapshot.imagePaths
    _voiceNotePath.value = snapshot.voiceNotePath
    _voiceDuration.value = snapshot.voiceDuration
    _lineAttachmentsSnapshot.value = snapshot.lineAttachmentsSnapshot
    _groupCategoryIds.value = snapshot.groupCategoryIds
    _groupPriorities.value = snapshot.groupPriorities
    _groupReminders.value = snapshot.groupReminders
    _groupRepeatTypes.value = snapshot.groupRepeatTypes
    _groupRelations.value = snapshot.groupRelations

    // 通知 UI 层重建 todoLines
    _restoreEvent.value = snapshot
}
```

- [ ] **Step 5: 添加新的 undo/redo 方法**

```kotlin
/** 撤销上一次操作 */
fun undo() {
    val current = currentSnapshot
    val previous = undoRedoStack.undo(current) ?: return
    _isRestoring.value = true
    restoreFromSnapshot(previous)
    _isRestoring.value = false
    _canUndo.value = undoRedoStack.canUndo
    _canRedo.value = undoRedoStack.canRedo
}

/** 恢复上一次撤销的操作 */
fun redo() {
    val current = currentSnapshot
    val next = undoRedoStack.redo(current) ?: return
    _isRestoring.value = true
    restoreFromSnapshot(next)
    _isRestoring.value = false
    _canUndo.value = undoRedoStack.canUndo
    _canRedo.value = undoRedoStack.canRedo
}
```

- [ ] **Step 6: 在 loadTodo() 末尾推入初始快照**

在 `loadTodo()` 方法的末尾（`_isLoaded.value = true` 之后），添加：

```kotlin
// 加载完成后推入初始快照，作为撤销的基准点
undoRedoStack.clear()
pushSnapshot()
```

- [ ] **Step 7: 在 clearCardDetail() 中清空栈**

在 `clearCardDetail()` 方法中添加：

```kotlin
undoRedoStack.clear()
_canUndo.value = false
_canRedo.value = false
```

- [ ] **Step 8: 提交**

```
git add app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt
git commit -m "feat: 接入统一全量快照栈，添加 pushSnapshot/undo/redo/restoreFromSnapshot"
```

---

### Task 4: 改造所有 setter 方法，在变更前推入快照

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt`

- [ ] **Step 1: 改造元数据 setter 方法**

以下每个方法的第一行添加 `pushSnapshot()` 调用：

| 方法 | 行号 | 改造说明 |
|------|------|---------|
| `setGroupCategory(groupId, categoryId)` | 475 | 方法体首行添加 `pushSnapshot()` |
| `clearGroupCategory(groupId)` | 484 | 方法体首行添加 `pushSnapshot()` |
| `setPriority(priority)` | 488 | 方法体首行添加 `pushSnapshot()` |
| `setStartDate(startDate)` | 492 | 方法体首行添加 `pushSnapshot()` |
| `setDueDate(dueDate)` | 502 | 方法体首行添加 `pushSnapshot()` |
| `setGroupReminder(groupId, time)` | 525 | 方法体首行添加 `pushSnapshot()` |
| `clearGroupReminder(groupId)` | 536 | 方法体首行添加 `pushSnapshot()` |
| `setGroupRepeatType(groupId, type)` | 556 | 方法体首行添加 `pushSnapshot()` |
| `setGeofenceLat(lat)` | 571 | 方法体首行添加 `pushSnapshot()` |
| `setGeofenceLng(lng)` | 575 | 方法体首行添加 `pushSnapshot()` |
| `setGeofenceRadius(radius)` | 579 | 方法体首行添加 `pushSnapshot()` |
| `setGeofenceType(type)` | 583 | 方法体首行添加 `pushSnapshot()` |
| `setGeofenceEnabled(enabled)` | 587 | 方法体首行添加 `pushSnapshot()` |
| `setGeofenceAddress(address)` | 591 | 方法体首行添加 `pushSnapshot()` |
| `setGroupPriority(groupId, priority)` | 1259 | 方法体首行添加 `pushSnapshot()` |
| `setVoiceNote(path, duration)` | 1545 | 方法体首行添加 `pushSnapshot()` |
| `clearVoiceNote()` | 1553 | 方法体首行添加 `pushSnapshot()` |
| `addImagePath(path)` | 1565 | 方法体首行添加 `pushSnapshot()` |
| `removeImagePath(path)` | 1579 | 方法体首行添加 `pushSnapshot()` |
| `reorderImagePaths(newPaths)` | 1595 | 方法体首行添加 `pushSnapshot()` |
| `setImagePaths(paths)` | 1610 | 方法体首行添加 `pushSnapshot()` |
| `setVoiceNotePath(path)` | 1621 | 方法体首行添加 `pushSnapshot()` |
| `clearImagePaths()` | 1629 | 方法体首行添加 `pushSnapshot()` |
| `setContentFormat(markdown)` | 1990 | 方法体首行添加 `pushSnapshot()` |

改造示例（以 `setGroupCategory` 为例）：

```kotlin
// 改造前
fun setGroupCategory(groupId: Int, categoryId: Long) {
    _groupCategoryIds.value = _groupCategoryIds.value.toMutableMap().apply {
        this[groupId] = categoryId
    }
    checkAndResetGroupSavedState()
}

// 改造后
fun setGroupCategory(groupId: Int, categoryId: Long) {
    pushSnapshot()
    _groupCategoryIds.value = _groupCategoryIds.value.toMutableMap().apply {
        this[groupId] = categoryId
    }
    checkAndResetGroupSavedState()
}
```

**注意**：以下方法**不需要**添加 `pushSnapshot()`：
- `setTitle(title)` — 文本编辑通过 `pushSnapshotDebounced()` 处理
- `setContent(content)` — 文本编辑通过 `pushSnapshotDebounced()` 处理
- `replaceSubTasks(newSubTasks)` — 文本编辑通过 `pushSnapshotDebounced()` 处理
- `addSubTask(title)` / `removeSubTask(subTask)` / `toggleSubTaskCompletion(subTask)` — 通过 `todoLines` 变化触发 `pushSnapshotDebounced()`
- `setBackgroundColor(colorInt)` — 背景色不需要撤销
- `syncContentBlocks(blocks)` — 内容块通过 `todoLines` 变化触发
- `setEstimatedDurationMinutes(minutes)` — 派生值，跟随 `dueDate`/`startDate` 变化
- `scheduleFormatExport(annotatedString)` — 导出操作，不是用户编辑

- [ ] **Step 2: 处理关联变更**

关联变更通过以下路径发生：
1. `RelationListViewModel` 调用 `cardRelationRepository` 删除关联
2. `TodoEditScreen` 的 `onRemoveRelation` 回调
3. `HomeViewModel.refreshRelationCountsOnDemand()` 刷新计数

在 `TodoEditScreen` 中，关联变更后需要调用 `viewModel.pushSnapshot()` 来记录。具体位置：
- 在 `RelationListBottomSheet` 的 `onRemoveRelation` 回调中，调用 `viewModel.pushSnapshot()` 后再执行删除
- 在 `onAddRelation` 回调中（关联选择器确认后），先 `viewModel.pushSnapshot()` 再更新 `_groupRelations`

由于关联的更新逻辑涉及 `groupRelations` StateFlow 的修改，需要在关联变更的 ViewModel 方法中添加 `pushSnapshot()`。查看 `_groupRelations` 的写入位置，在写入前添加 `pushSnapshot()`。

- [ ] **Step 3: 提交**

```
git add app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt
git commit -m "feat: 所有元数据 setter 方法在变更前推入快照"
```

---

### Task 5: 改造 TodoEditScreen — 接入撤销/恢复 UI

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt`

- [ ] **Step 1: 添加 canUndo/canRedo 状态收集**

在 TodoEditScreen 的 Composable 函数中（其他 `collectAsState()` 附近），添加：

```kotlin
val canUndo by viewModel.canUndo.collectAsState()
val canRedo by viewModel.canRedo.collectAsState()
```

- [ ] **Step 2: 添加 isRestoring 和 restoreEvent 状态收集**

```kotlin
val isRestoring by viewModel.isRestoring.collectAsState()
val restoreEvent by viewModel.restoreEvent.collectAsState()
```

- [ ] **Step 3: 在 CheckboxEditText 调用中传入撤销/恢复参数**

找到 `CheckboxEditText` 的调用位置（行 1386-1610 附近），在参数列表中添加：

```kotlin
CheckboxEditText(
    // ... 现有参数 ...
    canUndo = canUndo,
    canRedo = canRedo,
    onUndoClick = { viewModel.undo() },
    onRedoClick = { viewModel.redo() },
    // ... 其余参数 ...
)
```

- [ ] **Step 4: 修改 LaunchedEffect(todoLines) 中的防循环逻辑**

在 `LaunchedEffect(todoLines)` 开头（行 796 附近），将快照推入逻辑改为防抖模式：

```kotlin
LaunchedEffect(todoLines) {
    if (hasInitializedLines) {
        // 防循环：恢复快照时不推入新快照
        if (!isRestoring) {
            viewModel.pushSnapshotDebounced()
        }

        // 以下现有逻辑保持不变
        val plainText = todoLines
            .filter { it.text.isNotBlank() || todoLines.size == 1 }
            .joinToString("\n") { it.toPlainText() }

        viewModel.setContent(plainText)
        // ... 其余同步逻辑 ...
    }
}
```

- [ ] **Step 5: 添加 restoreEvent 监听，重建 todoLines**

在 `LaunchedEffect(todoLines)` 之后，添加一个新的 `LaunchedEffect`：

```kotlin
/** 监听恢复事件，从快照重建 todoLines */
LaunchedEffect(restoreEvent) {
    restoreEvent?.let { snapshot ->
        // 从快照重建 todoLines
        val attachmentMap = com.corgimemo.app.ui.model.LineSnapshotUtils
            .deserialize(snapshot.lineAttachmentsSnapshot)

        val titleLine = com.corgimemo.app.ui.model.TodoLine(
            text = snapshot.title,
            isChecked = false,
            isTitle = true,
            imagePaths = attachmentMap?.get(0)?.imagePaths ?: snapshot.imagePaths,
            voiceAttachments = attachmentMap?.get(0)?.voiceAttachments
                ?: snapshot.voiceNotePath?.let { listOf(it) } ?: emptyList(),
        )

        val subTaskLines = snapshot.subTasks.mapIndexed { index, subTask ->
            val lineIndex = index + 1
            com.corgimemo.app.ui.model.TodoLine(
                text = subTask.title,
                isChecked = subTask.isCompleted,
                isTitle = false,
                subTaskId = subTask.id,
                order = subTask.order,
                imagePaths = attachmentMap?.get(lineIndex)?.imagePaths ?: emptyList(),
                voiceAttachments = attachmentMap?.get(lineIndex)?.voiceAttachments ?: emptyList(),
            )
        }

        todoLines = listOf(titleLine) + subTaskLines
    }
}
```

**注意**：`LineSnapshotUtils.deserialize()` 的方法签名需要根据实际代码调整。如果该方法不存在，需要新增。参考 `LineSnapshotUtils.fromTodoLines()` 和 `LineSnapshotUtils.serialize()` 的实现来编写反序列化方法。

- [ ] **Step 6: 提交**

```
git add app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt
git commit -m "feat: TodoEditScreen 接入撤销/恢复 UI，添加防循环和恢复事件监听"
```

---

### Task 6: 改造 CheckboxEditText — 传递撤销/恢复参数到 TodoGroupContainer

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt`

- [ ] **Step 1: 确认 CheckboxEditText 的参数列表已有 canUndo/canRedo/onUndoClick/onRedoClick**

`CheckboxEditText` 已有这些参数定义（行 1386-1610 附近调用时已有参数）。确认这些参数存在。

- [ ] **Step 2: 在两处 TodoGroupContainer 调用中传入撤销/恢复参数**

**第一处（空状态占位，行 244-263）**：

在 `TodoGroupContainer(` 调用中添加：

```kotlin
TodoGroupContainer(
    groupId = 0,
    showBottomBar = true,
    // ... 现有参数 ...
    canUndo = canUndo,
    canRedo = canRedo,
    onUndoClick = onUndoClick,
    onRedoClick = onRedoClick,
    // ... 关联参数 ...
)
```

**第二处（正常分组，行 313-333）**：

```kotlin
TodoGroupContainer(
    groupId = groupId,
    showBottomBar = true,
    // ... 现有参数 ...
    canUndo = canUndo,
    canRedo = canRedo,
    onUndoClick = onUndoClick,
    onRedoClick = onRedoClick,
    // ... 关联参数 ...
)
```

- [ ] **Step 3: 提交**

```
git add app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt
git commit -m "feat: CheckboxEditText 传递撤销/恢复参数到 TodoGroupContainer"
```

---

### Task 7: 添加 LineSnapshotUtils 反序列化方法（如需要）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/model/LineSnapshotUtils.kt`（如果存在）

- [ ] **Step 1: 检查 LineSnapshotUtils 是否有反序列化方法**

查看 `LineSnapshotUtils` 的现有方法。如果已有 `deserialize()` 方法，则跳过此 Task。

如果只有 `fromTodoLines()` 和 `serialize()` 而没有反序列化，则需要添加：

```kotlin
/**
 * 从 JSON 字符串反序列化行级附件快照
 * @param json 行级附件快照 JSON 字符串
 * @return 行索引到附件的映射，null 表示 JSON 为空或解析失败
 */
fun deserialize(json: String?): Map<Int, LineAttachmentSnapshot>? {
    if (json.isNullOrBlank()) return null
    return try {
        val jsonArray = JSONArray(json)
        val map = mutableMapOf<Int, LineAttachmentSnapshot>()
        for (i in 0 until jsonArray.length()) {
            val obj = jsonArray.getJSONObject(i)
            val lineIndex = obj.getInt("lineIndex")
            val imagePaths = obj.optJSONArray("imagePaths")?.let { arr ->
                (0 until arr.length()).map { arr.getString(it) }
            } ?: emptyList()
            val voiceAttachments = obj.optJSONArray("voiceAttachments")?.let { arr ->
                (0 until arr.length()).map { arr.getString(it) }
            } ?: emptyList()
            map[lineIndex] = LineAttachmentSnapshot(
                imagePaths = imagePaths,
                voiceAttachments = voiceAttachments,
            )
        }
        map
    } catch (e: Exception) {
        null
    }
}
```

**注意**：`LineAttachmentSnapshot` 的字段名和 `serialize()` 的输出格式需要匹配。实际实现时需对照 `serialize()` 方法确认 JSON 结构。

- [ ] **Step 2: 提交**

```
git add app/src/main/java/com/corgimemo/app/ui/model/LineSnapshotUtils.kt
git commit -m "feat: 添加 LineSnapshotUtils 反序列化方法"
```

---

### Task 8: 清理 CorgiPreferences 中的 undo/redo 持久化方法

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/data/preferences/CorgiPreferences.kt`（如果存在）

- [ ] **Step 1: 检查 CorgiPreferences 中是否有 undo/redo 相关方法**

搜索以下方法名：
- `appendUndoLogEntry` / `appendRedoLogEntry`
- `appendExtendedUndoLog` / `appendExtendedRedoLog`
- `getUndoLog` / `getRedoLog`
- `getExtendedUndoLog` / `getExtendedRedoLog`
- `clearUndoRedoStacks`
- `migrateLegacyUndoLogIfPresent`

如果存在，由于我们改为仅会话内（不持久化），这些方法不再被调用。但为安全起见，**保留这些方法不删除**，避免影响灵感编辑页可能的使用。仅在 `TodoEditViewModel` 中确保不再调用即可。

如果这些方法仅被 `TodoEditViewModel` 使用，可以考虑删除。但需要确认 `InspirationEditViewModel` 是否也使用了这些方法。

- [ ] **Step 2: 提交（如有修改）**

```
git add app/src/main/java/com/corgimemo/app/data/preferences/CorgiPreferences.kt
git commit -m "refactor: 清理不再需要的 undo/redo 持久化方法（如果确认仅 TodoEditVM 使用）"
```

---

### Task 9: 处理 SubTask 数据模型的字段名差异

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt`
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt`

- [ ] **Step 1: 确认 SubTask 数据类的字段名**

在 CorgiMemo 项目中，`SubTask` 数据类可能有以下字段之一：
- `content` (设计文档中使用)
- `title` (实际代码中可能使用)

查看 `SubTask` 数据类定义，确认其字段名。在 `EditSnapshot` 和所有引用 `SubTask` 的代码中使用正确的字段名。

如果字段名是 `title` 而非 `content`，需要修改 `EditSnapshot` 中的注释以及 `buildTodoLinesFromSnapshot` 中的引用。

- [ ] **Step 2: 修正所有 SubTask 字段引用**

确保所有代码中 `subTask.content` 或 `subTask.title` 与实际数据类定义一致。

- [ ] **Step 3: 提交（如有修改）**

```
git add app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt
git commit -m "fix: 修正 SubTask 字段名引用与数据类定义一致"
```

---

### Task 10: 编译验证

**Files:**
- 无新文件

- [ ] **Step 1: 询问用户是否进行编译验证**

根据项目规则，编译前必须询问用户。向用户确认是否执行 `./gradlew assembleDebug`。

- [ ] **Step 2: 如果用户同意，执行编译**

```
./gradlew assembleDebug
```

- [ ] **Step 3: 修复编译错误（如果有）**

根据编译输出修复缺失的 import、类型不匹配等问题。特别关注：
- `SubTask` 数据类的 import 路径
- `LineSnapshotUtils` 的方法签名
- `TodoLine` 的构造参数
- `ContentBlock` 的 import 路径
- `CardRelation` 的 import 路径

- [ ] **Step 4: 提交修复**

```
git add -A
git commit -m "fix: 修复编译错误"
```

---

### Task 11: 手动测试验证

**Files:**
- 无新文件

- [ ] **Step 1: 询问用户进行手动测试**

根据项目规则，不自动执行运行时操作。建议用户在设备上测试以下场景：

| 场景 | 操作步骤 | 期望结果 |
|------|---------|---------|
| 基本文本撤销 | 编辑文字 → 点击 ↩ 撤销 | 文字恢复到编辑前状态 |
| 基本文本恢复 | 撤销后 → 点击 ↪ 恢复 | 文字回到撤销前状态 |
| 分类撤销 | 修改分类 → 撤销 | 分类恢复 |
| 优先级撤销 | 修改优先级 → 撤销 | 优先级恢复 |
| 日期撤销 | 修改截止日期 → 撤销 | 日期恢复 |
| 位置撤销 | 设置地理围栏 → 撤销 | 位置恢复 |
| 保存后撤销 | 保存 → 撤销 | 状态恢复到保存前 |
| 连续撤销 | 多次操作 → 连续撤销 | 逐步恢复 |
| 新操作清空恢复栈 | 撤销后 → 新操作 → 恢复按钮 | 恢复按钮禁用 |
| 防抖验证 | 快速输入多个字符 → 撤销 | 只撤销到输入前 |
| 退出重进 | 编辑后退出 → 重新进入编辑页 | 栈已清空，无撤销历史 |
| 多分组 | 修改子 todo 分类 → 撤销 | 子 todo 分类恢复 |

- [ ] **Step 2: 修复测试中发现的问题**

---

## 自审检查

### 1. Spec 覆盖

| Spec 需求 | 对应 Task |
|-----------|----------|
| EditSnapshot 数据类 | Task 1 |
| UndoRedoStack 栈管理器 | Task 1 |
| 移除双栈 | Task 2 |
| 快照采集时机（setter 前推入） | Task 4 |
| 文本防抖 | Task 3 (pushSnapshotDebounced) |
| 恢复机制 | Task 3 (restoreFromSnapshot) |
| 防循环 | Task 3 (_isRestoring) + Task 5 (LaunchedEffect 判断) |
| UI 层恢复事件 | Task 5 (restoreEvent) |
| undo/redo 新实现 | Task 3 |
| currentSnapshot 属性 | Task 3 |
| TodoEditScreen 参数传入 | Task 5 |
| buildTodoLinesFromSnapshot | Task 5 |
| 多分组场景 | 已在 EditSnapshot 中用 Map 覆盖 |
| 保存后保留栈 | 不清空栈（默认行为） |
| 仅会话内 | Task 2（移除持久化）+ Task 8（清理 Preferences） |

### 2. 占位符扫描

无 TBD/TODO/待填充内容。

### 3. 类型一致性

- `EditSnapshot` 中的 `subTasks: List<SubTask>` — SubTask 字段名待 Task 9 确认
- `restoreFromSnapshot` 中所有 StateFlow 赋值类型与 EditSnapshot 字段类型一致
- `undoRedoStack.push(currentSnapshot)` 参数类型为 `EditSnapshot`，与栈管理器匹配
- `TodoGroupContainer` 的 `canUndo: Boolean` 与 `viewModel.canUndo` 的 `StateFlow<Boolean>` 通过 `collectAsState()` 匹配
