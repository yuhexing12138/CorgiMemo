# 我的待办功能菜单实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将"我的待办"页面右上角柯基爪子替换为三点功能菜单，包含隐藏详情、隐藏已完成、待办排序等7个功能项

**Architecture:** 新建 TodoMenuDropdown 组件封装菜单逻辑；EnhancedTopBar 增加 rightIconType 参数支持按页面配置右侧图标；HomeViewModel 新增 hideDetails/hideCompletedItems 持久化状态；TodoListItem 增加 isSimpleMode 参数和右上角置顶图标

**Tech Stack:** Jetpack Compose (BOM 2026.04.01), Material 3, Hilt, EncryptedSharedPreferences, StateFlow

**设计文档:** `docs/superpowers/specs/2026-07-02-我的待办功能菜单设计.md`

---

## 文件结构

| 操作 | 文件路径 | 职责 |
|------|---------|------|
| 修改 | `app/src/main/java/com/corgimemo/app/data/local/datastore/CorgiPreferences.kt` | 新增 hideDetails/hideCompletedItems 持久化键 |
| 修改 | `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt` | 新增状态和方法 |
| 修改 | `app/src/main/java/com/corgimemo/app/ui/components/EnhancedTopBar.kt` | 增加 rightIconType/onMoreClick 参数 |
| 新建 | `app/src/main/java/com/corgimemo/app/ui/components/TodoMenuDropdown.kt` | 功能菜单组件 |
| 修改 | `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt` | isSimpleMode + 置顶图标 |
| 修改 | `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 集成菜单、移除排序按钮 |
| 修改 | `app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt` | 配置各页面右侧图标 |

---

## Task 1: CorgiPreferences 新增持久化键

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/data/local/datastore/CorgiPreferences.kt`

- [ ] **Step 1: 在 Keys object 中新增两个键**

在 `CorgiPreferences.kt` 的 `private object Keys` 中（约行 122-157），在 `SHOW_COMPLETED` 之后添加：

```kotlin
/** V2.9: 待办卡片简化显示（隐藏详情） */
const val HIDE_DETAILS = "hide_details"
/** V2.9: 隐藏所有已完成项 */
const val HIDE_COMPLETED_ITEMS = "hide_completed_items"
```

- [ ] **Step 2: 新增 hideDetails 的 Flow 读取和写入方法**

在 `showCompleted` 相关方法附近（约行 399-436），添加：

```kotlin
/** V2.9: 待办卡片简化显示 */
val hideDetails: Flow<Boolean> = booleanFlow(Keys.HIDE_DETAILS, false)

suspend fun setHideDetails(hide: Boolean) = withContext(Dispatchers.IO) {
    esp.edit().putBoolean(Keys.HIDE_DETAILS, hide).apply()
}

/** V2.9: 隐藏所有已完成项 */
val hideCompletedItems: Flow<Boolean> = booleanFlow(Keys.HIDE_COMPLETED_ITEMS, false)

suspend fun setHideCompletedItems(hide: Boolean) = withContext(Dispatchers.IO) {
    esp.edit().putBoolean(Keys.HIDE_COMPLETED_ITEMS, hide).apply()
}
```

- [ ] **Step 3: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/data/local/datastore/CorgiPreferences.kt
git commit -m "feat(preferences): 新增 hideDetails/hideCompletedItems 持久化键"
```

---

## Task 2: HomeViewModel 新增状态和方法

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt`

- [ ] **Step 1: 新增 hideDetails 和 hideCompletedItems 状态定义**

在 `_showCompleted` 状态定义附近（约行 140-141），添加：

```kotlin
/** 待办卡片简化显示（隐藏详情） */
private val _hideDetails = MutableStateFlow(false)
val hideDetails: StateFlow<Boolean> = _hideDetails.asStateFlow()

/** 隐藏所有已完成项 */
private val _hideCompletedItems = MutableStateFlow(false)
val hideCompletedItems: StateFlow<Boolean> = _hideCompletedItems.asStateFlow()
```

- [ ] **Step 2: 在 init 块中从持久化加载新状态**

在 `init` 块中 `showCompleted` 的 collect 逻辑附近（约行 580-582），添加：

```kotlin
viewModelScope.launch {
    corgiPreferences.hideDetails.collect { _hideDetails.value = it }
}
viewModelScope.launch {
    corgiPreferences.hideCompletedItems.collect { _hideCompletedItems.value = it }
}
```

- [ ] **Step 3: 新增 toggle 方法**

在 `toggleShowCompleted()` 方法附近（约行 1373-1379），添加：

```kotlin
/** 切换待办卡片简化显示 */
fun toggleHideDetails() {
    val newVal = !_hideDetails.value
    _hideDetails.value = newVal
    viewModelScope.launch {
        corgiPreferences.setHideDetails(newVal)
    }
}

/** 切换隐藏所有已完成项 */
fun toggleHideCompletedItems() {
    val newVal = !_hideCompletedItems.value
    _hideCompletedItems.value = newVal
    viewModelScope.launch {
        corgiPreferences.setHideCompletedItems(newVal)
    }
}
```

- [ ] **Step 4: 修改 filteredTodos 数据流集成 hideCompletedItems**

在 `filteredTodos` 的 `combine` 链中（约行 193-228），将第一个 combine 修改为包含 `_hideCompletedItems`：

```kotlin
val filteredTodos: StateFlow<List<TodoItem>> = run {
    val baseFlow = kotlinx.coroutines.flow.combine(
        pendingTodos, visibleCompletedTodos, _showCompleted, _hideCompletedItems
    ) { pending, completed, showCompleted, hideCompletedItems ->
        if (hideCompletedItems) {
            pending
        } else {
            if (showCompleted) pending + completed else pending
        }
    }
    // ... 后续 combine 链保持不变
}
```

- [ ] **Step 5: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 6: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt
git commit -m "feat(viewmodel): 新增 hideDetails/hideCompletedItems 状态及方法"
```

---

## Task 3: EnhancedTopBar 改造

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/EnhancedTopBar.kt`

- [ ] **Step 1: 新增 RightIconType 枚举**

在文件顶部 `EnhancedTopBar` 函数之前，添加枚举定义：

```kotlin
/** 顶部栏右侧图标类型 */
enum class RightIconType {
    /** 柯基爪子图标（我的页面） */
    CORGIE,
    /** 三点菜单图标（待办/灵感/日期页面） */
    MORE_MENU
}
```

- [ ] **Step 2: 修改 EnhancedTopBar 签名新增参数**

将函数签名（约行 38-45）修改为：

```kotlin
@Composable
fun EnhancedTopBar(
    title: String,
    onMenuClick: () -> Unit,
    onCorgiClick: () -> Unit,
    actionButtons: List<@Composable () -> Unit> = emptyList(),
    modifier: Modifier = Modifier,
    rightIconType: RightIconType = RightIconType.CORGIE,
    onMoreClick: (() -> Unit)? = null
)
```

- [ ] **Step 3: 修改柯基按钮区域实现右侧图标切换**

将柯基按钮实现（约行 79-86）替换为根据 `rightIconType` 切换：

```kotlin
when (rightIconType) {
    RightIconType.CORGIE -> {
        IconButton(onClick = onCorgiClick) {
            Icon(
                imageVector = Icons.Default.Pets,
                contentDescription = "柯基详情",
                tint = UiColors.Primary,
                modifier = Modifier.size(24.dp)
            )
        }
    }
    RightIconType.MORE_MENU -> {
        IconButton(onClick = { onMoreClick?.invoke() }) {
            Icon(
                imageVector = Icons.Default.MoreVert,
                contentDescription = "更多功能",
                tint = MaterialTheme.colorScheme.onSurface,
                modifier = Modifier.size(24.dp)
            )
        }
    }
}
```

- [ ] **Step 4: 添加缺失的 import**

在文件顶部 import 区域添加：

```kotlin
import androidx.compose.material.icons.filled.MoreVert
import androidx.compose.material3.MaterialTheme
```

- [ ] **Step 5: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 6: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/EnhancedTopBar.kt
git commit -m "feat(topbar): EnhancedTopBar 新增 rightIconType 参数支持三点图标"
```

---

## Task 4: 新建 TodoMenuDropdown 组件

**Files:**
- Create: `app/src/main/java/com/corgimemo/app/ui/components/TodoMenuDropdown.kt`

- [ ] **Step 1: 创建 TodoMenuDropdown.kt 文件**

```kotlin
package com.corgimemo.app.ui.components

import androidx.compose.foundation.layout.size
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.outlined.BarChart
import androidx.compose.material.icons.outlined.CheckBox
import androidx.compose.material.icons.outlined.ContentCopy
import androidx.compose.material.icons.outlined.CreateNewFolder
import androidx.compose.material.icons.outlined.SelectAll
import androidx.compose.material.icons.outlined.Share
import androidx.compose.material.icons.outlined.Visibility
import androidx.compose.material3.DropdownMenu
import androidx.compose.material3.DropdownMenuItem
import androidx.compose.material3.Icon
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.draw.alpha
import androidx.compose.ui.unit.dp

/**
 * 我的待办页面功能菜单弹窗
 *
 * 点击右上角三点图标后弹出，包含7个功能项：
 * 1. 隐藏/显示详情
 * 2. 隐藏/显示已完成
 * 3. 待办排序
 * 4. 管理分组（占位）
 * 5. 批量选择
 * 6. 分享（占位）
 * 7. 创建待办副本（占位）
 *
 * @param expanded 菜单是否展开
 * @param onDismiss 关闭菜单回调
 * @param hideDetails 当前 hideDetails 状态
 * @param onToggleHideDetails 切换 hideDetails
 * @param hideCompletedItems 当前 hideCompletedItems 状态
 * @param onToggleHideCompletedItems 切换 hideCompletedItems
 * @param onSortClick 打开排序弹窗
 * @param onBatchSelectClick 进入批量模式
 * @param onPlaceholderClick 占位项点击回调（Toast 提示）
 */
@Composable
fun TodoMenuDropdown(
    expanded: Boolean,
    onDismiss: () -> Unit,
    hideDetails: Boolean,
    onToggleHideDetails: () -> Unit,
    hideCompletedItems: Boolean,
    onToggleHideCompletedItems: () -> Unit,
    onSortClick: () -> Unit,
    onBatchSelectClick: () -> Unit,
    onPlaceholderClick: () -> Unit
) {
    DropdownMenu(
        expanded = expanded,
        onDismissRequest = onDismiss,
        modifier = Modifier.alpha(1f)
    ) {
        // 1. 隐藏详情 / 显示详情
        DropdownMenuItem(
            text = { Text(if (hideDetails) "显示详情" else "隐藏详情") },
            onClick = {
                onToggleHideDetails()
                onDismiss()
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Outlined.Visibility,
                    contentDescription = null,
                    modifier = Modifier.size(20.dp)
                )
            }
        )
        // 2. 隐藏已完成 / 显示已完成
        DropdownMenuItem(
            text = { Text(if (hideCompletedItems) "显示已完成" else "隐藏已完成") },
            onClick = {
                onToggleHideCompletedItems()
                onDismiss()
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Outlined.CheckBox,
                    contentDescription = null,
                    modifier = Modifier.size(20.dp)
                )
            }
        )
        // 3. 待办排序
        DropdownMenuItem(
            text = { Text("待办排序") },
            onClick = {
                onSortClick()
                onDismiss()
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Outlined.BarChart,
                    contentDescription = null,
                    modifier = Modifier.size(20.dp)
                )
            }
        )
        // 4. 管理分组（占位）
        DropdownMenuItem(
            text = { Text("管理分组") },
            onClick = {
                onPlaceholderClick()
                onDismiss()
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Outlined.CreateNewFolder,
                    contentDescription = null,
                    modifier = Modifier
                        .size(20.dp)
                        .alpha(0.4f)
                )
            },
            modifier = Modifier.alpha(0.4f)
        )
        // 5. 批量选择
        DropdownMenuItem(
            text = { Text("批量选择") },
            onClick = {
                onBatchSelectClick()
                onDismiss()
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Outlined.SelectAll,
                    contentDescription = null,
                    modifier = Modifier.size(20.dp)
                )
            }
        )
        // 6. 分享（占位）
        DropdownMenuItem(
            text = { Text("分享") },
            onClick = {
                onPlaceholderClick()
                onDismiss()
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Outlined.Share,
                    contentDescription = null,
                    modifier = Modifier.size(20.dp)
                )
            },
            modifier = Modifier.alpha(0.4f)
        )
        // 7. 创建待办副本（占位）
        DropdownMenuItem(
            text = { Text("创建待办副本") },
            onClick = {
                onPlaceholderClick()
                onDismiss()
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Outlined.ContentCopy,
                    contentDescription = null,
                    modifier = Modifier.size(20.dp)
                )
            },
            modifier = Modifier.alpha(0.4f)
        )
    }
}
```

- [ ] **Step 2: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/TodoMenuDropdown.kt
git commit -m "feat(menu): 新建 TodoMenuDropdown 功能菜单组件"
```

---

## Task 5: TodoListItem 改造（isSimpleMode + 置顶图标）

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt`

- [ ] **Step 1: 新增 isSimpleMode 参数**

在 `TodoListItem` 函数签名（约行 100-129）中，在 `isClickBlocked: Boolean = false` 之后添加：

```kotlin
isSimpleMode: Boolean = false
```

- [ ] **Step 2: 新增 import 语句**

在文件顶部 import 区域添加：

```kotlin
import androidx.compose.material.icons.outlined.PushPin
import androidx.compose.foundation.layout.Box
import androidx.compose.ui.Alignment
```

- [ ] **Step 3: 在卡片右上角添加置顶图标**

在卡片内容区域的 `Column` 中（标题行附近，约行 330-417 的 Row 内），用 `Box` 包裹内容并添加置顶图标。

找到卡片内容 Row（`CircularCheckbox` + 内容 Column），在内容 Column 的父级用 `Box` 包裹，在 `Box` 的 `TopEnd` 位置添加置顶图标：

```kotlin
Box(modifier = Modifier.weight(1f)) {
    // 原有的内容 Column
    Column(
        modifier = Modifier.padding(end = if (todo.isPinned) 24.dp else 0.dp)
    ) {
        // ... 原有内容
    }
    // 置顶图标（右上角，只图标无文字）
    if (todo.isPinned) {
        Icon(
            imageVector = Icons.Outlined.PushPin,
            contentDescription = "已置顶",
            tint = if (todo.isCompleted) CompletedColors.Text else UiColors.Primary,
            modifier = Modifier
                .align(Alignment.TopEnd)
                .size(16.dp)
        )
    }
}
```

**注意**：需要根据实际代码结构调整 `Box` 的嵌套位置。关键是置顶图标使用 `Modifier.align(Alignment.TopEnd)` 定位到卡片右上角，且内容区域 `padding(end = 24.dp)` 预留空间。

- [ ] **Step 4: 简化模式下隐藏元素**

在渲染分类标签、子任务进度、附件数量等元素的位置（约行 330-417 的 meta 行），用 `if (!isSimpleMode)` 包裹需要隐藏的元素：

```kotlin
// 分类标签 - 简化模式下隐藏
if (!isSimpleMode && categoryName != null) {
    CategoryTagWithShadow(
        categoryName = categoryName,
        isCompleted = todo.isCompleted
    )
}

// 提醒时间和附件 - 简化模式下仅显示提醒时间
// 注意：根据设计，简化模式保留提醒时间，隐藏附件数量
if (!isSimpleMode) {
    // 语音附件、图片附件等
    // ... 原有附件渲染代码
}

// 子任务进度 - 简化模式下隐藏
if (!isSimpleMode && subTaskProgress != null) {
    // ... 原有子任务进度渲染代码
}

// 子任务列表 - 简化模式下强制收起
// 找到 AnimatedVisibility 控制 expandedTodos 的地方，修改条件
val effectiveExpanded = isExpanded && !isSimpleMode
// 将 isExpanded 替换为 effectiveExpanded
```

- [ ] **Step 5: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 6: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt
git commit -m "feat(todo-item): 新增 isSimpleMode 简化模式和右上角置顶图标"
```

---

## Task 6: HomeScreen 集成菜单和移除排序按钮

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

- [ ] **Step 1: 新增菜单展开状态和 context**

在 HomeScreen 函数顶部状态定义区域（约行 250-251 附近），添加：

```kotlin
var showMenuDropdown by remember { mutableStateOf(false) }
val context = LocalContext.current
```

- [ ] **Step 2: 订阅 hideDetails 和 hideCompletedItems 状态**

在状态订阅区域（约行 202-203 附近），添加：

```kotlin
val hideDetails by viewModel.hideDetails.collectAsState()
val hideCompletedItems by viewModel.hideCompletedItems.collectAsState()
```

- [ ] **Step 3: 添加 TodoMenuDropdown 组件渲染**

在 HomeScreen 的 Composable 树中（与 `SortBottomSheet` 同级，约行 1165-1177 附近），添加：

```kotlin
TodoMenuDropdown(
    expanded = showMenuDropdown,
    onDismiss = { showMenuDropdown = false },
    hideDetails = hideDetails,
    onToggleHideDetails = { viewModel.toggleHideDetails() },
    hideCompletedItems = hideCompletedItems,
    onToggleHideCompletedItems = { viewModel.toggleHideCompletedItems() },
    onSortClick = { showSortSheet = true },
    onBatchSelectClick = {
        // 进入批量模式需要一个 todoId，这里用第一个待办的 id
        filteredTodos.firstOrNull()?.let { viewModel.enterBatchMode(it.id) }
    },
    onPlaceholderClick = {
        Toast.makeText(context, "功能开发中...", Toast.LENGTH_SHORT).show()
    }
)
```

- [ ] **Step 4: 添加缺失的 import**

在文件顶部 import 区域添加：

```kotlin
import com.corgimemo.app.ui.components.TodoMenuDropdown
import android.widget.Toast
import androidx.compose.ui.platform.LocalContext
```

- [ ] **Step 5: 移除搜索框排序按钮**

找到搜索框的 `trailingIcon` 实现（约行 647-659），移除排序按钮代码。将 `trailingIcon` 设置为 `null` 或移除整个参数：

```kotlin
SearchBar(
    query = searchQuery,
    onQueryChange = { newQuery -> viewModel.updateSearchQuery(newQuery) },
    onClear = { viewModel.clearSearch() },
    modifier = Modifier
        .fillMaxWidth()
        .padding(horizontal = 20.dp)
        .padding(bottom = dimensionResource(com.corgimemo.app.R.dimen.ui_search_bar_bottom_margin))
    // trailingIcon 已移除，排序功能迁移到三点菜单
)
```

- [ ] **Step 6: 传递 isSimpleMode 给 TodoListItem**

在 `TodoListItem` 调用处（约行 840-883），添加 `isSimpleMode` 参数：

```kotlin
TodoListItem(
    todo = todo,
    isSimpleMode = hideDetails,  // 新增
    // ... 其他现有参数保持不变
)
```

- [ ] **Step 7: 修改"已完成(N)"分隔符显示逻辑**

找到 `displayItems` 的计算逻辑（约行 735-747），在 `completedCount` 判断中加入 `hideCompletedItems` 条件：

```kotlin
val displayItems = remember(
    filteredPending, filteredCompleted, showCompleted, completedCount, hideCompletedItems
) {
    buildList {
        filteredPending.forEach { add(DisplayItem.Todo(it)) }
        if (!hideCompletedItems && completedCount > 0) {
            add(DisplayItem.CompletedDivider(count = completedCount, isExpanded = showCompleted))
            if (showCompleted) {
                filteredCompleted.forEach { add(DisplayItem.Todo(it)) }
            }
        }
    }
}
```

- [ ] **Step 8: 暴露菜单触发回调给 MainScreen**

由于顶部栏由 MainScreen 管理，需要将菜单触发回调暴露出去。在 HomeScreen 的参数中添加：

```kotlin
@Composable
fun HomeScreen(
    // ... 现有参数
    onMoreMenuClick: (() -> Unit)? = null  // 新增：三点菜单点击回调
)
```

然后在 HomeScreen 中使用 `LaunchedEffect` 或直接在 `showMenuDropdown` 状态变化时处理。

**更简洁的方案**：由于 MainScreen 中的 `onMoreClick` 需要触发 HomeScreen 内部的状态，可以通过共享 ViewModel 状态实现。在 HomeViewModel 中添加：

```kotlin
private val _menuExpanded = MutableStateFlow(false)
val menuExpanded: StateFlow<Boolean> = _menuExpanded.asStateFlow()

fun setMenuExpanded(expanded: Boolean) {
    _menuExpanded.value = expanded
}
```

然后在 HomeScreen 中订阅 `menuExpanded`，在 MainScreen 中调用 `homeViewModel.setMenuExpanded(true)`。

**注意**：选择此方案后，TodoMenuDropdown 的 `expanded` 参数使用 `menuExpanded`，`onDismiss` 调用 `setMenuExpanded(false)`。

- [ ] **Step 9: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 10: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt
git commit -m "feat(home): 集成功能菜单、移除搜索框排序按钮、传递 isSimpleMode"
```

---

## Task 7: MainScreen 配置各页面右侧图标

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt`

- [ ] **Step 1: 添加 import 语句**

在文件顶部 import 区域添加：

```kotlin
import com.corgimemo.app.ui.components.RightIconType
import android.widget.Toast
import androidx.compose.ui.platform.LocalContext
```

- [ ] **Step 2: 获取 context 并配置各页面的 rightIconType**

在 `topBarActionButtons` 定义附近（约行 150-182），添加 `rightIconType` 和 `onMoreClick` 的配置：

```kotlin
val context = LocalContext.current

val rightIconType = when (selectedTab) {
    TabItem.TODO -> RightIconType.MORE_MENU
    TabItem.INSPIRE -> RightIconType.MORE_MENU
    TabItem.DATE -> RightIconType.MORE_MENU
    TabItem.PROFILE -> RightIconType.CORGIE
    else -> RightIconType.CORGIE
}

val onMoreClick: (() -> Unit)? = when (selectedTab) {
    TabItem.TODO -> {
        { homeViewModel.setMenuExpanded(true) }
    }
    TabItem.INSPIRE -> {
        { Toast.makeText(context, "功能开发中...", Toast.LENGTH_SHORT).show() }
    }
    TabItem.DATE -> {
        { Toast.makeText(context, "功能开发中...", Toast.LENGTH_SHORT).show() }
    }
    else -> null
}
```

- [ ] **Step 3: 修改 EnhancedTopBar 调用传递新参数**

在 `EnhancedTopBar` 调用处（约行 242-260），添加新参数：

```kotlin
EnhancedTopBar(
    title = effectiveTitle,
    onMenuClick = {
        if (effectiveMenuEnabled) {
            coroutineScope.launch { drawerState.open() }
        }
    },
    onCorgiClick = {
        if (effectiveMenuEnabled) {
            navController.navigate(Screen.CorgiDetail.route)
        }
    },
    actionButtons = effectiveActionButtons,
    rightIconType = if (isBatchMode) RightIconType.CORGIE else rightIconType,
    onMoreClick = if (isBatchMode) null else onMoreClick
)
```

**注意**：批量模式下将 `rightIconType` 设为 `CORGIE`，避免显示三点菜单（批量模式有自己的操作栏）。

- [ ] **Step 4: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt
git commit -m "feat(main): 配置各页面右侧图标类型和点击行为"
```

---

## Task 8: HomeViewModel 新增菜单展开状态

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt`

**注意**：此 Task 补充 Task 6 Step 8 中提到的菜单展开状态，因为 MainScreen 需要通过 ViewModel 触发 HomeScreen 中的菜单展开。

- [ ] **Step 1: 新增 menuExpanded 状态**

在 HomeViewModel 的状态定义区域（约行 140-141 附近），添加：

```kotlin
/** 三点功能菜单展开状态 */
private val _menuExpanded = MutableStateFlow(false)
val menuExpanded: StateFlow<Boolean> = _menuExpanded.asStateFlow()
```

- [ ] **Step 2: 新增 setMenuExpanded 方法**

在方法区域（约行 1373-1379 附近），添加：

```kotlin
/** 设置三点功能菜单展开状态 */
fun setMenuExpanded(expanded: Boolean) {
    _menuExpanded.value = expanded
}
```

- [ ] **Step 3: 修改 HomeScreen 使用 ViewModel 的 menuExpanded 状态**

回到 `HomeScreen.kt`，将 Step 1 中的 `showMenuDropdown` 本地状态替换为 ViewModel 状态：

```kotlin
// 替换：var showMenuDropdown by remember { mutableStateOf(false) }
// 改为：
val menuExpanded by viewModel.menuExpanded.collectAsState()
```

然后修改 TodoMenuDropdown 的调用：

```kotlin
TodoMenuDropdown(
    expanded = menuExpanded,
    onDismiss = { viewModel.setMenuExpanded(false) },
    // ... 其他参数不变
)
```

- [ ] **Step 4: 验证编译通过**

Run: `./gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt
git commit -m "feat(menu): 菜单展开状态提升至 ViewModel 实现 MainScreen 触发"
```

---

## Task 9: 手动验证与边界情况处理

**Files:**
- 无文件修改，仅手动验证

- [ ] **Step 1: 编译并安装到设备**

Run: `./gradlew :app:installDebug --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 验证菜单弹出与关闭**

1. 打开"我的待办"页面
2. 确认右上角显示三点图标（非柯基爪子）
3. 点击三点图标，确认菜单弹出（7个功能项）
4. 点击遮罩区域，确认菜单关闭
5. 点击"我的"页面，确认显示柯基爪子

- [ ] **Step 3: 验证隐藏详情功能**

1. 点击"隐藏详情"，确认菜单关闭
2. 确认待办卡片切换为简化模式（隐藏分类标签、子任务进度、附件数量）
3. 确认置顶图标在右上角显示（如有置顶待办）
4. 再次打开菜单，确认文字变为"显示详情"
5. 点击"显示详情"，确认卡片恢复完整显示

- [ ] **Step 4: 验证隐藏已完成功能**

1. 确保有待办已完成
2. 点击"隐藏已完成"，确认菜单关闭
3. 确认"已完成(N)"分隔符消失
4. 确认列表中无已勾选待办
5. 再次打开菜单，确认文字变为"显示已完成"
6. 点击"显示已完成"，确认已完成项恢复显示

- [ ] **Step 5: 验证待办排序功能**

1. 点击"待办排序"，确认菜单关闭
2. 确认 SortBottomSheet 弹出
3. 选择不同排序方式，确认列表排序变化
4. 确认搜索框右侧不再显示 📊 排序按钮

- [ ] **Step 6: 验证批量选择功能**

1. 点击"批量选择"，确认菜单关闭
2. 确认进入批量模式（底部显示批量操作栏）
3. 点击返回键退出批量模式
4. 确认三点图标恢复显示

- [ ] **Step 7: 验证占位项 Toast 提示**

1. 打开菜单
2. 点击"管理分组"（半透明），确认 Toast 显示"功能开发中..."
3. 确认菜单关闭
4. 重复验证"分享"和"创建待办副本"

- [ ] **Step 8: 验证状态持久化**

1. 开启"隐藏详情"和"隐藏已完成"
2. 杀掉 App 进程
3. 重新打开 App
4. 确认"隐藏详情"和"隐藏已完成"状态保持

- [ ] **Step 9: 验证灵感/日期页面**

1. 切换到"灵感"页面
2. 确认右上角显示三点图标
3. 点击三点图标，确认 Toast 显示"功能开发中..."
4. 重复验证"日期"页面

- [ ] **Step 10: 验证手势冲突**

1. 开启"隐藏详情"（简化模式）
2. 点击待办卡片，确认正常进入编辑页
3. 如有待办含子任务，确认简化模式下子任务不展开
4. 长按待办卡片，确认正常进入批量模式

- [ ] **Step 11: 最终提交（如有修复）**

如果手动验证发现问题并修复，提交修复：

```bash
git add -A
git commit -m "fix(menu): 修复手动验证发现的问题"
```

---

## 自审清单

### Spec 覆盖检查

| 设计文档章节 | 对应 Task | 状态 |
|-------------|----------|------|
| 一、需求概述 - 7个功能项 | Task 4 | ✅ |
| 一、需求概述 - 各页面图标配置 | Task 7 | ✅ |
| 一、需求概述 - 菜单样式 | Task 4 | ✅ |
| 二、架构设计 - 组件关系 | Task 3, 4, 6, 7 | ✅ |
| 三、状态管理 - 新增状态 | Task 1, 2 | ✅ |
| 三、状态管理 - filteredTodos | Task 2 | ✅ |
| 四、TodoListItem 改造 | Task 5 | ✅ |
| 五、EnhancedTopBar 改造 | Task 3 | ✅ |
| 六、TodoMenuDropdown 组件 | Task 4 | ✅ |
| 七、搜索框改造 | Task 6 | ✅ |
| 八、错误处理 - 边界情况 | Task 9 | ✅ |
| 八、错误处理 - 手势冲突 | Task 9 Step 10 | ✅ |

### Placeholder 扫描

- 无 "TBD" / "TODO" / "fill in details" ✅
- 所有代码步骤包含完整代码 ✅
- 所有命令包含预期输出 ✅

### 类型一致性检查

- `RightIconType` 枚举在 Task 3 定义，Task 7 使用 ✅
- `TodoMenuDropdown` 签名在 Task 4 定义，Task 6 使用 ✅
- `hideDetails` / `hideCompletedItems` 在 Task 1-2 定义，Task 6 使用 ✅
- `menuExpanded` 在 Task 8 定义，Task 6-7 使用 ✅
- `isSimpleMode` 在 Task 5 定义，Task 6 使用 ✅

### 已知偏差（与设计文档的差异）

1. **CorgiPreferences 路径**：设计文档写 `data/CorgiPreferences.kt`，实际为 `data/local/datastore/CorgiPreferences.kt`，使用 EncryptedSharedPreferences 而非 DataStore。计划中已修正。
2. **置顶图标**：设计文档说"位置调整"，但实际 TodoListItem 当前没有置顶图标，是新增功能。计划中已修正为新增。
3. **描述内容**：设计文档说简化模式"隐藏描述内容"，但 TodoListItem 当前不渲染描述内容。计划中省略此项。
4. **菜单展开状态**：设计文档未提及，但因 MainScreen 管理顶部栏需要通过 ViewModel 触发 HomeScreen 的菜单展开，计划在 Task 8 中补充。
