# 灵感侧边栏标签筛选实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为灵感页侧边栏实现多选标签筛选（OR/AND/NOT 三种模式）、标签计数、搜索过滤，并将"添加标签"按钮固定到底部。

**Architecture:** 在 `InspirationViewModel` 中新增 `selectedTags`、`tagFilterMode`、`tagCounts` 三个状态及 `filteredDisplayInspirations` 过滤流；重写 `AppDrawer.kt` 中的 `InspirationFilterSection` 加入搜索框、模式芯片、多选标签列表；在 `MainScreen.kt` 中将 ViewModel 状态和回调接入 `AppDrawerContent`；最后让 `InspirationScreen.kt` 消费过滤后的列表。

**Tech Stack:** Kotlin、Jetpack Compose（Material3）、Hilt、StateFlow、Coroutines

---

## 文件结构

| 文件 | 路径 | 责任 | 改动类型 |
|------|------|------|------|
| InspirationViewModel.kt | `app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt` | 新增 `TagFilterMode` 枚举、`selectedTags`/`tagFilterMode`/`tagCounts` 状态、`filteredDisplayInspirations` 过滤流、`toggleTagSelection`/`setTagFilterMode`/`clearTagSelection` 方法 | 修改 |
| AppDrawer.kt | `app/src/main/java/com/corgimemo/app/ui/components/AppDrawer.kt` | 重写 `InspirationFilterSection`（加搜索框、模式芯片、多选、计数），更新 `AppDrawerContent` 签名（`selectedTag` → `selectedTags`/`filterMode`/`tagCounts`/`onFilterModeChange`），将 `AddCategoryButton` 移到 INSPIRE 分支外部 | 修改 |
| MainScreen.kt | `app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt` | 在 `AppDrawerContent` 调用处接入 `inspirationViewModel` 的 `savedTags`/`selectedTags`/`tagFilterMode`/`tagCounts` 及回调 | 修改 |
| InspirationScreen.kt | `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt` | 将 `displayInspirations` 替换为 `filteredDisplayInspirations` | 修改 |

---

## Task 1: ViewModel 新增标签筛选状态与过滤逻辑

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt`

- [ ] **Step 1: 在文件顶部 `InspirationViewModel` 类外部添加 `TagFilterMode` 枚举**

在 `InspirationViewModel.kt` 中，找到 `class InspirationViewModel @Inject constructor(...)` 这一行（约第 28-29 行），在 `@HiltViewModel` 注解的**上方**（即 `import` 块之后、`@HiltViewModel` 之前）插入以下枚举定义：

```kotlin
/**
 * 标签筛选模式
 * - OR：并集，显示包含任意选中标签的灵感
 * - AND：交集，显示同时包含所有选中标签的灵感
 * - NOT：非，显示不包含任何选中标签的灵感
 */
enum class TagFilterMode {
    OR,
    AND,
    NOT
}
```

- [ ] **Step 2: 在 ViewModel 类中添加 `selectedTags`、`tagFilterMode` 私有状态与公开 StateFlow**

在 `InspirationViewModel.kt` 中找到 `// ========== 状态定义 ==========` 区块（约第 102 行），在 `savedTags` StateFlow 定义之后（约第 165 行后）插入以下代码：

```kotlin
    /** 选中的标签集合（多选） */
    private val _selectedTags = MutableStateFlow<Set<String>>(emptySet())
    val selectedTags: StateFlow<Set<String>> = _selectedTags.asStateFlow()

    /** 当前标签筛选模式（默认 OR） */
    private val _tagFilterMode = MutableStateFlow(TagFilterMode.OR)
    val tagFilterMode: StateFlow<TagFilterMode> = _tagFilterMode.asStateFlow()
```

- [ ] **Step 3: 添加 `tagCounts` StateFlow（聚合每个标签的灵感数量）**

在 Step 2 插入的代码之后继续添加：

```kotlin
    /** 每个标签对应的灵感数量 */
    val tagCounts: StateFlow<Map<String, Int>> =
        _inspirations.map { list ->
            list.flatMap { decodeTags(it.tags) }
                .groupingBy { it }
                .eachCount()
        }.stateIn(
            scope = viewModelScope,
            started = SharingStarted.WhileSubscribed(5000),
            initialValue = emptyMap()
        )

    /** 灵感总数（用于"全部灵感"项的计数显示，避免 tagCounts.values.sum() 重复计算） */
    val totalInspirationCount: StateFlow<Int> =
        _inspirations.map { it.size }
            .stateIn(
                scope = viewModelScope,
                started = SharingStarted.WhileSubscribed(5000),
                initialValue = 0
            )
```

- [ ] **Step 4: 添加 `filteredDisplayInspirations` StateFlow（应用标签筛选后的显示列表）**

在 Step 3 插入的 `tagCounts` 之后继续添加：

```kotlin
    /** 应用标签筛选后的统一显示列表（供 InspirationScreen 消费） */
    val filteredDisplayInspirations: StateFlow<List<InspirationDisplayItem>> =
        combine(_inspirations, _selectedTags, _tagFilterMode) { list, tags, mode ->
            val filtered = filterInspirationsByTags(list, tags, mode)
            buildDisplayItems(filtered)
        }.stateIn(
            scope = viewModelScope,
            started = SharingStarted.WhileSubscribed(5000),
            initialValue = emptyList()
        )
```

- [ ] **Step 5: 添加 `combine` 的 import**

在 `InspirationViewModel.kt` 顶部的 import 块中，找到 `import kotlinx.coroutines.flow.map`（约第 17 行），在其后添加：

```kotlin
import kotlinx.coroutines.flow.combine
```

- [ ] **Step 6: 添加 `filterInspirationsByTags` 私有方法**

在 `InspirationViewModel.kt` 中找到 `// ========== 核心方法 ==========` 区块（约第 189 行），在 `loadInspirations()` 方法**之前**插入以下方法：

```kotlin
    /**
     * 根据选中标签和筛选模式过滤灵感列表
     * - 选中标签为空时返回全部灵感
     * - OR 模式：包含任意选中标签
     * - AND 模式：同时包含所有选中标签
     * - NOT 模式：不包含任何选中标签
     *
     * @param inspirations 原始灵感列表
     * @param selectedTags 选中的标签集合
     * @param mode 筛选模式
     * @return 过滤后的灵感列表
     */
    private fun filterInspirationsByTags(
        inspirations: List<Inspiration>,
        selectedTags: Set<String>,
        mode: TagFilterMode
    ): List<Inspiration> {
        if (selectedTags.isEmpty()) return inspirations
        return when (mode) {
            TagFilterMode.OR -> inspirations.filter { insp ->
                val tags = decodeTags(insp.tags)
                tags.any { it in selectedTags }
            }
            TagFilterMode.AND -> inspirations.filter { insp ->
                val tags = decodeTags(insp.tags)
                selectedTags.all { it in tags }
            }
            TagFilterMode.NOT -> inspirations.filter { insp ->
                val tags = decodeTags(insp.tags)
                selectedTags.none { it in tags }
            }
        }
    }
```

- [ ] **Step 7: 添加 `toggleTagSelection`、`setTagFilterMode`、`clearTagSelection` 公开方法**

在 Step 6 插入的 `filterInspirationsByTags` 方法之后继续添加：

```kotlin
    /**
     * 切换标签选中状态（已选中则取消，未选中则加入）
     * @param tag 标签名
     */
    fun toggleTagSelection(tag: String) {
        _selectedTags.value = _selectedTags.value.toMutableSet().apply {
            if (contains(tag)) remove(tag) else add(tag)
        }
    }

    /**
     * 设置标签筛选模式
     * @param mode 筛选模式（OR/AND/NOT）
     */
    fun setTagFilterMode(mode: TagFilterMode) {
        _tagFilterMode.value = mode
    }

    /**
     * 清空所有已选中的标签（恢复"全部灵感"状态）
     */
    fun clearTagSelection() {
        _selectedTags.value = emptySet()
    }
```

- [ ] **Step 8: 提交 Task 1**

```bash
git add app/src/main/java/com/corgimemo/app/viewmodel/InspirationViewModel.kt
git commit -m "feat(inspiration): ViewModel 新增多选标签筛选状态与过滤逻辑"
```

---

## Task 2: 重写 InspirationFilterSection 与 AppDrawerContent 签名

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/AppDrawer.kt`

- [ ] **Step 1: 更新 `AppDrawerContent` 的参数签名**

在 `AppDrawer.kt` 中找到 `fun AppDrawerContent(` 函数定义（约第 101 行），将原有的灵感相关参数：

```kotlin
    inspirationTags: List<String> = emptyList(),
    selectedTag: String? = null,
    selectedDateType: GroupType? = null,
    onCategoryClick: (Long?) -> Unit = {},
    onAddCategoryClick: () -> Unit = {},
    onCategoryAction: (CategoryAction) -> Unit = {},
    onTagClick: (String?) -> Unit = {},
    onAddTagClick: () -> Unit = {},
    onDateTypeClick: (GroupType?) -> Unit = {},
```

替换为：

```kotlin
    inspirationTags: List<String> = emptyList(),
    selectedTags: Set<String> = emptySet(),
    tagFilterMode: TagFilterMode = TagFilterMode.OR,
    tagCounts: Map<String, Int> = emptyMap(),
    totalInspirationCount: Int = 0,
    selectedDateType: GroupType? = null,
    onCategoryClick: (Long?) -> Unit = {},
    onAddCategoryClick: () -> Unit = {},
    onCategoryAction: (CategoryAction) -> Unit = {},
    onTagClick: (String) -> Unit = {},
    onTagFilterModeChange: (TagFilterMode) -> Unit = {},
    onClearTagSelection: () -> Unit = {},
    onAddTagClick: () -> Unit = {},
    onDateTypeClick: (GroupType?) -> Unit = {},
```

同时更新 KDoc 文档块（约第 86-99 行），将 `@param selectedTag` 替换为以下行（放在 `@param inspirationTags` 之后）：

```kotlin
 * @param selectedTags 当前选中的灵感标签集合（多选）
 * @param tagFilterMode 标签筛选模式（OR/AND/NOT）
 * @param tagCounts 每个标签对应的灵感数量
 * @param totalInspirationCount 灵感总数（用于"全部灵感"项计数）
 * @param onTagClick 标签点击回调（传入标签名，由调用方切换选中状态）
 * @param onTagFilterModeChange 筛选模式切换回调
 * @param onClearTagSelection 清空所有选中标签回调（"全部灵感"点击时调用）
```

并删除原有的 `@param selectedTag` 和原 `@param onTagClick` 行。

- [ ] **Step 2: 更新 `AppDrawerContent` 中 INSPIRE 分支（移出 AddCategoryButton）**

在 `AppDrawer.kt` 中找到 `TabItem.INSPIRE ->` 分支（约第 146-154 行），将：

```kotlin
            TabItem.INSPIRE -> {
                InspirationFilterSection(
                    tags = inspirationTags,
                    selectedTag = selectedTag,
                    onTagClick = onTagClick,
                    onAddTagClick = onAddTagClick,
                    modifier = Modifier.weight(1f)
                )
            }
```

替换为：

```kotlin
            TabItem.INSPIRE -> {
                InspirationFilterSection(
                    tags = inspirationTags,
                    selectedTags = selectedTags,
                    filterMode = tagFilterMode,
                    tagCounts = tagCounts,
                    totalInspirationCount = totalInspirationCount,
                    onTagClick = onTagClick,
                    onFilterModeChange = onTagFilterModeChange,
                    onClearTagSelection = onClearTagSelection,
                    modifier = Modifier.weight(1f)
                )
                AddCategoryButton(text = "添加标签", onClick = onAddTagClick)
            }
```

- [ ] **Step 3: 添加 `TagFilterMode` 的 import**

在 `AppDrawer.kt` 顶部的 import 块中，找到 `import com.corgimemo.app.viewmodel.GroupType`（约第 56 行），在其后添加：

```kotlin
import com.corgimemo.app.viewmodel.TagFilterMode
```

- [ ] **Step 4: 添加 `OutlinedTextField`、`FilterChip`、`mutableStateOf` 等 import**

在 `AppDrawer.kt` 顶部的 import 块中确认以下 import 是否存在，若不存在则添加：

```kotlin
import androidx.compose.material3.FilterChip
import androidx.compose.material3.FilterChipDefaults
import androidx.compose.material3.OutlinedTextField
import androidx.compose.material3.OutlinedTextFieldDefaults
import androidx.compose.material.icons.filled.Close
import androidx.compose.material.icons.filled.Search
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
```

注意：`OutlinedTextFieldDefaults`（约第 33 行）和 `mutableStateOf`（约第 40 行）、`remember`（约第 41 行）、`setValue`（约第 42 行）可能已存在，添加前先检查避免重复。

- [ ] **Step 5: 完全重写 `InspirationFilterSection` 函数**

在 `AppDrawer.kt` 中找到现有的 `InspirationFilterSection` 函数（约第 400-456 行），将其**整体替换**为以下实现：

```kotlin
/**
 * 灵感标签筛选区域
 *
 * 布局：标题 → 搜索框 → 模式芯片（OR/AND/NOT）→ 标签列表（可滚动）
 *
 * @param tags 所有可选标签列表
 * @param selectedTags 当前选中的标签集合
 * @param filterMode 当前筛选模式
 * @param tagCounts 每个标签对应的灵感数量
 * @param totalInspirationCount 灵感总数（用于"全部灵感"项计数）
 * @param onTagClick 标签点击回调（由调用方切换选中状态）
 * @param onFilterModeChange 筛选模式切换回调
 * @param onClearTagSelection 清空选中标签回调（点击"全部灵感"时触发）
 * @param modifier 修饰符
 */
@Composable
private fun InspirationFilterSection(
    tags: List<String>,
    selectedTags: Set<String>,
    filterMode: TagFilterMode,
    tagCounts: Map<String, Int>,
    totalInspirationCount: Int,
    onTagClick: (String) -> Unit,
    onFilterModeChange: (TagFilterMode) -> Unit,
    onClearTagSelection: () -> Unit,
    modifier: Modifier = Modifier
) {
    // 搜索框本地状态（不需要 ViewModel 持久化）
    var searchQuery by remember { mutableStateOf("") }

    // 根据搜索词过滤标签列表（忽略大小写的模糊匹配）
    val filteredTags = if (searchQuery.isBlank()) {
        tags
    } else {
        tags.filter { it.contains(searchQuery, ignoreCase = true) }
    }

    Column(modifier = modifier) {
        // 标题
        Text(
            text = "🏷️ 标签筛选",
            fontSize = 16.sp,
            fontWeight = FontWeight.Bold,
            color = Color(0xFF1C1B1F),
            modifier = Modifier.padding(horizontal = 20.dp, vertical = 8.dp)
        )

        // 橙色分割线
        Box(
            modifier = Modifier
                .padding(horizontal = 20.dp)
                .height(3.dp)
                .fillMaxWidth()
                .background(UiColors.Primary)
        )

        Spacer(modifier = Modifier.height(8.dp))

        // 搜索框
        OutlinedTextField(
            value = searchQuery,
            onValueChange = { searchQuery = it },
            placeholder = {
                Text(
                    text = "搜索标签",
                    fontSize = 14.sp,
                    color = Color(0xFF79747E)
                )
            },
            leadingIcon = {
                Icon(
                    imageVector = Icons.Default.Search,
                    contentDescription = null,
                    tint = Color(0xFF79747E),
                    modifier = Modifier.size(18.dp)
                )
            },
            trailingIcon = {
                if (searchQuery.isNotEmpty()) {
                    IconButton(onClick = { searchQuery = "" }) {
                        Icon(
                            imageVector = Icons.Default.Close,
                            contentDescription = "清除搜索",
                            tint = Color(0xFF79747E),
                            modifier = Modifier.size(16.dp)
                        )
                    }
                }
            },
            singleLine = true,
            shape = RoundedCornerShape(12.dp),
            colors = OutlinedTextFieldDefaults.colors(
                focusedBorderColor = UiColors.Primary,
                unfocusedBorderColor = Color(0xFFE0E0E0)
            ),
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 20.dp)
                .height(52.dp)
        )

        Spacer(modifier = Modifier.height(8.dp))

        // 筛选模式芯片（OR / AND / NOT）
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 20.dp),
            horizontalArrangement = Arrangement.spacedBy(8.dp)
        ) {
            FilterChip(
                selected = filterMode == TagFilterMode.OR,
                onClick = { onFilterModeChange(TagFilterMode.OR) },
                label = { Text("OR", fontSize = 12.sp) },
                colors = FilterChipDefaults.filterChipColors(
                    selectedContainerColor = UiColors.PrimaryLight,
                    selectedLabelColor = UiColors.Primary
                )
            )
            FilterChip(
                selected = filterMode == TagFilterMode.AND,
                onClick = { onFilterModeChange(TagFilterMode.AND) },
                label = { Text("AND", fontSize = 12.sp) },
                colors = FilterChipDefaults.filterChipColors(
                    selectedContainerColor = UiColors.PrimaryLight,
                    selectedLabelColor = UiColors.Primary
                )
            )
            FilterChip(
                selected = filterMode == TagFilterMode.NOT,
                onClick = { onFilterModeChange(TagFilterMode.NOT) },
                label = { Text("NOT", fontSize = 12.sp) },
                colors = FilterChipDefaults.filterChipColors(
                    selectedContainerColor = UiColors.PrimaryLight,
                    selectedLabelColor = UiColors.Primary
                )
            )
        }

        Spacer(modifier = Modifier.height(4.dp))

        // 标签列表（可滚动）
        LazyColumn(modifier = Modifier.fillMaxWidth()) {
            // "全部灵感"选项
            item {
                CategoryItem(
                    icon = DRAWER_ICON_ALL,
                    name = "全部灵感",
                    count = totalInspirationCount,
                    isSelected = selectedTags.isEmpty(),
                    showMenu = false,
                    onClick = { onClearTagSelection() }
                )
            }

            // 过滤后的标签列表
            items(filteredTags) { tag ->
                CategoryItem(
                    icon = "#",
                    name = tag,
                    count = tagCounts[tag] ?: 0,
                    isSelected = tag in selectedTags,
                    showMenu = false,
                    onClick = { onTagClick(tag) }
                )
            }
        }
    }
}
```

- [ ] **Step 6: 提交 Task 2**

```bash
git add app/src/main/java/com/corgimemo/app/ui/components/AppDrawer.kt
git commit -m "feat(inspiration): 重写侧边栏标签筛选区域，支持多选、搜索、计数"
```

---

## Task 3: MainScreen 接入 ViewModel 状态与回调

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt`

- [ ] **Step 1: 在 MainScreen 中收集灵感 ViewModel 的标签相关状态**

在 `MainScreen.kt` 中找到 `val inspirationViewModel: InspirationViewModel = hiltViewModel()`（约第 174 行），在其后添加以下状态收集代码（放在 `val corgiData by homeViewModel.corgiData.collectAsState()` 之前或之后均可，建议放在灵感 ViewModel 定义之后）：

```kotlin
    /** 灵感页标签相关状态（供侧边栏使用） */
    val inspirationTags by inspirationViewModel.savedTags.collectAsState()
    val selectedTags by inspirationViewModel.selectedTags.collectAsState()
    val tagFilterMode by inspirationViewModel.tagFilterMode.collectAsState()
    val tagCounts by inspirationViewModel.tagCounts.collectAsState()
    val totalInspirationCount by inspirationViewModel.totalInspirationCount.collectAsState()
```

- [ ] **Step 2: 在 `AppDrawerContent` 调用处传入灵感参数**

在 `MainScreen.kt` 中找到 `AppDrawerContent(` 调用（约第 321 行），在现有的参数列表中（`selectedCategoryId = selectedCategoryId,` 之后、`onCategoryClick` 之前）添加灵感相关参数：

```kotlin
                    inspirationTags = inspirationTags,
                    selectedTags = selectedTags,
                    tagFilterMode = tagFilterMode,
                    tagCounts = tagCounts,
                    totalInspirationCount = totalInspirationCount,
```

并在回调参数区域（`onRecentlyDeletedClick = { ... },` 之后、`onSettingsClick` 之前或 `onHelpClick` 之后）添加：

```kotlin
                    onTagClick = { tag ->
                        inspirationViewModel.toggleTagSelection(tag)
                    },
                    onTagFilterModeChange = { mode ->
                        inspirationViewModel.setTagFilterMode(mode)
                    },
                    onClearTagSelection = {
                        inspirationViewModel.clearTagSelection()
                    },
                    onAddTagClick = {
                        // 添加标签：跳转到标签管理页或弹出对话框（暂用 Toast 提示）
                        Toast.makeText(context, "添加标签功能开发中", Toast.LENGTH_SHORT).show()
                    },
```

注意：`Toast` 已在 `MainScreen.kt` 顶部 import（`import android.widget.Toast`，第 3 行），无需重复添加。

- [ ] **Step 3: 确认无需新增 import**

检查 `MainScreen.kt` 顶部 import 块：
- `TabItem`、`AppDrawerContent`、`Toast`、`collectAsState`、`InspirationViewModel` 均已存在
- `TagFilterMode` 不需要在 MainScreen 中直接引用（它作为参数传递，类型推断由 Kotlin 编译器处理）
- 无需新增任何 import

- [ ] **Step 4: 提交 Task 3**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt
git commit -m "feat(inspiration): MainScreen 接入标签筛选 ViewModel 状态与回调"
```

---

## Task 4: InspirationScreen 消费过滤后的列表

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt`

- [ ] **Step 1: 将 `displayInspirations` 替换为 `filteredDisplayInspirations`**

在 `InspirationScreen.kt` 中找到第 75 行：

```kotlin
    val displayItems by viewModel.displayInspirations.collectAsState()
```

替换为：

```kotlin
    val displayItems by viewModel.filteredDisplayInspirations.collectAsState()
```

- [ ] **Step 2: 提交 Task 4**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationScreen.kt
git commit -m "feat(inspiration): InspirationScreen 消费标签过滤后的显示列表"
```

---

## Task 5: Import 检查与编译验证

**Files:**
- 检查: 上述所有修改的文件

- [ ] **Step 1: 检查 InspirationViewModel.kt 的 import**

确认以下 import 已添加且无重复：
- `import kotlinx.coroutines.flow.combine`（Step 5 已添加）
- `import kotlinx.coroutines.flow.SharingStarted`（已存在）
- `import kotlinx.coroutines.flow.stateIn`（已存在）
- `import kotlinx.coroutines.flow.map`（已存在）

- [ ] **Step 2: 检查 AppDrawer.kt 的 import**

确认以下 import 已添加且无重复：
- `import com.corgimemo.app.viewmodel.TagFilterMode`
- `import androidx.compose.material3.FilterChip`
- `import androidx.compose.material3.FilterChipDefaults`
- `import androidx.compose.material3.OutlinedTextField`（可能已存在）
- `import androidx.compose.material3.OutlinedTextFieldDefaults`（可能已存在）
- `import androidx.compose.material.icons.filled.Close`
- `import androidx.compose.material.icons.filled.Search`
- `import androidx.compose.foundation.layout.Arrangement`（已存在）
- `import androidx.compose.runtime.mutableStateOf`（已存在）
- `import androidx.compose.runtime.remember`（已存在）
- `import androidx.compose.runtime.setValue`（已存在）

- [ ] **Step 3: 检查 MainScreen.kt 的 import**

确认无需新增 import（所有类型已在现有 import 中覆盖）。

- [ ] **Step 4: 检查 InspirationScreen.kt 的 import**

确认无需新增 import（仅替换了 StateFlow 名称）。

- [ ] **Step 5: 询问用户是否进行编译验证**

按照工作区规则（编译验证规则），**必须询问用户**是否进行编译验证，不得擅自执行编译命令。使用 AskUserQuestion 工具询问。

- [ ] **Step 6: 根据用户选择执行编译验证（可选）**

若用户同意，执行：

```bash
./gradlew assembleDebug
```

预期：BUILD SUCCESSFUL。若出现编译错误，根据错误信息修复（常见为 import 缺失或类型不匹配）。

---

## 验证标准对照

| 验证标准 | 对应 Task |
|------|------|
| 1. 侧边栏显示所有标签，每个标签右侧显示灵感计数 | Task 1 (tagCounts) + Task 2 (CategoryItem count) |
| 2. 点击标签可切换选中/取消（多选） | Task 1 (toggleTagSelection) + Task 2 (selectedTags) + Task 3 (onTagClick) |
| 3. OR/AND/NOT 三种模式可通过芯片按钮切换 | Task 1 (setTagFilterMode) + Task 2 (FilterChip) + Task 3 (onTagFilterModeChange) |
| 4. 搜索框实时过滤标签列表 | Task 2 (searchQuery + filteredTags) |
| 5. "全部灵感"点击清空所有选中标签 | Task 1 (clearTagSelection) + Task 2 (onClearTagSelection) + Task 3 (onClearTagSelection) |
| 6. "添加标签"按钮固定在底部（不随列表滚动） | Task 2 (AddCategoryButton 移到 INSPIRE 分支外部) |
| 7. 灵感列表根据选中标签和筛选模式正确过滤 | Task 1 (filteredDisplayInspirations) + Task 4 (InspirationScreen 消费) |
| 8. 编译通过，无 import 缺失 | Task 5 |
