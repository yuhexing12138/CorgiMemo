# 待办卡片滑动接触缩放动画 - 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在待办页面实现「滑动接触卡片时缩放 8% 后立即恢复，无水波纹」的交互优化，保持点击/长按原水波纹反馈。

**Architecture:** 抽取 `Modifier.pressFeedback()` 扩展函数封装三套反馈逻辑（滑动缩放 + 水波纹 + 长按），重构 TodoListItem 内部手势状态机。

**Tech Stack:** Jetpack Compose（BOM 2026.04.01 + Compose 1.9.2）、Kotlin、Android

---

## 文件结构

| 文件 | 变化 | 职责 |
|------|------|------|
| `app/src/main/java/com/corgimemo/app/ui/components/PressFeedback.kt` | 新建 | 封装 `Modifier.pressFeedback()` 扩展 + `PressFeedbackLogic` 纯函数 object + `PressState` 枚举 |
| `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt` | 重构 | 移除原 150 行 `pointerInput` 块，替换为新 Modifier；移除 `interactionChannel` 相关代码 |
| `app/src/test/java/com/corgimemo/app/ui/components/PressFeedbackLogicTest.kt` | 新建 | 测试 `PressFeedbackLogic` 纯函数 |

---

## Task 1: 创建 PressFeedbackLogic 纯函数与单元测试

**Files:**
- Create: `app/src/main/java/com/corgimemo/app/ui/components/PressFeedback.kt`
- Create: `app/src/test/java/com/corgimemo/app/ui/components/PressFeedbackLogicTest.kt`

- [ ] **Step 1.1: 创建 PressFeedback.kt 文件，先写 PressState 枚举和 PressFeedbackLogic 纯函数**

将整个 `app/src/main/java/com/corgimemo/app/ui/components/PressFeedback.kt` 文件创建为以下内容：

```kotlin
package com.corgimemo.app.ui.components

/**
 * 按压反馈状态机的状态枚举
 *
 * 用于将手势状态转换逻辑抽离为可独立测试的纯函数，
 * 避免依赖 Compose Runtime。
 */
enum class PressState {
    /** 静止状态：无任何反馈 */
    IDLE,
    /** 滑动接触状态：scale 缩放，无水波纹 */
    SCROLL_CONTACT,
    /** 按压状态：显示水波纹 */
    PRESS
}

/**
 * 按压反馈状态机的纯函数算法
 *
 * 将手势状态转换逻辑抽离为可独立测试的纯函数，
 * 避免依赖 Compose Runtime。
 */
object PressFeedbackLogic {

    /**
     * 根据当前 down 状态、移动距离、经过时间决定下一步状态
     *
     * @param currentState 当前状态
     * @param dragDistance 当前移动距离（px）
     * @param touchSlop 系统触摸阈值（px）
     * @param elapsedSinceDownMs 自 down 事件以来经过的毫秒数
     * @return 下一个状态
     */
    fun resolveNextState(
        currentState: PressState,
        dragDistance: Float,
        touchSlop: Float,
        elapsedSinceDownMs: Long
    ): PressState {
        // IDLE 状态由调用方根据是否处于 down 流程判断，此处不处理
        if (currentState == PressState.IDLE) return currentState

        // 在 down 后的前 16ms 内，若移动 > touchSlop → 切换为 SCROLL_CONTACT
        if (elapsedSinceDownMs < 16L && dragDistance > touchSlop) {
            return PressState.SCROLL_CONTACT
        }

        // 16ms 后未触发滑动 → 进入 PRESS
        if (elapsedSinceDownMs >= 16L && dragDistance <= touchSlop) {
            return PressState.PRESS
        }

        return currentState
    }

    /**
     * 根据目标状态计算 Animatable 目标 scale 值
     *
     * @param state 目标状态
     * @param scaleDown 缩小后的目标值（如 0.92f）
     * @return Animatable 的目标值
     */
    fun targetScaleForState(state: PressState, scaleDown: Float): Float {
        return when (state) {
            PressState.IDLE -> 1f
            PressState.SCROLL_CONTACT -> scaleDown
            PressState.PRESS -> 1f
        }
    }
}
```

- [ ] **Step 1.2: 创建单元测试文件**

将整个 `app/src/test/java/com/corgimemo/app/ui/components/PressFeedbackLogicTest.kt` 文件创建为以下内容：

```kotlin
package com.corgimemo.app.ui.components

import org.junit.Assert.assertEquals
import org.junit.Test

class PressFeedbackLogicTest {

    @Test
    fun `resolveNextState IDLE 状态保持 IDLE`() {
        val result = PressFeedbackLogic.resolveNextState(
            currentState = PressState.IDLE,
            dragDistance = 0f,
            touchSlop = 8f,
            elapsedSinceDownMs = 0L
        )
        assertEquals(PressState.IDLE, result)
    }

    @Test
    fun `resolveNextState 16ms 内移动超过 touchSlop 切换为 SCROLL_CONTACT`() {
        val result = PressFeedbackLogic.resolveNextState(
            currentState = PressState.SCROLL_CONTACT,
            dragDistance = 10f,
            touchSlop = 8f,
            elapsedSinceDownMs = 10L
        )
        assertEquals(PressState.SCROLL_CONTACT, result)
    }

    @Test
    fun `resolveNextState 16ms 后未移动 切换为 PRESS`() {
        val result = PressFeedbackLogic.resolveNextState(
            currentState = PressState.SCROLL_CONTACT,
            dragDistance = 0f,
            touchSlop = 8f,
            elapsedSinceDownMs = 20L
        )
        assertEquals(PressState.PRESS, result)
    }

    @Test
    fun `resolveNextState 16ms 内未移动 保持 SCROLL_CONTACT 等待 16ms 后再判断`() {
        // 在 16ms 内即使 dragDistance 较小，状态保持原状
        val result = PressFeedbackLogic.resolveNextState(
            currentState = PressState.SCROLL_CONTACT,
            dragDistance = 0f,
            touchSlop = 8f,
            elapsedSinceDownMs = 10L
        )
        assertEquals(PressState.SCROLL_CONTACT, result)
    }

    @Test
    fun `resolveNextState 16ms 后移动超过 touchSlop 保持 SCROLL_CONTACT`() {
        val result = PressFeedbackLogic.resolveNextState(
            currentState = PressState.SCROLL_CONTACT,
            dragDistance = 10f,
            touchSlop = 8f,
            elapsedSinceDownMs = 20L
        )
        assertEquals(PressState.SCROLL_CONTACT, result)
    }

    @Test
    fun `targetScaleForState IDLE 返回 1f`() {
        val result = PressFeedbackLogic.targetScaleForState(PressState.IDLE, 0.92f)
        assertEquals(1f, result, 0.001f)
    }

    @Test
    fun `targetScaleForState SCROLL_CONTACT 返回 scaleDown`() {
        val result = PressFeedbackLogic.targetScaleForState(PressState.SCROLL_CONTACT, 0.92f)
        assertEquals(0.92f, result, 0.001f)
    }

    @Test
    fun `targetScaleForState PRESS 返回 1f`() {
        val result = PressFeedbackLogic.targetScaleForState(PressState.PRESS, 0.92f)
        assertEquals(1f, result, 0.001f)
    }
}
```

- [ ] **Step 1.3: 运行单元测试验证通过**

Run:
```bash
./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.PressFeedbackLogicTest"
```

Expected: BUILD SUCCESSFUL，所有 8 个测试通过。

- [ ] **Step 1.4: 询问用户是否 git 提交**

按工作区规则询问用户是否提交。如果用户同意，使用中文提交信息：
```bash
git add app/src/main/java/com/corgimemo/app/ui/components/PressFeedback.kt app/src/test/java/com/corgimemo/app/ui/components/PressFeedbackLogicTest.kt
git commit -m "feat: 新增 PressFeedbackLogic 状态机纯函数与单元测试"
```

---

## Task 2: 实现 Modifier.pressFeedback() 扩展函数

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/PressFeedback.kt`

- [ ] **Step 2.1: 在 PressFeedback.kt 顶部添加 import 语句**

在 `package com.corgimemo.app.ui.components` 之后**追加**以下 import 块（不要删除 package 声明）：

```kotlin
import androidx.compose.foundation.gestures.awaitEachGesture
import androidx.compose.foundation.gestures.awaitFirstDown
import androidx.compose.foundation.interaction.MutableInteractionSource
import androidx.compose.foundation.interaction.PressInteraction
import androidx.compose.runtime.MutableFloatState
import androidx.compose.runtime.mutableFloatStateOf
import androidx.compose.runtime.remember
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.graphicsLayer
import androidx.compose.ui.input.pointer.PointerInputScope
import androidx.compose.ui.input.pointer.changedToUp
import androidx.compose.ui.input.pointer.pointerInput
import kotlinx.coroutines.delay
import kotlinx.coroutines.launch
```

- [ ] **Step 2.2: 在 PressFeedback.kt 末尾追加 Modifier.pressFeedback() 扩展函数**

将以下完整代码块追加到 PressFeedback.kt 文件末尾（PressFeedbackLogic 之后）：

```kotlin
/**
 * 为 Composable 提供统一的按压反馈修饰符
 *
 * 整合三套反馈逻辑：
 * 1. **滑动接触反馈**：滑动中手指接触时，卡片缩放至 0.92f 后恢复
 * 2. **点击/长按水波纹**：静止点击时显示水波纹
 * 3. **长按检测**：500ms 长按触发 onLongClick
 *
 * 状态机：
 * - down → 立即 scale 缩小（SCROLL_CONTACT 临时态）
 * - 16ms 内未移动 → 发射 Press（显示水波纹）
 * - 16ms 内移动 > touchSlop → 保持缩小（无水波纹）
 * - 手指抬起 → scale 恢复 + 发射 Release/Cancel
 *
 * 重要：scale 必须由调用方创建并传入（使用 remember { mutableFloatStateOf(1f) }），
 * 以保证 graphicsLayer 能在重组时读取最新值。
 *
 * @param interactionSource 外部传入的交互源，用于发射 Press/Release/Cancel 事件
 * @param scale 缩放状态（由调用方 remember 创建）
 * @param enabled 是否启用交互（false 时所有逻辑跳过）
 * @param isBatchMode 是否处于批量选择模式（保留参数以备扩展）
 * @param onTap 短按回调（< 500ms 抬起时触发）
 * @param onLongClick 长按回调（≥ 500ms 后抬起时触发）
 * @param scaleDown 缩小后的目标值，默认 0.92f
 * @param scaleDownDurationMs 缩小动画时长（毫秒），默认 60ms
 * @param scaleUpDurationMs 恢复动画时长（毫秒），默认 80ms
 * @param pressDelayMs Press 事件延迟发射时间（毫秒），默认 16ms
 * @return 包装了 graphicsLayer + pointerInput 的 Modifier
 */
fun Modifier.pressFeedback(
    interactionSource: MutableInteractionSource,
    scale: MutableFloatState,
    enabled: Boolean = true,
    isBatchMode: Boolean = false,
    onTap: () -> Unit = {},
    onLongClick: () -> Unit = {},
    scaleDown: Float = 0.92f,
    scaleDownDurationMs: Int = 60,
    scaleUpDurationMs: Int = 80,
    pressDelayMs: Long = 16L
): Modifier = this.then(
    if (!enabled) Modifier
    else Modifier
        .graphicsLayer {
            scaleX = scale.floatValue
            scaleY = scale.floatValue
        }
        .pointerInput(
            interactionSource,
            isBatchMode,
            onTap,
            onLongClick,
            scaleDown,
            scaleDownDurationMs,
            scaleUpDurationMs,
            pressDelayMs
        ) {
            // pointerInput 内部使用 this.launch 启动子协程
            // 这里的 this 是 PointerInputScope，launch 是其合法扩展
            awaitEachGesture {
                val down = awaitFirstDown(requireUnconsumed = false)
                val downPosition = down.position

                // 立即缩小 scale（视觉上立即反馈触摸）
                scale.floatValue = scaleDown

                // 启动 16ms 延迟 Press 事件发射
                var pressSent = false
                val pressInteraction = PressInteraction.Press(downPosition)
                val pressJob = launch {
                    delay(pressDelayMs)
                    interactionSource.emit(pressInteraction)
                    pressSent = true
                }

                // 启动 500ms 长按定时器
                var longPressTriggered = false
                val longPressJob = launch {
                    delay(500L)
                    longPressTriggered = true
                }

                var terminalEmitted = false
                try {
                    while (true) {
                        val event = awaitPointerEvent()
                        val change = event.changes.first()

                        // 手指抬起：触发 tap/longClick
                        if (change.changedToUp()) {
                            longPressJob.cancel()
                            pressJob.cancel()
                            if (pressSent) {
                                interactionSource.emit(PressInteraction.Release(pressInteraction))
                            }
                            terminalEmitted = true

                            if (longPressTriggered) {
                                onLongClick()
                            } else {
                                onTap()
                            }
                            // 恢复 scale
                            scale.floatValue = 1f
                            break
                        }

                        // 移动距离检测：超过 touchSlop 视为滑动接触
                        val dragDistance = (change.position - downPosition).getDistance()
                        val touchSlop = viewConfiguration.touchSlop
                        if (dragDistance > touchSlop) {
                            longPressJob.cancel()
                            pressJob.cancel()
                            if (pressSent) {
                                interactionSource.emit(PressInteraction.Cancel(pressInteraction))
                            }
                            terminalEmitted = true
                            // 恢复 scale
                            scale.floatValue = 1f
                            break
                        }
                    }
                } finally {
                    pressJob.cancel()
                    longPressJob.cancel()
                    if (!terminalEmitted && pressSent) {
                        interactionSource.emit(PressInteraction.Cancel(pressInteraction))
                    }
                    // 异常路径：恢复 scale
                    scale.floatValue = 1f
                }
            }
        }
)
```

- [ ] **Step 2.3: 询问用户是否 git 提交**

按工作区规则询问用户是否提交。如果同意，使用中文提交信息：
```bash
git add app/src/main/java/com/corgimemo/app/ui/components/PressFeedback.kt
git commit -m "feat: 实现 Modifier.pressFeedback 扩展函数"
```

---

## Task 3: 重构 TodoListItem.kt 使用新 Modifier

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt`

- [ ] **Step 3.1: 删除原 TodoListItem.kt 中的水波纹 + pointerInput 相关代码**

打开 [TodoListItem.kt](file:///C:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt)，**删除以下范围**：

| 范围 | 内容 |
|------|------|
| L184-L202 | `interactionSource` 变量、`interactionChannel` 变量、相关 `LaunchedEffect` 消费水波纹 |
| L210-L216 | Card modifier 中的 `.indication` 链式调用 |
| L217-L355 | 整个 `pointerInput` 块（包含 `awaitEachGesture` 与所有手势状态机代码） |

- [ ] **Step 3.2: 修改 Card 的 modifier 应用新 Modifier.pressFeedback()**

将 Card 的 `modifier` 参数替换为（注意：`onTap` 直接调用原 `onClick` / `onSelectClick` 回调，原 onClick 中的 `viewModel.onUserInteraction()` 调用已经在 onClick 闭包内部，无需在此处重复）：

```kotlin
val interactionSource = remember { MutableInteractionSource() }
// 缩放状态：由 Modifier.pressFeedback 驱动
val cardScale = remember { mutableFloatStateOf(1f) }

Card(
    modifier = Modifier
        .fillMaxWidth()
        .pressFeedback(
            interactionSource = interactionSource,
            scale = cardScale,
            isBatchMode = isBatchMode,
            onTap = {
                if (isBatchMode) {
                    onSelectClick()
                } else {
                    onClick()
                }
            },
            onLongClick = {
                if (!isBatchMode) {
                    HapticFeedbackManager.performHapticFeedback(
                        context = context,
                        type = InteractionType.LONG_CLICK,
                        enabled = hapticEnabled
                    )
                }
                onLongClick()
            },
            scaleDown = 0.92f,
            scaleDownDurationMs = 60,
            scaleUpDurationMs = 80
        ),
    elevation = CardDefaults.cardElevation(defaultElevation = 2.dp),
    shape = RoundedCornerShape(16.dp),
    colors = CardDefaults.cardColors(containerColor = cardBackground)
) {
    // 内容保持不变（原 L360-L719）
}
```

**设计说明**：原 TodoListItem 中 `onClick` 回调函数已经包含了 `viewModel.onUserInteraction()` 调用（在 [HomeScreen.kt L807](file:///C:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt#L807) 中传入时包裹），所以新 onTap 只需要透传原 onClick 即可，无需在此处重复副作用。

- [ ] **Step 3.3: 在 TodoListItem.kt 顶部添加缺失的 import**

确认以下 import 已存在（如缺失则添加）：

```kotlin
import androidx.compose.runtime.mutableFloatStateOf
```

确认以下 import 已被移除（如不再使用）：

```kotlin
import androidx.compose.ui.input.pointer.changedToUp
import androidx.compose.ui.input.pointer.pointerInput
import kotlinx.coroutines.delay
import kotlinx.coroutines.launch
import androidx.compose.foundation.interaction.PressInteraction
import androidx.compose.foundation.gestures.awaitEachGesture
import androidx.compose.foundation.gestures.awaitFirstDown
import androidx.compose.foundation.indication
import com.corgimemo.app.animation.HapticFeedbackManager  // 如果不在 isBatchMode 判断中使用则保留
import com.corgimemo.app.animation.InteractionType  // 如果不在 isBatchMode 判断中使用则保留
import kotlinx.coroutines.channels.Channel  // 如果未使用则移除
```

- [ ] **Step 3.4: 询问用户是否 git 提交**

按工作区规则询问用户。如果同意，使用中文提交信息：
```bash
git add app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt
git commit -m "refactor: 重构 TodoListItem 使用 Modifier.pressFeedback"
```

---

## Task 4: 修改 SubTaskInTodoListItem 应用新 Modifier

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt`

- [ ] **Step 4.1: 在 SubTaskInTodoListItem 函数签名中添加新参数**

将 SubTaskInTodoListItem 的函数签名修改为：

```kotlin
@Composable
private fun SubTaskInTodoListItem(
    subTask: SubTask,
    isParentCompleted: Boolean = false,
    isEnabled: Boolean = true,
    onToggleComplete: () -> Unit = {},
    onDisabledLongPress: () -> Unit = {},
    interactionSource: MutableInteractionSource = remember { MutableInteractionSource() },
    cardScale: MutableFloatState = remember { mutableFloatStateOf(1f) },
    modifier: Modifier = Modifier
) {
```

- [ ] **Step 4.2: 在 SubTaskInTodoListItem 的 Row 上应用 pressFeedback Modifier**

将 SubTaskInTodoListItem 中的 Row 修改为：

```kotlin
Row(
    modifier = modifier
        .fillMaxWidth()
        .pressFeedback(
            interactionSource = interactionSource,
            scale = cardScale,
            enabled = isEnabled,
            onTap = { onToggleComplete() },
            onLongClick = { onDisabledLongPress() }
        ),
    verticalAlignment = Alignment.CenterVertically
) {
```

- [ ] **Step 4.3: 询问用户是否进行 git 提交**

按工作区规则询问用户。如果同意，使用中文提交信息：
```bash
git add app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt
git commit -m "feat: 子任务卡片应用 Modifier.pressFeedback"
```

---

## Task 5: 编译验证与修复

**Files:**
- Verify: 上述修改的所有 .kt 文件

- [ ] **Step 5.1: 检查 import 语句完整性**

按工作区规则 `import语句检查.md`，检查所有修改后的 .kt 文件，确认：
- `import androidx.compose.runtime.mutableFloatStateOf` 已添加
- 未使用的 import 已移除（如 `pointerInput` / `changedToUp` / `delay` 等）
- 新使用的 import 已添加

- [ ] **Step 5.2: 询问用户是否进行编译验证**

按工作区规则 `编译验证.md`，**必须询问用户**：

> "已修改文件，是否进行编译验证？"

如果用户同意：
```bash
./gradlew :app:compileDebugKotlin
```

- [ ] **Step 5.3: 修复编译错误（如有）**

如果编译失败，根据错误信息修复。常见问题：
- 未导入类型（如 `mutableFloatStateOf`）
- 类型签名不匹配（如 `MutableFloatState` vs `MutableState<Float>`）

---

## Task 6: 手动验证（按设计文档 7.3 清单）

**Files:** 无（仅验证）

- [ ] **Step 6.1: 在真机/模拟器上运行 App，进入待办页面**

执行：
```bash
./gradlew :app:installDebug
adb shell am start -n com.corgimemo.app/.ui.MainActivity
```

- [ ] **Step 6.2: 逐项执行设计文档 7.3 中的 10 个测试场景**

| # | 场景 | 预期 |
|---|------|------|
| 1 | 快速下滑 | 滑过的卡片缩放 0.92f 后恢复，无水波纹 |
| 2 | 静止短按 | 正常水波纹 |
| 3 | 长按 500ms | 长按震动 + 水波纹 |
| 4 | 多选点击 | 选中态切换 + 缩放 |
| 5 | 左滑 | 无新缩放，无水波纹 |
| 6 | 拖拽 | 拖拽卡片保持原比例 |
| 7 | 滑动后点击 | 正常水波纹 |
| 8 | 已完成卡片 | 同样有缩放 |
| 9 | 子任务滑动 | 子任务项缩放 |
| 10 | 滑动中切换 | scale 正确重置 |

- [ ] **Step 6.3: 询问用户是否进行 git 提交**

按工作区规则询问用户。

---

## Task 7: 性能验证

**Files:** 无

- [ ] **Step 7.1: 监控滑动 50 个卡片时的帧耗时**

执行：
```bash
adb logcat -c
adb logcat -s AnimationPerf
# 在 App 中快速上下滑动列表
```

- [ ] **Step 7.2: 确认无帧耗时超过 16ms 的警告**

如果没有警告，继续。如果有警告，回到 Task 5 排查。

- [ ] **Step 7.3: 询问用户是否进行最终 git 提交**

提交信息：
```bash
git commit -m "feat: 完成待办卡片滑动接触缩放动画"
```

---

## 风险与注意事项

1. **pointerInput 作用域限制**：使用 `this.launch { ... }`（PointerInputScope 的合法扩展），不可用 GlobalScope
2. **scale 状态管理**：使用 `mutableFloatStateOf`（Compose 1.9+ 推荐）而非 `mutableStateOf<Float>`，避免自动装箱
3. **Modifier 顺序**：`pressFeedback` 必须在 `fillMaxWidth()` 之后、`offset` 之前，确保缩放基于卡片实际尺寸
4. **多选模式兼容**：`isBatchMode` 参数会传递到 onTap 语义，调用方需正确处理 onSelectClick
5. **回调闭包过期**：如 onTap / onLongClick 在 useEffect 中频繁变化，需考虑使用 `rememberUpdatedState`
