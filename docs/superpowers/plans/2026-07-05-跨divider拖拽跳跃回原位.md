# 跨 divider 拖拽跳跃回原位修复 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 divider 未被 ReorderableItem 包裹 + DragZoneStateMachine.inferZone 不感知 divider 两个根因，消除跨 divider 拖拽时的"跳跃回原位"现象。

**Architecture:** divider 包裹 `ReorderableItem(enabled=true)` + `Modifier.draggable(enabled=false)` 使其可作为 onMove 的 to 目标；新增 `inferZoneFromDisplayItems` 基于含 divider 的 displayItems 推断 zone；DragZoneStateMachine 新增 `setZone` 方法允许外部直接设置 currentZone。

**Tech Stack:** Kotlin、Jetpack Compose、Calvin-LL/Reorderable 库、JUnit 4

---

## File Structure

| 文件 | 类型 | 责任 |
|------|------|------|
| `app/src/main/java/com/corgimemo/app/ui/components/DragZoneStateMachine.kt` | 修改 | 新增 `setZone` public 方法 |
| `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt` | 修改 | ① divider 包裹 ReorderableItem；② onMove 校验简化；③ 调用 inferZoneFromDisplayItems + setZone；④ 新增 inferZoneFromDisplayItems top-level internal 函数 |
| `app/src/test/java/com/corgimemo/app/ui/components/DragZoneStateMachineTest.kt` | 修改 | 新增 3 个 setZone 测试 |
| `app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt` | 修改 | 新增 5 个 inferZoneFromDisplayItems 测试 |

---

## Task 1: 新增 inferZoneFromDisplayItems 纯函数 + 5 个单元测试

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt`（在文件末尾添加 top-level internal 函数）
- Modify: `app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt`（追加 5 个测试）

- [ ] **Step 1: 在 ZonedReorderableLazyColumnTest.kt 末尾追加 5 个失败测试**

打开 `app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt`，在文件末尾的最后一个 `}` 之前（即类的最后一个测试之后）追加以下 5 个测试方法：

```kotlin
    // ==================== inferZoneFromDisplayItems 测试 ====================

    /**
     * 场景：扫描到 PinnedDivider 返回 PINNED_PENDING
     *
     * 列表：[PinnedDivider, A, B]（A/B 均为 PINNED_PENDING）
     * 被拖项当前在 idx=2（B 的位置）
     *
     * 预期：扫描 newDisplay[1]=A（Todo，继续）→ newDisplay[0]=PinnedDivider → 返回 PINNED_PENDING
     */
    @Test
    fun `inferZoneFromDisplayItems 扫描到 PinnedDivider 返回 PINNED_PENDING`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PinnedDivider(count = 2, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1, isPinned = true)),
            DisplayItem.Todo(buildItem(id = 2, isPinned = true))
        )
        val result = inferZoneFromDisplayItems(
            displayItems = displayItems,
            draggedIndex = 2
        )
        assertEquals(TodoZone.PINNED_PENDING, result)
    }

    /**
     * 场景：扫描到 PendingDivider 返回 PENDING
     *
     * 列表：[PinnedDivider, A, PendingDivider, B]
     * 被拖项当前在 idx=3（B 的位置）
     *
     * 预期：扫描 newDisplay[2]=PendingDivider → 返回 PENDING
     */
    @Test
    fun `inferZoneFromDisplayItems 扫描到 PendingDivider 返回 PENDING`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PinnedDivider(count = 1, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1, isPinned = true)),
            DisplayItem.PendingDivider(count = 1, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 2))
        )
        val result = inferZoneFromDisplayItems(
            displayItems = displayItems,
            draggedIndex = 3
        )
        assertEquals(TodoZone.PENDING, result)
    }

    /**
     * 场景：扫描到 CompletedDivider 返回 COMPLETED
     *
     * 列表：[PendingDivider, A, CompletedDivider, X]
     * 被拖项当前在 idx=3（X 的位置）
     *
     * 预期：扫描 newDisplay[2]=CompletedDivider → 返回 COMPLETED
     */
    @Test
    fun `inferZoneFromDisplayItems 扫描到 CompletedDivider 返回 COMPLETED`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PendingDivider(count = 1, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1)),
            DisplayItem.CompletedDivider(count = 1, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 2, status = 1))
        )
        val result = inferZoneFromDisplayItems(
            displayItems = displayItems,
            draggedIndex = 3
        )
        assertEquals(TodoZone.COMPLETED, result)
    }

    /**
     * 场景：跨过多项找到最近的 divider
     *
     * 列表：[PinnedDivider, A, B, C]（A/B/C 均为 PINNED_PENDING）
     * 被拖项当前在 idx=3（C 的位置）
     *
     * 预期：扫描 newDisplay[2]=B、newDisplay[1]=A（Todo，继续）→ newDisplay[0]=PinnedDivider → 返回 PINNED_PENDING
     */
    @Test
    fun `inferZoneFromDisplayItems 跨过多项找到最近的 divider`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.PinnedDivider(count = 3, isExpanded = true),
            DisplayItem.Todo(buildItem(id = 1, isPinned = true)),
            DisplayItem.Todo(buildItem(id = 2, isPinned = true)),
            DisplayItem.Todo(buildItem(id = 3, isPinned = true))
        )
        val result = inferZoneFromDisplayItems(
            displayItems = displayItems,
            draggedIndex = 3
        )
        assertEquals(TodoZone.PINNED_PENDING, result)
    }

    /**
     * 场景：无 divider 回退到被拖项自身 zone
     *
     * 列表：[A, B, C]（无 divider，A/B/C 均为 PENDING）
     * 被拖项当前在 idx=1（B 的位置）
     *
     * 预期：扫描到列表开头仍无 divider → 回退到被拖项 B.zone() = PENDING
     */
    @Test
    fun `inferZoneFromDisplayItems 无 divider 回退到被拖项自身 zone`() {
        val displayItems = listOf<DisplayItem>(
            DisplayItem.Todo(buildItem(id = 1)),
            DisplayItem.Todo(buildItem(id = 2)),
            DisplayItem.Todo(buildItem(id = 3))
        )
        val result = inferZoneFromDisplayItems(
            displayItems = displayItems,
            draggedIndex = 1
        )
        assertEquals(TodoZone.PENDING, result)
    }
```

- [ ] **Step 2: 运行测试验证失败（函数未定义）**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ZonedReorderableLazyColumnTest" --no-daemon`

Expected: FAIL with "Unresolved reference: inferZoneFromDisplayItems"

- [ ] **Step 3: 在 ZonedReorderableLazyColumn.kt 末尾添加 inferZoneFromDisplayItems 纯函数**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt`，在文件末尾（`computeRelativeIndexInZone` 函数之后，文件最后一行 `}` 之后）添加：

```kotlin

/**
 * 基于 displayItems（含 divider）推断被拖项的当前 zone
 *
 * 算法：从被拖项位置向前扫描，遇到最近的 divider 即确定 zone：
 * - PinnedDivider → PINNED_PENDING（前面是置顶待完成区）
 * - PendingDivider → PENDING（前面是待完成区）
 * - CompletedDivider → COMPLETED（前面是已完成区）
 *
 * 若扫描到列表开头仍无 divider，根据被拖项自身 zone 推断（防御性回退）。
 *
 * 注：与 DragZoneStateMachine.inferZone 的区别：
 * - DragZoneStateMachine.inferZone 基于 todosOnly（divider 已过滤），看邻居 Todo 的 zone
 * - 本函数基于含 divider 的 displayItems，直接识别 divider 类型，准确度更高
 *
 * @param displayItems 含 divider 的完整显示列表
 * @param draggedIndex 被拖项在 displayItems 中的当前位置
 * @return 被拖项当前所在 zone
 */
internal fun inferZoneFromDisplayItems(
    displayItems: List<DisplayItem>,
    draggedIndex: Int
): TodoZone {
    // 1. 从被拖项前一项向前扫描，找最近的 divider
    for (i in draggedIndex - 1 downTo 0) {
        when (displayItems[i]) {
            is DisplayItem.PinnedDivider -> return TodoZone.PINNED_PENDING
            is DisplayItem.PendingDivider -> return TodoZone.PENDING
            is DisplayItem.CompletedDivider -> return TodoZone.COMPLETED
            else -> { /* Todo，继续向前扫描 */ }
        }
    }
    // 2. 扫描到列表开头仍无 divider，看被拖项自身 zone（单 zone 列表场景）
    val draggedItem = displayItems.getOrNull(draggedIndex)
    return if (draggedItem is DisplayItem.Todo) draggedItem.item.zone()
           else TodoZone.PENDING
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ZonedReorderableLazyColumnTest" --no-daemon`

Expected: PASS（9 个测试全部通过，含原 4 个 + 新增 5 个）

- [ ] **Step 5: 提交**

```powershell
git add app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt app/src/test/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumnTest.kt
$msg = @"
feat(reorder): 新增 inferZoneFromDisplayItems 纯函数与单元测试

新增基于含 divider 的 displayItems 推断被拖项 zone 的纯函数，
通过扫描前面最近的 divider 准确识别 zone 边界，
解决 DragZoneStateMachine.inferZone 基于 todosOnly 不感知 divider 的问题。

覆盖 5 种场景：
- 扫描到 PinnedDivider 返回 PINNED_PENDING
- 扫描到 PendingDivider 返回 PENDING
- 扫描到 CompletedDivider 返回 COMPLETED
- 跨过多项找到最近的 divider
- 无 divider 回退到被拖项自身 zone
"@
git commit -m $msg
```

---

## Task 2: 新增 DragZoneStateMachine.setZone 方法 + 3 个单元测试

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/DragZoneStateMachine.kt`（在 `endDrag()` 方法之后添加 `setZone` 方法）
- Modify: `app/src/test/java/com/corgimemo/app/ui/components/DragZoneStateMachineTest.kt`（追加 3 个测试）

- [ ] **Step 1: 在 DragZoneStateMachineTest.kt 末尾追加 3 个失败测试**

打开 `app/src/test/java/com/corgimemo/app/ui/components/DragZoneStateMachineTest.kt`，在文件末尾的最后一个 `}` 之前追加以下 3 个测试方法：

```kotlin
    // ==================== setZone 测试 ====================

    /**
     * 场景：setZone 同 zone 不触发翻转
     *
     * startDrag(A/PENDING) → setZone(PENDING)
     *
     * 预期：返回 false；visualIsPinned/visualStatus 不变
     */
    @Test
    fun `setZone 同 zone 不触发翻转`() {
        sm.startDrag(buildItem(id = 1, isPinned = false, status = 0))
        val result = sm.setZone(TodoZone.PENDING)
        assertFalse(result)
        assertEquals(false, sm.visualIsPinned)
        assertEquals(0, sm.visualStatus)
        assertEquals(TodoZone.PENDING, sm.currentZone)
    }

    /**
     * 场景：setZone 跨 zone 触发视觉翻转
     *
     * startDrag(A/PENDING) → setZone(COMPLETED)
     *
     * 预期：返回 true；visualStatus=1（PENDING → COMPLETED 翻转 status）
     */
    @Test
    fun `setZone 跨 zone 触发视觉翻转`() {
        sm.startDrag(buildItem(id = 1, isPinned = false, status = 0))
        val result = sm.setZone(TodoZone.COMPLETED)
        assertTrue(result)
        assertEquals(1, sm.visualStatus)
        assertEquals(false, sm.visualIsPinned)  // PENDING 和 COMPLETED 都是 isPinned=false，不翻转
        assertEquals(TodoZone.COMPLETED, sm.currentZone)
    }

    /**
     * 场景：setZone 跨 zone 后再 setZone 同 zone 不触发
     *
     * startDrag(A/PENDING) → setZone(COMPLETED) → setZone(COMPLETED)
     *
     * 预期：第一次返回 true，第二次返回 false
     */
    @Test
    fun `setZone 跨 zone 后再 setZone 同 zone 不触发`() {
        sm.startDrag(buildItem(id = 1, isPinned = false, status = 0))
        val first = sm.setZone(TodoZone.COMPLETED)
        val second = sm.setZone(TodoZone.COMPLETED)
        assertTrue(first)
        assertFalse(second)
        assertEquals(TodoZone.COMPLETED, sm.currentZone)
    }
```

- [ ] **Step 2: 运行测试验证失败（方法未定义）**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.DragZoneStateMachineTest" --no-daemon`

Expected: FAIL with "Unresolved reference: setZone"

- [ ] **Step 3: 在 DragZoneStateMachine.kt 的 endDrag() 方法之后添加 setZone 方法**

打开 `app/src/main/java/com/corgimemo/app/ui/components/DragZoneStateMachine.kt`，找到 `fun endDrag(): ZoneDragResult { ... }` 方法（约第 77-85 行），在其后（`reset()` 方法之前）添加：

```kotlin

    /**
     * 直接设置 currentZone 并应用视觉层翻转
     *
     * 用于外部基于更准确的信息源（如含 divider 的 displayItems）推断 zone 后，
     * 显式同步到状态机。
     *
     * 与 [onPositionChanged] 的区别：
     * - [onPositionChanged] 内部调用 inferZone 推断（基于 todosOnly）
     * - 本方法由调用方推断后传入，状态机只负责应用翻转
     *
     * @param newZone 新的 currentZone
     * @return true 表示发生跨区（currentZone 变化）
     */
    fun setZone(newZone: TodoZone): Boolean {
        if (newZone == currentZone) return false
        applyZoneTransition(currentZone, newZone)
        currentZone = newZone
        return true
    }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.DragZoneStateMachineTest" --no-daemon`

Expected: PASS（22 个测试全部通过，含原 19 个 + 新增 3 个）

- [ ] **Step 5: 提交**

```powershell
git add app/src/main/java/com/corgimemo/app/ui/components/DragZoneStateMachine.kt app/src/test/java/com/corgimemo/app/ui/components/DragZoneStateMachineTest.kt
$msg = @"
feat(reorder): DragZoneStateMachine 新增 setZone 方法与单元测试

新增 setZone(newZone): Boolean public 方法，允许外部基于更准确的
信息源（如含 divider 的 displayItems）推断 zone 后显式同步到状态机。

与 onPositionChanged 的区别：
- onPositionChanged 内部调用 inferZone 推断（基于 todosOnly）
- setZone 由调用方推断后传入，状态机只负责应用翻转

保留 onPositionChanged 接口不变，现有 19 个测试全部通过。
"@
git commit -m $msg
```

---

## Task 3: 修改 ZonedReorderableLazyColumn - divider 包裹 + onMove 简化

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt:88-131`（onMove 与 divider 渲染）

- [ ] **Step 1: 新增 import 语句**

打开 `app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt`，在文件顶部 import 区添加：

```kotlin
import androidx.compose.foundation.gestures.draggable
```

可放在 `import androidx.compose.foundation.layout.Box` 之前。

- [ ] **Step 2: 修改 onMove 校验与状态机调用**

定位 `onMove = { from, to ->` lambda（约第 88-116 行）。

修改前：
```kotlin
        onMove = { from, to ->
            val fromItem = displayItems.getOrNull(from.index)
            val toItem = displayItems.getOrNull(to.index)

            // ① 校验：from 和 to 都必须是 Todo 项（divider 不可拖拽、不可作为目标）
            if (fromItem !is DisplayItem.Todo || toItem !is DisplayItem.Todo) {
                return@rememberReorderableLazyListState
            }

            // ② 重排 displayItems
            val newDisplay = displayItems.toMutableList()
            newDisplay.removeAt(from.index)
            newDisplay.add(to.index, fromItem)
            displayItems = newDisplay

            // ③ 状态机追踪（仅 Todo 列表，过滤 divider）
            val todosOnly = newDisplay.filterIsInstance<DisplayItem.Todo>().map { it.item }
            val draggedTodoIndex = todosOnly.indexOfFirst { it.id == fromItem.item.id }
            val crossed = dragZoneState.onPositionChanged(todosOnly, draggedTodoIndex)

            // ④ 跨区触觉反馈
            if (crossed) {
                HapticFeedbackManager.performHapticFeedback(
                    context = context,
                    type = InteractionType.CONFIRM,
                    enabled = true
                )
            }
        }
```

修改后：
```kotlin
        onMove = { from, to ->
            val fromItem = displayItems.getOrNull(from.index)
            // ① 仅校验 from 是 Todo（divider 不可拖拽，由 Modifier.draggable(enabled=false) 保证）
            //    to 可以是 Todo 或 divider（divider 已加入 reorderableKeys，可作为目标）
            if (fromItem !is DisplayItem.Todo) {
                return@rememberReorderableLazyListState
            }

            // ② 重排 displayItems（A 跨过 divider / Todo，divider 不动）
            val newDisplay = displayItems.toMutableList()
            newDisplay.removeAt(from.index)
            newDisplay.add(to.index, fromItem)
            displayItems = newDisplay

            // ③ 基于 newDisplay 推断 currentZone（含 divider，准确识别 zone 边界）
            val newDraggedIdx = newDisplay.indexOfFirst {
                (it as? DisplayItem.Todo)?.item?.id == fromItem.item.id
            }
            val newZone = inferZoneFromDisplayItems(newDisplay, newDraggedIdx)
            val crossed = newZone != dragZoneState.currentZone
            if (crossed) {
                dragZoneState.setZone(newZone)
            }

            // ④ 跨区触觉反馈
            if (crossed) {
                HapticFeedbackManager.performHapticFeedback(
                    context = context,
                    type = InteractionType.CONFIRM,
                    enabled = true
                )
            }
        }
```

- [ ] **Step 3: 修改 divider 渲染分支，包裹 ReorderableItem**

定位 `when (item) {` 的 divider 分支（约第 125-131 行）。

修改前：
```kotlin
                is DisplayItem.PinnedDivider,
                is DisplayItem.PendingDivider,
                is DisplayItem.CompletedDivider -> {
                    // Divider 不可拖拽，直接渲染
                    content(index, item, false, isDragActive)
                }
```

修改后：
```kotlin
                is DisplayItem.PinnedDivider,
                is DisplayItem.PendingDivider,
                is DisplayItem.CompletedDivider -> {
                    // Divider 包裹 ReorderableItem(enabled=true) 加入 reorderableKeys，
                    // 使其可作为 onMove 的 to 目标（其他项可跨过 divider）。
                    // 但 Modifier.draggable(enabled=false) 保证 divider 本身不可被拖拽。
                    val dividerKey = when (item) {
                        is DisplayItem.PinnedDivider -> "pinned_divider"
                        is DisplayItem.PendingDivider -> "pending_divider"
                        is DisplayItem.CompletedDivider -> "completed_divider"
                    }
                    ReorderableItem(
                        state = reorderableState,
                        key = dividerKey,
                        enabled = true   // 加入 reorderableKeys，可作为 to
                    ) {
                        Box(
                            modifier = Modifier.draggable(enabled = false)  // 不可拖拽
                        ) {
                            content(index, item, false, isDragActive)
                        }
                    }
                }
```

- [ ] **Step 4: 验证 import 语句完整**

- `DisplayItem` 已在文件顶部 import（第 19 行）
- `ReorderableItem` 已在文件顶部 import（第 20 行）
- `draggable` 是 Step 1 新增的 import
- `inferZoneFromDisplayItems` 是 Task 1 新增的同文件 top-level internal 函数，无需 import
- `dragZoneState.setZone` 是 Task 2 新增的 public 方法，无需 import

- [ ] **Step 5: 编译验证**

Run: `.\gradlew.bat :app:compileDebugKotlin --no-daemon`

Expected: BUILD SUCCESSFUL

注意：由于 DragZoneStateMachine.kt 已在 Task 2 修改，ZonedReorderableLazyColumn.kt 是同包，整体编译应能通过。

- [ ] **Step 6: 提交**

```powershell
git add app/src/main/java/com/corgimemo/app/ui/components/ZonedReorderableLazyColumn.kt
$msg = @"
refactor(reorder): divider 包裹 ReorderableItem + onMove 校验简化

修复跨 divider 拖拽跳跃回原位问题的两个根因：

1. divider 包裹 ReorderableItem(enabled=true) + draggable(enabled=false)，
   使 divider 加入 reorderableKeys 可作为 onMove 的 to 目标，
   但仍不可作为 from（不可被拖拽）。

2. onMove 校验简化：仅校验 from 是 Todo，to 可以是 Todo 或 divider。
   A 跨过 divider 时 onMove 触发，removeAt + add 使 A 跨到 divider 之后。

3. 状态机调用改为 inferZoneFromDisplayItems + dragZoneState.setZone，
   基于含 divider 的 displayItems 准确推断 zone 边界。
"@
git commit -m $msg
```

---

## Task 4: 回归测试与手动验证

**Files:**
- 无新增/修改文件

- [ ] **Step 1: 运行所有单元测试**

Run: `.\gradlew.bat :app:testDebugUnitTest --no-daemon`

Expected: BUILD SUCCESSFUL
- ReminderTimeFormatterTest: 13/13 通过
- MigrationTest: 3/3 通过
- HomeViewModelReorderTest: 3/3 通过
- DragZoneStateMachineTest: 22/22 通过（原 19 + Task 2 新增 3）
- HomeViewModelPinnedButtonTest: 3/3 通过
- TodoZoneTest: 4/4 通过
- ZonedReorderableLazyColumnTest: 9/9 通过（原 4 + Task 1 新增 5）
- 合计：57/57 通过

- [ ] **Step 2: 验证 Debug APK 编译**

Run: `.\gradlew.bat :app:assembleDebug --no-daemon`

Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 手动验证场景 1 - 置顶区 → 待完成区（跨 PendingDivider）**

1. 启动 app
2. 创建 1 条置顶待完成（A）+ 1 条非置顶待完成（B）
3. 长按 A，向下拖到 PendingDivider 与 B 之间释放
4. 验证：A 落在 PendingDivider 之后、B 之前，**不跳跃回原位**

- [ ] **Step 4: 手动验证场景 2 - 待完成区 → 已完成区（跨 CompletedDivider）**

1. 创建 1 条非置顶待完成（B）+ 1 条非置顶已完成（X）
2. 长按 B，向下拖到 CompletedDivider 与 X 之间释放
3. 验证：B 落在 CompletedDivider 之后、X 之前，**不跳跃回原位**

- [ ] **Step 5: 手动验证场景 3 - 已完成区 → 待完成区（跨 PendingDivider）**

1. 创建 1 条非置顶待完成（B）+ 1 条非置顶已完成（X）
2. 长按 X，向上拖到 PendingDivider 与 B 之间释放
3. 验证：X 跨回 PENDING 区，位置在 PendingDivider 之后、B 之前

- [ ] **Step 6: 手动验证场景 4 - 快速拖拽跨多 zone**

1. 创建 1 条置顶待完成（A）+ 1 条非置顶待完成（B）+ 1 条非置顶已完成（X）
2. 长按 A，快速向下拖到 CompletedDivider 与 X 之后释放
3. 验证：状态机正确翻转（A 变为 COMPLETED），最终位置正确

- [ ] **Step 7: 最终提交（如有手动验证发现的修复）**

若手动验证发现新问题，按 spec 中的风险章节排查并修复后提交。
若全部通过，无需额外提交。

---

## Self-Review

### 1. Spec coverage

| Spec 章节 | 对应 Task |
|----------|-----------|
| §3.1 方案选型（方案 M） | Task 3 |
| §3.3 架构变更（divider 包裹 + onMove 简化 + inferZoneFromDisplayItems + setZone） | Task 1 + 2 + 3 |
| §4.1 divider 包裹 ReorderableItem | Task 3 Step 3 |
| §4.2 onMove 校验简化 | Task 3 Step 2 |
| §4.3 inferZoneFromDisplayItems 设计 | Task 1 |
| §4.4 DragZoneStateMachine.setZone | Task 2 |
| §6.2 inferZoneFromDisplayItems 测试（5 个） | Task 1 |
| §6.3 setZone 测试（3 个） | Task 2 |
| §6.4 手动验证场景（4 个） | Task 4 |
| §8.1 实施范围与验收标准 | Task 4 |

✅ 无遗漏。

### 2. Placeholder scan

- 无 TBD / TODO / "implement later"
- 所有代码步骤均含完整代码
- 所有命令均含 PowerShell 兼容语法
- 无"Similar to Task N"引用

✅ 无 placeholder。

### 3. Type consistency

- `inferZoneFromDisplayItems` 签名：`(displayItems: List<DisplayItem>, draggedIndex: Int) -> TodoZone` —— Task 1 定义，Task 3 调用，一致
- `DragZoneStateMachine.setZone` 签名：`(newZone: TodoZone) -> Boolean` —— Task 2 定义，Task 3 调用，一致
- `DisplayItem.PinnedDivider` / `PendingDivider` / `CompletedDivider` 类型 —— Task 1/3 使用，与项目现有类型一致
- `TodoZone` 4 值枚举 —— Task 1/2/3 使用，与项目现有类型一致

✅ 类型一致。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-05-跨divider拖拽跳跃回原位.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?
