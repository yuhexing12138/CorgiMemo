# 已完成区拖拽位置跳跃修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `checkPinnedZoneCrossed` 算法在已完成区内拖拽时误判跨区的 bug，让已完成区内拖拽不再翻转 `isPinned`。

**Architecture:** 修改 `ReorderAlgorithms.checkPinnedZoneCrossed` 单一函数：将 `currentZone=COMPLETED` 的分支从"基于 divider 直接判定"改为"回退到找邻居 + 比较 isPinned"，与无 divider 场景保持一致。算法层修复后，业务层 `reorderOnDisplayList` 不再误收 `crossedPinnedZone=true`，避免 `isPinned` 错误翻转。

**Tech Stack:** Kotlin、JUnit 4、Gradle、Compose 1.9.2

---

## File Structure

| 文件 | 职责 | 修改类型 |
|------|------|----------|
| `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt` | `checkPinnedZoneCrossed` 算法实现 | 修改第 2 步分支条件 |
| `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt` | 算法层测试 | 已添加 3 个新测试（当前失败），修复后应通过 |

---

## Task 1: 运行新增测试验证 bug 存在

**Files:**
- Test: `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`

- [ ] **Step 1: 运行 3 个新增的已完成区拖拽测试**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest.已完成区内向前拖拽不应跨区" --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest.已完成区内向后拖拽不应跨区" --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest.已完成区内拖到开头不应跨区"`

Expected: 3 tests FAILED with `java.lang.AssertionError`

- [ ] **Step 2: 不做提交（测试已添加在前序工作中）**

这是验证步骤，确认 bug 存在。无需 commit。

---

## Task 2: 修改 `checkPinnedZoneCrossed` 算法

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt:157-161`

- [ ] **Step 1: 修改算法第 2 步分支条件**

将 [ReorderableLazyColumn.kt:157-161](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt#L157-161) 中：

```kotlin
        // 2. 若找到 divider，基于 divider 类型直接判定
        if (currentZone != null) {
            val originalZone = if (draggedOriginalIsPinned) DividerKind.PINNED else DividerKind.PENDING
            return currentZone != originalZone
        }
```

替换为：

```kotlin
        // 2. 基于 divider 类型判定（仅对 PINNED 和 PENDING 区有效）
        //    已完成区（COMPLETED）没有 PinnedDivider，无法用 divider 判断置顶边界，
        //    应回退到"找邻居 + 比较 isPinned"逻辑
        if (currentZone == DividerKind.PINNED || currentZone == DividerKind.PENDING) {
            val originalZone = if (draggedOriginalIsPinned) DividerKind.PINNED else DividerKind.PENDING
            return currentZone != originalZone
        }
```

- [ ] **Step 2: 同步更新算法的 KDoc 注释**

将 [ReorderableLazyColumn.kt:119-137](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt#L119-137) 的 KDoc 中的"混合算法"描述更新为：

```kotlin
    /**
     * 检测拖拽是否跨越了置顶区边界
     *
     * 混合算法：
     * 1. 优先基于 divider 类型判定：扫描被拖项前面最近的 divider，
     *    根据 divider 类型直接确定当前所在区域（PINNED/PENDING）
     * 2. 若 currentZone=COMPLETED 或无 divider：回退到"找邻居 + 比较 isPinned"
     *
     * 为什么 COMPLETED 区要回退？
     * - 已完成区内没有 PinnedDivider，无法用 divider 类型判断置顶边界
     * - COMPLETED 的语义是"已完成区"，与 PINNED/PENDING 是不同维度，不能直接比较
     * - 已完成区同样支持置顶（visibleCompletedTodos 排序含 isPinned 降序）
     *
     * @param displayItems 全局显示列表（含分隔按钮和待办项）
     * @param isPinned 查询项是否置顶
     * @param dividerKind 查询项的分隔按钮类型，返回 null 表示非 divider
     * @param draggedOriginalIsPinned 被拖项原始 isPinned
     * @param draggedCurrentIndex 被拖项当前在 displayItems 中的索引
     * @return true 表示跨越了置顶区边界（需翻转 isPinned）
     */
```

- [ ] **Step 3: 运行新增的 3 个测试验证修复**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest.已完成区内向前拖拽不应跨区" --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest.已完成区内向后拖拽不应跨区" --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest.已完成区内拖到开头不应跨区"`

Expected: 3 tests PASSED

---

## Task 3: 回归测试 - 算法层全套

**Files:**
- Test: `app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`

- [ ] **Step 1: 运行 ReorderAlgorithmsTest 全部测试**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.ui.components.ReorderAlgorithmsTest"`

Expected: 20 tests PASSED（17 原有 + 3 新增）

- [ ] **Step 2: 检查测试报告无失败**

如有失败，定位是哪个原有测试受影响。预期所有原有 PINNED/PENDING 场景测试不受影响（因为修改只影响 `currentZone=COMPLETED` 分支）。

---

## Task 4: 回归测试 - ViewModel 层全套

**Files:**
- Test: `app/src/test/java/com/corgimemo/app/viewmodel/HomeViewModelReorderTest.kt`

- [ ] **Step 1: 运行 HomeViewModelReorderTest 全部测试**

Run: `.\gradlew.bat :app:testDebugUnitTest --tests "com.corgimemo.app.viewmodel.HomeViewModelReorderTest"`

Expected: 全部 PASSED

- [ ] **Step 2: 检查是否有跨区拖拽测试受影响**

如果有测试失败，需要确认是否是预期变化。但根据 spec 分析，本次修改不应影响 ViewModel 层行为（ViewModel 接收的 `crossedPinnedZone` 信号在已完成区内拖拽场景下从误判 `true` 变为正确 `false`，这正是修复目标）。

---

## Task 5: 提交修复

**Files:**
- All modified files

- [ ] **Step 1: 检查 git status**

Run: `git status`

Expected: 显示 `ReorderableLazyColumn.kt` 和 `ReorderAlgorithmsTest.kt` 修改

- [ ] **Step 2: 添加修改的文件**

Run: `git add app/src/main/java/com/corgimemo/app/ui/components/ReorderableLazyColumn.kt app/src/test/java/com/corgimemo/app/ui/components/ReorderAlgorithmsTest.kt`

- [ ] **Step 3: 提交修复**

Run: `git commit -m "fix(拖拽): 修复已完成区内拖拽位置跳跃 bug

修改 checkPinnedZoneCrossed 算法：当 currentZone=COMPLETED 时回退到
'找邻居 + 比较 isPinned' 逻辑，而非基于 divider 类型直接判定。

根因：已完成区内没有 PinnedDivider，原算法把 originalZone=PENDING
（仅依据 isPinned=false 推导）与 currentZone=COMPLETED 比较，误判为跨区，
导致 isPinned 被错误翻转，引发位置跳跃。

修复后：已完成区内同区域拖拽不再翻转 isPinned，位置不再跳跃。

新增 3 个算法层测试覆盖：
- 已完成区内向前拖拽不应跨区
- 已完成区内向后拖拽不应跨区
- 已完成区内拖到开头不应跨区"`

Expected: 提交成功

---

## Self-Review

### 1. Spec coverage

| Spec 章节 | 对应 Task |
|----------|-----------|
| §3.3 算法修改（第 2 步分支条件） | Task 2 Step 1 |
| §3.3 KDoc 同步更新 | Task 2 Step 2 |
| §5.1 新增 3 个测试通过 | Task 2 Step 3 |
| §5.2 算法层回归测试 | Task 3 |
| §5.2 ViewModel 层回归测试 | Task 4 |
| §8 验收标准 1-3 | Task 2-4 |
| §8 验收标准 4（手动验证） | 不在 plan 内（用户手动） |

### 2. Placeholder scan

- 无 "TBD"、"TODO"、"implement later"
- 所有代码块均完整
- 所有命令均给出具体执行内容和期望输出

### 3. Type consistency

- `DividerKind.PINNED`、`DividerKind.PENDING`、`DividerKind.COMPLETED` 在 Task 2 中与原代码一致
- `checkPinnedZoneCrossed` 签名未改变
- `draggedOriginalIsPinned`、`draggedCurrentIndex` 参数名与原代码一致

### 4. 备注

- Task 1 是验证步骤，确认 bug 存在；实际修复在 Task 2
- Task 5 提交信息使用中文（遵循项目规则 `.trae/rules/git提交.md`）
- 手动验证（§8.4）由用户在真机或模拟器上执行，不在 plan 内
