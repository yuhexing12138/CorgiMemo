# 搜索框显隐动画跳跃修复 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将搜索框显隐从 `Animatable + LaunchedEffect + snapTo` 异步架构重构为 `derivedStateOf` 同步跟手 + `Animatable` 停止吸附的两阶段架构,消除滚动跳跃感。

**Architecture:** 滚动中用 `derivedStateOf` 同步计算 progress(1:1 跟手,无协程延迟),滚动停止时用 `Animatable.snapTo` 同步对齐起点后 `animateTo` 吸附到端点。layout/graphicsLayer 读取 `currentProgress`(派生状态,二选一)。

**Tech Stack:** Jetpack Compose,Animatable,derivedStateOf,LaunchedEffect,Kotlin

---

## 文件结构

| 文件 | 责任 | 操作 |
|------|------|------|
| `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 重构 L533-L638 状态管理;L651,L658,L843 改用 `currentProgress` | 修改 |
| `app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt` | `computeScrollDrivenProgress` 纯函数单元测试 | 新建 |

---

## Task 1: 新增 `computeScrollDrivenProgress` 纯函数及单元测试

**Files:**
- Create: `app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt`

**说明:** 先把 `derivedStateOf` 内的计算逻辑提取为纯函数 `computeScrollDrivenProgress`,便于单元测试。函数放在 HomeScreen.kt 顶层(private fun),测试用 `@VisibleForTesting` 暴露。

- [ ] **Step 1.1: 在 HomeScreen.kt 末尾添加纯函数(暂时放在文件尾部,后续 Task 2 会引用)**

打开 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`,在文件最后一个 `}` 之后(文件末尾)添加:

```kotlin
/**
 * 计算滚动驱动的搜索框显隐进度
 *
 * 规则:
 * - firstVisibleItemIndex == 0: progress = 1 - scrollOffset / searchBarHeight,clamp 到 [0, 1]
 * - firstVisibleItemIndex > 0: 强制返回 0f(第一项已完全滚出)
 *
 * @param firstVisibleItemIndex LazyListState.firstVisibleItemIndex
 * @param firstVisibleItemScrollOffset LazyListState.firstVisibleItemScrollOffset
 * @param searchBarHeightPx 搜索框完整高度(px)
 * @return 搜索框显隐进度 [0f, 1f],1f=完全显示,0f=完全隐藏
 */
internal fun computeScrollDrivenProgress(
    firstVisibleItemIndex: Int,
    firstVisibleItemScrollOffset: Int,
    searchBarHeightPx: Float
): Float {
    if (searchBarHeightPx <= 0f) return 1f
    return if (firstVisibleItemIndex == 0) {
        (1f - firstVisibleItemScrollOffset / searchBarHeightPx).coerceIn(0f, 1f)
    } else {
        0f
    }
}
```

- [ ] **Step 1.2: 新建测试文件**

创建 `app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt`:

```kotlin
package com.corgimemo.app.ui.screens.home

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * computeScrollDrivenProgress 纯函数单元测试
 *
 * 覆盖:
 * - 列表在顶部(firstVisibleItemIndex == 0)的各种 scrollOffset
 * - 第一项已完全滚出(firstVisibleItemIndex > 0)
 * - 边界情况(searchBarHeightPx <= 0)
 */
class SearchRevealProgressTest {

    private val searchBarHeight = 192f  // 64dp 在 xxhdpi(3x)下的 px 值

    @Test
    fun `列表在顶部且未滚动时进度为1`() {
        assertEquals(
            1f,
            computeScrollDrivenProgress(
                firstVisibleItemIndex = 0,
                firstVisibleItemScrollOffset = 0,
                searchBarHeightPx = searchBarHeight
            ),
            0.001f
        )
    }

    @Test
    fun `列表在顶部且滚动半高度时进度为0_5`() {
        assertEquals(
            0.5f,
            computeScrollDrivenProgress(
                firstVisibleItemIndex = 0,
                firstVisibleItemScrollOffset = 96,  // searchBarHeight / 2
                searchBarHeightPx = searchBarHeight
            ),
            0.001f
        )
    }

    @Test
    fun `列表在顶部且滚动超过高度时进度为0`() {
        assertEquals(
            0f,
            computeScrollDrivenProgress(
                firstVisibleItemIndex = 0,
                firstVisibleItemScrollOffset = 300,  // > searchBarHeight
                searchBarHeightPx = searchBarHeight
            ),
            0.001f
        )
    }

    @Test
    fun `第一项已完全滚出时进度强制为0`() {
        assertEquals(
            0f,
            computeScrollDrivenProgress(
                firstVisibleItemIndex = 1,
                firstVisibleItemScrollOffset = 0,
                searchBarHeightPx = searchBarHeight
            ),
            0.001f
        )
    }

    @Test
    fun `第一项已完全滚出时无论scrollOffset都为0`() {
        assertEquals(
            0f,
            computeScrollDrivenProgress(
                firstVisibleItemIndex = 2,
                firstVisibleItemScrollOffset = 500,
                searchBarHeightPx = searchBarHeight
            ),
            0.001f
        )
    }

    @Test
    fun `searchBarHeight为0时返回1f避免除零`() {
        assertEquals(
            1f,
            computeScrollDrivenProgress(
                firstVisibleItemIndex = 0,
                firstVisibleItemScrollOffset = 100,
                searchBarHeightPx = 0f
            ),
            0.001f
        )
    }

    @Test
    fun `searchBarHeight为负时返回1f防御`() {
        assertEquals(
            1f,
            computeScrollDrivenProgress(
                firstVisibleItemIndex = 0,
                firstVisibleItemScrollOffset = 100,
                searchBarHeightPx = -10f
            ),
            0.001f
        )
    }
}
```

- [ ] **Step 1.3: 运行测试验证通过**

Run: `.\gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.ui.screens.home.SearchRevealProgressTest" --no-daemon`
Expected: 7 个测试全部 PASS

- [ ] **Step 1.4: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt app/src/test/java/com/corgimemo/app/ui/screens/home/SearchRevealProgressTest.kt
git commit -m "feat(search-reveal): 新增 computeScrollDrivenProgress 纯函数及 7 个单元测试"
```

---

## Task 2: 重构 HomeScreen 状态管理为两阶段架构

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` L530-L638

**说明:** 这是核心重构 Task。将 `searchRevealProgress`(Animatable + snapTo 异步)替换为 `scrollDrivenProgress`(derivedStateOf 同步) + `animatableProgress`(停止吸附) + `currentProgress`(二选一)。

- [ ] **Step 2.1: 替换状态定义与 LaunchedEffect(L530-L601)**

打开 `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`,定位 L530-L601(从 `val searchQuery by viewModel.searchQuery.collectAsState()` 到 `LaunchedEffect(isScrolling) { ... }` 结束的 `}`)。

将以下旧代码:

```kotlin
                    val searchQuery by viewModel.searchQuery.collectAsState()

                    /** 滚动驱动搜索框显隐进度：1f=完全显示，0f=完全隐藏，使用Animatable支持平滑动画 */
                    val searchRevealProgress = remember { Animatable(1f) }

                    /** 订阅 LazyListState 滚动状态（含 fling + settle），Compose 官方统一管理，跨设备一致 */
                    val isScrolling = lazyListState.isScrollInProgress

                    /** 搜索框完整高度（含padding），作为滚动→进度映射的基准 */
                    val searchBarFullHeightPx = with(LocalDensity.current) { 64.dp.toPx() }

                    /**
                     * 绝对位置驱动搜索框进度：
                     * - 进度 = 1 - scrollOffsetFromTop / searchBarFullHeightPx
                     * - scrollOffsetFromTop 来自 LazyListState 的 firstVisibleItemIndex + firstVisibleItemScrollOffset
                     * - 这是单调函数，跨 item 切换平滑（v2 修复版）
                     *
                     * 设计动机（v2 修复）：替换 v2 原始版使用的 firstItem.offset
                     * - v2 原始问题：layoutInfo.visibleItemsInfo.first().offset 在第一个 item
                     *   完全滚出并切换到下一项时会突变（如 item 0 高度 80px，offset 从 -80 突变到 -16），
                     *   导致 progress 从 0 跳变回 0.75，引发「搜索框来回震荡」
                     * - v2 修复方案：改用官方 API firstVisibleItemIndex + firstVisibleItemScrollOffset，
                     *   Compose 框架内部维护这两个状态，跨 item 切换是单调函数，无跳变
                     * - 当 firstVisibleItemIndex > 0（第一项已完全滚出）时，强制 progress = 0，
                     *   避免任何突变
                     */
                    val scrollOffsetFromTop by remember {
                        derivedStateOf {
                            val firstIndex = lazyListState.firstVisibleItemIndex
                            if (firstIndex == 0) {
                                // 还在第一项内：使用 firstVisibleItemScrollOffset（已向下滚动的像素距离）
                                lazyListState.firstVisibleItemScrollOffset.toFloat()
                            } else {
                                // 第一项已完全滚出：强制 progress = 0（搜索框完全隐藏）
                                searchBarFullHeightPx + 1f
                            }
                        }
                    }

                    LaunchedEffect(scrollOffsetFromTop) {
                        val target = (1f - scrollOffsetFromTop / searchBarFullHeightPx).coerceIn(0f, 1f)
                        if (searchRevealProgress.value != target) {
                            searchRevealProgress.snapTo(target)
                        }
                    }

                    /**
                     * 滚动真正停止时触发 snap：
                     * - 直接订阅 lazyListState.isScrollInProgress，key 变化时 effect 自动重启
                     * - 滚动开始（true）或滚动中（保持 true）→ body 内 if 判断跳过
                     * - 滚动真正停止（false）→ 检查 progress，snap 到 0f 或 1f
                     * - 使用 Compose 官方状态，准确捕获 fling + settle 结束，跨设备一致
                     *
                     * 设计动机：替换原 150ms 时间戳 debounce 方案
                     * - 旧方案：fling 惯性滚动期间 firstVisibleItemIndex/Offset 持续发射微事件，
                     *   每次都重置 lastScrollTimeMs，150ms 窗口永远不通过，progress 卡在半路
                     * - 新方案：isScrollInProgress 由 Compose 框架统一维护 fling + settle 状态，
                     *   真正停止时才变 false，effect 重启执行 snap
                     */
                    LaunchedEffect(isScrolling) {
                        if (!isScrolling && searchQuery.isBlank()) {
                            val current = searchRevealProgress.value
                            if (current > 0.05f && current < 0.95f) {
                                val target = if (current < 0.5f) 0f else 1f
                                val duration = if (target == 0f) 200 else 250
                                searchRevealProgress.animateTo(
                                    targetValue = target,
                                    animationSpec = tween(duration, easing = FastOutSlowInEasing)
                                )
                            }
                        }
                    }
```

替换为:

```kotlin
                    val searchQuery by viewModel.searchQuery.collectAsState()

                    /** 订阅 LazyListState 滚动状态（含 fling + settle），Compose 官方统一管理，跨设备一致 */
                    val isScrolling = lazyListState.isScrollInProgress

                    /** 搜索框完整高度（含padding），作为滚动→进度映射的基准 */
                    val searchBarFullHeightPx = with(LocalDensity.current) { 64.dp.toPx() }

                    /**
                     * 阶段 1：滚动中同步跟手（无协程延迟）
                     *
                     * 直接基于 LazyListState 官方 API 派生 progress，1:1 跟随滚动 delta。
                     * - firstVisibleItemIndex == 0：progress = 1 - offset / searchBarHeight
                     * - firstVisibleItemIndex > 0：强制 0f（第一项已完全滚出）
                     *
                     * 替换原 Animatable + LaunchedEffect + snapTo 异步链路，
                     * 消除每帧 effect 重启 + 协程调度引入的 1-3 帧延迟（跳跃感根因）。
                     */
                    val scrollDrivenProgress by remember {
                        derivedStateOf {
                            computeScrollDrivenProgress(
                                firstVisibleItemIndex = lazyListState.firstVisibleItemIndex,
                                firstVisibleItemScrollOffset = lazyListState.firstVisibleItemScrollOffset,
                                searchBarHeightPx = searchBarFullHeightPx
                            )
                        }
                    }

                    /**
                     * 阶段 2：滚动停止时的吸附动画
                     *
                     * 仅在 isScrollInProgress == false 时由 currentProgress 读取。
                     * 滚动中此 Animatable 不参与计算，被 scrollDrivenProgress 覆盖。
                     */
                    val animatableProgress = remember { Animatable(1f) }

                    /**
                     * 当前生效的 progress（二选一）
                     *
                     * - 滚动中：scrollDrivenProgress（同步跟手）
                     * - 滚动停止：animatableProgress.value（吸附动画结果）
                     *
                     * layout/graphicsLayer 应读取本字段，不要直接读 Animatable.value。
                     */
                    val currentProgress = if (isScrolling) scrollDrivenProgress else animatableProgress.value

                    /**
                     * 滚动停止时：同步对齐起点 → animateTo 吸附端点
                     *
                     * - snapTo(scrollDrivenProgress) 同步对齐，消除阶段切换时的数值跳跃
                     * - animateTo(0f 或 1f) 用 tween 动画吸附到端点
                     *
                     * 阈值：< 0.5 吸附到 0f（隐藏），>= 0.5 吸附到 1f（显示）
                     * 仅在 searchQuery 为空时执行（搜索词非空时强制保持显示）
                     */
                    LaunchedEffect(isScrolling) {
                        if (!isScrolling && searchQuery.isBlank()) {
                            val start = scrollDrivenProgress
                            if (start > 0.05f && start < 0.95f) {
                                val target = if (start < 0.5f) 0f else 1f
                                val duration = if (target == 0f) 200 else 250
                                animatableProgress.snapTo(start)
                                animatableProgress.animateTo(
                                    targetValue = target,
                                    animationSpec = tween(duration, easing = FastOutSlowInEasing)
                                )
                            }
                        }
                    }
```

- [ ] **Step 2.2: 替换搜索词变化处理(L619-L638)**

定位 L619-L638(`LaunchedEffect(searchQuery) { ... }`),将以下旧代码:

```kotlin
                    LaunchedEffect(searchQuery) {
                        if (searchQuery.isNotBlank()) {
                            // 输入搜索词：立即显示
                            searchRevealProgress.animateTo(
                                1f,
                                animationSpec = tween(250, easing = FastOutSlowInEasing)
                            )
                        } else {
                            // 清空搜索词：基于当前 progress 主动 snap 到端点
                            val current = searchRevealProgress.value
                            if (current > 0.05f && current < 0.95f) {
                                val target = if (current < 0.5f) 0f else 1f
                                val duration = if (target == 0f) 200 else 250
                                searchRevealProgress.animateTo(
                                    targetValue = target,
                                    animationSpec = tween(duration, easing = FastOutSlowInEasing)
                                )
                            }
                        }
                    }
```

替换为:

```kotlin
                    /**
                     * 搜索词变化时驱动搜索框：
                     * - 输入搜索词（blank → 非 blank）→ 立即显示（250ms 展开）
                     * - 清空搜索词（非 blank → blank）→ 基于当前 progress 主动 snap 到端点
                     *
                     * 注意：animatableProgress.snapTo 用于同步对齐起点，避免从旧值动画到目标值产生跳跃
                     */
                    LaunchedEffect(searchQuery) {
                        if (searchQuery.isNotBlank()) {
                            // 输入搜索词：立即显示
                            val start = if (isScrolling) scrollDrivenProgress else animatableProgress.value
                            animatableProgress.snapTo(start)
                            animatableProgress.animateTo(
                                targetValue = 1f,
                                animationSpec = tween(250, easing = FastOutSlowInEasing)
                            )
                        } else {
                            // 清空搜索词：基于当前 progress 主动 snap 到端点
                            val current = if (isScrolling) scrollDrivenProgress else animatableProgress.value
                            if (current > 0.05f && current < 0.95f) {
                                val target = if (current < 0.5f) 0f else 1f
                                val duration = if (target == 0f) 200 else 250
                                animatableProgress.snapTo(current)
                                animatableProgress.animateTo(
                                    targetValue = target,
                                    animationSpec = tween(duration, easing = FastOutSlowInEasing)
                                )
                            }
                        }
                    }
```

- [ ] **Step 2.3: 替换搜索框容器 layout/graphicsLayer(L651, L658)**

定位 L651(`val targetHeight = (placeable.height * searchRevealProgress.value).roundToInt()`),改为:

```kotlin
                                val targetHeight = (placeable.height * currentProgress).roundToInt()
```

定位 L658(`.graphicsLayer { alpha = searchRevealProgress.value }`),改为:

```kotlin
                            .graphicsLayer { alpha = currentProgress }
```

- [ ] **Step 2.4: 替换列表级联偏移(L843)**

定位 L843(`translationY = -(1f - searchRevealProgress.value) * searchBarFullHeightPx * 0.12f`),改为:

```kotlin
                                        translationY = -(1f - currentProgress) * searchBarFullHeightPx * 0.12f
```

- [ ] **Step 2.5: 编译验证**

Run: `.\gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 2.6: 运行所有单元测试**

Run: `.\gradlew :app:testDebugUnitTest --no-daemon`
Expected: 所有测试 PASS(含 Task 1 新增的 7 个 SearchRevealProgressTest)

- [ ] **Step 2.7: 构建 Debug APK 验证**

Run: `.\gradlew :app:assembleDebug --no-daemon`
Expected: BUILD SUCCESSFUL

- [ ] **Step 2.8: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt
git commit -m "fix(search-reveal): 重构搜索框显隐为两阶段架构消除滚动跳跃感" -m "滚动中用 derivedStateOf 同步跟手,停止时用 Animatable.snapTo 对齐起点后 animateTo 吸附端点。替换原 Animatable + LaunchedEffect + snapTo 异步链路。layout/graphicsLayer/级联偏移改用 currentProgress。"
```

---

## Task 3: 清理无用 import 与变量

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

**说明:** 重构后可能产生无用 import 或变量,需检查清理。

- [ ] **Step 3.1: 检查 Animatable import 是否仍在使用**

Run: `grep -n "Animatable" app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

如果除 `import androidx.compose.animation.core.Animatable` 外仍有 `Animatable(...)` 调用(如 `animatableProgress = remember { Animatable(1f) }`),则保留 import;否则删除该 import。

预期: `Animatable(1f)` 仍被使用(Task 2 新增的 `animatableProgress`),保留 import。

- [ ] **Step 3.2: 检查 scrollOffsetFromTop 是否仍在使用**

Run: `grep -n "scrollOffsetFromTop" app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

预期: 无匹配(已被 `scrollDrivenProgress` 替代)。如果有残留引用,删除或替换为 `scrollDrivenProgress`。

- [ ] **Step 3.3: 检查 searchRevealProgress 是否仍在使用**

Run: `grep -n "searchRevealProgress" app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

预期: 无匹配(已被 `currentProgress` 替代)。如果有残留引用,替换为 `currentProgress`。

- [ ] **Step 3.4: 编译验证**

Run: `.\gradlew :app:compileDebugKotlin --no-daemon`
Expected: BUILD SUCCESSFUL, 无 warning

- [ ] **Step 3.5: 提交(如有改动)**

如果 Step 3.1-3.3 发现并修复了残留引用:

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt
git commit -m "chore(search-reveal): 清理重构后残留的旧变量引用"
```

如果无改动,跳过本 Task。

---

## 验证清单

完成所有 Task 后,在真机上验证以下场景:

- [ ] 慢速上滑:搜索框平滑收缩,无跳跃
- [ ] 快速上滑 fling:搜索框跟手收缩,停止后吸附到 0f
- [ ] 慢速下滑:搜索框平滑展开,无跳跃
- [ ] 快速下滑 fling:搜索框跟手展开,停止后吸附到 1f
- [ ] 滚动中输入搜索词:立即展开到 1f
- [ ] 滚动中清空搜索词:等停止后吸附
- [ ] 列表为空时滚动:无异常
- [ ] 置顶按钮存在时:不影响搜索框显隐

---

## 后续可优化项

1. **吸附阈值调优**:当前 < 0.5 吸附到 0f,可考虑改为 < 0.3 让搜索框更容易保持显示
2. **吸附动画时长调优**:当前隐藏 200ms / 显示 250ms,可根据真机手感调整
3. **级联偏移系数调优**:当前 0.12f(12%),可根据视觉效果调整
4. **Animatable 替换为 AnimatedFloat**:如果遇到性能问题,可考虑用更轻量的 `mutableFloatStateOf + animateFloatAsState`
