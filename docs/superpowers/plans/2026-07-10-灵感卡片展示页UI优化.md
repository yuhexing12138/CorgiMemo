# 灵感卡片展示页 UI 优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复灵感卡片展示页 4 类 UI 偏差：状态栏沉浸式、TopBar 高度自适应、卡片尺寸/字号、Logo 品牌升级。

**Architecture:** 局部启用沉浸式（仅在 InspirationViewScreen 的 Scaffold 设置 `contentWindowInsets = WindowInsets(0)`，避免全局影响其他页面）；使用 `WindowInsets.statusBars` 动态计算 TopBar 高度；卡片参数集中调整；字符串资源从 `strings.xml` 读取。

**Tech Stack:** Jetpack Compose (BOM 2026.04.01) · `androidx.activity.compose.enableEdgeToEdge()` · `androidx.compose.foundation.layout.WindowInsets.statusBars` · Material 3

**Spec:** [2026-07-10-灵感卡片展示页UI优化-design.md](../specs/2026-07-10-灵感卡片展示页UI优化-design.md)

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `app/src/main/java/com/corgimemo/app/ui/MainActivity.kt` | 修改 | 在 `onCreate` 调用 `enableEdgeToEdge()` |
| `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationViewScreen.kt` | 修改 | Scaffold 设置 `contentWindowInsets = WindowInsets(0)`，TopBar 改为 Column 动态状态栏高度 |
| `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationViewCard.kt` | 修改 | 字号/外边距/圆角/Logo 调整，字数徽章内嵌 |
| `app/src/main/res/values/strings.xml` | 修改 | Logo 文字 "简记事" → "刻记+" |

---

## 任务依赖图

```
Task 1 (strings.xml: Logo 文字 → 刻记+)
   ↓
Task 2 (MainActivity 启用 enableEdgeToEdge)
   ↓
Task 3 (InspirationViewScreen: Scaffold contentWindowInsets + TopBar Column 动态高度)
   ↓
Task 4 (InspirationViewCard: 卡片参数调整)
   ↓
Task 5 (InspirationViewCard: 字数徽章内嵌)
   ↓
Task 6 (编译验证 + 提交)
```

---

## Task 1: strings.xml 更新 Logo 文字

**Files:**
- Modify: `app/src/main/res/values/strings.xml`

- [ ] **Step 1: 定位 inspiration_view_logo_text 字符串**

使用 Grep 工具搜索 `inspiration_view_logo_text` 定位现有定义。

- [ ] **Step 2: 替换 Logo 文字**

将 `<string name="inspiration_view_logo_text">- 简记事 -</string>` 替换为：

```xml
    <string name="inspiration_view_logo_text">- 刻记+ -</string>
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/res/values/strings.xml
git commit -m "feat(inspiration): Logo 品牌升级 简记事 → 刻记+"
```

---

## Task 2: MainActivity 启用 enableEdgeToEdge

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/MainActivity.kt:70-95`

- [ ] **Step 1: 定位 onCreate 方法**

使用 Read 工具打开 `MainActivity.kt`，定位 `onCreate` 方法第 70 行。

- [ ] **Step 2: 添加 enableEdgeToEdge import**

在文件顶部 import 区（第 6 行附近）新增：

```kotlin
import androidx.activity.enableEdgeToEdge
```

- [ ] **Step 3: 在 onCreate 中调用 enableEdgeToEdge**

在 `super.onCreate(savedInstanceState)` 之后（第 71 行后）添加：

```kotlin
        // 启用沉浸式全屏（系统栏透明 + 内容延伸到状态栏/导航栏）
        enableEdgeToEdge()
```

完整修改后第 70-73 行为：

```kotlin
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        // 启用沉浸式全屏（系统栏透明 + 内容延伸到状态栏/导航栏）
        enableEdgeToEdge()

        pendingNavigation = intent?.let { parseNavigationIntent(it) }
```

- [ ] **Step 4: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/MainActivity.kt
git commit -m "feat(inspiration): MainActivity 启用 enableEdgeToEdge 沉浸式"
```

---

## Task 3: InspirationViewScreen Scaffold + TopBar 自适应

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationViewScreen.kt`

- [ ] **Step 1: 添加 WindowInsets 相关 import**

在 `InspirationViewScreen.kt` 顶部 import 区（`androidx.compose.foundation.layout.*` 附近）添加：

```kotlin
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Spacer
import androidx.compose.foundation.layout.WindowInsets
import androidx.compose.foundation.layout.asPaddingValues
import androidx.compose.foundation.layout.statusBars
```

- [ ] **Step 2: Scaffold 设置 contentWindowInsets**

将 `InspirationViewScreen` 第 117 行的 Scaffold 调用修改为：

```kotlin
    Scaffold(
        contentWindowInsets = WindowInsets(0, 0, 0, 0),  // 由各 Composable 自行处理 WindowInsets
        containerColor = MaterialTheme.colorScheme.background,
        snackbarHost = { SnackbarHost(snackbarHostState) },
        topBar = {
            TopBar(
                onBack = { navController.popBackStack() },
                onCopy = {
                    // 复制当前页灵感到剪贴板
                    val ins = currentInspiration ?: return@TopBar
                    val formattedDate = SimpleDateFormat("yyyy.MM.dd HH:mm", Locale.getDefault())
                        .format(Date(ins.createdAt))
                    val text = InspirationTextUtils.buildInspirationPlainText(ins, formattedDate)
                    clipboardManager.setText(AnnotatedString(text))
                    coroutineScope.launch {
                        snackbarHostState.showSnackbar("文本内容已复制到剪切板")
                    }
                },
                onEdit = {
                    // 跳转编辑页
                    val ins = currentInspiration ?: return@TopBar
                    navController.navigate("inspiration_edit/${ins.id}")
                },
                onShare = {
                    // 弹出分享底部菜单
                    if (currentInspiration != null) showShareSheet = true
                }
            )
        }
    ) { paddingValues ->
```

- [ ] **Step 3: TopBar 改为 Column 动态状态栏高度**

将 `InspirationViewScreen.kt` 第 246-302 行的 `TopBar` 函数整体替换为：

```kotlin
@Composable
private fun TopBar(
    onBack: () -> Unit,
    onCopy: () -> Unit,
    onEdit: () -> Unit,
    onShare: () -> Unit
) {
    // 动态获取状态栏高度，跨设备一致
    val statusBarHeight = WindowInsets.statusBars.asPaddingValues().calculateTopPadding()

    Column(
        modifier = Modifier
            .fillMaxWidth()
            .background(MaterialTheme.colorScheme.background)
    ) {
        // 状态栏占位区域（透明，保留可点击性）
        Spacer(modifier = Modifier.height(statusBarHeight))
        // 实际内容区域（64dp）
        Box(
            modifier = Modifier
                .fillMaxWidth()
                .height(64.dp)
        ) {
            // 左侧返回箭头
            IconButton(
                onClick = onBack,
                modifier = Modifier.align(Alignment.CenterStart).padding(start = 6.dp)
            ) {
                Icon(
                    imageVector = Icons.AutoMirrored.Outlined.ArrowBack,
                    contentDescription = "返回",
                    tint = MaterialTheme.colorScheme.onSurface,
                    modifier = Modifier.size(24.dp)
                )
            }
            // 右侧功能按钮组（复制 + 编辑 + 分享）
            Box(
                modifier = Modifier.align(Alignment.CenterEnd).padding(end = 6.dp)
            ) {
                Row {
                    IconButton(onClick = onCopy) {
                        Icon(
                            Icons.Outlined.ContentCopy,
                            contentDescription = "复制",
                            tint = MaterialTheme.colorScheme.onSurface,
                            modifier = Modifier.size(24.dp)
                        )
                    }
                    IconButton(onClick = onEdit) {
                        Icon(
                            Icons.Outlined.Edit,
                            contentDescription = "编辑",
                            tint = MaterialTheme.colorScheme.onSurface,
                            modifier = Modifier.size(24.dp)
                        )
                    }
                    IconButton(onClick = onShare) {
                        Icon(
                            Icons.Outlined.Share,
                            contentDescription = "分享",
                            tint = MaterialTheme.colorScheme.onSurface,
                            modifier = Modifier.size(24.dp)
                        )
                    }
                }
            }
        }
    }
}
```

- [ ] **Step 4: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/InspirationViewScreen.kt
git commit -m "feat(inspiration): TopBar 高度自适应（状态栏 + 64dp）"
```

---

## Task 4: InspirationViewCard 卡片参数调整

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationViewCard.kt:82-194`

- [ ] **Step 1: 添加 stringResource import**

在 `InspirationViewCard.kt` 顶部 import 区（`androidx.compose.ui.text.font.FontWeight` 附近）添加：

```kotlin
import androidx.compose.ui.res.stringResource
import com.corgimemo.app.R
```

- [ ] **Step 2: 调整 Card 外边距和圆角**

将第 94-103 行的 `Card` 调用修改为：

```kotlin
        // 卡片主体
        Card(
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 10.dp),  // 18dp → 10dp：更紧凑
            shape = RoundedCornerShape(12.dp),  // 20dp → 12dp：更小更精致
            colors = CardDefaults.cardColors(
                containerColor = MaterialTheme.colorScheme.surface
            ),
            elevation = CardDefaults.cardElevation(defaultElevation = 4.dp)
        ) {
            Column(modifier = Modifier.padding(18.dp)) {  // 20dp → 18dp
                // 标题（18sp Medium，与参考图一致）
                Text(
                    text = inspiration.title,
                    fontSize = 18.sp,  // 16sp → 18sp
                    fontWeight = FontWeight.Medium,
                    color = MaterialTheme.colorScheme.onSurface,
                    letterSpacing = 0.5.sp
                )
                Spacer(modifier = Modifier.height(4.dp))
                // 日期时间（12sp 灰色）
                Text(
                    text = formattedDate,
                    fontSize = 12.sp,  // 11sp → 12sp
                    color = Color(0xFF999999),
                    letterSpacing = 0.5.sp
                )
                Spacer(modifier = Modifier.height(9.dp))
                // 正文（15sp，行高 22sp，#666666）
                Text(
                    text = inspiration.content,
                    fontSize = 15.sp,  // 14sp → 15sp
                    color = Color(0xFF666666),
                    lineHeight = 22.sp,  // 21sp → 22sp
                    letterSpacing = 0.5.sp
                )
                // 图片列表（如果有）
                if (imagePaths.isNotEmpty()) {
                    Spacer(modifier = Modifier.height(12.dp))
                    LazyRow(
                        horizontalArrangement = Arrangement.spacedBy(8.dp)
                    ) {
                        items(imagePaths.size) { index ->
                            val path = imagePaths[index]
                            AsyncImage(
                                model = path,
                                contentDescription = null,
                                modifier = Modifier
                                    .size(120.dp)
                                    .clip(RoundedCornerShape(6.dp))
                                    .clickable { onImageClick(index) }
                            )
                        }
                    }
                }
                // 标签（最多显示 5 个）
                if (tagsList.isNotEmpty()) {
                    Spacer(modifier = Modifier.height(12.dp))
                    Row(horizontalArrangement = Arrangement.spacedBy(6.dp)) {
                        tagsList.take(5).forEach { tag ->
                            Box(
                                modifier = Modifier
                                    .background(
                                        Color(0xFFFFF3E0),
                                        RoundedCornerShape(10.dp)
                                    )
                                    .padding(horizontal = 8.dp, vertical = 2.dp)
                            ) {
                                Text(
                                    text = "#$tag",
                                    fontSize = 11.sp,
                                    color = UiColors.Primary,
                                    letterSpacing = 0.5.sp
                                )
                            }
                        }
                    }
                }
                // Logo 居中区
                Spacer(modifier = Modifier.height(32.dp))
                Row(
                    modifier = Modifier.fillMaxWidth(),
                    horizontalArrangement = Arrangement.Center,
                    verticalAlignment = Alignment.CenterVertically
                ) {
                    Icon(
                        imageVector = Icons.Outlined.Edit,
                        contentDescription = null,
                        tint = Color(0xFF999999),
                        modifier = Modifier.size(14.dp)
                    )
                    Spacer(modifier = Modifier.width(6.dp))
                    Text(
                        text = stringResource(R.string.inspiration_view_logo_text),  // 从字符串资源读取
                        fontSize = 13.sp,
                        color = Color(0xFF999999),
                        letterSpacing = 0.5.sp
                    )
                }
            }
        }
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationViewCard.kt
git commit -m "feat(inspiration): 卡片参数调整（外边距 10dp/圆角 12dp/字号 +2sp）"
```

---

## Task 5: 字数徽章内嵌到 Column 内部

**Files:**
- Modify: `app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationViewCard.kt:196-214`

- [ ] **Step 1: 删除外层 Box 顶部的字数徽章**

删除 `InspirationViewCard.kt` 第 196-214 行的字数徽章（外部 Box）：

```kotlin
        // 字数徽章（卡片右上角，胶囊形状）
        Box(
            modifier = Modifier
                .align(Alignment.TopEnd)
                .padding(top = 16.dp, end = 28.dp)
                .background(
                    Color(0xFFE0E0E0).copy(alpha = 0.6f),
                    RoundedCornerShape(12.dp)
                )
                .padding(horizontal = 8.dp, vertical = 4.dp)
        ) {
            Text(
                text = "${charCount}字",
                fontSize = 11.sp,
                fontWeight = FontWeight.Medium,
                color = Color(0xFF666666)
            )
        }
```

- [ ] **Step 2: 在 Column 内部第一个元素添加字数徽章**

在 `Column(modifier = Modifier.padding(18.dp)) {` 之后（第 104 行附近），在 `Text(inspiration.title)` 之前插入：

```kotlin
            Column(modifier = Modifier.padding(18.dp)) {
                // 字数徽章（卡片内右上角，横向居右）
                Box(
                    modifier = Modifier
                        .align(Alignment.End)
                        .background(
                            Color(0xFFE0E0E0).copy(alpha = 0.6f),
                            RoundedCornerShape(12.dp)
                        )
                        .padding(horizontal = 8.dp, vertical = 4.dp)
                ) {
                    Text(
                        text = "${charCount}字",
                        fontSize = 11.sp,
                        fontWeight = FontWeight.Medium,
                        color = Color(0xFF666666)
                    )
                }
                Spacer(modifier = Modifier.height(8.dp))
                // 标题（18sp Medium）
                Text(
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/corgimemo/app/ui/screens/inspiration/components/InspirationViewCard.kt
git commit -m "feat(inspiration): 字数徽章内嵌到 Column 内部（align End）"
```

---

## Task 6: 编译验证 + 最终提交

**Files:**
- 无新增/修改

- [ ] **Step 1: 编译验证（需用户授权）**

向用户请求授权后执行：
```bash
./gradlew :app:compileDebugKotlin
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 手动测试清单验证**

按以下清单在设备/模拟器验证：

```markdown
- [ ] 状态栏不与返回箭头重叠
- [ ] 状态栏不与三个功能按钮重叠
- [ ] 卡片水平外边距看起来紧凑（约 10dp）
- [ ] 卡片圆角更小（12dp）
- [ ] 标题字号更大更接近参考图（18sp）
- [ ] Logo 文字显示为 "- 刻记+ -"
- [ ] 字数徽章位于卡片内右上角（不与系统状态栏重叠）
- [ ] 其他页面（灵感页、待办页等）布局未受影响
- [ ] HorizontalPager 左右滑动仍正常
- [ ] 复制/编辑/分享按钮仍可点击
```

- [ ] **Step 3: 报告完成**

向用户报告 4 个改进点的完成状态。

---

## 自审检查

**1. Spec coverage**：

| Spec 节 | 对应 Task |
|---------|-----------|
| 1. 目标 | 全部任务 |
| 2. 修改文件清单 | Task 1-5 |
| 3. 状态栏沉浸式 | Task 2 (MainActivity) + Task 3 (Scaffold contentWindowInsets) |
| 4. TopBar 高度自适应 | Task 3 |
| 5. 卡片参数调整 | Task 4 (外边距/圆角/字号) + Task 5 (字数徽章位置) |
| 6. strings.xml 更新 | Task 1 |
| 7. 风险评估 | Task 6 验证 |
| 8. 任务分解 | Task 1-6 |
| 9. 测试 | Task 6 (手动测试清单) |

**2. Placeholder scan**：✅ 无 TBD/TODO/xxx 占位符

**3. Type consistency**：
- `R.string.inspiration_view_logo_text` 在 Task 1 定义，在 Task 4 引用，一致
- `WindowInsets.statusBars.asPaddingValues().calculateTopPadding()` 在 Task 3 定义使用，一致
- `enableEdgeToEdge()` 是 `androidx.activity.enableEdgeToEdge` 顶层函数，Task 2 import 正确

---

## 执行选项

**计划完成并保存到 `docs/superpowers/plans/2026-07-10-灵感卡片展示页UI优化.md`。两个执行选项：**

1. **Subagent-Driven（推荐）** - 我为每个任务分派一个全新的子代理，在任务之间进行审查，快速迭代
2. **Inline Execution** - 在本次会话中执行任务，使用 executing-plans 批量执行并设置检查点

**Which approach?**
