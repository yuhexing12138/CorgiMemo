# 野兽派 + 故障艺术改造实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 corgimemo-showcase.html 的 tab1-6 改造为野兽派主导风格,tab7 改造为故障外壳+暖色内容,并跨 Tab 合并重复内容。

**Architecture:** 单文件 HTML/CSS 改造。扩展 CSS 设计系统新增野兽派主导样式类与故障艺术增强类;逐 Tab 改造主题组件;跨 Tab 移动重复内容区块并重编章节号;响应式三断点适配;全局文字可见性审查。

**Tech Stack:** HTML5, CSS3 (Grid/Flexbox/动画), 无外部依赖

**目标文件:** `【刻记+】APP/corgimemo-showcase.html`(337KB,7162+行)
**设计文档:** `docs/superpowers/specs/2026-06-18-野兽派故障艺术改造-design.md`

---

## 文件结构

仅修改单一文件:`【刻记+】APP/corgimemo-showcase.html`

| 区域            | 行号范围(约) | 责任                               |
| --------------- | ------------ | ---------------------------------- |
| CSS 设计系统    | 13-2440      | 全局样式变量与组件类               |
| Hero/导航       | 3747-3820    | 顶部刊头与 Tab 导航                |
| Tab1 概览       | 3821-3898    | 核心价值+差异化(移出目标用户)      |
| Tab2 柯基陪伴   | 3903-4252    | 姿态/表情/情绪/触摸/成长(移出装扮) |
| Tab3 身份自适应 | 4255-4409    | 身份对比/问候/专属功能             |
| Tab4 三位一体   | 4412-4592    | 功能关系图/待办/灵感/日期/关联     |
| Tab5 节日关怀   | 4595-4768    | 节日+装扮(合并吸收)+问候+场景      |
| Tab6 用户故事   | 4771-4978    | 用户群体(合并吸收)+画像+故事       |
| Tab7 原型展示   | 4981-6320    | 故障外壳+暖色手机原型              |

---

## Task 1: 扩展 CSS 野兽派主导设计系统

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` CSS 区块(约 374-524 行,新增类插入至 524 行后)

- [ ] **Step 1: 新增野兽派主导变量与色块背景类**

在 CSS `:root` 变量区(约 87 行 `}` 前)新增暖色色板变量:

```css
/* 野兽派主导色板 — 暖色调色块交替 */
--color-brutal-orange: #FF6B35;       /* 橙红主色块 */
--color-brutal-cream: #FFD4A8;        /* 暖橙黄 */
--color-brutal-yellow: #FFB74D;       /* 暖黄 */
--color-brutal-deep: #E8743C;         /* 深橙 */
--color-brutal-mint: #7EC8A0;         /* 薄荷对比 */
--color-brutal-ink: #1a1a1a;          /* 深黑边框 */
--color-brutal-paper: #f5f0e8;        /* 米白底 */
/* 野兽派尺寸 */
--brutal-border: 3px;
--brutal-shadow: 6px;
--brutal-shadow-lg: 8px;
--brutal-radius: 2px;
```

- [ ] **Step 2: 新增野兽派主导卡片类(覆盖现有 .brutal-card)**

在约 524 行(现有 `.glitch-border::before` 扫描线动画后)新增:

```css
/* ============================================
   野兽派主导组件 — Tab1-6 主视觉
   粗黑边框 + 硬阴影 + 大胆色块 + 原始质感
   ============================================ */

/* 野兽派主导卡片 — 移除圆角,强化粗框硬阴影 */
.brutal-card-dominant {
    border: var(--brutal-border) solid var(--color-brutal-ink);
    border-radius: var(--brutal-radius);
    padding: 18px;
    background: #ffffff;
    box-shadow: var(--brutal-shadow) var(--brutal-shadow) 0 var(--color-brutal-ink);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    position: relative;
}

/* hover — 阴影增大,位移偏移 */
.brutal-card-dominant:hover {
    transform: translate(-3px, -3px);
    box-shadow: var(--brutal-shadow-lg) var(--brutal-shadow-lg) 0 var(--color-brutal-ink);
}

/* 野兽派色块变体 — 橙红背景,白色文字(高对比) */
.brutal-card-dominant.bg-orange {
    background: var(--color-brutal-orange);
    color: #ffffff;
}

/* 暖橙黄背景,深黑文字 */
.brutal-card-dominant.bg-cream {
    background: var(--color-brutal-cream);
    color: var(--color-brutal-ink);
}

/* 暖黄背景,深黑文字 */
.brutal-card-dominant.bg-yellow {
    background: var(--color-brutal-yellow);
    color: var(--color-brutal-ink);
}

/* 深橙背景,白色文字 */
.brutal-card-dominant.bg-deep {
    background: var(--color-brutal-deep);
    color: #ffffff;
}

/* 薄荷背景,深黑文字 */
.brutal-card-dominant.bg-mint {
    background: var(--color-brutal-mint);
    color: var(--color-brutal-ink);
}

/* 深黑背景,白色文字 */
.brutal-card-dominant.bg-ink {
    background: var(--color-brutal-ink);
    color: #ffffff;
}
```

- [ ] **Step 3: 新增野兽派章节号与标签类**

```css
/* 野兽派章节号 — 超大数字 + 粗框包裹 + 硬阴影色块 */
.chapter-number-brutal {
    font-family: var(--font-sans);
    font-size: 88px;
    font-weight: 900;
    line-height: 0.85;
    letter-spacing: -3px;
    color: var(--color-brutal-orange);
    border: var(--brutal-border) solid var(--color-brutal-ink);
    padding: 8px 16px;
    background: #ffffff;
    box-shadow: var(--brutal-shadow) var(--brutal-shadow) 0 var(--color-brutal-ink);
    display: inline-block;
}

/* 野兽派章节号色块变体 */
.chapter-number-brutal.bg-orange { background: var(--color-brutal-orange); color: #fff; }
.chapter-number-brutal.bg-mint { background: var(--color-brutal-mint); color: var(--color-brutal-ink); }
.chapter-number-brutal.bg-yellow { background: var(--color-brutal-yellow); color: var(--color-brutal-ink); }

/* 野兽派标签 — 粗框硬阴影色块 */
.brutal-tag {
    display: inline-block;
    padding: 10px 20px;
    border: var(--brutal-border) solid var(--color-brutal-ink);
    border-radius: var(--brutal-radius);
    font-size: 14px;
    font-weight: 900;
    background: var(--color-brutal-orange);
    color: #ffffff;
    box-shadow: 4px 4px 0 var(--color-brutal-ink);
    margin: 4px;
}

.brutal-tag.bg-cream { background: var(--color-brutal-cream); color: var(--color-brutal-ink); }
.brutal-tag.bg-mint { background: var(--color-brutal-mint); color: var(--color-brutal-ink); }
.brutal-tag.bg-white { background: #ffffff; color: var(--color-brutal-ink); }

/* 野兽派错落网格 — 不等高布局 */
.brutal-stagger-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    align-items: start;
}

/* 错落:第2项下移,第3项上移 */
.brutal-stagger-grid > :nth-child(2) { margin-top: 24px; }
.brutal-stagger-grid > :nth-child(3) { margin-top: 12px; }

/* 野兽派引言 — 粗框 + 暖色块背景 */
.brutal-quote {
    margin: 32px 24px;
    padding: 24px;
    border: var(--brutal-border) solid var(--color-brutal-ink);
    border-radius: var(--brutal-radius);
    background: var(--color-brutal-cream);
    color: var(--color-brutal-ink);
    text-align: center;
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 18px;
    line-height: 1.6;
    box-shadow: var(--brutal-shadow) var(--brutal-shadow) 0 var(--color-brutal-orange);
}

.brutal-quote .quote-source {
    font-family: var(--font-sans);
    font-size: 11px;
    color: var(--color-brutal-ink);
    margin-top: 10px;
    letter-spacing: 2px;
    font-style: normal;
    font-weight: 900;
}

/* 野兽派章节标题 — 粗体 + 暖色下划线块 */
.section-title-brutal {
    font-family: var(--font-sans);
    font-size: 26px;
    font-weight: 900;
    color: var(--color-brutal-ink);
    margin-bottom: 16px;
    display: inline-block;
    border-bottom: var(--brutal-border) solid var(--color-brutal-orange);
    padding-bottom: 4px;
}
```

- [ ] **Step 4: 新增 Tab7 故障外壳增强类**

```css
/* ============================================
   Tab7 故障艺术外壳增强 — 冷色外壳 + 暖色内容
   ============================================ */

/* 故障手机外壳 — RGB分离霓虹边框 */
.glitch-phone-frame {
    border: 3px solid var(--color-neon-green);
    box-shadow:
        6px 6px 0 var(--color-neon-pink),
        -3px -3px 0 var(--color-electric-blue);
    background: var(--color-glitch-bg);
    padding: 12px;
    position: relative;
    overflow: hidden;
}

/* 故障外壳扫描线 */
.glitch-phone-frame::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background: linear-gradient(
        to bottom,
        transparent 50%,
        rgba(0, 255, 159, 0.04) 50%
    );
    background-size: 100% 4px;
    pointer-events: none;
    animation: scanline 8s linear infinite;
    z-index: 1;
}

/* 故障外壳抖动动画 */
.glitch-phone-frame::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    border: 1px solid transparent;
    pointer-events: none;
    animation: glitchJitter 4s infinite;
    z-index: 2;
}

@keyframes glitchJitter {
    0%, 95%, 100% { border-color: transparent; transform: translate(0, 0); }
    96% { border-color: var(--color-neon-pink); transform: translate(-2px, 0); }
    97% { border-color: var(--color-electric-blue); transform: translate(2px, 0); }
    98% { border-color: var(--color-neon-green); transform: translate(0, -1px); }
}

/* 故障终端提示条 — 等宽霓虹绿 */
.glitch-terminal-bar {
    background: var(--color-glitch-bg);
    border: 2px solid var(--color-neon-green);
    padding: 8px 14px;
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--color-neon-green);
    margin-bottom: 12px;
    position: relative;
}

.glitch-terminal-bar::before {
    content: '> ';
    color: var(--color-neon-pink);
}

/* 故障章节号增强 — 加粗抖动 */
.chapter-number-glitch {
    font-family: var(--font-mono);
    font-size: 72px;
    font-weight: 900;
    line-height: 0.85;
    color: var(--color-neon-green);
    text-shadow:
        3px 0 var(--color-neon-pink),
        -3px 0 var(--color-electric-blue);
    animation: glitchShift 3s infinite;
}

/* 故障暖色内容容器 — 屏幕内部暖色基调 */
.glitch-warm-content {
    background: var(--color-brutal-paper);
    border: 2px solid var(--color-brutal-orange);
    padding: 16px;
    position: relative;
    z-index: 3;
}

/* 故障暖色卡片 — 屏幕内野兽派暖色 */
.glitch-warm-card {
    border: 2px solid var(--color-brutal-ink);
    background: #ffffff;
    padding: 12px;
    box-shadow: 3px 3px 0 var(--color-brutal-orange);
    color: var(--color-brutal-ink);
    font-size: 11px;
}

.glitch-warm-card.bg-orange {
    background: var(--color-brutal-orange);
    color: #ffffff;
}
```

- [ ] **Step 5: 新增响应式断点适配**

在现有 `@media (max-width: 767px)` 块(约 675 行)内追加:

```css
/* 野兽派移动端适配 */
.chapter-number-brutal {
    font-size: 52px;
    padding: 6px 12px;
}

.brutal-stagger-grid {
    grid-template-columns: 1fr;
}

.brutal-stagger-grid > :nth-child(2),
.brutal-stagger-grid > :nth-child(3) {
    margin-top: 0;
}

.brutal-card-dominant {
    box-shadow: 4px 4px 0 var(--color-brutal-ink);
    padding: 14px;
}

.brutal-quote {
    margin: 20px 12px;
    padding: 16px;
    font-size: 15px;
}

.chapter-number-glitch {
    font-size: 48px;
}

.section-title-brutal {
    font-size: 22px;
}
```

在 `@media (max-width: 1199px)` 块(约 652 行)内追加:

```css
/* 野兽派平板适配 */
.chapter-number-brutal {
    font-size: 64px;
}

.brutal-stagger-grid {
    grid-template-columns: 1fr 1fr;
}

.brutal-stagger-grid > :nth-child(3) {
    margin-top: 0;
}
```

- [ ] **Step 6: 浏览器验证 CSS 无语法错误**

Run: 在浏览器打开 `corgimemo-showcase.html`,打开开发者工具 Console,确认无 CSS 解析错误。
Expected: 页面正常加载,无控制台报错。

- [ ] **Step 7: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: 扩展野兽派主导与故障艺术CSS设计系统"
```

---

## Task 2: Tab1 概览野兽派改造 + 移出目标用户

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` Tab1 区块(3821-3898 行)

- [ ] **Step 1: 改造章节01三大核心价值为野兽派主导**

将 3823-3847 行的章节01替换为野兽派主导结构:章节号用 `.chapter-number-brutal.bg-orange`,卡片用 `.brutal-card-dominant` 色块交替(白/橙红/薄荷),标题用 `.section-title-brutal`。确保橙红卡片用白色文字,白/薄荷卡片用深黑文字。

- [ ] **Step 2: 移除章节02目标用户群体(移至Tab6)**

删除 3849-3876 行的章节02目标用户群体整块(将在 Task 7 移入 Tab6)。

- [ ] **Step 3: 改造章节03差异化优势为野兽派标签**

将 3884-3897 行的差异化优势标签改为 `.brutal-tag` 色块标签(橙红/薄荷/暖黄/白交替),每个标签粗框硬阴影。

- [ ] **Step 4: 改造产品愿景引言为野兽派引言**

将 3878-3882 行的 `.editorial-quote` 改为 `.brutal-quote` 暖色块引言。

- [ ] **Step 5: 浏览器验证 Tab1**

打开 Tab1,确认:章节号粗框硬阴影大数字、卡片色块交替、文字清晰可见、引言暖色块背景。
Expected: 野兽派主导风格鲜明,文字高对比度可见。

- [ ] **Step 6: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: Tab1概览野兽派主导改造,移出目标用户群体"
```

---

## Task 3: Tab2 柯基陪伴野兽派改造 + 移出装扮系统

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` Tab2 区块(3903-4252 行)

- [ ] **Step 1: 改造姿态系统/表情系统为野兽派卡片**

将 3905-4010 行的姿态卡片与表情卡片改为 `.brutal-card-dominant` 色块交替,章节号用 `.chapter-number-brutal`。

- [ ] **Step 2: 改造情绪系统/触摸互动/成长系统**

将 4012-4216 行的情绪系统、触摸互动、成长系统区块的卡片改为野兽派主导风格。

- [ ] **Step 3: 移出装扮系统三节(移至Tab5)**

删除 4112-4216 行的"装扮系统-传统节日/现代节日/二十四节气"三节(将在 Task 6 移入 Tab5)。

- [ ] **Step 4: 浏览器验证 Tab2**

确认 Tab2 仅保留姿态/表情/情绪/触摸/成长系统,全部野兽派主导,文字清晰。
Expected: 装扮系统已移出,陪伴系统核心内容野兽派风格。

- [ ] **Step 5: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: Tab2柯基陪伴野兽派改造,移出装扮系统至Tab5"
```

---

## Task 4: Tab3 身份自适应野兽派改造

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` Tab3 区块(4255-4409 行)

- [ ] **Step 1: 改造三种身份对比为野兽派三栏**

将 4261-4326 行的三种身份对比卡片改为 `.brutal-card-dominant` 三色块(橙红/薄荷/暖黄),章节号野兽派。

- [ ] **Step 2: 改造问候语差异/专属功能/演示**

将 4327-4408 行的问候语差异、专属功能、身份自适应演示区块改为野兽派主导风格。

- [ ] **Step 3: 浏览器验证 Tab3**

确认身份对比三色块鲜明,文字高对比可见。
Expected: 野兽派主导,身份对比视觉冲击强。

- [ ] **Step 4: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: Tab3身份自适应野兽派改造"
```

---

## Task 5: Tab4 三位一体野兽派改造

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` Tab4 区块(4412-4592 行)

- [ ] **Step 1: 改造功能关系图为野兽派流程图**

将 4418-4447 行的三大功能关系图改为野兽派粗框流程图卡片(粗黑边框+硬阴影+色块)。

- [ ] **Step 2: 改造待办/灵感/日期/跨功能关联**

将 4448-4591 行的待办管理、灵感记录、特殊日期、跨功能关联区块改为野兽派主导卡片。

- [ ] **Step 3: 浏览器验证 Tab4**

确认功能关系图粗框流程,各功能卡片野兽派色块。
Expected: 三位一体功能野兽派呈现。

- [ ] **Step 4: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: Tab4三位一体野兽派改造"
```

---

## Task 6: Tab5 节日关怀野兽派改造 + 合并装扮系统

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` Tab5 区块(4595-4768 行)

- [ ] **Step 1: 改造节日列表/换装展示为野兽派**

将 4601-4714 行的节日列表与柯基换装展示改为野兽派主导卡片网格。

- [ ] **Step 2: 合并吸收Tab2装扮系统三节**

将从 Tab2 移出的"装扮系统-传统节日/现代节日/二十四节气"三节插入 Tab5 换装展示后(约 4714 行后),改为野兽派色块卡片,与现有换装展示整合为统一装扮展示区。

- [ ] **Step 3: 改造问候语/冬至场景/公益价值**

将 4715-4767 行的专属问候语、冬至场景演示、公益价值改为野兽派主导。

- [ ] **Step 4: 浏览器验证 Tab5**

确认节日+装扮内容整合,无重复,野兽派色块鲜明。
Expected: Tab5 成为节日+装扮综合展示,内容无重复。

- [ ] **Step 5: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: Tab5节日关怀野兽派改造,合并吸收装扮系统"
```

---

## Task 7: Tab6 用户故事野兽派改造 + 合并目标用户

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` Tab6 区块(4771-4978 行)

- [ ] **Step 1: 合并吸收Tab1目标用户群体**

将从 Tab1 移出的"目标用户群体"区块插入 Tab6 用户画像前(约 4777 行前),改为野兽派色块卡片,与用户画像整合形成"群体→画像→故事"叙事链。

- [ ] **Step 2: 改造用户画像/场景故事/场景总结**

将 4777-4977 行的用户画像、场景故事、场景总结改为野兽派主导风格,大引言式画像用野兽派粗框引言。

- [ ] **Step 3: 浏览器验证 Tab6**

确认目标用户+画像+故事叙事链完整,野兽派风格。
Expected: Tab6 用户全景叙事,内容无重复。

- [ ] **Step 4: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: Tab6用户故事野兽派改造,合并吸收目标用户群体"
```

---

## Task 8: Tab7 故障外壳 + 暖色内容改造

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` Tab7 区块(4981-6320 行)

- [ ] **Step 1: 改造Tab7章节号为故障章节号**

将 4986 行的章节号改为 `.chapter-number-glitch` 故障抖动 RGB 分离效果。

- [ ] **Step 2: 改造手机模拟器为故障外壳**

将 Tab7 手机展示容器(phone-showcase)外层改为 `.glitch-phone-frame` 故障外壳(RGB分离霓虹边框+扫描线+抖动动画)。

- [ ] **Step 3: 改造手机屏幕内部为暖色内容**

将手机屏幕内部 UI(`.phone-screen` 内容)改为 `.glitch-warm-content` 暖色基调容器,屏幕内卡片用 `.glitch-warm-card` 暖色野兽派,保留柯基暖色基调。

- [ ] **Step 4: 改造操作提示为故障终端条**

将 Tab7 操作提示/筛选器改为 `.glitch-terminal-bar` 终端式等宽霓虹绿提示。

- [ ] **Step 5: 浏览器验证 Tab7**

确认:外壳故障(RGB分离/扫描线/抖动),内容暖色(柯基基调),文字清晰可见。
Expected: 冷色故障外壳 + 暖色内容强烈对比,三层结构清晰。

- [ ] **Step 6: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: Tab7故障外壳+暖色内容改造"
```

---

## Task 9: 章节号全局重编

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` 全 Tab 章节号

- [ ] **Step 1: 按重组后结构重编章节号**

按设计文档第四章 4.3 节分配重编所有章节号:

- Tab1: 01/02
- Tab2: 03/04/05/06/07
- Tab3: 08/09/10/11
- Tab4: 12/13/14/15/16
- Tab5: 17/18/19/20/21/22/23/24
- Tab6: 25/26/27/28
- Tab7: 29

- [ ] **Step 2: 浏览器验证章节号连贯**

逐 Tab 检查章节号连续无跳号。
Expected: 01-29 连续编号。

- [ ] **Step 3: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: 全局章节号重编01-29"
```

---

## Task 10: 响应式 + 文字可见性全局审查

**Files:**

- Modify: `【刻记+】APP/corgimemo-showcase.html` 全局

- [ ] **Step 1: 三断点响应式验证**

在浏览器开发者工具切换三断点(>900px / 600-900px / <600px),验证:

- 野兽派多列错落→两列→单列堆叠
- 章节号 88px→64px→52px
- 硬阴影 6px→5px→4px

- [ ] **Step 2: 文字可见性全局审查**

逐 Tab 检查所有色块背景与文字对比度:

- 橙红/深橙/深黑背景 → 白色文字
- 暖橙黄/暖黄/薄荷/米白背景 → 深黑文字
- 故障深背景 → 霓虹绿文字
- 修复任何低对比度文字

- [ ] **Step 3: 浏览器全功能验证**

逐 Tab 点击切换,验证所有交互(筛选器、轮播、手机展示)功能正常。
Expected: 全功能正常,风格统一,文字清晰。

- [ ] **Step 4: 提交**

```bash
git add "【刻记+】APP/corgimemo-showcase.html"
git commit -m "feat: 响应式适配与文字可见性全局审查"
```

---

## 验收标准

- [ ] Tab1-6 呈现鲜明野兽派主导风格(粗框/硬阴影/大胆色块/原始质感)
- [ ] Tab7 外壳故障艺术 + 内容暖色基调,三层结构清晰
- [ ] 跨 Tab 内容合并完成(装扮系统→Tab5,目标用户→Tab6),无重复内容
- [ ] 章节号 01-29 连贯重编
- [ ] 所有文字在色块背景上清晰可见(高对比度)
- [ ] 桌面/平板/移动端响应式布局正常
- [ ] 与现有杂志编辑风格形成强烈视觉对比
- [ ] 暖色调为主(Tab1-6),故障冷色为辅(Tab7 外壳)
- [ ] 所有交互功能(筛选器、轮播、手机展示)正常
