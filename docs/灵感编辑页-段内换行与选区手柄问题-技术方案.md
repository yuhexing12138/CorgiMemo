# 段内换行与选区手柄跳变 · 深改方案

> 范围：灵感编辑页正文块（`BodyBlocksEditor` + richeditor 库）
> 问题来源：用户实测——块内多行时，从第 1 行行首拖动选择手柄到行尾，光标**立马跳到第 2 行行首**
> 产出日期：2026-09-15
> 状态：**方案待评审（未动代码）**；简单改法已试并回退，结论见 §3

---

## 一、需求（用户确认的行为口径）

| 行为 | 期望 |
|------|------|
| 拖拽过程 | 光标**始终跟手**移动 |
| 到达行尾后继续向右拖 | **保持在行尾**（不跳到下一行） |
| 到达行尾后向下拖 | 跟手移动到**下一行的对应位置** |
| 反向（从下行往上行拖） | 同理，不应有跳变 |

---

## 二、现状与根因（精确机理）

### 2.1 库的段落承载方式

库里**段落不是由 `\n` 承载的**，而是由 `ParagraphStyle` 的 range 边界承载。为此做了两个 hack
（均在 `RichTextState.updateAnnotatedString` / `updateRichParagraphList`）：

```kotlin
// ① 文本层把 \n 全部替换成空格（段落边界由 ParagraphStyle 负责换行）
val newText = if (singleParagraphMode) newTextFieldValue.text
              else newTextFieldValue.text.replace('\n', ' ')
// ② 每个段落末尾追加一个"有宽度的占位空格"
//    （原注释：修 Compose「多段落时最后一个字符不可选」）
if (i != richParagraphList.lastIndex && index < newText.length) { append(' '); index++ }
```

### 2.2 跳变的成因

设一个块内有两行「第一行 / 第二行」，则：

```
用户看到：   第一行          ← 换行（ParagraphStyle 边界）→
annotatedString： 第一行 · 第二行            （· = 段末占位空格）
offset：      0 1 2  3  4 5 6
                     ↑        ↑
              第1行"行尾"   第2行行首
              offset = 3    offset = 4
```

- 第 1 行的**行尾位置没有属于自己的 offset** —— offset 3 是占位空格，offset 4 已是第 2 行的首字符；
- 选择手柄拖到行尾继续往右时，`getOffsetForPosition` 把"超出行宽"的位置算作**该行最后一个位置之后**，
  即 clamp 到 offset 4 → 视觉上就是**跳到第 2 行行首**；
- 占位空格越宽/越"隐藏"，这个跳变越像"突然跳走"。

**一句话**：**「行尾」在文本坐标里没有落脚点**，这才是根因 —— 不是 Compose 的问题，是段落承载方式的副作用。

---

## 三、已排除的方案（实测结论，勿重试）

**「把段落分隔换成真实 `\n`」**（去掉 ① 的替换 + ②的占位空格改 `append('\n')`，共 4 处）——
2026-09-15 试过并**已回退**，因为它与库架构根本冲突：

| 坑 | 说明 |
|----|------|
| **双重换行** | `ParagraphStyle` 的 range 边界**本身就会另起一行**（`MultiParagraph` 逐段垂直堆叠）。段末再写一个 `\n`，会在段落之间**多出一个空行** |
| **段落数爆炸** | `checkForParagraphs()` 会扫描文本里**所有** `\n` 并拆成新段落；它原本依赖"文本层没有 `\n`"来区分「用户新输入的换行」与「段落分隔」。一旦段落分隔也变成 `\n`，每次文本变更都会把已有段落**再拆一次** |

> 这两条已写进库代码的**警示注释**（子模块 `03ae90e` / 主仓库 `b0334529`），避免后人重蹈。

**「改用 `U+2028 LINE SEPARATOR`」也已被实测否掉**（曾作为"方案 A"提出，2026-09-15 最小实验）：

- 当时的思路是"用一个库不认识的换行符"（库只扫 `\n`）→ 既不触发拆段、又让行尾有 offset 归属；
- **实测结果**：Compose / Android 的硬换行**只认 `\n`（与 `\r\n`）**，`\u2028` 不被支持，
  而它又属于 Unicode 空白字符 → **被渲染成一个空格**（既不换行、还多出空格宽）；
- **结论**：App 侧**不存在**"能让 Compose 换行、又不同于 `\n`"的字符。已回退为 `\n`
  （代码内保留该结论的注释，避免重复尝试）。

---

## 四、候选方案

### 方案 D（推荐）：**普通段落块**用「单段落 + 段内 `\n`」承载换行

**核心洞察**：让"行尾"有 offset 归属的关键，是让换行符**真实存在于文本层**。
App 侧只能用 `\n`（方案 A 已否），因此需要让**库接受"段内 `\n`"**：

- **普通段落块**：块内换行 = 段内 `\n`（**不拆段**）→ Compose 按 `\n` 换行、段内多行
  **不产生额外空行** ✅，且 `\n` 占一个 offset → "行尾"归属正确 ✅ 手柄拖拽正常 ✅；
- **列表 / 任务列表块**：回车仍**拆成新段落**（每项各自带 bullet / 勾选框，语义要求如此），
  保持现状不受影响。

**为什么这次不会再踩 §3 的两个坑**：

| 坑 | 为什么不发生 |
|----|--------------|
| 双重换行 | `\n` 位于**段落内部**（不是段末）。"段内多行"与"段与段之间的边界换行"是两套机制，互不叠加 |
| 段落数爆炸 | `checkForParagraphs` 改为**按段落类型区分**：普通段落不拆、列表类才拆（见下） |

**改动面（全部在库，App 侧几乎不动）**：

1. `updateAnnotatedString`：`text.replace('\n', ' ')` → **保留段内 `\n`**
   （段间分隔仍由 `ParagraphStyle` 边界承担）；
2. `checkForParagraphs`：按段落类型决定是否拆段——`DefaultParagraph` **不拆** `\n`，
   `UnorderedList` / `OrderedList` / `TaskList` 保持拆段（各自 marker 需要独立段落）；
3. 其余段落构建点（`computeTextFromTree` 与带 `append(' ')` 的那两处）：同样按上述规则，
   段内 `\n` 原样写出、不再追加占位空格；
4. offset ↔ span 映射（`getRichSpanByTextIndex` 等）：`\n` 不属于任何 span，
   需确认现有"跳过非 span 字符"的处理能否覆盖（库已有"段落间占位空格不算 span"的先例，
   属**同类处理**，预计改动小但要逐一回归）。

**优势**：

- markdown 往返**天然一致**（段内 `\n` → `toMarkdown` 输出 `\n` → 加载回段内 `\n`）；
- 列表 / 任务列表 / 缩进等 `TextIndent` 系能力**完全不受影响**（段落机制保留）；
- App 侧无需任何字符转换（继续用 `\n`）。

**风险与回归重点**：库的 offset 计算涉及面广（光标移动、选区、删除、IME）。
必须回归：跨行退格 / 跨行选中 / 上下键移动 / 中文 IME / 列表与任务列表内回车 /
markdown 往返 / 图片与载体块 / 撤销重做。

### 方案 B：重做库的段落承载方式

让 `\n` 成为换行的唯一承载者，`ParagraphStyle` 退化为"纯样式"：

1. 文本层保留 `\n`，去掉占位空格；
2. **关键**：让"段边界不再额外换行"——但 `MultiParagraph` 逐段堆叠是 Compose 的固有行为，**无法关掉**；
   因此实际做法只能是**「整块合并为一个 ParagraphStyle range」**；
3. 代价：`TextIndent`（列表缩进、任务列表勾选框预留宽、有序列表 marker 悬挂）等**段落级样式全部失效** ——
   库的一半能力建立在这上面（列表 marker、任务列表勾选框、缩进层级都依赖 `ParagraphStyle`）。

**评估**：等于重写库的段落模型，且会丢掉核心能力（需要另行设计逐行样式方案）。
**成本 / 收益比明显不划算**，除非将来还要做"每行不同缩进"这类需求。

### 方案 C：接受现状

把跳变记为已知问题（成因已在库注释中写清），不做改动。
**代价**：拖拽手柄跨行时体验不佳；点击定位不受影响。

---

## 五、若走方案 D：验证清单

方案 D 的改动全在库的**段落 / offset 逻辑**，风险集中在光标与选区，建议按下面三段回归：

**第一阶段：单段落场景（核心，也是本问题的验收点）**

1. [ ] 普通段落内换行：**行尾有归属** —— 手柄从行首拖到行尾停住、继续右拖不变、下拖跟手到下一行对应位置
2. [ ] 段内多行**不产生额外空行**（这是与 §3 已排除方案的关键差异）
3. [ ] 跨行退格 / 跨行选中 / Ctrl+A / 上下键移动
4. [ ] 中文 IME：跨行位置拼音输入、候选词上屏

**第二阶段：多段落与列表**

5. [ ] 列表 / 任务列表内回车：**仍拆成新段落**，bullet / 勾选框正常续行
6. [ ] 列表与普通段落混排时的缩进、marker 对齐无变化
7. [ ] markdown 往返：保存 → 重进，段内换行与段落结构都一致

**第三阶段：其它不变量**

8. [ ] 图片 / 分割线插入删除、载体空块数量正常
9. [ ] 相邻文本块自动合并、任务列表勾选切换正常
10. [ ] 撤销 / 重做粒度正常

---

## 六、待你确认

1. 是否推进**方案 D**？（改库的段落 / offset 逻辑，中等改造。我会按 §4 列的四点逐处改，每处说明影响面）
2. 若暂不推进：是否按**方案 C** 接受现状？（跳变已记录为已知问题，成因写在库代码注释里）
3. 推进时是否需要我同时准备"改动前后对照"的手动回归清单（覆盖 §5 十项）？
