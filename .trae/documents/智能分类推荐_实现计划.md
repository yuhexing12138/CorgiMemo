# 智能分类推荐功能 - 实现计划

**日期**: 2026-05-21  
**阶段**: 待执行

---

## 阶段 1：数据模型与数据库

### 任务 1.1：扩展 CategoryType 和默认分类

**文件**: [Category.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/data/model/Category.kt)

**修改内容**:
1. 新增 `CategoryType.SPORT = 3`
2. `CategoryType.CUSTOM` 保持为 3（与设计文档不同，不影响现有数据）
3. 新增 `DefaultCategoryName.SPORT = "运动"`

### 任务 1.2：创建 CategoryKeyword 数据类和 MatchType 枚举

**新建文件**: `data/model/CategoryKeyword.kt`

**内容**:
- `MatchType` 枚举（EXACT, FUZZY，带权重）
- `CategoryKeyword` 数据类（id, keyword, categoryType, matchType, isUserDefined）
- 权重计算逻辑

### 任务 1.3：创建 Room 实体和 Dao

**新建文件**: 
- `data/local/db/CategoryKeywordEntity.kt`
- `data/local/db/CategoryKeywordDao.kt`

**内容**:
- `CategoryKeywordEntity`: Room 实体，映射到 `category_keywords` 表
- `CategoryKeywordDao`: 数据访问接口（CRUD、按分类查询等）

### 任务 1.4：数据库迁移

**文件**: [CorgiMemoDatabase.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/data/local/db/CorgiMemoDatabase.kt)

**修改内容**:
1. 数据库版本从 9 升级到 10
2. 添加 `MIGRATION_9_10`，创建 `category_keywords` 表及索引
3. 添加 `categoryKeywordDao()` 抽象方法
4. 更新默认分类初始化，添加"运动"分类

### 任务 1.5：依赖注入配置

**文件**: [DatabaseModule.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/di/DatabaseModule.kt)

**修改内容**:
- 添加 `provideCategoryKeywordDao()` 方法

### 任务 1.6：更新 CategoryRepository

**文件**: [CategoryRepository.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/data/repository/CategoryRepository.kt)

**修改内容**:
1. `initDefaultCategories()` 添加"运动"默认分类
2. 新增 `getSportCategory()`、`getSportCategoryId()` 方法

---

## 阶段 2：仓库层与智能推荐引擎

### 任务 2.1：创建 CategoryKeywordRepository

**新建文件**: `data/repository/CategoryKeywordRepository.kt`

**内容**:
- 预设关键词列表（工作、学习、生活、运动四类）
- 内存缓存机制（AtomicReference + AtomicBoolean）
- 初始化缓存方法
- 用户自定义关键词 CRUD 方法
- `getAllKeywordsSync()` 同步方法（供匹配引擎使用）
- `toDomainModel()` 扩展方法（Entity → Domain）

### 任务 2.2：创建 CategoryMatcher

**新建文件**: `data/repository/CategoryMatcher.kt`

**内容**:
- `MatchResult` 内部数据类（categoryType, score, matchedKeywords）
- `Recommendation` 内部数据类
- `recommendCategory(title, content)` 方法
  - 合并文本
  - 遍历关键词匹配
  - 计算得分
  - 处理平局（返回 null）

### 任务 2.3：添加 CategoryMatcher 依赖注入

**文件**: `DatabaseModule.kt` 或新建 `CategoryModule.kt`

**内容**:
- 提供 `CategoryMatcher` 的 `@Provides` 方法

---

## 阶段 3：分类选择器 UI

### 任务 3.1：创建 CategorySelector 组件

**新建文件**: `ui/components/CategorySelector.kt`

**内容**:
- 接收参数：分类列表、当前选中分类 ID、选中回调
- 卡片列表形式，水平排列
- 每个卡片带图标（📚/💼/🏠/🏃）和名称
- 选中高亮（根据分类类型不同颜色）
- 未选中时使用默认样式

**颜色方案**:
- 学习：蓝色系（同 RepeatType.WEEKLY）
- 工作：橙色系（同优先级"中"）
- 生活：绿色系（同优先级"低"）
- 运动：紫色系（同 RepeatType.WEEKLY）

### 任务 3.2：扩展 TodoEditViewModel

**文件**: [TodoEditViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt)

**修改内容**:
1. 新增依赖注入：
   - `categoryRepository: CategoryRepository`
   - 需要获取用户类型（检查现有代码如何获取）

2. 新增状态：
   - `_categories: MutableStateFlow<List<Category>>`
   - `categories: StateFlow<List<Category>>`

3. 新增方法：
   - `loadCategories()`：加载分类列表并设置默认分类（根据用户类型）

4. 修改现有方法：
   - `loadTodo()` 中确保分类已加载

### 任务 3.3：集成到 TodoEditScreen

**文件**: [TodoEditScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt)

**修改内容**:
1. 收集 `categories` 状态
2. 在 `LaunchedEffect(Unit)` 中调用 `viewModel.loadCategories()`
3. 在优先级选择器之后添加分类选择器
4. 确保分类选择器能正确更新 `viewModel.setCategoryId()`

---

## 阶段 4：智能推荐标签

### 任务 4.1：创建 RecommendationChip 组件

**新建文件**: `ui/components/RecommendationChip.kt`

**内容**:
- 暖橙色背景（`0xFFFFF3E0`）
- 文字颜色（`0xFFD97706`）
- 圆角 8dp
- 文字格式："💡 推荐：{分类名称}"
- 点击回调

### 任务 4.2：扩展 TodoEditViewModel 推荐逻辑

**文件**: [TodoEditViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/TodoEditViewModel.kt)

**修改内容**:
1. 新增依赖注入：
   - `categoryMatcher: CategoryMatcher`

2. 新增状态：
   - `_recommendedCategory: MutableStateFlow<Category?>`
   - `recommendedCategory: StateFlow<Category?>`

3. 新增方法：
   - `triggerRecommendation()`：触发分类推荐
   - `acceptRecommendation()`：接受推荐
   - `setTitleWithRecommendation(title)`：带防抖的标题设置
   - `recommendationJob: Job`：防抖任务引用

4. 防抖实现：
   - 使用 `delay(300) + cancel` 实现简单防抖
   - 每次设置标题时取消上一个任务，启动新任务

### 任务 4.3：集成推荐标签到 TodoEditScreen

**文件**: [TodoEditScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt)

**修改内容**:
1. 收集 `recommendedCategory` 状态
2. 使用 `AnimatedVisibility` 包裹推荐标签
3. 推荐标签显示在分类选择器上方
4. 点击标签调用 `viewModel.acceptRecommendation()`
5. 标题输入框改用 `viewModel.setTitleWithRecommendation()`

### 任务 4.4：语音输入集成

**文件**: [TodoEditScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/todo/TodoEditScreen.kt)

**修改内容**:
```kotlin
if (speechResult.isNotEmpty()) {
    viewModel.setTitle(speechResult)
    viewModel.triggerRecommendation()  // 新增
    speechViewModel.startListening()
}
```

---

## 阶段 5：智能分类设置页面

### 任务 5.1：创建 SmartCategorySettingsViewModel

**新建文件**: `viewmodel/SmartCategorySettingsViewModel.kt`

**内容**:
- 状态：`userKeywordsByCategory`、`categories`
- 方法：`loadData()`、`addKeyword()`、`deleteKeyword()`、`updateKeywordCategory()`

### 任务 5.2：创建 SmartCategorySettingsScreen

**新建文件**: `ui/screens/settings/SmartCategorySettingsScreen.kt`

**内容**:
1. 页面结构：
   - 顶部 AppBar（返回按钮 + 标题）
   - 说明卡片（什么是智能分类）
   - 添加关键词按钮
   - 按分类分组显示用户自定义关键词

2. 关键词列表项：
   - 关键词文本
   - 修改按钮（弹出选择分类对话框）
   - 删除按钮（确认后删除）

3. 添加关键词弹窗：
   - 关键词输入框
   - 分类选择（卡片列表）
   - 取消/添加按钮

### 任务 5.3：添加设置入口

**文件**: [SettingsScreen.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/settings/SettingsScreen.kt)

**修改内容**:
1. 在"应用设置"区域添加：
```kotlin
SettingItemCard(
    title = "🧠 智能分类设置",
    description = "管理智能分类关键词",
    onClick = {
        navController.navigate("smart_category_settings")
    }
)
```

### 任务 5.4：添加导航路由

**文件**: [AppNavHost.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/navigation/AppNavHost.kt)

**修改内容**:
```kotlin
composable("smart_category_settings") {
    SmartCategorySettingsScreen(navController = navController)
}
```

---

## 执行顺序

1. **阶段 1**（数据模型 + 数据库）→ 必须先完成
2. **阶段 2**（仓库层 + 推荐引擎）→ 依赖阶段 1
3. **阶段 3**（分类选择器）→ 依赖阶段 1
4. **阶段 4**（智能推荐标签）→ 依赖阶段 2、3
5. **阶段 5**（设置页面）→ 依赖阶段 1、2

---

## 验证点

### 功能验证
- [ ] 新建待办时，分类选择器显示 4 个分类
- [ ] 新建待办时，根据用户类型默认选中对应分类
- [ ] 输入包含关键词的标题时，显示推荐标签
- [ ] 点击推荐标签自动选中对应分类
- [ ] 语音输入后触发推荐
- [ ] 设置页面能添加、修改、删除自定义关键词
- [ ] 自定义关键词权重高于预设关键词

### 编译验证
- [ ] 项目能成功编译
- [ ] 无 lint 错误

---

## 文件变更汇总

### 新增文件
| 文件 | 阶段 |
|------|------|
| `data/model/CategoryKeyword.kt` | 1 |
| `data/local/db/CategoryKeywordEntity.kt` | 1 |
| `data/local/db/CategoryKeywordDao.kt` | 1 |
| `data/repository/CategoryKeywordRepository.kt` | 2 |
| `data/repository/CategoryMatcher.kt` | 2 |
| `ui/components/CategorySelector.kt` | 3 |
| `ui/components/RecommendationChip.kt` | 4 |
| `ui/screens/settings/SmartCategorySettingsScreen.kt` | 5 |
| `viewmodel/SmartCategorySettingsViewModel.kt` | 5 |

### 修改文件
| 文件 | 阶段 |
|------|------|
| `data/model/Category.kt` | 1 |
| `data/local/db/CorgiMemoDatabase.kt` | 1 |
| `di/DatabaseModule.kt` | 1 |
| `data/repository/CategoryRepository.kt` | 1 |
| `viewmodel/TodoEditViewModel.kt` | 3, 4 |
| `ui/screens/todo/TodoEditScreen.kt` | 3, 4 |
| `ui/screens/settings/SettingsScreen.kt` | 5 |
| `ui/navigation/AppNavHost.kt` | 5 |

---

**计划结束**
