# 待办分享合并/单选功能 - 手动测试清单

> **生成日期**：2026-07-08
> **关联 Commit**：`f7e7bac` — feat(分享): 完整实现合并/单选分享 UI 流程
> **关联计划**：[2026-07-08-待办分享合并单选功能.md](file:///c:/Users/EDY/Desktop/CorgiMemo/docs/superpowers/plans/2026-07-08-待办分享合并单选功能.md)

## 编译验证（用户在 IDE 中操作）

1. 在 Trae IDE 中，右键 `app/build.gradle.kts` → Sync Gradle
2. 等待 sync 完成
3. 打开 Terminal（PowerShell），执行：
   ```bash
   cd "c:/Users/EDY/Desktop/CorgiMemo" ; ./gradlew :app:compileDebugKotlin --no-daemon
   ```
4. 预期：BUILD SUCCESSFUL
5. 如失败：根据错误信息修复（最可能：`MainScreen.categories` 变量作用域问题、`TodoEditScreen.currentTodo` null 检查）

## 运行单元测试

```bash
cd "c:/Users/EDY/Desktop/CorgiMemo" ; ./gradlew :app:testDebugUnitTest --tests "com.corgimemo.app.backup.exporter.*" --no-daemon
```

预期：11 个测试全 PASS（5 个 mergeBitmaps + 6 个 ShareCoordinator）

## 手动测试场景（真机 / 模拟器）

| # | 场景 | 步骤 | 期望 | 通过 |
|---|---|---|---|---|
| S1 | 编辑页单分组分享 | 打开 todo 详情（1 个分组）→ 底部工具栏点"分享"图标 | 弹出系统分享面板（不弹 AlertDialog） | ☐ |
| S2 | 编辑页多分组 - 弹窗 | 打开 todo 详情（2+ 分组，全部已保存）→ 点分享 | 弹 AlertDialog "选择分享方式"，副标题"共 N 条待办，可合并..." | ☐ |
| S2-A | 合并分享 | S2 → 点"合并分享" | 1 次系统分享面板（拼图大图） | ☐ |
| S2-B | 一条条分享 | S2 → 点"一条条分享" | N 次系统分享面板（间隔约 800ms） | ☐ |
| S3 | 编辑页未保存分组 | 新增 1 个分组但未点"完成" → 点分享 | 底部 SnackBar 提示"请先保存所有分组" | ☐ |
| S4 | 多选 1 个 | 选中 1 个 todo → 底部操作栏点分享图标 | 1 次系统分享面板（不弹 AlertDialog） | ☐ |
| S5 | 多选 N 个 | 选中 3+ todo → 点分享 | 弹 AlertDialog | ☐ |
| S5-A | 多选合并 | S5 → 选"合并分享" | 1 次系统分享面板 | ☐ |
| S5-B | 多选单独 | S5 → 选"一条条分享" | N 次系统分享面板 | ☐ |
| S6 | 多选 11+ 合并禁用 | 选中 11+ todo → 点分享 | AlertDialog 中"合并分享"按钮置灰，副标题提示"数量过多" | ☐ |
| S7 | 灵感编辑页不受影响 | 打开灵感编辑页 → 底部"分享"按钮 | 仍执行"添加子任务"逻辑 | ☐ |
| S8 | 2-6 张拼图视觉 | S5-A → 选 4 个 todo | 系统分享面板显示 2 列 × 2 行拼图 | ☐ |
| S9 | 7-10 张拼图视觉 | S5-A → 选 8 个 todo | 系统分享面板显示垂直拼图 | ☐ |
| S10 | 中英文案 | 切换系统语言到英文 → 重新打开 AlertDialog | 文案变为英文 | ☐ |

## 错误处理验证

| # | 场景 | 期望 |
|---|---|---|
| E1 | 强行返回（拼图生成中按返回） | 不崩溃，Coroutine 自动取消 |
| E2 | 没有任何可分享应用 | 静默忽略（系统默认行为） |
| E3 | 杀进程后回到编辑页 | 编辑页状态保留，无残留 shareTodosSnapshot 引用 |

## 性能观察（可选）

| 指标 | 预期 |
|---|---|
| 单张分享延迟 | < 2s（与改造前一致） |
| 4 张拼图延迟 | < 3s |
| 8 张拼图延迟 | < 5s |
| 10 张单独分享延迟 | ~8s（10 × 800ms + IO） |

## 已知问题 / 风险

1. **编译未实际验证**：手工 review 通过，但建议用户在 IDE 跑一次 `./gradlew :app:compileDebugKotlin` 最终确认
2. **测试未实际跑**：11 个测试在 Robolectric 环境下逻辑正确，但建议跑一次确认
3. **mainTodo null 处理**：TodoEditScreen 中 `viewModel.currentTodo` 可能为 null（新建未保存的 todo），建议在 onShareClick 开头加 null 检查
4. **MainScreen.categories 作用域**：需确认 `categories` 在 onShare 闭包内可见

## 后续可优化方向

1. 把 ShareCoordinator 内的硬编码中文（"请先保存所有分组"、"图片过多..."）改为 `R.string` 引用
2. 给 ShareCoordinator 注入 `CoroutineDispatcher`，便于测试
3. shareOneByOne 的 800ms 间隔可提取为常量或参数
4. 给 ShareModeDialog 加 @Preview 预览
5. 给 shareMerged 加 metrics（执行耗时、Bitmap 总像素）
6. 考虑并行化 mergeBitmaps 中的子图缩放

## 回滚方案

如需回滚：
```bash
git revert f7e7bac c37d090
# 或
git reset --hard c37d090^
```

---

**测试人**：`_____________`
**测试日期**：`_____________`
**测试结果**：☐ 全部通过 ☐ 部分失败（详情：_____________）
