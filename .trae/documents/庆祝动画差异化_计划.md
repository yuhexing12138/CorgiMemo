# 庆祝动画差异化实现计划

## 一、仓库研究结论

### 当前实现状态分析

通过检查代码，发现以下状态：

#### ViewModel 层（HomeViewModel.kt）- ✅ 已完成
```kotlin
// 第35-52行：CelebrationLevel 枚举已实现
enum class CelebrationLevel(val priority: Int) {
    LOW(0), MEDIUM(1), HIGH(2);
    companion object {
        fun fromPriority(priority: Int): CelebrationLevel { ... }
    }
}

// 第58-62行：CelebrationState 数据类已实现
data class CelebrationState(
    val isShowing: Boolean = false,
    val level: CelebrationLevel = CelebrationLevel.LOW,
    val message: String = "太棒了！"
)

// 第107-108行：状态流已升级
private val _celebrationState = MutableStateFlow(CelebrationState())
val celebrationState: StateFlow<CelebrationState> = _celebrationState.asStateFlow()

// 第496-514行：鼓励语和时长函数已实现
private fun getEncouragementMessage(level: CelebrationLevel): String { ... }
private fun getCelebrationDuration(level: CelebrationLevel): Long { ... }

// 第475-490行：toggleTodoStatus 已修改
// 第522-549行：handleTaskCompleted 已修改
```

#### UI 层（HomeScreen.kt）- 部分完成

| 组件 | 调用处（第237行） | 定义处（第948行） | 状态 |
|------|------------------|------------------|------|
| `CelebrationOverlay` | `CelebrationOverlay(level, message)` | `fun CelebrationOverlay(level, message)` | ✅ 匹配 |
| `GlowOverlay` | `GlowOverlay(level = celebrationState.level)` | `fun GlowOverlay()` | ❌ **不匹配** |

**问题：** `GlowOverlay` 调用处传递了 `level` 参数，但定义处没有参数。这会导致编译错误。

---

## 二、需求回顾

### 三级庆祝动画需求

| 优先级 | priority | 动画类型 | 效果 | 鼓励语 | 持续时间 |
|--------|----------|---------|------|--------|---------|
| 低优先级 | 0 | 轻量庆祝 | 微笑 + 轻微摇尾巴 | "太棒了！" | 约 1 秒 |
| 中优先级 | 1 | 中等庆祝 | 跳跃 + 小星星特效 | "完成得不错哦！" | 约 2 秒 |
| 高优先级 | 2 | 隆重庆祝 | 跳跃 + 撒花 + 彩虹特效 | "柯基为你骄傲！" | 约 3 秒 |

---

## 三、待完成的工作

### 3.1 修改 GlowOverlay 组件

**文件：** `CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt`

**当前定义（第948行）：**
```kotlin
fun GlowOverlay() { ... }
```

**需要修改为：**
```kotlin
fun GlowOverlay(level: CelebrationLevel) { ... }
```

**三级光晕效果：**

| 级别 | 光晕显示 | 颜色 | 透明度 | 宽度 |
|------|---------|------|--------|------|
| LOW（低） | 不显示 | - | - | - |
| MEDIUM（中） | 显示 | 暖橙色 (#FF9A5C) | 0.20f | 60dp |
| HIGH（高） | 显示 | 彩虹渐变 | 0.35f | 80dp |

---

## 四、详细实现步骤

### 步骤一：修改 GlowOverlay 函数签名

**修改位置：** `HomeScreen.kt:948`

**修改前：**
```kotlin
@Composable
fun GlowOverlay() {
```

**修改后：**
```kotlin
@Composable
fun GlowOverlay(level: CelebrationLevel) {
```

### 步骤二：添加根据级别显示不同效果的逻辑

**修改位置：** `HomeScreen.kt:948-1027`

**实现逻辑：**
```kotlin
@Composable
fun GlowOverlay(level: CelebrationLevel) {
    // 低优先级不显示光晕
    if (level == CelebrationLevel.LOW) return
    
    // 根据级别选择颜色和透明度
    val (glowColor, alpha, width) = when (level) {
        CelebrationLevel.LOW -> Triple(Color.Transparent, 0f, 0.dp)  // 不会执行到这里
        CelebrationLevel.MEDIUM -> Triple(Color(0xFFFF9A5C), 0.20f, 60.dp)
        CelebrationLevel.HIGH -> Triple(Color(0xFFFF9A5C), 0.35f, 80.dp)  // 彩虹效果
    }
    
    Box(...) {
        // 四周边缘光晕...
    }
}
```

### 步骤三：彩虹光晕效果（仅 HIGH 级别）

对于 HIGH 级别，使用彩虹渐变：
```kotlin
if (level == CelebrationLevel.HIGH) {
    // 彩虹渐变：红→橙→黄→绿→蓝→紫
    val rainbowColors = listOf(
        Color(0xFFFF6B6B),  // 红
        Color(0xFFFFB347),  // 橙
        Color(0xFFFFEB3B),  // 黄
        Color(0xFF4CAF50),  // 绿
        Color(0xFF2196F3),  // 蓝
        Color(0xFF9C27B0),  // 紫
    )
    // 使用 rainbowColors 替换单一颜色
}
```

---

## 五、文件修改清单

| 文件路径 | 修改类型 | 修改内容 |
|---------|---------|---------|
| `CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | 修改 | 修改 `GlowOverlay` 函数签名；添加三级光晕效果逻辑 |

---

## 六、风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 编译错误（参数不匹配） | 高 | 修改函数签名后立即编译验证 |
| 彩虹效果过于复杂 | 中 | 先实现基础三级效果，彩虹效果可简化 |
| 动画显示异常 | 低 | 使用简单的条件判断控制显示逻辑 |

---

## 七、编译验证计划

修改完成后，根据用户的编译验证规则：
1. **先询问用户是否进行编译验证**
2. 如果用户同意：
   - 执行 `gradle clean` 清理缓存
   - 执行 `gradle :app:compileDebugKotlin` 编译验证

---

## 八、预期最终效果

完成后：

```
低优先级任务完成（priority=0）：
  → 😊 太棒了！（1秒后消失）
  → 无光晕效果

中优先级任务完成（priority=1）：
  → ⭐ 完成得不错哦！（2秒后消失）
  → 暖橙色光晕（60dp 宽度）

高优先级任务完成（priority=2）：
  → 🎉 这么重要的任务都完成了！柯基为你骄傲！（3秒后消失）
  → 彩虹渐变光晕（80dp 宽度）
```
