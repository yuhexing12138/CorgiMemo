# 待办卡片优先级视觉标识增强 - 收尾计划

## Context（背景）

CorgiMemo 项目待办卡片优先级视觉标识增强任务的主体代码改动已在上一轮完成。本轮任务是**收尾**，包括：

1. **验证 import 语句完整性**（按 `.trae/rules/import语句检查.md`）
2. **询问用户是否进行 git 提交**（按 `.trae/rules/git提交.md`）
3. **处理与本任务无关的残留修改**（CorgiDetailScreen.kt、HomeScreen.kt）

**主体改动文件**（本次任务范围，共 7 个）：

| # | 文件 | 改动 |
|---|------|------|
| 1 | `app/src/main/java/com/corgimemo/app/ui/components/PriorityColors.kt` | 新增 None/NoneDim/PriorityVisual/priorityVisualOf |
| 2 | `app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt` | Card 边框+阴影，elevation=0 |
| 3 | `app/src/main/java/com/corgimemo/app/ui/screens/recyclebin/DeletedTodoCard.kt` | 完整三联视觉 |
| 4 | `app/src/main/java/com/corgimemo/app/ui/screens/recyclebin/RecycleBinUiState.kt` | DeletedTodoListItem.priority |
| 5 | `app/src/main/java/com/corgimemo/app/ui/screens/recyclebin/RecycleBinViewModel.kt` | 透传 priority |
| 6 | `app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt` | TodoGroupContainer 无优先级边框 |
| 7 | `docs/superpowers/specs/UI设计规范.md` | §12.1.2.3/§12.1.2.4/§12.1.10 |

## 当前状态分析

### Phase 1 探索结果

执行 `git status` 确认工作树状态：

```
modified:   app/src/main/java/com/corgimemo/app/ui/components/CheckboxEditText.kt
modified:   app/src/main/java/com/corgimemo/app/ui/components/PriorityColors.kt
modified:   app/src/main/java/com/corgimemo/app/ui/components/TodoListItem.kt
modified:   app/src/main/java/com/corgimemo/app/ui/screens/corgi/CorgiDetailScreen.kt  ← 非本次任务
modified:   app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt            ← 非本次任务
modified:   app/src/main/java/com/corgimemo/app/ui/screens/recyclebin/DeletedTodoCard.kt
modified:   app/src/main/java/com/corgimemo/app/ui/screens/recyclebin/RecycleBinUiState.kt
modified:   app/src/main/java/com/corgimemo/app/ui/screens/recyclebin/RecycleBinViewModel.kt
modified:   "docs/superpowers/specs/UI设计规范.md"

Untracked:  .trae/documents/待办卡片优先级视觉标识增强.md
```

### import 语句检查结论

逐文件核验，所有 import 完整、无缺失：

| 文件 | 关键 import | 结论 |
|------|------------|------|
| `PriorityColors.kt` | `androidx.compose.ui.graphics.Color` | ✓ 已有 |
| `TodoListItem.kt` | `border`、`shadow`、PriorityColors | ✓ PriorityColors 同 package，无需 import；border/shadow 已存在 |
| `DeletedTodoCard.kt` | `PriorityColors`（跨 package） | ✓ 第 29 行已 import |
| `CheckboxEditText.kt` | PriorityColors | ✓ 同 package，无需 import |
| `RecycleBinUiState.kt` | 不使用 | ✓ |
| `RecycleBinViewModel.kt` | 不使用 | ✓ |
| `CorgiDetailScreen.kt` | `rememberModalBottomSheetState`、`BehaviorType` | ✓ 两个 import 都有真实使用（L253, L319/374） |
| `HomeScreen.kt` | `androidx.compose.ui.window.Dialog` | ✓ 已有 import，3 处使用（L2097/2163/2252） |

**结论：所有 import 完整，本次任务无新增 import。**

### 与本任务无关的残留修改

`CorgiDetailScreen.kt` 和 `HomeScreen.kt` 改动属于**柯基互动页行为接入与死按钮修复**任务（参见 `柯基互动页行为接入与死按钮修复方案.md`），是上一轮工作残留，需在 commit 时与用户确认如何处理。

## Proposed Changes（提议的变更）

### Step 1: 询问用户如何处理非本任务文件

由于 `CorgiDetailScreen.kt` 和 `HomeScreen.kt` 不属于本次"待办卡片优先级视觉标识增强"任务，但工作树已修改，必须询问用户：

- 选项 A：本次 commit 只包含本任务 7 个文件，2 个残留文件 revert（推荐）
- 选项 B：本次 commit 包含本任务 7 个文件 + 2 个残留文件一起 commit
- 选项 C：先 revert 2 个残留文件，本次 commit 暂不执行

### Step 2: 询问用户是否进行 git 提交

按照 `.trae/rules/git提交.md`，使用 AskUserQuestion 询问：
- 选项 A：进行 commit（生成中文提交信息）
- 选项 B：暂不 commit

### Step 3: 如果用户同意 commit，按以下规范执行

按 `.trae/rules/git提交.md` 规范：
- 智能生成**中文**提交信息
- 格式建议：`feat(ui): 待办卡片三联优先级视觉(竖条+边框+阴影) + 无优先级浅绿色`
- **绝不在未询问的情况下执行**
- **绝不在未编译验证的情况下自动 commit**（按 `.trae/rules/编译验证.md`，编译由用户执行）

### Step 4: 不自动执行的项

- ❌ **不执行 `gradlew assembleDebug`**（按 `.trae/rules/编译验证.md`）
- ❌ **不自动 push**（按项目记忆）
- ❌ **不修改任何代码**（本轮只是收尾+确认）

## Assumptions & Decisions

| 决策 | 理由 |
|------|------|
| 不修改任何代码 | 所有 import 已完整，主体改动已完成 |
| 不自动 commit | 按 `.trae/rules/git提交.md`，必须用户确认 |
| 不自动编译 | 按 `.trae/rules/编译验证.md`，必须用户确认 |
| 残留文件需用户决策 | 这 2 个文件与本任务无关，应单独确认 |
| plan 文件命名 | `待办卡片优先级视觉标识收尾计划.md`（中文命名，按 `.trae/rules/文档命名语言要求.md`） |

## Verification（验证步骤）

完成本计划后，验证清单：

| # | 验证项 | 方法 |
|---|--------|------|
| 1 | 所有本任务 import 完整 | rg 搜索 + Read 抽样（已完成） |
| 2 | 残留文件与本任务无关 | git diff 比对（已完成） |
| 3 | 工作树状态清晰 | git status（已完成） |
| 4 | 用户已确认 commit 范围 | AskUserQuestion 询问 |
| 5 | commit 信息中文 | 智能生成 + 二次确认 |
| 6 | commit 成功 | git log -1 显示新 commit |

## 不在本计划范围

- ❌ 编译验证（由用户手动执行）
- ❌ UI 原型浏览器展示（按 `.trae/rules/ui原型展示.md` 仅在 UI 设计阶段需要，本次是代码改造）
- ❌ 推送远程（按用户偏好）
- ❌ 重构/优化残留文件（CorgiDetailScreen/HomeScreen 由后续柯基任务处理）

## 下一步（After Plan Approval）

执行顺序：

1. **AskUserQuestion #1**：询问如何处理 2 个残留修改文件（CorgiDetailScreen.kt / HomeScreen.kt）
2. **AskUserQuestion #2**：询问是否进行 git commit
3. **如果同意 commit**：
   - 按用户选择 add 对应文件
   - `git commit -m "..."` 用中文提交信息
   - 验证 `git log -1` 显示成功
4. **最终回复**：给出总结 + 优化建议（按 `.trae/rules/优化建议.md`）
