# 创建待办副本 实施计划

> **创建日期**：2026-07-27
> **状态**：已批准，待实施
> **类型**：功能增强（v1）

## 1. Context（背景与目标）

### 1.1 背景

`HomeViewModel.batchDuplicate()`（[HomeViewModel.kt:3739-3837](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt#L3739-L3837)）是**多选模式**下的批量复制功能：
- 数据源：`_selectedTodoIds`（用户主动选中的 todo）
- status 行为：**一律**重置为 0（未完成）

但当前**待办页右上角三点菜单**的"创建待办副本"是**占位项**（`Modifier.alpha(0.4f)` + `onPlaceholderClick`），点击后弹"功能开发中..." Snackbar（[MainScreen.kt:725](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt#L725)）。

### 1.2 目标

将"创建待办副本"占位项升级为**真实功能**：
- 入口：待办页 → 三点菜单 → 创建待办副本
- 行为：弹中心 AlertDialog，让用户选择复制范围
- 数据源：首页当前**可见**的 todo（`filteredTodos`，受 hideCompleted / search / categoryId / statusFilter 过滤）
- 复制范围由用户在弹窗中**主动选择**（3 选 1）

### 1.3 与 `batchDuplicate` 的关键差异

| 维度 | `batchDuplicate`（已有） | `duplicateAllTodos`（本任务） |
| --- | --- | --- |
| 数据源 | `_selectedTodoIds`（用户多选） | `filteredTodos`（首页可见） |
| status 行为 | 一律重置 0 | 按 range 决定（3 选 1） |
| 走 batch 模式 | 是 | 否 |
| 入口 | 批量选择模式 → 复制按钮 | 三点菜单 → 创建待办副本 |

## 2. 关键决策（已与用户确认）

| 决策点 | 选择 |
| --- | --- |
| 数据源 | `filteredTodos`（所有可见 todo） |
| 多卡 / 整组 | 项目**无 groupId 概念**（TodoItem 实体无此字段）；"整组"= 复制每个 todo 的 `subTasks`（与 `batchDuplicate` 行为一致） |
| 回调重命名 | `onPlaceholderClick` → `onDuplicateTodoClick`（更清晰） |
| 弹窗位置 | `HomeScreen` 内部（与 `showBatchDeleteDialog` 同模式） |
| 失败 Snackbar | 复用 `_pendingBatchDuplicateFailure`（零新代码） |
| 成功反馈 | 静默（与 `batchDuplicate` 一致） |

## 3. 文件改动清单

| # | 文件 | 操作 | 关键改动 |
| --- | --- | --- | --- |
| 1 | `app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt` | **修改** | 新增 `DuplicateRange` 枚举、`_showDuplicateDialog` + `setShowDuplicateDialog()`、`duplicateAllTodos(range)` 方法 |
| 2 | `app/src/main/java/com/corgimemo/app/ui/components/DuplicateTodoDialog.kt` | **新增** | 中心 AlertDialog 组件，3 选 1 RadioButton |
| 3 | `app/src/main/java/com/corgimemo/app/ui/components/TodoMenuDropdown.kt` | **修改** | `onPlaceholderClick` → `onDuplicateTodoClick`；移除"创建待办副本"项的 `Modifier.alpha(0.4f)`；更新 KDoc 注释 |
| 4 | `app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt` | **修改** | **只**改 TODO Tab 的 `TodoMenuDropdown` 调用点（行 725）：`onPlaceholderClick = { ... }` → `onDuplicateTodoClick = { homeViewModel.setShowDuplicateDialog(true) }` |
| 5 | `app/src/main/java/com/corgimemo/app/ui/screens/home/HomeScreen.kt` | **修改** | 1) 顶部 `collectAsState` 订阅 `showDuplicateDialog`；2) 渲染 `DuplicateTodoDialog` |
| 6 | `app/src/main/res/values/strings.xml` | **修改** | 新增 4 个字符串 |
| 7 | `app/src/main/res/values-en/strings.xml` | **修改** | 新增 4 个英文 |

> **不**修改 `InspirationMenuDropdown.kt` / `SpecialDateMenuDropdown.kt`（它们的 `onPlaceholderClick` 是独立参数）

## 4. 实施步骤

### Step 1：`HomeViewModel.kt` 新增核心逻辑

**位置**：[HomeViewModel.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/viewmodel/HomeViewModel.kt)

**1.1 新增枚举**（紧跟 `StatusFilter` 同构模式）：

```kotlin
/**
 * 创建待办副本 - 复制范围枚举
 */
enum class DuplicateRange(val labelKey: String) {
    /** 只复制未完成任务（status == 0）—— 默认值 */
    PENDING_ONLY("duplicate_range_pending_only"),
    /** 复制全部任务，保留原 status（已完成仍标完成） */
    KEEP_STATUS("duplicate_range_keep_status"),
    /** 复制全部任务，重置 status 为 0 */
    RESET_STATUS("duplicate_range_reset_status"),
}
```

**1.2 新增 StateFlow + setter**（参考 `_showBatchDeleteDialog` 模式）：

```kotlin
// ========== 创建待办副本（首页可见 todo 批量复制） ==========
private val _showDuplicateDialog = MutableStateFlow(false)
val showDuplicateDialog: StateFlow<Boolean> = _showDuplicateDialog.asStateFlow()

fun setShowDuplicateDialog(show: Boolean) {
    _showDuplicateDialog.value = show
}
```

**1.3 新增 `duplicateAllTodos(range)` 方法**（参考 `batchDuplicate()` 改造，**核心**是 status 三态处理）：

```kotlin
fun duplicateAllTodos(range: DuplicateRange) {
    val currentTime = System.currentTimeMillis()

    // 步骤 1：按 range 决定预过滤
    val sourceTodos = when (range) {
        DuplicateRange.PENDING_ONLY  -> filteredTodos.value.filter { it.status == 0 }
        DuplicateRange.KEEP_STATUS   -> filteredTodos.value
        DuplicateRange.RESET_STATUS  -> filteredTodos.value
    }
    if (sourceTodos.isEmpty()) return  // 边界：0 个 todo 静默返回

    val ids = sourceTodos.map { it.id }
    viewModelScope.launch {
        var hasFailure = false
        try {
            val newSubTaskMaps = mutableMapOf<Long, Long>()
            val duplicatePairs = mutableListOf<Pair<Long, Long>>()

            // 阶段 1：主表 + SubTask 数据库复制（同步）
            ids.forEach { id ->
                val todo = todoRepository.getTodoById(id) ?: return@forEach

                // ★ status 三态处理（关键差异点）
                val (newStatus, newCompletedAt) = when (range) {
                    DuplicateRange.PENDING_ONLY -> 0 to null
                    DuplicateRange.KEEP_STATUS  -> todo.status to todo.completedAt
                    DuplicateRange.RESET_STATUS -> 0 to null
                }

                val newId = todoRepository.insertTodo(
                    todo.copy(
                        id = 0,
                        status = newStatus,
                        completedAt = newCompletedAt,
                        createdAt = currentTime,
                        updatedAt = currentTime
                    )
                )

                // 复制子任务（与 batchDuplicate 一致）
                val subTasks = SubTaskManager.getSubTasks(context, id)
                if (subTasks.isNotEmpty()) {
                    SubTaskManager.addSubTasks(context, newId, subTasks)
                    val newSubTasks = SubTaskManager.getSubTasks(context, newId)
                    for (original in subTasks) {
                        val matched = newSubTasks.firstOrNull { it.order == original.order }
                        if (matched != null) newSubTaskMaps[original.id] = matched.id
                    }
                }
                duplicatePairs.add(id to newId)
            }

            // 刷新子任务进度 Map
            if (newSubTaskMaps.isNotEmpty()) {
                val updatedProgressMap = _subTaskProgressMap.value.toMutableMap()
                val updatedSubTasksMap = _subTasksMap.value.toMutableMap()
                duplicatePairs.forEach { (_, newId) ->
                    val progress = SubTaskManager.getProgressText(context, newId)
                    if (progress != null) updatedProgressMap[newId] = progress
                    val subTasks = SubTaskManager.getSubTasks(context, newId)
                    updatedSubTasksMap[newId] = subTasks
                }
                _subTaskProgressMap.value = updatedProgressMap
                _subTasksMap.value = updatedSubTasksMap
            }

            // 阶段 2：文件复制（后台异步）
            for ((originalId, newId) in duplicatePairs) {
                try {
                    fileCopyManager.copyAllAttachments(originalId, newId).collect { /* 静默 */ }
                } catch (e: CancellationException) { throw e }
                catch (e: Exception) {
                    Log.e("HomeViewModel", "复制附件失败: originalId=$originalId, newId=$newId", e)
                    hasFailure = true
                }
            }
        } catch (e: CancellationException) { throw e }
        catch (e: Exception) {
            Log.e("HomeViewModel", "创建待办副本失败", e)
            hasFailure = true
        } finally {
            if (hasFailure) _pendingBatchDuplicateFailure.value = true  // 复用现有失败 trigger
        }
    }
}
```

### Step 2：新增 `DuplicateTodoDialog.kt`

**文件**：[DuplicateTodoDialog.kt](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/DuplicateTodoDialog.kt)（新增）

**UI 规范**（参考 [UI设计规范.md:1168-1475](file:///c:/Users/EDY/Desktop/CorgiMemo/docs/superpowers/specs/UI设计规范.md#L1168-L1475)）：
- 容器：Material3 AlertDialog 默认（圆角 16dp、阴影 8dp）
- 标题：`FontWeight.SemiBold`
- 确认按钮：`MaterialTheme.colorScheme.primary`（橙色 `0xFFFF9A5C`）
- **不**加 icon 槽位，**不**显式 containerColor / shape
- RadioButton 范本：[LocationPicker.kt:184-195](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/components/LocationPicker.kt#L184-L195)

```kotlin
@Composable
fun DuplicateTodoDialog(
    showDialog: Boolean,
    onDismiss: () -> Unit,
    onConfirm: (DuplicateRange) -> Unit,
    initialRange: DuplicateRange = DuplicateRange.PENDING_ONLY,
) {
    var selectedRange by remember(showDialog) { mutableStateOf(initialRange) }
    if (!showDialog) return

    AlertDialog(
        onDismissRequest = onDismiss,
        title = { Text(stringResource(R.string.duplicate_dialog_title), fontWeight = FontWeight.SemiBold) },
        text = {
            Column(modifier = Modifier.selectableGroup()) {
                DuplicateRange.entries.forEach { range ->
                    Row(
                        modifier = Modifier
                            .fillMaxWidth()
                            .height(48.dp)
                            .selectable(
                                selected = (selectedRange == range),
                                onClick = { selectedRange = range },
                                role = Role.RadioButton,
                            )
                            .padding(end = 24.dp),
                        verticalAlignment = Alignment.CenterVertically,
                    ) {
                        RadioButton(
                            selected = (selectedRange == range),
                            onClick = null,
                            colors = RadioButtonDefaults.colors(
                                selectedColor = MaterialTheme.colorScheme.primary,
                            ),
                            modifier = Modifier.size(20.dp),
                        )
                        Spacer(Modifier.size(12.dp))
                        Text(
                            stringResource(
                                when (range) {
                                    DuplicateRange.PENDING_ONLY  -> R.string.duplicate_range_pending_only
                                    DuplicateRange.KEEP_STATUS   -> R.string.duplicate_range_keep_status
                                    DuplicateRange.RESET_STATUS  -> R.string.duplicate_range_reset_status
                                }
                            ),
                            style = MaterialTheme.typography.bodyLarge,
                        )
                    }
                }
            }
        },
        confirmButton = {
            TextButton(onClick = { onConfirm(selectedRange) }) {
                Text(stringResource(R.string.common_confirm), color = MaterialTheme.colorScheme.primary)
            }
        },
        dismissButton = {
            TextButton(onClick = onDismiss) {
                Text(stringResource(R.string.common_cancel), color = MaterialTheme.colorScheme.onSurface)
            }
        },
    )
}
```

### Step 3：修改 `TodoMenuDropdown.kt`

**3 处改动**：
1. 函数签名：参数 `onPlaceholderClick` → `onDuplicateTodoClick`
2. "创建待办副本"项：移除 `Modifier.alpha(0.4f)`（现为真实功能，不再是占位）
3. 顶部 KDoc 注释：第 5 项"创建待办副本（占位）" → "创建待办副本"
4. 若 `androidx.compose.ui.draw.alpha` 不再被使用 → 删除该 import

### Step 4：修改 `MainScreen.kt`（**只**改 TODO Tab 的调用点）

**位置**：[MainScreen.kt:725](file:///c:/Users/EDY/Desktop/CorgiMemo/app/src/main/java/com/corgimemo/app/ui/screens/main/MainScreen.kt#L725)

```kotlin
// 原
onPlaceholderClick = {
    coroutineScope.launch { snackbarHostState.showSnackbar("功能开发中...") }
},
// 新
onDuplicateTodoClick = {
    homeViewModel.setShowDuplicateDialog(true)
},
```

> **不**动 `SpecialDateMenuDropdown` / `InspirationMenuDropdown` 的同名参数。

### Step 5：修改 `HomeScreen.kt`

**2 处改动**：
1. **顶部订阅**（紧跟 `showBatchDeleteDialog`）：

```kotlin
val showDuplicateDialog by viewModel.showDuplicateDialog.collectAsState()
```

2. **渲染 dialog**（在 `showBatchDeleteDialog` 弹窗之后）：

```kotlin
DuplicateTodoDialog(
    showDialog = showDuplicateDialog,
    onDismiss = { viewModel.setShowDuplicateDialog(false) },
    onConfirm = { range ->
        viewModel.setShowDuplicateDialog(false)
        viewModel.duplicateAllTodos(range)
    },
)
```

> **不需要**新写 `LaunchedEffect` —— 失败 Snackbar 完全复用 `_pendingBatchDuplicateFailure`（HomeScreen.kt:588-606 已有监听）

### Step 6：新增字符串（4 个）

**`values/strings.xml`**：
```xml
<!-- 创建待办副本 对话框 -->
<string name="duplicate_dialog_title">创建待办副本</string>
<string name="duplicate_range_pending_only">只复制未完成的任务</string>
<string name="duplicate_range_keep_status">复制全部任务，保留已完成状态</string>
<string name="duplicate_range_reset_status">复制全部任务，不保留已完成状态</string>
```

**`values-en/strings.xml`**（对应英文）：
```xml
<string name="duplicate_dialog_title">Create Todo Copies</string>
<string name="duplicate_range_pending_only">Only copy incomplete tasks</string>
<string name="duplicate_range_keep_status">Copy all tasks, keep completed status</string>
<string name="duplicate_range_reset_status">Copy all tasks, reset to incomplete</string>
```

## 5. 复用的现有代码

| 复用项 | 位置 | 用途 |
| --- | --- | --- |
| `batchDuplicate()` 复制逻辑骨架 | `HomeViewModel.kt:3739-3837` | 复制流程参考（status 三态处理是新差异点） |
| `_pendingBatchDuplicateFailure` + `clearPendingBatchDuplicateFailure()` | `HomeViewModel.kt:744-752` | 失败 Snackbar trigger |
| `LaunchedEffect(pendingBatchDuplicateFailure, ...)` | `HomeScreen.kt:588-606` | 失败 Snackbar 监听（零新代码） |
| `fileCopyManager.copyAllAttachments()` | `FileCopyManager.kt:37-92` | 附件深复制 |
| `SubTaskManager.addSubTasks()` | `SubTaskManager.kt` | 子任务复制 |
| `MaterialTheme.colorScheme.primary` | M3 + UiColors.Primary `0xFFFF9A5C` | 确认按钮 / RadioButton 选中色 |
| `AppSnackbarHost` | `AppSnackbarHost.kt` | Snackbar 渲染（**禁用**裸 SnackbarHost） |
| `_showBatchDeleteDialog` + `setShowBatchDeleteDialog()` 模式 | `HomeViewModel.kt:563-582` | dialog state 管理模板 |

## 6. 边界情况处理

| 边界 | 处理 |
| --- | --- |
| 复制 0 个 todo（filter 后为空） | 静默 return，不弹 Snackbar（与 `batchDuplicate` 行 3741 一致） |
| 复制成功 | 静默，不弹 Snackbar（与 `batchDuplicate` 一致） |
| 复制失败（部分/全部） | 复用 `_pendingBatchDuplicateFailure` → 弹"⚠️ 部分文件复制失败" |
| 复制进度反馈 | 不显示（与 batchDuplicate"方案 B"一致） |
| 取消按钮 | 仅 `setShowDuplicateDialog(false)`，无其他副作用 |
| 复制中再次打开 dialog | 新建独立 `viewModelScope.launch` 协程，不冲突（**优化点**：可显示进度） |
| 已删除 todo（race condition） | `todoRepository.getTodoById(id) ?: return@forEach` 防御 |
| 主题切换 / 旋转屏 | dialog state 走 StateFlow，自动恢复 |

## 7. UI 规范遵循

按 [UI设计规范.md](file:///c:/Users/EDY/Desktop/CorgiMemo/docs/superpowers/specs/UI设计规范.md)：
- **12.1.11** AlertDialog 章节（L1168-1475）：Material3 默认容器（圆角 16dp、阴影 8dp）、标题 `FontWeight.SemiBold`、**不**加 icon、不显式 `containerColor` / `shape`
- **12.1.11.8** 视觉不一致项指出破坏性按钮应迁移到 `Color(0xFFFF6B6B)`。**本任务"复制"非破坏性操作**，确认按钮用 `MaterialTheme.colorScheme.primary`（橙色），不适用 `0xFFFF6B6B`
- RadioButton 选中色：M3 默认 = `MaterialTheme.colorScheme.secondary` = `0xFFFF9A5C`，与 LocationPicker 一致

## 8. 验证步骤（端到端测试）

### 8.1 编译验证（按 `编译验证.md`，必须先询问用户）
```powershell
cd c:\Users\EDY\Desktop\CorgiMemo
.\gradlew assembleDebug
```

### 8.2 功能验证（14 个场景）

| # | 场景 | 操作 | 预期 |
| --- | --- | --- | --- |
| 1 | 默认选项 | 5 未完成 + 3 已完成 → 三点 → 创建待办副本 → 选"只复制未完成" → 确定 | 新增 5 个未完成 todo |
| 2 | 选项 2 | 同上 → 选"复制全部，保留已完成状态" | 新增 8 个 todo（5 未完成 + 3 已完成） |
| 3 | 选项 3 | 同上 → 选"复制全部，不保留已完成状态" | 新增 8 个 todo（全部未完成） |
| 4 | 取消 | 点"取消" | 弹窗关闭，无数据变化 |
| 5 | 0 个 todo | 删除所有 todo → 三点 → 创建待办副本 → 确定 | 静默无效果 |
| 6 | 搜索过滤 | 搜索"买菜"命中 2 个 → 三点 → 创建待办副本 | 只复制 2 个 |
| 7 | 分类过滤 | 选"工作"分类（3 个）→ 三点 → 创建待办副本 | 只复制工作分类的 3 个 |
| 8 | 隐藏已完成 | 开"隐藏已完成" → 三点 → 创建待办副本 | 只复制可见的未完成 |
| 9 | 含子任务 | 1 todo + 3 子任务 → 三点 → 创建待办副本 | 新 todo 含 3 子任务，进度立即显示 |
| 10 | 含附件 | 1 todo + 2 图 + 1 语音 → 三点 → 创建待办副本 | 新 todo 含 2 图 + 1 语音（文件复制） |
| 11 | 附件复制失败 | 手动删源附件 → 三点 → 创建待办副本 | Snackbar"⚠️ 部分文件复制失败" |
| 12 | 旋转屏 | 打开 dialog → 旋转 | 弹窗保留，选项保留 |
| 13 | 国际化 | 切换英文 | 4 个字符串全英文 |
| 14 | 灰显去除 | 三点菜单 → 看"创建待办副本"项 | **不**再有 0.4 alpha 灰显 |

### 8.3 回归验证
- 批量复制（多选模式）→ 仍走 `batchDuplicate`，不应受影响
- 顶部三点菜单其他项（隐藏详情 / 隐藏已完成 / 待办排序 / 回收站）→ 全部正常
- 灵感页 / 日期页 → 各自的"创建副本"占位项**保持不变**（不误改）

## 9. 风险与回退

| 风险 | 缓解 |
| --- | --- |
| 复制大量 todo 时阻塞 UI | 已有顶层 try/catch + 阶段 2 异步文件复制 |
| 复制失败 Snackbar 抢显示 | HomeScreen.kt:591-594 已有"showLevelUp/achievement 抢占"保护 |
| `DuplicateRange` 命名冲突 | 包内 enum，无外部依赖 |
| `setShowDuplicateDialog` 命名冲突 | 同上 |

## 10. 后续优化点

1. **进度反馈**：复制 N 个 todo 时弹窗内显示进度条
2. **复制总数预览**：弹窗打开时显示"将复制 N 个待办"
3. **范围记忆**：把用户上次选择的 range 持久化到 `CorgiPreferences`
4. **复制后跳转**：完成后自动滚动到新副本
5. **统一 dialog 模式**：抽 `DialogController<T>` 通用组件
6. **SpecialDate / Inspire 复用**：`SpecialDateMenuDropdown` 也有"创建日期副本"占位，可直接套用本任务的 `DuplicateRange` 模式

## 11. 关键文件位置

| 用途 | 绝对路径 |
| --- | --- |
| ViewModel 改造 | `c:\Users\EDY\Desktop\CorgiMemo\app\src\main\java\com\corgimemo\app\viewmodel\HomeViewModel.kt` |
| Dialog 新增 | `c:\Users\EDY\Desktop\CorgiMemo\app\src\main\java\com\corgimemo\app\ui\components\DuplicateTodoDialog.kt` |
| Dropdown 改造 | `c:\Users\EDY\Desktop\CorgiMemo\app\src\main\java\com\corgimemo\app\ui\components\TodoMenuDropdown.kt` |
| MainScreen 改造 | `c:\Users\EDY\Desktop\CorgiMemo\app\src\main\java\com\corgimemo\app\ui\screens\main\MainScreen.kt` |
| HomeScreen 改造 | `c:\Users\EDY\Desktop\CorgiMemo\app\src\main\java\com\corgimemo\app\ui\screens\home\HomeScreen.kt` |
| strings.xml | `c:\Users\EDY\Desktop\CorgiMemo\app\src\main\res\values\strings.xml` |
| strings-en.xml | `c:\Users\EDY\Desktop\CorgiMemo\app\src\main\res\values-en\strings.xml` |
| UI 规范 | `c:\Users\EDY\Desktop\CorgiMemo\docs\superpowers\specs\UI设计规范.md` |

## 12. 实施流程

1. 实施 5 个文件改动
2. **询问用户**是否编译（按 `编译验证.md`）
3. 用户编译 + 手动测试 14 个场景
4. **询问用户**是否 git commit（按 `git提交.md`）
5. 提交信息中文，包含：feat(duplicate-todo) + 实现创建副本 + 删除占位 + 重命名回调
