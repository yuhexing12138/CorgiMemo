# 2026-09-17

## BlockNote 撤销/重做按钮收归宿主顶栏（v1.7）

**需求**：灵感编辑页删除 BlockNote 内部的「↺ 撤销 / ↻ 重做」按钮，桥接到页面左上角画红圈的撤销栈图标。

**关键发现**：截图里那对胶囊按钮**不是 BlockNote 官方 UI**，而是 `blocknote-probe/src/editor/EditorApp.tsx` 里自绘的
`AutoRepeatButton`（位于 `.editor-toolbar` 容器，调 `editor.undo()/redo()`）。官方从未内置这对按钮，
`BlockNoteView` 也没有对应关闭配置——**只能删代码**。
而宿主顶栏那对 ↶/↷ 图标按钮**早已桥接完成**（`blockNoteController.undo()` → JS `editor.undo()`），
所以核心工作其实只是「删 JS 侧自绘按钮 + 补可用态上行」。

### 撤销/重做的正确判定 API（重要）

BlockNote **公开** `editor.can(editor.undo)` / `editor.can(editor.redo)` 返回 boolean。
原理（读 `node_modules/@blocknote/core/src/editor/managers/StateManager.ts`）：
`can(cb)` 置 `isInCan=true` 调 cb，`exec()` 见 `isInCan` 走 `canExec()` 分支——**只判定不 dispatch**，
绝不污染历史栈。
> ⚠️ 不要走 `editor._tiptapEditor.can().undo()`（私有字段、跨版本易碎），也别
> `editor.on("transaction", ...)`——`BlockNoteEditor extends EventEmitter<{ create: void }>`，
> **只有 `create` 一个事件**，没有 transaction 事件（`EventEmitter.ts` 的 `emit` 是同步的）。

`editor.on("update", cb)` 存在但不覆盖全部路径，故本方案用「onChange + undo/redo 后主动推」覆盖。

### 改动清单

| 文件 | 改动 |
|---|---|
| `blocknote-probe/src/editor/EditorApp.tsx` | 删 `AutoRepeatButton` 组件 + `.editor-toolbar` JSX；新增 `pushUndoState()`（`editor.can()` 判定 + 变化去重）并在 onChange/undo/redo 后调用；`EditorCore` 新增 `onUndoStateChange` prop |
| `blocknote-probe/src/editor/editor.css` | 删 `.editor-toolbar` / `.editor-toolbar button` 样式 |
| `blocknote-probe/src/editor/bridge.ts` | `UpMessage` 新增 `{ type: "undoState"; canUndo; canRedo }`；更新单向数据流注释 |
| `BlockNoteEditorWebView.kt` | controller 新增 `var canUndo/canRedo by mutableStateOf(false) private set`（快照态，宿主可直接读）；`handleUpMessage` 新增 `undoState` 分支（`mainHandler.post` 改快照，主线程安全）；补 import `mutableStateOf`/`getValue`/`setValue` |
| `InspirationEditScreen.kt` | 顶栏 blocknote 分支按钮接 `noteCanUndo/noteCanRedo` 置灰（对齐 Compose 分支语义），更新注释 |
| `BlockNoteEditorScreen.kt` | 探针页独立 inline 处理器补 `undoState` 分支（仅打 log；该页无顶栏按钮） |
| `docs/bridge-protocol.md` | 上行表补 `undoState` + 时序图 + v1.7 版本记录 |
| `docs/BlockNote迁移实施计划.md`、`BlockNote迁移验收清单.md`、`BlockNote加号菜单与工具栏桥接对照.md` | S12 条目与验收项改写（删除「长按连发」，新增置灰验收） |

### 长按连发已放弃

原 JS `AutoRepeatButton` 有按住连退（450ms 后每 150ms），**本次未在 Compose 侧复刻**（用户选「仅删按钮+同步 canUndo 状态」）。
顶栏 `IconButton` 是即点即走。若日后要恢复，需 `pointerInput` + `awaitPointerEventScope` 自建长按循环。

### 遗留：构建产物

**JS 源码改完必须重新构建**：`cd blocknote-probe && npm run build:editor`
（`vite.editor.config.ts` 输出到 `../app/src/main/assets/blocknote-web/editor`，`viteSingleFile` 内联成单个 1.8MB `editor.html`）。
未构建时真机仍加载旧 bundle，JS 侧按钮依旧显示。

---

## P1.5 浮层格式工具栏 11 键重桥（响应用户「图标未变化、竖线还在」）

### 根因
- 上一轮只产出了诊断与计划，`RichTextFormatToolbar.kt` 从未被真正修改：组一/组二仍由 `ToolbarDivider()` 分隔，B/I/U/S（Material FormatItalic/Underlined/Strikethrough）、对齐×3（Material FormatAlign×）、嵌套±（Material FormatIndent±）、链接（Icons.Default.Link）全非 Ri 同款。
- `rebuild-toolbar.cjs` 的 NEW_CONTENT 是旧布局的固化来源，任何重跑都会回滚手工修改 → 已在脚本头部加 **FORCE_REBUILD 停用守卫**，默认运行直接 exit(1)。

### 修复内容
1. **图标补提取**：`extract-ri-icons.cjs` WANT 追加 align_left/center/right（RiAlignLeft/Center/Right）+ nest/unnest（RiIndentIncrease/RiIndentDecrease）。
   - 坑：`blocknote-probe/node_modules` 里 react-icons 已消失（上次是 --no-save 装的，重装依赖后被清）。从 `@blocknote/react` dist 的 sourcemap 路径确认版本为 **react-icons@5.6.0**，`npm i react-icons@5.6.0 --no-save` 后重跑（耗时 5 分钟）。
   - 重新生成 `BlockNotePlusMenuIcons.kt`：**30 个唯一图标**（21 → 新增 5 + 复用 4），全量校验 10 个浮层键 OK、花括号配平。
2. **第一分类区重桥**（按用户指令「先彻底删除这 11 个按钮再重建」）：合并旧组一+组二为单一 `FormatButtonGroup`，区内无分割线；顺序严格 T、Aa、B、I、U、S、对齐×3、Color(A)、Nest、UnNest、Link。
   - B：BlockNote 单档模式渲染 `RiBold`（浮层 BasicTextStyleButton 同款），点击直接 `onSetFontWeight(700)`；Compose 模式保留 B1/B2/B3 展开分支。
   - I/U/S/对齐×3/Nest/UnNest/Link 全部 `RiFormatButton`；Color 保持 `FormatTextButton("A")`（浮层 ColorStyleButton 本就是 A 字母样式）。
   - T/Lucide.Type、Aa/Lucide.CaseSensitive 按要求暂不动。
3. 清理 13 条失效 Material icon import + HorizontalDivider import；KDoc 分组说明同步更新。

### 自检与验证
- grep 确认无 FormatItalic/FormatAlign/FormatIndent/Icons.Default.Link 残留（仅注释中提及 HorizontalDivider 历史）。
- ToolbarDivider 调用 6 处（7 组之间），函数定义 1 处，无多余分隔。
- `:app:compileDebugKotlin` **BUILD SUCCESSFUL（1m26s，零错误）**。

### 经验
- 「图标未变化」类反馈先读文件现状再动手——上一轮的诊断计划没有落地为 Edit。
- react-icons --no-save 安装在 node_modules 重装后会丢失；extract 脚本依赖的源文件路径应连同版本号（5.6.0）写进脚本注释。

### 验收反馈 1：部分按钮显示文字（17:35 截图）
- 现象：U/S/🔗 为图标，左对/居中/右对/增加/减少为文字（RiFormatButton 的 vector==null 降级）。
- 诊断：用户 APK 构建于修复中途——toolbar.kt 已改、BlockNotePlusMenuIcons.kt 重生成（17:29-30）前触发构建 → 新布局 + 旧 21 图标集（恰好缺新增 5 枚）。
- 处置：无需改码；当前树已含 30 枚完整 defs（逐枚验证 path 数据非空，已提交 6596f69e），请用户重新构建即可。教训：多文件联动的改动中途用户可能触发构建，长步骤（npm 装包 5 分钟）期间应提示用户暂缓构建。
