# 2026-08-31

## 清理项目内一类已弃用工具的残留信息
- **根因**：用户以为另一台电脑已全删某弃用工具并推云端，但 `origin/master` 实际仍含 3 份历史/弃用文档的引用（AppDrawer 拆分计划、Kuikly 改动前检查清单、弃用文件对应表）；且本地有 git 忽略/未追踪的该工具残留。
- **处理**：编辑 3 份文档移除引用 → 提交 `564e19b8` → 推送 `origin/master`（云端现已无相关追踪引用，已用 `git grep` 校验）。本地 `git clean` 清掉相关输出目录产物；虚拟环境中该工具的包/dist-info/pth/finder 与 CLI 可执行文件等已随回收站移除。
- **残留（被本机 safe-delete/genie-trash 删除守卫拦截，自动化删不掉）**：输出目录内的 watch 日志（守护进程已停止，日志为陈旧产物）、虚拟环境 Scripts 下的 CLI 可执行文件（被陈旧句柄占用）。须由用户在 Windows 文件资源管理器原生删除，或重启释放句柄后删除。
- **环境教训**：WorkBuddy 的 safe-delete 守卫会把删除重定向到 `genie-trash` 回收站，回收站操作失败时拒绝硬删除（fail-closed），导致 `rm`/`rmdir`/Python `os.remove` 均失败；此类文件须用本机文件管理器（非 WorkBuddy 命令）删除。
- **遗留项**：服务端用户配置仍可能注入旧的工具相关指令（非仓库问题，需在平台用户设置中清理）。
- **最终验证（10:54）**：用户手动删除两处本地残留后，四层验证全部通过——① 命名残留（含忽略项）无；② git 追踪文件引用无；③ 工作区所有文件**内容文本**无；④ 云端 `origin/master` 引用无。本机工作区已 100% 无该工具残留。git 历史对象库中仍可能含旧字样（如清理提交 `564e19b8` 的提交信息及更早历史），但属 `.git` 对象库、不出现在工作区，改写历史风险大故不动。

## 灵感编辑页内联图片：现状分析 + 编辑态点击预览修复
- **问号方块根因**：编辑态 `BasicRichTextEditor` 基于 `BasicTextField`，Compose Foundation 1.11（BOM 2026.04.01）的 `BasicTextField` 无 `inlineContent` 参数，`RichSpanStyle.Image` 的占位符 U+FFFD 被当普通字符画出；只读态 `BasicRichText`（`BasicText`）才支持 inlineContent。
- **覆盖层方案已在库内**（compose-rich-editor 子模块，`InlineImageOverlay.kt` + `RichSpanStyle` 占位符染色透明 + `RichTextState.withImageBlockLineHeight` 段落行高预留）：组合期 `resolveInlineImagePlacements` 解析 painter/尺寸 → `ApplyInlineImageSizes` 写回尺寸重建 annotatedString → `drawInlineImages` 按行绘制。已静态核查链路自洽（含 `Painter.draw(size)` API 存在性、Coil 3.5 RememberObserver 自动执行请求）。用户尚未编译验证。
- **子模块指针隐患**：主仓库仍指向已丢失的提交 `c1d49bf9`（子模块历史重写"重置到 v1.0.0 并重新应用"所致），工作区在 `a2d4a73`，主仓库有未提交的 `M compose-rich-editor`，需重新提交指针。
- **本次新增：编辑态点击内联图片预览**。图片是 `RichSpanStyle.Image`（非 token），原 `mediaTokenClickHandler` 的 "image" 分支成死代码（保留用于兼容旧数据 trigger:image token）。新增：
  - 库：`TokenInteractionHandlers.kt` 新增 `ImageClickHandler` fun interface + `LocalImageClickHandler`；`InlineImageOverlay.kt` 新增 `findInlineImagePlacementAt`（与绘制同口径的行级命中检测）+ `Modifier.pointerInputInlineImages`（Initial pass 抢占消费，阻断光标定位）；`BasicRichTextEditor` 两个重载与 material3 `RichTextEditor` 新增 `onImageClick: ImageClickHandler? = LocalImageClickHandler.current` 参数并挂载手势。
  - App：`InspirationEditScreen` 的 `CompositionLocalProvider` 注入 `LocalImageClickHandler`，点击图片置 `inlineImageViewerPath` 打开既有全屏预览 Dialog。
- **语音现状**：语音是 `trigger:voice` 的 🎤00:12 atomic token（点击经 LocalTokenClickHandler 播放），旧的独立语音条 `VoicePlayerComponent` 在 v2026-08-30 内联方案下不再渲染；保存路径 `saveInlineMediaBlocks` 从 markdown 反解析重建 content_blocks 表。

## 记忆整合（15:42 起）
- 将 `CorgiMemo\.workbuddy\memory\` 下散落的每日日志整合进结构化记忆，并按"用户级+工作级分流"导入 WorkBuddy：
  - **用户级** `~\.workbuddy\MEMORY.md`：新增跨项目通用经验——Git 提交写保护/safe-delete 守卫、Kotlin `minOf/maxOf` import 坑、Compose 命中测试/animateContentSize 裁剪/分支内 remember/去水波纹/onSizeChanged 时机、HTML 原型实跑验证、Ardot 设计稿要点、子模块指针隐患。
  - **工作级** `CorgiMemo\.workbuddy\memory\MEMORY.md`：重写为项目专属知识——项目约定、SwipeableImageStack 可见深度/扇形/收起按钮半胶囊吸附时间线竖线、编辑态内联图片与语音现状、已弃用工具清理结论、资源位置索引。
- 清理已整合的旧日志：`2026-07-24.md`、`2026-08-25.md`、`2026-08-26.md`、`2026-08-28.md` 已删除；保留 `2026-08-31.md`（当日记录）。

## 灵感编辑页三个 bug 修复（16:00 起）
- **Bug ① 图片相互重叠（v1 修复：toAddRichSpanStyle 污染）**：根因是 `RichTextState.toAddRichSpanStyle` 全局态——连续插入图片时上一张图的 Image style 污染下一段的前导 `\n`，两张图落到同一段 lineHeight 取 max → 绘制位置相同。修复 `insertBlockImage`：先 reset toAdd 为 Default → addText("\n") 段尾分隔 → addRichSpan(Image) → addText("\n")。同时给 Image 初始宽高 200.sp × 200.sp（替代原来的 0.sp），避免插入瞬间段高退化成"一行文字"导致图片压在后续内容上。
- **Bug ① 第二轮根因（v2 修复：raw text 字符错位）**：`addText("\n")` 让 Image span 的 text 字段 = "\n"，但 `Image.appendCustomContent` 不 append(richSpan.text)，渲染时只输出 inline-content placeholder（U+FFFD）。raw text 字符 "\n" 在 annotatedString 中被替换为 placeholder，**layout 不会把它当换行处理**——连续两张图的 lineTop 重叠。markdown parser 解析 `![](path)` 时把 image span 的 text 设为 "\uFFFD"（与 placeholder 字符值一致），offset 1-to-1 映射正确。**最终修复**：`insertBlockImage` 改用 `richTextState.insertMarkdownAfterSelection("\n\n![](path)\n\n")` 走 markdown 解析路径，前后各 2 个 `\n` 形成独立段，image span 独占段。
- **Bug ② 编辑态点图片预览不是「图片附件」全屏模式**：原实现是 `Dialog + Box + InlineImagePreview` 单图预览，缺失缩放/左右切换/删除/下载。改为复用现有 `InspirationImageGallery`（沉浸式 + HorizontalPager + ZoomableImage + 下载 + 删除）。图片路径列表从 `richTextState.toMarkdown()` 用 `!\[[^\]]*\]\(([^)]+)\)` 正则扫描（即 `saveInlineMediaBlocks` 的同款 Markdown 图片语法）；删除按钮走 `setMarkdown(去掉对应行)` 路径重建 RichTextState。
- **Bug ③ 插入语音后正文无 🎤 标识**：根因 `addRichSpan(RichSpanStyle.Token(...))` 只对"已有选区"生效，collapsed selection（光标闪烁）下什么也不做 → 录音结束后正文里没有任何标记。改为用库的公共 `insertMarkdownAfterSelection("[🎤mm:ss](trigger:voice:path|duration)")`，走 setMarkdown 路径，库自动从伪链接语法解析为 RichSpan(text=label, richSpanStyle=Token)，label 作为 raw text 进入正文。
- 配套：注册 `voice` trigger（用于 Token 渲染颜色，char 用 `/`，因为 voice 不通过输入字符触发，token 只在录音成后整体插入）；删除 `v2026-08-01 Phase 4 回退` 的过时注释；清理 `import androidx.compose.ui.window.Dialog`。
- **关键经验**：库内 inline content 在 raw text 中必须占 1 个 U+FFFD 占位符字符（与 rendered placeholder 字符值一致）才能让 offset 1-to-1 映射、layout 行为正确。用 `addText("\n")` 创建 image span 会让 raw text 字符 = "\n" 但 rendered = U+FFFD，layout 不会按换行处理。**所有 image 插入都应走 markdown parser 路径**，不要直接 `addText + addRichSpan(Image)`。
- 文件：`CorgiMemo\app\src\main\java\com\corgimemo\app\ui\screens\inspiration\InspirationEditScreen.kt`

## 优化建议落实（16:40 起，4 项）
1. **voice token id 唯一化（第 2 项）**：onSaved 与迁移逻辑的 token id 追加毫秒时间戳 `$path|$duration|$ts`，避免同一路径/时长下 markdown 解析把相邻同 style token 合并（删一误删二）。`saveInlineMediaBlocks` 的解析由 `split("|", limit = 2)` 改为 `split("|")`（只取前两段，忽略时间戳；兼容旧数据）。点击播放的 `substringBefore("|")` 不受影响（path 在前）。同时修复了旧迁移逻辑用 `addRichSpan(Token)` 在 collapsed selection 下不生效的隐患（改走 insertMarkdownAfterSelection）。
2. **库新增 `insertImage(model, contentDescription?)`（第 3 项）**：内部走 `insertMarkdownAfterSelection("\n\n![alt](model)\n\n")`，确保 image span text = U+FFFD 与 placeholder 对齐。`RichTextState.insertBlockImage` 改为调用它。**API 注意**：public API 变更需同步更新 3 个 api dump 文件（android/desktop/klib），否则 binary-compatibility-validator 的 apiCheck 会失败。
3. **库新增 `removeImage(model): Boolean`（第 4 项）**：按 model 找到 Image span 的 textRange，用 `removeTextRange` 局部删除图片占位符（独占行时吞并前后各一个 \n），替代 Project 层"toMarkdown + 正则替换 + setMarkdown 全量重建"——不丢光标、不重建整篇。Project 层删除图片逻辑已改用此方法。
4. **删除 ReorderableColumn 内 Image/Voice 死渲染分支（第 5 项）**：contentBlocks 初始化即清空、运行时只含 Text 块，ReorderableColumn 的 items（filter 掉 Text）恒空，两个分支永不执行。保留 when 的 Text 空分支占位 + onReorder/baseModifier 骨架（highlightedIndex 两步删除仍被 onPreviewKeyEvent 引用）。第 569-570 行的**迁移逻辑**（loadContentBlocks → insertBlockImage）保留，不能删。
- 文件：InspirationEditScreen.kt、InspirationEditViewModel.kt、compose-rich-editor 子模块 RichTextState.kt + 3 个 api dump

## 图片插入第二轮根因修复（17:00 起，用户实测仍重叠+多图只插一张+单图偶发失败）
- **多图只插入一张 / 插到开头**：`insertParagraphs` 依赖 `getRichSpanByTextIndex(position-1)` 定位锚点 span，但段落间的**连接空格（updateRichParagraphList 里 append(' ')）不属于任何 RichSpan**。连续插入时，第一次插入后光标停在段尾空格上，`getRichSpanByTextIndex` 在该位置返回 null → fallback 指向第一段 → 第二张图插到文档开头（或静默失败）。
- **空文档插入失败**：`getFirstNonEmptyChild` 对无 children 的空段返回 null → `insertParagraphs` 的 `?: return` 静默退出。
- **重叠真正根因（v2）**：`updateRichParagraphList` 构建 annotatedString 时**没有应用** `withImageBlockLineHeight`（只有 `updateAnnotatedString` 用了）。图片段落插入瞬间 lineHeight 不撑高 → 覆盖层把 200sp 高的图画在默认行高的段落上 → 画出段外压住后续内容 → 重叠；painter 异步解析后 setResolvedSize → updateAnnotatedString 才撑高，窗口期闪烁/重叠。
- **修复（库 RichTextState.kt + InlineImageOverlay.kt）**：
  1. `insertImage` 彻底重写为 `performInsertImage`：直接构造图片段落（RichParagraph + RichSpan.Image，text=U+FFFD，宽高 200sp）插入 richParagraphList，绕开 markdown parser 多段插入。新增**段落级定位**（findSpanInParagraph/richSpanTextLength/paragraphTextLength）：遍历段落累计 raw text 偏移（含段落间空格），精确找光标所在段落与 span，解决空格位置定位错误。`recordHistory(Structural)` 包裹支持 undo。
  2. `updateRichParagraphList` 的段落构建也应用 `withImageBlockLineHeight`（与 updateAnnotatedString 一致），图片段落插入瞬间即撑高行高，消除重叠窗口。
  3. 覆盖层 `drawInlineImages` 加固：按 textRange.start 排序，**同一 line 多图垂直错开**（cursorY 累计），兜底防重叠；`findInlineImagePlacementAt` 命中检测同步同一套 y 计算（否则点击错位）。
  4. `removeImage` 吞并前后连接空格（图片独占段时 raw text 前后是 ' ' 而非 \n）。
- **关键经验升级**：库段落间连接是空格（非 \n），**用 getRichSpanByTextIndex 定位光标段落不可靠**（空格位置返回 null），应段落级遍历定位；`updateRichParagraphList` 与 `updateAnnotatedString` 必须都应用图片段落 lineHeight 预留，否则插入瞬间重叠。
- 文件：compose-rich-editor 子模块 RichTextState.kt、InlineImageOverlay.kt（App 层 InsertBlockImage 无需改动，已调 insertImage）

## insertParagraphs 空文档 bug 修复（15:49 提交）
- **问题**：`insertParagraphs` 仍用 `getRichSpanByTextIndex(position - 1) ?: return`，insertImage 虽已绕开，但其他调用方（粘贴多段 markdown 到空文档）仍触发同类 bug：position==0 时传 -1 返回 null → 静默退出；光标落在段落间空格同样失败/错位。
- **修复**：复用 performInsertImage 的段落级定位——遍历 richParagraphList 累计 raw text 偏移（含段落间空格），命中条件 `position >= start && position <= cursor`（空格位置命中前一段末尾），再用 findSpanInParagraph 找锚点 span；三重兜底（findSpanInParagraph 未命中→段落最后 child→构造空 RichSpan→段落遍历未命中退回原逻辑）保证永不静默失败。
- **安全性验证**：RichSpan.textRange 默认 TextRange(0,0)，空文档 sliceIndex = max(0,0) = 0，RichParagraph.slice 内部 coerceIn 兜底；光标在段落间空格命中前一段末尾（语义合理）；普通中间/末尾粘贴与原实现语义一致（回归安全）。
- **顺手清理**：withIndex() 未使用的 i 变量移除。
- **提交**：子模块 `9e388c0` + 主仓库指针 `861fabae`（两步提交）。仅子模块 RichTextState.kt 一个文件（+44/-5），无新增 import。

## 图片重叠终极根因：段落行高被 textStyle 锁死（17:27 截图，修复后提交）
- **现象**：多图全部插入成功（定位已修好），但每张图只露出约一行文字高的条带，相互覆盖；黑图/最后一张完整。
- **排查路径**：库内 withImageBlockLineHeight 两条构建路径都在 → 悬疑在"annotatedString 段落间是空格（无 \n），多段落怎么换行"。**查 Compose ui-text 1.11.0 源码（gradle 缓存 sources jar）确认**：`AnnotatedString.normalizedParagraphStyles` 按 **ParagraphStyle range 边界**切分排版段落（每个 range 独立 AndroidParagraph 实例，不依赖 \n）——这解释了本库无 \n 也能多段落显示；**同时发现关键一行：`it.copy(defaultParagraphStyle.merge(it.item))`——default（BasicTextField 的 textStyle）的已指定字段优先于 range**。
- **根因**：App 层 `textStyle = bodyLarge` 自带 lineHeight=24sp（已指定）→ default 覆盖图片段落 range 里预置的 400sp 行高 → 图片段落行高被锁死为一行正文 → 覆盖层按占位符行 lineTop 画大图 → 相邻图片相互覆盖。此前的 withImageBlockLineHeight 修复方向对但**通路被 Compose 的 merge 方向封死**。
- **修复**（库内，公共 API 无变化）：① BasicRichTextEditor 传给 BasicTextField 的 textStyle 剥掉 lineHeight（置 Unspecified）；原始行高经 SideEffect 注入新增的 `state.editorLineHeight`（internal），变化时 updateAnnotatedString 重建（isSpecified 判断，防 Unspecified.value=NaN 造成 != 恒真 → 无限重建循环）。② withImageBlockLineHeight 重写为三候选取 max（段落自身行高/editorLineHeight/图片高度）——普通段落行高视觉不变，图片段落撑到图片高度；顺带修掉 NaN 比较隐患。
- **验证**：只读态 BasicRichText 走 inlineContent（placeholder 高度参与行高）不受此 bug 影响，无需处理；material3 RichTextEditor 的 mergedTextStyle 原样传递，在实现处统一剥离；标题编辑器（headlineMedium 32sp）同理注入，视觉不变。
- **提交**：子模块 `f123f38` + 主仓库指针 `e768d3af`。
- **方法论沉淀**：Compose 的 ParagraphStyle range 即排版段落边界（不需要 \n）；default textStyle 永远覆盖 range 的同名已指定字段——任何"往段落 range 里塞样式"的方案都会被编辑器 textStyle 压制，必须剥 default 或改从 range 注入全量值。
- **编译补丁（17:58）**：上一提交漏 import 导致 5 处 Unresolved reference——`TextUnit.isSpecified/isUnspecified` 是 `androidx.compose.ui.unit` 包**顶层扩展属性**（非成员），必须显式 import；`TextUnit.Unspecified.value` 是 NaN，判断未指定不能用 value 比较。已补 2 文件 import，子模块 `a594eb5` + 主仓库 `fef00122`。教训记入用户级 MEMORY.md（Compose 经验区）。
