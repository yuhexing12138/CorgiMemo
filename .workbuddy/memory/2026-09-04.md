# 2026-09-04 工作日志

## 字体预览改为「分离式预览」（编辑页 + 设置页）
- 需求：① 编辑页取消「点选即预览」的正文位图复刻，改为点字体区右上角「完成」才应用（字重按钮同步变化），且一次最多只同时加载两种字体（中文 1 + 英文/数字 1）；② 设置页加「确认」按钮，点确认后系统字体才切换，字体行/按钮上的字形仍用位图实时预览。
- 用户决策（AskUserQuestion）：确认按钮放在两个分组标题行右侧各一个；未点确定直接返回 = 丢弃选择；内存只做软约束（不重建 FontFamilyResolver）。
- 改动（4 文件）：
  - `ui/theme/FontPreviewEngine.kt`：单池(3) 拆为 `previewTypefacePool`(2) + `probeTypefacePool`(3)；新增 `clearPreviewTypefaces/clearProbeTypefaces`，`clearTypefaces` 清两者；位图缓存 16→32；删除 `contentPreviewAsync/contentPreviewBitmap` 及 StaticLayout/TextPaint 相关 import。
  - `screens/inspiration/InspirationEditScreen.kt`：删除 `fontPreviewBitmap`/`bodyPreviewWidthPx` 状态、正文位图覆盖层与 `onSizeChanged` 测宽；「完成」回调改为一次性应用 + `clearTypefaces()`；清理 6 个因此失效的 import（含 `clickable`）。
  - `screens/inspiration/components/FontPickerPanel.kt`：仅文档更新（cell 仍是引擎位图预览，不变）。
  - `screens/settings/AppearanceScreen.kt`：删除 `DisposableEffect` 离页自动应用；新增 `hasPendingFontChange` + 局部函数 `confirmPendingFonts()`；`AppearanceSectionTitle` 增加 `action` 插槽；新增 `FontConfirmButton`（36dp 高、无改动时置灰）。
- 校验：未跑 gradle（用户要求不自动编译）；用括号平衡基线对比 + import 使用审计做静态自检，改动后与改动前基线一致，无新增未使用 import、无缺失 import。
- 提交：50166b1b（字体预览改为分离式预览）。

## 第二轮迭代（同日，应用不收起 + 硬约束 + 清 import）
- 需求：① 编辑页「完成」改成「应用但不收起」（有改动=应用并保持面板展开，无改动=收起）；② 上 FontFamilyResolver 硬约束；③ 清掉 AppearanceScreen 未使用的 `FontFamily` import。
- **关键查证（读 compose-ui-text 1.11.2 源码 + javap core-1.12.0）**：`createFontFamilyResolver` 各实例**共享全局缓存**（换实例不丢缓存，源码注释明示）；真丢弃 = `emptyCacheFontFamilyResolver`（@InternalTextApi，私有缓存）+ `androidx.core.graphics.TypefaceCompat.clearCache()`（**公开 API**，无需反射）。AndroidFontLoader 加载 ResourceFont 走 `ResourcesCompat.getFont` → TypefaceCompat 静态 LruCache(16)。
- 改动（6 文件）：
  - 新增 `ui/theme/FontResolverPolicy.kt`：`createIsolatedResolver()` = `TypefaceCompat.clearCache()` + `emptyCacheFontFamilyResolver`（@OptIn(InternalTextApi)+@SuppressLint("RestrictTo")，失败兜底回退公开 `createFontFamilyResolver`）。
  - `ui/theme/Theme.kt`：以四元字体组合为 key `remember` 创建隔离 resolver，`CompositionLocalProvider(LocalFontFamilyResolver provides …)` 包住整树。
  - `FontPickerPanel.kt`：新增 `hasPendingChange` 参数；面板头按钮文案「应用」（有改动）/「完成」（无改动）。
  - `InspirationEditBottomBar.kt`：透传 `hasPendingChange`。
  - `InspirationEditScreen.kt`：`hasPendingFontChange` 计算；`onFontPanelDismiss` 改为有改动→应用+清池+不收起，无改动→收起。
  - `AppearanceScreen.kt`：删未使用 `FontFamily` import。
- 已知取舍（已写入代码注释与 MEMORY）：隔离 resolver 缺系统「粗体文字」无障碍字重加成；切字体整树重解析（几十 ms）；兜底退化软约束。
- 校验：括号平衡基线 + import 审计全部通过（六个文件无新增未使用/缺失 import）。

## 第三轮：连续换字 ~25 款后 OOM 闪退（实测 meminfo 数据）
- 实测两份快照（mem_before/after.txt，UTF-16 编码需 iconv 转 UTF-8）：连切约 25 款字体后 TOTAL PSS 232975→462197（+229MB）。
- **关键结论**：增长几乎全在 **Dalvik(Java) Heap**（+203MB，Heap Size 71k→350k ×4.9，GC 收不回=真泄漏）；**Native Heap 仅 +8.8MB**（隔离 resolver + clearCache 的硬约束对 native 生效，常驻字体被压在 2 款内）。
- 代码排查：FontPreviewEngine（池 2/3 + 位图 LruCache 32，位图每张≈60KB）、FontWeightProbe.cache（无界 LinkedHashMap 但只存极小 Set<Int>）、应用流程（onCjkFontSelected→ContentFontManager.setCjkFont→clearTypefaces，无意外持有）——**全部有界，均非 200MB 主因**。
- 判定：200MB Java 持有链在 Compose 字体解析路径内部（最疑每次换字 new FontFamilyResolverImpl 的重建震荡 + 重组/异步边界未释放），纯读代码无法精确定位，需堆转储。
- 用户决策：选「抓堆转储精确定位」。新增 `scripts/capture_heap_on_leak.sh`（轮询 Dalvik PSS 超阈值 180000KB 自动 dumpheap+pull+存 meminfo 快照）。待用户连切字体后分享 heap_*.hprof 再分析持有链。
- 待办：拿到 hprof 后用 MAT/Android Studio 或 hprof 解析按 Retained Size 定位最大持有者（FontFamilyResolverImpl/FontFamily/Typeface/Paragraph/AnnotatedString/Bitmap）；若环境无法解析则引导用户在 Android Studio Profiler 3 步看泄漏。

## 第四轮：堆转储定位根因 + Option A 根治
- 用户交付 `heap.hprof`。先试官方 `hprof` PyPI 库（开代理后 SSL/404 均失败，PyPI 上无此包）→ 改为修复自写流式解析器 `scripts/hprof_histogram.py`。
- **解析器关键修复**：CLASS_DUMP 静态字段名是 **idSize 字节（非 u2）**，原代码 `i += 2` 每字段少读 2 字节、跨字段累积偏移直至读取长度爆表（曾读到 34 亿长度的 OBJECT_ARRAY）；修正为 `i += idSize` 后直方图可信。另加 INSTANCE/数组长度合理性兜底 + 段级 abandon/resync（替代激进「扫到任意 tag」再对齐）。配套 `hprof_diag.py`（佐证 0xff 是 ART 无体子记录）、`hprof_drift.py`（首处不可能 size 即退，定位漂移点）、`hprof_referrers.py`（≥3MB byte[] 持有者扫描，背景运行补充）。
- **根因（堆转储实证）**：`byte[]` 总量 ≈ 502MB（502258410 个、最大项均 13~18MB 成组出现，恰与 10 款 CJK + 3 款 Latin 字体文件尺寸吻合）；`Typeface` 仅 133 个（6384B，**非 Typeface 泄漏**）。泄漏来自 Compose 解析 `Font(resId)`（ResourceFont）路径：`AndroidFontLoader`→`TypefaceCompat.createFromResources`→`Typeface.createFromInputStream` **把整份字体文件读进 Java 堆 byte[]**；每个字体被 chrome resolver / content resolver / 框架字体缓存各留 1 份 → 常驻 3~4 份。既有 `FontResolverPolicy.clearCache()`（TypefaceCompat.clearCache + emptyCacheFontFamilyResolver）只清 Typeface 对象，**清不掉**文件 byte[]，故无效。
- **修复（用户选「文件路径加载(推荐)」= Option A）**：`ui/theme/FontCatalog.kt` 新增 `fileTypefaceCache`（LruCache<Int,Typeface>(64)）+ `FilePathTypefaceLoader`（`AndroidFont.TypefaceLoader`，首次按 resId 拷到 cacheDir 后 `Typeface.Builder(path).build()` native mmap，**不占 Java 堆**）+ `filePathFont(resId,weight)`；**全部 13 款字体** `family` 构造与 `fonts` getter 由 `Font(R.font.x)` 改为 `filePathFont(R.font.x)`。Theme.kt 的 `combinedFamily`、FontManager/ContentFontManager 全链自动继承该修复。与 FontPreviewEngine 预览路径（同为 mmap）一致。
- 清理：删除 `val fonts` 上方重复的旧 doc 注释（规则 #6）。import 审计：`LruCache`/`AndroidFont`/`CompletableDeferred`/`Deferred`/`File` 均被实际使用，无缺失。
- 校验：未跑 gradle（用户要求不自动编译）；静态括号平衡 + import 使用审计通过。
- 待办：规则 #9 询问用户是否 git 提交（中文提交信息）；FontResolverPolicy 内 `TypefaceCompat.clearCache()` 已无必要但无害，可保留作防御。

## 第四轮补充：编译错误修复（AndroidFont API 纠偏）
- 提交 9206ac4f 后编译报错：`AndroidFont` 实际为 **abstract class**，不能直接 `AndroidFont(weight, loader, name)` 实例化；`TypefaceLoader` 接口方法实为 `loadBlocking(context, font: AndroidFont): Typeface?` + `suspend awaitLoad(context, font: AndroidFont): Typeface?`（非 `loadBlocked`）；主构造签名 `(loadingStrategy, typefaceLoader, variationSettings)`，第 1 参是 `FontLoadingStrategy`，无 `name`、无 `weight`。
- **查证方式**：从 gradle 缓存 sources jar 抽出真实源码 `ui-text-android-1.11.2-sources.jar` → `AndroidFont.android.kt` / `AndroidPreloadedFont.android.kt` / `Font.kt`，确认 `AndroidFileFont`（`Font(file: File,…)`）亦走 `Typeface.Builder(file)` mmap、不读 byte[]（与 FontPreviewEngine 同源，安全）。
- **修复**：新增私有子类 `FilePathAndroidFont(resId, weight) : AndroidFont(loadingStrategy=Blocking, typefaceLoader=FilePathTypefaceLoader(resId), variationSettings=FontVariation.Settings(weight, Normal))`，并 override `weight`/`style`（来自 Font 接口）；`FilePathTypefaceLoader` 改用 `loadBlocking`/`awaitLoad(context, AndroidFont)`；`filePathFont` 返回 `FilePathAndroidFont(resId, weight)`。移除误用的 `CompletableDeferred`/`Deferred`/`AndroidFont(weight,loader,name)`，新增 `FontLoadingStrategy`/`FontStyle`/`FontVariation` import。
- 未跑 gradle（用户要求不自动编译），已对拍真实源码签名，预期可编译；待用户重跑构建确认。

## 第四轮收尾（用户把优化建议列为落地清单 1-4）
- 用户把上轮 4 条优化建议列为待办清单，逐条落地：
  1. **清理一次性调试残留**：`.gitignore` 新增 OOM 排查产物忽略规则（heap*.hprof / heap_*.txt / mem_*.txt / drift_out.txt / referrers_*.txt / scripts/hprof_*.py / scripts/capture_heap_on_leak.sh / .workbuddy/tmp_androidfont/）。工作区 `heap.hprof`、`heap_std.hprof`（数百 MB）及全部 hprof 解析脚本/快照已 `rm` 移出；小体积残留已被 .gitignore 兜底防误提交。`app崩溃信息收集/` 为用户原有目录，非本次产物，不动。
  2. **cacheDir 字体副本版本管理**：`FontCatalog.kt` 新增 `fontCacheVersion`（首次从 `PackageManager.getPackageInfo().versionName` 读取并缓存，因本项目 BuildConfig 不生成）；缓存文件名改为 `ff_font_<resId>_v<versionName>.ttf`，资源/版本更新后旧副本自动失效（避免 stale 字体）。`fileTypefaceCache`(LruCache<Int,Typeface>(64)) 仍按 resId 复用，进程内版本恒定不受影响。
  3. **查 gradle 缓存源码核 API → 固化为技能**：新建用户级技能 `~/.workbuddy/skills/gradle-cache-source-lookup/`（`SKILL.md` + `scripts/extract_source.py` + `references/gradle_sources_lookup.md`），记录从 gradle 缓存 `*-sources.jar` 抽真实 .kt 源码核对 API 签名的工作流，并以 `AndroidFont`(Compose 1.11.2) 为案例。
  4. **Font(file: File, …) 内置等价路径**：核对确认内置 `AndroidFileFont` 同样走 `Typeface.Builder(file)` mmap、不读 byte[]（与 FontPreviewEngine 同源）。当前保留自定义 loader（首次拷贝 + LruCache 复用 + 版本后缀可控），列为未来可选减重项，本次不改可用代码。
- 改动文件：`app/src/main/java/com/corgimemo/app/ui/theme/FontCatalog.kt`、`.gitignore`；项目记忆补 `BuildConfig 不生成` 与技能引用约定。
- 待办：规则 #9 询问用户是否提交（新建提交，不 amend）。
- 补充：技能 `gradle-cache-source-lookup` 由用户级 `~/.workbuddy/skills/` 迁至项目级 `CorgiMemo/.workbuddy/skills/`（仅本仓库共享，随 .workbuddy 入库同步），用户级副本已删除；项目 MEMORY.md 引用路径同步更新。

## 第五轮：修复「中文字体选中并应用后不生效」bug（编辑页 + 设置页）
- **现象**：编辑页/设置页 选「英文/数字字体」应用→英文数字变了；再选「中文字体」应用→中文**不变**。两处都复现。
- **根因（查 compose-ui-text 1.11.2 源码 `FontListFontFamilyTypefaceAdapter.kt` + `FontMatcher.kt` 实证）**：旧 `combinedFamily = FontFamily(latin.fonts + cjk.fonts)` 把拉丁放前列。Compose 对每个 (字重,字型) 单元格只从合成族取**一个** Typeface——`FontMatcher.matchFont` 同字重(如都400)精确匹配返回两者，但 `firstImmediatelyAvailable` 取列表**首位(拉丁)**的 Typeface；缺失字形交给 Android **系统**兜底链，而非族内另一个 App 字体。于是同字重中文被拉丁字体覆盖→落到系统字体→永远用不上用户选的中文字体。与「英文变了、中文没变」完全吻合，且对每个共有字重都成立。
- **修复（用户选「字形回退链」方案）**：在 `FontCatalog.kt` 新增 `CombinedTypefaceLoader`(`Typeface.Builder(latin).addCustomFallback(cjk).build()` 串成单一**按字形回退**的 Typeface) + `CombinedAndroidFont` + `combinedFamilyFonts(cjk,latin)`（按字重配对、closestRes 就近选档）；`FontManager.combinedFamily` 改走 `FontFamily(FontCatalog.combinedFamilyFonts(cjk, latin))`（latin==null 仍走 `cjk.family`，行为不变）。`minSdk=26` 满足 `addCustomFallback` 要求。
- **覆盖范围**：编辑页内容(`ContentFontManager.currentFamily`)、设置页 chrome(`FontManager.combinedFamily`)、列表卡片(`InspirationCard/InspirationViewCard/TimelineInspirationItem` 经 `contentFontFamily`) 全部经 `combinedFamily`，一处改动全链路生效。预览/字重探测(`FontPreviewEngine`/`FontWeightProbe`)走单字体 `resByWeight`，不受影响。
- **校验**：未跑 gradle（用户要求不自动编译）；静态括号平衡 + import 审计通过（FontCatalog/FontManager 无需新增 import，同包 FontCatalog 直接引用）。待用户重跑构建确认；已清理分析用临时源码副本(.workbuddy/tmp_*.kt)。
- 待办：规则 #9 询问是否 git 提交（中文提交信息）。

## 第六轮：第五轮修复后仍「中文不生效」——真正根因是 setFallback 误用
- **现象（用户复测）**：第五轮用 `Typeface.Builder(latin).setFallback(cjk).build()` 合成后，拉丁生效、中文仍不变（编辑页 + 设置页两处）。
- **真正根因（javap 反编译 android-36 `Typeface.Builder` 实证）**：`setFallback` 在该 stub 里**只有 `setFallback(String familyName)` 一种重载**，参数必须是**系统已注册的字族名**（如 "sans-serif"），传文件路径会被当成家族名找不到→回退链失效→中文落系统字体。且 `setFallback(int)` 与 `addCustomFallback` 在该 stub 中**均不存在**（第五轮最初误用 `addCustomFallback` 报错、改 `setFallback` 也没真生效，同源）。`Typeface.create(FontFamily, FontStyle)` 也不存在于该 stub。
- **正确修复（字形级回退）**：改用 **`Typeface.CustomFallbackBuilder`(API 29+)**：`CustomFallbackBuilder(latinFamily).addCustomFallback(cjkFamily).setStyle(...).build()` 把拉丁作 base、中文作自定义兜底，单一 Typeface 内按字形回退（拉丁字形走拉丁、中文走中文）。构建 `FontFamily` 用 `android.graphics.fonts.Font.Builder(File).build()` → `FontFamily.Builder(font).build()`（与 Compose 的 `Font`/`FontFamily`/`FontStyle` 重名，已用 `GFont/GFontFamily/GFontStyle` 别名隔离）。
- **降级**：`Build.VERSION.SDK_INT >= Q` 守卫；API 26–28 无公开自定义回退 API，退化到单一字体（优先中文，`Typeface.createFromFile`）。守卫内引用 Q-only 类在 <29 不执行，不会触发类加载异常。
- **改动文件**：`FontCatalog.kt`（imports 加 `android.os.Build` + 三个 graphics 字体别名；`fileTypefaceCache` 改 `LruCache<Long,Typeface>`（合成 key 含字重，避免 Int 溢出），`FilePathTypefaceLoader` 同步 `.toLong()`；`CombinedTypefaceLoader` 加 `weight` 参数，逻辑改走 `buildCombinedTypeface`+`combinedKey`+`buildFontFamily`；`CombinedAndroidFont` 传 `weight.value`）；`FontManager.kt`（combinedFamily 注释同步为 CustomFallbackBuilder）。
- **校验**：未跑 gradle；已对拍 `Typeface`、`Typeface.CustomFallbackBuilder`、`android.graphics.fonts.Font(Family/Style)` 真实签名，import 齐全且无缺失/冲突；源码内已无 `setFallback`/`addCustomFallback` 实际调用（仅作「不可用」注释）。待用户重跑构建确认。

## 第七轮：单档 400 书法体在「粗体标题」下中文不渲染
- **现象（用户复测）**：选「英文/数字字体」→英文数字变；再选三个**单档 400 书法体**（马善政毛笔楷书/钟齐志莽行书/寒蝉·龙藏楷书）作为中文字体应用→**灵感编辑页标题的中文不变**（正文 400 正常）。设置页标题走同一合成路径，会同样复现。
- **根因**：三书法体是目录里仅有的单档(400)中文字体。标题用 `headlineMedium`=**Bold 700**；`combinedFamilyFonts` 对 700 请求经 `closestRes({400},700)` 取回 400 档文件，`buildCombinedTypeface` 把它**声明为 700 字重**挂进 `CustomFallbackBuilder` 中文回退族。单档字体在「请求字重≠真实字重」时，中文回退层被字重不匹配跳过 → 中文落系统字体；正文(400)因精确匹配正常，故只标题复现。字体文件均有效（TTF/OTF 数 MB），排除缺资源。
- **修复（集中改 `FontCatalog.kt` 一处，编辑页+设置页同链共享）**：
  1. `buildFontFamily`：回退族**同时挂两份**——「按请求字重声明的字体」(主匹配) + 「文件真实(自然)字重的字体」(兜底)，确保任意字重请求都命中中文回退；多档字体不受影响（精确档仍为主匹配项）。
  2. `buildCombinedTypeface`：`when` 优先级改为 拉丁+中文 > 仅中文 > 仅拉丁；末尾兜底「中文资源有效却双双构建失败→直接用中文文件 `Typeface.createFromFile`」，**绝不静默丢弃中文**（之前 latin 成功而 cjk 失败时中文会被丢）。
- **API 已 javap 核验**：`FontFamily.Builder(Font)` / `addFont(Font)` / `build()`、`CustomFallbackBuilder(FontFamily).addCustomFallback().setStyle().build()`、`Font.Builder.setWeight/setSlant/build()`（build 抛 IOException，已 runCatching 包裹）在 android-36 均存在。无新增 import。
- **校验**：未跑 gradle（用户要求不自动编译）；静态括号平衡 + import 审计通过。待用户真机验证（标题现应正确渲染书法体）。
- 待办：规则 #9 询问是否 git 提交（中文提交信息，新建提交不 amend）。

## 第八轮：第七轮修复编译验证后仍失败 → 修正根因假设
- **用户复验结果**：第七轮提交 `1a74efc7` 后真机复验，问题**依旧**（三个单档书法体在粗体标题下中文仍不渲染）。说明上轮根因判断有误。
- **复盘上轮为何无效**：第七轮 `buildFontFamily` 把 cjk 回退族做成 [主字体@请求字重(700) + 真实字重@400] 两份。在 700 请求下，Matcher 优先命中那份「声明 700」的主字体——而它正是把 400 档文件声明为 700 的坏声明，被跳过 → 仍落系统字体。即「再加一份真实字重」被同族里的坏声明盖过，无效。
- **新根因（确定性更强）**：`GFont.Builder(file).setWeight(请求字重)` 把 400 档书法体文件声明成 700 字重，单档字体在粗体标题(700) 请求下，CustomFallbackBuilder 按「声明字重≠文件真实字重」判定回退层不匹配而跳过（正文 400 因精确匹配正常）。
- **新修复（仍集中 FontCatalog.kt）**：`buildFontFamily` 改为**只按文件真实（自然）字重**构建（`GFontFamily.Builder(GFont.Builder(file).build()).build()`，去掉 `setWeight`），不再声明请求字重；单字体回退族天然覆盖所有 (字重,字型) 请求，任意字重（含 700）都命中中文回退；多档字体由文件自身多档保证精确匹配，不受影响。`buildCombinedTypeface` 的 `when` 优先级与末尾兜底保留（绝不静默丢弃中文）。
- **加诊断日志**（tag `FontCJK`，待真机复验通过后删除）：`buildCombinedTypeface` 打印 w/latin/cjk/latinOk/cjkOk；`CombinedTypefaceLoader.loadBlocking` 打印 key 与结果 ok/default。若复验仍失败，凭日志判断是否 cjk 族为 null（构建失败）还是 typeface 有 cjk 却未渲染（渲染/标题路径问题）。
- 校验：未跑 gradle（用户要求不自动编译）；静态括号平衡 + import 审计通过（新增 `android.util.Log` import，无缺失）。待用户重编复验。

## 第九轮：第八轮仍失败 → 真因是 closestRes 返回字重 key 而非 resId value
- **用户复验结果**：第八轮（自然字重 + FontCJK 日志）真机复验**仍失败**。关键日志行：
  `buildCombinedTypeface w=700 latin=2131820584 cjk=400 latinOk=true cjkOk=false`
  决定性证据：`cjk=400` 是**字重值**（真实 resId 形如 `2131820561`），说明 `closestRes` 传回了字重 key 而非 resId。
- **真因（本次才是根因，前两轮都打错位置）**：`closestRes(map, target)` 在非精确字重匹配时，旧末行 `return above ?: below ?: map.values.first()` 中 `above`/`below` 是**字重 key（如 400）**，被直接当返回值——而非 `map[key]` 对应的 **resId value**。单档书法体 `resByWeight={400→2131820561}` 在标题 Bold(700) 请求下 `closestRes` 无精确 key → `below=400`(重量 key) 被直接返回 → `CombinedAndroidFont` 把 400 当 cjkResId → `buildFontFamily(context,400)`→`cacheFontFile(400)` 用错误 id 失败 → `cjkFamily=null`→`cjkOk=false`→中文回退层被丢弃，标题落系统字体。正文(400)精确命中返回 resId→正常，故只标题复现。
- **修复（集中 FontCatalog.kt 一处，编辑页+设置页同链共享）**：`closestRes` 末行改为 `return above?.let{map[it]} ?: below?.let{map[it]} ?: map.values.first()`，非精确匹配时返回真实 resId value。doc 注释同步更新点明返回值语义（规则 #6）。`combinedFamilyFonts`→`closestRes`→`CombinedAndroidFont(cjkResId)`→`CombinedTypefaceLoader`→`buildCombinedTypeface(cjkResId)`→`buildFontFamily`→`cacheFontFile` 整链自洽；`ContentFontManager`/`FontManager` 均经 `combinedFamily` 共用此链，一处修复覆盖两条标题路径。
- **清理**：用户复验通过后删除两处 `FontCJK` 诊断日志（buildCombinedTypeface、CombinedTypefaceLoader.loadBlocking），并移除已无引用的 `import android.util.Log`（规则 #10）。
- **校验**：用户真机复验**已通过**（选英文/数字字体→应用→选三书法体之一→应用，标题中文生效；设置页同路径核对正常）；静态括号平衡 + import 审计通过（删 Log 后无残留引用）。
- 提交：`cdca7951`（修复字体合成：单档书法体在粗体标题下中文回退层被跳过）；临时中文提交信息文件写后删。新建提交，未改写历史。

## 灵感编辑页字体选择面板原型 · 图标候选扩充（HTML 高保真）
- 文件：`原型/工具栏/灵感编辑页字体选择面板.html`（独立于上面安卓源码，属高保真原型，用户仍在「先细看原型」阶段，尚未改源码）。
- 改动：字体按钮 `FONT_ICONS` 与字号/颜色按钮 `STYLE_ICONS` 均由 4 个增至 **8 个**，混合 Material Icons（fill）与 Lucide（stroke，取自 lucide-static 官方 SVG）。
  - 字体按钮新增 Lucide：type / text / baseline / case-sensitive。
  - 字号颜色按钮新增 Lucide：type / baseline / palette / paint-bucket。
- 渲染机制：新增 `applyIcon(svgEl, icon)` 按 `icon.mode`（fill/stroke）切换 svg 属性；切换器与按钮均支持描边图标。候选加 `lib`/`kotlin` 字段（Lucide 标注 `LucideIcons.*`，需引入 compose-icons-lucide 第三方库）。
- 验证：Node 语法检查通过；Chrome 无头渲染无报错，16 候选按钮全部渲染，Lucide 描边图标（palette 填充圆点）正确显示。
- 待办源码（仍未开始）：RichTextFormatToolbar / 新建 FontSizeColorPanel.kt / InspirationEditBottomBar / InspirationEditScreen。

## 第十七轮：字号/颜色面板源码落地（原型审核通过后）
- 用户四点要求：①字体按钮换 Lucide「Type」；②新按钮用 Lucide「CaseSensitive」；③整行按钮视觉一致；④激活态橙色背景块（用户称"橙色阴影"）改为按钮本身变色。
- **依赖查证（maven central 实测）**：Lucide 库选 `io.github.ardasoyturk.compose.icons:lucide:2.0.7`（DevSrSouza 风格；根 artifact 是 KMP metadata 壳，Android 变体 lucide-android；包名 `compose.icons.LucideIcons` + 扩展 `compose.icons.lucideicons.Type/CaseSensitive`，从 AAR classes.jar 的 `TypeKt/CaseSensitiveKt` 实证）。libs.versions.toml + app/build.gradle.kts 已接入。
- **改动（4 文件 + 1 新建）**：
  - `RichTextFormatToolbar.kt`：字体按钮 FontDownload→LucideIcons.Type；字体与 B 之间插字号颜色按钮（CaseSensitive，新参数 isSizeColorPanelOpen/onSizeColorPanelClick）；三个按钮组件删 `0xFFFFE0C0` 激活背景块（激活=图标变 primary 0xFFFF9A5C）；清理 background/clip/RoundedCornerShape import。
  - 新建 `FontSizeColorPanel.kt`：字号 8 档（Aa 按 sp 真实缩放）+ 12 色板（默认=白底斜杠圆）+ HSV 取色器（18dp 色相条+84dp SV 板，拖动每帧回调 HEX；HSV 走平台 AndroidColor.HSVToColor/colorToHSV；手势闭包经 `var hsv by remember` delegate 读最新值规避 pointerInput(Unit) 陈旧闭包）。常量 FONT_SIZE_TIERS/DEFAULT_BODY_SP(16)/TEXT_COLORS public 供 Screen 使用。
  - `InspirationEditBottomBar.kt`：新 AnimatedVisibility（与字体面板互斥、同用 keyboardHeight）+ 参数透传。
  - `InspirationEditScreen.kt`：isSizeColorPanelExpanded（双向互斥）；回显值直接从 richTextState.currentSpanStyle 派生（fontSize 未指定回落 16；color 匹配预设→下标、否则自定义 hex），不持双份状态；写入与加粗同构（枚举 removeSpanStyle 仅值匹配生效→toggleSpanStyle；默认档/默认色只清除）；自定义色 AndroidColor.parseColor + runCatching。
- **库写入行为查证（RichTextState.kt 源码）**：removeSpanStyle 仅在 `isSpecifiedFieldsEquals`（当前值与传入值完全相等）时生效；toggleSpanStyle 换值走 addSpanStyle——故"枚举 remove + if(current!=target) toggle"模式成立。
- **校验**：未跑 gradle（约定）；括号平衡与 HEAD 基线一致（Screen 基线本身 braces=2/parens=3，脚本剥离器既有局限非本次引入）；新增 import 针对性审计全 PASS。
- 待办：用户首次构建验证（新依赖解析/Lucide 渲染/拖动取色手感）；规则 #9 询问 git 提交。
