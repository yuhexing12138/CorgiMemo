# 2026-09-03 工作日志

## 拉取 free-font 字体库到项目并产出调研报告

### 任务
用户要求：① 把 `jaywcjlove/free-font`（GitHub 可商用中英文字体合集）拉取到本项目；② 总结该库并输出 md 报告。

### 关键环境坑：代理导致 GitHub 不可达（重要，已记入用户级记忆）
- 本机环境变量注入了 `http_proxy/https_proxy = http://127.0.0.1:56997`，该代理对 `github.com` 的 CONNECT 隧道返回 **502**，`git clone` 直接失败：`fatal: unable to access ... CONNECT tunnel failed, response 502`。
- WebFetch / api.github.com 走另一通道不受影响，故容易误判"网络正常"。
- **解法**：克隆时临时剥离代理变量即可直连：
  `env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY git clone --depth=1 <url>`
- 同理适用于 `git pull` / `git ls-remote` / curl 等所有访问 github.com 的命令。

### 拉取结果
- 路径 `C:\Users\EDY\Desktop\CorgiMemo\free-font\`，方式 `git clone --depth=1`（必须浅克隆，完整历史 4.74 GB）。
- 耗时 **25 分 51 秒**，文件 **4,272** 个，占用 **13 GB**（`.git` 4.2 GB + 字体工作区 8.39 GB），HEAD `8602c6e4`。
- **未纳入主仓库 git**：`git status` 显示 `?? free-font/`。已在报告中建议把 `/free-font/` 加入主仓库 `.gitignore`（13 GB 绝不可入库，也不可作子模块）。

### 仓库核心数据（分析 `scripts/data.json` + 扫描 `docs/fonts/` 得出）
- 仓库：MIT / Star 4,036 / Fork 260 / 主语言 JavaScript / 创建 2024-07-16 / 基于已停更的「字集」(wordshub/free-font) 重建。
- 收录 **1,048** 款字体 = data.json **1,048** 条记录（**一一对应，一致性好**），总体积 **8.39 GB**，**81** 个字体系列。
- 格式：ttf 806 (76.9%) / otf 177 (16.9%) / ttc 65 (6.2%)；**全为静态字重字体，无可变字体(VF)**。
- 分类(type)：未分类 479 (45.7%，主因英文字体靠 `english/` 目录自动归类故不填 type)、艺术体 185、黑体 156、宋体 94、手绘体 84、楷体 50。
- 授权(license)：OFL-1.1 690 (65.8%)、**未标注 302 (28.8%，商用前必须回官方核实)**、IPA-1.0 21、AGPL-3.0 12、MIT 10、个人免费 4、GPL-2.0 3、需要授权 2、GPL-3.0 1、CC BY-NC 4.0 1、个人非商业 1、BSD-3-Clause 1。
- 含官网 895 (85.4%)、含百度网盘 908 (86.6%)；`officialDownload` 字段基本未启用（仅 1 条）。
- 文件数 TOP 系列：english 365、其他字体 138、思源字体系列 56、梦源字体 54、寒蝉字型 39。

### 数据分析踩坑（复用价值高）
1. **`subfamilyName` 不可靠**：思源系列 `familyName="Gen Shin Gothic P Medium"` 但 `subfamilyName` 恒为 `Regular`/`Bold`。判断字重必须从**文件名**提取。
2. **文件名优先于 subfamilyName**：否则 `思源黑体-Medium.otf`（sub=Regular）会被误判为 400 而非 500，导致档数统计少算。
3. **带字重后缀的 familyName 会被误拆字族**：如「梦源宋体-W3…W9」各自 familyName 不同，按 familyName 归组会把 10 档拆成 10 个"字族"。**应按文件所在目录归组**。
4. `data.json` 字段：`byte/size/ctime/postscriptName/fullName/familyName/subfamilyName/version/numGlyphs/copyright` 为自动生成；`type/license/home/baidu/officialDownload` 为人工维护（故缺失率高）。

### 对 CorgiMemo 的选型结论（直击"中文字重撞档"痛点）
内置多字重中文字体是解决 B1(500)/B2(700)/B3(900) 撞档的根本办法。本库多字重中文字体排行（按目录归组）：
- 梦源宋体/黑体 10 档 65535 字形（643–994 MB，过大）
- 小米 MiSans 8 档 29601 字形 76.9 MB
- 獅尾半月字體 7 档、初夏明朝體 7 档、源样明体 7 档、思源宋体 7 档
- **思源黑体 6 档 30888 字形 55.8 MB**（首选）
- 源音黑體 6 档 65535 字形 120.6 MB（满覆盖）
- **阿里巴巴普惠体 5 档 28987 字形 38.6 MB**（体积最优）

**首选思源黑体**：已核对实际文件 7 个（ExtraLight 7.4M / Light 8.0M / Normal 8.0M / Regular 8.0M / **Medium 8.1M** / **Bold 8.3M** / **Heavy 8.4M**）。其中 **Medium(500)/Bold(700)/Heavy(900) 精确对应 B1/B2/B3 三档**，只内置这 3 档约 **24.8 MB**。

落地路径：拷贝所需档位到 `app/src/main/res/font/`（**不要子模块**）→ 建 FontFamily → 改 `Type.kt` 的 `fontFamily = FontFamily.Default` → 同步字重可用集合常量 → 档位自动派生 500/700/900 三档分明；`FontWeightProbe` 保留作兜底置灰。

### 产出
- 报告：`C:\Users\EDY\Desktop\CorgiMemo\free-font-可商用字体库调研报告.md`（9 章：仓库概览 / 本地拉取 / 项目组织 / 收录统计 / 数据质量坑 / 对 CorgiMemo 的实用价值 / 集成建议 / 风险提示 / 速查附录）。
- 临时分析脚本 4 个（`_analyze_tmp*.py`、`_probe_tmp.py`）已清理，`free-font/` 内 `git status` 干净。

### 风险提示（已写入报告）
- 商用授权**必须以官方为准**：`license` 为人工维护、302 条缺失、可能有误。
- 避开 AGPL-3.0(12)/GPL(4) 传染性协议；排除明确不可商用的 8 条（个人免费 4 / 需要授权 2 / CC BY-NC 1 / 个人非商业 1）。
- 免费商用授权厂商可收回，需保留版本与授权快照。

## 内置思源黑体根治字重撞档 · 落地完成并入库

### 决策修正（重要）
- 最初报告建议"只内置 Medium/Bold/Heavy 三档"会漏掉默认正文 400，导致正文回落系统字体、视觉割裂。
- **最终方案：内置 4 档（Regular 8.0 / Medium 8.1 / Bold 8.3 / Heavy 8.4 MB，共 32.8 MB）**，封面齐全。

### 关键修正 2：像素探测必须用应用真实字体
- `FontWeightProbe.distinctWeights` 原来用 `Typeface.DEFAULT` 探测；内置思源黑体后若不传内置字体，B1(500) 会被系统字体探测误判"无独立字面"而误置灰。
- 已加 `typefaceOf` + `fontTag` 两参数：`FontWeightProbe` 默认仍走系统字体(`defaultTypefaceForWeight`)，调用方(`RichTextFormatToolbar`)传入 `appTypefaceForWeight` + `APP_FONT_TAG="source-han-sans-cn"` 隔离缓存。

### 改动文件（已提交 `44519e40`）
- `app/src/main/res/font/`：4 个 `.otf`（Regular/Medium/Bold/Heavy），`R.font.source_hans_sans_cn_*` 命名。
- `Type.kt`：新增 `SourceHanSansCN` FontFamily（400/500/700/900）、`APP_FONT_TAG`、`appTypefaceForWeight(context,weight)`（静态字重按区间取档，非可变字体量化）；Typography 13 个 TextStyle 全部改用 `SourceHanSansCN`；`APP_FONT_AVAILABLE_WEIGHTS`(400/500/700/900) 派生出 `BOLD_WEIGHT_TIERS`(500/700/900)。
- `FontWeightProbe.kt`：`typefaceForWeight`→`defaultTypefaceForWeight`（私有），`distinctWeights` 加 `fontTag`/`typefaceOf`，缓存键 `"$fontTag|排序候选"`；含 use-after-recycle 根因修复 + 模块级 `LinkedHashMap` 缓存（样例 `"字重Hg"`）。
- `RichTextFormatToolbar.kt`：探测调用传入内置字体 Typeface + fontTag。
- 全局核查：无残留旧 `typefaceForWeight` 引用；无其他写死 `FontFamily.Default` 处（仅注释）；`internal` 跨包可见性在 app 模块内成立。

### OFL 1.1 合规快照（用户选"补"）
- `licenses/SourceHanSansCN-OFL-1.1.txt`：权威英文原文 + 思源黑体版权头（已用脚本从 .otf name 表实际抽取核对：`Copyright © 2014 Adobe Systems Incorporated. All Rights Reserved.`，RFN=`Source`）。
- `licenses/SourceHanSansCN-NOTICE.md`：字体身份 / 来源(free-font 路径) / 入库 4 文件清单 / OFL 关键义务摘要 / 上架前建议在 App「关于」页展示本授权的建议。
- `free-font/` 已被 `.gitignore` 忽略，不入库。

### 提交结果
- 提交 `44519e40`：9 文件，259 插入 / 44 删除，工作区干净。字体 32.8 MB 随本次入库（用户确认直接提交，未走 LFS）。
- 注：本机 safe-bin 写保护此前可能令 `git commit` 报 Permission denied；本次提交成功（若再遇阻，暂存区就绪时可在原生终端手动 commit）。

## 正文字体选择功能（精选包）· 代码接入完成（待提交）

### 需求澄清（两次 AskUserQuestion）
- 接入方式：**字体选择功能 + 精选中文多字重**（非全量打入包体）。
- 范围口径：严格可商用（OFL/MIT/BSD/IPA）；因 `data.json` 的 `license` 字段人工维护不可靠，实际以**字体 name 表版权 + OFL 标记**判定。
- 精选包（均衡，新增约 520MB）：思源黑体(已内置) + 思源宋体 + 源音黑體 + 獅尾半月SC + 悠哉 + 初夏明朝。排除梦源等单族近 1GB 与小米/阿里自定义授权族。

### 资产与授权（已完成，提交 `44519e40` 之后的增量，本轮未提交）
- `res/font/` 共 **35** 个字体文件（思源黑体 4 otf / 思源宋体 7 ttf / 源音黑體 6 otf / 獅尾半月光 7 ttf / 悠哉 4 ttf / 初夏明朝 7 ttf）。
- `licenses/` 6 份 OFL 快照 + `THIRD_PARTY_FONTS.md` 索引（初夏明朝版权串为 UTF-16 未干净抽出，占位待核）。

### 代码改动（本轮，均未提交）
- **新增 `FontCatalog.kt`**：`FontEntry`(id/显示名/licenseName/licenseAsset/copyright/FontFamily/tag/resByWeight)，`availableWeights`+`boldTiers`(>400 前三档) 派生，`typefaceForWeight(ctx,weight)` 按字重就近取档；`FontCatalog.entries` 列出 6 款。獅尾半月 demilight 映射 `FontWeight(350)`（构造器在 app 模块可调用，已证实 InspirationEditScreen 亦用 `FontWeight(weight)`）。
- **新增 `FontManager.kt`**：反应式单例（镜像 ThemeManager），`currentEntry: StateFlow<FontEntry>` 默认思源黑体；`initFont`/`setFont`/`tag`/`boldTiers`/`typefaceForWeight`/`collectCurrentEntry()`。
- **`Type.kt` 重写**：删静态 `SourceHanSansCN`/`APP_FONT_TAG`/`appTypefaceForWeight`/`Typography`/`APP_FONT_AVAILABLE_WEIGHTS`/`BOLD_WEIGHT_TIERS`，改 `buildTypography(fontFamily)` 工厂。
- **`CorgiPreferences.kt`**：新增 `FONT_ID` 键 + `fontId` Flow(默认字面量 `"source_han_sans_cn"`) + `saveFontId`（data 层不反向依赖 ui 包，故用字面量默认）。
- **`SettingsViewModel.kt`**：新增 `_fontId`/`fontId`/`setFontId`，`loadSettings` 内 `FontManager.initFont(loadedFontId)`。
- **`Theme.kt`**：`typography = buildTypography(FontManager.collectCurrentEntry().family)`。
- **`RichTextFormatToolbar.kt`**：`collectAsState` 取 `fontEntry`，`boldTiers`/`tag`/`typefaceForWeight` 改用选中字体（remember 依赖 `fontEntry.tag` 以便换字体重探测）。
- **`InspirationEditScreen.kt`**：`onSetFontWeight` 清除集合改用 `FontManager.currentEntry.value.boldTiers`。
- **`AppearanceScreen.kt`**：新增「正文字体」分组 + `AppearanceFontOption`（本字体预览 + 授权 + 档数 + 选中✓），点击 `setFontId`。
- **`app/src/main/assets/licenses/`**：拷贝 6 OFL + NOTICE + 索引（随 APK 分发，满足"补附带的授权文本与版权声明"）。

### 注意（未强制/未做）
- 关于页展示授权尚未做（资产已就绪，未来读 `licenses/xxx-OFL-1.1.txt` 即可）。
- FontManager 仅在 SettingsViewModel 加载时初始化（与 ThemeManager 同模式），冷启动非默认字体会在打开设置前短暂显示默认字体——与现有主题行为一致，属已知、可接受。
- **待用户提交**：本轮字体选择功能代码 + 31 新字体资源 + 6 授权快照 + assets 拷贝尚未 git 提交。
