# 2026-09-21 工作日志

## 灵感编辑页：T / Aa 面板展开期间抑制软键盘（已实施，待真机验证）

**需求**：底部工具栏「T 字体面板」（FontPickerPanel）与「Aa 字号颜色面板」（FontSizeColorPanel）
展开时，用户在**正文中聚焦光标 / 多选**不得唤起软键盘——键盘会把"占键盘位"的面板顶走，
并压缩 WebView 视口（与 v1.11.9 的"视口坍缩到一行高"同源正反馈）。

**落地位置**
- `ui/screens/probe/BlockNoteEditorWebView.kt`
  - 新增 `private class ImeSuppressibleWebView : WebView` + `IME_SUPPRESS_SCRIPT` 常量。
  - `BlockNoteEditorWebView` Composable 新增 `suppressIme: Boolean = false`（默认 false，
    探针页 BlockNoteEditorScreen 与阅读卡不受影响）；在 `AndroidView.update` 里同步状态。
- `ui/screens/inspiration/InspirationEditScreen.kt`
  - 新增派生值 `isFormatPanelOpen = isFontPanelExpanded || isSizeColorPanelExpanded`。
  - 正文 WebView：`suppressIme = isFormatPanelOpen`。
  - 顶部标题 `RichTextEditor`：面板展开时用 `PointerEventPass.Initial` 消费指针事件
    （点击不聚焦 ⇒ 自然不弹键盘）；**刻意不用 `enabled = false`**（会切成禁用色并清焦点）。
- `components/InspirationEditBottomBar.kt`：KDoc「交互规则」同步。

**抑制手段共 4 层（全部保留，互兜）**
1. `isImeSuppressed` setter 变化即 `hideSoftInputFromWindow`（收掉可能已弹出的键盘）；
2. `onCreateInputConnection` 返回 null；
3. `onCheckIsTextEditor` 返回 false；
4. 页面注入 `inputmode="none"`（Chromium 官方键盘抑制语义）+ MutationObserver 持续补标，
   解除时按 `data-ime-prev` 精确还原。

**关键约定**
- 抑制期间**保留光标与选区**（点击定位 / 长按选词 / 拖手柄多选全部保留）；
- 收起面板只恢复"可唤起"能力，**不主动弹回**键盘（用户再点一次正文/标题即恢复）；
- JS 注入必须覆盖三个时机（缺一不可）：状态变化 / `onPageFinished` / 桥 `ready`（DOM 才挂载）。

**回退点**：若个别 ROM 上选择手柄 / 浮动工具条异常，注释掉两个 override 即退到
「JS inputmode + 主动隐藏」两道，焦点与选区走原生默认路径。

## 修复：正文卡在「正在装载…」（同日，上一轮改动引入的回归）

- **现象**：进入已有内容的灵感，标题 / 字数行正常，正文区一直停在「正在装载…」（JS 侧文案）。
- **根因**：`ready` 分支里同步调用了 `applyImeSuppression()`（内部 `evaluateJavascript`），
  而 `handleUpMessage` 由 `@JavascriptInterface` 调用、运行在 WebView 的 **Java 桥线程**
  → 抛 `IllegalStateException` → 被 `handleUpMessage` 外层 `catch` 静默吞掉
  → 同一行后面的 `mainHandler.post { flushIfReady() }` 不再执行 → `init` 永不下发
  → JS 侧 `booted` 恒为 false（它只在 `case "init"` 里 `setBooted(true)`）→ 一直「正在装载…」。
- **修法**：
  1. 止血：把注入与 `flushIfReady()` 放进同一个 `mainHandler.post { ... }`；
  2. 加固（用户选定）：**上行消息入口统一切主线程**——`handleUpMessage` 只负责
     `Log` + `mainHandler.post { handleUpMessageOnMainThread(json) }`，解析逻辑全部移入
     主线程侧的 `handleUpMessageOnMainThread`，各分支原有的 `post` 全部删除。
     线程正确性收敛为入口唯一一处约定，以后往分支里加 WebView 调用不会重踩。
- **教训**：① `@JavascriptInterface` 回调里任何 WebView 方法（`evaluateJavascript` /
  `loadUrl` / `setBackgroundColor`）都必须先 post 回主线程，**原来把 `flushIfReady` 包在
  post 里正是这个原因，改代码时不能拆散这层包装**；② `catch { Log.e }` 会把线程违规
  伪装成"某功能没生效"；③ WebView 问题要先分清「JS 内状态」与「Kotlin 侧状态」——
  本例装载文案在 JS，而切换它的 init 来自 Kotlin。

## 颜色入口迁移：⋮ 菜单的块级颜色移入「A」对话框（同日）

**需求**：⋮ 菜单里的「背景色 / 文字色」（块级）移入底部「A」按钮的颜色对话框并更名
「段落背景色 / 段落文字色」；「A」对话框原有的「文字颜色 / 背景颜色」（行内）更名
「选中文字色 / 选中背景色」；四组色板**样式统一**（用户选定：按 ⋮ 里的色板设置，
含颜色图标样式）；⋮ 侧完全移走；点选后关闭弹窗。

**结构性改动**

- 新增 `components/BlockColorPicker.kt`：把原 RichTextFormatToolbar 里 private 的
  `BlockColorPalette` / `BlockColorRow` / `BlockColorDot` 迁出并提升为 `internal`，
  供两处共用；新增 `blockColorHexOf(name, isDark, background)` 把色名转 "#RRGGBB"
  （行内色下行需要 hex，块级下行需要色名，色值同源于同一张 Palette）。
- `BlockColorRow` 两处调整：① 色点行 `Row` → `FlowRow`（对话框内容区仅 ~300dp，
  10 个 24dp 色点单行放不下会被裁）；② 新增 `showSelection`——行内色无状态上行，
  传 false 避免「默认」恒定高亮。
- 新增 `components/ColorStyleDialog.kt`：四组色板（选中文字色 / 选中背景色 /
  段落文字色 / 段落背景色），`heightIn(max=360.dp)+verticalScroll` 防窄屏裁切，
  自行订阅 `ThemeManager` 判定 isDark（宿主不必再引主题 API）。
- 删除：`RichTextFormatToolbar` 的 `onSetBlockColor` 参数与 ⋮ 菜单里两行色板、
  `InspirationEditBottomBar` 的同名透传、`InspirationEditScreen` 的透传闭包与内联
  AlertDialog（改调组件）；清理 `isSystemInDarkTheme` / `ThemeManager` /
  `android.graphics.Color as AndroidColor` 三个不再使用的 import。

**下行通道差异（易错点）**：行内色走 `format("textColor"/"backgroundColor", hex|"default")`，
块级色走 `setBlockColor(textColor=/backgroundColor= 色名|"default")`，两者维度不同、互不覆盖。

## 「A」按钮改为内联面板 + 移到 Aa 右侧（同日，接续上一条）

**需求**：① A 按钮移到 Aa 右侧；② 删除 A 按钮的弹窗展示模式；③ 展示模式改成与 T / Aa 一致。

**改动**

- 新增 `components/ColorStylePanel.kt`：与 `FontSizeColorPanel` 同规格的内联面板
  （Surface + `Column.height(panelHeight)` + 40dp 面板头「颜色 / 点选即时生效 / 完成」
  + 分隔线 + `weight(1f)` 纵向滚动内容），内含上一步的四组色板（复用 `BlockColorRow`）。
- **删除** `components/ColorStyleDialog.kt`（弹窗形态彻底移除）。
- `RichTextFormatToolbar`：`FormatTextButton("A")` 从"对齐×3 之后"移到 **Aa 正右侧**
  （顺序 T → Aa → A → B…）；参数 `onOpenColorStyleDialog/showColorStyleDialog`
  → `onColorPanelClick/isColorPanelOpen`；原位置留说明注释。
- `InspirationEditBottomBar`：新增 `isColorPanelOpen` + `onColorPanelClick` +
  `onColorPanelDismiss` + 四组颜色回调（onInlineTextColorSelect /
  onInlineBackgroundColorSelect / onBlockTextColorSelect / onBlockBackgroundColorSelect），
  在"上行二c"槽位插入 `ColorStylePanel`（三面板互斥、同槽位、同 `keyboardHeight`）。
- `InspirationEditScreen`：`showColorStyleDialog` → `isColorPanelExpanded`；
  `isFormatPanelOpen` 纳入第三个面板（**A 面板展开同样抑制软键盘**）；
  A 按钮回调改为三选一互斥（开前关另两个 + 收键盘）；删除内联弹窗接线与相应 import。

**行为差异**：点选颜色后**不再关闭**面板（弹窗时代是点选即关），与 Aa 的"点选即时生效、
面板保持展开"一致，收起靠「完成」或再点 A。


