# 2026-09-23 工作日志

## 修复「滑动起手闪一下原生波纹」（Nest/Unnest，待真机验证）

**现象**：左右滑动底部工具栏，起手落在 Nest/Unnest 上时瞬间闪一下原生波纹；其他按钮不会。

**根因**：上一轮根因修复（手势全权驱动 interactionSource）里 Press 在 down 即 `tryEmit`；
而其他按钮走 IconButton（内部 clickable），Foundation 1.11 在 horizontalScroll 容器内会把
Press 交互延迟 `TapIndicationDelay`（= `ViewConfiguration.getTapTimeout()` = **100ms**）发射、
滑动被父级消费时**什么都不发**——所以其他按钮滑动起手不闪、这两个连发按钮闪。
（官方源码核对：foundation-android-1.11.2 `Clickable.kt` 的 `ClickableNode.handlePressInteraction`
2228-2271 行 + `Clickable.android.kt` 的 `TapIndicationDelay`。）

**修法（逐条复刻官方 handlePressInteraction 语义）**：
- `longPressRepeat` 新增 `delayPressIndication: Boolean = false`；`RiFormatButton` 传 `true`。
- 滚动容器路径：`onPress` 内 `coroutineScope{}` 起 delayJob，延迟
  `DEFAULT_PRESS_INDICATION_DELAY_MS`（自定义常量 100L，对齐 internal 的 TapIndicationDelay）
  再发 Press——
  ① 延迟期间被父级消费（滑动起手）→ `cancelAndJoin` 且**什么都不发**（不闪波纹）；
  ② 延迟期间正常抬起（快速点按）→ 瞬时补发 Press+Release（点按波纹不受影响）；
  ③ 延迟已过（Press 已发）→ 抬起发 Release、取消发 **Cancel**（对齐官方，取消不再误用 Release）。
- finally 兜底从「必发 Release」改为「pendingPress 未收尾时补发 Cancel」——协程被杀
  （enabled 翻转重启 pointerInput / 组合离开）时波纹照常消散（Cancel 与 Release 视觉等价）。
- **两处作用域各有理由勿混用**：波纹 delayJob 挂 onPress 内 `coroutineScope{}`（结构化子协程，
  随手势协程消亡、无孤儿发射）；连发定时器仍在 `rememberCoroutineScope()` 普通域（不受手势
  生命周期限制）。KDoc 要点 3/5 已写明。
- 顶栏撤销/重做（无 interactionSource）与默认 `false` 路径行为不变，零回归。

**改动文件**：LongPressRepeatIconButton.kt（新常量/新参数 + onPress 重写）+
RichTextFormatToolbar.kt（RiFormatButton 传参 + 注释块补一条）。纯 Kotlin，无需重建 JS 产物。
**已提交 `f2072daf`**（仅上述两个 Kotlin 文件，2 files changed, +125 −21；pre-commit 钩子
正确跳过。editor.html / st9.txt 等并行会话在制品未卷入）。

**真机验证点**：
1. 在 Nest/Unnest 上起手左右滑动 → 全程无波纹闪现（与其他按钮同观感）；
2. 快速点一下 Nest → 波纹正常出现并消散、缩进一级（延迟期内补发 Press+Release 生效）；
3. 长按连发、到顶停发、松手无残留（上一轮行为回归）；
4. 顶栏撤销/重做长按连发与波纹不变。

## 撤销失效排查（「按钮亮但点了没反应」+ 静态穷尽后加埋点）

**症状（用户确认）**：顶栏撤销按钮亮（canUndo=true）但点击无效；最近改动后才出现。

**链路梳理（全链已验证）**：InspirationEditScreen.kt:1220-1237（LongPressRepeatIconButton →
blockNoteController.undo()）→ BlockNoteEditorWebView.kt:303（requestUndo 下发）→
EditorApp.tsx case "requestUndo"（editor.undo() → StateManager.undo() → history.undoCommand）；
可用态 = canExec(undoCommand) → undoState 上行 → 宿主 canUndo 快照态。

**静态已排除 7 项**：①产物过期（bn-src 哈希一致）②canExec/undo 扩展错位（同源，默认扩展只有
HistoryExtension，无 yUndo）③exec 前置拦截（无）④链接 saveSelection/restoreSelection 污染栈
（纯读/纯选区 tr）⑤最近 3 提交未碰撤销代码 ⑥LongPressRepeatIconButton 今日改动不影响顶栏路径
（delayPressIndication 默认 false）⑦undoState 上行断了（按钮亮恰恰证明上行通）。

**剩余 3 假设**：A.undo() 抛异常（logcat 有 error 上行）；B.撤销了「视觉无差异」步骤
（TrailingNode 尾块补写/id 事务）——特征=点后 canUndo 翻 false 但内容不变；C.命令未送达
（无 `[editor] down: requestUndo` 日志）。

**已加临时埋点**（EditorApp.tsx requestUndo/requestRedo case）：上行 diagnostic
`undo: canBefore=<bool|nocmd> ret=<bool|noed>`，异常走 error 通道。**取证后须恢复原两行实现**
（注释里写了恢复代码）。判读表已写进埋点注释。

**待办**：用户重建 JS 产物（npm run build:editor）+ 重编 APK → 复现（打字后点撤销）→
`adb logcat -s BlockNoteEditor` 取证。埋点代码未提交。

## 撤销失效第二轮取证（真机日志反转让嫌疑移到宿主手势/桥层）

**用户取证**：已重建产物+重编 APK，点了撤销按钮；logcat（-s BlockNoteEditor）只有两条
打字 `changed` 上行，**点撤销后零日志**——无宿主 `down(...)`、无 JS `undo:` 埋点、无 error、
无 undoState 翻转。

**推理**：JS 埋点在 `case "requestUndo"` 内、宿主 sendDown 的 `down(...)` 日志在下发前——
两者都没出现 = **命令根本没进桥**。嫌疑收敛三层：①手势层 onAction 未触发
（LongPressRepeatIconButton）②enqueueCommand `ready&&!initSent` 静默丢弃 ③sendDown
`webView==null` 静默跳过。已排除：onRelease 置 null 竞态（编辑页 WebView 无条件组合）、
今日手势改动（对 source==null 分支语义等价）。

**新增宿主日志（Kotlin，无需重建 JS 产物）**：undo()/redo() 加 `undo() called`；
enqueueCommand 三分支各留日志（DROPPED / ->sendDown / ->pending）；sendDown null 分支改
WARN `sendDown SKIPPED (webView==null)`。判读：无 `undo() called` = 手势层；有 called 无
enqueueCommand = 不可能；DROPPED = 桥状态机窗口；有 down(...) 无 JS 响应 = evaluate 层。

**待用户重编 APK 复现，按上述判读定位。宿主日志代码未提交。**

## 撤销失效破案（根因 = 双层手势互斥，9-22 重写引入）+ 修复

**决定性证据链**：①用户确认点了撤销/按钮亮/能点出波纹；②真机 logcat（新进程 7397）
打字后 `undoState canUndo=true` 上行正常 → 可用态机制健康；③点撤销后连旧代码就有的
宿主 `down(...)` 日志都没有 → 命令未进桥 → 手势层未触发；④「有波纹无动作」= 双层手势、
内层独活。

**根因**：b8140c5e（9-22 08:59）把 longPressRepeat 底层由
`awaitFirstDown(requireUnconsumed = false)` 重写为 `detectTapGestures`（其内部
awaitFirstDown **默认 requireUnconsumed = true**）。顶栏撤销/重做 = IconButton(onClick={})
包裹 + 外层 longPressRepeat——IconButton 内层 clickable 在 down 时消费事件（本项目
PressFeedback.kt 同款坑，旧实现正是为此写 requireUnconsumed=false）→ 外层永远等不到
未消费的 down → onPress 不进入 → 动作永不触发；内层波纹照常 → 「亮、有波纹、没反应」。
Nest/Unnest（RiFormatButton）同提交已改 Box 自持波纹、无内层 clickable → 不受影响，
故只有撤销坏。提交信息里「顶栏撤销/重做无回归」判断错误；9-22 后撤销入口首次被点即今天。

**修复**（LongPressRepeatIconButton.kt）：与 RiFormatButton 对齐——去掉 IconButton 包裹，
改 `Box + minimumInteractiveComponentSize + size + clip(CircleShape) +
indication(interactionSource, ripple()) + longPressRepeat(…, interactionSource)`；
手势成为 down 唯一消费者并全权驱动波纹。顶栏不在 horizontalScroll，无需
delayPressIndication。import：material3.ripple / foundation.indication /
material3.minimumInteractiveComponentSize（Foundation 1.11 已移除 foundation 路径）。
签名未变，调用方（InspirationEditScreen 撤销/重做）零改动。

**临时诊断已清理**：EditorApp.tsx requestUndo/requestRedo 恢复原两行（**产物需重建**）；
宿主 undo()/redo()/enqueueCommand 恢复原状，仅保留两处异常路径 WARN
（`enqueueCommand DROPPED (ready && !initSent)` / `sendDown SKIPPED (webView==null)`）。

**真机验证清单**：打字→点撤销回退一步→按钮变灰；连续撤多步；长按连发 450ms/150ms；
重做对称；Nest/Unnest 与其他格式按钮回归无恙；撤销按钮波纹正常出现消散。
**JS 产物必须重建（埋点已还原）+ 重编 APK。修复代码未提交。**

## 补丁：Modifier.indication 的 import 包路径（首次编译报 Unresolved）

**错误**：`LongPressRepeatIconButton.kt:4:36 Unresolved reference 'indication'`——我把
import 写成了 `androidx.compose.foundation.indication.indication`（照搬了 b8140c5e 提交
信息里的包路径描述，那只是简写）。

**查证（源码 jar 实锤）**：foundation-android-1.11.2-sources.jar 里只有
`androidx/compose/foundation/Indication.kt`，**不存在 `foundation/indication/` 子包**；
`fun Modifier.indication(interactionSource: InteractionSource, indication: Indication?)`
是 **androidx.compose.foundation 包**的顶层扩展。正确 import =
`import androidx.compose.foundation.indication`（RichTextFormatToolbar.kt 一直是这么写的，
它编译通过、本次错误列表无它）。已修正 import 并加注释防回退；签名对
InteractionSource 非空，本组件传非空源 ✓。**待用户重编。**

## 修复「Code Block 按钮吞掉当前行文字」（根因 = 官方 updateBlock 跨内容类型清空）

**症状**：三行文字（测试一/二/三，三个独立段落块），光标在"测试二"点底部工具栏 Code Block
→ 该行文字消失，只留空代码块。

**根因**（@blocknote/core 0.52.1，实锤源码）：
`updateBlock.ts:200-214` 在**未显式传 content**时，只按 ProseMirror content 表达式**字符串**
判断能否沿用旧内容：
- 段落/标题/引用/列表 = `content: "inline"` → `"inline*"`（createSpec.ts:185-186）
- 代码块 = `content: "plain"` → `"text*"`（blocks/Code/block.ts:65 → createSpec.ts:187-188）
字符串不等 → `content = []` 主动清空；又因旧文本 `text("测试二")` 对 `text*` 恰好合法
（`validContent` true，同文件 259-262），不走整体替换兜底，而是 `replaceContentMinimal`
字符级 diff → 全删。**这是只有 Code Block 吞字的原因**：其余块类型与段落同为 `inline*`。

**位置语义**：官方斜杠菜单走 `insertOrUpdateBlockForSlashMenu`
（getDefaultSlashMenuItems.ts:46-83）：空块 → 就地 updateBlock；有内容 →
`insertBlocks(..., "after")` 插到下一块并 `setTextCursorPosition`。本实现据此对齐（与官方
唯一差异：不把"仅含 /"当空块，工具栏无触发字符，避免误吞真实 "/"）。附带反查：
`insertBlocks` 内部只做 `tr.step`，**不移动光标**，必须显式 setTextCursorPosition。

**改动**：EditorApp.tsx `case "codeBlock"` 重写（含分支 3 条 + 完整根因注释；反向
codeBlock→paragraph 也显式回传 content，否则 "text*"→"inline*" 同样清空）；
RichTextFormatToolbar.kt 该按钮 KDoc 同步更新（"toggle 语义" → 官方 insert 语义）。
无需新增 import。**JS 产物必须重建（npm run build:editor）+ 重编 APK。**

**同类隐患（未修，待用户定）**：`case "paragraph"`（+ 菜单普通段落按钮）对代码块同样
`updateBlock(block, { type: "paragraph" })` 未传 content → 在代码块内点它会丢掉代码文本。
修法需防御性判断（仅当 `block.content` 是数组/字符串时回传，tableContent 不可当 inline 传）。

**续：paragraph 隐患已一并修复**（用户确认）。`case "paragraph"` 改为把原 inline 内容
显式回传（`unknown` 中转再 `Array.isArray || typeof === "string"`，否则 TS2367 类型不相交），
tableContent / 无内容块原样放行。

**再续：全部同类分支已统一收口**（用户确认）。新增模块级工具函数
`retypeBlockSafely(ed, block, type, props?)`（EditorApp.tsx，紧邻其它模块级工具函数）：
内部只回传 inline 内容形态（数组/字符串），tableContent 与无内容块放行，返回新块对象。
改造点：heading1-6、toggleHeading/2/3、toggleList、quote、paragraph、codeBlock（双向），
以及**已存在的** `toggleBlockType(ed, type)`（bulletList / numberedList / checkList 三个
列表按钮的唯一入口）—— 它此前同样是裸 `updateBlock(block, { type: target })`。
复查：全文仅剩 4 处 `ed.updateBlock` 调用，均为"只改 props / 只改 content"（对齐、块色、
表格内容更新），无类型切换。

**已提交 `bf610e64`（本地，未 push）**：5 files changed, +295 −112。
EditorApp.tsx / RichTextFormatToolbar.kt / editor.html 产物，一并打包了今日"撤销-波纹"
修复线两个此前未入库的文件（LongPressRepeatIconButton.kt、BlockNoteEditorWebView.kt）——
用户选择总集提交。pre-commit 产物校验通过（`bn-src:0baf161c108e`，7 个文件）；
入库断言：`git show HEAD:EditorApp.tsx` 中 `retypeBlockSafely` 出现 16 次、
`HEAD:LongPressRepeatIconButton.kt` 已无 IconButton 组件调用（残留 2 处匹配均为注释引用
与函数名 `LongPressRepeatIconButton(`）。用户真机验证通过。

【本次踩坑·工具层】排查 diff 时连续 4 次以**完全相同参数**读取同一临时文件，被判定
"工具调用循环"强制中断（未造成写操作）。教训：tmp 文件读完即删、同一路径不重复读。

## 灵感编辑页顶栏间距微调（「完成」按钮被挤成两行）

**现象**（真机截图）：顶栏右侧「完成」按钮文字折成两行「完 / 成」，橙色胶囊变形。

**根因（宽度账）**：顶栏 Row 共 9 个按钮 + 完成，总需求 ~390.dp **超出 360.dp 机型可用宽度
~352.dp**（`fillMaxWidth` + 左右各 4.dp 内边距）。Row 的非 weight 子项按序测量、逐个扣减剩余
空间，超额的 38.dp 全部由末尾的「完成」承担 → 被压到 ~28.dp，而 M3 `Button` 的
`defaultMinSize(minWidth = 58.dp)` 请求放不下 2 个 13.sp 汉字 → 换行。
其中**最大的隐性浪费是撤销/重做**：`LongPressRepeatIconButton` 内部链首是
`minimumInteractiveComponentSize()`（默认 48.dp），它会把 Box 实测宽度抬到 **48.dp**；
而同行的 `IconButton` 因为调用处传的 `Modifier.size(36.dp)` 位于**更外层**、先用固定约束
卡死了内层，所以能保住 36.dp。于是只有撤销/重做各多占 12.dp，合计多 24.dp —— 这正是
「只有撤销栈间距显得大」的原因。

**改动**（仅 InspirationEditScreen.kt 顶栏 Row；Runtime 视觉无其它变化）：
- 返回按钮 40.dp → 36.dp（与顶栏其余图标按钮统一，24.dp 图标居中不变）；
- 撤销/重做调用处**显式**传 `Modifier.size(36.dp)`（省 24.dp）；
- 分段间隔 4 / 4 / 8.dp → 2 / 2 / 2.dp；Row 左右内边距 4.dp → 2.dp；
- 「完成」`contentPadding` 水平 14.dp → 12.dp；Text 加 `maxLines = 1` + `softWrap = false`
  作为抗压缩兜底（不再出现两行变形）。
总需求 390 → **352.dp**，可用 356.dp → 余 4.dp 由中间的 weight Spacer 吸收，完成按钮
稳定拿到 58.dp。**改动未提交。**

**可复用经验**：M3 `minimumInteractiveComponentSize()` 只尊重**更外层**的尺寸约束；
要在调用处把触摸区压到 <48.dp，必须用调用处传入的 `Modifier.size(...)` 先固定约束，
在组件内部加 `size()` 是无效的（内层已被外层卡死或反之）。

**续（用户决策）**：进一步减负的方案由用户拍板为「**删除顶栏的复制按钮及其逻辑**」，
而非我建议的「收进溢出菜单」。已执行：移除顶栏复制 IconButton + 整块删除
`copyToClipboard`（原 965-982，含选区/全文分支与 snackbar 提示）+ 清理失效 import
（`android.content.ClipData` / `ClipboardManager` / `Icons.Default.ContentCopy`；
`android.content.Context` 因 2801 行 `hasRecordAudioPermission` 仍在用而保留）。
**删除后总需求降到 ~314.dp**（可用 356.dp），余量充足。
**已提交 `97a01a94`（本地，未 push）**：1 file changed, +54 −60；pre-commit 钩子
正确跳过 blocknote 产物校验（未涉及 blocknote-probe/src）。

【本次踩坑·工具层】PowerShell 里 `git log` / `git cat-file` 的输出中文一律显示成
`鐏垫劅缂栬緫`——是 PowerShell 用 CP936 解码 UTF-8 字节造成的**显示层**乱码，
**不代表仓库里存的是乱码**，勿据此重提交。正确核验方式：用 Python 以字节模式取回
（`subprocess.run([...], capture_output=True)` → `.stdout` 直接写二进制文件），
再用 Read 打开即可看到真实 UTF-8 内容（本次已确认提交信息中文完好）。
另：`git log --pretty=format:"%H%n%s"` 这类命令在本机 PowerShell 会被安全层拦
（`%VAR%` 语法误判），改用 `git cat-file -p HEAD` 可绕开。

**再续（用户二次决策）**：复制按钮下线后余量回到 42.dp，用户选择把撤销/重做触摸区从
临时值 36.dp **回调到 44.dp**——比被自动抬高的 48.dp 省 8.dp，又比 36.dp 更接近 M3 推荐的
无障碍触摸目标（长按连发时手指不易滑出热区），图标仍 18.dp 居中。总需求 314 → **330.dp**，
可用 356.dp 仍余 26.dp。**已提交 `6c3d5261`（本地，未 push）**：1 file changed, +12 −9。

**顶栏宽度账（360.dp 机型・长期参考，新增按钮前先按此核算）**：
可用 356.dp = 返回 36 + 2 + 撤销 44 + 重做 44 + 2 + 画板 36 + 分享 36 + 删除 36 + 锁 36 + 2
+ 完成 58 = **330.dp**，剩余 26.dp 由中间的 `weight(1f)` Spacer 吸收。
即：再往顶栏加一个 36.dp 档图标按钮是可容纳的，加两个就会再次挤压「完成」。

## 修复「Divider 按钮插入分割线后编辑区看不见」+ 发现产物哈希盲区

**现象**：灵感编辑页底部工具栏点 Divider，编辑区看不到任何分割线。

**判据（用户确认）**：点上后"多出一行空白但没有线" → 块其实插进去了，只是画不出来，
故先排除命令链路、直指渲染层。

**链路核验（全通，非根因）**：RichTextFormatToolbar.kt:645-650（onClick=onInsertDivider）→
InspirationEditScreen.kt:1569-1574（blockNoteController.insertDivider()）→
BlockNoteEditorWebView.kt:331（下发 `{"type":"insertDivider"}`）→
EditorApp.tsx:882-889（`insertBlocks([{type:"divider",props:{style:"solid"}}], cursor.block, "after")`）。
产物里 case/命令/CSS 均在。

**根因**：官方 `.bn-block-content{width:100%;padding:3px 0;display:flex}` 是 **flex 容器**；
官方内置 divider 渲染 `<hr>` 并配套 `[data-content-type=divider] hr{flex:1;…}` 撑满主轴。
本项目 `probes/dividerBlock.tsx` 同名覆盖 render 成普通 div 后，外层行 div 成了 flex item，
`flex-grow:0` + 宽度按 max-content 收缩，内部又是**空** div ⇒ 两层宽度同时坍缩为 **0**；
`border-top`（solid/dashed）与 `svg`（wavy）样式全命中但**线长为 0** → 肉眼不可见。
（这也解释了 wavy 的分支虽写 `svg{width:100%}` 仍看不见：父 flex item 宽度为 0。）

**修复**：
- `probes/dividerBlock.tsx`：抽出 `DIVIDER_ROW_STYLE: CSSProperties`
  （`flex:"1 1 auto"` + `width:"100%"` + `minWidth:0`，保留 cursor/padding），两个渲染分支共用；
  顶部 KDoc 写明根因与"为何必须撑满"。新增 `type CSSProperties` 具名导入。
- `probe.css`：新增兜底规则 `.bn-block-content[data-content-type="divider"] > div{flex:1 1 auto;min-width:0}`。
- 两者都改是刻意的：内联是主修（组件自洽），CSS 兜底防将来内联被改动/遗漏。

**产物重建**：`npm run build:editor`（exit=0，1m28s）→ 产物核验：
① `.bn-block-content[data-content-type=divider]>div{flex:1 1 auto;min-width:0}` 在 editor.html；
② 压缩后常量 `const ax={cursor:"pointer",padding:"2px 0",flex:"1 1 auto",width:"100%",minWidth:0}`；
③ `.probe-divider[data-style=solid|dashed]` 规则仍在。④ 本地 tsc --noEmit 零错误。
**改动未提交。**

**⚠️ 顺带发现（未修，建议项）— 产物新鲜度哈希有盲区**：
`vite.editor.config.ts` 的 `collectSrcHash()` 与 `scripts/check-blocknote-artifact.ps1` 的采集范围
都只有 **`editor.html` + `src/editor/`**，而 `schema.ts` 实际还 import `src/probes/dividerBlock.tsx`、
`src/probes/fontSizeStyle`，`EditorApp.tsx` 还 import `src/probe.css` —— **这些会被打进产物却不在哈希里**。
本轮实测：改完这两个文件后跑校验仍打印 `[OK] 产物与源码一致（bn-src:0baf161c108e，7 个文件）`，
即"改了 probes/CSS 忘记重建"这类问题会被 pre-commit 静默放过（与既有的两次踩坑同类，只是换了目录）。
修法需两处同步扩范围（加 `src/probes`、`src/probe.css`），否则会恒等失配。

**可复用经验**：`display:flex` 的块容器里，任何"靠边框/背景撑出视觉"的空元素都必须自己
`flex:1`（或 `width:100%` 且父宽确定）——`display:block` 直觉在这里不成立。

**续（用户确认「一并修」）：哈希盲区已修 + 两处改动合并为一次提交**
- `vite.editor.config.ts`：`collectSrcHash()` 采集清单抽成 `ENTRIES` 常量并扩为
  `editor.html → src/editor → src/probe.css → src/probes/dividerBlock.tsx → src/probes/fontSizeStyle.tsx`；
  KDoc 写明扩范围理由、`src/probes/` 下 probes 页专用文件（checks.ts / probes/schema.ts）
  **刻意不纳入**以免误报，以及"新增 editor 依赖须同步清单"。
- `scripts/check-blocknote-artifact.ps1`：`foreach ($entry in @(...))` 同步为同一 5 项、
  同一顺序（顺序即喂哈希顺序）；头注释"原理/口径"两段一并更新。编辑后**已复核 BOM**：
  前 3 字节仍 `ef bb bf`（PS 5.1 读无 BOM 的 UTF-8 会乱码，这条不能丢）。
- 修复有效性实测：扩范围后未重建时校验**首次报出** `[FAIL] 产物已过期（产物 0baf161c108e
  vs 源码 45b48af47f47，10 个文件）` → 重建后 `[OK] 一致`。此前同样改动会静默放过。
- **为什么合成一次提交**：`editor.html` 是同一个产物文件，两次改动（组件修复 + 哈希口径）
  都体现在它里面，物理上无法拆成两个自洽的 commit（拆开会让中间那个提交的产物哈希与
  当时的脚本口径不一致）。已与用户说明。

**已提交 `03ebfeea`（本地，未 push）**：5 files changed, +154 −80
（dividerBlock.tsx / probe.css / vite.editor.config.ts / check-blocknote-artifact.ps1 / editor.html）。
入库断言（`git show HEAD:<path>` 精确匹配）：dividerBlock.tsx 含 `DIVIDER_ROW_STYLE`×3 与
`flex: "1 1 auto"`；probe.css 含兜底选择器；config 含 `ENTRIES` 与两个新路径；ps1 含
`src\probes\dividerBlock.tsx`；editor.html 含 `bn-src:45b48af47f47` 与兜底规则。
（.workbuddy/memory/* 按项目惯例单独提交，本次未入库。）

**真机验证清单**：点 Divider → 出现实线；点线弹样式工具条切虚线/波浪即时生效；
保存重进、详情卡内分割线样式一致（Compose 侧 BodyBlock.Divider 已支持三样式）。

## 分割线工具条第二轮：「Aa/A 蓝块」真相 + 边缘裁剪修复

**用户两个问题**：① 点分割线后左侧出现 `Aa` 和 `A` 两个蓝色块，点了没反应，是什么？
② 分割线靠上时工具条被编辑区上边缘裁剪；点偏左/偏右时被左右边缘裁剪。

**问题①真相（非 bug，是编辑器自带的行内工具栏）**：那是本项目在格式化工具栏里
**自己加的两个按钮**（`EditorApp.tsx`，实锤产物）：
- `Aa` = **字号**（`label:\`字号 ${n??"默认"}\``，点击在候选档位间循环、末档清除，`Mie()`）；
- `A` = **行内文字颜色**（`label:\`文字颜色 ${n??"默认"}\``，`Tie()`，A 的颜色即当前文字色
  / 未设时用 `--editor-primary` 蓝）。

**为什么"点了没反应"**：它们读的是 `editor.getActiveStyles()`，而**分割线块是 `content:"none"`**
（`dividerBlock.tsx` 的 `content: "none"`）——没有可承载行内样式的文本，字号/文字色都无处施加，
所以点了视觉零变化。**它们根本不该在这个位置出现**。

**为什么它们会弹出来**：行内工具栏（FormattingToolbar）在"有文本选区/光标"时显示。
根因是 `EditorApp.tsx` 的 `case "insertDivider"` 只做 `ed.insertBlocks([divider], cursor.block, "after")`
——**`insertBlocks` 内部只 `tr.step`、不移动光标**（官方源码确认，改 Code Block 那轮已在
`case "codeBlock"` 注释里写明此坑），于是插入后光标**停在分割线之前的原块上** →
行内工具栏继续弹 → 恰好压在分割线工具条下方。

**问题②根因**：`DividerToolbar` 只写 `position:fixed` + `translate(-50%,-120%)`（"点击点正上方居中"），
**没有任何视口夹取**。分割线本来就常贴着编辑区顶部，理想位置于是整体落到编辑区上方被裁；
点偏左/偏右时左右两端同样越界被裁。

**修复（两个文件）**：
1. `EditorApp.tsx` `case "insertDivider"`：改为一次插入 `[divider, paragraph]`，再把光标移到
   末尾那个空段落（`insertBlocks` 返回数组按插入顺序排列 → 取 `[len-1]`，`setTextCursorPosition(tail,"start")`），
   并补 `try/catch` 上行 `insertDivider: <msg>` 诊断（与本文件其它分支口径一致）。
   依据 = 官方 `insertOrUpdateBlockForSlashMenu` 插入后同样显式定位光标。
   效果：光标不再落在分割线上 → 行内工具栏不弹（Aa/A 消失），且输入位置符合"插线后在下面写"的直觉。
   **刻意不插官方 TrailingNode 那种"占位隐式块"**：空段落在本项目 markdown 口径里就是空行，
   往返不会被 `isBlankBodyParagraph` / 尾部裁剪误伤，也不必与 compose-rich-editor 的占位符常量耦合。
2. `probes/dividerBlock.tsx` `DividerToolbar`：改为**两步定位**——
   ① 先按理想位置以 `visibility:hidden` 挂载；② `useLayoutEffect` 里 `getBoundingClientRect()`
   量真实尺寸后夹取再显示。用 `useLayoutEffect`（绘制前同步）而非 `useEffect`，避免看到
   "先错位再跳回"的一帧闪烁。夹取规则：水平默认居中于点击点，越界则滑动进安全区（**不**越界保持居中）；
   垂直默认抬到点击点上方 4px 间隙，上方放不下则**翻转到下方**。安全边距常量 `TOOLBAR_SAFE_GAP = 8`。
   为什么必须实测尺寸而非写死：按钮含 `┈ ╌ 〰` 等可能缺字形的字符 + "删除"两汉字，宽度随设备字体变化。

**产物重建** + 核验：`bn-src` → `55c33ffbbdce`（10 个文件，校验 [OK]）；产物内含
`Ru=8` 安全边距、`Math.min(f,s-Ru-a.width),f=Math.max(f,Ru)` 水平夹取、
`c>=Ru?c:Math.min(e.y+4,l-Ru-a.height)` 垂直翻转，以及
`insertBlocks([{type:"divider"...},{type:"paragraph"}],ne.block,"after")` + `setTextCursorPosition`；
`tsc --noEmit` 零错误。

**真机验证清单**：① 点分割线 → 不再出现 Aa/A 蓝块；② 分割线在页面最上方时点它 → 工具条翻到下方完整可见；
③ 点分割线最左/最右端 → 工具条完整贴住安全边距、不被裁；④ 点分割线中部 → 仍居中于点击点上方；
⑤ 切换到虚线/波浪、删除均正常；⑥ 插入分割线后光标在下方空段落，可直接接着打字。

**续（用户确认「一并修」）：Aa / A 按钮已按块类型隐藏（根治）**
- 新增 `canApplyInlineStyles(editor)`（EditorApp.tsx，放在 `FontSizeButton` 之前）：
  取 `editor.getTextCursorPosition().block` → `editor.schema.blockSpecs[type].config.content`，
  **`content === "inline"` 才返回 true**；取不到光标/schema 时返回 true（宁可多显示，不误藏）。
- `FontSizeButton` / `TextColorButton` 开头 `if (!canApplyInlineStyles(editor)) return null;`。
- **判据口径已用源码实锤核对**（`@blocknote/core/dist/blocks-CiuUGa1E.js`）：
  `divider` / `image` / `pageBreak` = `content:"none"`（各 6 处 none），
  段落/标题/引用/列表 = `"inline"`，`table` = `"table"`。
  且**官方自身就用同一判据**（`blocks-CiuUGa1E.js` 的 keyboardShortcuts：
  `schema.blockSchema[t.block.type].content==="inline"` 才执行更新）——
  说明这不是自创规则，而是与库同源的正确口径，比按块类型名硬编码白名单更稳。
  ⚠️ 注意 `schema.blockSpecs[type].config.content`（spec 层）与
  `schema.blockSchema[type].content`（PM 节点层）都能读到，本次用前者（spec 层，更贴近声明）。
- 产物重建 + 核验：`bn-src` 由 `55c33ffbbdce` → **`f80b181f7002`**（10 个文件，校验 [OK]）；
  产物末尾 `1185324` 处即 `canApplyInlineStyles` 压缩后实现；`tsc --noEmit` 零错误。

**已提交 `abcef2a5`（本地，未 push）**：3 files changed, +216 −83
（EditorApp.tsx / dividerBlock.tsx / editor.html）；pre-commit 产物校验通过。
入库断言（`git show HEAD:<path>`）：EditorApp 含 `canApplyInlineStyles`×3、
`content === "inline"`、`setTextCursorPosition(tail, "start")`、`insertDivider: ${e.message}`；
dividerBlock 含 `TOOLBAR_SAFE_GAP`×5、`useLayoutEffect`×4、`getBoundingClientRect`、
`visibility: pos ? "visible" : "hidden"`；editor.html 含 `bn-src:f80b181f7002`、
`content)==="inline"`、`Math.min(f,s-Ru-a.width)`。另核对磁盘三文件与 HEAD 逐字节相等。

【本次踩坑·工具层】断言脚本里我多写了一条"EditorApp.tsx 应含 `useLayoutEffect`"——
该 hook 实际只用在 `dividerBlock.tsx`，导致出现假 MISS。教训：**断言清单要按文件分别写对**，
MISS 出现时先怀疑断言本身，用 `git show | 计数` 交叉验证，不要据此回改正确代码。

## 核实：官方内置行内按钮在 content:"none" 块上是库级缺口

**结论（源码级证据）**：`content:"none"` 的块（分割线/图片/视频/音频/文件/分页符）在
`nodeToBlock` 后 block 对象的 `content` **是 `undefined`**（`blocks-CiuUGa1E.js` 函数 `M(e,t)`：
`if(f.content==="inline")m=j(...); else if(f.content==="table")m=tt(...); else if(f.content==="plain")m=[...];
else if(f.content==="none")m=void 0; else throw`）。**块类型名与 content 形态是两回事**。

**官方判据**：`@blocknote/react` 的 bold/italic/underline/strike（styleButton `Z`）、
`colorStyleButton`（`Pn`）、`createLinkButton`（`Rn`）、`nestBlockButton`（`Un`）、
`unnestBlockButton`（`Wn`）统一用
`(editor.getSelection()?.blocks || [editor.getTextCursorPosition().block]).find(b => b.content !== void 0)`
——对 `content:"none"` 的块而言 `b.content === undefined` → `find` 得 undefined → 返回 undefined → 不渲染。
即**官方这道防线本来就为"无文本块"设计**，divider 也覆盖到了（先前"字符串 'none' ≠ undefined"的猜测是错的，
未证实前不要写进结论）。

**仍属缺口的只有**：`textAlignLeft/Center/Right`（走 PM `setTextAlignment`，`content:"none"` 上无处可设）
与 `blockTypeSelect`（把段落/标题/引用换成 divider 会让按钮自身失去落点）——两者判据不含 content 检查。

**顺带确认的产物时戳语义**：editor.html 里的 `aie="YYYY-MM-DD HH:mm:ss <git描述>"` 是**构建时戳**
（构建脚本注入），与 `bn-src:<hash>`（源码哈希）相互独立。同一份源码重建 → `bn-src` 相同、
时戳必然不同（例：HEAD 为 `13:58:06 03ebfeea-dirty`，磁盘为 `14:03:13 abcef2a5`，
diff 仅此一处，149676 vs 149670 字节）。**产物 diff 只差时戳时不要重复提交**——
无意义且会来回抖动；判断依据是 `bn-src` 是否变化，不是字节是否相等。

