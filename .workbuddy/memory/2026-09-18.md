# 2026-09-18

---

## 留白间隙收紧 6 → 0（v1.11.2）

用户要求「改小到 0（再挤一点）」。

**改动**：`SIDE_MENU_GUTTER_GAP` 6 → 0，于是 `padding-inline-start = 24 + 0 = 24px`，
**⋮⋮ 手柄紧贴编辑区左边缘**（手柄左边缘 = 24 − 24 = 0）。

**先验证了安全性再动手**（这是关键）：
- 嵌套列表竖线在 `left: -20px` → 需 20px，24 > 20 ✓
- toggle 添加按钮 `margin-left: 22px` → 需 22px，24 > 22 ✓
- → **两者仍不被裁**，所以收紧到 0 无副作用。

⚠️ **24px 是该值的下限**：手柄本身已占 24px，而上面两个内置控件各需 20/22px，
故间隙只能为 0、不能为负。这条已写进 editor.css、MEMORY.md 与方案文档。

**效果**：内容可用宽度 `W − 30` → `W − 24`（再净增 6px）。
对比最初官方 `W − 108`，累计净增 **84px**。

**位置对照**（手柄距编辑区左边缘）：

| 版本 | 菜单宽 | 左留白 | 手柄左边缘 |
|---|---|---|---|
| 官方默认 | 48px | 54px | 6px |
| v1.11 | 24px | 30px | 6px |
| **v1.11.2（当前）** | 24px | 24px | **0px** |

> 有趣的是 v1.11 的 6px 与官方完全一致（因为间隙常量就是照官方「48+6=54」里的 6 取的），
> 所以上一轮真机上会看到"视觉位置没变、内容变宽了"。本轮收紧后手柄才真正贴边。

**同步更新**：`EditorApp.tsx`（常量 + KDoc）、`editor.css`（fallback 30→24 + 说明）、
`docs/bridge-protocol.md`、`docs/BlockNote编辑器占满与背景色修复方案.md`（§3.8 + 改动清单）、
`.workbuddy/memory/MEMORY.md`。

**校验**：`npm run typecheck` exit 0 ✅；产物重建 1,884.54 kB ✅；
关键字核验：`.bn-editor{padding-inline-start:var(--bn-side-menu-gutter, 24px)!important;padding-inline-end:0!important}`
（fallback 已同步）✅。

---

## 内容区留白改为左右对称（v1.11.3）

用户提问：文本左右边缘到屏幕的距离各是多少？并要求把右侧设成与左侧相等。

### 核算结果（改动前）

| 侧 | 链路 | 合计 |
|---|---|---|
| 左 | 宿主内容区 Column `padding(horizontal = 8.dp)` + `.editor-page` 0 + `.bn-root` 0 + `.bn-editor` `padding-inline-start: 24px` | **32dp** |
| 右 | 宿主 8dp + `.editor-page` 0 + `.bn-root` 0 + `.bn-editor` `padding-inline-end: 0` | **8dp** |

差 24dp。**相差的正好是 v1.11.2 让右侧归零的那 24px**。

### 改动

`.bn-editor` 的 padding 由 start/end 分开写改为**左右同值**：

```css
.bn-editor {
  padding-inline: var(--bn-editor-gutter, 24px) !important;
}
```

于是左右均 = 8dp（宿主）+ 24px = **32dp** ✓ 对称。

**顺带把 CSS 变量改名** `--bn-side-menu-gutter` → **`--bn-editor-gutter`**：
它现在同时控制左右两侧，而右侧与"侧边菜单"毫无关系，旧名会误导。
同步更新 EditorApp.tsx / editor.css / bridge-protocol.md / 方案文档 / MEMORY.md。

**新增常量** `EDITOR_CONTENT_GUTTER = SIDE_MENU_HANDLE_WIDTH + SIDE_MENU_GUTTER_GAP`：
- `SIDE_MENU_HANDLE_WIDTH = 24`（手柄物理宽度，事实）
- `SIDE_MENU_GUTTER_GAP = 0`（手柄与文本间隙）
- 两者相加 = 内容留白 24

⚠️ 已在 KDoc 里写明**该值同时受两个语义支配**：左侧"容纳手柄"（硬约束）、
右侧"视觉对称"（用户要求）。若日后冲突（想单独调右侧），
拆成 `-start` / `-end` 两个变量即可。

### 效果

| | 内容可用宽度 | 左/右留白 |
|---|---|---|
| 官方默认 | W − 108 | 54 / 54（对称但都过大） |
| v1.11 | W − 30 | 30 / 0 |
| v1.11.2 | W − 24 | 24 / 0（最满，但不对称） |
| **v1.11.3（当前）** | **W − 48** | **24 / 24（对称）** |

相对官方仍净增 60px；相比 v1.11.2 **主动让出右侧 24px** 换取视觉对称。

### 校验

- `npm run typecheck` exit 0 ✅
- 产物重建 1,884.50 kB ✅
- 关键字核验：`--bn-editor-gutter` ×2、`--bn-side-menu-gutter` **×0**（旧名已清）、
  `.bn-editor{padding-inline:var(--bn-editor-gutter, 24px)!important}` ✅
  另注意产物里还有一条 `.bn-editor{padding:0}`（来自 BlockNote 官方 CSS），
  被我们的 `!important` 覆盖，无影响。
- ⚠️ 未编译 App，待真机验证。

---

## 排查「右边边距看着大」+ 修复背景色变量被覆盖（v1.11.4）

### 用户问题：为什么感觉右边边距大？

**没有目测、没有推算，直接逐像素分析用户上传的截图**（pillow 逐行/逐列扫描深色像素）：

| 元素 | 左边缘 | 右边缘 | 左距 | 右距 |
|---|---|---|---|---|
| 正文行 | 73–88px | 757–771px | 77px | 94px |
| 标题行 | 21px | — | 8.7dp | — |
| 日期行 | 20px | — | 8.3dp | — |

**结论**：右侧确实多 ~17px。但 **CSS padding 是严格对称的**（已用产物逐条核过
`.bn-editor{padding-inline:var(--bn-editor-gutter, 24px)!important}` 唯一生效，
官方 `.bn-editor{padding:0}` 无 `!important` 且在前 → 不生效）。

**真因**：这行文字**没有填满可用宽度**（差约 7dp）。左对齐文本的剩余空间必然落在右侧，
所以视觉空隙 = 32dp（对称内边距）+ 7dp（未填满）= 39dp。

**交叉验证**：由正文左距 77px ↔ 理论值 32dp 反推出 屏幕 ≈ 360dp、密度 2.4，
与整屏 864px 自洽 ✓ —— 这让结论可信度大幅提升。

> **概念澄清**（值得记住）：**"容器内边距对称" ≠ "文字两侧视觉空隙对称"**。
> 只要文字不满一行，后者必然不对称。用户要的前者已经达成。

### 顺带发现的真 bug：`.bn-editor` 背景其实是纯白

分析截图时发现正文位置有 **830×128 的纯白 `(255,255,255)`** 矩形，而主题背景是
暖米色 `(255,251,245)`。它无圆角、宽度等于 WebView 宽、高度等于内容高 → 就是 `.bn-editor` 自身。

**根因**：BlockNote 官方在 **`.bn-root`** 上定义了整套颜色变量
（`--bn-colors-editor-background: #fff`，暗色分支 `#1f1f1f`），
而 v1.9 把同名变量设在 `<html>` 上 → **CSS 变量就近取值**，`.bn-root` 更近、胜出。
我们 CSS 里那个 `var(--bn-colors-editor-background, var(--editor-bg))` 的 fallback
**永远不执行**（前一变量总有值）。

**为何长期没发现**：`.bn-editor` 高度只由内容决定，白条又被 `html/body` 与 `.editor-page`
的米色包围，容易被当成"输入区域的强调"。且 v1.9 的验证只做了「产物含 #FFFBF5 字符串」计数。

**修复（v1.11.4，双保险）**：
- CSS：`.bn-root, .bn-root[data-color-scheme="dark"] { --bn-colors-editor-background: var(--editor-bg, #ffffff) !important }`
  （**必须含暗色分支**，官方对暗色单独定义过）
- JS：改为 `document.querySelectorAll(".bn-root").forEach(el => el.style.setProperty(k, v, "important"))`
  （不再写无效的 `<html>`）

### padding 的决策过程（最终未改）

用户先要求「去掉两侧 padding」，但 **`.bn-editor` 左侧那 24px 是手柄的落脚区**
（手柄左边缘 = padding-left − 24），去掉则手柄完全出视口 —— 即 v1.9 的回归。
说明冲突后用户改选「去掉宿主 8dp」（无副作用）。

但实施前实测发现：**那 8dp 是作用在整个内容区上的，标题/日期行只有它没有编辑器的 24px**
（标题 8.7dp、正文 30.3dp，本来就差 24dp 未对齐）。去掉 8dp 会让标题**贴到屏幕边缘 0dp**。
告知后用户选择**不改了** → 本轮 padding 零改动。

> 副产品：量化了「标题 8dp / 正文 32dp 不对齐」这个**既有**不一致，已记入文档待议。

---

## 拖拽手柄删除 + 块移动改按钮化（v1.11.5）

### 用户问题：按住 ⋮⋮ 为什么无法拖拽？

**排查结论：平台级限制，不是我们的 bug，且无法绕过。**

证据链（三层）：
1. **BlockNote 源码**（`SideMenu.ts` 802 行）：块拖拽**纯用 HTML5 原生 Drag & Drop**
   （`draggable` + `dragstart`/`dragover`/`drop`/`dragend` + `dataTransfer`），
   **零 touch / pointer 事件处理**——没有任何触摸兜底。
2. **平台规范**：W3C 把 drag 事件定义为**鼠标驱动**行为。Android WebView、
   Chrome for Android、iOS Safari 在触摸下**根本不派发这些事件**
   （`draggable="true"` 与否无关，`dataTransfer` 在触摸上下文也是空操作）。
   多个来源一致：这不是兼容性问题，是浏览器没实现。
3. **旁证**：OpenProject 团队评估 BlockNote 的文档明确记录
   *"Blocknote has some features that have to be disabled in mobile (drag-and-drop, toolbar)"*。

**重要推论**：v1.11 保留手柄的理由（"它承担拖拽重排"）**在 Android 上根本不成立**。
我们为一个不可用的功能保留了 UI + 占用了左侧 24px，还让用户困惑了一轮
（之前没暴露是因为手柄本身被裁在屏幕外看不见）。

### 决策（用户）：删除手柄 + 「上移/下移」按钮替代

排查中确认 BlockNote 提供程序化 API：
`editor.moveBlocksUp(blockIdentifier?)` / `moveBlocksDown(blockIdentifier?)`。

| 设计决策 | 理由 |
|---|---|
| 不置灰 | BlockNote 内部 `getMoveUpPlacement` 到顶返回 undefined 直接 return，是**安全 no-op**；但 API 未暴露"能否移动"的判定，点了没反应即代表已在边界 |
| 不关菜单 | 便于连续点击把块一路移上去（与色板行的"保持打开"一致） |
| 不传参 | BlockNote 会取**选区首/末块**或**光标块**，天然支持多选一起移动与嵌套块 |

### 改动清单

**JS 侧**（EditorApp.tsx / bridge.ts / editor.css）：
- 删除：`SIDE_MENU_HANDLE_WIDTH` / `SIDE_MENU_GUTTER_GAP` / `NoDragHandleMenu` /
  `DragHandleOnlySideMenu` / `<SideMenuController>` / 相关 import（含 `type FC`）
- `EDITOR_CONTENT_GUTTER` = 24 → **20**（不再是"手柄宽+间隙"，语义变为
  "嵌套列表缩进线的下限"）；CSS fallback 同步 24 → 20
- 新增下行命令 `moveBlockUp` / `moveBlockDown`（handler 直接调 `ed.moveBlocksUp/Down()`）
- `sideMenu={false}` 保留，`<SideMenuController>` 移除

**Kotlin 侧**：
- controller：新增 `moveBlockUp()` / `moveBlockDown()`
- 工具栏：`BlockOpsMenuButton` 增「上移 / 下移」两个菜单项（紧跟"删除块"，
  因为同样与块的位置相关；参数透传三层：BottomBar → Screen → controller）

### 校验
- `npm run typecheck` exit 0 ✅
- 产物重建 1,902,290 bytes ✅
- 关键字核验：`moveBlockUp`/`moveBlockDown` ×2、`--bn-editor-gutter` ×2
  （fallback 已是 20px）、`DragHandleOnlySideMenu` ×0、`bn-side-menu-gutter` ×0
  （`bn-drag-handle` ×2 是官方 CSS 类名，库自带样式，正常）✅
- 4 个 Kotlin 文件配平差分检查（与 HEAD 对比）全 OK ✅
- ⚠️ 未编译 App，待真机验证

### 真机验证要点
1. ⋮⋮ 手柄**不再显示**（左右两侧均无手柄）
2. 工具栏「块操作」菜单出现「上移」「下移」，点击后块真实移动（多选时整体移动）
3. 嵌套列表左侧**竖向缩进线仍可见**（20px 下限的临界验证）
4. 文本两侧留白左右对称（各 28dp = 宿主 8dp + 编辑器 20px）

---

## 回答「内部编辑区初始高度」+ 修复点击死区（v1.11.6）

### 用户问题：BlockNote 内部编辑区初始高度是多少？

**必须区分两个高度，否则会答错**：

| 层 | 高度 | 依据 |
|---|---|---|
| WebView（宿主） | 屏高 × 62%（用户屏 ≈ 800dp → **496dp**） | `Modifier.heightIn(min = screenHeightDp * 0.62f)` |
| `.bn-editor`（内部编辑区） | **≈ 30dp**（1 个空块） | editor.css **无任何 height/min-height 规则**，纯内容撑 |

`.bn-editor` 那 30dp 的构成：16px 字号 × `line-height: 1.5` = 24px，
加 `.bn-block-content { padding: 3px 0 }` 上下各 3px → 30px。

**实测佐证**（用上一张截图）：白色编辑区 = 2 个块，高 128px 物理 ≈ 53dp
→ 约 26.5dp/块，与理论 30dp 同量级（差异来自字号/行高实际取值）。

### 由此暴露的体验问题（用户选了修复）

WebView 496dp 但编辑区只有 30dp → **中间约 466dp 是死区**：
点击那片空白**不会聚焦光标**，因为 `contenteditable` 只在 `.bn-editor` 这个 30dp 盒子里。

（项目 v2026-09-17 的注释其实已记录过这个现象：「内容少时塌成几行，下方大片空白
其实不属于编辑器」，当时为不动外层 `verticalScroll` 而选了 `heightIn(min)` 保底，
记作"方案 B 的已知取舍"。本轮把"点击死区"这个残留副作用补掉了。）

### 修复：宿主下发高度（不用 vh）

| 层 | 改动 |
|---|---|
| bridge.ts | 新增下行 `{ type: "setEditorMinHeight"; height: number }`（单位 dp） |
| EditorApp.tsx | 新增 `editorMinHeight` state + case + `EditorCore.minHeight` prop + 写入 `--bn-editor-min-height` |
| editor.css | `.bn-editor { min-height: var(--bn-editor-min-height, 0px) !important }`（缺省 0 向后兼容） |
| controller | `setEditorMinHeight(heightDp: Float)` |
| Screen | `LaunchedEffect(editorMinHeight) { controller.setEditorMinHeight(editorMinHeight.value) }` |

**为何不用 `62vh`**：本项目 WebView 高度随内容增长、可能撑出屏幕（外层 Column 滚动），
`vh` 是相对视口的单位，此时语义不直观；宿主下发确定值才可预测。

### 顺带补的上轮疏漏
v1.11.5 只在 bridge-protocol.md 的**版本记录**里写了 moveBlockUp/Down，
**漏了更新「下行消息表」**。本轮一并补上（含 setEditorMinHeight 三行）。

### 校验
- `npm run typecheck` exit 0 ✅
- 产物重建 1,902,481 bytes ✅
- 关键字核验：`--bn-editor-min-height` ×2、`setEditorMinHeight` ×1
  （类型定义编译后消失，只剩 handler 的 case，符合预期）、
  `.bn-editor{min-height:var(--bn-editor-min-height, 0px)!important}` ✅、
  JS 注入 `"--bn-editor-min-height":"${e.minHeight}px"` ✅
- 2 个 Kotlin 文件配平差分 OK ✅
- ⚠️ 未编译 App，待真机验证

### 真机验证要点
1. 新建/打开空灵感后，点击**正文下方大片空白**应能聚焦并把光标落到最后一行
2. 编辑区（背景色区域）应铺满到接近 WebView 底部，不再只有顶部一条
3. 内容很多时仍正常滚动（min-height 不封顶）

---

## min-height 修复未生效 → 加诊断通道（v1.11.7）

### 用户反馈
上一轮（19bef00）改了「宿主下发 min-height」后，用户重新编译验证：
**空灵感下点击正文下方大片空白仍然不能聚焦**。

### 决策：停止猜测，加埋点

按项目教训「连续 2 次猜测→改码→失败后，停止猜测、加埋点取真实数据」，
本轮**不改业务逻辑**，只加一条诊断通道把 WebView 内的运行时真值回传宿主。

**新增上行消息 `diagnostic`（v1.11.7）**：仅用于 logcat，不参与业务逻辑。
与 `error` 的区别：error 是异常（宿主可能提示用户），diagnostic 是观测值（只打 log）。

存在理由：WebView 内的 CSS 变量是否注入、computed style 是什么、元素实际高度多少，
在 Kotlin 侧**完全无法观测**，以往只能靠猜——这正是本轮踩坑的根源。

### 三处埋点（串起来定位）

| 埋点 | 位置 | 回答的问题 |
|---|---|---|
| `down(setEditorMinHeight)` | Kotlin `sendDown`（**原本已有**） | 命令发出去了吗 |
| `diag \| setEditorMinHeight received: X` | JS 收到后 | 到 JS 了吗、值对吗 |
| `diag \| editor geom \| prop=X computedMinHeight=Y offsetH=Z` | CSS 生效后 | 变量注入了吗、min-height 生效了吗 |

最后一条同时上报三个值，任一环出问题都能一眼看出：
- `prop`：宿主下发、经 React 传到 CSS 变量的值
- `computedMinHeight`：浏览器实际解析的 min-height（变量没注入时是 `0px`）
- `offsetH`：元素实际渲染高度（min-height 没生效时仍是内容高 ≈ 30）

### 过程中的一个失误
初版把 `diagnostic` 误插到 `DownMessage` union 末尾（它是**上行**消息），
会造成语法错误；已修正到 `UpMessage` 末尾。（教训：改 union 类型时先确认是上行还是下行。）

### 改动
- bridge.ts：`UpMessage` 增 `diagnostic`
- EditorApp.tsx：`setEditorMinHeight` case 回报；新增 `useEffect([props.minHeight])`
  在挂载后与高度变化时各回报一次几何信息
- BlockNoteEditorWebView.kt / BlockNoteEditorScreen.kt：增 diagnostic 分支打 log

### 校验
- `npm run typecheck` exit 0 ✅
- 产物重建 1,885.33 kB ✅
- 关键字核验：diagnostic ×2、setEditorMinHeight received ×1、editor geom ×1、
  computedMinHeight ×1、--bn-editor-min-height ×2 ✅
- 2 个 Kotlin 文件配平差分 OK ✅

### 待用户执行
```
adb logcat -s BlockNoteEditor:V | grep -E "diag|down\("
```
根据三条日志的出现情况决定下一步：
- 缺 `down(setEditorMinHeight)` → Kotlin 侧没发（查 `enqueueCommand` 的
  `ready && !initSent` 丢弃窗口与 `LaunchedEffect` 时序）
- 有 down 但缺 received → evaluateJavascript 未送达 / JS 未绑定宿主
- 有 received 但 computedMinHeight=0px → CSS 变量没注入（查 `.editor-page` 的 inline style）
- computedMinHeight 正确但 offsetH 仍 ≈30 → min-height 被覆盖（查是否有更高优先级规则）
- **三者都正常但仍不聚焦** → 说明 min-height 思路不对，需要改走
  「JS 监听空白点击 → 主动聚焦文档末尾」的兜底方案

---

## ✅ 结论：min-height 修复**确实生效**，此前是误判

用户抓到日志后确认：**可以聚焦，是之前看错了**。上一轮（19bef00）的修复是有效的。

### 决定性日志（实测）

```
down(setEditorMinHeight)
diag | setEditorMinHeight received: 541.260009765625
diag | editor geom | prop=541.260009765625px computedMinHeight=541.26px offsetH=541
```

三个环节全通：
- Kotlin 发出 ✓
- JS 收到且值正确（541.26 dp = 屏高 873 × 0.62，该机型屏高偏大）✓
- CSS 变量注入、`computedMinHeight=541.26px`、**`offsetH=541` 证明元素实际高度已铺满** ✓

→ **"点击正文下方空白不能聚焦"这个问题已解决**，不需要再做兜底方案
（此前构思的"JS 监听空白点击主动聚焦文档末尾"作废，未实现）。

### 关键教训：不要凭一次主观观感否定已验证的修复

本次我一度准备基于"用户说还是不行"去改实现方向（怀疑 ProseMirror 不建 selection、
准备加点击兜底）。**加埋点后数据立刻表明实现完全正确**，改动只会引入新复杂度。
这与「产物里有字符串 ≠ 运行时生效」是同一类问题：**主观观感 ≠ 实测数据**。

### diagnostic 通道（v1.11.7）保留

该通道本次直接证明了修复有效，价值已验证，予以保留：
- `UpMessage.diagnostic`：JS 主动回传运行时观测值，宿主只打 log、不做 UI 反应
- 与 `error` 区分：error=异常（宿主可能提示），diagnostic=观测值（只打 log）
- 用途：WebView 内的 CSS 变量/computed style/元素实际高度，在 Kotlin 侧无法直接观测

排查命令（**PowerShell**）：
```powershell
adb logcat -c
# 打开 App 操作
adb logcat -d -s BlockNoteEditor:V | Select-String -Pattern 'diag|down\('
```

### 当前编辑区几何（该机型）
- WebView 最小高度 = 屏高 × 62% = **541dp**
- `.bn-editor` 实测 offsetH = **541px**，已铺满 WebView
- 左右留白各 28dp（宿主 8dp + 编辑器 20px），对称

---

## 方案 A（编辑区高度 = 视口真实剩余高度）单独评估

用户要求对当初搁置的"方案 A"做单独评估（当初因"会波及两条链路"而选了 62% 经验值）。

### 核心发现：两条"拦路虎"都是死代码

| 当初担心的影响 | 核查结果 |
|---|---|
| 图片画廊 inset（`editorViewportBounds`） | ❌ 不存在：全项目**只被赋值、无读取方**（290 声明 / 1560 赋值，注释称用于"图片工具栏 clamp"，但 UI 已随 BodyBlocksEditor 下线） |
| 滚动清除选中态（`clearBlockSelection()`） | ❌ 无实际效果：它清的 `highlightedBlockId` / `highlightedTapX` / `imageToolbarBlockId` **无 UI 消费方**（引用全在 BodyBlocksController 内部） |

→ 方案 A 的成本远低于当初估计。

### 其他核查
- 内容区 Column **只有 4 个子元素**：标题 / Spacer 8dp / 日期行 / WebView
- `contentScrollState` 全文仅 4 处引用（声明 + LaunchedEffect + verticalScroll）
- 实测：用户机屏高 873dp，62% = 541dp，`.bn-editor` offsetH = 541（诊断日志确认）

### 方案 A 改动（很小）
- Column 去掉 `.verticalScroll()`；WebView 改 `Modifier.weight(1f)`
- min-height 下发值从"62% 屏高"改为"WebView 实测高度"（`onSizeChanged` 测量，
  复用 v1.11.6 既有通道，无需改协议）
- 顺带清理 `contentScrollState`、`editorViewportBounds` 两处死代码

### 收益 / 风险
- 收益：高度精确、键盘弹出自适应、消除嵌套滚动、布局更标准（标题固定+正文滚动）
- 风险：①**标题与日期行将固定不滚**（UX 变化，最需用户拍板）
  ②首帧 min-height 未测到时可能闪烁 ③WebView 内滚动+软键盘需验证

### 产出
新建 `docs/编辑区高度方案A评估.md`（现状 / 改动 / 核查详情 / 收益风险 / 三方案对比 / 建议）。
结论：**推荐实施方案 A**，待用户确认"标题固定"是否可接受。

---

## 实施方案 A：编辑区高度 = 视口真实剩余高度（v1.11.8）

用户确认后分两步落地。

### 步骤 1：死代码清理（d070cad6）

- 删除 `editorViewportBounds`（声明 + `onGloballyPositioned` 赋值 + 注释）
- 连带删除 3 个因此孤立的 import：
  `geometry.Rect` / `layout.boundsInWindow` / `layout.onGloballyPositioned`

> 该状态注释称用于"图片选中工具栏的垂直 clamp"，但 UI 已随 Compose 版
> BodyBlocksEditor 下线，状态残留至今，全项目无读取方。

### 步骤 2：方案 A（4f320648）

```diff
 Column(
     .fillMaxSize()
     .padding(innerPadding)
     .background(contentBackgroundPaint)
     .padding(horizontal = 8.dp)
-    .verticalScroll(contentScrollState),
+    ,
 ) {
     RichTextEditor(标题)      // 固定
     Spacer(8.dp)              // 固定
     Row(日期 + 字数)          // 固定
     BlockNoteEditorWebView(
-        .heightIn(min = 屏高 × 0.62f)
+        .weight(1f)
+        .onSizeChanged { size ->
+            val hDp = with(density) { size.height.toDp() }
+            blockNoteController.setEditorMinHeight(hDp.value)
+        }
     )
 }
```

- 删除 `contentScrollState` + 其 LaunchedEffect（滚动清除选中态）
- 删除 `editorMinHeight`（62% 屏高）与 `LocalConfiguration`
- import 调整：删 `rememberScrollState`/`verticalScroll`/`LocalConfiguration`，
  加 `layout.onSizeChanged`（`weight` 此前已 import）

**关于首帧闪烁**：`onSizeChanged` 在布局后触发，远早于 JS ready，命令先进
pendingCommands 待 init 后发出 → 不会"先矮后高"。

**只改 Kotlin，JS 产物无需重建** ✅

### 校验
- Kotlin 配平差分 HEAD/NOW 一致 ✅
- 符号检查：weight / onSizeChanged / density 均在作用域，import 正确 ✅
- ⚠️ 未编译 App，待真机验证

### 真机验证要点（也写进了 commit message）
1. 编辑区高度是否 = 视口剩余高度（不再依赖 62%）
2. **标题与日期行现在固定不随正文滚动** ← 需用户确认的 UX 变化
3. 软键盘弹出时编辑区是否收缩（取决于 innerPadding 是否含 ime；
   若不含则不会收缩，需另行处理——这是我发现的潜在遗留风险）
4. 内容超出时 WebView 内部滚动是否正常
5. 旋转屏后高度是否正确重算

---

## 键盘弹出 WebView 不收缩 → 加埋点（v1.11.9，待真机数据）

### 用户反馈
方案 A 落地后（用户两张截图）：在正文光标处点击，**键盘与一大片空白同时弹出**，
WebView 没有向上收缩，光标不可见。这正是实施前预警的风险点 3。

### 静态排查（本轮先查事实、未改逻辑）

| 事实 | 结论 |
|---|---|
| `windowSoftInputMode="adjustResize"` | ✅ 已配置 |
| `MainActivity` 调用 `enableEdgeToEdge()` | ✅ 已确认 |
| `imePadding()` **从未被调用**（只有孤立 import） | ❌ 无人消费 ime |
| Scaffold `innerPadding` 默认只含 systemBars（不含 ime） | ❌ 不含 ime |

**初步假设（待埋点证实）**：edge-to-edge 下 `adjustResize` 不再 resize 窗口，
改为派发 ime insets 交由应用消费；无人消费 → 布局不动 → 键盘直接盖住 WebView。

### 埋点（按用户要求只加埋点、不改逻辑）

Kotlin（tag=BlockNoteEditor）：
1. `diag | ime bottom=Xpx (Ydp)` —— WindowInsets.ime 变化即打印
2. `diag | innerPadding: ...` —— Scaffold 分发的 PaddingValues
3. `diag | webview onSizeChanged: WxHpx = Hdp` —— WebView 实际分配高度

JS（经 diagnostic 通道）：
4. `diag | viewport | bnEditor=Xpx innerHeight=Y visualViewportH=Z`
   - `visualViewport.height` 是**关键指标**：键盘弹出且系统真正 resize 时会变小；
     保持不变 = 窗口没被 resize（edge-to-edge 需应用自行消费 ime）

JS 改动已重建产物（1,885.87 kB）✅；Kotlin 仅加 Log 与 2 个 import。

### 待用户执行
```powershell
adb logcat -c
# 打开 App → 进入灵感 → 点击正文末尾让键盘弹出
adb logcat -d -s BlockNoteEditor:V | Select-String -Pattern 'diag'
```
关注键盘弹出前后 ime / innerPadding / onSizeChanged / viewport 四组数值的变化，
据以定位收缩失败的环节（insets 未派发 / 派发了未消费 / 消费了但 WebView 未跟随）。

---

## 修复键盘弹出视口坍缩（v1.11.9 正式修复，616dff82）

### 日志判读（用户提供的埋点数据）

| 指标 | 键盘前 | 键盘后 | 判定 |
|---|---|---|---|
| ime insets | 0 | 836px (304dp) | ✅ 正常派发 |
| innerPadding | 打印一次 | 从未变化 | ✅ Scaffold 不含 ime（符合预期） |
| WebView onSizeChanged | 620dp | 坍缩到 332dp 且震荡 | ⚠️ 异常 |
| JS innerHeight | 620 | **坍缩到 28px**（≈一行高） | ❌ 失控 |
| bnEditor | 1680px | 恒定 | ✅ 内容高正确 |

### 根因：自造的正反馈循环

键盘弹出 → BottomBar 面板接管键盘位（项目既有设计，WebView 620→332 属正确收缩）
→ **onSizeChanged 把每次收缩值都经 setEditorMinHeight 写入 .bn-editor { min-height }**
→ 与 Android WebView 自身的键盘视口避让互相激发
→ innerHeight 620→28（视口只剩一行高）→ onSizeChanged 再触发 → ……

视觉表现即用户截图的「大片空白 + 光标不可见」。

**教训：min-height 跟随 WebView 实测高度"在键盘场景下是致命设计"——
它把每次压缩固化为新的 min-height。测量回写布局的闭环必须考虑键盘场景。**

### 修法：键盘弹出期间冻结 min-height

onSizeChanged 仅在 ime == 0 时下发 setEditorMinHeight：

- 长文档不受影响（内容 1680 > min-height，照常滚动）
- 空文档在压缩后视口里变为可滚动，光标可见
- 键盘收起后自然恢复

实现：`mutableStateOf` 存 ime 底边（onSizeChanged 是普通回调，
捕获普通局部变量会是旧值，读 state 才最新）。⚠️ mutableStateOf 用 `.value`
不是 `.intValue`（后者属 mutableIntStateOf）——本次踩了又即时修正。

### 待办
- 用户真机验证：键盘弹出/收起，编辑区应稳定收缩到 ~316dp 且光标可见
- 验证通过后移除临时埋点（ime/innerPadding/onSizeChanged/viewport 四组 diag 日志）
