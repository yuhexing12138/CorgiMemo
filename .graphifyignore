# ============================================================
# CorgiMemo · graphify 扫描排除规则
# ------------------------------------------------------------
# 本文件语法与 .gitignore 完全一致（last-match-wins）。
# graphify 会同时读取本文件与 .gitignore，
# 因此下面只列出「仅与 graphify 扫描相关、不适合放进 .gitignore」的规则。
#
# 📌 关键区别：
#   .gitignore     —— 控制 Git 提交范围
#   .graphifyignore —— 仅控制 graphify 的扫描范围
# 把子模块源码从图谱里排除，但仍然保留为 Git 子模块，就是这个文件的作用。
# ============================================================

# ------------------------------------------------------------
# 1. Git 子模块源码（4 个，不应混入项目图谱）
# ------------------------------------------------------------
/graphify/
/compose-rich-editor/
/Reorderable/
/tyme4kt/

# 子模块的 .bak 备份目录（防止误扫）
/compose-rich-editor.bak/
/Reorderable.bak/
/tyme4kt.bak/

# ------------------------------------------------------------
# 2. Android / Gradle 构建产物与 IDE 临时文件
# ------------------------------------------------------------
**/.gradle/
**/build/
**/.idea/
**/*.iml
**/local.properties
**/captures/

# ------------------------------------------------------------
# 3. 仓库根目录的二进制与临时文件
# ------------------------------------------------------------
/icons.aar
/icons.zip
/icons_extracted/
*.apk
*.aab
*.aar
*.ap_
*.dex
*.class

# ------------------------------------------------------------
# 4. 备份 / 弃用 / 演示目录（与代码图谱无关）
# ------------------------------------------------------------
/弃用文件/
/【刻记+】APP/

# ------------------------------------------------------------
# 5. 个人笔记与报错记录（属于开发者本地资料，不属于项目结构）
# ------------------------------------------------------------
/报错汇总.md
/提示词.md
/初赛demo贴初稿.md
/app崩溃信息收集.md
/log/
/log/**

# ------------------------------------------------------------
# 6. 密钥与本地配置（保险起见再写一次，graphify 也有兜底）
# ------------------------------------------------------------
*.jks
*.keystore
key.properties
local.properties
**/.env
**/.envrc
