# 待办后端数据与前端对应关系报告

> 范围：主程序（Android 原生 Compose + Hilt + Room），不含 Kuikly 详情页（`todoDetail`）。
> 目标：理清「数据库行 → 前端卡片」的完整映射链路，并标注易混淆点。

---

## 1. 总体数据流（一图看懂）

```
┌─────────────────────────┐    Room Flow     ┌──────────────────────┐
│  Room 数据库              │  observeAllSorted │  HomeViewModel        │
│  todo_items 表            │ ───────────────► │  _todos (单一数据源)   │
│  sub_tasks 表             │                  │                       │
│  content_blocks 表        │                  │  4 个 zone StateFlow  │
│  card_relation 表         │                  │  (isPinned×status)    │
└─────────────────────────┘                  │  + 子任务/附件/关联映射 │
        ▲ 写入回流                                └───────────┬───────────┘
        │                                              collectAsState
        │  Repository/Dao                              │
┌───────┴─────────────────┐                  ┌──────────▼───────────┐
│  TodoRepository          │                  │  HomeScreen / MainScreen│
│  SubTaskManager          │                  │  ZonedReorderable      │
│  (增删改查 + 软删除        │                  │  LazyColumn            │
│   + 提醒调度 + 进度计算)    │                  │        │ items()       │
└─────────────────────────┘                  └──────────┬───────────┘
                                                        ▼
                                              TodoListItem（单卡渲染）
                                              + SwipeableTodoBox（左滑层）
```

**核心原则**：`_todos`（一条 `List<TodoItem>`）是 UI 的唯一真相源。数据库任何变更经 Room Flow 重新发射 → `_todos` 更新 → 4 个 zone 与各类映射重算 → Compose 自动重组，**无需手动 `notify`**。

---

## 2. 后端数据模型（持久化层）

### 2.1 `TodoItem` ←→ `todo_items` 表
文件：`app/.../data/model/TodoItem.kt`、`data/local/db/TodoDao.kt`

| 字段 | 类型 | 默认值 | 含义 |
|---|---|---|---|
| `id` | Long (PK, auto) | 0 | 待办唯一 ID |
| `title` | String | — | 标题（非空） |
| `content` | String? | null | 纯文本内容（搜索/摘要/详情页） |
| `contentFormat` | String | "" | 富文本 Markdown 版本（编辑器恢复） |
| `categoryId` | Long | — | 所属分类（外键指向 `categories`） |
| `priority` | Int | — | 优先级 0=无 /1=低 /2=中 /3=高 |
| `status` | Int | — | **0=未完成 / 1=已完成** |
| `dueDate` | Long? | null | 截止时间（毫秒） |
| `reminderTime` | Long? | null | 提醒时间（毫秒） |
| `repeatType` | Int | — | 重复 0=无/1每天/2每周/3每月/4工作日/5每年/6周末 |
| `isPinned` | Boolean | false | 是否置顶 |
| `sortOrder` | Int | 0 | 拖拽排序索引（同分区内有效） |
| `completedAt` | Long? | null | 完成时间戳 |
| `hasSubTasks` | Boolean | false | 是否含子任务（索引用） |
| `backgroundColor` | Int | -1 | 卡片背景色（ARGB，与 Compose Color 兼容） |
| `imagePaths` / `voiceNotePath` | String | "" | **已废弃**（v2026-07-25 重构后由 `content_blocks` 表接管） |

> ⚠️ `status` 是「完成态」不是「优先级」，优先级是单独的 `priority` 字段。

### 2.2 `SubTask` ←→ `sub_tasks` 表
文件：`data/model/SubTask.kt`、`data/local/db/SubTaskDao.kt`

| 字段 | 含义 |
|---|---|
| `id` | 子任务 ID |
| `todoId` | **父待办 ID**（外键，决定归属） |
| `title` | 子任务标题 |
| `isCompleted` | 是否完成 |
| `order` | 排序 |
| `imagePaths` / `voicePaths` | 子任务附件（**已废弃**，同由 `content_blocks` 表按 `subTaskId` 接管） |

### 2.3 附件与关联（独立表，单一数据源）
- 附件：`content_blocks` 表（`sourceType="todo"` 或 `subTaskId` 非空）→ 通过 `HomeViewModel.todoAttachmentsMap` / `subTaskAttachmentsMap` 提供。
- 关联卡片：`card_relation` 表（`sourceType="todo"`, `groupId=0`）→ 通过 `HomeViewModel.relationCountMap` 提供。

---

## 3. 数据访问层

### 3.1 `TodoDao`（Room）
- `observeAllSorted()`：**UI 主入口**，按 `isPinned DESC, sortOrder ASC, createdAt DESC` 返回 `Flow<List<TodoItem>>`。
- 其他查询：`getTodosByStatus`、`getTodosByCategory`、`getTodosByStatusPriorityDueDate`、分页 `getTodosPaging`、过期 `getOverdueTodos`、已完成清理 `deleteOldCompletedTodos` 等。
- 拖拽专用：`updateSortOrder`、`updatePinnedStatus`、`togglePin`、`getMaxSortOrder`。

### 3.2 `TodoRepository`（单例，Hilt 注入）
文件：`data/repository/TodoRepository.kt`
- 增：`insertTodo` / `insertTodos`（写入时若 `reminderTime!=null` 自动调度 `AlarmScheduler`，并广播桌面小部件刷新）。
- 改：`updateTodo` / `updateTodos`（批量更新走单次事务，Room Flow 单次发射 → UI 一次性重组）。
- 删：**软删除**——先写 `DeletedTodoRepository`，再物理删 `todo_items`，并清理 `card_relation`、取消提醒。
- 观察：`observeAllSorted()` 直接透传给 ViewModel。

### 3.3 `SubTaskManager`（object）
文件：`data/repository/SubTaskManager.kt`
- `getSubTasks` / `getSubTasksFlow`：按 `todoId` 取子任务。
- `getProgressText`：返回 `"已完成/总数"`（如 `"2/5"`），无子任务返回 `null`。
- `toggleSubTaskCompletion`：切换子任务；**全部完成时自动把父待办 `status` 置 1**（父任务自动完成）。

---

## 4. ViewModel：后端 → 前端的桥

文件：`viewmodel/HomeViewModel.kt`

### 4.1 单一数据源
```kotlin
private val _todos = MutableStateFlow<List<TodoItem>>(emptyList())   // 唯一真相源
// loadTodos():
todoRepository.observeAllSorted().collect { allTodos ->
    _todos.value = allTodos
    // 同步构建子任务进度/列表映射
    _subTaskProgressMap.value = ...   // id -> "2/5"
    _subTasksMap.value = ...          // id -> List<SubTask>
}
```

### 4.2 四个「待办区」StateFlow（核心前端概念）
`(isPinned, status)` 的笛卡尔积，**仅 UI 概念、不持久化**（定义见 `ui/components/TodoZone.kt`）：

| zone 枚举 | 过滤条件 | 排序 |
|---|---|---|
| `PINNED_PENDING` 置顶待完成 | `isPinned && status==0` | sortOrder |
| `PENDING` 普通待完成 | `!isPinned && status==0` | sortOrder |
| `PINNED_COMPLETED` 置顶已完成 | `isPinned && status==1`（近 30 天） | sortOrder |
| `COMPLETED` 普通已完成 | `!isPinned && status==1`（近 30 天） | sortOrder |

派生方式：`pinnedPendingTodos = _todos.map { it.filter{...}.sortedBy{sortOrder} }.stateIn(...)`。

### 4.3 合并可见列表 `filteredTodos`
`combine(4 个 zone) → baseList`，再叠加：
1. 展开开关（`showCompleted` / `hideCompletedItems`）粗筛；
2. 跨维度过滤（`_selectedFilterItems` + `_filterMode`：分组/置顶/已完成/已过期/重复提醒，OR/AND/NOT 组合）；
3. 搜索（`searchQuery`：匹配 `title` / `content` / `contentFormat`，命中高亮）。

### 4.4 其它派生映射（喂给卡片）
| 映射 | 来源 | 卡片用途 |
|---|---|---|
| `subTaskProgressMap` | `SubTaskManager.getProgressText` | 进度文本 `(2/5)`、进度条 |
| `subTasksMap` | `SubTaskManager.getSubTasks` | 展开后的子任务列表 |
| `todoAttachmentsMap` | `content_blocks` 表 | 图片/语音角标计数 + 预览 |
| `subTaskAttachmentsMap` | `content_blocks`（`subTaskId` 非空） | 子任务自身附件角标 |
| `relationCountMap` | `card_relation` 表 | 关联卡片角标 `🔗×N` |
| `categories` | `CategoryDao` | 分类名/图标 → 分组角标 |

---

## 5. 前端对应：字段 → 卡片显示

组件：`ui/components/TodoListItem.kt`（单卡）、`ui/components/SwipeableTodoBox.kt`（左滑操作层，包在外层）。`HomeScreen` 在 `ZonedReorderableLazyColumn` 的 `items()` 中调用 `TodoListItem`。

| 后端字段 / 数据 | 卡片显示位置 | 显示规则 |
|---|---|---|
| `title` | 标题文本 | 16sp / Medium；单行省略；`status==1` 加删除线 + 灰字；搜索命中逐字符波浪高亮 |
| `status` | 左侧 `CircularCheckbox` | `checked = status==1`；已完成视觉降权（浅橙底+白√） |
| `status` + `completedAt` | 完成时间 | `status==1` 时显示「X 分钟/小时/天前完成」 |
| `isPinned` | 右上角图钉 | `Icons.Outlined.PushPin`；未完成=品牌色，已完成=灰 |
| `priority` | 左侧 4dp 竖条 + 卡片边框/阴影 | 0 透明 /1 柔蓝 /2 柔橙 /3 柔红；已完成用 dim 色 |
| `reminderTime` | 元数据行提醒 | `🔔 时间`；过期变红（`isOverdueTodo`：未完成且提醒或截止已过） |
| `repeatType` | 元数据行循环 | ≠0 时追加「，🔁 每天/每周/…」（来自 `RepeatTaskManager`） |
| `dueDate` | 同上（过期判定） | 与 `reminderTime` 共同决定过期红字 |
| `hasSubTasks` + `sub_tasks` | 展开按钮 + 进度 | 有子任务显示 `(2/5)` 文本 + 进度条；点击展开子任务列表 |
| `sub_tasks[].title` / `isCompleted` | 子任务行 | 14sp，完成加删除线 + 浅橙√；可独立勾选（全部完成触发父任务自动完成） |
| `categoryId` → `category.name` | 左上角分组角标 | 点击弹出改分组弹窗（简化模式隐藏） |
| `content_blocks`(图片) | 图片角标 `🖼×N` | 点击全屏预览（简化模式隐藏） |
| `content_blocks`(语音) | 语音角标 `🎤×N` | 点击全屏录音预览 |
| `card_relation` 数量 | 关联角标 `🔗×N` | 点击弹关联列表 |
| `backgroundColor` | 卡片背景 | 用于高亮背景（非卡片底色，卡片底色恒为 `surface`） |

> 📌 `content` 字段**不在卡片标题/正文区直接显示**（卡片正文是子任务树，由 `sub_tasks` 渲染）。`content` 只在：详情页、搜索高亮、进度条显示条件（`status!=1 && content.isBlank() && 有子任务` 才显示进度条）时参与。

---

## 6. 四个待办区如何渲染到屏幕

`HomeScreen.kt`：
```kotlin
val pinnedPendingTodos by viewModel.pinnedPendingTodos.collectAsState()
val pendingTodos      by viewModel.pendingTodos.collectAsState()
val pinnedCompletedTodos by viewModel.pinnedCompletedTodos.collectAsState()
val completedTodos    by viewModel.completedTodos.collectAsState()

val pendingTodosAll        = pinnedPendingTodos + pendingTodos        // 待办区
val visibleCompletedTodosAll = pinnedCompletedTodos + completedTodos  // 已完成区（默认折叠）
```
- 两个列表喂给 `ZonedReorderableLazyColumn`，每个 `todo` 渲染为一个 `TodoListItem`。
- **跨区拖拽**：`DragZoneStateMachine` 根据落点翻转 `isPinned` / `status`，松手后 `reorderOnDragResult` 重写 `sortOrder`（按 zone 分段 0/10000/20000/30000）并批量 `updateTodos` 持久化。

---

## 7. 写入回流（前端 → 后端）

卡片上的任何操作都走「ViewModel 方法 → Repository → Dao → Room → Flow 重发 → 自动重组」：

| 前端操作 | ViewModel 入口 | 后端副作用 |
|---|---|---|
| 勾选完成 | `toggleTodoStatus(id, checked)` | `status` 切换 + `completedAt` 写入 |
| 勾选子任务 | `toggleSubTaskCompletion(subId)` | 子 `isCompleted` 切换；全完成则父 `status=1` |
| 左滑置顶 | `togglePin(id)` / `updatePinnedStatus` | `isPinned` 切换 |
| 左滑删除 | `deleteTodo(id)` | 软删除（先存最近删除表）+ 清理关联 + 取消提醒 |
| 拖拽排序/跨区 | `reorderOnDragResult(...)` | 重写 `sortOrder`（分区段）+ `isPinned`/`status` |
| 点击卡片 | `navController → TodoEditScreen` | 编辑页经 `TodoEditViewModel` 保存回写 `todo_items` |

---

## 8. 易混淆点小结

1. **「四个区」≠ 四张表**：区是 `(isPinned, status)` 的视图分组，数据全在 `todo_items` 一张表，`TodoZone` 实时计算，不持久化。
2. **父子任务对应**：测试1（父，`hasSubTasks=true`，`todo_items` 一行）与测试2/测试3（`sub_tasks` 两行，`todoId=测试1.id`）。卡片上「父标题=测试1.title」，展开后「子任务=sub_tasks 列表」。
3. **详情页三行文本来源**：你之前看到的详情页内容区「☐ 测试1 ☐ 测试2 ☐ 测试3」来自父待办 `content` 字段本身存了子任务树文本——**不是详情页聚合**，详情页只镜像数据库字段。
4. **附件/关联已迁移**：`TodoItem.imagePaths` 等字段在 v2026-07-25 重构后已被置空，附件/关联的真实数据源是 `content_blocks` / `card_relation` 表。
5. **`status` vs `priority`**：`status`=完成态（0/1），`priority`=优先级（0~3，决定左侧竖条颜色），两者独立。

---

## 9. 关键文件索引

| 层 | 文件 |
|---|---|
| 模型 | `data/model/TodoItem.kt`、`data/model/SubTask.kt` |
| 持久化 | `data/local/db/TodoDao.kt`、`data/local/db/SubTaskDao.kt` |
| 仓库 | `data/repository/TodoRepository.kt`、`data/repository/SubTaskManager.kt`、`data/repository/RepeatTaskManager.kt` |
| ViewModel | `viewmodel/HomeViewModel.kt` |
| 区定义 | `ui/components/TodoZone.kt`、`ui/components/DragZoneStateMachine.kt` |
| 列表容器 | `ui/components/ZonedReorderableLazyColumn.kt`、`ui/screens/home/HomeScreen.kt` |
| 卡片渲染 | `ui/components/TodoListItem.kt`、`ui/components/SwipeableTodoBox.kt` |
| 编辑回写 | `ui/screens/todo/TodoEditScreen.kt`、`viewmodel/TodoEditViewModel.kt` |
